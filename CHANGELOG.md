# CHANGELOG.md

سجل زمني لكل تغيير مهم في المشروع. أحدث تغيير في الأعلى.

> **ملاحظة:** لكل commit فيه تغيير سلوك أو بنية، سطر هنا يوضح إيه اللي اتغير وليه.

---

## 2026-08-30 — 🆕 رسالة الاستئناف التلقائي مخفية عن المحادثة — تظهر في تاب مخصص

### الطلب
إخفاء رسالة الاستئناف التلقائي (#175) من المحادثة، وعرضها في تاب مخصص ببيانات الموظف.

### التصميم
علامة مخفية جديدة (نفس نمط علامة الوقت الحية) تُفلتَر مركزيًا من كل مسارات عرض الأحداث.

### خطأ اتكشف قبل النشر
ترتيب العلامات (الوقت أولًا، ثم الاستئناف) كان يكسر الفحص — اتصلح بفحص بعد إزالة علامة الوقت.

### التنفيذ
تاب جديد "الاستئناف التلقائي" في بيانات الموظف + سجل تخزين + Endpoint جديد.

### التحقق
اختبار حاسم: 3 أحداث خام → رسالتان فقط وصلتا للمتصفح، الثالثة اختفت تمامًا. `npm test` → 82/82.

## 2026-08-30 — 🆕 استئناف تلقائي للمحادثات المتوقفة بسبب حد الاستخدام

### الحادثة
كيرلس توقف بسبب حد استخدام، وفاضلة 4 ساعات — المالك عايز استئناف تلقائي بدون تدخل يدوي.

### المتطلبات
كل الموظفين، بدون حد زمني (الحد ممكن يستغرق أيام)، تنبيه فوري عند الاستئناف.

### التصميم
مبني على أساس موجود بالفعل (#169/#170/#172) + معيار استمرارية العمل الموثَّق مسبقًا للموظفين.

### التنفيذ
`GET /api/internal/resume-stopped-conversations` مربوط بدورة صحة النظام كل 5 دقايق.

### التحقق
3 سيناريوهات (فات الوقت، لسه ما جاش، سليمة) — كلها صحيحة. `npm test` → 82/82.

## 2026-08-30 — 🎯 إصلاح عناوين متطابقة عند استخدام زرار "محادثة جديدة" مرارًا

### التصحيح
المالك صحّح افتراضي — الزرار موجود ومُستخدَم عمدًا (#61)، مش استثناء نادر.

### محاولتان فشلتا حيًا
دقة الدقيقة، ثم الثواني — كلاهما تصادم فعليًا في اختبار حي.

### الحل النهائي
آخر 4 أحرف من UUID المحادثة الحقيقي — ضمان رياضي مطلق.

### التحقق
أسوأ سيناريو (طلبين في نفس الثانية) — عنوانان مختلفان مؤكَّدان. `npm test` → 82/82.

## 2026-08-30 — 🎯🎯 إصلاح عناوين المحادثات الغريبة في واجهة OpenHands

### الاكتشاف
فحص محادثات حقيقية أكّد `title: null` لكل المحادثات، رغم إرسال العنوان صراحة.

### السبب
API OpenHands الحالي (v1.16.0) مفيهوش حقل `title` في طلب الإنشاء خالص — اتغيّر عن إصدارات قديمة.

### الحل
حقل `title` موجود فقط في `PATCH /api/conversations/{id}` — استدعاء منفصل بعد الإنشاء.

### التحقق
تتبّع كل طلب فعلي وصل للخادم — `PATCH` بالعنوان الصحيح مؤكَّد. `npm test` → 82/82.

## 2026-08-30 — 🆕 عداد تنازلي حي للوقت المتبقي في صحة النظام

### الطلب
عداد حقيقي بيتحرك، بدل نص ثابت "حوالي X ساعة" يبقى قديم فورًا.

### التنفيذ
طابع زمني حقيقي (`resetsAt`) يوصل من الخادم للواجهة، مكوّن `LiveCountdown.tsx` يتحدَّث كل ثانية بشكل مستقل.

### التحقق
تأكيد مطابقة الطابع الزمني، اختبار كامل من طرف لطرف، تأكيد عدم تأثر باقي الفحوصات. `npm test` → 82/82.

## 2026-08-30 — 🆕 ترجمة كاملة لكل رسائل صحة النظام لعربي واضح ومفهوم

### الطلب
كل الفحوصات السبعة تكون بنفس وضوح الرسالة المفصَّلة اللي بُنيت في #170.

### التنفيذ
ترجمة منهجية لكل رسالة نجاح/فشل/تخطٍّ عبر الفحوصات السبعة، شامل قاموس ترجمة كامل لأسباب فشل اشتراكات LLM.

### التحقق
اختبار شامل بحالات مختلطة — كل رسالة عربية واضحة، ملف JSON لسه صالح 100%. `npm test` → 82/82.

## 2026-08-30 — 🆕 شرح واضح لأخطاء المحادثات + إصلاح باگ JSON متداخل

### الطلب
شرح مفهوم زي "الاشتراك لسه شغال، هيرجع بعد كذا" بدل كود تقني مختصر.

### خطأ اتكشف واتصلح
Regex بسيط فشل مع JSON متداخل (`{"error": {...}}`). اتصلح بـ`JSON.parse` من أول `{`.

### النتيجة
"الاشتراك لسه شغال ومتصل (خطة plus)، لكن وصل لحد الاستخدام المؤقت. هيرجع تلقائيًا خلال حوالي 2 ساعة و36 دقيقة."

### التحقق
نفس بيانات الحادثة الحقيقية بالضبط. `npm test` → 82/82.

## 2026-08-30 — 🎯🎯 اكتشاف حي: فحص LLM لا يكتشف "حد الاستخدام" — إصلاح بتصنيف OpenHands الرسمي

### الحادثة
المالك واجه `usage_limit_reached` حيًا، بينما صحة النظام عرضت كل شيء ✅.

### السبب
`/api/llm/subscription/*/status` يعرض حالة الاتصال/الصلاحية بس، مش حصة الاستخدام — مفهومان مختلفان.

### الحل
فحص آخر حدث في المحادثة الفعلية — `ConversationErrorEvent` يحمل تصنيفًا رسميًا (`code`, `classification.kind`) من OpenHands نفسها.

### التحقق
إعادة إنتاج الحادثة الحقيقية بالضبط — رسالة واضحة `LLMRateLimitError (rate_limit)`. `npm test` → 82/82.

## 2026-08-30 — 🚨 إصلاح حرج: health-check.sh مكانش بيقرأ .env خالص

### الاكتشاف
المالك أضاف الإعدادات بدقة لـ.env، لكن الفحص العميق فضل معطَّل.

### السبب
السكريبت لا يقرأ .env خالص — لا هو، ولا تعريف الخدمة (كانت أمثلة معلَّقة بس).

### الإصلاح
`source .env` صريح مع `set -a` مباشرة بعد تحديد DEPLOY_DIR.

### التحقق
محاكاة السيناريو الحقيقي بالضبط (قيم في .env فقط، بدون export يدوي) — القيم اتحمَّلت بدقة.

## 2026-08-30 — 🆕 زرار "شوف الكل" لتاريخ Commits — بالسقف الحقيقي من OpenHands (200)

### التحقق
`/api/git/commits` بحد أقصى حقيقي 200 (لا Pagination حقيقية أبعد).

### التنفيذ
`limit` اختياري في الـEndpoint + حماية تقييد تلقائي + شاشة `ProjectCommitHistoryModal.tsx`.

### خطأ اتكشف واتصلح
عنصرين جذريين بدون Fragment — خطأ React أساسي، اتكشف بـ`tsc -b`.

### التحقق
3 سيناريوهات (افتراضي، 200، قيمة غير صالحة) — كلها دقيقة. `npm test` → 82/82.

## 2026-08-30 — 🆕 معلومات مستودع Git في صفحة المشروع (اسم الريبو + الحالة + آخر Commits)

### القيد الحاكم
المالك أكّد صراحة: أي ميزة لازم أساس حقيقي في OpenHands.

### رحلة التحقيق
`/api/git/commits` و`/api/git/changes` حقيقيتان في OpenHands (اختُبِرتا حيًا بCommit تجريبي). حالة PRs الحية اتأكّدت رسميًا إنها ميزة OpenHands Cloud مدفوعة، مش موجودة في النسخة المفتوحة المصدر. استدعاء MCP مباشرة بمعزل عن محادثة غير مدعوم معماريًا.

### القرار
اكتفاء بما هو مدعوم فعليًا — الCommits المحلية + حالة النظافة، تأجيل PRs لحد توفر أساس حقيقي.

### التنفيذ
اسم الريبو من `.git/config` مباشرة + Endpoint جديد `/api/projects/git-info` + شاشة `ProjectGitInfoCard.tsx`.

### التحقق
اختبار حي كامل (بيانات حقيقية + حالات حدّية). `npm test` → 82/82.

## 2026-08-30 — 🔍 إعادة كتابة كاملة لـREADME.md (من لقطة زمنية قديمة لمرجع حي)

### السبب
الملف القديم (2074 سطر) كان لقطة زمنية من 2026-08-13، مش مرجع حي.

### التنفيذ
إعادة كتابة كاملة (261 سطر) بموافقة صريحة — رؤية، معمارية، كل نظام حالي، مع توجيه واضح لملفات التفاصيل.

### التحقق
قائمة الموظفين وخريطة الملفات استُخرجت آليًا من المصدر الحقيقي، لا الذاكرة — diff مباشر أكّد تطابق 100%.

## 2026-08-30 — 🔍 تدقيق شامل لسلامة التوثيق (بطلب المالك)

### النتيجة
`install.sh` كان محدَّثًا فعليًا، لكن `deploy/README.md` (قديم جدًا، بيانات خاطئة) و`PROJECT_SUMMARY.md` (تاريخه 14 يوم قديم) لأ.

### الإصلاحات
تصحيح تناقض "20 ثانية مقابل دقيقة" في install.sh، إعادة كتابة deploy/README.md بالكامل (شامل نظام المراقبة الجديد)، تحديث PROJECT_SUMMARY.md.

### شفافية
اكتشاف أولي خاطئ لفجوة في ترقيم BUGS_AND_FIXES.md — تم التصحيح والاعتراف الصريح بالخطأ.

### متبقي
README.md الرئيسي (2074 سطر) يحتاج مراجعة منفصلة.

## 2026-08-30 — إصلاح مسار افتراضي خاطئ في health-check.sh

القيمة الافتراضية لمسار المفتاح الداخلي كانت مسار إنتاج غلط، بدل `$DEPLOY_DIR/projects` المطابق لـcompose.yml. صُحح.

## 2026-08-30 — 🆕 فحص عام لصحة اشتراكات LLM (بدون تثبيت أي موديل/مزوّد)

### الهدف
اكتشاف انقطاع المحادثات بسبب انتهاء الاشتراك/الحدود — بدون تثبيت اسم مزوّد في الكود.

### التصميم
يكتشف الملفات الشخصية ونوع مصادقتها ديناميكيًا من `/api/profiles`، ويفحص فقط الاشتراكات الفعلية.

### خطأ حرج اتكشف واتصلح
f-string فيها علامات اقتباس متداخلة = خطأ Syntax في Python، كان مكتوم بـ`2>/dev/null`. اتصلح باستخراج المتغيرات قبل الدمج.

### درس مُعاد تطبيقه
معرِّف ثابت منفصل عن الرسالة المتغيّرة (نفس درس #159).

### التحقق
4 سيناريوهات + السكريبت الكامل من طرف لطرف. `npm test` → 82/82.

---

## 2026-08-30 — 🎯 إصلاح إيجابي كاذب: فحص جدولة auto-deploy كان يقرأ خاصية غلط

### الاكتشاف
الفحص بيقول "مش مجدولة" رغم إن systemctl list-timers أكّد إنها مجدولة فعليًا.

### السبب
`NextElapseUSecRealtime` خاصة بـ`OnCalendar` بس — الـTimer بتاعنا بيستخدم `OnBootSec`/`OnUnitActiveSec`، الخاصية الصحيحة `NextElapseUSecMonotonic`.

### الإصلاح
الفحص بقى يتحقق من الاتنين معًا.

### التحقق
4 سيناريوهات اختُبرت منطقيًا، شاملة السيناريو الحقيقي بالضبط.

---

## 2026-08-30 — 🎯🎯 نظام المراقبة الجديد يلقط انحدارًا حقيقيًا: إصلاح Git (#152) ضاع أثناء ترقية v1.16.0

### الاكتشاف
أول تشغيل فعلي لنظام صحة النظام على السيرفر كشف فورًا: إصلاح Git الدائم غير موجود خالص في الحاوية.

### السبب
استبدال شجرة `openhands-agent-canvas` بالكامل أثناء ترقية v1.16.0 — أُعيد دمج تعديل الواجهة بس، ونُسي إصلاح Dockerfile المنفصل تمامًا.

### التحقق
مراجعة تاريخ Git الكاملة أكّدت عدم فقدان أي ملف آخر — Dockerfile كان الوحيد.

### الإصلاح
إعادة السطر المفقود لنفس الموضع الصحيح.

### الدرس
بالضبط الهدف من نظام المراقبة — أثبت قيمته من أول استخدام حقيقي.

### 🚨 مطلوب بعد النشر
إعادة بناء صورة agent-canvas، ثم تشغيل health-check.sh للتأكد من ✅.

---

## 2026-08-30 — 🆕 سجل تاريخي للحوادث + إصلاح باگ حقيقي في اكتشاف التحوّلات

### الهدف
تبويب "السجل" بجانب "الحالة الحية" — يوضح متى حصلت مشاكل سابقة، مش بس الآن.

### خطأ حقيقي اتكشف واتصلح
الرسالة نفسها كانت بتُستخدَم كمعرِّف الفحص — فحص فاشل ونفسه ناجح بأسماء مختلفة = "فحصين منفصلين" بالغلط، فمفيش تحوّل كان بيتسجل خالص. اتصلح بفصل معرِّف ثابت عن الرسالة المتغيّرة، في 13 موقع استدعاء.

### التنفيذ
`health-check.sh` يسجّل التحوّلات فقط (مش كل فحص) في `health-history.jsonl` محدود الحجم + `GET /api/system-health-history` + تبويب جديد في `SystemHealthModal.tsx`.

### التحقق
سيناريو كسر→إصلاح حقيقي سجّل الحدثين بدقة بعد الإصلاح. `npm test` → 82/82.

---

## 2026-08-30 — 🆕 شاشة "صحة النظام" في السايدبار

### الهدف
مكان دائم للمالك يشوف حالة المنظومة كاملة، مش بس إشعار عابر.

### التنفيذ
`health-check.sh` بقى يكتب ملف JSON منظَّم (بأمان تام عبر python3، تفاديًا لتكرار باگ UTF-8 من #157) + `GET /api/system-health` (مصادقة عادية) + شاشة سايدبار جديدة `SystemHealthModal.tsx` (تحديث كل 30 ثانية).

### خطأ اتكشف واتصلح
مسار افتراضي غير دقيق لملف الحالة — صُحح ليطابق الـVolume الفعلي.

### التحقق
سلسلة اختبارات حية كاملة (JSON صالح، مصادقة صحيحة، محتوى دقيق). `npm test` → 82/82.

---

## 2026-08-30 — 🆕 نظام مراقبة استباقي + اختبارات دخان (سد فجوتين معماريتين كبيرتين)

### الهدف
سد فجوتين حدَّدهما المالك: الاعتماد الهش على OpenHands، وغياب المراقبة الاستباقية.

### النظام الجديد
`/api/internal/health-deep` (فحص محادثة حقيقي بمهلة 10 ثوانٍ مضمونة) + `deploy/health-check.sh` (فحوصات شاملة: حاويات، Git ownership، جدولة auto-deploy، فحص عميق) + `mkdd-health-check.timer` (كل 5 دقائق، تنبيه فوري عند الفشل).

### حل استباقي إضافي
`install.sh` بقى يشغّل الخدمات مرة تلقائيًا بعد التفعيل — يمنع تكرار مشكلة "نقطة الانطلاق" اللي واجهناها مرتين اليوم.

### خطأ اتكشف وأتصلح
فاصلة عربية كـ delimiter في `paste` كانت بتتلف UTF-8 — اتصلحت بحلقة for آمنة.

### التحقق
سلسلة اختبارات حية شاملة أكّدت كل سيناريو (نجاح، تعليق محاكاة، أمان النطاق). `npm test` → 82/82.

### 🚨 مطلوب بعد النشر
ضبط `MKDD_HEALTH_CHECK_PROJECT`/`EMPLOYEE_ID`/`EMPLOYEE_NAME` لتفعيل الفحص العميق.

---

## 2026-08-30 — ترقية OpenHands لـv1.16.0 (استهداف باگ WebSocket الأحداث الصامت)

### الاكتشاف
لوج مباشر أثبت: WebSocket الأحداث بيتصل ويتصادق عليه بنجاح، لكن ما بيرسلش أي بيانات خالص. تقرير رسمي من OpenHands يصف نفس الظاهرة.

### ⚠️ ملاحظة أمانة
التقرير مُصنَّف "Enhancement" مش "Bug مُصلَح مؤكَّد" — الفائدة غير مضمونة 100%.

### التنفيذ
اكتشاف مهم: الملف الأصلي في v1.16.0 اتغيّر جوهريًا (ميزة جديدة `enable_switch_llm_tool`) — دُمج تعديلنا بعناية بدل استبدال كامل، بـ8 نقاط دمج دقيقة.

### التحقق
diff أظهر فقط إضافاتنا المتوقعة، مفيش أي حذف غير مقصود. websocket-auth.ts لم يتغيّر.

### المتبقي
بناء واختبار حي على Staging.

---

## 2026-08-29 — سد فجوة تعليمات: "اعمل تقرير" من المالك = استخدم نظام التقارير

### السبب
التعليمات كانت موجّهة لسيناريو "الموظف بيلاحظ مشكلة بنفسه" بس — مفيش ربط صريح لطلب المالك المباشر.

### الحل
توضيح صريح: طلب المالك "اعمل تقرير" يعني استدعاء الـAPI، مش كتابة مستند.

---

## 2026-08-29 — 🆕 التقارير بقت تتسلّم تلقائيًا للموظف المستلم

### المطلوب
الموظف المستلم يشوف التقرير ويشتغل عليه فورًا، بدون انتظار المالك ينقله يدويًا.

### الحل
`deliverMessageToEmployee` — تسليم تلقائي فوري (fire-and-forget) لحظة تسجيل التقرير، برسالة جاهزة شاملة معرّف التقرير الحقيقي.

### خطأ اتصحح
أول محاولة استخدمت `void` جوّه try/catch (مش بيلتقط الخطأ فعليًا) — اتصحح بـ`.catch()` صريح.

### التحقق
اختبار حي: 23ms زمن استجابة، محتوى الرسالة مؤكَّد صحيح. `npm test` → 82/82.

---

## 2026-08-27 — 🚨🚨 حرج: مفيش حد قادر يسجّل دخول — انهيار كامل للسيرفر

### السبب
`ERR_HTTP_HEADERS_SENT` — استثناء غير معالج بعد إرسال رد جزئي (ملف صورة موجود بافتراض، لكن غير قابل للقراءة فعليًا) بيكسر عملية Node.js بالكامل.

### الإصلاح
1. `buildOwnerAvatarUrl` تتدهور بأمان (`null`) بدل رمي استثناء.
2. حماية عامة إضافية في `index.mjs` — فحص `res.headersSent` قبل أي كتابة، تحمي من أي خطأ مشابه مستقبلاً في أي Route.

### التحقق
محاكاة حية لملف غير قابل للقراءة — تسجيل دخول ناجح، السيرفر بقي حيًا. `npm test` → 82/82.

---

## 2026-08-27 — 🎯 إصلاح دائم: "الموظف بيقف فجأة" — Git بيرفض /projects بعد تغيير ملكية

### السبب
Volume خارجي (`/projects`) — أي تغيّر في ملكيته (زي تنصيب نظيف) بيخلي Git يرفض كل الأوامر فيه (إجراء أمان قياسي، CVE-2022-24765)، بما فيها إنشاء محادثة جديدة.

### الحل الفوري
`git config --global --add safe.directory '*'` يدويًا (مؤقت، هيضيع في أي إعادة بناء).

### الحل الدائم
نفس الأمر في Dockerfile نفسه — يتفعّل تلقائيًا مع كل بناء صورة.

### 🚨 خطوة مطلوبة
إعادة بناء صورة agent-canvas لتفعيل الإصلاح الدائم.

---

## 2026-08-27 — تراجع عن حل GITHUB_TOKEN المزدوج + الوضع الحالي للتحقيق

### السبب
المالك أوضح بحق: حفظ نفس التوكن في مكانين (OpenHands + متغيّر MKDD خاص) مش حل حقيقي، مجرد عبء صيانة إضافي.

### الإجراء
تراجع كامل (`git revert`) عن حل #151 (المتغيّر `MKDD_GITHUB_TOKEN`).

### اكتشاف مهم
السرّ الحالي (`GITHUB_PERSONAL_ACCESS_TOKEN`) اتولّد تلقائيًا بواسطة OpenHands نفسها عند إعداد MCP — مش من اختيار المستخدم، وعلى الأرجح مرتبط بأداة MCP داخليًا بس.

### الخطوة التالية
اختبار مباشر: هل GitHub MCP فعليًا عاجزة عن رفع ملفات ثنائية، أو ده ادّعاء غير مُتحقَّق من الموظف؟

### التحقق
`npm test` → 82/82 بعد التراجع.

---

## 2026-08-27 — سد فجوة تعليمات: خلط بين "الدفع" و"الدمج" منع مريم من رفع شغلها

### السياق
مريم أنهت Gate 2 لكن امتنعت عن رفع ملفاتها لـGitHub، معتقدة إن القيد على "الدمج بدون إذن" بيشمل "الرفع" كمان.

### التشخيص
مراجعة نص AGENTS.md الفعلي أكّدت: القيد خاص بالدمج فقط، الرفع وفتح PR جزء طبيعي من سير العمل.


### الحل
توضيح صريح جديد يمنع نفس سوء الفهم مستقبلاً.

### ⚠️ ملاحظة
إصلاح تعليمات، مش كود — مع احتمال مشكلة تقنية إضافية (Remote مش مربوط).

---

## 2026-08-27 — إصلاح: "جاري التحميل" بيختفي بدون عرض المحادثة (نفس نمط باگ #144)

### التشخيص
اختبار مباشر أكّد: الخادم سريع جدًا (53-68ms) وبيرجّع بيانات كاملة صحيحة — المشكلة 100% في الفرونت إند.

### السبب
`useConversation.ts` كان بيعتمد على `[project, employee]` كـObjects كاملة بدل معرّفاتهم — أي Reference جديد وهمي (من التحديث الدوري مثلاً) كان بيلغي الطلب الجاري ويبدأ من جديد.

### الإصلاح
4 مواقع في نفس الملف (فتح المحادثة، مسح الكتابة، WebSocket، التحديث كل 15 ثانية) — توحيد الاعتماد على `path`/`id` بدل الكائنات الكاملة.

### التحقق
`npm test` → 82/82، `tsc -b` → نظيف.

---

## 2026-08-26 — رسائل الخطأ عند فشل الإرسال بقت ظاهرة + تشخيص "الرسالة بترجع"

### السبب الفعلي
اختبار حي كشف: `LLM profile 'glm-5.2' not found` — إعداد الاتصال بمزوّد الـAI اتمسح مع التنصيب النظيف (طبيعي، مش باگ).

### فجوة UX اتصلحت
الفرونت إند كان بيتجاهل رسائل الخطأ الحقيقية من الخادم تمامًا، ويرجّع النص بصمت. دلوقتي بتظهر رسالة واضحة فوق مكان الكتابة لأي سبب فشل مستقبلي.

### ⚠️ مطلوب منك
إضافة LLM Profile (`glm-5.2`) من واجهة OpenHands قبل أي اختبار إرسال.

### التحقق
`npm test` → 82/82، `tsc -b` → نظيف.

---

## 2026-08-26 — تحسين: الموظفون بقوا يتعملوا تلقائيًا بالكامل عند أي تنصيب مستقبلي

### السياق
المستخدم لاحظ إن السكريبت اضطر لخطوة يدوية منفصلة لعمل الموظفين.

### الحل
توثيق `MKDD_BOOTSTRAP_LLM_PROFILE_REF=glm-5.2` في `.env.staging` — أي تنصيب مستقبلي هيعمل الموظفين تلقائيًا بدون أي تدخل.

---

## 2026-08-26 — تدقيق سكريبتات النشر قبل تنصيب نظيف من الصفر

### السياق
قبل مسح كل حاجة وإعادة التنصيب، تدقيق شامل لـ`install.sh`/`setup.sh` للتأكد من شمولهم كل الإصلاحات.

### اكتُشف وصُحح
مرجعين قديمين (بورت 15173 القديم في رسائل نصية، اسم صورة v1.13.0 في compose.yml) — توثيقية بس، مش أخطاء وظيفية. القيم الفعلية كانت صحيحة بالفعل.

### تأكيد
`install.sh` شامل بالكامل (Clone + نظام نشر تلقائي)، ويرفض عمدًا الكتابة فوق مجلد موجود بالفعل.

### ⚠️ ملاحظة
الموظفون محتاجين Bootstrap من جديد بعد التنصيب (`MKDD_BOOTSTRAP_LLM_PROFILE_REF=glm-5.2`).

---

## 2026-08-26 — 🎯🎯🎯 السبب الجذري النهائي لكل مشكلة البطء

### الاكتشاف الحاسم
اختبار بجلسة تسجيل دخول حقيقية (لأول مرة في التحقيق) كشف: `/api/projects` بتاخد 120 ثانية بالظبط ثم تفشل بـ502.

### السبب
`handleProjects` كانت بتعيد فحص **كل** المحادثات على السيرفر من الصفر **لكل مشروع على حدة** — تعقيد O(مشاريع × محادثات) بدل O(محادثات) بس.

### الحل
جلب كل المحادثات مرة واحدة، وفلترتها محليًا لكل مشروع.

### التحقق الحاسم
محاكاة 5 مشاريع/300 محادثة — قبل: 15 استدعاء متوقع نظريًا. بعد: 3 استدعاءات فعليًا (تقليل 80%). الزمن: من 120+ ثانية (فشل) لـ0.053 ثانية.

### درس مهم
اختبار سرعة أي Endpoint محمي لازم يكون بجلسة حقيقية — الاختبار بدون مصادقة بيدي انطباع "سريع" مضلِّل.

### التحقق
`npm test` → 82/82 (4 اختبارات جديدة)، `tsc -b` → نظيف.

---

## 2026-08-25 — إصلاح: المحادثات كانت بتعمل "ريفريش" لوحدها كل 30 ثانية

### السبب
تأثير جانبي غير مقصود من #141 — التحديث الدوري لقائمة الموظفين كان بيغيّر Reference كائن الموظف المُختار، وده كان بيخلي المحادثة تمسح نفسها وتتحمّل من جديد كل 30 ثانية.

### الحل
تغيير Dependency من الـArrays الكاملة لطولها بس — نمط موجود بالفعل ومقبول في المشروع.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف.

---

## 2026-08-25 — الحل النهائي لبطء المحادثات: سلوك OpenHands نفسه (خارج نطاقنا)

### الاكتشاف القاطع
اختبار حي: نفس المحادثة مرتين متتاليتين — الأولى بطيئة، الثانية فورية تقريبًا. تأكيد إنه "إيقاظ بيئة عمل" من OpenHands نفسه، مش باگ في MKDD.

### التحسين
رسالة طمأنة تظهر بعد 8 ثوانٍ انتظار حقيقي فقط — بدون أي تأثير على المسار السريع.

### خطأ اتصحح
حذف بالغلط حالة موجودة أثناء التعديل — اكتُشف فورًا عبر tsc.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف.

---

## 2026-08-25 — ✅✅ تأكيد نهائي: النشر التلقائي رجع لحالته الصحية بالكامل

### تحديث مطمئن
النظام كان متوقفًا فعليًا من 21 أغسطس (مش شغال في حلقة مستمرة) — تحديثاتنا اليدوية كانت آمنة طوال الجلسة.

### مشكلة ثانوية اتصلحت
جدولة الـTimer ما كانتش بتحسب بعد إعادة التفعيل اليدوي — حُلّت بتشغيل الخدمة مرة واحدة يدويًا لتنشيط نقطة الانطلاق.

### التحقق النهائي
5 دورات نشر متتالية كاملة — كل واحدة نجحت بدون أي رجعة خالص.

---

## 2026-08-24 — 🚨🚨 الأخطر في الجلسة: النشر التلقائي عالق في حلقة رجعة لا نهائية

### الاكتشاف
سؤال بسيط ("المفروض السكريبت بيعمل تحديث اتوماتيك؟") كشف: `/api/health` مش مستثناة من المصادقة، فنظام النشر التلقائي بيفشل ويرجع تلقائيًا لكوميت قديم جدًا **كل 20 ثانية منذ #127**.

### التأثير
من المحتمل جدًا إن هذا كان بيمحي تحديثاتنا اليدوية تلقائيًا خلال دقائق من كل نشر طوال الجلسة.

### الحل
إضافة `/api/health` لاستثناءات المصادقة — بعد التأكد إنه فحص آمن بدون بيانات حساسة.

### 🚨 عاجل جدًا
لازم إيقاف النظام التلقائي مؤقتًا ونشر يدوي فوري.

### التحقق
`/api/health` بدون كوكي → 200. `/api/projects` لسه محمية → 401. `npm test` → 78/78.

---

## 2026-08-24 — إصلاح نهائي لمشكلة الموديل: تحديث دوري لقائمة الموظفين

### السبب
قائمة الموظفين (شاملة الموديل) كانت بتتحمّل مرة واحدة بس عند فتح التطبيق — إصلاح #131 صحّح المصدر لكن مش حداثته.

### الحل
تحديث دوري صامت كل 30 ثانية، بنمط "آخر بيانات معروفة صحيحة".

### التحقق
محاكاة حية لتغيير الموديل بين طلبين متتاليين — أكّدت وصول البيانات الحديثة بدقة. `npm test` → 78/78.

---

## 2026-08-24 — ⏳ إضافة مؤشر تحميل لفتح المحادثة (تحقيق بطء التحميل)

### التحقيق
5 قياسات حية متتالية (بحث، جلب رسائل، طلب كامل، شبكة، متصفح جديد) — كلها أثبتت سرعة عالية جدًا (أقل من ثانية).

### الاكتشاف
سؤال دقيق للمستخدمة كشف: مفيش أي مؤشر تحميل خالص لفتح المحادثة لأول مرة — الشاشة فاضية تمامًا بدون إشارة.

### الحل
حالة `isOpeningConversation` جديدة + مؤشر تحميل واضح.

### ⚠️ ملاحظة أمانة
مفيش دليل تقني على تأخير فعلي حقيقي بمقدار 20-25 ثانية — الإصلاح يحل فجوة UX مؤكَّدة، محتاج تأكيد المستخدمة بعد النشر.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف.

---

## 2026-08-24 — ✅ ترقية OpenHands Agent Canvas مؤكَّدة ناجحة على Staging

### النتيجة
اختبار وظيفي شامل نجح بالكامل (مشاريع، محادثة قديمة، رسالة جديدة).

### مشكلة عابرة
`mkdd-ui-staging` توقفت لحظيًا بعد بدء `agent-canvas` الجديدة (كود 137) — لم يُؤكَّد السبب بدقة (dmesg فاضية)، إعادة التشغيل حلّتها فورًا.

### 🆕 بند تحقيق منفصل (#139)
بطء تحميل محادثات (20-25 ثانية) — تم التأكد صراحة إنه موجود من قبل الترقية، مش سببها. مفتوح كتحقيق مستقل.

### المتبقي
نشر نفس الصورة على الإنتاج بعد فترة مراقبة كافية.

---

## 2026-08-24 — ترقية OpenHands Agent Canvas من v1.12.0 لـv1.15.0

### الفحص
بحث دقيق قبل الترقية أكّد عدم وجود تغييرات كاسرة تمس تكاملنا.

### اكتشاف مهم
الكود المُخصَّص كان محفوظ بالفعل جوّه المستودع (مش محتاج Fork منفصل) — تصحيح مسار العمل بناءً على سؤال محق من المستخدم.

### التنفيذ
استنساخ v1.15.0 الرسمي، مقارنة دقيقة أكّدت مطابقة الفرق حرفيًا لنفس التعديل المعروف (لا تعارض)، تطبيق التعديل، تحقق نهائي شامل.

### 💡 فرصة مستقبلية
OpenHands بقى بيخزّن مفتاح الجلسة في ملف حقيقي — بديل أنظف من آلية `/proc` الحالية.

### المتبقي
بناء صورة Docker جديدة واختبار على Staging.

---

## 2026-08-24 — 🎯 إصلاح المحادثة الحية: مفتاح مخزَّن قديم بدون إعادة محاولة

### السياق
شكوى متكررة — لازم تحديث يدوي لرؤية رسائل جديدة، رغم إن الميزة كانت شغالة ومُختبَرة قبل كده.

### السبب الجذري
الـWebSocket كان بيستخدم مفتاح مصادقة مخزَّن مؤقتًا (من تحسين #124)، لكن آلية إعادة المحاولة كانت موجودة بس في REST، مش في الـWebSocket. لو Agent Canvas اتعاد تشغيلها بمفتاح جديد، الاتصال يفشل بصمت للأبد.

### الحل
إعادة محاولة تلقائية مرة واحدة لو الاتصال فشل قبل استقبال أي رسالة — نفس نمط REST، لكن للـWebSocket.

### التحقق (بعد استبعاد شرط اختبار زائف مرتين)
محاكاة كاملة للسيناريو الحقيقي — لوج أكّد رفض المفتاح القديم، قبول الجديد، ووصول الحدث الحي فعليًا. `npm test` → 78/78.

---

## 2026-08-20 — تبسيط: المفتاح السري يتولّد لوحده تلقائيًا الآن

### السؤال
هل ضبط المفتاح يدويًا إلزامي، أو فيه طريقة أبسط؟

### الاكتشاف
`/projects` مشتركة فعليًا بين الحاويتين (قراءة/كتابة للطرفين).

### الحل
المفتاح بيتولّد تلقائيًا أول مرة، ويتخزن في ملف مشترك — بدون أي إعداد يدوي من المالك خالص. إزالة متغيّر البيئة اليدوي بالكامل.

### ⚠️ ملاحظة صادقة
أول طلب وحيد في عمر النظام ممكن يفشل مرة واحدة (قبل توليد الملف) — إعادة المحاولة تحله فورًا.

### التحقق
5 سيناريوهات حية، شامل اختبار الثبات عبر إعادة التشغيل. `npm test` → 78/78.

---

## 2026-08-20 — 🚨 رجعة حرجة: تسجيل الدخول عطّل كل تفاعلات الموظفين مع سير العمل

### الاكتشاف
نظام تسجيل الدخول (#127) عطّل كل الـ10 نقاط اتصال الموثّقة للموظفين (بوابات، عوائق، نتائج، تقارير) — الموظفون بيتصلوا عبر curl بدون Cookie خالص.

### الحل
مفتاح سري داخلي (`MKDD_INTERNAL_SERVICE_KEY`) مضاف لخدمتي mkdd-ui وagent-canvas، مقصور بدقة شديدة على `/api/workflow/*` بس. AGENTS.md محدَّثة بالكامل (10 أمثلة curl).

### 🔒 التحقق الأمني
تأكيد قاطع إن المفتاح مش بيعطي أي صلاحية على أي endpoint تاني (projects, settings) — النطاق ضيق ودقيق. جلسة المالك عبر المتصفح لسه سليمة بدون أي تراجع.

### ⚠️ خطوة إلزامية
لازم تحدّد قيمة حقيقية عشوائية لـ`MKDD_INTERNAL_SERVICE_KEY` في `.env` — القيمة الافتراضية غير آمنة للإنتاج.

### التحقق
7 سيناريوهات حية شاملة. `npm test` → 78/78، `tsc -b` → نظيف.

---

## 2026-08-20 — تحذير إضافي: باجوش ادّعى قدرة تقنية غير موجودة

### السياق
باجوش ادّعى إنه يقدر "يبدأ محادثة مستقلة" لموظف تاني — تحقق تقني قاطع أكّد إن ده مستحيل (مفتاح الاتصال بالنظام محجوب عن أي موظف).

### الحل
تحذير صريح في AGENTS.md يمنع هذا الادّعاء المضلِّل — نظام التقارير هو الآلية الحقيقية الوحيدة.

### ⚠️ ملاحظة
إصلاح ثانٍ في تعليمات الموظفين، مش في الكود — النظام التقني كان آمن وصحيح من البداية.

---

## 2026-08-20 — محاولة إضافية لخط الـPWA الأبيض (⏳ محتاجة تأكيد)

### السياق
المشكلة لسه ظاهرة بعد 3 إصلاحات سابقة (#114, #115, #120).

### الإصلاح
توحيد `100vh` → `100dvh` في `.app` وشاشات تسجيل الدخول — عدم اتساق حقيقي كان موجود.

### ⚠️ محتاج تأكيد
هذا إصلاح منطقي إضافي، مش تأكيد قاطع إنه السبب الوحيد المتبقي.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-20 — النموذج في تفاصيل الموظف بقى يعكس الإعداد الحالي الفعلي

### السبب
النموذج المعروض كان بيتيجي من إحصائيات تراكمية للمحادثة القديمة، مش الإعداد الحالي من الـAgent Profile.

### الحل
استبدال `cost.modelName` (قديم/متجمد) بـ`employee.llm_profile_ref` (حالي دايمًا) — البيانات كانت متاحة بالفعل، بس مش مُستخدَمة.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-20 — سد فجوة تعليمات: باجوش كتب "رسائل" بدل استخدام نظام التقارير

### التشخيص
باجوش كتب رسائل نصية في المحادثة موجّهة لمريم وشنودة، بدل استدعاء API التقارير الفعلي — مفيش أي تقرير ظهر في صفحة المشروع.

### السبب
التعليمات كانت مفصّلة تقنيًا لكن بدون تحذير صريح ضد تفسير "التسجيل" على إنه مجرد كتابة رسالة.

### الحل
تحذير صريح في AGENTS.md — "التسجيل" يعني استدعاء الـAPI بس، مفيش نظام رسائل بين الموظفين، المالك مش وسيط رسائل.

### ⚠️ ملاحظة
إصلاح في تعليمات الموظفين، مش في كود MKDD — النظام نفسه شغال 100% تقنيًا.

---

## 2026-08-17 — نظام نسخ احتياطي تلقائي + إصلاح أمني وقائي مهم

### الميزة
`backup-mkdd-data.sh` جديد — backup/list/restore، احتفاظ تلقائي (14 نسخة افتراضيًا)، جاهز للجدولة عبر cron.

### 🚨 اكتشاف أثناء البناء
`mkdd-data/` مش كانت مُستبعَدة من Git خالص — خطر حقيقي لرفع بيانات حساسة (كلمات سر، جلسات) بالغلط. تم التأكد من عدم وجود أي تسريب فعلي سابق (فحص التاريخ الكامل)، وإصلاح `.gitignore` فورًا.

### التحقق
اختبار حي شامل للنسخ/التقليم/الاسترجاع (7+ سيناريوهات)، ومحاكاة مباشرة تؤكد استبعاد الملفات الحساسة من Git.

### 🎉 اكتمال الأولويات الثلاث المتفق عليها
تسجيل الدخول + تنظيف الميزات المعطّلة + النسخ الاحتياطي.

---

## 2026-08-17 — صورة شخصية لكل مستخدم مسجّل

### الميزة
كل مستخدم يقدر يرفع/يغيّر صورته الشخصية من السايدبار، بنفس تجربة رفع صور الموظفين.

### الأمان
صاحب الصورة بيتحدد من الجلسة الحالية بس — مفيش طريقة لمستخدم يغيّر صورة مستخدم تاني.

### خطأ اتلقى وصُحح قبل الاختبار
استيراد دائري بين auth.mjs وowner-avatar.mjs — تم فصل المنطق المشترك في ملف مساعد منفصل.

### التحقق
6 سيناريوهات حية، شامل اختبار أمني حاسم (مستخدم تاني رفع صورته، صورة الأول فضلت بدون تأثر). `npm test` → 78/78.

---

## 2026-08-17 — 🔐 نظام تسجيل دخول كامل (الخطوة 2 — أهم أولوية أمنية)

### الميزة
اسم مستخدم + كلمة سر، شاشة إعداد أول مرة من الواجهة، جلسات دائمة (90 يوم)، حماية كل الـAPI + الـWebSocket.

### الأمان
تشفير scrypt مدمج (بدون مكتبات خارجية)، كوكي HttpOnly + SameSite=Lax، بوابة حماية مركزية حقيقية (مش مجرد إخفاء واجهة).

### التحقق
22 اختبار حي إجمالي (12 وحدة + 10 نظام كامل عبر HTTP فعلي). `npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

### ⚠️ بعد النشر
أول زيارة هتعرض شاشة إعداد فارغة — لازم إنشاء حساب جديد.

---

## 2026-08-17 — تنظيف ميزة "الدومينات المسموحة" المعطّلة (الخطوة 1 من الأولويات المتبقية)

### السبب
الميزة فقدت تأثيرها الفعلي بعد التحول لنسخة إنتاج (#123) — كانت بتحمي Vite Dev Server بس، اللي مبقاش شغال.

### التنظيف
شيل site-config.mjs + 3 endpoints + 3 دوال client معطّلة، استبدال DomainSettingsModal بـRestartModal مبسّط، تبسيط vite.config.ts.

### التحقق
3 سيناريوهات حية (زرار الإعادة لسه شغال، الendpoint القديم اختفى تمامًا، تقديم الملفات سليم). `npm test` → 78/78.

---

## 2026-08-17 — تنبيه بصري على روابط التطبيق الحي القديمة

### التشخيص
روابط "التطبيق الحي" (كيرلس) مرتبطة بسيرفر شغال طول ما المحادثة نشطة بس — مختلف عن روابط النموذج الثابت (مريم) اللي دايمًا شغالة. سلوك طبيعي متوقع، مش مرتبط بأي تعديل حديث.

### الميزة الجديدة
الكارت بيفحص عمر الرسالة — لو أكتر من ساعة، الشارة بتتحول لـ"قد يكون منتهي" (أصفر تحذيري) بدل "شغال الآن" (أخضر مضلِّل).

### التحقق
محاكاة منطقية لـ5 حالات، شاملة سيناريو المستخدم الحقيقي (رسالة يومين). `npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — 🎯 الاكتشاف الأهم: مفتاح الجلسة كان بيُكتشَف من الصفر مع كل طلب

### السياق
بعد إصلاح #123، أوضح المستخدم إن مافيش تحسن خالص — حتى صفحة بمشروعين بس بطيئة.

### السبب الجذري
`sessionKey()` كانت بتمسح كل عمليات النظام من الصفر (`/proc`) مع **كل** طلب API واحد، بدون أي تخزين مؤقت.

### الحل
تخزين المفتاح مؤقتًا بعد أول اكتشاف، مع إعادة محاولة تلقائية لو المفتاح بقى غير صالح (401/403).

### التحقق (قياس فعلي)
19.24ms للمسح الأول، مقابل 0.0001ms للاستدعاء المخزَّن — فرق ~206,000 ضعف. `npm test` → 78/78.

### الأهمية
هذا كان بيؤثر على كل طلب API في التطبيق بالكامل، مش ميزة معينة — يفسّر ليه الإصلاحات السابقة (تجميع الملفات، ترتيب البحث) مالحقتش المشكلة الحقيقية.

---

## 2026-08-17 — نسخة إنتاج حقيقية: حل البطء العام في كل التطبيق

### السبب
التطبيق كان شغال بوضع التطوير (Vite Dev Server) طول الوقت — كل ملف بيتقدم منفصل بدل تجميع واحد محسّن.

### الحل
بناء إنتاج حقيقي مرة واحدة وقت بناء الحاوية، تقديمه كملفات ثابتة من نفس السيرفر (بورت 8787 بس، بدل بورتين).

### ⚠️ تأثير جانبي أمني موثّق
نظام "الدومينات المسموحة" (allowedHosts) كان خاص بحماية Vite Dev Server تحديدًا — مبقاش له تأثير فعلي بعد هذا التغيير (الخطر الأصلي لم يعد قائمًا). الحماية الحقيقية المستقلة (فحص Host header للوصول المحلي) لسه شغالة زي ما هي.

### التحقق
اختبار حي كامل (5 سيناريوهات، شامل رفض Path Traversal). `npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — تصحيح تشخيص تأخير المحادثة: السبب الحقيقي مختلف تمامًا

### تصحيح
المحادثة المُختبَرة قصيرة (أقل من 20 رسالة) — إصلاح #121 مالوش تأثير حقيقي هنا.

### السبب الحقيقي
`findAuthorizedConversation` بتدوّر عبر كل محادثات النظام صفحة بصفحة، وكانت بتعتمد على ترتيب افتراضي غير محدد صراحة (`CREATED_AT_DESC` موثّق) — محادثة قديمة الإنشاء لكن نشطة حاليًا كانت هتحتاج عشرات الصفحات للعثور عليها.

### الحل
`sort_order=UPDATED_AT_DESC` صراحة — أي محادثة نشطة بتظهر قريبة من الأول تلقائيًا.

### التحقق
مقارنة حية مباشرة (500 محادثة وهمية): طلب واحد مع الإصلاح، مقابل 6 طلبات متتالية بدونه. `npm test` → 78/78.

---

## 2026-08-17 — المحادثة بقت تفتح فورًا، زي أي تطبيق محادثة حقيقي

### السبب
تحميل تاريخ المحادثة بالكامل (حلقة صفحات متتالية) بدل آخر رسائل بس، عبر طلبين HTTP متتاليين.

### الاكتشاف
الـAPI بتاعت OpenHands بتدعم `sort_order=TIMESTAMP_DESC` — ميزة موجودة، مش مُستخدَمة.

### الحل
Endpoint مدمج جديد (`/api/chat/open`) بيجمع حل المحادثة + آخر 50 رسالة في طلب واحد. نظام تحميل تدريجي كامل (رسائل أقدم عند السحب لأعلى)، مع حل تحدي "قفزة التمرير".

### 4 أخطاء حقيقية اتلقت بالفحص المباشر
تكرار طلب بالغلط، كسر لحظي لبنية ملف، تمرير `null` بدل `undefined` (كان هيمسح خطة العمل)، و`.catch()` مستحيل التفعيل.

### التحقق
اختبار حي شامل لمحادثة 120 رسالة + الـEndpoint المدمج. `npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إصلاح حافة بيضاء مستمرة في الـPWA (مختلفة عن مشكلة شاشة البداية)

### السبب
`viewport-fit=cover` مش موجودة — الصفحة مبتمدش لمناطق الحواف المدورة/الحزّة في وضع الـPWA (بدون واجهة متصفح تغطيها).

### الحل
إضافة `viewport-fit=cover` لمنع الفراغ الأبيض المستمر في المناطق الآمنة.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إصلاح: تغيير الـLLM من الواجهة كان بيترجع للقيمة الافتراضية

### السبب
سكريبت Bootstrap كان بيبعت قيمة LLM ثابتة في كل مرة يتشغّل، وبما إن الـEndpoint Upsert حقيقي، كان بيكتب فوق أي تغيير يدوي.

### الحل
السكريبت بقى يجلب القيمة الحالية أولًا ويحافظ عليها للموظفين الموجودين، ويستخدم الافتراضي بس للموظفين الجدد.

### التحقق
محاكاة حية كاملة (موظف بتغيير يدوي + موظف جديد) — النتيجة صحيحة 100%. `npm test` → 78/78.

---

## 2026-08-17 — تحسين وضوح بار التنقل: اسم المشروع وزرار الرجوع

### السبب
اسم المشروع صغير جدًا وبلون رمادي باهت، زرار الرجوع بلون محايد غير مميز.

### الحل
اسم المشروع أكبر (17px، عريض) وبلون أساسي واضح. زرار الرجوع بلون مميز وخلفية ملوّنة خفيفة.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — توضيح: قفل التقرير هو التوقيع الفعلي على التعديل

### النقاش
سؤال معماري: هل التوقيع الأصلي على بوابة يفضل صالح لو حصل تعديل بعدها؟

### القرار
قفل التقرير من المالك = التوقيع الكافي، بدون حاجة لإعادة فتح البوابة.

### التنفيذ
تغيير نص الزرار لـ"اعتماد التعديل وقفل التقرير"، توضيح صريح في AGENTS.md §6.

### التحقق
`npm test` → 78/78، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إصلاح ترتيب الموظفين: شيري بقت بعد مريم مباشرة

### اكتشاف
كل الموظفين عندهم حقل `order` صريح بالفعل جوّه ملفاتهم، لكن الـBackend مكنش بيقراه — كان بيعتمد على ترتيب نظام الملفات العشوائي.

### الحل
تصحيح قيمة `order` بتاعة شيري (14 → 4) مع إزاحة الباقي، وإصلاح `list-employee-definitions.mjs` عشان يقرا ويرتب بالحقل الفعلي.

### اختبار انحدار جديد
يحاكي نفس السيناريو الحقيقي (ملف يترتب أول أبجديًا لكن `order` بتاعه يقول آخر واحد).

### التحقق
اختبار وحدة جديد (78/78 إجمالي) + اختبار حي بالبيانات الفعلية أكّد الترتيب النهائي الصحيح.

---

## 2026-08-17 — إصلاح حافة فاتحة تظهر بس في وضع الـPWA

### السبب
التطبيق افتراضيًا داكن، لكن manifest.json وtheme-color meta tag كانوا بألوان فاتحة. background_color بتُستخدَم كخلفية شاشة بداية الـPWA بس (مش المتصفح العادي)، فالتعارض ظهر في الـPWA فقط.

### الحل
تحديث manifest.json + theme-color الثابتة للألوان الداكنة، بالإضافة لتحديث ديناميكي (وقت التحميل وأثناء تبديل الوضع لاحقًا).

### قيد موثّق
manifest.json ملف ثابت، بتتقرا وقت التثبيت بس — مش قابلة للتحديث الديناميكي بعد كده.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح، manifest.json تحقّق JSON.

---

## 2026-08-17 — إصلاح خط أبيض حوالين الصفحة (يمين/شمال/تحت)

### السبب
`html` معندهاش لون خلفية محدد، بيرث الأبيض الافتراضي — بيظهر في الفراغات الناتجة عن max-width الصفحة واختلافات ارتفاع الـviewport.

### الحل
`background: var(--mkdd-page);` على `html` نفسها، مش `body` بس.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — أيقونة الـPWA تستخدم شعار MKDD الحقيقي، مش أيقونة عامة

### السبب
شعار الهيدر الحقيقي (PNG قابل للتغيير من الواجهة) مختلف عن الـSVG الثابت المُستخدَم أصلًا لتوليد الأيقونات.

### الحل
توليد ديناميكي (`sharp`) من نفس ملف الشعار الحقيقي وقت الطلب — يبقى متزامن تلقائيًا مع أي تغيير مستقبلي، مع Fallback آمن لو مفيش شعار مرفوع لسه.

### التحقق
اختبار حي بفحص لوني دقيق على مستوى البكسل، تأكيد عدم التشويه مع شعار غير مربع. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — تقييد الإعدادات/إعادة التشغيل للوصول المحلي بس — حماية حقيقية

### تصحيح مهم
إخفاء الزرار في الواجهة بس مش كافي — الـEndpoints هتفضل شغالة لأي حد يعرف الرابط.

### الحل
Backend يفحص Host header ويرفض (403) أي طلب مش من IP محلي/خاص، على كل الـ4 endpoints. Frontend بنفس المنطق (شكلي، يخفي الزرار بس).

### التحقق
محاكاة منطقية لـ13 حالة (شاملة اختبار دقة حرج)، واختبار حي كامل لـ4 سيناريوهات على السيرفر الفعلي. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — زرار إعادة تشغيل حقيقي (Docker Socket) + نقل الإعداد للسايدبار

### الميزة
إعادة تشغيل فعلية للحاوية من الواجهة (بدون مكتبات إضافية، عبر Docker Engine API)، الإعداد بقى في السايدبار بدل الهيدر.

### تريد-أوف أمني موثّق
وصول Docker Socket — موثّق صراحة كديون تقني مقصود، بنفس نمط `pid: service` الموجود بالفعل.

### باگ React اتلقى
Modal الإعدادات كان هيختفي فورًا مع قفل السايدبار — اتصلح بفصل حالة الـModal تمامًا.

### 🔴 باگ خطير في compose.yml اتلقى بالتحقق المباشر
section `volumes:` مكررة مرتين لنفس الـservice — YAML بياخد آخر قيمة بس، فكانت كل الـmounts الأصلية (AGENTS.md، البيانات، المشاريع) هتختفي بالكامل لو اتنشر! تم الدمج والتحقق من وجود كل الـ4 mounts معًا.

### التحقق
اختبار حي للـEndpoint (يفضل السيرفر شغال حتى مع فشل الاتصال بالـsocket). `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إعداد الدومين من الواجهة، مع رسالة "محتاج إعادة تشغيل"

### تصحيح تقييم سابق
رُفضت فكرة الواجهة بالغلط بحجة "الإعداد بيتقرا وقت البدء بس" — المستخدم أوضح إن ده مش سبب كافٍ (النمط الشائع: احفظ + تنبيه إعادة تشغيل).

### التنفيذ
`site-config.mjs` جديد (تخزين دائم)، endpoints للإضافة/العرض/الحذف، `vite.config.ts` بقى يدمج مصدرين (متغيّر بيئة + ملف محفوظ)، `DomainSettingsModal.tsx` جديد بتحذير واضح غير قابل للتجاهل.

### التحقق
محاكاة دمج المصدرين + اختبار حي كامل لـ6 سيناريوهات. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — الدومين يتظبط عبر متغيّر بيئة بدل Hardcoding في الكود

### السبب
Vite برفض دومين غير مُصرَّح (`allowedHosts`) — إجراء أمان حقيقي. واجهة UI مش هتفيد بما إن الإعداد بيتقرا وقت البدء بس.

### الحل
متغيّر بيئة جديد (`MKDD_ALLOWED_HOSTS`)، بنفس نمط `OPENHANDS_URL` الموجود — مفيش دومين مكتوب جوّه الكود المصدري خالص.

### التحقق
محاكاة حية لـ3 سيناريوهات (قائمة دومينات، غير موجود، فاضي). `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — البنية التحتية الكاملة للـPWA والإشعارات الحقيقية (جاهزة، تنتظر HTTPS)

### التنفيذ
أيقونات PWA، manifest.json، Service Worker، نظام Push كامل بالـBackend (مفاتيح VAPID، تخزين اشتراكات، إرسال)، مربوط بنقطتي إطلاق حقيقيتين (رسالة جديدة + تقرير جديد)، زرار تفعيل في الهيدر يظهر تلقائيًا فقط عند توفر الدعم.

### 3 أخطاء حقيقية اتلقت بالاختبار الحي
1. مسار استخراج نص الرسالة كان غلط (اتأكد بتشغيل normalizeEvent فعليًا).
2. متغيّر غير موجود (`language`) في الـBackend.
3. **خطير:** دالة إرسال بترمي استثناء غير معالَج قادر يوقف السيرفر — اتلقى بمحاكاة فشل حقيقي، واتصلح، واتعاد نفس الاختبار للتأكد.

### التحقق
اختبارات حية كاملة لكل جزء من دورة الحياة. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

### مؤجَّل
التفعيل الفعلي (ظهور الزرار، عمل الاشتراكات) بانتظار HTTPS.

---

## 2026-08-17 — إشعار داخل التطبيق للرسائل الجديدة (نقطة حمراء على كارت الموظف)

### السياق
اكتشاف: التقارير أصلًا عندها إشعار جاهز (شارة العدد). الجديد هو إشعار للرسائل.

### التنفيذ
endpoint خفيف جديد لآخر رسالة، تتبّع "آخر مشاهدة" في localStorage، نقطة حمراء على كارت الموظف تختفي تلقائيًا بعد الفتح.

### التحقق
اختبار حي للـendpoint + محاكاة منطقية لـ5 حالات حدّية. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

### مؤجَّل
الإشعار الحقيقي (يظهر والموبايل قافل) يحتاج HTTPS أولًا — نفس قيد الـPWA.

---

## 2026-08-17 — إصلاح شكل صندوق الإدخال البيضاوي المتطرف مع النص متعدد الأسطر

### السبب
`border-radius: 999px` بيشتغل تمام لسطر واحد، لكن على صندوق أطول (متعدد الأسطر) بيخلق شكل بيضاوي متطرف.

### الحل
تقليل القيمة لـ22px ثابتة — تفضل شكلها كويس في كل الحالات.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — زرار اعتماد المرحلة: توقيع المالك الرسمي

### اكتشاف
الـBackend والـAPI كانوا موجودين بالكامل من شغل سابق، بس مش مربوطين بأي زرار في الواجهة.

### التنفيذ
زرار "اعتماد" يظهر بس للبوابة الحالية، نافذة تأكيد إجبارية، رسائل خطأ مترجمة (بدل الأكواد التقنية الخام)، `approvedBy: "Owner"` كتوقيع ثابت.

### التحقق
سيناريو حي كامل: عائق مفتوح → رفض → حل → اعتماد ناجح → انتقال تلقائي للبوابة التالية. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — تقارير الموظفين: إضافة حقل التفاصيل الكاملة

### السبب
التصميم الأصلي كان بيحفظ عنوان قصير بس، مفيش مكان للتقرير الكامل المفصّل.

### الحل
حقل `details` جديد اختياري (Backend+Frontend+تعليمات الموظفين)، عرض واضح تحت العنوان.

### التحقق
اختبار حي أكّد حفظ النص الكامل بدون اقتطاع. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — ميزة جديدة: نظام تقارير عابر للموظفين (Cross-Employee Reports)

### السياق
سيناريو حقيقي (كيرلس بلّغ عن نقص في متطلبات مريم) كشف الحاجة لنظام تتبّع تقارير عام بين أي موظف وموظف، منفصل عن نظام الملاحظات الرسمي (مقصور على المراجعين الأربعة).

### التصميم (بعد 3 جولات نقاش)
نظام عام لكل المراحل، لكن كل تقرير مرتبط ببوابة معينة. سير عمل 3 مراحل: تقرير → رد الموظف (نفّذ/رفض بسبب إجباري) → قفل نهائي من المالك.

### Backend
`workflow-state.mjs` + `workflow.mjs` (handleReports) — تم التحقق الحي الكامل لدورة الحياة.

### Frontend
البوابات الأربعة بقت قابلة للدوس، نافذة منبثقة تعرض التقارير، زرار قفل نهائي للمالك.

### تعليمات الموظفين
قسم جديد في AGENTS.md — اكتُشف وصُحِّح خطأ إشارة توثيقي ذاتي أثناء الكتابة.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح، اختبار حي شامل لكل السيناريوهات.

---

## 2026-08-17 — إصلاح انعكاس ألوان خط سير المشروع في الواجهة الإنجليزية

### السبب
`document.documentElement.dir` بتتحول لـ`ltr` فعليًا مع الإنجليزية، فمواضع البوابات بتنعكس بصريًا، لكن اتجاه التدرج اللوني كان ثابت (مبني على RTL بس).

### الحل
اتجاه التدرج بقى ديناميكي حسب اللغة — `to left` للعربية، `to right` للإنجليزية.

### التحقق
محاكاة رياضية حية لكلا اللغتين. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — ارتفاع ثابت موحّد للفوتر عبر كل كروت الفريق

### السبب
`min-height` كانت حد أدنى بس، بتكبر تلقائيًا لو النص احتاج سطرين — ارتفاع متفاوت حسب طول اسم الوظيفة.

### الحل
`height: 38px` ثابتة فعليًا + `flex-shrink: 0` + `overflow: hidden` احترازي.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — الفوتر يمتد لآخر الكارت ويأخذ الحواف الدائرية

### السبب
صندوق الصورة بحجم ثابت، فمفيش حاجة تاخد المساحة الفاضية الزيادة (لو كارت مجاور أطول) — فضلت فاضية تحت الفوتر.

### الحل
`flex: 1 1 auto` على صندوق الصورة (مش الصورة الدائرية نفسها) — يمتص المساحة الفاضية، الفوتر يلتصق بآخر الكارت تلقائيًا.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إعادة تصميم كارت الموظف بالكامل: هيدر/صورة/فوتر (حل جذري)

### السياق
مشكلة فيضان البادج تكررت رغم إصلاحين متتاليين — كلها أعراض لنفس السبب: حشر أيقونة+نص جنب بعض في عمود ضيق جدًا.

### الحل الجذري
هيدر بعرض الكارت الكامل (اسم + أيقونة وحيدة)، صورة بسيطة، فوتر بعرض الكارت الكامل (نص الوظيفة لوحده، min-height ثابتة موحّدة). إلغاء تكرار الأيقونة نهائيًا.

### تنظيف شامل
حذف كل الكود القديم بعد التأكد من عدم استخدامه في أي مكان (بحث نصي شامل قبل الحذف).

### تعارض CSS إضافي اتلقى
قاعدة عامة كانت هتطغى على تنسيق الهيدر الجديد — تم تعديلها مباشرة.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح، حجم CSS قلّ فعليًا.

---

## 2026-08-17 — إصلاح فيضان بادج الوظيفة برّه حدود كارته

### السبب
البادج Flex container وعنصر Flex في نفس الوقت — بدون min-width:0 كانت ترفض الانكماش لعرض الكارت.

### الحل
`width: 100%; min-width: 0;` صريحة بدل `width: fit-content`.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — بادج الوظيفة يلف لسطرين بدل قطع الأسماء الطويلة

### السبب
أسماء الوظائف الطويلة كانت بتتقطع بـ"..." بسبب ضيق عرض الكارت.

### الحل
السماح للنص باللف الطبيعي بدل القص، حواف مدورة عادية بدل دائرية كاملة، الكارت الأب يتمدد تلقائيًا (min-height: auto).

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — توسيع بادج الوظيفة لشريط الفريق في صفحة المشروع

### التنفيذ
بادج مصغّر بنفس تصميم شاشة المحادثة (موف صلب، نص أبيض، أيقونة) بدل النص الرمادي القديم.

### تعارض CSS اتلقى
قاعدة `.project-home .employee-card-info span` أعلى تحديدًا كانت بتطغى بصمت — تم حذفها (كود ميت) واستبدالها بoverride صريح.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — بادج الوظيفة: لون موف صلب، أيقونة، نص أبيض، خط أكبر

### تنظيف بنيوي
استخراج `ROLE_ICONS` لملف مشترك (`src/utils/roleIcons.ts`) — مصدر واحد بدل تكرار، إزالة 10 استيرادات غير مستخدمة.

### التنفيذ
خلفية موف صلبة (`--mkdd-accent`)، نص أبيض واضح، أيقونة الوظيفة، خط من 11px لـ13px.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — الوظيفة في بار الموظف تتحول لبادج مميز متمركز فوق

### التنفيذ
إعادة هيكلة البار لعمود بصفين: بادج الوظيفة (متمركز، خلفية ملوّنة، حواف دائرية) فوق، وصف الرجوع/الصورة/الاسم/الأزرار تحته. إزالة الكود القديم بعد التأكد من عدم استخدامه.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — مساحة أكبر لصندوق الإدخال + إصلاح نص فقاعتي في الوضع الفاتح

### التنفيذ
padding-bottom من 10px لـ20px لصندوق الإدخال. قاعدة صريحة قوية (`article.me *`) تفرض لون أبيض على كل عناصر فقاعة "أنا" الفرعية، بدل الاعتماد على وراثة قد تنقطع عبر سلسلة عناصر Markdown.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

### مؤجَّل
تحسين بار الموظف العلوي — بانتظار توضيح دقيق.

---

## 2026-08-17 — إصلاح تباين الوضع الداكن في المحادثة + تخفيف حدة فقاعة "أنا"

### السبب
فروق ضئيلة جدًا بين ألوان طبقات الوضع الداكن (خلفية/فقاعات/بارات)، ولون فقاعة "أنا" أزرق مشبع بالكامل ومرهق للعين.

### الحل (خاص بالوضع الداكن بس)
خلفية منطقة الرسائل → `--mkdd-surface-hover` (فرق حقيقي). فقاعة "أنا" → لون ممزوج أهدأ عبر `color-mix()`.

### التحقق
حساب اللون الناتج فعليًا (`#1f3a8b`، أهدأ لكن لسه واضح). `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — تصميم تطبيق محادثة حقيقي: فصل لوني + صندوق إدخال بيضاوي

### السبب
كل الشاشة كانت بنفس اللون بالضبط، بدون تمايز بصري بين البارات ومنطقة الرسائل.

### الحل
منطقة الرسائل بلون أفتح (`--mkdd-surface`)، صندوق إدخال بيضاوي بالكامل، أزرار دائرية، حد علوي للفصل البصري.

### تعارضات CSS اتلقت
3 قواعد `.project-view .composer ...` أعلى تحديدًا كانت بتطغى بصمت على التصميم الجديد — تم تحديدها وتصحيحها.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إزالة الهيدر العام من شاشة المحادثة

### التنفيذ
إخفاء AppHeader وBreadcrumbBar في شاشة المحادثة بس، إضافة زرار رجوع جديد جوّه بار الموظف نفسه (عشان مانفقدش وظيفة الرجوع)، تصحيح padding-top، تصميم بصري لبار الموظف كهيدر مصغّر.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إصلاح لصق آخر رسالة + رفع ملفات عامة للمحادثة

### الرسالة اللاصقة
حل ذكي (`--composer-height` ديناميكي) كان موجود بالفعل لكن قاعدة تانية بنفس درجة التحديد كانت بتطغى عليه بترتيب المصدر — ربطنا القاعدة الصحيحة بالمتغيّر.

### الملفات العامة
تحقّقنا من الكود المصدري: OpenHands نفسها بتدعم نص وصور بس في الرسائل، مفيش نوع "ملف". الحل: زرار رفع ملف يستخدم آلية الرفع الموجودة (uploads/) + رسالة تلقائية للموظف.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — فحص جاهزية VM جديد: لقيت وصلّحت فجوة توثيقية في setup.sh

### السياق
سؤال المستخدم "هل السكريبت مظبوط لـVM جديد؟" دفع لفحص فعلي شامل بدل افتراض.

### النتيجة
كل شيء مؤتمَت (موظفين + condenser + بورتات حية + نشر تلقائي) عدا بروفايل LLM نفسه (قرار تصميمي واعٍ). لكن لقيت فجوة حقيقية: `Max Input Tokens` على بروفايل LLM موثّق بس في `BUGS_AND_FIXES.md` (سجل تاريخي)، مش في `setup.sh` الفعلي.

### الحل
تذكير مفصَّل في مكانين بـ`setup.sh` — رسالة التوجيه ورسالة الختام الدائمة.

### التحقق
`bash -n` نظيف، `npm test` → 77/77، `tsc -b` → نظيف.

---

## 2026-08-17 — مجلدات ملفات المشروع قابلة للطي/الفتح

### التنفيذ
`expandedFolders` (Set) تتبع المجلدات المفتوحة، فلترة تعرض بس العناصر اللي أبوها المباشر مفتوح، مجلدات بقت أزرار قابلة للدوس مع سهم دوّار يدل على الحالة. كل ده مبني من نفس البيانات المسطّحة الموجودة بالفعل — بدون تعديل backend.

### التحقق
محاكاة حية بـ3 حالات (لا شيء/مستوى واحد/مستويين مفتوحين) أكّدت صحة الطي المتدرج. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إعادة تصميم ملفات المشروع: كروت ملوّنة بأيقونة لكل نوع

### التنفيذ
5 ألوان مميزة حسب نوع الملف (مجلد=برتقالي، صورة=وردي، كود=بنفسجي، نص=أخضر، عام=رمادي)، تحويل القائمة النصية لكروت مستقلة بنفس أسلوب بقية الصفحة.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إعادة هيكلة جذرية للخط الواصل (تدرّج ثنائي اللون) + توهج نابض

### السبب الحقيقي لفشل المحاولة السابقة
مدّينا الخط 8px بس (الفجوة)، لكن المسافة الحقيقية لمركز الدائرة التالية أكبر بكتير.

### الحل
خط واحد متصل شامل خارج كل الأعمدة، بخلفية `linear-gradient` محسوبة رياضيًا (كل قطعة نصفين بلوني المرحلتين المجاورتين). تم التحقق الرياضي حيًا قبل البناء.

### باگ RTL اتلقى
`linear-gradient` بيستخدم اتجاهات فيزيائية ثابتة مش بتتأثر بـ RTL تلقائيًا — تم تصحيح الاتجاه.

### التوهج
إعادة استخدام نفس نمط النبضة المُستخدَم لبطاقة الموظف النشط، على دائرة المرحلة الحالية.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — خط متصل بالكامل + 4 ألوان مميزة + دائرة منتظرة بلون الأيقونة

### الخط
امتداد إضافي (-8px) يعبر الفجوة بين الأعمدة بالكامل.

### الألوان
4 ألوان مميزة فعليًا (أزرق/أحمر/برتقالي/أخضر) بدل 3 درجات من نفس الأزرق، عبر متغيّر CSS واحد مشترك (`--gate-color`) بين الأيقونة والدائرة المنتظرة — يضمن التطابق دايمًا.

### قرار تصميمي
بعد سؤال توضيحي، المعتمدة/الحالية تفضل بألوان الحالة (أخضر/بنفسجي)، بس المنتظرة تاخد لون الأيقونة.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إصلاح قاطع لتمركز دوائر المراحل مع الأسماء والأيقونات

### السبب الجذري الحقيقي
`margin: 0 auto` على الدائرة و`flex: 1 1 auto` على الخط جنبها كانوا بيتنافسوا على نفس المساحة الفارغة — تنافس غير مضمون النتيجة بدقة في Flexbox.

### الحل القاطع
الدائرة تتمركز عبر `justify-content: center` على الصف (بدون منافس)، والخط بقى `position: absolute` مستقل تمامًا.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إعادة تصميم صفحة المشاريع الرئيسية بأسلوب مرجعي، ببيانات حقيقية

### التحقق الأولي
`/api/projects` مفيهاش "آخر تحديث" أو "مرحلة حالية" أصلًا — تأكدنا من الحقول المتاحة فعليًا (created_at/updated_at) قبل البناء عليها.

### التنفيذ (Backend)
`currentGate` من workflow state الحقيقي، `lastActivityAt` من أحدث updated_at بين كل محادثات المشروع — تم التحقق الحي إن الاختيار دقيق (الأحدث فعليًا، مش عشوائي).

### باگ بنية اتلقى
استيراد دائري محتمل عند إضافة `WorkflowGateName` — تم الحل بنقل التعريف لمكانه الصحيح وإعادة التصدير.

### التنفيذ (Frontend)
كارت جديد بأيقونة، اسم، نوع+آخر تحديث، شارة مرحلة ملوّنة، وصف أفاتار حقيقي.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — تصحيح خطأ توثيقي: إصلاحان (#33، #34) كانا موثّقين مرتين

### السياق
سؤال المستخدم "أنت موثّق كل شيء؟" دفع لفحص فعلي بدل افتراض. اكتُشف تكرار حقيقي.

### الإصلاح
حذف النسخة المكررة من #33/#34، الاحتفاظ بالأشمل. تحقق برمجي: تسلسل 1-79 (ثم 1-80 بعد توثيق هذا الإصلاح نفسه) بدون أي فجوة أو تكرار. `CHANGELOG.md` فُحص أيضًا ووُجد نظيفًا.

---

## 2026-08-17 — إصلاح شامل لدوائر مراحل سير المشروع والخط الواصل

### المشاكل
دوائر صغيرة، خط رفيع وباهت، توهج مبالغ فيه، وخط بيدخل جوّه الدائرة الأولى فعليًا.

### السبب الجذري للتداخل
`margin-inline: -4px` سالبة على الخط كانت بتسحبه ليمتد جوّه مساحة الدائرة.

### الحل
تكبير الدوائر (36px→48px)، تقوية الخط (2px باهت → 3px واضح)، تقليل التوهج نسبيًا، وإزالة الـ margin السلبية بالكامل.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إصلاح محاذاة أيقونات بوابات سير المشروع مع الأسماء الطويلة

### السبب
الأيقونة والاسم كانوا جنب بعض في نفس السطر — "واجهة وتجربة المستخدم" (اسم طويل بيتلف لـ3 أسطر) خلّى الأيقونة تتمركز مع السطر الأول بس، مش الكتلة بالكامل.

### الحل
الأيقونة تظهر فوق الاسم دايمًا (Column بدل Row) — حل جذري بغض النظر عن طول الاسم.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — أيقونات بوابات سير المشروع: تكبير + ألوان مميزة (مع اكتشاف تعارض ألوان)

### اكتشاف مهم
`--mkdd-info` و`--mkdd-accent` نفس القيمة بالضبط في الوضع الداكن — أول محاولة استخدام `info` للمتطلبات كانت هتخلي لونين "مختلفين" يبانوا متطابقين فعليًا. تم التصحيح لاستخدام 3 درجات أزرق مميزة فعليًا (accent-deep/accent/accent-soft) + أخضر للإنتاج.

### باگ تقني
خطأ TypeScript حقيقي عند تمرير `className` لمكوّن أيقونة بنوع عام — تم تحديد النوع بدقة.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — تكبير الأيقونات وألوان مميزة في صفحة المشروع الجديدة

### التنفيذ
حجم أكبر لكل أيقونات الكروت الجديدة، مع ألوان تعكس معنى كل كارت (أزرق=معلوماتي، بنفسجي=مراجعات، أحمر=عوائق، أصفر=ملاحظات). تكبير أيقونات القوائم الفرعية وبوابات المشروع كمان.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-17 — إعادة تصميم صفحة المشروع: Dashboard منظم ببيانات حقيقية 100%

### السياق
صفحة المشروع (بعد شريط الفريق) كانت "شكله وحش ومش منظم". طلب تبنّي أسلوب تصميم مرجعي، ببيانات حقيقية فعليًا لا موك.

### التنفيذ
حالة الفريق المجمّعة (عدادات حقيقية من executionStatus)، مراجعات إلزامية كقائمة أضغط، عوائق وملاحظات كقوائم تفصيلية حقيقية (عنوان + مين + وقت نسبي) — كل البيانات كانت موجودة بالفعل، بس مش معروضة.

### أدوات جديدة
`formatRelativeTime.ts`، `getFindingStatusLabel`.

### باگ اتلقى أثناء التنفيذ
استبدال غير دقيق حذف دالة `getReviewStatusLabel` الموجودة بالكامل — اتصلح فورًا بعد ما `tsc -b` كشفه.

### مؤجَّل بالاتفاق
خطة العمل والنشاط المجمّعين من كل الموظفين (يحتاجوا تجميع backend إضافي).

### التحقق
محاكاة حية لحسابات التجميع أكّدت صحتها. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — سير المشروع: الفجوة الحقيقية (مفيش موظف بيحدّث الحالة) + تحسينات شكل

### السياق
"سير المشروع" مش بيجيب بيانات حقيقية رغم شغل حقيقي مستمر للموظفين.

### الاكتشاف
النظام الخلفي حقيقي 100% (endpoints فعلية، تخزين دائم، قواعد تحقق صارمة)، لكن **مفيش ملف موظف واحد من الـ14 بيذكر إزاي أو إمتى يستخدمه**.

### الحل
قسم جديد في `AGENTS.md` (§6) يوضح مين مسؤول عن كل تحديث ومتى، مع أوامر curl فعلية بأسماء حقول صحيحة (من كود الـ Backend مباشرة).

### باگ اتلقى أثناء التعديل
تكرار عنوان بالغلط أثناء الاستبدال — اتصحح فورًا، الترقيم اتأكد سليم 1-14.

### تحسينات الشكل
أيقونات لكل بوابة ونوع مراجعة، وترجمة حالة المراجعة (كانت بتظهر بالإنجليزي الخام حتى في الواجهة العربية).

### التحقق
قراءة حية لكل الـ14 موظف بعد التعديل، `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

### باقي خطوة
تشغيل `bootstrap-employees.mjs` على الـ VM.

---

## 2026-08-16 — إصلاح صورة بار الشات + ميزة تحديد موضع الصورة عند الرفع (سحب + زوم)

### إصلاح 1
نفس مشكلة "الصورة الفالتة من الدائرة"، هذه المرة في `.chat-employee-avatar` — أضفنا `overflow: hidden` احترازي.

### ميزة 2 (جديدة)
مكوّن `AvatarPositioner.tsx` — بعد اختيار الصورة، نافذة منبثقة تسمح بالسحب والتكبير/التصغير قبل الحفظ (Pointer Events API، شريط تمرير للزوم). التصدير عبر `<canvas>` بدقة 320×320، بنفس تنسيق الـ Backend الحالي (بدون أي تعديل backend).

### التحقق
محاكاة رياضية حية أكّدت صحة حسابات التغطية/التصدير. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — إعادة تصميم كاملة: بيانات الموظف كنافذة منبثقة بدل لوحة مدمجة

### السياق
بعد سلسلة إصلاحات متتالية لمشكلة سكرول اللوحة المدمجة، حل جذري مختلف: نافذة منبثقة منفصلة تمامًا عن سياق سكرول الشات.

### التصميم الجديد
زرار "بيانات الموظف" (بنفس ستايل "محادثة جديدة")، لونه يتغيّر حسب حالة النشاط. الضغط يفتح نافذة منبثقة (Bottom Sheet)، حدودها ملوّنة بنفس الحالة، بـ3 تبويبات منفصلة: التكلفة، خطة العمل، النشاط.

### لماذا هذا يحل المشكلة من جذورها
النافذة خارج سياق سكرول صفحة الشات تمامًا — تعارض إيماءات اللمس بقى مستحيل بنيويًا، مش مجرد مُصلَّح.

### باگ اتلقى أثناء التنفيذ
`margin-inline-start: auto` على زرار "محادثة جديدة" كانت مصممة لآخر عنصر في الصف — بعد إضافة زرار جديد بعده، نقلنا المرونة لعنصر معلومات الموظف بدلًا من ذلك.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح، حجم CSS النهائي قلّ رغم إضافة ميزات جديدة.

---

## 2026-08-16 — خطة العمل مقفولة افتراضيًا (قابلة للطي) — تشخيص عملي

### السياق
مشكلة السكرول لسه موجودة رغم تأكيد نشر كل الإصلاحات السابقة وإعادة تشغيل mkdd-ui.

### الحل العملي
`WorkPlanPanel.tsx` بقت `<details>` قابلة للطي، مقفولة افتراضيًا — العنوان والنسبة المئوية ظاهرين دايمًا، قائمة المهام التفصيلية بس بتتفتح عند الحاجة. ده بيقلل المساحة الافتراضية بشكل كبير، وبيأكد لنا هل السبب فعلاً "المساحة" أو حاجة تانية.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — إصلاح تعارض إيماءات السكرول + تصغير أوضح للكارتين

### السبب (سكرول)
لوحة تفاصيل الموظف موجودة جوّه حاوية رسائل خارجية قابلة للسكرول بنفسها. الحاوية الخارجية عندها `overscroll-behavior: contain`، لكن اللوحة الداخلية لأ — فالسحب كان بيتسرب للخارج قبل ما يوصل لآخر محتوى اللوحة.

### الحل (سكرول)
إضافة `overscroll-behavior: contain` على اللوحة الداخلية.

### السبب (تصغير الكارتين)
القيم من الإصلاح السابق كانت صحيحة وموجودة فعليًا (اتأكد بفحص السيرفر مباشرة)، لكن الفرق كان صغير جدًا وغير ملحوظ.

### الحل (تصغير)
تقليل أوضح للـ padding والخطوط، شاملة نسخة الموبايل.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — إصلاح نهائي لتعارض CSS في صور الموظفين (بيضاوية بدل دائرية)

### السبب
قاعدتين CSS منفصلتين بتحددان أبعاد مختلفة لنفس عنصر الصورة (`52px` مقابل `42px`) — القاعدة الأكثر تحديدًا فازت بالأبعاد، لكن `border-radius: 50%` (في القاعدة الأقل تحديدًا) اتلغت من التعارض.

### الحل
توحيد كل خصائص الحجم والشكل في قاعدة واحدة بأعلى تحديد، حذف التكرار بالكامل، وتحديد أبعاد الصورة صراحة بالبكسل بدل النسبة المئوية.

### التحقق
تحقق حي من محتوى الملف على السيرفر أكّد وصول النشر (استبعد مشكلة نشر). `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — إصلاح صور الموظفين الفالتة برّه الدائرة في شريط الفريق

### السبب
أثر جانبي من إصلاح شارة الوظيفة (#62) — `overflow: visible` على حاوية الصورة (لإظهار الشارة) ألغى قص الصورة نفسها بالغلط كمان.

### الحل
`border-radius: 50%` مباشرة على الصورة نفسها، بدل الاعتماد على قص الحاوية الأب.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — إصلاح سكرول متداخل في لوحة الموظف + تصغير كارتي الحالة والتكلفة

### السبب
سكرول متداخل مزدوج (الحاوية الخارجية 40vh + سكرول داخلي منفصل لقسم النشاط 220px) — قسم النشاط كان بيتقطع من الظهور تمامًا أحيانًا.

### الحل
إلغاء السكرول الداخلي المنفصل، زيادة مساحة الحاوية الخارجية لـ65vh، تصغير padding/خط كارتي الحالة والتكلفة (شاملة نسخة الموبايل).

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — endpoint خفيف لخطة العمل: تقليل نقل بيانات ضخمة غير ضرورية

### السبب
`/api/chat/events` (719 كيلوبايت لمحادثة كيرلس الحقيقية) كانت بتتجاب كاملة كل 5 ثواني لكل الـ14 موظف، بس عشان استخراج `work_plan` الصغيرة.

### الحل
`GET /api/chat/work-plan` (جديد) — نفس منطق الجلب، لكن بيرجّع `work_plan` بس. `useProjectTeamStatus` بقى يستخدمه بدل `fetchEvents` الكاملة.

### التحقق
اختبار حي: استجابة كاملة 9.5 كيلوبايت مقابل استجابة خفيفة 18 بايت (تقليل يفوق 99%). `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — الحل النهائي الصحيح: فصل الحالة السريعة عن التكلفة الإجمالية

### السياق
#64 قلّل عدد الطلبات لكن الطلب المتبقي كان النسخة البطيئة (مسح شامل بدون توقف مبكر) — النتيجة أسوأ من الأصل، مش أفضل.

### الحل
`/api/conversation` رجعت بالضبط لشكلها الأصلي السريع. `/api/projects/{slug}/total-cost` (endpoint جديد) بيعمل مسح شامل واحد للمشروع كله (مش لكل موظف)، بمعدل تحديث كل 30 ثانية بدل 5. `findAllAuthorizedConversations` القديمة اتشالت (كود ميت).

### النتيجة
14 طلب سريع كل 5 ثواني + طلب بطيء واحد كل 30 ثانية = أداء أفضل من كل المحاولات السابقة (شامل الحالة الأصلية قبل أي إصلاح).

### التحقق
اختبار حي أكّد: `/api/conversation` نظيفة تمامًا، `/api/projects/.../total-cost` رجّع `11.25` صح. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — إصلاح: مضاعفة الطلبات بعد إصلاح التكلفة تسببت في تعطل الواجهة

### السبب
إصلاح #63 (حساب `totalCost`) كان بيعمل طلبين منفصلين لكل استدعاء. بما إن الواجهة بتسأل عن كل الـ14 موظف كل 5 ثواني، ده ضاعف عدد الطلبات الفعلي من 14 لـ28 كل دورة — ضغط أدى لتعطل ظهور البيانات بالكامل.

### الحل
دمج الطلبين في طلب واحد بس (`findAllAuthorizedConversations`)، مع استنتاج "المحادثة النشطة" كأول عنصر في النتيجة (الترتيب الافتراضي مؤكَّد: الأحدث أولًا).

### التحقق
عدّاد طلبات حي أكّد: طلب واحد بالظبط بدل اتنين. `totalCost` لسه صحيح 100% (لم يتأثر). `npm test` → 77/77، `tsc -b` → نظيف.

---

## 2026-08-16 — إصلاح: "بدء محادثة جديدة" بيخصم تكلفة المحادثة القديمة من الإجمالي

### السبب
حساب التكلفة الإجمالية كان بيعتمد على "المحادثة النشطة الحالية بس" (`findAuthorizedConversation`، بيرجّع محادثة واحدة). بعد إضافة "بدء محادثة جديدة"، تكلفة أي محادثة قديمة (رغم إنها اتصرفت فعليًا) بقت غير مرئية في الإجمالي.

### الحل
فصل واضح بين سؤالين: "أي محادثة أفتح للشات" (يفضل الأحدث بس) مقابل "كام صُرف فعليًا" (دالة جديدة `findAllAuthorizedConversations` بترجّع كل المحادثات، ومجموع تكلفتها كلها = `totalCost` الحقل الجديد).

### التحقق
اختبار حي بمحاكاة السيناريو بالضبط (محادثة قديمة 10.75$ + جديدة 0.50$) → `totalCost: 11.25` صحيح. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

---

## 2026-08-16 — إصلاح شريط فريق المشروع: تراكب الوظائف + حدود + أيقونة دور

### السبب
`.employee-card-info` معندهوش عرض صريح — في flex container عمودي، الأطفال بتاخد عرض محتواها الطبيعي، فالنص الطويل كان بيفيض برّه حدود البطاقة ويتراكب مع الجيران.

### الحل
`width: 100%` على `.employee-card-info`، استعادة الحدود (كانت `border: none` صراحة)، خريطة أيقونات جديدة لكل دور كشارة على الصورة الشخصية.

### باگ فرعي
`FaDraftingCompass` مش موجودة فعليًا في الحزمة المُثبَّتة — اتصلحت لـ`FaCompassDrafting` بعد تحقق برمجي من كل الأيقونات المُستخدَمة.

### التحقق
`npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء.

---

## 2026-08-16 — إصلاح تجاوز حد السياق + ميزة "بدء محادثة جديدة"

### السياق
كيرلس واجه `litellm.APIError: input exceeds context window`. المستخدم طلب حل حقيقي من إعدادات OpenHands، مش مجرد تجاهل.

### التشخيص
- زرار "Compact context" في OpenHands نفسه فشل بـ"No condenser configured" — مفيش Condenser مُفعَّل خالص.
- الرقم المعروض في الواجهة (1.1M توكن) رقم افتراضي عام، مش الحد الحقيقي (موثّق رسميًا: GitHub Issue #9573).
- بحث دقيق (بناءً على طلب المستخدم) أكّد الحد الحقيقي لموديل `GPT-5.6 Sol` عبر ChatGPT Codex = **258,400 توكن فقط**.

### الحل
- `max_input_tokens: 200000` في بروفايل الـ LLM.
- Condenser مُفعَّل صراحة بـ `max_tokens: 160000`.
- `bootstrap-employees.mjs` مُحدَّث ليشمل الإعداد ده تلقائيًا للمستقبل.

### ميزة جديدة: بدء محادثة جديدة
المحادثات القديمة فضلت عالقة على الإعدادات القديمة (مبدأ معروف من قبل). بنينا `POST /api/chat/new` (بيفرض إنشاء محادثة جديدة دايمًا)، مبني على تأكيد حي إن ترتيب البحث الافتراضي "الأحدث أولًا" — فمفيش داعي لأي تعديل في منطق البحث الموجود. زرار "محادثة جديدة" في الشات، مع تأكيد قبل التنفيذ.

### التحقق
اختبار حي كامل لكل خطوة (فحص GET بعد كل POST، استدعاء `/api/chat/new` مرتين لتأكيد إنشاء محادثتين منفصلتين فعليًا). `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء.

---

## 2026-08-16 — تحديد صريح لمسؤولية إنشاء ريبو المشروع على GitHub

### السياق
الاتصال بـ GitHub عبر MCP متاح للموظفين بالفعل، لكن مفيش تحديد صريح مين المسؤول عن إنشاء الريبو ومتى.

### الحل (ثلاث طبقات)
- `AGENTS.md` §2: باجوش مسؤول، بعد اعتماد Gate 1، مسجَّل في `project-context.md`.
- `product-manager.md`: قسم "Repository Creation" تفصيلي.
- `github-repo-instructions.mjs`: تعليمات مشتركة لكل الموظفين — تأكد من الريبو قبل أي شغل Git.

### اكتشاف إضافي
قائمة أقسام `project-context.md` القياسية معندهاش حقل "Repository" خالص. تم إصلاحه في كل الـ15 ملف (14 موظف + الأوركستريتور).

### التحقق
قراءة حية لكل الـ14 موظف، تأكيد سلامة `product-manager.md` بعد التعديل اليدوي والسكريبتي معًا. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح.

### باقي خطوة
تشغيل `bootstrap-employees.mjs` على الـ VM.

---

## 2026-08-16 — رفع ملفات للمشروع مباشرة + تحسينات جذرية على تعليمات كيرلس ومريم

### ميزة الرفع الجديدة
`server/routes/project-upload.mjs` — الملفات بتتحط في `{المشروع}/uploads/`، متاحة فورًا لأي موظف (نفس المجلد المشترك)، وبتظهر تلقائيًا في قائمة الملفات والمعاينة الموجودة بالفعل (بدون أي تعديل إضافي). زرار رفع في صفحة المشروع. حماية Path Traversal كاملة.

### تحسينات كيرلس (implementation.md)
- Mission مُعاد صياغته: "مُنفَّذ" = تجربة حقيقية مترابطة، مش Backend بس.
- قسم جديد "Upstream Consistency Check": لو شغل مريم/شيري ناقص أو متعارض، يوقف ويبلّغ المالك، مش يخمّن.
- قسم جديد "Full-Scope Completion and Honest Handoff": مفيش "خلصت" إلا 100% من النطاق مُغطّى، والعوائق تُسمّى بوضوح.

### تحسين مريم (ui-ux.md)
قسم جديد "Real Images, Not CSS Placeholders": صور حقيقية مرخّصة، ملفات فعلية داخل المشروع، فحص `uploads/` أولًا.

### التحقق
اختبار حي كامل (رفع → تأكيد على القرص → ظهور تلقائي في القائمة → معاينة → رفض traversal). 5 اختبارات دائمة جديدة. `npm test` → 77/77، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء.

### باقي خطوة
تشغيل `bootstrap-employees.mjs` على الـ VM.

---

## 2026-08-16 — مراجعة شاملة لكل ملفات الموظفين الـ14: ضمان تسليم احترافي حقيقي

### السياق
طلب صريح لمراجعة كل موظف والتأكد إن شغله محترف للموظف اللي بعده، بعد ملاحظات على شغل كيرلس.

### الاكتشاف الأول (الأهم)
`AGENTS.md` كانت عنده قاعدة "تحقق قبل الادعاء" بس محصورة في التوقيعات الرسمية والفحص البصري — مفيش قاعدة عامة تغطي أي ادعاء عادي أثناء الشغل اليومي (زي "السيرفر شغال"). أضفت §8 "Evidence-Based Claims" — بتنطبق على الـ14 موظف تلقائيًا بما إنهم كلهم بيرجعوا لـ`AGENTS.md`.

### الاكتشاف الثاني
قسم "Deliverables" كان ناقص في 10 من 14 موظف (شامل كيرلس نفسه). أضفته لكل واحد بمحتوى مخصص، مع تعزيز إضافي للقيود في الأدوار الأكتر ارتباطًا بالمشكلة (Implementation، DevOps، Integration Engineer، Release Manager).

### التحقق
`listEmployeeNames()` و`readEmployeeDisplayInfo()` نجحوا لكل الـ14 موظف بدون أي كسر في البنية. `npm test` → 72/72.

### باقي خطوة
تشغيل `bootstrap-employees.mjs` على الـ VM.

---

## 2026-08-16 — إعادة تصميم كاملة للمعاينة الحية: بورت مخصص دائم، بدون إعادة كتابة

### السياق
بعد سلسلة إصلاحات متتالية (كل واحد بيكشف مسار جديد ما اتغطاش)، طُلب حل نهائي شامل — مش رقعة تانية.

### القرار المعماري
التخلي الكامل عن "بورت مشترك + مسار فرعي + إعادة كتابة كل مسار". البديل: **بورت مخصص دائم لكل مشروع** (نطاق 4001-4020) + **تمرير خام 100%** (Raw TCP، بدون أي فحص محتوى) — بما إن التطبيق بيوصل من جذره الحقيقي، مفيش أي مسار مطلق ممكن يفلت. الفئة الكاملة من المشاكل بقت مستحيلة بنيويًا.

### التنفيذ
- `server/lib/live-port-registry.mjs`: سجل تخصيص دائم.
- `server/live-port-proxy.mjs`: 20 سيرفر TCP خام.
- `server/routes/project-live-port.mjs`: API تخصيص البورت.
- الرابط الكامل بيتبني في الفرونت إند (مش الموظف)، نفس درس `AUTOMATION_BASE_URL` القديم.
- النظام القديم (`live-proxy.mjs`) اتشال بالكامل — مفيش كود ميت.

### باگين اتلقوا وأتصلحوا
1. تخطيط أولي غلط لنطاق بورت خارجي مختلف لـ staging — كان هيكسر الرابط المبني في الفرونت إند. اتصلح بتوحيد الأرقام دايمًا.
2. **فجوة حقيقية في `npm test` نفسه**: النمط `server/**/*.test.mjs` مش بيغطي ملفات اختبار موجودة مباشرة جوه `server/` — اختبارات جديدة كانت بتنجح يدويًا بس تختفي من العدّاد الرسمي! تم إصلاح النمط.

### التحقق
اختبار حي لمنطق التمرير بعزل شبكي حقيقي (تجنّب تعارض بورت صامت اكتشفته أول محاولة)، اختبار حي لسجل التخصيص، 9 اختبارات دائمة جديدة. `npm test` → 72/72، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، كل ده بعد Prettier.

### باقي خطوات
تشغيل `bootstrap-employees.mjs`، تأكيد فتح نطاق البورتات على staging، تأكيد حي نهائي.

---

## 2026-08-15 — إصلاح استدعاءات API الحية (`/api/`) — تأكَّد بدليل كيرلس نفسه

### السياق
صفحة `/admin` فاضية رغم إن كل الملفات بترجع 200. كيرلس فتح الصفحة مباشرة من بيئته (بدون Proxy) واشتغلت صح 100% — دليل قاطع إن المشكلة عندنا احنا مش في شغله.

### السبب
استدعاءات `fetch('/api/...')` بتحصل وقت التشغيل جوه كود JavaScript، مش نص في HTML الأولي. `/api/` مكانتش في قائمة المسارات اللي بنعيد كتابتها، فالطلبات كانت بتروح لجذر موقعنا احنا بدل تطبيق كيرلس.

### الحل
- إضافة `/api/` لقائمة إعادة الكتابة.
- توسيع نطاق إعادة الكتابة ليشمل ملفات JavaScript، مش HTML بس.
- إعادة تسمية الدالة (`rewriteLiveAppHtml` → `rewriteLiveAppPaths`) لتعكس نطاقها الجديد.

### التحقق
اختبار حي كامل بمحاكاة السيناريو الحقيقي بالضبط (HTML → JS يحتوي `fetch("/api/admins/me")`) — النتيجة النهائية مطابقة تمامًا للمطلوب. اختباران دائمان جدد (شامل البناء الديناميكي للمسار). `npm test` → 76/76، `tsc -b` → نظيف، `npm run build` → ناجح، كل ده بعد Prettier.

---

## 2026-08-15 — إصلاح تعارض رؤوس HTTP في المعاينة الحية (502 رغم السيرفر شغال)

### السبب
`Error: Parse Error: Content-Length can't be present with Transfer-Encoding` — استجابات SSR الحقيقية (Next.js) بتستخدم `transfer-encoding: chunked`، وكود إعادة كتابة الـ HTML كان بيضيف `content-length` جديد من غير ما يشيل القديمة، فبقى عندنا الاتنين مع بعض (مخالفة HTTP)، وVite رفضت الاستجابة.

### الحل
حذف صريح لـ `transfer-encoding` (بالإضافة لـ `content-encoding` من الإصلاح السابق) قبل إعادة الإرسال.

### درس مهم
الاختبار السابق كان بسيط قوي (سيرفر وهمي بيستخدم `res.end()` مباشرة) فمعكسش سلوك استجابة Chunked حقيقية. اختبار طرف-لطرف جديد بيحاكي ده بدقة.

### التحقق
`npm test` → 74/74 (اختبار جديد يتأكد صراحة من عدم وجود `transfer-encoding` في الاستجابة)، `tsc -b` → نظيف، `npm run build` → ناجح، كل ده بعد Prettier.

---

## 2026-08-15 — تصحيح تعليمات المعاينة الحية: تشغيل السيرفر في الخلفية

### السبب
سيرفر كيرلس كان بيوقف من تلقاء نفسه بعد فترة. بحث في توثيق OpenHands الرسمي أكّد إن أداة الـ Terminal مبنية على `tmux` — أي سيرفر طويل الأمد شغال بشكل عادي (foreground) بيحجز الجلسة، وأي أمر تاني بعده بيقاطعه.

### الحل
`live-app-instructions.mjs`: توضيح صريح لضرورة `nohup ... &`، مع خطوة تحقق (`curl`) إجبارية قبل إعلان الجاهزية.

### التحقق
`npm test` → 73/73، `tsc -b` → نظيف، `npm run build` → ناجح.

### باقي خطوة
تشغيل `bootstrap-employees.mjs` تاني على الـ VM.

---

## 2026-08-15 — حل عام دائم لمشكلة "الصفحة الفاضية" في المعاينة الحية

### السياق
المعاينة الحية اشتغلت (200، HTML حقيقي)، لكن الصفحة بانت فاضية. المستخدم طلب حل عام وثابت في الكود، مش تعليم يدوي لكل موظف.

### السبب
التطبيقات (زي Next.js) بتحط روابط CSS/JS بمسارات مطلقة من الجذر. تحت `/live/{مشروع}/`، دي بتتفسّر غلط من المتصفح فكل الأصول بتفشل تحميلها.

### الحل
إعادة كتابة الروابط المطلقة **داخل استجابة HTML نفسها** — تقنية Reverse Proxy معروفة، عامة لأي إطار عمل بيستخدم نفس الاصطلاحات (`/_next/`, `/__nuxt/`, `/static/`, `/assets/`)، ثابتة في الكود فمش هتتأثر بأي تنصيب جديد.

### تفاصيل احترازية
منع الضغط عند الطلب من التطبيق (تجنّب كسر النص وقت إعادة الكتابة)، وحذف صريح لـ `content-encoding` من الاستجابة المُرسَلة.

### التحقق
اختبار حي كامل بسيرفر وهمي يحاكي استجابة كيرلس الحقيقية — الصفحة والملف المُعاد كتابة مساره نجحوا فعليًا. 5 اختبارات دائمة جديدة (شاملة اختبار دقة عدم التأثير على نص عادي). `npm test` → 73/73، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء.

### حدود معروفة
بيغطي المسارات المطلقة جوه HTML بس، مش روابط بعنوان كامل مُثبَّت داخل JavaScript نفسه.

---

## 2026-08-15 — معاينة حية لتطبيقات حقيقية (Reverse Proxy)

### السياق
كيرلس خلّص شغله، والمستخدم احتاج يراجع المشروع لايف (تطبيق حقيقي شغال)، مش مجرد ملفات ستاتيك زي نظام مريم.

### الحل المُتفَق عليه
- `server/routes/live-proxy.mjs`: Reverse Proxy حقيقي — الموظف بيشغّل سيرفره على بورت متفق عليه (`4001`)، وMKDD بيوجّه `/live/{مشروع}/{مسار}` مباشرة له.
- `server/lib/live-app-instructions.mjs`: تعليمات مشتركة لكل الـ14 موظف.
- `detectPreviewLinks.ts`: كشف موسّع لروابط `/live/` بكارت مختلف في الشات (رابط واضح، مش iframe مصغّر).

### قيود صريحة (مُتفَق عليها، مش عيوب مخفية)
بورت واحد مشترك (مشروع واحد بس في كل لحظة)، السيرفر لازم يكون شغال فعليًا، HTTP عادي بس (مش WebSocket الخاص بالتطبيق).

### باگين اتلقوا وأتصلحوا أثناء الاختبار
1. فحص وجود المشروع كان بيرجع نجاح كاذب (نقص فحص `fs.existsSync`).
2. استخراج hostname كان بيحصل وقت تحميل الملف (كسر الاختبارات) — تحويله لدالة كسولة.
3. رابط جذر التطبيق (أشيع استخدام) مكانش بيتلقط في كشف الروابط — الـ regex اتصلح.

### التحقق
اختبار حي كامل (سيرفر وهمي حقيقي، تأكيد النجاح/404/502)، 4 اختبارات دائمة لتحليل المسار، تحقق يدوي شامل (7 سيناريوهات) لكشف الروابط. `npm test` → 67/67، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، كل ده بعد Prettier.

---

## 2026-08-15 — تحسين شكل "ملفات المشروع" (أيقونات + توزيع)

### التعديل
كل صف ملف/مجلد بقى له أيقونة في شارة دائرية (مجلد، أو حسب نوع الملف — صور/كود/نص)، وعنوان القسم بقى له أيقونة بجانبه، زي أسلوب باقي أقسام الصفحة في الصورة المرجعية.

### التحقق
`npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

---

## 2026-08-15 — السبب الجذري الحقيقي والنهائي لفشل إرسال الرسائل

### التشخيص (جولة ثانية)
بعد إصلاح #48، المشكلة استمرت. تنبيهات تشخيصية شاملة كشفت: `404 - agent_profile_not_found` — نفس الخطأ اللي كتبناه إحنا في الإصلاح السابق.

### السبب الجذري
`GET /api/agent-profiles` (الجمع) بترجع UUID حقيقي كـ `id`، منفصل عن `name` (الاسم النصي). كود `/api/employees` بتاعنا كان بيستخدم `profile.id` (UUID) كمعرّف الموظف — يعني الفرونت إند كان بيبعت UUID من الأساس. إصلاح #48 كان مبني على افتراض غلط إن القيمة اسم نصي.

### الحل
`directory.mjs`: `id: profile.name` بدل `profile.id` — توحيد مع باقي المشروع (bootstrap، ملفات `.md`) اللي كله مبني على الاسم المستقر كمعرّف.

### التحقق
اختبار حي كامل أكّد إن `/api/employees` بترجّع `id: "architect"` دلوقتي، مش UUID. `npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء. كل تنبيهات التشخيص المؤقتة اتشالت.

---

## 2026-08-15 — إصلاح حرج: أي محادثة جديدة كانت مستحيلة بعد ترقية 1.13.0

### السياق
مشروع جديد — ولا موظف بيرد، مفيش أي محادثة اتعملت في OpenHands نفسه.

### التشخيص (تسلسل كامل بالأدلة)
1. تنبيهات تشخيصية مؤقتة (لا أدوات تطوير على الموبايل) كشفت رسالة خطأ غامضة `send_message_failed`.
2. باگ حقيقي: `chat.mjs` كان بيغلّف أي استجابة (نجاح/فشل) بنفس الشكل، فرسالة الخطأ الحقيقية كانت بتضيع.
3. بعد إصلاح كشف الخطأ، اختبار `curl` مباشر كشف السبب الحقيقي: `422 - Input should be a valid UUID` لحقل `agent_profile_id`.
4. تأكيد نهائي: كل بروفايل عنده `id` (UUID) منفصل عن `name` (السلاج)، وكودنا كان بيبعت السلاج مباشرة.

### السبب الجذري
تأثير جانبي من ترقية 1.13.0 (agent-server 1.42.1) — بقى يرفض السلاج النصي كـ `agent_profile_id`، بعكس النسخة القديمة. المحادثات القديمة فضلت شغالة (اتعملت قبل الترقية)، أي محادثة جديدة كانت هتفشل بنفس الطريقة.

### الحل
`resolveAgentProfileUuid()` جديدة في `chat.mjs` — بتجيب الـ UUID الحقيقي قبل إنشاء أي محادثة.

### التحقق
سيرفر OpenHands وهمي بمحاكاة دقيقة لسلوك الرفض الحقيقي + عملية وهمية بنفس توقيع اكتشاف مفتاح الجلسة — الطلب الكامل عدّى بنجاح بعد الإصلاح (كان بيفشل بـ 422 قبله). `npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء. تنبيهات التشخيص المؤقتة اتشالت.

---

## 2026-08-15 — موظفة رابعة عشر: شيري (Content & Localization Writer)

### إضافات
- `company-agents-definitions/content-writer.md`: تعريف كامل — توطين حقيقي مش ترجمة حرفية، تبدأ من البوابة 1 بالتوازي مع مريم، مساهمة مش بوابة إلزامية.
- `company-skills/content-writer/SKILL.md`: مهارة الدور.
- `company-orchestrator.md`: جدول التوجيه، مثال طلب مباشر، وصف دورة العمل — بدونها الأوركستريتور مكانش هيعرف شيري موجودة.
- تحديث قائمة الهوية المشتركة (سطر واحد) في كل الـ13 ملف موظف الموجودين، بسكريبت آمن (13/13 نجح).

### التحقق
اكتشاف حي (14 موظف، شيري متضمنة)، قراءة حية لبيانات العرض، بحث شامل يؤكد عدم وجود رقم "13" مُثبَّت في الـ frontend. `npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح.

### ملاحظة بنيوية
كل ملفات الموظفين فيها جزء مشترك مكرر بالكامل (تكرار غير مُبرَّر). لم يتم إعادة هيكلته كاملًا الآن (خطر عالي)، موثّق كتحسين مستقبلي في `BUGS_AND_FIXES.md` #47.

### باقي خطوة
تشغيل `bootstrap-employees.mjs` على الـ VM عشان شيري تتعمل كـ Agent Profile حقيقي.

---

## 2026-08-15 — إصلاح قصّ محتوى لوحة "تفاصيل الموظف والنشاط"

### السبب
`.chat-screen` بارتفاع ثابت و`overflow:hidden`، وقسم الرسائل بس عنده تمرير داخلي خاص. لوحة "تفاصيل الموظف والنشاط" (خطة العمل + النشاط) كانت بتقصّ محتواها الزايد تمامًا بدون أي تمرير، بدل ما تكون قابلة للتمرير.

### الحل
`max-height: 40vh` + `overflow-y: auto` للوحة نفسها — تمرير داخلي مستقل، من غير ما تاكل مساحة الرسائل تحتها.

### التحقق
`npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

---

## 2026-08-15 — تعليم الموظفين استخدام ميزة المعاينة الحقيقية (بدل روابط localhost)

### السياق
بعد إصلاح تعليق رفع الصور (#44)، ظهرت نفس مشكلة #41 الأصلية: الموظفين بيبعتوا لينكات `localhost` بدل `/preview/...` لأنهم مش عارفين الميزة موجودة.

### الحل
- `server/lib/preview-instructions.mjs` (جديد): تعليمات مشتركة، نفس نمط `time-context.mjs`.
- اتضافت لكل الـ13 موظف عبر `bootstrap-employees.mjs`.
- تأكيد إن الصيغة المُعلَّمة مطابقة تمامًا لنمط الكشف التلقائي في الشات (`detectPreviewLinks.ts`) — تكامل تلقائي بدون خطوة إضافية.

### التحقق
`npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، كل ده بعد Prettier.

### خطوة متبقية
تشغيل `bootstrap-employees.mjs` تاني على الـ VM عشان الموظفين الموجودين ياخدوا التعليمات الجديدة.

---

## 2026-08-15 — السبب الجذري الحقيقي لتعليق رفع الصور/الملفات في OpenHands نفسه

### التشخيص
رفع صورة من واجهة OpenHands الأصلية (مش MKDD) كان بيعلّق للأبد. اللوج الحي أثبت إن الطلب **مبيوصلش للسيرفر خالص**. بحث في الكود المصدري + مشاكل GitHub حقيقية كشف السبب: `docker/entrypoint.sh` بيحدد `AUTOMATION_BASE_URL` بقيمة افتراضية `http://127.0.0.1:$PORT` — عنوان غير قابل للوصول من أي جهاز غير الحاوية نفسها، وإحنا معملناش override له خالص.

### الحل
- `compose.yml`: `AUTOMATION_BASE_URL` بقى متغيّر بيئة قابل للتمرير لخدمة `agent-canvas`.
- `deploy/.env.staging`: القيمة الصحيحة للـ staging الحالي (`http://192.168.2.18:13000`).
- `setup.sh`: تحذير واضح لو المتغيّر مش متظبط.

### التحقق
تشخيص حي كامل (مراقبة لوج فعلية، تأكيد غياب أي طلب رفع، فحص الكود المصدري مباشرة لتأكيد القيمة الافتراضية). محتاج إعادة تشغيل الحاوية على staging للتأكيد النهائي.

---

## 2026-08-14 — إكمال البندين المتبقيين: ملفات المشروع + كارت معاينة داخل المحادثة

### إضافات
- `server/lib/project-paths.mjs`: استخراج منطق حماية مسار المشروع لملف مشترك، يستخدمه route المعاينة (#41) وroute قائمة الملفات الجديد معًا (بدل تكرار المنطق).
- `server/routes/project-files.mjs`: قائمة حقيقية لكل ملفات/مجلدات المشروع (`GET /api/projects/<مشروع>/files`)، باستثناء `.git`/`node_modules`/إلخ.
- قسم "ملفات المشروع" في `ProjectHomeScreen.tsx` — روابط معاينة مباشرة لكل ملف.
- `src/utils/detectPreviewLinks.ts`: كشف روابط `/preview/...` (بس روابطنا الخاصة، عمدًا — قرار أمان) جوه نص الرسالة، مع تنظيف علامات ترقيم نهاية الجملة.
- كارت معاينة (`<iframe sandbox="">` آمن بدون تنفيذ) يظهر تلقائيًا تحت أي رسالة فيها رابط معاينة، والضغط عليه يفتح المعاينة الكاملة في تبويب جديد.

### التحقق
اختبار حي كامل لقائمة الملفات (استثناء الضوضاء، مشروع غير موجود، رفض traversal) + 3 اختبارات دائمة. اختبار يدوي شامل (5 سيناريوهات) لكشف الروابط — بدون إطار اختبار frontend في المشروع حاليًا (نفس نمط `formatMessageTime.ts`). `npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

### الحالة
**كل البنود الأربعة المطلوبة (فقاعة الموظف، إرفاق صور، ملفات المشروع، كارت المعاينة) مكتملة الآن.**

---

## 2026-08-14 — فقاعة رد الموظف + إرفاق صور في المحادثة

### إصلاحات
- رد الموظف بقى في فقاعة حقيقية (كان بخلفية شفافة بدون حدود)، بلون مختلف عن فقاعة المستخدم لتمييز أوضح.
- المسافة أسفل الرسائل زادت (34px إضافية بدل 16px فوق الارتفاع الفعلي لصندوق الكتابة).

### إضافات (إرفاق صور)
- زر مشبك في صندوق الكتابة، معاينة قبل الإرسال (بإمكانية إزالة أي صورة)، حد أقصى 4 صور × 5 ميجا لكل رسالة.
- الشكل الفعلي لمحتوى الصور (`{type:"image", image_urls:[...]}`) اتأكد من كود `openhands-agent-canvas` نفسه قبل أي بناء.
- الصور بتظهر جوه فقاعة الرسالة (المرسلة والمستقبلة).

### باگ كامن اكتُشف وأُصلح
`sendChatMessage` كانت بترجّع استجابة خطأ السيرفر وكأنها نجحت (من غير `throw`) — فشل الإرسال كان بيحصل بصمت من غير أي تراجع. تم الإصلاح، وده فعّل منطق rollback الموجود بالفعل في `useConversation.ts` لأول مرة لحالات الخطأ الحقيقية.

### التحقق
9 اختبارات جديدة لمنطق التحقق من الصور (حجم/عدد/صيغة) + اختبار حي كامل لإرسال رسالة بصورة. المجموع: 60/60. `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

### باقٍ صراحة لجولة قادمة
- عرض ملفات المشروع في صفحة المشروع الرئيسية.
- ظهور روابط المعاينة (زي رابط مريم) بكارت معاينة جوه المحادثة نفسها.

---

## 2026-08-14 — معاينة ملفات المشروع + إصلاح تجربة المحادثة (سكرول/صندوق كتابة مرن)

### إضافات
- `server/routes/preview.mjs`: عرض ملفات أي مشروع مباشرة (`/preview/<مشروع>/<مسار>`) — بديل موثوق لروابط سيرفرات المعاينة المؤقتة اللي الموظفين بيشغّلوها (مش معروضة للـ VM أصلًا).
- `server/routes/preview.test.mjs`: 9 اختبارات دائمة، تشمل باگ أمني حقيقي اكتُشف وأُصلح أثناء الكتابة (اسم مشروع = `..` كان بيخترق حماية path traversal بالكامل).

### إصلاحات (تجربة المحادثة)
- تمرير تلقائي لآخر رسالة عند فتح المحادثة أو وصول رسالة جديدة.
- صندوق الكتابة بقى `<textarea>` بارتفاع تلقائي (Enter = سطر جديد، زر الإرسال بس هو اللي بيبعت).
- المسافة أسفل الرسائل بقت ديناميكية (`ResizeObserver` + متغيّر CSS) بدل رقم ثابت — حلّ مشكلة تغطية آخر رسالة عند نمو صندوق الكتابة.

### التحقق
- بحث دقيق للتمييز بين تطبيع `URL` التلقائي و`..` المُرمَّزة (اللي بتتخطاه فعليًا) — اختبار حي أثبت الفرق.
- `npm test` → 51/51 (9 جدد)، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

---

## 2026-08-14 — السبب الحقيقي لمشكلة "الصورة بتترفع لكن مبتظهرش": مسار Vite proxy ناقص

### التشخيص
بعد استبعاد كل الاحتمالات (السيرفر شغال، الملف محفوظ صح، Hard Refresh مش بيحل المشكلة) — لقينا إن `vite.config.ts` عنده تحويل لـ `/api` و`/ws` بس، ومفيهوش تحويل لـ `/avatars`. المتصفح (بيمر على Vite) كان بيفشل دايمًا، بينما `curl` المباشر على الـ backend كان بينجح دايمًا — وده فسّر كل الأدلة.

### الحل
إضافة قاعدة `/avatars` لـ `vite.config.ts`. تأكيد إن `/avatars` هو المسار الوحيد الغير-`/api` في السيرفر كله (بحث شامل).

### التحقق
تشغيل حي فعلي لـ backend + Vite dev server مع بعض، وطلب صورة عبر بروكسي Vite بالظبط زي المتصفح: `Status: 200, Content-Type: image/png`. `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح.

### أيضًا في نفس الجولة
إزالة زر رفع الصورة المكرّر من كروت "فريق المشروع" (بناءً على فيدباك صريح) — الزر بقى موجود بس في بروفايل الموظف من الـ Sidebar.

---

## 2026-08-14 — ترقية OpenHands Agent Canvas لإصدار 1.13.0

### السياق
إصدار جديد من OpenHands Agent Canvas (1.13.0)، تم التحقق الكامل قبل أي تنفيذ.

### التحقق (بأدلة حقيقية)
- استنساخ v1.13.0 الحقيقي من GitHub وتطبيق الـ patch المُخصَّص (شخصية الموظف) عليه فعليًا — نجح بدون تعارض، مطابق بايت-لبايت للنسخة الحالية.
- الحد الأدنى المطلوب لـ Agent Server: `1.28.0` (إحنا كنا فوقه بكتير على `1.40.1`).
- القيم الرسمية الجديدة (من `config/defaults.json`): `agentServer: 1.42.1`, `automation: 1.7.1`.
- آلية اكتشاف مفتاح الجلسة (`--session-api-key` عبر `/proc`) موجودة بالحرف في النسخة الجديدة.

### التنفيذ
- استبدال `openhands-agent-canvas/` بالكامل بمصدر 1.13.0، إعادة تطبيق الـ patch المُخصَّص.
- تحديث `compose.yml` و`setup.sh` بالقيم الجديدة.
- تصحيح تعليق في `setup.sh` كان بيدّعي تأكيد بناء حي — القيم الجديدة لسه محتاجة بناء واختبار فعلي على VM (بيئتي بدون Docker).

### حدود التحقق
كل التحقق على مستوى الكود المصدري فقط — **لسه محتاج `docker compose build` واختبار حي على بيئة معزولة** قبل أي اعتماد نهائي.

---

## 2026-08-14 — إصلاح صلاحيات الملفات (الموظفين مايقدروش يكتبوا في المشروع)

### السبب الجذري
`chown` في Dockerfile الخاص بـ `openhands-agent-canvas` بيشتغل وقت البناء على مسار جوه الـ image — لكن `docker compose` بيعمل mount لمجلد الـ host فوق نفس المسار وقت التشغيل، وده بيلغي الـ ownership دي تمامًا. مجلد الـ host بيتعمل root، والمستخدم غير-root جوه الحاوية مالوش صلاحية كتابة.

### الحل
- `setup.sh`: `chmod -R 777` على `projects/` وقت التنصيب.
- `server/routes/projects.mjs`: `fs.chmodSync(projectPath, 0o777)` بعد إنشاء أي مشروع جديد (مش بس وقت التنصيب) — عشان الإصلاح يشمل كل مشروع مستقبلي تلقائيًا.

### باگ إضافي اكتُشف وأُصلح أثناء الاختبار
Rollback مكانش بيتفعّل لو فشل الاتصال بـ OpenHands بالكامل (استثناء شبكة، مش رفض HTTP) — بيسيب مجلد يتيم. تم إضافة `try/catch` حوالين استدعاء التسجيل.

### التحقق
اختبار حي مباشر: صلاحيات المجلد قبل/بعد `chmod` (755 → 777)، واختبار إنشاء مشروع كامل يتأكد من الصلاحية الصحيحة والـ rollback عند الفشل. `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء.

---

## 2026-08-14 — `setup.sh` بقى بينشئ الموظفين تلقائيًا

### السياق
المستخدم طلب مسح staging كامل وتنصيب من الصفر، وتوقّع إن السكريبت هيعمل الموظفين تلقائيًا. بالفحص، تأكد إن ده كان دايمًا خطوة يدوية منفصلة.

### التعديل
- `setup.sh`: خطوة جديدة تلقائية (بعد التأكد من جاهزية الـ backend) تشغّل `bootstrap-employees.mjs` **لو** `MKDD_BOOTSTRAP_LLM_PROFILE_REF` متظبط في البيئة. لو مش متظبط، السكريبت يكمل عادي مع تعليمات واضحة بدل ما يفشل بالكامل.
- `deploy/install.sh` بيستفيد تلقائيًا من التحديث ده (بينادي `./setup.sh up` داخليًا) من غير أي تعديل إضافي.

### التحقق
- فحص صحة bash syntax (`bash -n`)، ومراجعة ترتيب الخطوات (bootstrap بعد التأكد من جاهزية الـ backend).

---

## 2026-08-13 — اعتماد نظام الألوان الرسمي (DOCX) + إصلاح كسر الوضع الفاتح + تصغير شريط الفريق

### السياق
المستخدم رفع مستند Word رسمي (`MKDD_Color_System_Light_Dark_AR_RTL.docx`) بنظام ألوان دقيق (Token names + HEX exact values) للوضعين، بعد ما شاف الوضع الفاتح "متكسر" فعليًا على staging (كروت بتدرج غامق غريب، عنوان قسم مختفي، كروت QA سودة بالكامل رغم إنه وضع فاتح).

### إضافات
- اعتماد **20 توكن رسمي بالضبط** بقيمهم الحقيقية من المستند (`canvas`, `page`, `surface`, `border-subtle/strong`, `text-primary/secondary/muted/disabled`, `accent/accent-deep/accent-soft`, `info`, `success`, `warning`, `danger`) — الفاتح هو الافتراضي في بنية الـ CSS نفسها (`:root, [data-theme="light"]`) والداكن `override` صريح (`[data-theme="dark"]`)، مطابق تمامًا لبنية المستند.
- 4 توكنز مُشتقة (مش موجودة صراحة في المستند، محسوبة من التوكنز الرسمية عبر `color-mix()`): `--mkdd-page-translucent`، `--mkdd-success-tint`، `--mkdd-danger-tint`، `--mkdd-accent-glow`.

### إصلاحات
- **السبب الجذري لكسر الوضع الفاتح:** لقينا 4 قيم hex كانت لسه مكتوبة مباشرة جوه `linear-gradient()` (تجاوزت التحويل الجزئي السابق تمامًا) — دي بالظبط سبب الكروت الغامقة الغريبة. تم إصلاحها بالكامل.
- استبدال **كل** الأسماء القديمة للتوكنز بالأسماء الرسمية الجديدة في الملف بالكامل (مش بس التعريف).
- تحويل **كل الـ74 لون hex المتبقي** (كان جزء منها لسه غير محوَّل من مرة سابقة) للتوكنز الصحيحة حسب السياق (خلفية/نص/حدود) — الملف بقى خالي تمامًا من الألوان الثابتة إلا حالة واحدة مقصودة (نص أبيض ثابت جوه دايرة بنفسجية مليانة).
- **باگ اكتُشف وأُصلح أثناء التعديل:** قاعدتين CSS قديمتين كانوا بنفس تخصصية قواعدي الجديدة وبعدهم بالترتيب، فكانوا بيلغوا تنسيق الكارت المصغّر بصمت. تم حذفهم.
- **خطأ توازن أقواس اكتُشف ذاتيًا وأُصلح** أثناء التعديلات (فحص برمجي قبل أي بناء، زي المعتاد).

### تصغير شريط الفريق (شكوى صريحة: "كبير جدًا مالوش علاقة بالتصميم")
- الكارت من 128px إلى 76px، بدون حدود ولا خلفية (بسيط زي المرجع)، الأفاتار 42-52px بدل مستطيل مدوّر، وحذف صف البادجات (حالة/تكلفة/مهام) من هذا العرض المختصر تمامًا.

### التحقق
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، فحص توازن أقواس CSS (338/338)، `oxlint` → 0 أخطاء، كل ده بعد Prettier.

---

## 2026-08-13 — ضبط أسلوب Workflow Stepper + Sidebar ليتطابق مع الصور المرجعية

### السياق
المستخدم أكّد الصور المرجعية كـ"الأسلوب النهائي"، وطلب ضبط الأجزاء المبنية بالفعل (Stepper + Sidebar + الأيقونات) بدقة قبل أي بند جديد.

### تعديلات
- **WorkflowStepper:** علامة "✓" النصية بقت أيقونة `FaCheck` حقيقية، دوائر أكبر (36px موبايل / 44px ديسكتوب)، مسافات أوسع، خط مخصص (Segoe UI/system-ui) للأرقام بدل الخط الزخرفي (Cormorant Garamond) اللي مكانش هيبان واضح في دايرة صغيرة.
- **Sidebar:** أيقونات كل عنصر بقت جوه شارة دائرية بلون أساسي (Accent) — نفس أسلوب الأيقونات الملوّنة في الصور المرجعية — بدل أيقونة عادية بس.
- إضافة توكنين جديدين (`--mkdd-success-bg`, `--mkdd-accent-glow`) للوضعين الداكن والفاتح، بدل قيم hex كانت مكتوبة مباشرة.
- تحسين عرض الـ Sidebar على الديسكتوب (340px بدل عرض متجاوب ضيق).

### التحقق
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

---

## 2026-08-13 — نظام الوضع الليلي/النهاري (Design Tokens)

### السياق
طلب استباقي من المستخدم: تجهيز نظام ألوان قابل للتبديل قبل ما المشروع يكبر أكتر ويصعب تحويله لاحقًا. المشروع كان فيه 103 لون hex مختلف مكتوب مباشرة في الكود.

### إضافات
- **20 Design Token** (CSS Variables) في `:root` (الوضع الداكن، افتراضي، بنفس قيم الألوان الأصلية بالظبط — التحويل ده مغيّرش أي شكل بصري لوحده) و`[data-theme="light"]` (وضع فاتح جديد).
- `src/hooks/useTheme.ts`: hook بنفس نمط `useLanguage.ts` (حفظ في localStorage + تطبيق على `document.documentElement`).
- زر تبديل الوضع (☀️/🌙 بأيقونات Font Awesome حقيقية) في `AppHeader.tsx`.
- سكريبت صغير في `index.html` يطبّق الوضع المحفوظ **قبل** ما React يشتغل، لمنع "ومضة" اللون الغلط عند فتح الصفحة.

### إعادة هيكلة
- استبدال 165 استخدام لأكتر الألوان تكرارًا (46 لون مختلف، دمج الألوان شبه المتطابقة في توكن واحد) بمتغيرات CSS.
- **باقٍ صراحة (غير مكتمل 100%):** ~88 استخدام لألوان نادرة/تدرجات فردية لسه مكتوبة مباشرة — هتتحوّل تدريجيًا في مرات لاحقة، موثّق هنا كمتابعة مفتوحة مش مخفية.

### التحقق
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، فحص توازن أقواس CSS، `oxlint` → 0 أخطاء، كل ده بعد Prettier.
- **تنبيه صريح:** تجنّبنا استخدام `color-mix()` (خاصية CSS حديثة نسبيًا) لصالح متغير خلفية شفافة مُعرَّف صراحة لكل وضع — نفس درس الحذر من `calc()` الخطرة اللي اتعلمناه قبل كده في نفس المشروع.

---

## 2026-08-13 — أيقونات حقيقية (Font Awesome) بدل الإيموجي

### السياق
طلب صريح: استخدام أيقونات حقيقية (Google Icons / Font Awesome) بدل رموز الإيموجي (📂⏳✅👥📷) اللي كانت مؤقتة.

### إضافات
- مكتبة `react-icons` (تضم Font Awesome وMaterial Icons في حزمة واحدة، أيقونات SVG حقيقية قابلة للتلوين، مع دعم tree-shaking صحيح — الحجم زاد 7KB بس رغم إضافة 9 أيقونات).

### إعادة هيكلة
- `Sidebar.tsx`: كل الإيموجي (📂⏳✅👥) + رمز الإغلاق (✕) + الأسهم (←→) بقوا أيقونات Font Awesome حقيقية.
- `EmployeeAvatarUpload.tsx`: أيقونة الكاميرا (📷) بقت `FaCamera`، مؤشر الرفع بقى spinner دوّار حقيقي (`FaSpinner` + أنيميشن دوران CSS)، مؤشر الخطأ بقى `FaTriangleExclamation`.

### التحقق
- بحث شامل في كل ملفات المصدر أكّد عدم وجود أي إيموجي متبقي.
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح (حجم إضافي بسيط 7KB بس)، `oxlint` → 0 أخطاء، كل ده بعد Prettier.

---

## 2026-08-13 — إعادة بناء الـ Sidebar (تصحيح المنطق: تنقل بس، مش عرض بيانات)

### السياق
تصحيح صريح من المستخدم: الـ Sidebar كان بيعرض البيانات (قوائم مشاريع، موظفين) جوّاه مباشرة — غلط في المفهوم. المطلوب: Sidebar = قائمة أزرار تنقل بس، كل زرار يفتح صفحة/Popup منفصل فيه المحتوى الفعلي.

### إضافات
- `src/components/ProjectListModal.tsx`: Popup يعرض قائمة مشاريع تصنيف واحد (نشطة/قربت تخلص/خلصت).
- `src/components/EmployeeListModal.tsx`: Popup يعرض قائمة كل الموظفين.

### إعادة هيكلة
- `src/components/Sidebar.tsx`: بقى قائمة 4 أزرار بس (بأيقونة لكل واحد)، مفيهوش أي عرض بيانات مباشر.
- `App.tsx`: بقى فيه state لتحديد أي Popup مفتوح (`activeSidebarMenu`)، والـ Popups بتاخد بياناتها من نفس المصادر الحقيقية الموجودة بالفعل (workflow summaries، قائمة الموظفين).
- حذف CSS ميت (`.sidebar-section h3`, `.sidebar-empty`) بعد التأكد من عدم استخدامهم.

### التحقق
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

---

## 2026-08-13 — Workflow Stepper (أول جزء من إعادة التصميم نحو المرجع البصري)

### السياق
بعد مناقشة مفصّلة مع المستخدم حول صور مرجعية جديدة للتصميم، تم الاتفاق على: (1) بناء مكوّن واحد متجاوب لكل جزء (موبايل + ديسكتوب مع بعض، مش تطبيقين منفصلين)، (2) البدء بـ"Workflow Overview" (الدايرة المرقّمة للبوابات).

### إضافات
- `src/components/WorkflowStepper.tsx`: دايرة مرقّمة لكل بوابة (✓ لو اتوافق عليها، دايرة مميّزة للحالية، رمادي للمعلّقة)، مع خط واصل بينهم بيتلوّن أخضر تدريجيًا مع كل موافقة.
- `src/utils/workflowLabels.ts`: نصوص البوابات والمراجعات، مُستخرَجة كمصدر واحد بدل تكرارها في `ProjectHomeScreen.tsx` والمكوّن الجديد.

### إعادة هيكلة
- حذف الكروت القديمة البسيطة للبوابات، واستبدالها بالـ Stepper الجديد.
- حذف CSS ميت (الكروت القديمة + قاعدة `.project-home .project-header` متبقية من تنظيف سابق ناقص).

### التحقق
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء، فحص توازن أقواس CSS، كل ده بعد Prettier.

---

## 2026-08-13 — هيدر موحّد + Sidebar + خطوط مخصصة + إعادة تنظيم + توثيق خريطة الملفات

### إضافات
- `server/routes/workflow.mjs`: endpoint جديد `GET /api/workflow/summary` (حالة كل المشاريع دفعة واحدة).
- `server/workflow-state.mjs`: دالة `listWorkflowSummaries()`.
- `src/utils/projectGateStatus.ts`: تصنيف المشروع (نشط/قربت تخلص/خلصت) — دالة نقية، مُختبَرة يدويًا على كل السيناريوهات.
- `src/components/AppHeader.tsx`: الهيدر الموحّد الوحيد في التطبيق كله (خط Bodoni Moda).
- `src/components/BreadcrumbBar.tsx`: التنقل الخاص بكل شاشة، منفصل عن الهيدر عمدًا.
- `src/components/Sidebar.tsx`: تصنيف المشاريع + قائمة الموظفين.
- `src/components/EmployeeProfileModal.tsx`: بروفايل موظف (اسم/دور/صورة) من الـ Sidebar.
- خط Cormorant Garamond لباقي التطبيق (Google Fonts في `index.html`).
- `README.md` قسم 59: خريطة كاملة لكل ملفات الواجهة والـ backend ومسؤولية كل واحد.

### إعادة هيكلة
- `App.tsx`: بقى يستضيف الهيدر/الـ Sidebar/بروفايل الموظف مرة واحدة، بدل تكرارهم في كل شاشة.
- `ProjectsScreen.tsx`, `ProjectHomeScreen.tsx`, `ChatScreen.tsx`: اتشال منهم كود الهيدر الخاص بيهم بالكامل.
- حذف 16 قاعدة CSS ميتة (هيدرات قديمة منفصلة)، وprop غير مُستخدَم (`project` في `ProjectHomeScreen`).

### التحقق الذي تم إجراؤه فعليًا
- اختبار حي لـ `/api/workflow/summary` (موافقة بوابة → الملخص يعكسها بدقة).
- اختبار يدوي لمنطق التصنيف على 4 سيناريوهات حقيقية.
- `npm test` → 42/42، `tsc -b` → نظيف من أول مرة، `npm run build` → ناجح، `oxlint` → 0 أخطاء.
- اختبار تكامل حي: مسارات قديمة وجديدة تعمل مع بعض بدون تعارض.

---

## 2026-08-13 — كروت الموظفين أفقية + إصلاح كاش صورة الموظف

### إصلاحات
- **كروت الموظفين:** تحويل من عرض عمودي (تحت بعض) لصف أفقي قابل للتمرير (جنب بعض)، بناءً على توضيح مباشر من المستخدم بعد فيدباك سابق كان فهمه غير دقيق.
- **صورة الموظف "مكسورة" رغم نجاح الرفع:** تشخيص مباشر عبر الـ VM أثبت إن الـ backend سليم 100% (الملف موجود، والـ endpoint بيرجّع `200`/`image/png` صح) — المشكلة كانت كاش المتصفح لمحاولة فاشلة قديمة على نفس الرابط. الحل: كل رابط صورة بقى يحمل `?v={وقت آخر تعديل}` فريد لكل رفعة جديدة.

### التحقق
- اختبار حي: رفع → الرابط الراجع فيه نسخة زمنية → نفس الرابط يرجّع `200` صح.
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، بعد Prettier.

---

## 2026-08-13 — إصلاح هيدر المشروع والـ composer (كانوا بيتحركوا بدل ما يفضلوا ثابتين)

### السبب
فيدباك حي من المستخدم بالصور — هيدر صفحة المشروع كان بيختفي أثناء التمرير العادي رغم `position: sticky`.

### التشخيص
- الهيدر: `overflow-x: hidden` على عنصر أب كان بيلغي `sticky` بصمت (سلوك موثّق في CSS spec).
- الـ composer: قاعدتين CSS صريحتين (`position: static`، على الأغلب leftover من تصميم قديم) كانوا بيلغوا أي `sticky`/`fixed` تمامًا.

### الحل
استبدال `sticky` بـ `position: fixed` للاتنين (أكتر ثباتًا)، مع تعويض المساحة في الـ layout (padding مناسب فوق/تحت المحتوى).

### خطر تم تجنّبه ذاتيًا
أول محاولة لتوسيط الـ composer استخدمت `calc(50% - 410px)` — قيمة بتبقى سالبة على شاشات الموبايل العادية. تم اكتشافها وتصحيحها بالنمط الآمن (`max-width` + `margin:auto`) قبل أي رفع.

### التحقق
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، فحص توازن أقواس CSS برمجيًا، كل ده بعد Prettier.

---

## 2026-08-13 — فيدباك حي على الـ UI + رفع صورة الموظف + لون غلاف المشروع

### إصلاحات (بناءً على فيدباك حقيقي بالصور من staging)
- **شات:** اسم الموظف بقى ملاصق لصورة صغيرة في صف واحد، فوق الرسالة مباشرة (كان معلّق في صف منفصل بعرض كامل).
- **صفحة المشروع:** حذف تكرار عرض الموظفين (شريط + كروت) — بقى عرض واحد بس، بالبادجات، في المكان العلوي.

### إضافات
- **رفع صورة الموظف:** `server/routes/avatars.mjs` (رفع + عرض حقيقي)، بعد اكتشاف إن `avatarUrl` كان مسار وهمي من غير سيرفر يقدّمه أصلًا (باگ موجود من قبل، اتكشف أثناء التنفيذ).
- **لون غلاف المشروع:** `server/lib/project-metadata.mjs` (تخزين مخصص لـ MKDD، بما إن OpenHands مفهوش مفهوم "لون" أصلًا)، منتقي ألوان في Modal الإنشاء، وحدّ لوني على كل كارت.

### إصلاح إضافي اكتُشف أثناء التنفيذ
- تعشيش عناصر `<button>` غير صالح (بعد إضافة زر رفع الصورة جوه كارت الموظف) — تم التحويل لـ `<div role="button">` مع دعم لوحة المفاتيح.

### التحقق الذي تم إجراؤه فعليًا
- اختبار حي كامل لرفع/عرض الصور: 5 سيناريوهات (نجاح، عرض، موظف وهمي، بدون صورة، بيانات غلط) — كلها نجحت.
- 5 اختبارات جديدة لمنطق تخزين لون المشروع (معزولة تمامًا بمجلد مؤقت، بدون تأثير على باقي الاختبارات).
- `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، كله بعد Prettier.
- الجزء المعتمد على OpenHands الحي (إنشاء مشروع بلون فعليًا) محتاج تأكيد على staging (بيئتي بدون Docker).

---

## 2026-08-13 — إعادة تصميم بصري لثلاث شاشات (بناءً على مرجع تصميم من المستخدم)

### التغييرات
- **شاشة المشاريع:** هيدر علامة تجارية جديد (لوجو + "MKDD" + "تصميم وتطوير" + زر لغة صغير)، وزر "+ مشروع جديد" انتقل من الهيدر لشريط ثابت أسفل الشاشة (full-width، بارز).
- **شاشة المشروع:** إضافة شريط أفقي لأفاتار كل الموظفين أعلى الشاشة (بإعادة استخدام أنماط `.employee-strip`/`.employee-chip` الموجودة بالفعل، بدل بناء جديد)، مع نقطة حالة خضراء للموظف الشغال حاليًا. لمسة علامة تجارية صغيرة في هيدر المشروع (لوجو مصغّر + "MKDD").
- **شاشة المحادثة:** أفاتار الموظف الحقيقي (`avatarUrl`) بدل الحرف الأول فقط، تخطيط رسائل جديد (أفاتار الموظف بجانب رسائله)، زر إرسال دائري بأيقونة بدل نص.

### ملاحظة تصميمية
- التصميم المرجعي اللي قدّمه المستخدم كان "إلهام مش نسخ حرفي" — تم تكييفه مع قيود المنتج الحقيقية (مفيش group chat في MKDD، فشريط الموظفين ما بيمثلش "مشاركين في نفس المحادثة"، لكنه تنقل سريع بين محادثات كل موظف على حدة).
- تجنّب تعارض specificity في CSS (زي ما ينبّه دليل التصميم) عند إعادة تصميم زر الإرسال — تم إضافة قاعدة override بنفس مستوى الخصوصية بدل الاعتماد على ترتيب الملف بس.

### التحقق
- `npm test` → 37/37، `tsc -b` → نظيف، `npm run build` → ناجح، كل ده بعد Prettier.

---

## 2026-08-13 — عنوان المحادثة (اسم الموظف + المشروع) + وعي الموظف بالوقت

### إضافات
- `server/lib/employee-display-info.mjs`: دالة مشتركة لقراءة بيانات عرض الموظف (اسم، دور) من ملف التعريف — مُستخرجة من `directory.mjs` لتُستخدم في مكانين بدون تكرار.
- `server/lib/time-context.mjs`: حقن وتجريد علامة الوقت الحالي المخفية في رسائل المستخدم، بعد بحث فعلي في توثيق OpenHands (مفيش آلية جاهزة).
- 7 اختبارات جديدة (5 لـ `time-context.mjs`، 2 لتكامله مع `normalizeEvent`).

### إصلاحات
- **`server/routes/chat.mjs`:** المحادثات الجديدة بقى ليها `title` حقيقي (اسم الموظف + اسم المشروع)، بعد اكتشاف الحقل ده موجود فعليًا في الـ payload الأصلي لـ Agent Canvas ومفقود من كود MKDD.
- **`bootstrap-employees.mjs`:** كل الـ13 موظف بقى عندهم تعليمات فهم علامة الوقت المخفية، مُضافة مرة واحدة مشتركة بدل تكرارها في كل ملف.

### التحقق الذي تم إجراؤه فعليًا
- بحث في `docs/` الكامل لـ `openhands-agent-canvas` — تأكيد عدم وجود آلية وقت جاهزة.
- المستخدم أكّد بنفسه من واجهة Agent Canvas الحقيقية عدم وجود إعداد لهذا الموضوع.
- فحص كود `use-create-conversation.ts` الحقيقي لتأكيد حقل `title`.
- `npm test` → 35/35 ناجح (منها round-trip كامل لحقن/تجريد الوقت بنصوص عربية ومتعددة الأسطر)، `tsc -b` → نظيف، `npm run build` → ناجح، تشغيل فعلي للسيرفر بعد كل التعديل.

---

## 2026-08-13 — عرض وقت كل رسالة في الشات

### إضافات
- `src/utils/formatMessageTime.ts`: دالة تنسيق وقت الرسالة — بتعرض الوقت بس لو النهاردة، وتاريخ+وقت لو أي يوم تاني (عشان رسالة من الشهر اللي فات متبانش وكأنها لسه حصلت).
- عرض الوقت أسفل كل رسالة في `ChatScreen.tsx`.

### التحقق
- تم التأكد يدويًا من المنطق (النهاردة، شهر فات، قيمة فاضية، تاريخ غير صالح) قبل الدمج.
- `npm test` → 28/28، `tsc -b` → نظيف، `npm run build` → ناجح.

### ملاحظة إيجابية غير مؤكدة رسميًا
- المستخدم لاحظ إن مشكلة "اختفاء بيانات الموظفين أحيانًا" (`BUGS_AND_FIXES.md` #4) اختفت بعد تفعيل الـ realtime chat. أثر جانبي محتمل ومنطقي، لكن غير مؤكَّد كسبب جذري رسميًا بعد.

---

## 2026-08-13 — تنفيذ Realtime Chat (WebSocket) — تطبيق خطة REALTIME_CHAT_RESEARCH.md

### إضافات
- `server/lib/ws-bridge.mjs`: جسر WebSocket آمن بين المتصفح و MKDD backend، وبين MKDD backend و OpenHands (Phase C). يعيد التحقق من صلاحية المحادثة عند كل اتصال (نفس دالة التفويض المُستخدمة في REST)، ويطبّع كل الأحداث بنفس دالة REST قبل إعادة بثها.
- `useConversation.ts`: إعادة كتابة كاملة — تحميل أولي عبر REST، ثم اتصال WebSocket حي مع reconnect بـ backoff (Phase D)، رسالة تفاؤلية فورية عند الإرسال (Phase E)، وإزالة الـ polling الرئيسي كل ثانيتين (Phase F) — استُبدل بـ resync خفيف كل 15 ثانية كـ fallback فقط.

### التحقق الذي تم إجراؤه فعليًا (بدون Docker، عبر تشغيل السيرفر مباشرة)
- اختبار حي: اتصال WebSocket ببارامترات ناقصة → رفض صحيح (`4400`).
- اختبار حي: اتصال بمسار غلط → رفض فوري.
- اختبار حي: فشل التحقق من الصلاحية (OpenHands غير متاح) → رفض صحيح (`1011`).
- اختبار تكامل: مسارات HTTP العادية + WebSocket bridge يعملان معًا بدون تعارض بعد كل التعديلات.
- `npm test` → 28/28 ناجح، `tsc -b` → نظيف، `npm run build` → ناجح.

### قيود معروفة (موثقة صراحة، انظر REALTIME_CHAT_RESEARCH.md قسم 8)
- Work Plan لسه بيتحدّث كل 15 ثانية عبر REST resync، مش لحظيًا مع كل حدث WebSocket.
- لم يتم بعد اختبار محادثة حية كاملة (رسالة → رد فعلي من موظف) على staging — يحتاج بيئة Docker فعلية.

---

## 2026-08-13 — ميزة "مشروع جديد" + مبدأ هندسي جديد

### إضافات
- `ENGINEERING_PRINCIPLES.md`: مبدأ جديد موثق — أي ميزة في `mkdd-ui` لازم يكون لها أساس حقيقي مؤكَّد في `openhands-agent-canvas`/الـ API الحي، مش واجهة مُختلَقة.
- زر "+ مشروع جديد" في شاشة المشاريع، مع Modal لإدخال الاسم.
- `POST /api/projects` (backend جديد): ينشئ مجلد مشروع فعلي ويسجّله كـ Workspace حقيقي عند OpenHands عبر `POST /api/workspaces` — نفس الآلية بالضبط المستخدمة في واجهة Agent Canvas الأصلية (`folder-browser-modal.tsx`)، مؤكَّدة عبر فحص `openapi.json` الحي قبل التنفيذ.

### إصلاحات (اكتُشفت أثناء البناء، قبل أي رفع)
- `handleProjects` كان بيتفحص الـ URL بس، مش الـ method — لو سبناه، أي `POST /api/projects` كان هيتخطف من route القراءة القديم قبل ما يوصل للـ route الجديد. تم إصلاحه بإضافة تحقق `method === "GET"`.
- أسماء مشاريع عربية بالكامل كانت هتفشل الإنشاء (الـ slug بيطلع فاضي). تم الفصل بين اسم العرض (يُخزَّن كما هو، أي لغة) والـ slug (يُستخدم للمسار على القرص فقط)، مع fallback تلقائي (`project-<uuid>`) لو الاسم مفيهوش أي حرف/رقم إنجليزي.

### التحقق الذي تم إجراؤه فعليًا
- تأكيد `POST /api/workspaces` من `openapi.json` الحي قبل الكتابة (مش تخمين).
- 7 اختبارات جديدة لدالة `slugifyProjectName` (تشمل منع path traversal ومحارف shell الخطرة).
- `npm test` → 28/28 ناجح، `tsc -b` → نظيف، `npm run build` → ناجح، كل ده بعد تشغيل Prettier للتأكد من عدم كسر أي شيء.

---

## 2026-08-13 — عزل نظام النشر التلقائي بالكامل عن الإنتاج

### إضافات
- `deploy/.env.staging`: بورتات/أسماء حاويات/اسم صورة/اسم volume منفصلة تمامًا عن الإنتاج (نفس بورتات التجربة اليدوية السابقة: 18000/13000/15173/18787).

### إصلاحات
- `compose.yml`: كل معرّف يواجه المضيف بقى قابل للتهيئة عبر متغيرات بيئة، بقيم افتراضية = القيم الأصلية (صفر تأثير على الاستخدام العادي).
- **بگ مكتشف بالاختبار الفعلي:** `setup.sh` كان بيفترض بورت 8787 دايمًا للـ health check حتى لو `.env` بيحدد بورت مختلف (لأن bash لا يقرأ `.env` تلقائيًا زي `docker compose`). تم إصلاحه بجعل `setup.sh` و`auto-deploy.sh` يقرآن `.env` صراحة.

### التحقق
- اختبار معزول أثبت أن قيمة البورت المُحلَّلة تتغير صح حسب وجود/غياب `.env` — بدون تشغيل Docker فعلي (غير متاح في بيئة الاختبار)، لكن بمنطق القراءة نفسه مُثبت بالتشغيل الفعلي.

---

## 2026-08-13 — نظام النشر التلقائي (Auto-Deploy)

### إضافات
- `deploy/auto-deploy.sh`: يراقب `main` على GitHub، يسحب أي commit جديد، يبني، يشغّل، يعمل health check، ويرجع تلقائيًا لآخر نسخة شغالة عند الفشل.
- `deploy/install.sh`: تركيب لمرة واحدة — clone مخصص + تفعيل systemd timer.
- `deploy/mkdd-auto-deploy.service` + `.timer`: وحدات systemd، الـ timer يعمل كل دقيقة.
- `deploy/README.md`: توثيق كامل للتصميم والمخاطر وكيفية المراقبة/الإيقاف.

### إصلاحات (اكتُشفت أثناء اختبار فعلي بمحاكاة معزولة)
- إصلاح افتراض خاطئ بأن `git clone` يهبط دائمًا على فرع `main` — تمت إضافة `checkout -B main origin/main` صريحة.
- إصلاح غياب النشر الأولي عند التركيب — `install.sh` الآن يشغّل `setup.sh` صراحة كخطوة أخيرة.

### التحقق الذي تم إجراؤه فعليًا
- محاكاة كاملة بريبو git محلي معزول (بدون أي تأثير على الريبو الحقيقي): تدفق ناجح، تدفق فاشل + rollback تلقائي ناجح، تعافي بعد commit مُصلَح — الثلاثة تم إثباتها بالتشغيل الفعلي وليس بالقراءة فقط.

---

## 2026-08-13 — إعادة هيكلة الـ backend + إصلاح قابلية إعادة البناء + تنظيم شامل

### إضافات
- `setup.sh`: سكريبت واحد يبني ويشغّل المشروع بالكامل من الصفر (clone → build → up)، يتضمن كل القيم المكتشفة (`AGENT_SERVER_IMAGE`, `AUTOMATION_VERSION`).
- `server/lib/`: 5 ملفات جديدة (openhands-client, normalize-conversation, normalize-event, work-plan, authorize-conversation) تحمل منطق مشترك كان مدفون داخل `server/index.mjs`.
- `server/routes/`: 5 ملفات جديدة (branding, workflow, directory, conversation, chat) تحمل كل route منطقه في ملف منفصل.
- 17 اختبار آلي جديد (`node:test`، بدون اعتمادية خارجية جديدة) تغطي منطق التحقق الأمني وتطبيع الأحداث وحساب خطة العمل.
- Prettier (`.prettierrc.json`, `.prettierignore`) لتنسيق موحد للكود.
- `PROJECT_AUDIT_REPORT.md`: تقرير فحص شامل للمشروع بأرقام وأدلة فعلية.
- `REALTIME_CHAT_RESEARCH.md`: بحث فني في كود OpenHands الفعلي لحل فجوة الـ realtime chat.
- `BUGS_AND_FIXES.md`: أول نسخة، ثم تحديثها لاحقًا بكل الإصلاحات المذكورة أدناه.

### إصلاحات
- **`compose.yml` — مسارات مطلقة:** تم استبدال كل المسارات المطلقة (`/opt/openhands/...`) بمسارات نسبية + متغير بيئة `MKDD_PROJECTS_DIR`. المشروع أصبح قابلاً للنقل لأي مسار/جهاز.
- **`compose.yml` — عدم القدرة على بناء صورة agent-canvas:** تمت إضافة `build.context` + `build.args` (`AGENT_SERVER_IMAGE`, `AUTOMATION_VERSION`) بالقيم المؤكدة عمليًا بعد اختبار بناء حقيقي ناجح.
- **Message content normalization (bug #3):** دالة `textContent` الموحدة الجديدة تدعم الآن الشكلين (`string` و array) معًا.
- **تكرار منطق التحقق من ملكية المحادثة:** تم توحيده في `matchesEmployee`/`findAuthorizedConversation`، مُستخدم الآن في كل الأماكن الثلاثة بدلاً من نسخ منفصلة قد تنحرف عن بعضها بمرور الوقت.

### إعادة هيكلة (بدون تغيير سلوك)
- `server/index.mjs` انتقل من 830 سطر بمنطق كل شيء مدمج، إلى نقطة دخول رفيعة (~45 سطر) تربط الـ routes ببعضها. تم التحقق من تطابق السلوك بالكامل: نفس رموز الأخطاء، نفس شكل الاستجابات، نفس ترتيب معالجة الـ routes، مع تشغيل فعلي للسيرفر بعد التعديل للتأكد.

### التحقق (Verification) الذي تم إجراؤه فعليًا قبل الرفع
- `node --check` على كل ملف `.mjs` جديد/معدّل — بدون أخطاء.
- تشغيل فعلي للسيرفر (`node server/index.mjs`) والتأكد من استجابته.
- `npm test` → 17/17 ناجح.
- `npx tsc -b` → بدون أخطاء.
- `npm run build` → بنجاح (dist تم إنشاؤه وحذفه بعد التأكد، لا يُرفع للريبو).
- `npx prettier --write .` ثم إعادة تشغيل الاختبارات والـ build للتأكد من عدم كسر أي شيء.

---

## 2026-08-13 — الرفع الأولي على GitHub

- تصدير كامل لكود `mkdd-ui` و`openhands-agent-canvas` (المعدّل) و`company-agents-definitions` و`company-skills` و`mkdd-data/branding` من الـ VM.
- التحقق من مطابقة كل التعديلات غير المحفوظة (uncommitted) للمصدر الأصلي قبل الرفع.
- التحقق من عدم وجود أسرار (`.env` حقيقي) أو `node_modules`/`dist` في التصدير.
- اختبار فعلي لبناء صورة `openhands-agent-canvas` من الكود المصدري (وليس من صورة جاهزة)، واكتشاف الحاجة لـ `AGENT_SERVER_IMAGE`/`AUTOMATION_VERSION` (موثق بالتفصيل في `BUGS_AND_FIXES.md` #7/#8).
- تشغيل تجريبي لـ `agent-canvas` و`mkdd-ui` كحاويات معزولة على بورتات بديلة للتأكد من نجاح البناء، ثم حذفهما بأمان دون التأثير على الحاويات الأصلية الشغالة.
- الرفع الفعلي إلى `https://github.com/menokemo/openhands-mkdd`.

---

## قالب للإضافة المستقبلية

```markdown
## YYYY-MM-DD — عنوان مختصر للتغيير

### إضافات
- ...

### إصلاحات
- ...

### إعادة هيكلة
- ...
```
