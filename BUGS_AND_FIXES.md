# BUGS_AND_FIXES.md

سجل بالمشاكل (Bugs) التي واجهت المشروع، وكيف تم اكتشافها، وحلها (أو حالتها الحالية إذا لم تُحل بعد).

> **ملاحظة:** هذا الملف يجب تحديثه مع كل commit فيه إصلاح لمشكلة أو اكتشاف مشكلة جديدة.

---

## ✅ تم حلها (Fixed)

### 1. Events response status regression
**الوصف:** بعد إضافة الـ pagination الكامل للـ events، الـ route كان بيستخدم متغير response قديم بالغلط عند إرجاع البيانات النهائية، مما كان يتسبب أحيانًا في فشل الاستجابة رغم نجاح جلب البيانات من OpenHands.

**الأثر:** الرسائل كانت بتتأخر أو متظهرش صح في الشات.

**الحل:** تم تصحيح استخدام متغير الـ response الصحيح.

**النتيجة بعد الإصلاح:** تحسن ظهور الرسائل بشكل ملحوظ.

---

### 2. Health response status regression
**الوصف:** الـ health route كان بيرجع بالغلط status variable تبع الـ chat events بدل الـ status الخاص به هو.

**الحل:** تم إرجاعه لاستخدام response status الخاص بالـ health request نفسه.

---

### 3. Message content normalization mismatch
**الوصف:** الـ normalization في MKDD كان بيقبل محتوى الرسائل فقط لما تكون array من text-content objects. لكن بعض المصادر (طبقًا لتوثيق `REALTIME_CHAT_RESEARCH.md`، شكل الرسالة الرسمي في OpenHands هو array دايمًا `(TextContent | ImageContent)[]`) — المشكلة كانت في نقاط استدعاء أخرى ممكن تبعت string خام.

**محاولة سابقة فشلت (موثقة للأمانة):** تمت محاولة تعديل تلقائي (scripted edit) سابقًا فشلت في إيجاد الـ source anchor بالضبط، فتم إيقاف المحاولة **بدون** تعديل الملف عشوائيًا حينها.

**الحل النهائي:** أثناء إعادة هيكلة `server/index.mjs` (انظر `PROJECT_AUDIT_REPORT.md`)، تم استخراج دالة `textContent` الموحدة في `server/lib/normalize-event.mjs`، وتم تعديلها لتقبل الشكلين معًا (`string` أو `[{type:"text", text:"..."}]`) وتُرجع دائمًا الشكل الموحد. الدالة مُغطاة باختبارات في `server/lib/normalize-event.test.mjs`.

**الحالة:** ✅ تم الحل.

---

## 🔴 معروفة ولسه مفتوحة (Known / Open)

### 4. اختفاء بيانات الموظفين (Employee Information Disappearance)
**الوصف:** معلومات الموظف أحيانًا بتختفي من الواجهة، وممكن تفضل مختفية حتى بعد عمل refresh للمتصفح.

**الحالة:** لم يتم حلها بالكامل، مشكلة نشطة.

**تحديث (2026-08-13):** المستخدم لاحظ إن المشكلة اختفت بعد تطبيق الـ realtime chat (WebSocket بدل polling). هذا **منطقي كأثر جانبي محتمل** — الـ polling القديم (REST كل ثانيتين) كان أكثر عرضة لتضارب الطلبات المتزامنة (race conditions) اللي كانت سبب رئيسي مشتبه به (انظر السبب #3 و#4 أدناه). **لم يتم تأكيد السبب الجذري رسميًا** (يحتاج مراقبة أطول لتأكيد الاستقرار)، لكن يُسجَّل هنا كملاحظة إيجابية مهمة.

**مناطق مشتبه بها لسه تحت التحقيق:**
1. سلوك الـ background polling لحالة الفريق.
2. فشل مؤقت في lookup المحادثة.
3. استبدال الـ state ببيانات جزئية (partial response).
4. تبعيات الـ React hooks اللي ممكن تعمل إعادة تحميل عند تغيّر identity الكائن.
5. عدم التفريق بين "لا توجد قيمة" و"الطلب فشل" — القاعدة المهمة: `fetch failed ≠ employee has no current work plan`. لو الاتنين بيتحولوا لـ `null`، الواجهة مش هتعرف تفرّق بينهم.
6. توقيت بيانات المحادثة أثناء تشغيل OpenHands run فعليًا.
7. تحديثات execution-state بتوصل بشكل مستقل عن تاريخ المحادثة.

**الحل المقترح:** استخدام semantics واضحة (success / error / update) بدل الاعتماد على شروط إضافية في الواجهة.

---

### 5. معمارية الشات الحالية غير كافية (Polling بدل Realtime)
**الوصف:** الشات حاليًا بيعتمد على REST + polling متكرر، مش WebSocket حقيقي.

**الأعراض الملاحظة:**
- ردود الـ agent بتظهر متأخرة في MKDD مقارنة بواجهة OpenHands الأصلية.
- محتوى المحادثة ممكن يختفي أحيانًا.
- الـ refresh مش بيضمن استرجاع فوري للمعلومات الناقصة.

**الحل النهائي:** تم تنفيذ خطة `REALTIME_CHAT_RESEARCH.md` بالكامل (Phases A-F) — WebSocket bridge آمن بين المتصفح و MKDD backend و بين MKDD backend و OpenHands، مع reconnect تلقائي، رسالة تفاؤلية فورية، وإزالة الـ polling الرئيسي (استبدلناه بـ resync خفيف كل 15 ثانية كـ fallback بس). التفاصيل الكاملة والاختبارات المُجراة في `REALTIME_CHAT_RESEARCH.md` قسم 8.

**الحالة:** ✅ تم الحل (مُختبَر أمنيًا ووظيفيًا؛ محتاج اختبار محادثة حية كاملة على staging للتأكيد النهائي).

**تحديث — تم التأكيد الفعلي (2026-08-13):** المستخدم اختبر محادثة حقيقية حية على الـ staging، وأكّد إن الردود بتوصل لحظيًا (live) بدون أي refresh يدوي. الـ realtime chat شغال بالكامل من أول تجربة.

---

### 6. آلية اكتشاف مفتاح الجلسة عبر `/proc` (Technical Debt)
**الوصف:** الـ backend حاليًا بيكتشف الـ OpenHands session API key عن طريق فحص `/proc` والبحث عن argument اسمه `--session-api-key`، وده معتمد على:
- Shared PID namespace بين الحاويتين.
- شكل معين لسطر أوامر العملية.
- اسم launcher/process محدد.

**الحالة:** يعمل حاليًا، لكنه **دين تقني (Technical Debt)** ومش معماري نهائي.

**الحل المستهدف:** استخدام آلية secret/config مدعومة رسميًا بدل فحص `/proc`.

---

## 🧪 اكتشافات تحقق (Verification Findings) — 2026-08-13

### 7. الـ compose.yml مش قادر يبني صورة agent-canvas من الكود مباشرة
**الوصف:** تم اختبار بناء صورة `openhands-agent-canvas` من الكود المصدري مباشرة (بدون الاعتماد على صورة جاهزة locally)، واكتُشف إن الـ Dockerfile يحتاج build arguments إجبارية غير موثقة في `compose.yml`:
```
ARG AGENT_SERVER_IMAGE   ← إجباري، بدون default
ARG AUTOMATION_VERSION   ← إجباري، بدون default
```

**الأثر:** أي محاولة لبناء الصورة من الصفر (على VM جديدة مثلاً) كانت هتفشل بالخطأ:
```
ERROR: failed to build: failed to solve: base name (${AGENT_SERVER_IMAGE}) should not be blank
```

**التحقيق:** تم العثور على القيم الصحيحة داخل `openhands-agent-canvas/config/defaults.json`:
```json
"versions": { "agentServer": "1.40.1", "automation": "1.6.0" },
"images": { "agentServer": "ghcr.io/openhands/agent-server" }
```

**القيم المؤكدة عمليًا (تم اختبار البناء بنجاح بها):**
```
AGENT_SERVER_IMAGE=ghcr.io/openhands/agent-server:1.40.1-python
AUTOMATION_VERSION=1.6.0
```

**نتيجة الاختبار:** تم بناء الصورة بنجاح (34/34 خطوة)، وتم تشغيلها فعليًا كـ container منفصل (اسم مؤقت `mkdd-agent-canvas-TEST`) على بورتات بديلة (`18000`, `13000`) بدون التأثير على الحاويات الأصلية الشغالة، وتم التأكد من عملها ثم حذفها بأمان بعد الاختبار.

**الحل النهائي:** تم دمج هذه القيم في `compose.yml` كـ `build.args` بقيم افتراضية (`${AGENT_SERVER_IMAGE:-...}` / `${AUTOMATION_VERSION:-...}`)، وفي `setup.sh` الذي يصدّرها كمتغيرات بيئة قبل البناء.

**الحالة:** ✅ تم الحل.

---

### 8. `compose.yml` بدون `build:` context لصورة agent-canvas
**الوصف:** ملف `compose.yml` كان يستخدم:
```yaml
agent-canvas:
    image: mkdd/agent-canvas:1.12.0-mkdd2
```
بدون سطر `build:`، يعني الملف كان يفترض أن الصورة موجودة مسبقًا (locally أو على registry)، ولا يحتوي على تعليمات لبنائها من الكود المصدري (`openhands-agent-canvas/`).

**الأثر:** لم يكن من الممكن إعادة بناء البيئة بالكامل على VM جديدة باستخدام `docker compose up` فقط.

**الحل النهائي:** تمت إضافة `build.context: ./openhands-agent-canvas` و`build.dockerfile: docker/Dockerfile` مع `build.args` الصحيحة (انظر مشكلة #7).

**الحالة:** ✅ تم الحل.

---

### 9. `compose.yml` يحتوي على مسارات مطلقة مربوطة بجهاز الـ VM الحالي (اكتشاف من PROJECT_AUDIT_REPORT.md)
**الوصف:** الفحص الشامل للمشروع (`PROJECT_AUDIT_REPORT.md`، بند 2.2) اكتشف أن `compose.yml` كان يحتوي على مسارات مطلقة مثل:
```yaml
- /opt/openhands/projects:/projects
- /opt/openhands/company-skills:/home/openhands/.agents/skills:ro
build:
  context: /opt/openhands/projects/mkdd-ui
```
رغم أن نفس هذه الملفات موجودة فعليًا داخل الريبو نفسه بجانب `compose.yml`.

**الأثر:** هذه مشكلة **منفصلة وإضافية** عن مشكلتي #7/#8. حتى بعد حلّهما، كان تشغيل `docker compose up` على أي جهاز آخر (أو حتى نفس الجهاز في مسار مختلف) سيفشل فورًا لعدم وجود `/opt/openhands/...` هناك.

**الحل النهائي:** تحويل جميع المسارات إلى نسبية (`./mkdd-ui`, `./company-skills`, `./company-agents-definitions`)، واستخدام متغير بيئة قابل للتخصيص لمسار المشاريع الفعلية (`${MKDD_PROJECTS_DIR:-./projects}`) لأن هذا المسار يمثل بيانات المستخدم الفعلية وليس جزءًا من كود المنتج.

**الحالة:** ✅ تم الحل.

---

## ✅ إصلاحات ناتجة عن PROJECT_AUDIT_REPORT.md (2026-08-13)

### 10. `server/index.mjs` كان ملف واحد ضخم (830 سطر) بمنطق كل الـ routes
**الوصف:** 13 route كلهم متعاملين معاهم داخل handler واحد، بدون framework أو تقسيم لملفات.

**الحل النهائي:** تم تقسيمه إلى `server/lib/` (openhands-client, normalize-conversation, normalize-event, work-plan, authorize-conversation) و`server/routes/` (branding, workflow, directory, conversation, chat)، مع `server/index.mjs` كنقطة دخول فقط تربط الـ routes ببعضها. تم التحقق من التطابق الوظيفي الكامل: نفس الأخطاء، نفس الاستجابات، نفس ترتيب المعالجة.

**الحالة:** ✅ تم الحل.

---

### 11. تكرار منطق التحقق من ملكية المحادثة في 3 أماكن
**الوصف:** منطق المطابقة (project + employeeId + fallback لاسم الموظف القديم) كان مكرر في 3 routes منفصلة.

**الحل النهائي:** تم استخراجه في دالة واحدة `matchesEmployee` + `findAuthorizedConversation` في `server/lib/authorize-conversation.mjs`، تُستخدم الآن في الأماكن الثلاثة بدون استثناء. مُغطاة بـ 6 اختبارات في `server/lib/authorize-conversation.test.mjs`.

**الحالة:** ✅ تم الحل.

---

### 12. صفر اختبارات آلية في `mkdd-ui`
**الحل النهائي:** تمت إضافة `node:test` (built-in، بدون اعتمادية جديدة) مع 17 اختبار تغطي: `matchesEmployee` (منطق أمني)، `textContent`/`normalizeEvent` (منطق تطبيع حساس للأخطاء)، و`deriveWorkPlan`. كل الاختبارات تعدّي (`npm test`).

**الحالة:** ✅ تم الحل (تغطية أولية للمنطق الأكثر حساسية؛ لسه فيه مجال للتوسع لاحقًا في اختبارات الـ routes نفسها).

---

### 13. لا يوجد Formatter موحّد
**الحل النهائي:** تمت إضافة Prettier (`.prettierrc.json` + `.prettierignore`)، وتم تشغيله على المشروع بالكامل (`npm run format`). تم التحقق من عدم كسر أي شيء بعد التنسيق (الاختبارات + `tsc -b` + `vite build` كلها ناجحة).

**الحالة:** ✅ تم الحل.

---

## 🚀 نظام النشر التلقائي (Auto-Deploy) — 2026-08-13

### 14. تم إضافة نظام نشر تلقائي كامل (`deploy/`)
**الوصف:** بناءً على طلب صريح، تمت إضافة نظام يراقب فرع `main` على GitHub كل دقيقة (systemd timer)، وعند وصول commit جديد: يسحبه، يبني الحاويات، يشغّلها، يعمل health check، ولو فشل أي شيء يرجع تلقائيًا لآخر commit شغال معروف.

**قرار معماري مهم:** يستخدم `git reset --hard` على مجلد نشر مخصص (`/opt/mkdd-live`) منفصل تمامًا عن أي مجلد تطوير يدوي، لأن هذا الأمر يمسح أي تعديل غير محفوظ. التفاصيل الكاملة في `deploy/README.md`.

**تم اختباره فعليًا (محاكاة معزولة، ريبو git محلي وهمي):**
- Commit جديد سليم → يُبنى ويُشغَّل بنجاح.
- Commit مكسور (build يفشل عمدًا) → السكريبت يكتشف الفشل، **يرجع تلقائيًا** لآخر commit شغال، يعيد بناءه، يتأكد من الـ health check، وينجح.
- Commit مُصلَح بعد commit مكسور → يعود للعمل الطبيعي.

**بگين حقيقيين اكتُشفا وأُصلحا أثناء الاختبار الفعلي (وليس نظريًا):**
1. **افتراض خاطئ لاسم الفرع الافتراضي:** السكريبت الأصلي كان بيعتمد على إن `git clone` هيهبط تلقائيًا على فرع `main`. تم اكتشاف (بالاختبار الفعلي) أن هذا غير مضمون حسب إعدادات السيرفر البعيد. **الحل:** إضافة `git checkout -B main origin/main` صريحة بعد كل clone.
2. **مفيش نشر أولي عند التركيب:** `install.sh` كان بيعمل `clone` بس بدون تشغيل أول build فعلي، والسكريبت الدوري ماكانش هيعتبرها "تحديث جديد" لحد أول push تاني — يعني الموقع كان هيفضل واقف بعد التركيب مباشرة. **الحل:** `install.sh` بقى يستدعي `./setup.sh up` صراحة كخطوة أخيرة في التركيب.

**الحالة:** ✅ تم البناء والاختبار (محاكاة محلية)؛ يحتاج تأكيد نهائي بتشغيل `deploy/install.sh` على الـ VM الفعلي.

### 15. عزل نظام النشر التلقائي بالكامل عن النظام الإنتاجي (بورتات/أسماء/صورة/volume منفصلة)
**الوصف:** طلب المستخدم صراحة أن يعمل نظام النشر التلقائي دون التأثير على النظام الشغال فعليًا على الـ VM — بنفس مبدأ التجربة اليدوية المعزولة في 2026-08-13.

**الحل:** تم تعديل `compose.yml` ليكون كل معرّف يواجه المضيف (host-facing) — البورتات، أسماء الحاويات، اسم صورة `agent-canvas`، اسم الـ volume — قابلاً للتهيئة عبر متغيرات بيئة، بقيم افتراضية = القيم الأصلية بالضبط (صفر تأثير على أي استخدام عادي). تم إنشاء `deploy/.env.staging` بنفس البورتات المستخدمة في التجربة اليدوية (18000/13000/15173/18787)، وأسماء حاويات/صورة/volume منفصلة تمامًا. `install.sh` ينسخ هذا الملف لمجلد النشر تلقائيًا.

**بگ حقيقي اكتُشف وأُصلح أثناء الاختبار الفعلي:** `docker compose` يقرأ ملف `.env` تلقائيًا لتعويض المتغيرات داخل `compose.yml`، لكن `setup.sh` نفسه (منطقه الخاص بالـ health check والرسائل المطبوعة) **لا** يقرأ `.env` تلقائيًا من تلقاء نفسه — لازم يُصرَّح بذلك صراحة. تم اكتشاف هذا بالاختبار الفعلي (health check كان سيفحص المنفذ الخطأ رغم أن الحاوية نفسها تعمل صح على المنفذ المعزول)، وتم إصلاحه بجعل `setup.sh` و`auto-deploy.sh` يقرآن `.env` صراحة (`set -a; source .env; set +a`) في بداية كل تشغيل.

**تم التحقق فعليًا:** اختبار معزول أثبت أن القيمة المُحلَّلة لمنفذ health check تكون `8787` في غياب `.env` (سلوك إنتاجي طبيعي)، وتتحول تلقائيًا لـ `18787` في وجود `deploy/.env.staging` — بدون أي تغيير آخر.

**الحالة:** ✅ تم الحل.

---

### 16. الموظفين (Agent Profiles) لا يظهرون في نشر جديد (staging / VM جديدة)
**الوصف:** بعد تشغيل الـ staging stack بنجاح لأول مرة، لوحظ أن قائمة الموظفين فاضية تمامًا رغم أن ملفات `company-agents-definitions/*.md` موجودة ومربوطة صح.

**السبب الجذري (تم تأكيده بفحص الكود + استعلام حي على الإنتاج):** الـ Agent Profiles **ليست مُولَّدة تلقائيًا** من ملفات `company-agents-definitions/`. هي كيانات runtime منفصلة تمامًا، تُنشأ فقط عبر استدعاء `POST /api/agent-profiles/{name}` (تم تأكيد الـ endpoint من `openapi.json` الحي على الـ VM)، ومُخزَّنة داخل الـ volume الخاص بحالة OpenHands نفسه (`openhands_state`). الإنتاج عنده الـ 13 موظف لأنهم أُنشئوا يدويًا في مرحلة سابقة وتراكموا في الـ volume القديم — وهذا **غير موجود في أي مكان بالريبو**، فأي نشر جديد (staging، أو VM جديدة تمامًا) سيبدأ بصفر موظفين حتى لو كل الكود صحيح 100%.

**دليل حي من الإنتاج (عبر `GET /api/employees`):**
```json
{
  "name": "product-manager",
  "agent_kind": "openhands",
  "llm_profile_ref": "nemotron-3-ultra-free",
  ...
}
```

**الحل (قرار منتج صريح):** تم إنشاء `mkdd-ui/server/scripts/bootstrap-employees.mjs` — يقرأ أسماء الموظفين من `company-agents-definitions/`، وينشئ profile لكل واحد عبر نفس آلية المصادقة الموجودة بالفعل في `server/lib/openhands-client.mjs`، **بدون ربط بـ LLM** (بقرار صريح: الموظف يُنشأ فارغًا، والربط بـ LLM يتم لاحقًا لكل موظف يدويًا من الإعدادات).

**⚠️ غير مؤكد 100% بعد:** حقول الطلب (`agent_kind: "openhands"`, `agent: "CodeActAgent"`) مبنية على استنتاج من بيانات حقيقية موجودة + بحث في الكود المصدري، **وليس من توثيق رسمي صريح** (ملف `openapi.json` عام جدًا ولا يحدد الحقول). يحتاج اختبار فعلي على الـ staging (بيئة معزولة وآمنة للتجربة) قبل اعتباره حلاً نهائيًا.

**الحالة:** 🔴 مفتوحة — الكود جاهز، لسه محتاج اختبار فعلي وتأكيد النجاح.

---

### 17. `auto-deploy.sh` كان يفشل بصمت تحت systemd بسبب `git` "dubious ownership"
**الوصف:** بعد التركيب الأول الناجح (`install.sh` شغّله المستخدم مباشرة)، كل الدورات اللاحقة اللي شغّلها الـ systemd timer كانت **تفشل فورًا** برسالة:
```
fatal: detected dubious ownership in repository at '/opt/mkdd-live'
```
والنتيجة: أي commit جديد بعد التركيب الأول **لم يصل أبدًا** للـ staging، رغم أن اللوج لم يُظهر أي "CRITICAL" واضح (فشل الأمر بصمت داخل `git fetch` نفسه، قبل حتى منطق اكتشاف الـ commit الجديد).

**التشخيص:** إصلاح يدوي بـ `sudo git config --global --add safe.directory /opt/mkdd-live` بدا ناجحًا (اتسجل فعليًا في `/root/.gitconfig`)، لكن الفشل استمر. تم تأكيد السبب بالفحص المباشر:
```
sudo systemctl show mkdd-auto-deploy.service -p Environment
→ Environment=   (فاضي تمامًا)
```
يعني الـ systemd service بيشتغل بدون `$HOME` معرّف، فـ git بيتجاهل `/root/.gitconfig` بالكامل تحت هذا السياق.

**الحل الدائم (بدل الاعتماد على إعداد يدوي على كل VM):**
1. `auto-deploy.sh` نفسه بقى يسجّل `git config --global --add safe.directory "$DEPLOY_DIR"` **في بداية كل تشغيل** (idempotent — بيتفحص الأول قبل الإضافة)، فمبقاش معتمد على أي إعداد خارجي.
2. `mkdd-auto-deploy.service` بقى فيه `Environment=HOME=/root` صريح، كدفاع إضافي.

**الحالة:** ✅ تم الحل (الكود جاهز، محتاج push + دورة واحدة تنجح للتأكيد النهائي).

---

### 18. سرعة الفحص الدورية (بناءً على طلب صريح)
**الوصف:** تم تقليل الفاصل الزمني للـ `mkdd-auto-deploy.timer` من دقيقة واحدة إلى **20 ثانية**.

**السبب:** `git fetch` ضد ريبو بدون تغيير عملية خفيفة جدًا، والقفل (`flock`) في `auto-deploy.sh` يضمن عدم تداخل الدورات مع بعضها حتى لو استغرق أي build فعلي وقتًا أطول من 20 ثانية.

**الحالة:** ✅ تم الحل.

---

### 19. `AGENTS.md` اتحسب غلط كـ "موظف رقم 14"
**الوصف:** بعد إضافة `AGENTS.md` لمجلد `company-agents-definitions/` (مشكلة #16)، اكتُشف أثناء اختبار سكريبت الـ bootstrap الفعلي إن الفلتر الموجود (بيستثني `company-orchestrator.md` بس) مش كافي — `AGENTS.md` نفسه بقى بيتحسب كموظف اسمه "AGENTS".

**الأثر:** المشكلة **موجودة في مكانين**: `server/routes/directory.mjs` (الإنتاج الحقيقي، `/api/employees`) و`bootstrap-employees.mjs`. لو محدش صلّحها، أي استدعاء لـ `/api/employees` بعد إضافة `AGENTS.md` كان هيحاول يتعامل مع "AGENTS" كموظف حقيقي.

**الحل:** استخراج دالة موحدة `listEmployeeNames()` في `server/lib/list-employee-definitions.mjs`، بقائمة استثناء صريحة (`company-orchestrator.md`, `AGENTS.md`) بدل شرط واحد بس. تُستخدم الآن في المكانين. مُغطاة بـ 4 اختبارات جديدة (منها اختبار regression صريح لنفس الباگ ده).

**الحالة:** ✅ تم الحل.

---

### 20. إنشاء Agent Profile يتطلب `llm_profile_ref` إجباريًا (لا يمكن الإنشاء فارغًا)
**الوصف:** أول تشغيل فعلي لـ `bootstrap-employees.mjs` على الـ staging فشل في كل الـ 13 محاولة بنفس الخطأ:
```
HTTP 422: {"detail":[{"loc":["openhands","llm_profile_ref"],"type":"missing","msg":"Field required"}]}
```
يعني الافتراض الأصلي (إنشاء الموظف فارغًا، وربط LLM بعدين) **غير ممكن تقنيًا** — الـ API يرفض الإنشاء بالكامل بدون `llm_profile_ref` صالح وقت الإنشاء نفسه.

**الحل (قرار منتج):** بما إن قيمة الـ LLM محتاجة تكون حقيقية وقت الإنشاء، وبما إن المستخدم هو اللي هيظبط إعدادات الـ LLM بنفسه، تم تعديل السكريبت ليطلب اسم LLM profile **كمدخل صريح** عبر متغير بيئة (`MKDD_BOOTSTRAP_LLM_PROFILE_REF`) بدل تخمين قيمة أو استخدام قيمة من الإنتاج القديم (زي `nemotron-3-ultra-free`، غير موجودة أصلًا في الـ volume الفارغ الجديد). السكريبت يرفض العمل برسالة خطأ واضحة لو المتغير غير موجود، بدل فشل صامت أو تخمين خاطئ.

**سير العمل المطلوب من المستخدم:**
1. إنشاء LLM profile واحد يدويًا من الإعدادات (بمفتاحه الخاص).
2. تشغيل: `docker exec -e MKDD_BOOTSTRAP_LLM_PROFILE_REF=<اسم-الـ-LLM-profile> mkdd-ui-staging node server/scripts/bootstrap-employees.mjs`
3. كل الـ 13 موظف يُنشأون مرتبطين بنفس الـ LLM مبدئيًا، وقابلين للتعديل الفردي لاحقًا من الواجهة.

**الحالة:** ✅ تم الحل (الكود جاهز، محتاج تشغيل فعلي بعد ما المستخدم يجهّز LLM profile).

---

### 21. الموظفين اتعملوا بنجاح بس بدون تعليمات/شخصية (Employee Instructions فاضية)
**الوصف:** بعد نجاح إنشاء الـ13 موظف فعليًا (مشكلة #20 اتحلت وتم تشغيلها بنجاح: `created=13 failed=0`)، لوحظ بصريًا في الواجهة إن حقل "Employee Instructions" لكل موظف **فاضي تمامًا**.

**السبب:** السكريبت أنشأ الـ profile بأساسيات بس (`agent_kind`, `agent`, `llm_profile_ref`) — من غير الحقل `system_message_suffix` (نفس الحقل اللي التخصيص الأصلي لـ MKDD في `agent-profiles-local-view.tsx` معمول عشان يعرضه/يعدّله، موثق في README قسم 9). الحقل ده هو اللي المفروض يحمل هوية وتعليمات الموظف الفعلية (كل محتوى ملف الـ`.md` بعد الـ frontmatter — "أنت باجوش، مدير المنتج..." إلخ).

**الحل:** تعديل السكريبت ليقرأ محتوى كل ملف `.md`، يفصل الـ frontmatter (بين علامتي `---`) عن المحتوى الفعلي، ويبعت المحتوى كـ `system_message_suffix` ضمن نفس طلب الإنشاء. تم التحقق من صحة منطق الفصل على كل الـ13 ملف فعليًا (كل ملف بيدّي محتوى بين 9,500 و16,000 حرف، يبدأ بجملة تعريف الموظف الصحيحة زي "You are Shenouda...").

**تحسين إضافي:** بما إن `POST /api/agent-profiles/{name}` عملية upsert (إنشاء أو تحديث)، تم تعديل السكريبت ليعيد التطبيق على أي موظف موجود بالفعل (مش يتخطاه) — عشان يكون آمن يتشغّل أكتر من مرة، ويصلح تلقائيًا أي موظف اتعمل قبل كده بدون تعليمات.

**الحالة:** ✅ تم الحل (الكود جاهز، محتاج إعادة تشغيل فعلي للتأكيد النهائي).

**تحديث — تم التأكيد الفعلي (2026-08-13):** بعد إعادة تشغيل السكريبت بعد هذا الإصلاح، تم التأكد بصريًا من الواجهة إن حقل "Employee Instructions" بقى ممتلئ بمحتوى شخصية/تعليمات الموظف الصحيحة. رحلة تفعيل الموظفين (المشاكل #16، #19، #20، #21) اكتملت بنجاح كامل: 13 موظف، كل واحد بهويته وتعليماته وربطه بـ LLM.

---

### 22. اكتشافات أثناء بناء ميزة "مشروع جديد" (قبل الرفع)
**السياق:** تنفيذ زر "مشروع جديد"، طبقًا للمبدأ الجديد في `ENGINEERING_PRINCIPLES.md` #1 (كل ميزة لازم أساس حقيقي في `openhands-agent-canvas`). تم تأكيد الآلية الحقيقية (`POST /api/workspaces`) من `openapi.json` الحي، ومن كود `folder-browser-modal.tsx` الأصلي، قبل أي تنفيذ.

**بگ 1 — تعارض في التوجيه (routing):** `handleProjects` (route القراءة الموجود مسبقًا لـ `/api/projects`) كان بيتفحص الـ URL بس بدون الـ method. لو سبناه، أي `POST /api/projects` جديد كان هيتخطف من الـ route القديم قبل ما يوصل لـ route الإنشاء الجديد خالص. **تم اكتشافه ذاتيًا أثناء المراجعة قبل أي اختبار حي، وتم إصلاحه فورًا** بإضافة `req.method !== "GET"` للشرط.

**بگ 2 — فشل أسماء المشاريع العربية بالكامل:** المشروع بالكامل ثنائي اللغة (README قسم 32)، لكن دالة تحويل الاسم لمسار آمن (`slugify`) كانت بتشيل أي حرف غير إنجليزي بالكامل — يعني اسم عربي خالص كان هيطلع فاضي ويرفض الإنشاء بدون سبب واضح للمستخدم. **الحل:** فصل بين اسم العرض (يُخزَّن كما هو، أي لغة، يُرسَل لـ OpenHands كـ `name`) والـ slug (يُستخدم للمسار على القرص فقط بمحارف إنجليزية آمنة)، مع توليد تلقائي (`project-<uuid>`) لو الاسم مفيهوش أي حرف/رقم إنجليزي يصلح كمسار.

**الحالة:** ✅ تم الحل — الاثنين اكتُشفا ذاتيًا قبل أي رفع أو اختبار حي، طبقًا لقاعدة "اختبر قبل ما تعتمد".

---

### 23. `POST /api/workspaces` يحتاج غلاف `{"workspaces": [...]}`، مش array مباشر
**الوصف:** أول محاولة فعلية لإنشاء مشروع من الواجهة فشلت برسالة `"Input should be a valid dictionary or object to extract fields from"`. الكود الأصلي كان بيبعت array مباشر (`[{id, name, path}]`)، بناءً على قراءة سريعة لكود `folder-browser-modal.tsx` اللي بيستدعي `client().addWorkspaces(items)` — بدون التأكد من الـ HTTP body الفعلي اللي الـ SDK بيبنيه تحت الغطاء.

**التشخيص:** استعلام حي لـ `openapi.json` على schema اسمه `AddWorkspacesRequest` أظهر إن الشكل الصحيح هو:
```json
{ "workspaces": [ { "id": "...", "name": "...", "path": "..." } ] }
```
وليس array مباشر. تم أيضًا التأكد من `WorkspaceItem` نفسه: الحقول الإجبارية هي بالظبط `id`, `name`, `path` (و`parentPath` اختياري) — يعني باقي الكود كان صحيحًا، والمشكلة فقط في الغلاف الخارجي.

**الدرس المستفاد (يعزز ENGINEERING_PRINCIPLES.md #1):** قراءة كود الـ frontend اللي بيستخدم SDK خارجي (`@openhands/typescript-client`) مش كافية لمعرفة شكل الـ HTTP body الفعلي — الـ SDK ممكن يغيّر شكل البيانات تحت الغطاء. **يجب دائمًا التأكيد من `openapi.json` الحي كخطوة أخيرة قبل الاعتماد على أي endpoint**، حتى لو الكود بيوحي بشكل معين.

**الحل:** تعديل `server/routes/projects.mjs` ليبعت الغلاف الصحيح.

**الحالة:** ✅ تم الحل، محتاج إعادة اختبار فعلي للتأكيد النهائي.

---

### 24. المحادثات اللي بينشئها MKDD تظهر بدون اسم في Agent Canvas
**الوصف:** المستخدم لاحظ إن المحادثات اللي بتتعمل من MKDD بتظهر في واجهة Agent Canvas الأصلية بدون اسم، وبتبان وكأنها مش منظمة تحت الـ Workspace بتاعها.

**التحقيق:** فحص كود `use-create-conversation.ts` الحقيقي في `openhands-agent-canvas` أظهر إن الـ payload الأصلي لإنشاء محادثة بيبعت حقل `title` صريح (`title: conversationInstructions ?? null`). كود MKDD (`server/routes/chat.mjs`) كان بيبني المحادثة الجديدة **بدون** هذا الحقل خالص، فتفضل بدون اسم دايمًا (`autotitle: false` كمان يمنع أي تسمية تلقائية من OpenHands نفسه).

**الحل:** تعديل `handleChatSend` ليبني `title` تلقائيًا من **اسم عرض الموظف + اسم المشروع** (مطابق تمامًا لطلب المستخدم صراحة: "لازم تاخد اسم الموظف واسم المشروع مع بعض")، عبر:
- `server/lib/employee-display-info.mjs` (دالة مشتركة مُستخرجة من `directory.mjs`، بدل تكرار نفس منطق قراءة الـ frontmatter).
- استعلام `/api/workspaces` لجلب اسم عرض المشروع الحقيقي (نفس مصدر البيانات اللي `fetchProjects()` بيقرأ منه أصلًا).

**الحالة:** ✅ تم الحل (يُطبَّق على المحادثات الجديدة فقط؛ المحادثات القديمة الموجودة بالفعل بدون اسم تحتاج معالجة يدوية أو سكريبت منفصل لاحقًا لو لزم الأمر).

---

### 25. الموظف مفيش عنده "وعي" بالوقت الحالي (بيقول "الشهر اللي فات" وهو اتكلّم من ساعة)
**الوصف:** طلب صريح من المستخدم — الموظف محتاج يعرف الوقت الحقيقي وقت كل رسالة، عشان مايحصلش تصريحات زمنية غلط.

**البحث (طبقًا لـ ENGINEERING_PRINCIPLES.md #1، قبل أي تنفيذ):**
- تم البحث في التوثيق الرسمي الكامل لـ `openhands-agent-canvas` (`docs/`) — **مفيش أي ذكر** لآلية وعي بالتاريخ/الوقت.
- المستخدم تأكد بنفسه من واجهة Agent Canvas الحقيقية (`http://192.168.2.18:13000`) — **مفيش أي إعداد** بخصوص الموضوع ده في المحادثات.
- تم العثور على آلية قريبة (`agent_context` / `system_message_suffix`، موثقة في `docs/architecture.md` قسم "Runtime services") لكنها **ثابتة (static)** تُضبط مرة واحدة وقت إنشاء الـ Agent Profile — مش قادرة تحمل وقت حي يتغيّر كل رسالة.

**الحل (الحل الضامن، بعد استبعاد أي آلية جاهزة):** حقن الوقت الحالي الحقيقي مع كل رسالة تتبعت للموظف عبر `server/lib/time-context.mjs`:
- علامة مخفية بالشكل `<!--mkdd:time:ISO_TIMESTAMP-->` تُضاف في بداية كل رسالة مُرسَلة لـ OpenHands.
- الموظف يتعلّم شكل العلامة دي مرة واحدة بس (إضافة موحدة في `system_message_suffix` لكل الـ13 موظف عبر `bootstrap-employees.mjs`، بدل تكرارها في كل ملف `.md` على حدة).
- العلامة تُشال تلقائيًا من رسائل المستخدم قبل ما توصل للواجهة (في `normalizeEvent` المشترك — يعني REST والـ WebSocket الاثنين بيستفيدوا من نفس الإصلاح، طبقًا لانضباط الـ "Phase A").

**التحقق الذي تم إجراؤه فعليًا:** 5 اختبارات جديدة تغطي: صحة شكل العلامة، الحقن، التجريد، عدم التأثير على نص بلا علامة، والـ round-trip الكامل (حقن ثم تجريد يرجّع النص الأصلي تمامًا، بما فيه نصوص عربية ومتعددة الأسطر). كمان اختباران في `normalize-event.test.mjs` يتأكدوا إن التجريد بيحصل لرسائل المستخدم بس، مش رسائل الموظف.

**الحالة:** ✅ تم الحل (الكود جاهز ومُختبَر منطقيًا؛ محتاج تشغيل `bootstrap-employees.mjs` تاني على الموظفين الموجودين عشان ياخدوا التعليمات الجديدة، ثم اختبار فعلي بمحادثة حية للتأكيد النهائي).

**تحديث — اكتُشفت مشكلة فرعية أثناء الاختبار الفعلي:** بعد تصحيح المنطقة الزمنية للـ VM نفسه (من UTC لـ `Europe/Amsterdam`)، لوحظ إن الموظف لسه بيرد بوقت UTC خام (فارق ساعتين عن التوقيت المحلي الصحيح). **السبب:** الحاوية (`mkdd-ui-staging`) بتعرض الوقت الداخلي بـ UTC دايمًا، بغض النظر عن إعداد المنطقة الزمنية للـ VM المضيف (Docker containers لا ترث إعداد timezone المضيف تلقائيًا). بما إن `withTimeContext` كان بيبعت UTC صراحةً أصلًا (ممارسة هندسية صحيحة — التخزين/النقل بـ UTC دايمًا)، المشكلة الحقيقية كانت إن الموظف **مايعرفش** يحوّل الـ UTC ده لتوقيت المستخدم المحلي.

**الحل النهائي:** إضافة `MKDD_TIMEZONE` كمتغير بيئة قابل للتهيئة (افتراضي: `Europe/Amsterdam`)، بيتحقن في تعليمات كل موظف صراحة: "المستخدم في منطقة زمنية كذا، حوّل الـ UTC ليها قبل ما ترد". الدالة `buildTimeContextInstructions(timezone)` بدل ثابت واحد، عشان تدعم أي منطقة زمنية مستقبلًا بدون تعديل كود.

**الحالة:** ✅ تم الحل بالكامل، محتاج إعادة تشغيل `bootstrap-employees.mjs` واختبار فعلي أخير للتأكيد.

---

### 26. ثلاث ملاحظات فعلية من المستخدم بعد رؤية الـ UI الجديد على staging
**السياق:** بعد نشر التصميم الجديد (مشكلة #22)، اختبر المستخدم فعليًا وقدّم فيدباك محدد بالصور.

**26.1 — اسم الموظف في الشات كان "معلّق" مش ملاصق للصورة/الرسالة**
الاسم كان في صف منفصل بعرض كامل الحاوية بدل ما يكون ملاصق للصورة الصغيرة فوق الرسالة مباشرة. **الحل:** إعادة هيكلة لصف مضغوط (`agent-message-header`): صورة صغيرة (22px) + اسم بجانبها في نفس السطر، فوق الرسالة مباشرة.

**26.2 — تكرار عرض الموظفين في صفحة المشروع**
بعد إضافة شريط الأفاتار (مشكلة #22)، بقى فيه عرضين منفصلين لنفس الـ13 موظف (شريط بسيط فوق + كروت تفصيلية تحت). **الحل:** حذف الشريط البسيط، ونقل الكروت التفصيلية (بالبادجات: الحالة، التكلفة، تقدم المهام) لتبقى هي نفسها في المكان العلوي.

**26.3 — إضافة صورة للموظف كشفت باگ حقيقي كان موجود من قبل**
أثناء تنفيذ طلب "إضافة صورة للموظف"، اكتُشف إن `avatarUrl` كان دايمًا بيتبني كمسار (`/avatars/{name}.webp`) **بدون وجود أي سيرفر بيقدّمه فعليًا** — يعني كل الصور كانت بتظهر كأيقونة "صورة مكسورة" بدل الحرف الافتراضي. **الحل الكامل:**
- `server/routes/avatars.mjs` (جديد): رفع (`POST /api/employees/{id}/avatar`، عبر data URL بدل multipart لتجنّب اعتمادية جديدة) وعرض (`GET /avatars/{id}`) حقيقيين، مع تخزين فعلي في `/mkdd-data/avatars/`.
- `directory.mjs` بقى يرجّع `avatarUrl: null` (مش مسار وهمي) لو مفيش صورة مرفوعة فعليًا — الواجهة بترجع للحرف الافتراضي صح دلوقتي.
- زر رفع (أيقونة كاميرا صغيرة) على كل كارت موظف.

**تم التحقق فعليًا (تشغيل حي للسيرفر):** رفع صورة حقيقية → عرضها بنجاح (`200`, `image/png` صح) → رفض موظف وهمي (`404`) → رفض بيانات غير صالحة (`400`) → موظف حقيقي بدون صورة يرجّع `404` بدل صورة مكسورة. الخمس سيناريوهات نجحوا.

**باگ HTML إضافي اكتُشف وأُصلح أثناء التنفيذ:** إضافة زر الرفع جوه كارت الموظف (اللي كان `<button>`) خلق تعشيش عناصر تفاعلية غير صالح (`<button>` جوه `<button>`) — HTML غير صحيح. تم التصحيح بتحويل الكارت لـ `<div role="button" tabIndex={0}>` مع دعم لوحة المفاتيح (`Enter`/`Space`).

**الحالة:** ✅ تم الحل بالكامل، الثلاثة نقاط.

---

### 27. لون غلاف لكارت المشروع (طلب صريح)
**الوصف:** لما يكون فيه أكتر من مشروع، صعب التفرقة بينهم بصريًا بسرعة. طلب المستخدم اختيار لون غلاف وقت إنشاء المشروع.

**قرار معماري:** OpenHands نفسه (`LocalWorkspace` type) مفهوش أي مفهوم "لون" — الحقول الوحيدة هي `{id, name, path, parentPath}` (مؤكَّد من الكود، طبقًا لـ `ENGINEERING_PRINCIPLES.md` #1). بما إن ده بيانات حقيقية اختارها المستخدم فعلًا (مش مُختلَقة)، لكن مفيش مكان حقيقي في OpenHands لتخزينها، تم إنشاء تخزين مخصص بسيط لـ MKDD (`server/lib/project-metadata.mjs`)، بنفس نمط الكتابة الذرية (`atomic write: temp file + rename`) المُستخدم بالفعل في `workflow-state.mjs` — إعادة استخدام نمط مُثبَت بدل اختراع آلية جديدة.

**التنفيذ:** قائمة ألوان محدودة ومُتحقَّق منها (6 ألوان)، `POST /api/projects` يقبل `color` اختياري (يرجع للون الافتراضي لو غير صالح بصمت، بدل رفض الطلب بالكامل)، `GET /api/projects` يُرفق اللون في كل مشروع. منتقي ألوان (دوائر قابلة للنقر) في Modal "مشروع جديد"، ولون حدّ جانبي واضح (4px) على كل كارت مشروع في القائمة.

**تم التحقق فعليًا:** 5 اختبارات جديدة لمنطق التخزين (قيمة افتراضية، حفظ/قراءة، رفض لون غير مسموح، عدم تداخل مشاريع مختلفة، الكتابة فوق قيمة سابقة) — كلها عدّت. الجزء المعتمد على OpenHands (إنشاء مشروع فعلي) لم يُختبر حيًا في هذه البيئة (بدون Docker)، محتاج تأكيد فعلي على staging.

**الحالة:** ✅ تم الحل (منطق التخزين مُختبَر بالكامل؛ التكامل الكامل مع الواجهة يحتاج تأكيد حي على staging).

---

### 28. الهيدر والـ composer مبيلزموش مكانهم فعليًا رغم `position: sticky` (اكتشاف من فيدباك حي بالصور)
**الوصف:** المستخدم لاحظ إن هيدر صفحة المشروع بيختفي أثناء التمرير العادي (مش ملازم لأعلى الشاشة زي المفروض)، رغم إنه مكتوب `position: sticky` في الكود.

**السبب الجذري (اتّضح بالتدقيق، مش تخمين):**
1. **الهيدر:** عنصر أب (`.app`) عنده `overflow-x: hidden`. طبقًا لمواصفة CSS، لو أي محور (x أو y) اتحدد بقيمة غير `visible`، المحور التاني بيتحول ضمنيًا لـ `auto` — وده بيلغي سلوك `sticky` بصمت في حالات معينة، من غير أي رسالة خطأ.
2. **الـ composer (صندوق كتابة الرسالة):** المشكلة كانت أوضح — لقينا **قاعدتين CSS صريحتين** (`​.project-view .composer` و`.chat-screen > .composer`) بيفرضوا `position: static` بتخصصية أعلى من القاعدة الأساسية، فكانوا بيلغوا أي `sticky`/`fixed` بشكل كامل ومقصود (على الأرجح leftover من تصميم قديم).

**الحل:** الاستبدال بـ `position: fixed` (أكتر ثباتًا من `sticky` في التركيبات المعقدة، ومناعة ضد مشاكل الـ containing block) للهيدر والـ composer، مع تعويض المساحة المفقودة من الـ flow (`padding-top` على المحتوى تحت الهيدر، `padding-bottom` على قائمة الرسائل فوق الـ composer)، وتصحيح القاعدتين اللي كانوا بيفرضوا `static`.

**خطر تجنّبناه أثناء الإصلاح:** أول محاولة استخدمت `calc(50% - 410px)` لتوسيط الـ composer الثابت أفقيًا — القيمة دي بتبقى **سالبة** على شاشات الموبايل العادية (أضيق من 820px)، وده كان ممكن يكسر التنسيق بالكامل على أغلب الأجهزة. تم اكتشاف الخطر ذاتيًا قبل أي رفع، واستُبدل بالنمط الآمن القياسي (`max-width` + `margin: 0 auto`).

**الحالة:** ✅ تم الحل (الكود جاهز ومبني بنجاح؛ محتاج تأكيد بصري حي على staging).

---

### 29. كروت الموظفين لسه عمودية بدل أفقية (توضيح إضافي من المستخدم)
**الوصف:** بعد دمج الشريط والكروت (مشكلة #26.2)، المستخدم وضّح إن المطلوب كان **عرض أفقي** (جنب بعض، زي المرجع البصري الأصلي)، مش عرض عمودي (تحت بعض) — كان فهم خاطئ مني للتعليمات الأولى.

**الحل:** تحويل `.project-home .employee-grid` من `grid` عمودي لصف قابل للتمرير أفقيًا (`display: flex; overflow-x: auto`) مع `scroll-snap`، وتحويل كل كارت موظف لتصميم مضغوط رأسي (أفاتار فوق، اسم، دور، بادجات) بعرض ثابت (128px) بدل التصميم الأفقي العريض القديم. حذف الـ media queries القديمة الخاصة بعدد الأعمدة (مبقتش لازمة).

**الحالة:** ✅ تم الحل، محتاج تأكيد بصري حي على staging.

---

### 30. صورة الموظف اترفعت بنجاح لكن ظهرت "مكسورة" في المتصفح
**الوصف:** المستخدم رفع صورة فعليًا لموظف ("مريم")، لكن ظهرت كأيقونة صورة مكسورة بدل الصورة الحقيقية.

**التشخيص (بالأدلة، مش تخمين):** تأكيد مباشر من الـ VM:
```
ls -la /opt/mkdd-live/mkdd-data/avatars/  → ui-ux.png موجود فعليًا (972KB)
curl .../avatars/ui-ux  → 200, image/png (السيرفر شغال 100% صح)
```
يعني الـ backend سليم تمامًا. السبب الحقيقي: **كاش المتصفح**. لو المتصفح حاول يحمّل نفس الرابط (`/avatars/ui-ux`) قبل كده ووصله 404 (مثلاً في محاولة رفع أولى فشلت)، بيفضل يعرض الفشل المخزّن ده حتى بعد ما الملف بقى موجود فعليًا — لأن الرابط نفسه ما اتغيّرش.

**الحل:** كل رابط صورة موظف بقى يحمل "بصمة إصدار" (`?v={وقت آخر تعديل للملف}`) — أي رفعة جديدة تُنتج رابط مختلف شكليًا تلقائيًا، فالمتصفح مُجبَر يجيب نسخة جديدة دايمًا، مستحيل يعرض فشل قديم مخزّن. تمت إضافة `Cache-Control: immutable` أيضًا كتحسين إضافي، آمن تمامًا بما إن كل رابط بقى فريد لكل نسخة من الصورة.

**تم التحقق فعليًا:** رفع صورة → الرابط الراجع فيه `?v=...` → طلب نفس الرابط بالظبط يرجّع `200` و`image/png` صح.

**الحالة:** ✅ تم الحل، محتاج تأكيد بصري حي على staging (ولو المستخدم لسه شايف الصورة القديمة، مسح الكاش أو Hard Refresh هيحل أي أثر متبقي من قبل الإصلاح).

---

### 31. رفع صورة الموظف: الجاليري بيفتح، لكن مفيش أي حاجة بتحصل بعد اختيار الصورة
**الوصف:** المستخدم أكّد إنه اختار صورة فعلًا من الجاليري، لكن مفيش أي تأكيد، خطأ، أو أي مؤشر — "مفيش أزرار نهائي".

**تشخيص أولي فشل:** طلبت مراقبة `docker logs -f mkdd-ui-staging` وقت اختيار الصورة — **مفيش سطر جديد ظهر**. لكن اتضح إن ده تشخيص مُضلِّل: السيرفر أصلًا مكانش بيسجّل أي طلبات واردة، فغياب السطور مايثبتش إن الطلب معملوش وصل — بس إثبات إن مفيش logging أصلًا.

**السبب الجذري المرجّح:** الكود كان بيعتمد على `<label>` بيلف `<input type="file" hidden>` — أسلوب شائع لكن مش موثوق 100% في كل تركيبات المتصفح/الجهاز، وكان بيفشل بصمت من غير أي مؤشر بصري يوضح المشكلة للمستخدم.

**الحل الشامل:**
1. **إعادة بناء الميزة في component منفصل** (`src/components/EmployeeAvatarUpload.tsx`) — بيستخدم `useRef` + `.click()` صريح على `<input>` بدل الاعتماد على تمرير الكليك التلقائي من `<label>`، وده أسلوب أوضح وأكتر ثباتًا في React.
2. **مؤشرات بصرية واضحة على الموبايل نفسه** (بدون الحاجة لأدوات المطور): أيقونة الكاميرا بتتحول لـ "…" أثناء الرفع، ولـ "!" أحمر لو فشل (لمدة 3 ثواني)، فالمستخدم يشوف أي مشكلة فورًا من غير أي أداة إضافية.
3. **تسجيل حقيقي في السيرفر** (`console.log`) لكل محاولة رفع — نجاح أو فشل، مع التفاصيل (حجم الصورة، نوعها، سبب الرفض لو حصل) — عشان أي تشخيص مستقبلي يبقى فعّال فعلًا (مش زي المحاولة الأولى اللي فشلت لغياب أي تسجيل من الأساس).

**تم التحقق فعليًا:** تشغيل حي أثبت التسجيل الجديد بيشتغل صح لحالتي النجاح والفشل، مع تفاصيل دقيقة (حجم، نوع، سبب الرفض).

**الحالة:** ✅ تم الحل، محتاج تأكيد بصري حي على staging (وده أول مرة هيكون عندنا تسجيل حقيقي لو المشكلة استمرت بشكل مختلف).

---

### 32. هيدر موحّد + Sidebar + خطوط مخصصة + إعادة تنظيم كامل (طلب كبير مُقسَّم لمراحل)
**الوصف:** طلب شامل من المستخدم: هيدر ثابت 100% بدون أي تغيير بين الشاشات (خط Bodoni Moda، "MKDD" إنجليزي دايمًا وأكبر من الوصف الفرعي)، زر منيو يفتح Sidebar بتصنيف المشاريع (نشطة/قربت تخلص/خلصت) وقائمة الموظفين (مع بروفايل وتغيير صورة)، خط Cormorant Garamond لباقي التطبيق، تنظيم كود أفضل، وتوثيق خريطة الملفات في README.

**القرار المعماري الأساسي:** الهيدر (`AppHeader.tsx`) بقى مكوّن واحد يُستضاف مرة واحدة بس في `App.tsx` (بدل ما كل شاشة تبني هيدرها الخاص) — أي تعديل عليه دلوقتي بيظهر تلقائيًا في كل مكان، وهيدر واحد مستحيل يختلف بين الشاشات لأنه فعليًا نفس الـ instance. التنقل الخاص بكل شاشة (زر الرجوع، اسم المشروع/الموظف) اتفصل في `BreadcrumbBar.tsx` منفصل تمامًا عن الهيدر، عشان الهيدر يفضل ثابت 100% زي ما طُلب صراحة.

**تصنيف المشاريع مبني على بيانات حقيقية (مش مُختلَقة):** استُخدمت بيانات الـ workflow المحفوظة فعليًا (`workflow-state.mjs`) — مشروع بدون بيانات محفوظة = نشط (بوابة 1، الحالة الافتراضية الحقيقية)، وصل لبوابة الإنتاج بدون موافقة = "قربت تخلص"، بوابة الإنتاج متوافق عليها = "خلصت". تم إضافة `GET /api/workflow/summary` (endpoint جديد يرجّع حالة كل المشاريع دفعة واحدة، بدل طلب منفصل لكل مشروع).

**تنظيف كود حقيقي:** حذف 16 قاعدة CSS ميتة كانت خاصة بالهيدرات القديمة المنفصلة (تم التأكد من عدم استخدامها في أي JSX قبل الحذف)، إزالة prop غير مُستخدَم (`project`) من `ProjectHomeScreen`.

**تم التحقق فعليًا:**
- اختبار حي لـ `GET /api/workflow/summary`: موافقة على بوابة لمشروع تجريبي → الملخص عكس التغيير بدقة (`currentGate: "ui_ux"`).
- اختبار منطق التصنيف يدويًا على الأربع سيناريوهات الحقيقية الممكنة (بدون بيانات، بوابة 2، بوابة 4 بدون موافقة، بوابة 4 بموافقة) — كلها صحيحة.
- `npm test` → 42/42، `tsc -b` → نظيف من أول مرة، `npm run build` → ناجح، `oxlint` → 0 أخطاء (نفس التحذيرين الموجودين من قبل، غير مرتبطين بالتعديلات).
- اختبار تكامل حي: مسارات قديمة وجديدة تعمل مع بعض بدون تعارض.

**الحالة:** ✅ تم بناء المرحلتين 1 و2 (الهيدر + الـ Sidebar) والخطوط بالكامل، محتاج تأكيد بصري حي على staging. المرحلة 4 (توثيق خريطة الملفات) تمت في `README.md` قسم 59.

---

### 33. الوضع الفاتح كان "متكسر" فعليًا — اعتماد نظام الألوان الرسمي (DOCX) لحل الجذر
**الوصف:** المستخدم اختبر الوضع الفاتح حيًا على staging وشاف كسر واضح: كروت بتدرج لوني غامق غريب (نص فاتح فوق خلفية غامقة رغم إن الوضع فاتح)، عنوان قسم "Project workflow" مختفي بالكامل (نص أبيض على خلفية فاتحة)، وكروت "QA"/"Test Automation" سودة بالكامل. رفع كمان مستند Word رسمي (`MKDD_Color_System_Light_Dark_AR_RTL.docx`) بنظام ألوان دقيق ومُعتمَد.

**السبب الجذري (بالتشخيص، مش تخمين):** التحويل الجزئي السابق لنظام التوكنز (مشكلة #32) كان سايب **4 قيم hex لسه مكتوبة مباشرة جوه `linear-gradient()`** — دي بالظبط اللي كانت بتفضل غامقة بغض النظر عن الوضع المُختار، وده اللي ظهر كتدرج غريب في الصور.

**الحل الشامل:**
1. **اعتماد التوكنز الرسمية بالحرف** من المستند — 20 توكن، بقيم HEX دقيقة لكل وضع، بنفس أسماء وترتيب الملف بالظبط (`canvas`, `page`, `surface`, `border-subtle/strong`, `text-primary/secondary/muted/disabled`, `accent/accent-deep/accent-soft`, `info`, `success`, `warning`, `danger`).
2. **اكتشاف مهم من المستند:** بنية الـ CSS فيه فعليًا **الفاتح هو الافتراضي** (`:root, [data-theme="light"]` مُجمَّعين)، والداكن `override` صريح (`[data-theme="dark"]`) — عكس افتراضي كنت بنيته أنا. طبّقناها بالحرف، مع إجبار الداكن كتجربة افتراضية فعلية للمستخدم عبر الـ JavaScript (`useTheme.ts` وسكريبت `index.html`) حفاظًا على التجربة الحالية.
3. **4 توكنز مُشتقة** (المستند مش بيعرّفهم صراحة): `--mkdd-page-translucent`، `--mkdd-success-tint`، `--mkdd-danger-tint`، `--mkdd-accent-glow` — محسوبة من التوكنز الرسمية نفسها عبر `color-mix()`، مش قيم مُختلَقة.
4. **تحويل شامل لكل الألوان الثابتة المتبقية** (74 قيمة hex كانت لسه موجودة من تحويل جزئي سابق) للتوكنز الصحيحة حسب السياق — الملف بقى خالي تمامًا من الألوان الثابتة عدا حالة واحدة مقصودة (نص أبيض ثابت جوه دايرة لون أساسي مليانة، مش محتاج تغيير مع الوضع).

**باگين إضافيين اكتُشفا وأُصلحا أثناء التنفيذ:**
- قاعدتين CSS قديمتين بنفس تخصصية القواعد الجديدة وترتيب لاحق في الملف، كانوا بيلغوا التنسيق الجديد بصمت — تم حذفهم.
- خطأ توازن أقواس (338 مقابل 339) نتج عن قطعة كود يتيمة من تعديل سابق — اكتُشف بالفحص البرمجي المعتاد قبل أي بناء، وتم إصلاحه فورًا.

**حُفظ المستند الرسمي دائمًا في الريبو:** `design/MKDD_Color_System_Light_Dark_AR_RTL.docx` (مش مجرد رفعة مؤقتة في المحادثة).

**تم التحقق فعليًا:** `npm test` → 42/42، `tsc -b` → نظيف، `npm run build` → ناجح، فحص توازن أقواس CSS برمجيًا، `oxlint` → 0 أخطاء، كل ده بعد Prettier.

**الحالة:** ✅ تم الحل بالكامل، محتاج تأكيد بصري حي على staging للوضعين.

---

### 34. شريط الفريق في صفحة المشروع كان كبير جدًا (شكوى صريحة)
**الوصف:** "شريط الموظفين كبير جدًا مالوش علاقة بالتصميم الي بعتهولك" — الكروت كانت 128px بحدود وخلفية متدرجة وصف بادجات (حالة/تكلفة/مهام)، أكبر بكتير من الشكل المضغوط في الصور المرجعية.

**الحل:** تصغير الكارت لـ 76px، إزالة الحدود والخلفية تمامًا (بساطة زي المرجع)، أفاتار دائري 42-52px بدل مستطيل مدوّر، وحذف صف البادجات بالكامل من هذا العرض المختصر (الاسم والدور بس).

**الحالة:** ✅ تم الحل، محتاج تأكيد بصري حي.

---

### 35. `setup.sh` كان "الاسكربت اللي بيعمل كل حاجة" — إلا إنشاء الموظفين
**الوصف:** المستخدم طلب مسح شامل للـ staging وتنصيب من الصفر، وقال صراحة: "المفروض الاسكربت كمان بينشئ الموظفين". بالفحص، تأكد إن `setup.sh` (ومن ورا `deploy/install.sh` اللي بيناديه) كان بيبني ويشغّل الحاويات بس — إنشاء الـ13 موظف (Agent Profiles) كان دايمًا خطوة يدوية منفصلة (`docker exec ... bootstrap-employees.mjs`)، من أول ما اتبنى السكريبت ده.

**الحل:** إضافة خطوة تلقائية في `setup.sh` (بعد التأكد من جاهزية الـ backend، وقبل طباعة الروابط النهائية) تشغّل `bootstrap-employees.mjs` تلقائيًا — **بس لو** `MKDD_BOOTSTRAP_LLM_PROFILE_REF` متظبط كـ environment variable (لأن السكريبت مايقدرش يخترع اسم LLM profile؛ لازم يكون موجود فعليًا في إعدادات Agent Canvas الأول). لو المتغيّر مش متظبط، السكريبت بيكمل عادي مع رسالة واضحة توضح الخطوة اليدوية البديلة — مفيش فشل كامل للتنصيب بسبب ده.

بما إن `deploy/install.sh` بينادي `./setup.sh up` داخليًا، التحديث ده بيوصل تلقائيًا لأي تنصيب staging جديد من غير أي تعديل إضافي على `install.sh` نفسه.

**تم التحقق:** فحص صحة bash syntax (`bash -n`) للسكريبت المُحدَّث، ومراجعة الترتيب (الـ bootstrap لازم يجي بعد التأكد من جاهزية الـ backend، مش قبلها).

**الحالة:** ✅ تم الحل، محتاج تأكيد حي عند أول تنصيب فعلي بالمتغيّر الجديد.

**تحديث — تم التأكيد الفعلي (2026-08-14):** تنصيب كامل على staging أكّد إن الخطوة بتشتغل تلقائيًا صح — "Bootstrapping employees" ظهرت في الناتج، والـ13 موظف اتعملوا (`ok=13 failed=0`) من غير أي أمر يدوي منفصل.

---

### 36. الموظفين مايقدروش ينشئوا ملفات جوه المشروع (باگ صلاحيات bind mount)
**الوصف:** المستخدم لاحظ إن باجوش (موظف Product Manager) في محادثة سابقة مكانش قادر ينشئ ملفات جوه المشروع.

**السبب الجذري (بالتأكيد من الكود المصدري، مش تخمين):** ملف `Dockerfile` بتاع `openhands-agent-canvas` بيعمل:
```dockerfile
RUN mkdir -p ... /projects && chown -R openhands:openhands ... /projects
USER openhands
```
لكن الـ `chown` ده بيشتغل وقت **بناء الـ image**، على مسار **جوه الـ image نفسه**. وقت التشغيل الفعلي، `docker compose` بيعمل mount لمجلد الـ **host** فوق نفس المسار، وده **بيلغي تمامًا** أي ownership اتحددت وقت البناء. مجلد الـ host بيتعمل بصلاحيات `root` (لأن `setup.sh` بيشغّل `mkdir -p` تحت `sudo`)، فالمستخدم غير-root جوه الحاوية (`openhands`) مالوش صلاحية كتابة فيه.

**تم التحقق فعليًا (قبل الاعتماد على الحل):**
```
Before chmod: 755
After chmod: 777
```
تأكيد مباشر إن `fs.mkdirSync`'s `mode` option بيتأثر بـ `umask` النظام ومش كافي وحده، محتاج `chmod` صريح بعده.

**الحل الشامل (مكانين):**
1. `setup.sh`: `chmod -R 777` على مجلد `projects/` وقت التنصيب.
2. `server/routes/projects.mjs`: `fs.chmodSync(projectPath, 0o777)` بعد إنشاء **أي** مشروع جديد من زرار "+ مشروع جديد" (مش بس وقت التنصيب) — عشان كل مشروع مستقبلي يتولد بصلاحيات صحيحة تلقائيًا، مش بس المشاريع الموجودة وقت التنصيب.

**باگ إضافي اكتُشف أثناء الاختبار الحي (غير مطلوب، لكن تم إصلاحه فورًا):** لو OpenHands مش متاح تمامًا (فشل اتصال شبكة كامل، مش رفض HTTP عادي)، كود التراجع (rollback) مكانش بيتفعّل — فبيتسيب مجلد يتيم غير مُسجَّل. **تم التحقق فعليًا:** قبل الإصلاح كان المجلد يفضل موجود رغم فشل التسجيل؛ بعد الإصلاح (`try/catch` حوالين استدعاء الشبكة) اتأكد إن المجلد بيتشال صح.

**الحالة:** ✅ تم الحل بالكامل، مُختبَر حيًا (صلاحيات 777 مؤكدة، rollback مؤكد)، محتاج تأكيد نهائي عند إنشاء مشروع فعلي على staging بعد التنصيب الجديد.

---

### 37. ترقية OpenHands Agent Canvas لإصدار 1.13.0 (تحقّق كامل قبل التنفيذ)
**السياق:** المستخدم لاحظ صدور إصدار جديد (1.13.0) من OpenHands Agent Canvas وسأل هل الترقية آمنة. طُلب التحقق الكامل قبل أي تنفيذ، مع التذكير الصريح بالتعديل المُخصَّص بتاعنا (ميزة "شخصية الموظف" — `system_message_suffix`).

**التحقق الذي تم إجراؤه فعليًا (كل خطوة بأدلة حقيقية، مش افتراضات):**
1. **استنساخ النسخة الحقيقية 1.13.0 من GitHub** (`git clone --branch v1.13.0`) — مش قراءة changelog بس.
2. **تطبيق الـ patch الفعلي** لتعديلنا المُخصَّص (`agent-profiles-local-view.tsx`) على النسخة الجديدة: نجح **بدون أي تعارض**، والناتج **مطابق بايت-لبايت** لنسختنا الحالية الشغالة.
3. **الحد الأدنى المطلوب لـ Agent Server** (من `config/defaults.json` الرسمي): `1.28.0` — إحنا كنا على `1.40.1`، فوقه بكتير.
4. **القيم الرسمية الجديدة** (من نفس الملف، مش تخمين): `agentServer: 1.42.1`, `automation: 1.7.1` (تحديث عن `1.40.1`/`1.6.0` السابقين).
5. **آلية اكتشاف مفتاح الجلسة** (بحث `/proc` عن `--session-api-key`، تقنية دَين موثّقة في README قسم 14): تأكدنا إنها **موجودة بالحرف** في `entrypoint.sh` و`static-server.mjs` بنسخة 1.13.0 — مفيش أي تغيير مطلوب في كودنا.
6. **بنية المشروع** (top-level directories) **متطابقة تمامًا** بين النسختين.

**التنفيذ:**
- استبدال شجرة `openhands-agent-canvas/` بالكامل بمصدر 1.13.0 الحقيقي.
- إعادة تطبيق الـ patch المُخصَّص فوقها.
- تحديث `compose.yml` و`setup.sh` بقيم `AGENT_SERVER_IMAGE`/`AUTOMATION_VERSION` الجديدة.
- فحوصات سلامة شاملة: `bash -n`، صحة YAML، صحة كل ملفات JSON، تأكيد عدم وجود ملفات backup من الـ patch (`.orig`/`.rej`).

**حدود التحقق (صراحة، مش مخفية):** كل التحقق ده على **مستوى الكود المصدري**، لأن بيئتي معندهاش Docker — **لسه محتاج بناء (`docker compose build`) واختبار تشغيل حي فعلي على الـ VM** قبل أي اعتماد نهائي. عدّلت التعليق في `setup.sh` عشان يوضح ده صراحة (بدل ما يدّعي تأكيد بناء حي لقيم لسه ماتأكدتش عليها فعليًا).

**الحالة:** ✅ التحقق على مستوى الكود مكتمل بالكامل، محتاج بناء واختبار حي على بيئة معزولة (مش `mkdd-live`) قبل أي اعتماد.

**تحديث — تم التأكيد الفعلي (2026-08-14):** تنصيب كامل ناجح على staging — بناء `docker compose` نجح بالكامل (شامل صور `agent-server:1.42.1` و`automation:1.7.1` الجديدتين)، الـ backend اشتغل، و**الـ13 موظف اتعملوا تلقائيًا** (`ok=13 failed=0`) من غير أي خطوة يدوية. النسخة الجديدة شغالة فعليًا، مش نظري بس.

---

### 38. `install.sh` بيفشل لو اتنسخ لمكان تاني قبل التشغيل (`.env.staging` not found)
**الوصف:** اتكشف حيًا أثناء تنفيذ فعلي — لما `install.sh` بينتسخ لـ `/tmp/install.sh` قبل التشغيل (طريقة كنا بنستخدمها لتفادي عمل clone كامل بس عشان نجيب السكريبت)، السكريبت كان بيفشل بـ `cp: cannot stat '/tmp/.env.staging': No such file or directory`.

**السبب الجذري:** السكريبت كان بيحدّد مكان الملفات المساعدة (`.env.staging`, `mkdd-auto-deploy.service/.timer`) بناءً على **مكان تشغيل السكريبت نفسه** (`SCRIPT_DIR` من `BASH_SOURCE[0]`)، مش بناءً على الريبو اللي هو بيعمله clone فعليًا. لما السكريبت اتنسخ لـ `/tmp`، بقى بيدوّر على الملفات دي في `/tmp` (مش موجودة هناك)، بدل ما يدوّر في `/opt/mkdd-live/deploy/` (فين ما هي موجودة فعليًا بعد الـ clone).

**الحل:** السكريبت بقى يحدّد مكان الملفات المساعدة نسبةً لـ `$DEPLOY_DIR/deploy/` (الريبو اللي اتعمله clone فعليًا)، مش نسبةً لمكان تشغيل السكريبت نفسه — ده مضمون يشتغل صح بغض النظر عن مكان تشغيل `install.sh`، لأن `$DEPLOY_DIR` بيكون اتعمله clone فعليًا قبل الوصول للسطور دي بالضرورة.

**الحالة:** ✅ تم الحل، محتاج تأكيد حي (إعادة تشغيل أمر التنصيب بعد سحب هذا الإصلاح).

**تحديث — تم التأكيد الفعلي (2026-08-14):** إعادة تشغيل أمر التنصيب (بنفس أسلوب نسخ السكريبت لـ `/tmp`) نجح بالكامل من غير أي خطأ `.env.staging`.

---

### 39. زر رفع صورة الموظف كان مكرّر في المكان الغلط
**الوصف:** المستخدم أكّد إن التصميم بقى "أحسن كتير" بعد ترقية 1.13.0، لكن لاحظ إن زر رفع الصورة كان ظاهر على كروت "فريق المشروع" برضو — مفروض يكون موجود بس في بروفايل الموظف (من الـ Sidebar)، مش مكرّر في مكانين.

**الحل:** كروت فريق المشروع بقت تعرض أفاتار عادي بس (بدون زر رفع)، وزر الرفع فضل موجود حصريًا في `EmployeeProfileModal` (بيتفتح من Sidebar ← الموظفين ← اختيار موظف). شيل الـ prop والاستيراد اللي بقوا مش مستخدمين من `ProjectHomeScreen.tsx`.

**الحالة:** ✅ تم الحل، محتاج تأكيد بصري حي.

---

### 40. السبب الحقيقي وراء "الصورة بتترفع لكن مبتظهرش" — مسار Vite proxy ناقص
**الوصف:** بعد استبعاد كل الاحتمالات التانية (السيرفر شغال صح، الملف اتحفظ صح، Hard Refresh مش حل المشكلة) — لقينا السبب الحقيقي: `vite.config.ts` عنده تحويل (`proxy`) لمسارات `/api` و`/ws` بس، **ومفيهوش تحويل لمسار `/avatars` خالص**. لما المتصفح بيطلب صورة من بورت الواجهة (Vite)، مفيش قاعدة توجّهها للـ backend الحقيقي، فـ Vite بيرجّع صفحة الـ SPA الافتراضية بدل الصورة — والمتصفح بيفسّرها كصورة تالفة.

**ليه ده فسّر كل الأدلة اللي جمعناها:**
- `curl` مباشر على الـ backend (`18787`) نجح دايمًا (بيتخطى Vite تمامًا).
- المتصفح (بيمر على Vite أولًا) فشل دايمًا، **حتى بعد Hard Refresh** — لأن المشكلة مش كاش، كل طلب كان بيفشل بنفس الطريقة.

**التحقق الذي تم إجراؤه فعليًا (تشغيل حي، مش نظري):** شغّلت الـ backend وVite dev server مع بعض فعليًا في بيئتي، وطلبت صورة تجريبية عبر بروكسي Vite بالظبط زي ما المتصفح بيعمل:
```
Status: 200, Content-Type: image/png
```
نجح من أول محاولة بعد الإصلاح.

**الحل:** إضافة قاعدة `/avatars` لـ `vite.config.ts`، بنفس نمط `/api`. تم التأكد كمان إن `/avatars` هو المسار الوحيد في السيرفر كله اللي مش تحت `/api/*` (بحث شامل في كل ملفات الـ routes) — يعني مفيش أي مسار تاني ناقص من نفس النوع.

**الحالة:** ✅ تم الحل والتحقق الحي منه بالكامل، محتاج تأكيد نهائي على الموبايل الحقيقي.

---

### 41. معاينة ملفات المشروع مباشرة من MKDD (ميزة جديدة)
**السياق:** المستخدم واجه رابط معاينة (`127.0.0.1:4173/...`) عملته موظفة تصميم، ومكانش شغال حتى بعد تغيير الـ IP.

**التشخيص (من الكود الحقيقي، مش تخمين):** آلية `runtime_services` في OpenHands مخصصة بس لخدمات معروفة مسبقًا (`agent_server`, `ingress`, `frontend`, `automation`) — أي بورت عشوائي الموظف بيشغّله بنفسه (زي `vite preview` على 4173) بيفضل جوه شبكة الحاوية بس، مش معروض للـ VM خالص، بغض النظر عن الـ IP.

**الحل:** `server/routes/preview.mjs` — يعرض أي ملف داخل `/projects/<مشروع>/` مباشرة، مبني على بنية تحتية موجودة فعليًا (نفس مجلد `/projects` الـ mounted في الحاويتين من زمان)، مش معتمد على أي سيرفر مؤقت. **يغطي مواقع/بروتايبات ستاتيك بالكامل (HTML/CSS/JS/صور)، مش تطبيقات محتاجة سيرفر شغال فعليًا (Backend حقيقي) — ده احتياج مختلف تمامًا هيحتاج ميزة تانية لاحقًا لو ظهر.**

**باگ أمني حقيقي اكتُشف وأُصلح بفضل الاختبارات نفسها:** لو "اسم المشروع" في الرابط كان `..`، كان بيهرب لمجلد الجذر ويخترق كل حماية `path traversal` بعده (لأن `path.join(projectsDir, "..")` بيرجّع مجلد الأب، وبعدين أي مسار كان بيعدّي فحص "بيبدأ بمسار المشروع" بسهولة). تم إصلاحه بفحص إضافي: مجلد المشروع نفسه لازم يكون فرع مباشر حقيقي من `/projects` **قبل** أي فحص تاني.

**تم التحقق فعليًا:** 9 اختبارات دائمة (`preview.test.mjs`) — تشمل حالة الترميز الخطرة (`%2e%2e%2f`) اللي بتتخطى تطبيع `URL` التلقائي، وحالة الباگ الأمني المُكتشَف. + اختبار حي كامل (عرض ملف متداخل، صورة، رفض traversal، 404 لملف مش موجود).

**الحالة:** ✅ تم البناء والاختبار بالكامل، محتاج تأكيد حي على staging.

---

### 42. ثلاث مشاكل في تجربة المحادثة (سكرول، تغطية آخر رسالة، صندوق كتابة غير مرن)
**الوصف:** (1) فتح محادثة قديمة بيوريها من الأول بدل آخر رسالة. (2) جزء من آخر رسالة مختفي وراء صندوق الكتابة. (3) صندوق الكتابة مش بيكبر مع الرسالة، وEnter بيبعت الرسالة بدل ما يعمل سطر جديد.

**الحل:**
- **تمرير تلقائي لآخر رسالة:** عنصر "مؤشر" في آخر قائمة الرسائل، مع `scrollIntoView` عند فتح المحادثة أو وصول رسالة جديدة.
- **صندوق كتابة مرن:** استبدال `<input>` بـ `<textarea>` بارتفاع تلقائي (بحد أقصى 160px، بعده تمرير داخلي).
- **Enter = سطر جديد:** ده سلوك `<textarea>` الطبيعي أصلًا (بعكس `<input>`) — الإرسال بيحصل بس بالضغط على الزر، من غير أي كود إضافي.
- **إصلاح تغطية آخر رسالة (السبب الجذري):** المسافة أسفل الرسائل كانت رقم ثابت (84px) مش بيتغيّر مع ارتفاع صندوق الكتابة الجديد المتغيّر. تم قياس الارتفاع الفعلي حيًا (`ResizeObserver`) وتحديث متغيّر CSS (`--composer-height`) بيه، فالمسافة بقت دايمًا مطابقة للحجم الحقيقي.

**الحالة:** ✅ تم البناء والاختبار (`npm test` → 51/51، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء)، محتاج تأكيد بصري حي.

---

### 43. أربع تحسينات على تجربة المحادثة (فقاعة الموظف، مسافة، إرفاق صور، ملفات المشروع)
**السياق:** فيدباك مباشر بعد استخدام ميزة معاينة الملفات، أربع نقاط منفصلة.

**43.1 — رد الموظف بقى في فقاعة حقيقية + مسافة أكبر:**
كان `.agent-message` بخلفية شفافة بدون حدود (تصميم قديم). بقى فقاعة كاملة (خلفية، حدود، حواف مدورة) بنفس أسلوب فقاعة المستخدم لكن بلون مختلف (`--mkdd-surface-raised` للموظف، `--mkdd-accent` للمستخدم — تمييز بصري أوضح بين الطرفين). المسافة أسفل الرسائل زادت من 16px لـ 34px إضافية فوق الارتفاع الفعلي لصندوق الكتابة.

**43.2 — إرفاق صور في المحادثة (ميزة جديدة، مبنية على schema حقيقي):**
تأكيد الشكل الفعلي لمحتوى الصور من كود `openhands-agent-canvas` نفسه (`MessageImageContent = {type:"image", image_urls: string[]}`) قبل أي بناء. زر مشبك (📎) في صندوق الكتابة، معاينة الصور المرفقة قبل الإرسال (مع إمكانية الإزالة قبل الإرسال)، حد أقصى 4 صور لكل رسالة و5 ميجا لكل صورة (نفس حد رفع صورة الموظف). الصور بتتحفظ وبتظهر جوه فقاعة الرسالة نفسها (المرسلة والمستقبلة).

**باگ كامن اكتُشف وأُصلح أثناء التنفيذ:** دالة `sendChatMessage` القديمة كانت بترجّع استجابة الخطأ من السيرفر وكأنها نجحت (من غير `throw`)، يعني فشل الإرسال الحقيقي كان بيفشل بصمت من غير أي rollback للرسالة التفاؤلية أو استرجاع النص. تم إصلاحه (`throw` عند استجابة غير ناجحة)، وده بيخلي منطق rollback الموجود بالفعل في `useConversation.ts` يشتغل صح لأول مرة لحالات الخطأ الحقيقية.

**تم التحقق فعليًا:** 9 اختبارات جديدة (`chat.test.mjs`) لمنطق التحقق من الصور (حجم، عدد، صيغة)، + اختبار حي كامل (إرسال رسالة بصورة، رفض عدد صور زيادة). المجموع الكلي: 60/60 اختبار.

**43.3 — ملفات المشروع بقت ظاهرة في صفحة المشروع الرئيسية:**
`server/routes/project-files.mjs` (جديد): يعرض قائمة كل ملفات/مجلدات المشروع الحقيقية (`GET /api/projects/<مشروع>/files`)، مع استثناء الضوضاء (`.git`, `node_modules`, إلخ). **إعادة استخدام** لمنطق حماية المسار (`resolveProjectDir`) بدل تكراره — تم استخراجه لملف مشترك (`server/lib/project-paths.mjs`) يستخدمه هذا الـ route وميزة المعاينة (#41) معًا. قسم جديد "ملفات المشروع" في `ProjectHomeScreen.tsx` — كل ملف قابل للمعاينة رابط مباشر لـ `/preview/...`، والمجلدات نص عادي بمسافة بادئة حسب العمق.

**تم التحقق فعليًا:** اختبار حي كامل (قائمة صحيحة، استثناء `.git`/`node_modules` مؤكَّد، مشروع غير موجود يرجّع قائمة فاضية مش خطأ، رفض traversal). + 3 اختبارات دائمة (`project-files.test.mjs`).

**43.4 — روابط المعاينة بقت بتظهر كـ كارت داخل المحادثة نفسها:**
`src/utils/detectPreviewLinks.ts` (جديد): يكتشف روابط `/preview/...` (نسبية أو كاملة) جوه نص أي رسالة، مع تنظيف علامات الترقيم اللي ممكن تتلقط غلط في آخر الرابط (زي نقطة نهاية الجملة). **قرار أمان مقصود:** الكشف بيقتصر على روابطنا الخاصة بس (`/preview/...`) — مش أي رابط خارجي عشوائي، تجنّبًا لعرض `iframe` لمحتوى خارجي غير موثوق. كل رابط مكتشَف بيتحول لكارت فيه `<iframe sandbox="">` (بدون أي صلاحيات تنفيذ، معاينة بصرية بس، آمنة تمامًا) + اسم الملف، والضغط على الكارت بيفتح المعاينة الكاملة الحقيقية في تبويب جديد.

**تم التحقق فعليًا:** اختبار يدوي شامل لمنطق الكشف (5 سيناريوهات: رابط نسبي، رابط كامل، نقطة نهاية جملة، بدون روابط، روابط متعددة مع تكرار) — كلهم نجحوا. (مفيش إطار اختبار frontend في المشروع حاليًا — نفس النمط المتّبع بالفعل مع `formatMessageTime.ts`.)

**الحالة:** ✅ **كل البنود الأربعة (43.1 – 43.4) تم بناؤها واختبارها بالكامل** الآن. `npm test` → 63/63، `tsc -b` → نظيف، `npm run build` → ناجح، `oxlint` → 0 أخطاء. محتاج تأكيد بصري حي على staging.

---

### 44. رفع الصور/الملفات في واجهة OpenHands الأصلية بيعلّق للأبد (السبب الجذري الحقيقي)
**السياق:** المستخدم اختبر رفع صورة من واجهة **OpenHands Agent Canvas الأصلية نفسها** (مش MKDD)، وبانت علامة تحميل بدون أي إكمال. ده استبعد كودنا تمامًا كسبب — المشكلة في البنية التحتية.

**التشخيص (بالأدلة، خطوة بخطوة):**
1. راقبنا لوج الحاوية حيًا وقت المحاولة — **مفيش أي طلب رفع وصل للسيرفر خالص**، يعني الطلب مش "معلّق"، هو مش وصل من الأساس.
2. المستخدم اقترح إن السبب مش في المتصفح، وطلب البحث في التوثيق الرسمي — بحث دقيق في الكود المصدري الحقيقي (مش تخمين) كشف الآلية الفعلية: رفع الملفات بيمر عبر خدمة اسمها "automation"، وبتحتاج عنوان قابل للوصول من برّه.
3. بحث في مشاكل GitHub الحقيقية لـ OpenHands كشف مشاكل مشابهة موثّقة (النشر على شبكة محلية بعنوان IP بدل `localhost`).
4. **التأكيد النهائي المباشر:** لقينا في `docker/entrypoint.sh` بتاعنا نفسه:
   ```
   export AUTOMATION_BASE_URL="${AUTOMATION_BASE_URL:-http://127.0.0.1:${PORT}}"
   ```
   بتعليق صريح: *"Override in production when the external URL differs."* — كنا معملناش override له خالص، فكان بيفضل `127.0.0.1` (عنوان الحاوية نفسها) بشكل دائم، غير قابل للوصول من أي جهاز تاني على الشبكة.

**ليه ده مأثرش على باقي الميزات:** المحادثة، الـ REST، والـ WebSocket كلهم بيستخدموا عنوان الصفحة نفسها اللي المتصفح فاتحها (نسبي، same-origin) — بيشتغلوا صح مهما كان العنوان المُستخدَم للوصول. رفع الملفات بس هو اللي بيعتمد على العنوان المُعلَن ده صراحة (`AUTOMATION_BASE_URL`) بغض النظر عن العنوان الفعلي اللي المتصفح استخدمه.

**الحل:**
- `compose.yml`: إضافة `AUTOMATION_BASE_URL` كمتغيّر بيئة قابل للتمرير لخدمة `agent-canvas` (افتراضي فاضي، يحافظ على السلوك القديم لو محدش حدده).
- `deploy/.env.staging`: تحديد القيمة الصحيحة لبيئة الـ staging الحالية (`http://192.168.2.18:13000`).
- `setup.sh`: تحذير واضح لو المتغيّر مش متظبط، يشرح بالظبط إيه اللي هيحصل (تعليق صامت بدون رسالة خطأ) وإزاي يتصلح.

**الحالة:** ✅ تم التشخيص والحل بالكامل، محتاج إعادة تشغيل الحاوية (`docker compose up -d` بعد سحب التحديث) وتأكيد حي على staging.

---

### 45. الموظفين لسه بيبعتوا روابط `localhost` بدل استخدام ميزة المعاينة الحقيقية
**الوصف:** بعد ما أصلحنا سبب تعليق رفع الصور (#44)، ظهرت نفس المشكلة الأصلية اللي بدأنا بيها الحوار (#41): مريم لسه بتبعت لينك `http://127.0.0.1:4173/...` بدل ما تستخدم `/preview/...` اللي بنيناها.

**السبب:** ميزة المعاينة (#41) اتبنت في الـ backend، لكن محدش "علّم" الموظفين إزاي يستخدموها — هما مش عارفين إنها موجودة أصلًا.

**الحل:** بنفس النمط بالظبط المُستخدَم لتعليم الموظفين وعي الوقت (`time-context.mjs` / #25) — `server/lib/preview-instructions.mjs` (جديد): تعليمات مشتركة توضح للموظف يمنع تشغيل سيرفر معاينة محلي خاص بيه، وبدل كده يشارك رابط بالصيغة `/preview/{مشروعه}/{المسار}` مباشرة كنص عادي. اتضافت لكل الـ13 موظف عبر `bootstrap-employees.mjs` (إضافة موحدة مرة واحدة، مش تكرار في كل ملف).

**تفصيلة تكاملية مهمة:** تأكدت إن الصيغة اللي بنعلّمها للموظف مطابقة تمامًا لنمط الكشف التلقائي (`detectPreviewLinks.ts` من #43.4) — يعني لما الموظف يستخدم الصيغة الصح، كارت المعاينة هيظهر تلقائيًا تحت رسالته في الشات، من غير أي خطوة إضافية.

**الحالة:** ✅ تم البناء والتحقق (63/63 اختبار، بناء نظيف)، محتاج تشغيل `bootstrap-employees.mjs` تاني على الـ VM عشان الموظفين الموجودين ياخدوا التعليمات الجديدة.

---

### 46. لوحة "تفاصيل الموظف والنشاط" بتقصّ المحتوى بدون أي تمرير
**الوصف:** المستخدم بعت صورة توضح إن اللوحة (خطة العمل + النشاط) بتنقطع فجأة (زي "بناء Design System" في الصورة) بدون أي طريقة لرؤية الباقي — مش عارف الموظفة بتعمل إيه فعليًا.

**السبب الجذري:** `.chat-screen` بارتفاع ثابت (`100dvh`) و`overflow: hidden` — بس قسم الرسائل (`.chat`) عنده تمرير داخلي خاص بيه (`overflow-y: auto`). لوحة "تفاصيل الموظف والنشاط" (`.employee-insights-content`) كانت بتاخد مساحتها الطبيعية بدون أي تمرير خاص بيها — لو محتواها (مهام + نشاط) زاد عن المساحة المتاحة، الزيادة كانت **بتتقص وتختفي تمامًا**، مش بس محتاجة سكرول.

**الحل:** إضافة `max-height: 40vh` و`overflow-y: auto` للوحة نفسها (`.employee-insights-content`) — بتاخد مساحة محدودة ومعقولة، وأي محتوى زيادة بقى قابل للتمرير داخلها، من غير ما تاكل مساحة الرسائل تحتها.

**الحالة:** ✅ تم الحل (`npm test` → 63/63، `tsc -b` نظيف، `npm run build` ناجح)، محتاج تأكيد بصري حي.

---

### 47. إضافة موظفة جديدة — شيري (Content & Localization Writer)
**السياق:** طلب صريح لإضافة دور رابع عشر مسؤول عن كل نص يشوفه المستخدم النهائي (توطين، UX writing، marketing copy)، بمبدأ أساسي: **توطين حقيقي، مش ترجمة حرفية**.

**قرارات تصميم اتناقشت وأتفق عليها:**
- **التوقيت:** تبدأ من اعتماد البوابة 1، بالتوازي مع مريم من بداية مرحلة UI/UX — عشان البروتايبات تطلع بنصوص حقيقية من الأول، مش placeholder.
- **الإلزامية:** مساهمة (زي نادر)، مش بوابة إلزامية زي QA/Security — لأن نصوص محتاجة تحسين مش زي باگ حقيقي أو ثغرة أمنية، وكمان مش كل مشروع محتاج نصوص واجهة كتير.

**اكتشاف مهم أثناء التنفيذ (بتنبيه من المستخدم):** `company-orchestrator.md` فيه **جدول توجيه** يستخدمه الأوركستريتور عشان يوجّه أي طلب للموظف الصح ("عايز أكلم فلان" → تفويض مباشر). لو اتنسي تحديثه، الأوركستريتور مكانش هيعرف شيري موجودة أصلًا. تم تحديثه (جدول التوجيه، مثال طلب مباشر، وصف دورة العمل، قائمة الهوية المشتركة).

**تفصيلة بنيوية اكتُشفت:** كل ملفات الموظفين الـ13 فيها **نفس الجزء المشترك مكرر بالكامل** (سياسة الهوية، قائمة الموظفين، معايير الجودة...) — تكرار حقيقي مش مُبرَّر (يخالف مبدأ "No unexplained duplication" المكتوب في نفس الملفات دي!). قرار مقصود: **مش عملنا إعادة هيكلة كاملة دلوقتي** (خطر عالي، بعض الملفات فيها أقسام إضافية مختلفة بعد الجزء المشترك) — بس عملنا تحديث دقيق ومحدود (سطر واحد) في قائمة الهوية في الـ13 ملف، بسكريبت آمن اتأكد إن كل استبدال حصل صح (13/13 من غير أي حالة محتاجة مراجعة يدوية). إعادة الهيكلة الكاملة (استخراج الجزء المشترك لملف واحد، زي `time-context.mjs`) باقية كتحسين مستقبلي موثّق.

**تم التحقق فعليًا:**
- اكتشاف حي: `listEmployeeNames()` رجّعت 14 موظف بالظبط، شيري متضمنة، الملفات غير-الموظفين مستثناة صح.
- قراءة حية: `readEmployeeDisplayInfo('content-writer')` رجّعت البيانات صح (`Sherry`, `شيري`, `Content & Localization Writer`, `order: 14`).
- بحث شامل في الـ frontend أكّد عدم وجود أي رقم "13" مُثبَّت (كل حاجة بتتحسب ديناميكيًا).

**الحالة:** ✅ تم البناء والتحقق الكامل. محتاج تشغيل `bootstrap-employees.mjs` على الـ VM عشان شيري تتعمل فعليًا كـ Agent Profile حقيقي.

---

### 48. أي محادثة جديدة (لأي موظف، في أي مشروع) بقت مستحيلة بعد ترقية 1.13.0 — سبب جذري خطير
**السياق:** المستخدم اكتشف إن مشروع جديد ("test-for-new-employees") — ولا موظف فيه بيرد خالص، وزرار الإرسال "بيتحرك بس مفيش حاجة بتحصل"، ومفيش أي محادثة اتعملت في واجهة OpenHands نفسها. كمان اتأكد إن مريم مش بتقدر "تسلّم" لشنودة تلقائيًا (نقطة معمارية منفصلة: مفيش تسليم تلقائي بين الموظفين — كل محادثة معزولة تمامًا، القرار بتاع المالك يدويًا).

**رحلة التشخيص (كل خطوة بدليل حقيقي، مش تخمين):**
1. **تنبيهات تشخيصية مؤقتة (`alert()`)** أُضيفت على 3 مراحل (submit الفورم، بداية `sendMessage`، الـ catch block) — لأن مفيش أدوات تطوير متاحة على الموبايل. النتيجة: الفورم بيتنفذ صح، `sendMessage` بتتنفذ صح، لكن الطلب بيفشل برسالة `send_message_failed` **غامضة** بدون أي تفاصيل.
2. **باگ حقيقي اتلقى في الكود:** فرع إنشاء محادثة جديدة في `chat.mjs` كان بيغلّف أي استجابة من OpenHands (نجاح أو فشل) بنفس الشكل `{conversation: ...}` — يعني لو فشلت، رسالة الخطأ الحقيقية كانت بتضيع جوه حقل غلط، والـ frontend كان بيرجع لرسالة عامة (`send_message_failed`) بديلة. تم إصلاحه فورًا (كشف الخطأ الحقيقي).
3. **اختبار حي مباشر (`curl`) يحاكي طلب الكود بالظبط:** كشف السبب الحقيقي النهائي — استجابة `422` من OpenHands: `"Input should be a valid UUID"` لحقل `agent_profile_id`.
4. **التأكيد النهائي:** `GET /api/agent-profiles/{name}` بترجّع `profile.id` (UUID حقيقي) منفصل تمامًا عن `name` (السلاج النصي زي "architect"). كودنا كان بيبعت السلاج النصي مباشرة كـ `agent_profile_id`.

**السبب الجذري الكامل:** ده **تأثير جانبي حقيقي من ترقية 1.13.0** (agent-server 1.42.1) — النسخة الجديدة بقت صارمة في التحقق وبترفض أي قيمة غير UUID فعلي لـ `agent_profile_id`، بعكس النسخة القديمة (1.40.1) اللي كانت بتقبل السلاج النصي كـ alias. المحادثات القديمة (زي مريم) فضلت شغالة لأنها اتعملت **قبل** الترقية ومحصلش أي إعادة تحقق عليها بعد كده — أي محادثة **جديدة** من دلوقتي فصاعدًا (لأي موظف، في أي مشروع) كانت هتفشل بنفس الطريقة بالظبط.

**الحل:** دالة جديدة `resolveAgentProfileUuid()` في `chat.mjs` — بتجيب الـ UUID الحقيقي من `GET /api/agent-profiles/{employeeId}` **قبل** أي محاولة إنشاء محادثة، وتستخدمه بدل السلاج النصي. لو البروفايل مش موجود، رسالة خطأ واضحة (`agent_profile_not_found`) بدل فشل غامض.

**تم التحقق فعليًا (مش نظري):** بنيت سيرفر OpenHands وهمي في بيئتي بيحاكي بالظبط سلوك الرفض الحقيقي (يرفض أي قيمة غير الـ UUID المتوقع)، مع عملية وهمية بنفس توقيع اكتشاف مفتاح الجلسة (`/proc` scan) — الطلب الكامل عدّى بنجاح واستخدم الـ UUID الصحيح، بعد ما كان بيفشل بـ 422 قبل الإصلاح.

**تنظيف:** كل تنبيهات التشخيص المؤقتة (`alert()`) اتشالت بعد التأكد من الحل.

**الحالة:** ✅ تم التشخيص الكامل والإصلاح والتحقق الحي. محتاج تأكيد نهائي على staging (إرسال رسالة حقيقية لشنودة).

---

### 49. السبب الجذري الحقيقي والنهائي — الفرونت إند كان بيبعت UUID بدل الاسم من الأساس
**السياق:** بعد إصلاح #48 (حل الـ UUID وحل اسم بروفايل الـ LLM)، المشكلة استمرت — رسالة لشنودة كانت بتظهر ثواني وترجع لصندوق الكتابة تاني.

**رحلة التشخيص (كل خطوة بدليل مباشر):**
1. اختبار حي مباشر لكل خطوة من التسلسل (جلب UUID، إنشاء محادثة، إرسال لمحادثة موجودة) — **كل حاجة نجحت 100%** لما اتعملت يدويًا عبر `curl`.
2. لوج حي وقت محاولة حقيقية من الواجهة — **مفيش أي طلب وصل للسيرفر خالص**.
3. استبعاد فرضيات (بورت غلط، كاش متصفح قديم) — الاتنين مؤكَّدين مش هما السبب.
4. **تنبيهات تشخيصية شاملة** (تتبع `fetch()` نفسها، حالة الاستجابة، محتوى الاستجابة الخام) كشفت: `status=404`، ورسالة `agent_profile_not_found` — نفس الخطأ اللي إحنا كتبناه في #48!

**السبب الجذري الحقيقي (مؤكَّد بمقارنة استجابات API حية):** `GET /api/agent-profiles` (الجمع) بترجع **UUID حقيقي** كحقل `id` منفصل عن `name` (الاسم النصي المستقر). كود `/api/employees` بتاعنا (`directory.mjs`) كان بيستخدم `profile.id` (الـ UUID) مباشرة كمعرّف الموظف المُرسَل للواجهة — **يعني الفرونت إند كان بيبعت UUID كـ `employeeId` من الأساس، مش الاسم النصي أبدًا**.

**المفارقة المهمة:** إصلاح #48 (`resolveAgentProfileUuid`) كان **مبني على افتراض غلط** إن القيمة الواصلة اسم نصي زي "architect"، فحاول يستخدمها كـ path parameter في مسار بيتوقع اسم — وده فشل لأن القيمة فعليًا UUID أصلًا (زي محاولة تدوّر على ملف بالـ UUID بدل اسمه الحقيقي). الإصلاح السابق كان صحيح **في السيناريو اللي اختبرته يدويًا** (بعت "architect" كاسم صريح)، لكن **الفرونت إند الحقيقي مبعتش كده خالص**.

**الحل الصحيح والنهائي:** تصحيح المصدر نفسه — `directory.mjs`'s `handleEmployees` بقى يرجّع `id: profile.name` (الاسم النصي المستقر) بدل `profile.id` (UUID داخلي لـ OpenHands). **كل باقي المشروع** (bootstrap script، أسماء ملفات `company-agents-definitions/*.md`، `resolveAgentProfileUuid`) مبني على افتراض ثابت إن معرّف الموظف هو الاسم المستقر — فالتصحيح ده بيوحّد الفرونت إند مع باقي النظام، بدل ما يسرّب تفاصيل داخلية لـ OpenHands (UUID ممكن يتغيّر لو البروفايل اتعمله حذف وإعادة إنشاء) لمنطق MKDD.

**تم التحقق فعليًا:** اختبار حي كامل (تشغيل السيرفر الحقيقي + محاكاة OpenHands) أكّد إن `/api/employees` بترجّع دلوقتي `id: "architect"` (مش UUID) بعد الإصلاح.

**تنظيف:** كل تنبيهات التشخيص المؤقتة (الجولة التانية) اتشالت.

**الحالة:** ✅ تم التشخيص الكامل (جولتين متتاليتين من الأدلة الحية) والإصلاح الحقيقي والتحقق الحي. محتاج تأكيد نهائي على staging.

---

### 50. تحسين شكل "ملفات المشروع" — أيقونات وتوزيع زي الصورة المرجعية
**الوصف:** المستخدم بعت صورتين — القسم الحالي (نص عادي بدون أيقونات) والصورة المرجعية (كل قسم/عنصر له أيقونة في شارة، توزيع منظم) — وطلب نفس الأسلوب.

**الحل:**
- كل صف ملف/مجلد بقى له أيقونة في شارة دائرية (زي أسلوب Sidebar المُستخدَم بالفعل) — مجلد (`FaFolder`)، وأيقونة حسب نوع الملف (صور، كود، نص) بدل أيقونة عامة واحدة.
- عنوان القسم نفسه ("ملفات المشروع") بقى له أيقونة بجانبه، زي باقي عناوين الأقسام في الصورة المرجعية.
- تحسين المسافات والتسلسل الهرمي للمجلدات المتداخلة.

**الحالة:** ✅ تم البناء والتحقق (`npm test` → 63/63، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء)، محتاج تأكيد بصري حي.

---

### 51. معاينة حية لتطبيقات حقيقية (زي كيرلس) — ميزة جديدة، مختلفة جذريًا عن معاينة مريم
**السياق:** كيرلس (Implementation Engineer) خلّص شغله وسلّمه، والمستخدم احتاج "يراجع المشروع لايف مش مجرد كود" — طلب صريح لنظام معاينة زي مريم، لكن لتطبيق حقيقي محتاج سيرفر شغال فعليًا، مش ملفات ستاتيك.

**نوقشت الفروق الجوهرية قبل التنفيذ** (زي ما اتفقنا): نظام مريم (`/preview/`) بيعرض ملفات ستاتيك من القرص مباشرة (HTML/CSS/JS جاهزة، مفيهاش "تشغيل"). التطبيق الحقيقي محتاج **عملية شغالة فعليًا** (سيرفر) عشان يشتغل، وده احتياج مختلف تمامًا.

**الحل المُتفَق عليه:** **Reverse Proxy حقيقي** — الموظف بيشغّل سيرفر مشروعه على بورت متفق عليه (`4001`، مقصود تجنّب 3000/8000 المُستخدَمين بالفعل من Agent Canvas نفسه)، وMKDD backend بيعمل توجيه مباشر لأي طلب على `/live/{المشروع}/{المسار}` لنفس البورت ده جوه حاوية OpenHands المشتركة.

**قيود صريحة اتفقنا عليها (مش عيوب تصميم مخفية):**
- بورت واحد مشترك — مشروع واحد بس معاينته الحية متاحة في كل لحظة.
- السيرفر لازم يكون شغال فعليًا وقت الطلب — لو وقف، رسالة خطأ واضحة (502) بدل خطأ متصفح غامض أو تعليق صامت.
- بروكسي لطلبات HTTP عادية بس في النسخة الأولى — مش WebSocket الخاص بالتطبيق نفسه (زي Hot Reload بعض الأطر).

**تفاصيل التنفيذ:**
- `server/routes/live-proxy.mjs`: بروكسي حقيقي (`http.request` streaming، مش تحميل الاستجابة كاملة في الذاكرة).
- `server/lib/live-app-instructions.mjs`: تعليمات مشتركة لكل الـ14 موظف (نفس نمط تعليمات المعاينة الستاتيك).
- `detectPreviewLinks.ts` اتوسّعت لتكتشف روابط `/live/` كمان، بكارت مختلف تمامًا في الشات (رابط واضح + شارة "شغال الآن"، مش معاينة مصغّرة بـ iframe مقيَّد — لأن تطبيق تفاعلي حقيقي محتاج مساحة كاملة، مش صندوق صغير).

**باگين حقيقيين اتلقوا وأتصلحوا أثناء الاختبار الحي:**
1. فحص وجود المشروع كان بيرجع نجاح كاذب لمشروع غير موجود (`resolveProjectDir` بتتأكد من الأمان بس، مش من وجود المجلد فعليًا على القرص) — تم إصلاحه بفحص `fs.existsSync` صريح.
2. استخراج hostname الحاوية كان بيحصل وقت تحميل الملف نفسه (مش وقت الاستخدام)، فكان بيكسر أي استيراد للدالة القابلة للاختبار (`OPENHANDS_URL` مش متظبطة وقت الاختبارات) — تم تحويله لدالة كسولة (lazy).
3. أثناء بناء كشف روابط `/live/`: رابط جذر التطبيق (`/live/x/` من غير مسار فرعي — **أشيع استخدام فعليًا**) مكانش بيتلقط خالص، لأن الـ regex كان بيتطلب حرف واحد على الأقل بعد اسم المشروع. تم إصلاحه (الجزء الأخير بقى اختياري بالكامل).

**تم التحقق فعليًا:** اختبار حي كامل (تشغيل سيرفر وهمي حقيقي يمثّل تطبيق موظف، تأكيد نجاح البروكسي، تأكيد 404 لمشروع غير موجود، تأكيد 502 لما السيرفر يوقف). 4 اختبارات دائمة لمنطق تحليل المسار. تحقق يدوي شامل (7 سيناريوهات) لكشف الروابط المُوسَّع.

**الحالة:** ✅ تم البناء والاختبار الكامل (`npm test` → 67/67، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء)، محتاج تأكيد حي على staging (كيرلس فعليًا يشغّل سيرفر مشروعه على البورت المتفق عليه).

---

### 52. المعاينة الحية بترجع "صفحة فاضية" — حل عام دائم (مش تعديل يدوي لكل مشروع)
**السياق:** المعاينة الحية اشتغلت (الـ Proxy وصل لتطبيق كيرلس بنجاح، `200` مع 53 كيلوبايت HTML حقيقي)، لكن الصفحة بانت فاضية بصريًا. المستخدم طلب صراحة **حل ثابت وعام**، مش تعليم كل موظف يظبط إعدادات إطار العمل بتاعه في كل مشروع، وأهم حاجة يفضل شغال صح لو MKDD اتنزّل على VM جديد من غير أي خطوة إضافية.

**السبب الجذري (من محتوى الاستجابة الحقيقية نفسها):** التطبيق (Next.js) بيحط روابط CSS/JS بمسارات **مطلقة من الجذر** (زي `href="/_next/static/chunks/x.css"`). لما المتصفح يحمّل الصفحة من `/live/{مشروع}/...`، أي مسار بيبدأ بـ `/` بيتفسّر كـ"من جذر الموقع بالكامل"، مش من مكانه الصح تحت `/live/{مشروع}/` — فكل ملفات التنسيق والتفاعل بتفشل تحميلها، والصفحة (رغم وصولها 200 فعليًا) بتبان فاضية بصريًا.

**الحل المُختار (بعد نقاش مباشر مع المستخدم):** إعادة كتابة الروابط المطلقة **داخل استجابة HTML نفسها** — تقنية معروفة في أنظمة الـ Reverse Proxy، بدل تعليم كل موظف بشكل يدوي. الـ proxy بقى يجمّع استجابات HTML كاملة (بدل التمرير المباشر)، ويستبدل أي إشارة لمسارات معروفة شائعة (`/_next/`, `/__nuxt/`, `/static/`, `/assets/`) عشان تبقى نسبية لـ `/live/{مشروع}/` — **عام لأي إطار عمل يستخدم نفس الاصطلاحات، بدون أي إعداد يدوي، وثابت في الكود نفسه فمش هيتأثر بأي تنصيب جديد.**

**دقة الاستبدال (تفصيلة أمان مهمة):** الاستبدال بيحصل بس لو المسار جاي مباشرة بعد علامة اقتباس أو قوس (زي ما بيظهر فعليًا في خصائص HTML والبيانات المضمّنة جوه JavaScript)، مش لأي ظهور عشوائي للنص في محتوى الصفحة العادي — **تم اختباره صراحة** (اختبار دائم يتأكد إن جملة عادية زي "شوف /_next/ للتفاصيل" متتأثرش).

**تفصيلتين تقنيتين إضافيتين اتغطّوا احترازيًا:**
- منع الضغط (`Accept-Encoding: identity`) عند الطلب من تطبيق الموظف — لأن قراءة محتوى مضغوط كنص عادي (لإعادة الكتابة) كانت هتكسره.
- حذف صريح لـ `content-encoding` من الاستجابة المُعاد إرسالها للمتصفح، احتياطًا لو تطبيق الموظف تجاهل الطلب.

**تم التحقق فعليًا (مش نظري):** اختبار حي كامل بسيرفر وهمي يحاكي بالضبط نمط استجابة Next.js الحقيقي اللي شفناه من كيرلس — الصفحة الرئيسية اترجّعت بروابط مُعاد كتابتها صح، وطلب لاحق للملف المُعاد كتابة مساره **نجح فعليًا** ورجّع محتواه الحقيقي. 5 اختبارات دائمة جديدة لمنطق إعادة الكتابة.

**حدود معروفة (موثّقة صراحة، مش مخفية):** الحل بيغطي المسارات المطلقة **جوه HTML** بس — لو تطبيق عنده روابط API مطلقة بعنوان كامل مُثبَّت داخل كود JavaScript نفسه (زي `http://localhost:4001/api/...` اللي ظهر فعليًا في بيانات كيرلس)، ده محتاج معالجة إضافية مستقبلية لو ظهر كمشكلة متكررة.

**الحالة:** ✅ تم التصميم والتنفيذ والتحقق الحي الكامل (`npm test` → 73/73، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء)، محتاج تأكيد حي نهائي على staging.

---

### 53. سيرفر المعاينة الحية بيوقف من تلقاء نفسه — فجوة في التعليمات، مش باگ في الكود
**السياق:** كيرلس شغّل سيرفره، اتأكد الاتصال بـ Postgres والمعاينة الحية شغالة فعليًا (200، HTML كامل)، لكن بعد فترة قصيرة السيرفر بيوقف من تلقاء نفسه (`ss -tlnp | grep 4001` بيرجع فاضي).

**التشخيص (بحث في التوثيق الرسمي، مش تخمين):** بحثت في توثيق OpenHands الرسمي (`software-agent-sdk`) ولقيت إن أداة الـ Terminal بتاعة كل موظف **مبنية على جلسات `tmux`**. ده معناه: لو الموظف شغّل سيرفر طويل الأمد بشكل **عادي (foreground)** — من غير ما يخليه شغال في الخلفية صراحة — الأمر بيحجز جلسة الـ terminal بالكامل، وأي خطوة أو أمر تاني بعده بيقاطع (Interrupt) العملية الطويلة، فالسيرفر بيوقف.

**السبب الجذري الحقيقي:** `server/lib/live-app-instructions.mjs` (التعليمات المشتركة لكل الموظفين) **معندهاش أي توضيح صريح لضرورة تشغيل السيرفر في الخلفية**. الأمثلة الموجودة كانت أوامر عادية (`npm start -- --port 4001`) من غير أي إشارة لـ `nohup`/`&`، فأي موظف بيتبع التعليمات دي حرفيًا هيقع في نفس المشكلة.

**الحل:** تحديث `LIVE_APP_INSTRUCTIONS` — توضيح صريح ("Critical") لضرورة التشغيل كعملية خلفية حقيقية (`nohup ... > /tmp/live-app.log 2>&1 &`)، مع أمثلة محدّثة، وخطوة تحقق إجبارية (`curl` على البورت) قبل ما الموظف يقول للمالك إن الرابط جاهز — بدل ما يفترض النجاح من غير تأكيد.

**الحالة:** ✅ تم تحديث التعليمات، محتاج تشغيل `bootstrap-employees.mjs` تاني على الـ VM عشان الموظفين الموجودين ياخدوا التعليمات الجديدة، وتأكيد حي إن كيرلس بقى سيرفره فاضل شغال.

---

### 54. 502 في المعاينة الحية رغم إن السيرفر شغال ومؤكَّد — باگ حقيقي في رؤوس الاستجابة
**السياق:** بعد ما اتأكدنا (بالأدلة الكاملة) إن سيرفر كيرلس شغال فعليًا ومستقر (`LISTEN` على 4001، `curl` محلي بيرجع 200)، ولسه بيظهر `502` في المتصفح. التشخيص السابق (باگ الروابط المطلقة #52) كان صحيح لكن مش كافي — لقينا باگ تاني منفصل تمامًا.

**التشخيص:** لوج حي مباشر أثناء المحاولة كشف الخطأ الحقيقي:
```
Error: Parse Error: Content-Length can't be present with Transfer-Encoding
```

**السبب الجذري:** استجابة تطبيق Next.js الحقيقية (SSR) بتستخدم `transfer-encoding: chunked` (شائع جدًا في السيرفرات اللي بتبعت المحتوى تدريجيًا وقت توليده). كود إعادة كتابة الـ HTML (#52) كان بينسخ كل الرؤوس الأصلية (شاملة `transfer-encoding` القديمة) **ويضيف** `content-length` جديد بدون ما يشيل القديمة — فبقى عندنا الاتنين مع بعض في نفس الرد، وده **مخالفة صريحة لمواصفات HTTP**. Vite (طبقة الـ proxy في الواجهة) رفضت الاستجابة الفاسدة دي فورًا، وده اللي ظهر كـ `502` للمستخدم.

**ليه الاختبارات السابقة معملتش الباگ ده يظهر؟** السيرفر الوهمي اللي استخدمته في اختبار #52 كان بسيط (`res.end(htmlString)` مباشرة) — ده خلّى Node يحسب `content-length` تلقائيًا من غير `transfer-encoding` خالص، فمعكسش سلوك سيرفر SSR حقيقي بيبعت استجابة بالتدريج (Streaming/Chunked). درس مهم: الاختبار البسيط مش دايمًا كافٍ لتمثيل السلوك الحقيقي.

**الحل:** حذف صريح لـ `transfer-encoding` من الرؤوس (بالإضافة لـ `content-encoding` من #52) قبل إعادة الإرسال، بما إننا بنبعت الجسم كامل كقطعة واحدة ثابتة الحجم، مش تدريجي.

**تم التحقق فعليًا:** اختبار طرف-لطرف كامل جديد (دائم) بيحاكي بالضبط استجابة Chunked حقيقية (سيرفر وهمي بيستخدم `res.write()` مرتين بدل `res.end()` مباشرة، زي Next.js فعليًا)، ويتأكد صراحة من عدم وجود `transfer-encoding` في الاستجابة النهائية.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الحي والدائم بالكامل (`npm test` → 74/74)، محتاج تأكيد حي نهائي على staging.

---

### 55. صفحة `/admin` فاضية رغم إن كل الملفات بترجع 200 — كود التطبيق نفسه بريء، تأكَّد بدليل كيرلس نفسه
**السياق:** كل الملفات (HTML، CSS، JS) كانت بترجع `200` من خلال `/live/`، لكن صفحة `/admin` لسه فاضية بصريًا. المستخدم سأل صراحة: "ده عيب من شغل كيرلس ولا مشكلتنا احنا؟"

**الدليل الحاسم (من كيرلس نفسه، مش تخمين منّا):** كيرلس فتح `/admin` **مباشرة من داخل بيئته هو** (بدون المرور بأي Proxy بتاعنا) — الصفحة ظهرت كاملة وصح (`/admin/create-first-user` بكل الحقول). كمان راجع لوج تطبيقه وقت فتح الصفحة عبر `/live/` ولقى استدعاءات API بترجع `200` بنجاح (`GET /api/admins/me`, `GET /api/payload-preferences/nav`) — يعني تطبيقه بريء 100%، والمشكلة **مؤكَّدة إنها عندنا**.

**السبب الجذري (تحليل دقيق من نفس الأدلة):** استدعاءات `/api/admins/me` دي **مش نص مكتوب في الـ HTML الأولي** — دي كود JavaScript بيتنفذ **وقت التشغيل في المتصفح** (React بتاع Payload بيطلب بياناته ديناميكيًا). لما المتصفح يفتح الصفحة من `/live/test-for-13/`، وكود React يعمل `fetch('/api/admins/me')`، الطلب ده بيروح **لجذر موقعنا احنا** (`192.168.2.18:15173/api/admins/me`)، مش لتطبيق كيرلس — لأن `/api/` **مش كانت موجودة** في قائمة المسارات اللي بنعيد كتابتها (#52 كانت بتغطي `/_next/`, `/static/`, `/assets/` بس).

**الحل:**
1. إضافة `/api/` لقائمة المسارات القابلة لإعادة الكتابة.
2. **توسيع نطاق إعادة الكتابة نفسه** ليشمل ملفات **JavaScript** كمان، مش HTML بس — لأن استدعاءات `fetch('/api/...')` غالبًا مكتوبة كنص ثابت جوه ملفات JS المُجمَّعة نفسها (مش جوه HTML الأولي).
3. إعادة تسمية الدالة (`rewriteLiveAppHtml` → `rewriteLiveAppPaths`) عشان الاسم يعكس نطاقها الحقيقي الجديد.

**تفصيلة تقنية مهمة اتأكد منها:** إعادة الكتابة بتشتغل حتى لو الـ endpoint الكامل مبني ديناميكيًا في كود مُصغَّر (زي `"/api/" + endpoint`)، لأن الاستبدال بيحصل على **البادئة القصيرة الثابتة فقط** (`"/api/`)، مش على المسار الكامل — تم اختباره صراحة.

**تم التحقق فعليًا:** اختبار حي كامل بمحاكاة السيناريو الحقيقي بالضبط (صفحة HTML بترجع ملف JS، والملف نفسه فيه `fetch("/api/admins/me")`) — النتيجة النهائية `fetch("/live/test-for-13/api/admins/me")`، مطابقة تمامًا للمطلوب. اختباران دائمان جدد (شامل حالة البناء الديناميكي).

**الحالة:** ✅ تم التشخيص الكامل (بالتعاون مع تشخيص كيرلس نفسه) والإصلاح والتحقق الحي (`npm test` → 76/76)، محتاج تأكيد حي نهائي على staging.

---

### 56. إعادة تصميم كاملة للمعاينة الحية — بورت مخصص دائم لكل مشروع، بدون أي إعادة كتابة خالص
**السياق:** بعد سلسلة إصلاحات متتالية (#52 روابط ثابتة، #53 تشغيل خلفي، #54 تعارض رؤوس، #55 استدعاءات API) كانت المشكلة لسه بتظهر من مسار جديد كل مرة — نمط "نلحق كل حفرة نلاقيها". المستخدم طلب صراحة حل نهائي شامل: **"عاوزين نعمل المنظومة صح ومرة واحدة عشان مانرجعش ليها تاني."**

**القرار المعماري (بعد نقاش مباشر ومفصّل مع المستخدم):** التخلي الكامل عن نموذج "بورت مشترك واحد + مسار فرعي (`/live/{مشروع}/`) + إعادة كتابة كل مسار مطلق"، واستبداله بـ:

- **بورت مخصص دائم لكل مشروع** من نطاق محجوز (4001–4020، أي 20 مشروع حي متزامن).
- **تمرير خام 100% (Raw TCP Passthrough)** — بدون أي فحص أو تعديل للمحتوى خالص. بما إن التطبيق بيوصل من "جذره" الحقيقي فعليًا (مش تحت مسار فرعي)، **مفيش أي مسار مطلق ممكن "يفلت"** — الفئة الكاملة من المشاكل السابقة بقت **مستحيلة بنيويًا**، مش مُصلَّحة حالة بحالة.
- **سجل تخصيص دائم** (`live-port-registry.mjs`) — كل مشروع ياخد بورته أول ما يطلبه (عبر `GET /api/projects/{مشروع}/live-port`)، ونفس البورت يفضل ثابت له للأبد (مش بيتغيّر بين الجلسات).
- **بناء الرابط الكامل في الفرونت إند** — الموظف بيشارك بس صيغة `/live-port/{بورت}/{مسار}` كنص، والواجهة نفسها (اللي عندها وصول لـ `window.location` الحقيقي) بتبني الرابط الكامل — بدل ما نعتمد على الموظف يعرف عنوان الشبكة الخارجي (نفس الدرس المُستفاد من مشكلة `AUTOMATION_BASE_URL` القديمة).

**ملفات جديدة:**
- `server/lib/live-port-registry.mjs` — سجل التخصيص الدائم (كتابة ذرية آمنة، نفس نمط `project-metadata.mjs`).
- `server/live-port-proxy.mjs` — 20 سيرفر TCP خام، كل واحد بيمرّر لنفس رقم البورت جوه حاوية OpenHands.
- `server/routes/project-live-port.mjs` — الـ API اللي الموظف بيسأل بيه عن بورته.

**ملفات اتشالت بالكامل** (منعًا لأي كود ميت): `server/routes/live-proxy.mjs` و`live-proxy.test.mjs` (النظام القديم بالكامل).

**تحديثات:** `detectPreviewLinks.ts` (صيغة `/live-port/{بورت}/{مسار}` بدل `/live/{مشروع}/{مسار}`)، `ChatScreen.tsx` (بناء الرابط ديناميكيًا)، `live-app-instructions.mjs` (إعادة كتابة كاملة — خطوة سؤال عن البورت، تشغيل خلفي، تحقق، مشاركة الصيغة الصح)، `compose.yml` (نشر نطاق 4001-4020، **بدون** أي إزاحة قابلة للتخصيص بين الداخلي والخارجي — عمدًا، لأن الفرونت إند بيستخدم رقم البورت الحقيقي مباشرة، فأي إزاحة كانت هتكسر الرابط)، `vite.config.ts` (حذف قاعدة `/live` القديمة).

**باگين حقيقيين اتلقوا وأتصلحوا أثناء الاختبار الحي:**
1. **تخطيط أولي غلط لنطاق البورت الخارجي لـ staging** — كان المفروض يكون مختلف عن الداخلي (زي باقي البورتات)، لكن اكتشفت إن ده هيكسر الرابط المبني في الفرونت إند (اللي بيستخدم رقم البورت الحقيقي من غير أي تحويل). تراجعت عن الفكرة، وخليت الأرقام متطابقة دايمًا (توثيق صريح في `compose.yml` ليه القرار ده).
2. **فجوة تغطية حقيقية في `npm test` نفسه** — النمط `server/**/*.test.mjs` **مش بيغطي أي ملف اختبار موجود مباشرة جوه `server/`** (لازم مجلد فرعي واحد على الأقل). اختباراي الجديدين (`live-port-proxy.test.mjs`) كانوا بينجحوا لما بشغّلهم يدويًا، لكن **بيختفوا تمامًا** من عدّاد `npm test` الرسمي! تم إصلاح النمط ليغطي الحالتين.

**تم التحقق فعليًا (مش نظري):**
- اختبار حي لمنطق التمرير الخام بمعزل تام (عناوين IP منفصلة تحاكي حاويتين حقيقيتين، لتجنّب تعارض بورت صامت اكتشفته في محاولة أولى فاشلة) — البيانات وصلت واتردّت صح في الاتجاهين.
- اختبار حي لسجل التخصيص (`GET /api/projects/{مشروع}/live-port` رجّع بورت صحيح).
- 9 اختبارات دائمة جديدة (7 للسجل، شاملة اختبار استنفاد النطاق بعزل صحيح؛ 2 لمنطق التمرير، شاملة حالة "لا يوجد سيرفر شغال").

**الحالة:** ✅ تم التصميم والتنفيذ الكامل والتحقق الحي (`npm test` → 72/72، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء). محتاج: (1) تشغيل `bootstrap-employees.mjs` تاني عشان الموظفين ياخدوا التعليمات الجديدة، (2) تأكيد إن `docker compose up` فتح نطاق 4001-4020 صح على staging، (3) تأكيد حي نهائي مع كيرلس.

---

### 57. مراجعة شاملة لكل ملفات الموظفين الـ14 — ضمان تسليم احترافي حقيقي بين الموظفين
**السياق:** طلب صريح لمراجعة كل موظف والتأكد إن شغله "محترف جدًا" للموظف اللي بعده، بعد ملاحظة مشاكل في شغل كيرلس (اللي اتضح لاحقًا إنها كانت في نظامنا، مش في تنفيذه الفعلي — لكن المراجعة كشفت فجوات حقيقية في التعليمات المكتوبة له وللباقي).

**الفحص المنهجي:** راجعت كل ملف من الـ14 بعناية، وفحصت تحديدًا: (1) هل عنده قسم "Deliverables" واضح يحدد المُخرجات المتوقعة، (2) هل عنده قاعدة "تحقق فعلي قبل الادعاء" — مش بس افتراض نجاح بناءً على المنطق.

**الاكتشاف الأول (الأهم، مؤثر على كل الـ14 موظف مرة واحدة):** `AGENTS.md` (القاعدة المشتركة) كان عنده بالفعل قاعدة "تحقق قبل الادعاء" — لكن **محصورة بس في سياقين**: (أ) التوقيعات الرسمية قبل الإصدار (§6)، و(ب) الفحص البصري للواجهات (§7). **مفيش قاعدة عامة تغطي أي ادعاء عادي أثناء الشغل اليومي** (زي "السيرفر شغال")، وده بالظبط النمط اللي حصل مع كيرلس (قال "شغال" أكتر من مرة قبل ما يتأكد فعليًا، في سياق محادثة عادية مش sign-off رسمي).

**الحل:** قسم جديد شامل في `AGENTS.md` (§8، "Evidence-Based Claims") يوسّع المبدأ ليشمل **أي** ادعاء عن حالة شيء في أي وقت — مش بس السياقين المحدودين. بما إن كل الموظفين بيرجعوا لـ`AGENTS.md` كقاعدة عليا، الإضافة دي انطبقت على الـ14 موظف تلقائيًا من غير أي تكرار.

**الاكتشاف الثاني:** قسم "Deliverables" الصريح كان **ناقص في 10 من 14 موظف** (implementation، qa، test-automation، code-review، security-review، devops، technical-writer، business-analyst، integration-engineer — وproduct-manager اتضح إن عنده تغطية مكافئة أقوى تحت اسم "PRD Responsibilities"، فمش فعليًا ناقص).

**الحل:** أضفت قسم "Deliverables" واضح ومخصص لكل دور، مع تعزيز إضافي للقيود (Constraints) في الأدوار الأكتر ارتباطًا مباشرًا بمشكلة "الادعاء بدون تحقق" (كيرلس، Antonious/DevOps، مارينا/Integration، أبانوب/Release Manager — بما إنه آخر خط دفاع قبل الإنتاج، وأضفت له تحديدًا شرط إنه "ميقبلش" تقارير الأدوار التانية بدون أدلة حقيقية مرفقة).

**تم التحقق فعليًا:**
- اكتشاف حي: `listEmployeeNames()` رجّعت 14 موظف بالظبط، بدون أي كسر.
- قراءة حية: `readEmployeeDisplayInfo()` لكل الـ14 موظف نجحت، بدون أي خطأ في الـ frontmatter أو بنية الملف.
- فحص شامل بالكود يتأكد من وجود قسم Deliverables/مكافئ وConstraints في كل ملف.
- `npm test` → 72/72 (مفيش تأثير على الكود، بس تأكيد شامل).

**الحالة:** ✅ تم الفحص والإصلاح الكامل والتحقق الحي لكل الـ14 ملف. محتاج تشغيل `bootstrap-employees.mjs` تاني على الـ VM عشان الموظفين الموجودين ياخدوا التعليمات المُحدَّثة.

---

### 58. رفع ملفات مباشر من صفحة المشروع + تحسينات جذرية على تعليمات كيرلس ومريم
**السياق:** بعد المراجعة الشاملة (#57)، ناقش المستخدم مشاكل جوهرية إضافية: (1) كيرلس بيطلّع شغل أقرب لبروتايب مش موقع حقيقي متكامل، (2) لازم يبلّغ المالك لو لقى شغل الموظف اللي قبله (مريم/شيري) ناقص أو فيه تعارض بدل ما يتجاهله أو يخمّن، (3) مريم لازم تستخدم صور حقيقية بدل أشكال CSS، (4) المالك محتاج يقدر يرفع ملفات (زي صور منتجات حقيقية) مباشرة للمشروع نفسه، مش لمحادثة موظف واحد بس.

**1. ميزة الرفع المباشر (جديدة بالكامل):**
- `server/routes/project-upload.mjs`: الملفات بتتحط مباشرة في `{المشروع}/uploads/` — بما إن ده نفس المجلد المشترك اللي كل موظف شايفه في بيئته، الملف بيبقى متاح فورًا لأي موظف، بدون أي مشاركة يدوية لكل محادثة على حدة.
- **تكامل تلقائي كامل** مع الأنظمة الموجودة بالفعل (بدون أي تعديل إضافي): الملفات المرفوعة بتظهر تلقائيًا في قسم "ملفات المشروع" (#50) وقابلة للمعاينة عبر `/preview/` (#41) — لأنها فعليًا ملفات حقيقية في نفس المجلد.
- حماية صارمة من Path Traversal في اسم الملف (نفس نمط الحماية المُستخدَم في كل مسارات الملفات السابقة).
- زرار رفع في صفحة المشروع الرئيسية (`ProjectHomeScreen.tsx`).
- تعليمات مشتركة جديدة لكل الـ14 موظف (`owner-uploads-instructions.mjs`) — يفحصوا مجلد `uploads/` قبل شغلهم ويستخدموا الملفات الحقيقية بدل ما يخترعوا بديل.

**2. تحسينات جذرية على ملف كيرلس (implementation.md) — ثلاث إضافات منفصلة ومتكاملة:**
- **Mission مُعاد صياغته بالكامل:** تعريف صريح لمعنى "مُنفَّذ" = التجربة الحقيقية شغالة ومترابطة فعليًا (مش بنية تحتية/Backend بس مُقدَّمة كأنها المنتج النهائي).
- **قسم جديد "Upstream Consistency Check":** لو شغل مريم/شيري نفسه ناقص أو فيه تعارض داخلي (مثال حقيقي اتناقش: بروتايب بلغات ناقصة عن المطلوب) — **ممنوع** يخمّن أو ينفّذ الجزء الواضح ويسيب الباقي، **لازم** يوقف ويبلّغ المالك تحديدًا مين المسؤول (مريم أو شيري) عشان يكمّلوا شغلهم.
- **قسم جديد "Full-Scope Completion and Honest Handoff":** مفيش "خلصت" إلا لو 100% من النطاق المعتمد مُغطّى فعليًا — ولو فيه عائق حقيقي، يُسمّى بوضوح بدل ما يُقدَّم شغل جزئي كأنه كامل، عشان الأدوار اللي بعده (QA، المراجعين) يقدروا يشتغلوا صح على أساس تسليم صادق.

**3. تحسين ملف مريم (ui-ux.md):** قسم جديد "Real Images, Not CSS Placeholders" — صور حقيقية مرخّصة قانونيًا، محفوظة كملفات فعلية داخل المشروع (مش روابط خارجية)، مع فحص مجلد `uploads/` أولًا لو المالك رفع صور حقيقية بالفعل. توضيح صريح للفرق بين صور البروتايب التوضيحية وصور المنتج النهائي الحقيقية (اللي لازم تيجي من المالك، مش من مريم).

**تم التحقق فعليًا:** اختبار حي كامل من الآخر للآخر (رفع ملف حقيقي → تأكيد وجوده على القرص بصلاحيات صح → ظهوره التلقائي في قائمة الملفات الموجودة → قابليته للمعاينة عبر الرابط الموجود بالفعل → رفض محاولة traversal). 5 اختبارات دائمة جديدة لحماية المسار. تأكيد حي من قراءة `implementation.md` و`ui-ux.md` بعد كل التعديلات (بدون أي كسر في البنية).

**الحالة:** ✅ تم البناء والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء). محتاج تشغيل `bootstrap-employees.mjs` تاني على الـ VM.

---

### 59. الاتصال بـ GitHub عبر MCP متاح، لكن مفيش تحديد صريح مين المسؤول عن إنشاء الريبو ومتى
**السياق:** المستخدم أشار إن الموظفين عندهم اتصال حقيقي بـ GitHub عبر MCP Server (متأكَّد فعليًا من بيانات محادثة حقيقية شفناها قبل كده لشنودة: `"github": {"url": "https://api.githubcopilot.com/mcp/", ...}`)، وطلب إن باجوش يكون مسؤول عن إنشاء ريبو مخصص للمشروع بعد الاتفاق على المتطلبات، وكل الموظفين يكون عندهم علم بمكانه.

**الفحص:** `AGENTS.md` كان عنده القاعدة العامة بالفعل ("كل مشروع له ريبو مخصص")، **لكن مفيش تحديد صريح لمين المسؤول عن إنشائه ومتى بالظبط** — نفس نمط الفجوة المُكتشَفة في المراجعة الشاملة السابقة (#57): قاعدة موجودة، لكن التنفيذ الفعلي (مين، إمتى) غير محدد.

**الحل (ثلاث طبقات متكاملة):**
1. **`AGENTS.md` §2 (القاعدة العليا، تنطبق على الجميع تلقائيًا):** تحديد صريح — باجوش هو المسؤول، التوقيت بعد اعتماد Gate 1 مباشرة، والمكان المسجَّل فيه (`docs/project-context.md`).
2. **`product-manager.md` (تفصيلي لباجوش تحديدًا):** قسم جديد "Repository Creation" — خطوات محددة (إنشاء ريبو مخصص عبر أدوات GitHub المتاحة، تسمية واضحة، تسجيل فوري في project-context.md، حفظ الـ PRD فيه)، مع تعليمة صريحة: لو فشل الإنشاء لأي سبب، يبلّغ المالك بصراحة بدل ما يكمل وكأن الريبو موجود.
3. **`github-repo-instructions.mjs` (تعليمات مشتركة لكل الـ14 موظف):** تذكير عملي — تأكد من `project-context.md` قبل أي شغل يحتاج Git، استخدم نفس الريبو المسجَّل، لو Gate 1 معتمد ومفيش ريبو مسجَّل، بلّغ المالك بدل الافتراض أو الإنشاء من تلقاء نفسك (بما إن ده مش دورك).

**اكتشاف إضافي أثناء التنفيذ:** قائمة أقسام `project-context.md` القياسية (المُكرَّرة في كل الـ14 ملف) **معندهاش حقل صريح لـ"Repository"** خالص — فحتى لو باجوش سجّله، مفيش مكان معياري موحّد يضمن كل الموظفين يلاقوه بنفس الطريقة. تم إصلاحه بإضافة حقل "Repository (GitHub name/URL)" لنفس القائمة في كل الملفات، بسكريبت آمن (15/15 نجح: الـ14 موظف + الأوركستريتور).

**تم التحقق فعليًا:** قراءة حية لكل الـ14 موظف بعد كل التعديلات (بدون أي كسر)، تأكيد إن `product-manager.md` (اللي اتعدَّل يدويًا وبالسكريبت معًا) لسه سليم البنية بالكامل. `npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء.

**الحالة:** ✅ تم التصميم والتنفيذ والتحقق الكامل. محتاج تشغيل `bootstrap-employees.mjs` تاني على الـ VM.

---

### 60. `litellm.APIError: input exceeds the context window` — تشخيص كامل وإصلاح حقيقي
**السياق:** كيرلس واجه فشل كامل أثناء الشغل: `litellm.APIError: Your input exceeds the context window of this model`. المستخدم سأل صراحة: "أكيد في إعدادات OpenHands حل، الغرض من الاستضافة الذاتية إننا نطلع من المشكلة دي."

**رحلة التشخيص الكاملة (كل خطوة بدليل حقيقي، مش تخمين):**
1. **بحث في التوثيق الرسمي:** أكّد وجود آلية اسمها "Condenser" (تلخيص تلقائي لتاريخ المحادثة عند اقتراب الحد الأقصى) — لكن التوثيق الحديث (مارس 2026) أوضح إن **النموذج نفسه هو اللي بيقرر إمتى يطلب التلخيص** (عبر أداة `request_condensation`)، مش حد صريح تلقائي.
2. **اختبار حي مباشر (زرار "Compact context" في واجهة OpenHands نفسها):** فشل برسالة قاطعة: **"No condenser configured"** — يعني **مفيش Condenser مُفعَّل خالص** على بروفايل كيرلس، لا تلقائي ولا حتى يدوي.
3. **تناقض مهم اكتُشف من المستخدم نفسه:** لقطة شاشة أظهرت `375.2k / 1.1M` توكن (36% مُستخدَم فقط) وقت حصول الخطأ — سؤال المستخدم "دي مش لسه بدري؟" كشف تناقضًا حقيقيًا محتاج تفسير.
4. **بحث إضافي (بناءً على طلب صريح من المستخدم للبحث الدقيق):** أكّد إن الرقم `1.1M` المعروض هو **حد عام افتراضي من قاعدة بيانات LiteLLM الداخلية**، مش الحد الحقيقي — موثّق رسميًا في GitHub Issue #9573 ("OpenHands ignores custom context window... displays hardcoded limit"). النموذج الحقيقي المُستخدَم (`GPT-5.6 Sol` عبر اشتراك ChatGPT Plus/Codex) له **حد فعلي حقيقي موثَّق = 258,400 توكن فقط** — أقل بـ76% من الرقم المعروض، وتم تخفيضه من OpenAI **بدون إعلان رسمي** في يوليو 2026.

**الحل (طبقتين، اتأكدوا حيًا على بروفايل كيرلس أولًا قبل التعميم):**
1. **`max_input_tokens: 200,000`** في إعداد بروفايل الـ LLM نفسه (عبر واجهة الإعدادات) — حد حقيقي محافظ، أقل من 258,400 الموثَّق.
2. **Condenser مُفعَّل صراحة** بـ `max_tokens: 160,000` (هامش أمان إضافي 40 ألف توكن قبل حد الـ200K) — عبر API مباشرة (الواجهة فشلت تحفظ الحقل ده تحديدًا مرتين، اتأكد بفحص حي متكرر).

**تفاصيل تقنية اتأكدت أثناء التنفيذ:**
- `POST /api/agent-profiles/{name}` بيرفض أي طلب من غير `llm_profile_ref` صراحة، حتى لو الهدف تعديل حقل `condenser` بس — اكتُشف من رسالة خطأ `422` واضحة.
- تم تطبيق نفس الإصلاح على باقي الـ13 موظف عبر حلقة أوامر واحدة (كلهم بيستخدموا نفس بروفايل الـ LLM حاليًا).
- `bootstrap-employees.mjs` اتحدَّث ليشمل نفس إعداد الـ Condenser تلقائيًا لأي موظف جديد أو إعادة تشغيل مستقبلية — مع ملاحظة صريحة في الكود إن الرقم `160000` خاص بالموديل الحالي المشترك، ولازم يُعاد التحقق منه لو كل موظف بقى ليه بروفايل LLM منفصل مستقبلًا (خطة المستخدم المُعلنة).

**تنبيه صريح للمستخدم قبل التنفيذ:** تم توضيح التريد-أوف كاملاً (تكلفة بسيطة عند التلخيص، مقابل منع فشل كامل غير متوقع)، وتم التأكيد إن الحماية الحقيقية للتسليم النهائي مش الـ Condenser نفسه، لكن الالتزام بالفعل بقاعدة "المفروض أصلًا موجودة" (`Conversation memory must never be the sole source of important project decisions` — أي قرار مهم لازم يكون في `docs/project-context.md`، مش في ذاكرة المحادثة).

**الحالة:** ✅ تم التشخيص الكامل (بحث رسمي + اختبار حي متكرر) والإصلاح والتحقق الحي (فحص GET مباشر بعد كل POST) على كل الـ14 موظف.

---

### 61. مفيش طريقة لبدء محادثة جديدة مع موظف موجود بالفعل — ميزة جديدة كاملة
**السياق:** بعد إصلاح #60، المحادثة **الحالية** مع كيرلس فضلت عالقة على النسخة القديمة (بدون Condenser) — بما إن كل محادثة بتاخد نسخة ثابتة من إعدادات الموظف وقت إنشائها (نفس المبدأ المُكتشَف قبل كده في مشاكل التعليمات والروابط). المستخدم أراد يبدأ محادثة جديدة، لكن **معماريتنا الحالية (project + employee = محادثة واحدة بس) معندهاش آلية لده خالص**.

**الحل المعماري (اتأكد حيًا قبل التنفيذ):**
- تأكدنا (عبر فحص الـ OpenAPI Schema مباشرة) إن `GET /api/conversations/search` بترتيب افتراضي **`CREATED_AT_DESC`** (الأحدث أولًا) — يعني أي محادثة جديدة بنفس الوسوم هتظهر تلقائيًا كأول نتيجة، وكودنا الحالي (`findAuthorizedConversation`) هيلاقيها ويستخدمها تلقائيًا **من غير أي تعديل لمنطق البحث نفسه**.
- بناءً على ده: `POST /api/chat/new` (route جديد) — بيفرض إنشاء محادثة جديدة **دايمًا**، بدل الفحص المعتاد عن وجود محادثة سابقة (`handleChatSend`). المحادثة القديمة **بتفضل موجودة بالكامل** (وصولها متاح مباشرة من واجهة OpenHands الأصلية)، بس بتبطل "النشطة" بالنسبة لواجهة MKDD.
- استُخرج منطق إنشاء المحادثة المشترك (`createNewConversation`) لدالة واحدة يستخدمها الـ Routeان (`handleChatSend` للحالة "أنشئ لو مش موجود"، و`handleChatNew` للحالة "أنشئ دايمًا") — بدل تكرار نفس المنطق.
- زرار "محادثة جديدة" في واجهة الشات، مع تأكيد صريح (Confirmation) قبل التنفيذ، وتنبيه واضح لو المستخدم دوس عليه من غير ما يكتب رسالة أولًا.

**تم التحقق فعليًا:** اختبار حي كامل — استدعاء `/api/chat/new` مرتين متتاليتين لنفس المشروع/الموظف رجّع **معرّفين مختلفين تمامًا** لمحادثتين حقيقيتين منفصلتين (`conv-1` ثم `conv-2`)، يؤكد إن كل استدعاء بيفرض إنشاء جديد فعليًا بدون أي إعادة استخدام. اختبارات الانحدار الموجودة (`chat.test.mjs`) اتأكد نجاحها بالكامل بعد إعادة الهيكلة (9/9).

**الحالة:** ✅ تم التصميم والتنفيذ والتحقق الحي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء).

---

### 62. شريط فريق المشروع — تراكب الوظائف، حدود ناقصة، مفيش أيقونة دور
**السياق:** بعد تأكيد نجاح ميزة "بدء محادثة جديدة"، المستخدم لاحظ 3 مشاكل بصرية في شريط الفريق (Project Home): (1) نصوص الوظائف بين البطاقات المتجاورة بتتراكب فوق بعض، (2) بطاقة الموظف مفيهاش حدود واضحة، (3) طلب أيقونة تدل على الوظيفة.

**السبب الجذري للتراكب (اكتشاف دقيق، مش تخمين):** `.employee-card-info` (الحاوية اللي جوّاها الاسم والوظيفة) **معندهاش عرض صريح محدد**. بما إن البطاقة الأب `display: flex; flex-direction: column`، الأطفال في flex container عمودي بياخدوا **عرض محتواهم الطبيعي** بدل عرض الأب بالكامل — فالنص الطويل ("Integration Engineer") كان "بيفيض" برّه حدود البطاقة (76px) ويتراكب مع البطاقة المجاورة، رغم وجود `overflow: hidden` و`text-overflow: ellipsis` على النص نفسه (اللي كانوا عاجزين يشتغلوا من غير عرض محدود للأب أولًا).

**الحدود الناقصة:** `.project-home .employee-card` كان فيها `border: none; background: transparent;` صراحة — قاعدة CSS أكثر تحديدًا (بسبب `.project-home` prefix) كانت بتطغى على الحدود الافتراضية الموجودة في القاعدة العامة.

**الحل:**
1. إضافة `width: 100%` لـ `.employee-card-info` — الإصلاح الجذري للتراكب.
2. استبدال `border: none` بحدود واضحة (`1px solid var(--mkdd-border-subtle)`) + `border-radius`.
3. خريطة أيقونات جديدة (`ROLE_ICONS`) تربط كل معرّف موظف مستقر (زي `architect`, `implementation`) بأيقونة مناسبة لدوره من `react-icons/fa6`، معروضة كشارة دائرية صغيرة على زاوية الصورة الشخصية (مع `overflow: visible` على الصورة نفسها، بما إنها كانت `overflow: hidden` بالوراثة من القاعدة الأساسية، وده كان هيقطع الشارة).

**باگ فرعي اتلقى وأتصلح أثناء التنفيذ:** أيقونة `FaDraftingCompass` (المُختارة أول مرة للمعماري) **مش موجودة فعليًا** في نسخة `react-icons/fa6` المُثبَّتة — اكتُشف عبر `tsc -b` مباشرة، وتم استبدالها بـ`FaCompassDrafting` (الاسم الصحيح الفعلي)، بعد التحقق البرمجي المباشر من كل الأسماء الـ21 المُستخدَمة في الملف مقابل الحزمة المُثبَّتة فعليًا.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف بعد تصحيح اسم الأيقونة، `npm run build` ناجح، `oxlint` 0 أخطاء).

---

### 63. "بدء محادثة جديدة" بيخصم تكلفة المحادثة القديمة من إجمالي المشروع
**السياق:** بعد التأكد من نجاح ميزة "بدء محادثة جديدة" (#61) وإصلاح استعادة الهوية (#62)، لاحظ المستخدم إن التكلفة الإجمالية للمشروع **قلّت فجأة** بعد إنشاء محادثة جديدة — كأن الفلوس اللي اتصرفت فعليًا على المحادثة القديمة (طويلة، تكلفتها حقيقية ومؤكَّدة) اختفت من الحساب.

**السبب الجذري:** حساب التكلفة الإجمالية (`totalProjectCost` في `useProjectTeamStatus.ts`) كان بيعتمد بالكامل على `findAuthorizedConversation` — اللي بيرجّع **محادثة واحدة بس** (الأحدث، بتصميم مقصود لأغراض الشات ذاتها). قبل الميزة الجديدة، كانت المحادثة الوحيدة الموجودة هي المحادثة "النشطة" الوحيدة، فحسابها كان صحيحًا بالصدفة. بعد إضافة إمكانية إنشاء محادثة جديدة، بقى فيه **أكتر من محادثة حقيقية** لنفس الموظف/المشروع، لكن الحساب لسه بيعتمد بس على "الأحدث" — فتكلفة أي محادثة قديمة (رغم إنها اتصرفت فعليًا) بقت **غير مرئية تمامًا** في الإجمالي.

**الحل (فصل واضح بين سؤالين مختلفين):**
- **"أي محادثة أفتح للشات؟"** → يفضل زي ما هو، الأحدث بس (`findAuthorizedConversation`) — صحيح ومقصود.
- **"كام صُرف فعليًا على المشروع ده؟"** → دالة جديدة `findAllAuthorizedConversations` (`server/lib/authorize-conversation.mjs`) بترجّع **كل** المحادثات المطابقة، ومجموع تكلفتها كلها (`totalCost` — حقل جديد في استجابة `GET /api/conversation`) هو المُستخدَم للإجمالي، مش تكلفة المحادثة النشطة بس.

**تفصيلة مهمة أثناء التنفيذ:** واجهت جلسة العمل عدة انقطاعات تقنية متكررة في استقرار أدوات التنفيذ نفسها (استجابة متقطعة لأوامر بسيطة). تم التعامل معها بصراحة كاملة مع المستخدم — توضيح دقيق لأي جزء من الإصلاح اكتمل فعليًا (Backend بالكامل) وأي جزء لسه ناقص (ربط الواجهة الأمامية)، بدل الادعاء باكتمال العمل قبل التأكد الفعلي، ثم استكمال العمل بمجرد استقرار الأدوات.

**تم التحقق فعليًا:** اختبار حي كامل بمحاكاة سيناريو مطابق تمامًا للمشكلة المُبلَّغة (محادثة قديمة بتكلفة حقيقية 10.75$ + محادثة جديدة بتكلفة 0.50$) — النتيجة `totalCost: 11.25` (المجموع الصحيح الكامل)، بينما `conversation.cost` (الخاص بالمحادثة النشطة فقط) فضل يوضح 0.50$ بشكل منفصل وصحيح لغرضه الخاص (بدون خلط الاستخدامين).

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الحي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء).

---

### 64. كل شيء فاضي في الواجهة بعد إصلاح التكلفة — مضاعفة عدد الطلبات
**السياق:** بعد نشر إصلاح التكلفة (#63)، أبلغ المستخدم إن الواجهة بالكامل بقت فاضية — المشاريع موجودة لكن المحادثات فاضية، ومفيش أي بيانات بتظهر ("يعملون الآن" و"التكلفة الإجمالية" ظاهرين كـ"..." مستمرة، مش أرقام). تم استبعاد فرضية الكاش صراحة (اختبار في وضع تصفح خاص، نفس المشكلة ظهرت).

**التشخيص:** فحص حي مباشر أكّد إن الـ Backend نفسه شغال سليم بدون أي استثناء (لوجات نظيفة)، وإن `/api/conversation` بيرجّع بيانات صحيحة وكاملة فعليًا لما اتفحص مباشرة (`curl`). هذا استبعد وجود خطأ برمجي واضح، ووجّه الشك لمشكلة أداء/حمل بدل خطأ منطقي.

**السبب الجذري:** إصلاح #63 (لحساب `totalCost`) كان بيستخدم `Promise.all([findAuthorizedConversation, findAllAuthorizedConversations])` — **طلبين منفصلين تمامًا**، كل واحد منهم بيعمل استعلام `/api/conversations/search` مستقل بالكامل. بما إن `useProjectTeamStatus` بتستدعي `/api/conversation` لكل واحد من الـ14 موظف **كل 5 ثواني**، ده معناه **تضاعف عدد الطلبات الفعلي من 14 لـ28 كل دورة تحديث** — ضغط حقيقي إضافي على النظام بسبب إصلاح كان الهدف منه بريء تمامًا.

**الحل:** إلغاء الازدواجية بالكامل — استخدام `findAllAuthorizedConversations` **فقط** (طلب واحد بس)، واستنتاج "المحادثة النشطة" كأول عنصر في النتيجة، بالاعتماد على الترتيب الافتراضي المؤكَّد سابقًا لـ`/api/conversations/search` (الأحدث أولًا، `CREATED_AT_DESC`) — بدل عمل استعلام ثاني منفصل للحصول على نفس المعلومة.

**تم التحقق فعليًا (الاثنين معًا في نفس الاختبار):**
- عدّاد طلبات حي مباشر أكّد: **طلب واحد بالظبط** لكل استدعاء (بعد ما كان اتنين) — رجّع عدد الطلبات لنفس المستوى الأصلي قبل #63.
- `totalCost` لسه صحيح 100% (11.25 = 0.50 + 10.75) — التأكد من عدم فقدان إصلاح #63 نفسه أثناء تحسين الأداء.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الحي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `oxlint` 0 أخطاء). محتاج تأكيد حي نهائي من المستخدم إن الواجهة رجعت تعرض البيانات صح بعد الـ deploy.

---

### 65. #64 مكانتش كافية — تصميم صحيح نهائي: فصل الحالة السريعة عن التكلفة الإجمالية
**السياق:** بعد نشر #64 (تقليل الطلبات من اتنين لواحد)، أبلغ المستخدم إن المشكلة **اتفاقمت أكتر**: "قبل إصلاح التكلفة كان بيعمل تأخير بسيط، دلوقتي مش يظهر المحادثات نهائيًا".

**السبب الجذري الحقيقي (اكتُشف بعد تحليل أعمق):** إصلاح #63 استخدم دالة `findAllAuthorizedConversations` — دي **مسح شامل بدون توقف مبكر** لكل صفحات `/api/conversations/search` في النظام. مقابلها، الدالة الأصلية (`findAuthorizedConversation`) كانت **بتتوقف فور ما تلاقي أول تطابق** (Short-circuit)، فكانت سريعة جدًا. إصلاح #64 قلّل عدد الطلبات من اتنين لواحد، **لكن الطلب الوحيد المتبقي كان النسخة البطيئة (المسح الشامل)** — يعني كل استدعاء لكل موظف (كل 5 ثواني × 14 موظف) بقى بيعمل مسح شامل كامل بدل بحث سريع، وده أسوأ من الوضع الأصلي (طلبين سريعين) مش أحسن.

**الحل الصحيح (فصل معماري حقيقي بين استخدامين مختلفين تمامًا):**
1. **`/api/conversation`** (الحالة السريعة، المتكررة كل 5 ثواني لكل موظف) — رجعت **بالضبط** لشكلها الأصلي قبل #63: `findAuthorizedConversation` بس، بدون أي حساب تكلفة، بدون أي overhead إضافي خالص.
2. **`/api/projects/{slug}/total-cost`** (endpoint جديد كليًا، منفصل تمامًا) — مسح شامل واحد بس **لكل المشروع دفعة واحدة** (`findAllProjectConversations`، بتفلتر بالمشروع بس مش بالموظف)، بدل مسح شامل منفصل لكل موظف على حدة.
3. **الفرونت إند:** هوك جديد `useProjectTotalCost` منفصل تمامًا عن `useProjectTeamStatus`، بمعدل تحديث **أبطأ بستة أضعاف** (كل 30 ثانية بدل كل 5 ثواني) — بما إن التكلفة الإجمالية مش محتاجة نفس درجة التحديث اللحظي زي حالة التنفيذ.

**تنظيف إضافي:** حذفت `findAllAuthorizedConversations` (النسخة القديمة، بالموظف) بالكامل بعد ما بقت كود ميت غير مستخدم — مبدأ "No dead code" من معايير الجودة.

**النتيجة الكلية (مقارنة الحمل الفعلي):**
| المرحلة | عدد الطلبات كل 5 ثواني | نوع الطلبات |
|---|---|---|
| قبل #63 | 14 | سريعة (توقف مبكر) |
| بعد #63 (خطأ) | 28 | سريعة × 2 |
| بعد #64 (تحسين جزئي، لسه فيه مشكلة) | 14 | **بطيئة (مسح شامل)** — أسوأ فعليًا من الأصل |
| بعد #65 (الحل النهائي) | 14 سريعة + **طلب واحد بطيء كل 30 ثانية للمشروع كله** | الأفضل أداءً من كل المحاولات السابقة |

**تم التحقق فعليًا:** اختبار حي كامل أكّد الاثنين معًا — استجابة `/api/conversation` نظيفة تمامًا (بدون `totalCost`)، واستجابة `/api/projects/.../total-cost` المنفصلة رجّعت `11.25` صح.

**الحالة:** ✅ تم التشخيص العميق والإصلاح المعماري الصحيح والتحقق الحي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء). محتاج تأكيد حي نهائي من المستخدم بعد الـ deploy.

---

### 66. "تأخير ملحوظ" في تحميل الصفحة والمحادثات — نقل بيانات ضخمة غير ضرورية
**السياق:** بعد الـ Restart (حسّن الوضع بشكل كبير)، لسه فيه "تأخير ملحوظ" عند فتح صفحة المشروع وفتح محادثة موظف. المستخدم راجح إن السبب مش من شغلنا — تأكدنا إن ده صحيح جزئيًا (الـ Backend نفسه سريع فعليًا: 0.16-0.18 ثانية لكل طلب حالة/تكلفة، اتأكد بقياس زمني مباشر).

**التشخيص:** فحص حجم استجابة `/api/chat/events` لمحادثة كيرلس الحقيقية (الأضخم في النظام) — **719 كيلوبايت** لمحادثة واحدة! والأهم: `useProjectTeamStatus` (المتكررة كل 5 ثواني لكل الـ14 موظف) كانت بتستدعي هذا الـ endpoint **بالكامل** لكل موظف، بس عشان تستخرج منه معلومة صغيرة جدًا (`work_plan` — خطة العمل)، رغم إنها مش محتاجة قائمة الأحداث الكاملة خالص في هذا السياق.

**الحل:** استخراج المنطق المشترك (جلب كل صفحات الأحداث، تطبيعها) لدالة واحدة (`fetchAllNormalizedEvents`)، واستخدامها في **مسارين منفصلين**:
1. `GET /api/chat/events` (زي ما هو، للاستخدام في شاشة الشات نفسها — محتاجة القائمة الكاملة للعرض).
2. **`GET /api/chat/work-plan`** (جديد كليًا) — يعمل نفس عملية الجلب والاستخراج داخليًا، لكن **بيرجّع بس `work_plan`**، بدون قائمة الأحداث الكاملة في الاستجابة.

**الفرونت إند:** `useProjectTeamStatus` (المسار الساخن، المتكرر) بقى يستخدم `fetchWorkPlan` (الخفيفة) بدل `fetchEvents` (الكاملة).

**تم التحقق فعليًا:** اختبار حي بمحاكاة محادثة كبيرة (100 حدث) — الاستجابة الكاملة كانت 9.5 كيلوبايت، بينما استجابة `work-plan` الجديدة كانت **18 بايت بس** (تقليل يفوق 99%). مع محادثة كيرلس الحقيقية (719 كيلوبايت)، الفرق المتوقع أكبر بمراحل.

**ملاحظة مهمة:** ده تحسين لحجم البيانات المنقولة عبر الشبكة (مهم جدًا لمستخدم على شبكة موبايل)، مش بالضرورة وقت معالجة السيرفر الداخلي (لسه محتاج يجيب كل الأحداث من OpenHands داخليًا لاستخراج work_plan) — تحسين إضافي لوقت السيرفر (زي التخزين المؤقت لمحادثات "منتهية") محتمل مستقبلًا لو استمرت الحاجة.

**الحالة:** ✅ تم التشخيص والتنفيذ والتحقق الحي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء). محتاج تأكيد حي نهائي من المستخدم بعد الـ deploy.

---

### 67. سكرول متداخل مانع الوصول لقسم النشاط + تكبير غير ضروري لكارتي الحالة والتكلفة
**السياق:** في لوحة "تفاصيل الموظف والنشاط"، السكرول كان بيتوقف عند خطة العمل ومش بيوصل لقسم النشاط. كمان طلب المستخدم تصغير كارتي "الحالة" و"التكلفة" أعلى اللوحة، بما إنهم كانوا واخدين مساحة كبيرة نسبيًا.

**السبب الجذري للسكرول (اكتُشف بالفحص الدقيق):** فيه **سكرول متداخل مزدوج** — الحاوية الخارجية كلها (`.employee-insights-content`، تحتوي الملخص + خطة العمل + النشاط) كانت محدودة بـ `max-height: 40vh` ومعاها سكرول خاص بيها، **وجوّاها كمان** قسم النشاط (`.activity-list`) عنده سكرول داخلي منفصل تمامًا (`max-height: 220px`). السكرولين المتداخلين بيتعارضوا، وبما إن الملخص + خطة العمل (لو طويلة) بياخدوا مساحة كبيرة من الـ 40vh المحدودة، قسم النشاط أحيانًا **كان بيتقطع من الظهور تمامًا** قبل حتى ما يوصله السكرول الخارجي.

**الحل:**
1. إلغاء السكرول الداخلي المنفصل من `.activity-list` — بقى جزء طبيعي من تدفق المحتوى، يتحكم فيه السكرول الخارجي الموحّد بس (مش سكرولين متنافسين).
2. زيادة المساحة المتاحة للحاوية الخارجية من `40vh` لـ`65vh` — مساحة كافية لعرض المحتوى كله بشكل طبيعي.
3. تصغير كارتي الحالة والتكلفة — تقليل الـ padding (من `11px 12px` لـ`7px 10px`) وحجم الخط الرئيسي (من `12px` لـ`11px`)، **شاملة النسخة الخاصة بشاشات الموبايل (`@media max-width: 560px`)** — كانت هتطغى على التصغير الأساسي لو اتركت زي ما هي.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 68. صور الموظفين بتفلت برّه الدائرة في شريط الفريق — أثر جانبي من إصلاح شارة الوظيفة
**السياق:** المستخدم لاحظ إن صور الموظفين في شريط فريق المشروع بقت بتظهر برّه الدائرة المفروض تكون جوّاها.

**السبب الجذري:** ده أثر جانبي مباشر من إصلاح #62 (إضافة شارة الوظيفة الصغيرة على زاوية الصورة) — كان لازم نضيف `overflow: visible` على حاوية الصورة (`.project-home .employee-avatar`) عشان الشارة (اللي طالعة عمدًا شوية برّه حدود الدائرة) تبان كاملة. لكن الخاصية دي أثّرت **كمان على الصورة نفسها** — بما إن قص الصورة (Clipping) كان بيعتمد بالكامل على `overflow: hidden` بتاع الحاوية الأب، إلغاؤه خلّى أي جزء من الصورة يفيض عن حدود الدائرة يبان ظاهر.

**الحل:** بدل الاعتماد على قص الحاوية الأب، أضفنا `border-radius: 50%` **مباشرة على الصورة نفسها** (`.project-home .employee-avatar img`) — كده الصورة بتقص نفسها بشكل دائري بغض النظر عن إعداد `overflow` بتاع الحاوية الأب، والشارة تفضل بادية كاملة برّه الحدود زي المطلوب.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 69. صور الموظفين لسه بيضاوية بعد إصلاح #68 — تعارض حقيقي في تحديد أولوية CSS
**السياق:** بعد نشر إصلاح #68، أبلغ المستخدم إن الصور **لسه بتظهر بشكل بيضاوي/طولي، مش دائري خالص**، مع ملاحظة "مافيش حاجة اتغيرت". تحقيق حي مباشر على السيرفر أكّد إن التعديل **وصل فعليًا** (اتأكد بفحص محتوى الملف على الـ VM)، فاستبعدنا مشكلة النشر واتجهنا لتشخيص أعمق.

**السبب الجذري الحقيقي:** فيه **قاعدتين CSS منفصلتين** كانوا بيحددوا أبعاد نفس عنصر الصورة الشخصية بقيم مختلفة تمامًا:
- `.project-home .employee-card .employee-avatar` (3 فئات، أكثر تحديدًا) → `width: 52px; height: 52px;`
- `.project-home .employee-avatar` (فئتين بس) → `width: 42px; height: 42px;` + خاصية `border-radius: 50%;` وباقي التصميم

بما إن القاعدة الأولى **أكثر تحديدًا** (Specificity أعلى)، قيم `52px` كانت هي اللي بتفوز فعليًا للعرض والارتفاع، **لكن** خاصية `border-radius: 50%` (اللي كانت موجودة بس في القاعدة التانية الأقل تحديدًا) كانت بتتلغى تمامًا من هذا التعارض — نتيجته شكل غير متوقع (مش دائرة كاملة).

**الحل:** توحيد **كل** خصائص الحجم والشكل (`width`, `height`, `border-radius`, الحدود، الألوان) في **قاعدة واحدة بس** بأعلى تحديد (3 فئات)، وحذف القاعدة المكررة الأقل تحديدًا بالكامل — إلغاء أي احتمال تعارض مستقبلي. كمان أضفنا `width`/`height` صريحين بالبكسل على الصورة نفسها (بدل الاعتماد على `100%` النسبي)، ضمانًا لمربع مثالي بغض النظر عن أي سلوك Flexbox/Grid معقد في العنصر الأب.

**درس مهم:** التحقق الحي المباشر (فحص محتوى الملف الفعلي على السيرفر) كان أساسيًا لاستبعاد "مشكلة نشر" بسرعة وتوجيه التشخيص للسبب الحقيقي (تعارض CSS)، بدل تكرار نفس الحل الفاشل أو التخمين العشوائي.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 70. سكرول اللوحة الداخلية بيتقطع قبل نهاية المحتوى + تصغير الكارتين مش ملحوظ
**السياق:** بعد إصلاح #67 (السكرول المتداخل)، أبلغ المستخدم إن قسم النشاط لسه مش باين، وحدّد بدقة (بعد سؤال توضيحي): **خطة العمل نفسها بتتقطع قبل ما تخلص** — مش بس قسم النشاط بعدها. كمان أبلغ إن الكارتين (الحالة/التكلفة) "زي ما هما"، رغم التصغير السابق.

**التحقق الأول (استبعاد الأسباب السطحية):** فحص حي مباشر لمحتوى الملف الفعلي على السيرفر أكّد إن كل تعديلات #67 وصلت بدقة تامة (`max-height: 65vh`, إلغاء السكرول الداخلي المنفصل). المستخدم كمان اختبر في وضع تصفح خاص (Incognito) واستبعد الكاش صراحة. يبقى المشكلة حقيقية، مش نشر ولا كاش.

**السبب الجذري الحقيقي (سكرول):** لوحة "تفاصيل الموظف" (`.employee-insights-content`) موجودة **جوّه** حاوية رسائل الشات الخارجية (`.chat-screen > .chat`) — اللي هي نفسها قابلة للسكرول! لاحظنا إن الحاوية الخارجية دي عندها بالفعل `overscroll-behavior: contain` (حل مُطبَّق من قبل لنفس النوع من المشاكل)، **لكن اللوحة الداخلية نفسها معندهاش نفس الخاصية خالص** — فلما المستخدم يسحب بإصبعه جوّه اللوحة، السحب كان "بيتسرب" للطبقة الخارجية قبل ما يوصل فعليًا لآخر محتوى اللوحة الداخلية (تعارض إيماءات لمس شائع بين طبقات سكرول متداخلة على الموبايل).

**الحل (سكرول):** إضافة `overscroll-behavior: contain;` على `.employee-insights-content` — بالظبط نفس النمط المُطبَّق مسبقًا على الحاوية الخارجية لنفس المشكلة.

**السبب الجذري (تصغير الكارتين):** القيم من #67 كانت **صحيحة فعليًا وموجودة على السيرفر** (تأكَّد بالفحص المباشر)، لكن الفرق (4 بكسل تقريبًا) كان **صغير جدًا وغير ملحوظ بصريًا** للمستخدم.

**الحل (تصغير):** تقليل إضافي أوضح — `padding` من `7px 10px` لـ`5px 8px`، الخط الرئيسي من `11px` لـ`10px`، تسمية الحقل من `9px` لـ`8px`، شاملة نسخة الموبايل (`padding: 5px 7px`).

**درس مهم:** التحقق الحي المباشر من محتوى الملف على السيرفر، مع استبعاد الكاش صراحة (Incognito)، كان أساسيًا لتوجيه التشخيص بسرعة نحو السبب الحقيقي (تعارض لمسات سكرول، وتصغير غير كافٍ) بدل تكرار نفس الحلول أو التخمين العشوائي.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 71. خطة العمل مقفولة افتراضيًا — تشخيص عملي لمشكلة السكرول المستمرة
**السياق:** بعد إصلاح #70 (تعارض إيماءات اللمس)، أبلغ المستخدم إن المشكلة **لسه موجودة** رغم التأكد الحي من وصول التعديل للسيرفر (فحص مباشر لمحتوى الملف + آخر كوميت مطابق). حتى إعادة تشغيل حاوية `mkdd-ui` نفسها (لاستبعاد أي حالة HMR/Vite dev عالقة) معملتش فرق.

**الحل العملي المقترح من المستخدم:** بدل الاستمرار في تخمين السبب، خلي **خطة العمل** (اللي ممكن تكون طويلة جدًا — شفنا حالات فيها 5-7 مهام، كل واحدة كارت كامل بعنوان ووصف وحالة) تبقى **مقفولة افتراضيًا** (قابلة للطي/الفتح عند الحاجة، مش مفتوحة بالكامل من البداية) — ده هيقلل المساحة الافتراضية اللي بتاخدها بشكل كبير، وفي نفس الوقت هيأكد لنا هل السبب فعلاً "المساحة" أو حاجة تانية تمامًا.

**التنفيذ:** `WorkPlanPanel.tsx` تحويل من `<section>` عادية لـ `<details>` قابلة للطي (`work-plan-drawer`) — العنوان والنسبة المئوية (`<summary>`) يفضلوا ظاهرين دايمًا (حتى وهي مقفولة)، وقائمة المهام التفصيلية هي بس اللي بتتفتح/تتقفل. الحالة الافتراضية: **مقفولة** (بدون `open` attribute).

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد). محتاج تأكيد حي نهائي من المستخدم — هل قسم النشاط بقى ظاهر دلوقتي (بيأكد إن السبب كان المساحة)، أو لسه المشكلة موجودة (يعني السبب حاجة تانية تمامًا محتاجة تشخيص إضافي).

---

### 72. إعادة تصميم كاملة لبيانات الموظف — من لوحة مدمجة لنافذة منبثقة (تحل مشكلة السكرول من جذورها)
**السياق:** بعد سلسلة إصلاحات متتالية للوحة "تفاصيل الموظف والنشاط" (تعارضات سكرول، تعارضات CSS specificity) كانت المشكلة لسه مستمرة رغم كل محاولات التشخيص والإصلاح. المستخدم اقترح حل جذري مختلف تمامًا: بدل ما اللوحة تكون **مدمجة داخل صفحة الشات** (وبالتالي عرضة لتعارضات سكرول متداخلة مع حاوية الرسائل)، تتحول لـ**نافذة منبثقة (Popup/Modal)** منفصلة تمامًا، يفتحها زرار مخصص جمب زرار "محادثة جديدة".

**التصميم الجديد المُتفَق عليه:**
1. **زرار جديد "بيانات الموظف"** — بنفس ستايل زرار "محادثة جديدة" بالضبط (أيقونة + نص)، **لونه يتغيّر حسب حالة نشاط الموظف الحالية** (نفس الفئات اللونية اللي كانت مستخدمة في النقطة الصغيرة القديمة: أخضر=شغال، أصفر=محتاج موافقة، أحمر=خطأ/متعثر، بنفسجي=اكتمل، رمادي=غير نشط).
2. **الضغط على الزرار يفتح نافذة منبثقة حقيقية** (Bottom Sheet على الموبايل) — **خارج سياق سكرول صفحة الشات تمامًا**، فمشكلة تعارض إيماءات اللمس اللي كانت بتحصل من قبل **بقت مستحيلة بنيويًا** الآن، مش مجرد مُصلَّحة.
3. **حدود النافذة المنبثقة** كمان بتتلوّن بنفس فئة حالة النشاط.
4. **3 تبويبات منفصلة داخل النافذة**: التكلفة | خطة العمل | النشاط — كل تبويب بيعرض محتواه بس عند اختياره، بدل عرض كل حاجة مع بعض في مساحة واحدة محدودة (السبب الأصلي لكل مشاكل المساحة/السكرول السابقة).

**التنفيذ:**
- إعادة كتابة `EmployeeInsightsPanel.tsx` بالكامل — من `<details>` مدمجة لمكوّن يدير حالة فتح/قفل النافذة وحالة التبويب النشط.
- نقل موضع استدعاء المكوّن في `ChatScreen.tsx` — من قسم منفصل تحت شريط الموظف، لجوّه شريط الهوية نفسه، جمب زرار "محادثة جديدة" مباشرة.
- استبدال كامل لقسم CSS "MKDD Employee Insights" (321 سطر قديم) بتصميم جديد أنظف وأصغر حجمًا (204 سطر، حتى بعد إضافة أنماط النافذة المنبثقة والتبويبات) — تم التحقق من عدم بقاء أي إشارة لأي selector قديم في أي مكان تاني بالملف (شامل أي `@media` queries كانت بتشاور عليه) قبل الحذف، ضمانًا لعدم وجود كود ميت.

**باگ حقيقي اتلقى وأتصلح أثناء التنفيذ:** زرار "محادثة جديدة" كان عنده `margin-inline-start: auto` — كانت مصممة أصلًا عشان تدفعه لآخر الصف باعتباره **آخر عنصر فيه**. بعد إضافة زرار "بيانات الموظف" بعده مباشرة، الخاصية دي كانت هتسبب تخطيط غريب (فجوة كبيرة غير متوقعة بين الزرارين، أو زحزحة الزرار الجديد لمكان غير مقصود). الحل: نقل المرونة (`flex: 1 1 auto`) لعنصر معلومات الموظف (الاسم/الدور) بدلًا من ذلك — هو اللي المفروض يتقلّص وينكمش (مع `text-overflow: ellipsis` للاسم الطويل) لو المساحة قلّت، بينما الزرارين يفضلوا بحجمهم الطبيعي الكامل دايمًا.

**الحالة:** ✅ تم التصميم والتنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد، حجم CSS النهائي قلّ فعليًا رغم إضافة ميزات جديدة). محتاج تأكيد حي نهائي من المستخدم.

---

### 73. صورة البار العلوي فالتة من الدائرة + ميزة تحديد موضع الصورة عند الرفع (تحريك + زوم)
**السياق:** المستخدم لاحظ نفس فئة مشكلة "الصورة الفالتة من الدائرة" (#68/#69) لكن في مكان مختلف تمامًا — صورة الموظف في بار الشات العلوي (`.chat-employee-avatar`)، مش شريط الفريق. كمان طلب ميزة جديدة: عند رفع صورة موظف، إمكانية **تحريك الصورة وتكبيرها/تصغيرها (زوم)** لتحديد الجزء المطلوب بالظبط قبل الحفظ، بدل القص التلقائي من المنتصف بدون أي تحكم.

**إصلاح 1 (باگ):** نفس السبب الجذري المُكتشَف قبل كده — الحاوية (`.chat-employee-avatar`) معندهاش `overflow: hidden` احترازي، رغم إن الصورة نفسها عندها `border-radius: 50%`. تم إضافة `overflow: hidden` كطبقة حماية إضافية، بنفس النمط الناجح المُطبَّق سابقًا.

**ميزة 2 (جديدة بالكامل) — محرر موضع الصورة قبل الرفع:**
- مكوّن جديد `AvatarPositioner.tsx` — بعد اختيار ملف الصورة، **مش بيترفع مباشرة** — بدل كده بتظهر نافذة منبثقة فيها معاينة دائرية للصورة، المستخدم يقدر:
  - **يسحب الصورة (Drag)** بإصبعه/الماوس لتحديد الجزء الظاهر (باستخدام Pointer Events API — نفس آلية بتشتغل موحّدة على اللمس والماوس معًا).
  - **يكبّر/يصغّر (Zoom)** عبر شريط تمرير (Slider) — من 1x لـ3x.
- **حساب رياضي دقيق** يضمن إن الصورة تفضل تغطي الدائرة بالكامل دايمًا (بدون أي مساحات فاضية)، بغض النظر عن نسبة أبعاد الصورة الأصلية أو مستوى التكبير/التحريك المختار — تم التحقق من صحة الحسابات بمحاكاة حية (`baseScale` بيغطي المعاينة بالضبط عند التكبير الأدنى، وتم التأكد رياضيًا).
- **عند الحفظ**: يتم رسم نفس التموضع/التكبير المُختار بدقة على عنصر `<canvas>` بدقة أعلى (320×320 بكسل، مستقلة عن حجم المعاينة 220 بكسل)، وتصديره كصورة PNG نهائية — **بنفس التنسيق المتوقع تمامًا من الـ Backend الحالي** (`data:image/png;base64,...`)، فمحتجناش أي تعديل في `avatars.mjs` خالص.
- تم التحقق من عدم تجاوز حد حجم الملف المسموح (5 ميجابايت) — صورة PNG بحجم 320×320 بكسل أصغر بمراحل من هذا الحد.

**الحالة:** ✅ تم التشخيص والتصميم والتنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد، محاكاة رياضية حية للتحقق من صحة حسابات التموضع/التصدير).

---

### 74. "سير المشروع" مش بيجيب بيانات حقيقية — الفجوة الحقيقية: مفيش موظف عنده تعليمات لتحديثه
**السياق:** المستخدم أبلغ إن قسم "سير المشروع" (البوابات، المراجعات الإلزامية، العوائق، الملاحظات) في صفحة المشروع **مش بيجيب أي بيانات حقيقية خالص** — رغم إن الموظفين (كيرلس خصوصًا) بيشتغلوا فعليًا على المشروع منذ فترة طويلة.

**التشخيص (بحث دقيق، مش افتراض):**
1. فحص `useProjectWorkflow.ts` أكّد إنه بيستدعي API حقيقي (`fetchWorkflow`)، مش قيمة ثابتة.
2. فحص `workflow-state.mjs` (الـ Backend) أكّد وجود نظام كامل وحقيقي — تخزين دائم، endpoints فعلية لكل التحديثات (`approve-gate`, `blockers`, `reviews`, `findings`)، مع قواعد تحقق صارمة مبنية فعليًا (منع اعتماد بوابة لو فيه عوائق مفتوحة أو ملاحظات غير مؤكَّدة، منع نفس المراجع من إصلاح ملاحظته، إلزام نفس المراجع الأصلي بالتحقق من الإصلاح).
3. **الاكتشاف الحاسم:** بحث في كل ملفات الموظفين الـ14 + الأوركستريتور — **مفيش ملف واحد بيذكر إزاي أو إمتى يُستخدَم الـ API ده خالص!** يعني النظام الخلفي **حقيقي 100% ومصمَّم بدقة، لكن مفيش أي موظف عنده توجيه لاستخدامه فعليًا** — فالحالة فضلت على "الافتراضي" (Gate 1 قيد التنفيذ، كل شيء تاني Pending) طول الوقت، بغض النظر عن التقدم الحقيقي الفعلي.

**الحل:** قسم جديد شامل في `AGENTS.md` (§6 "Updating Persisted Workflow State") — يوضح بدقة **مين المسؤول عن كل تحديث ومتى بالظبط**، مع **الأمر الفعلي (curl) المطلوب** بأسماء الحقول الصحيحة المؤكَّدة مباشرة من كود الـ Backend (مش تخمين):
- **اعتماد بوابة:** الدور اللي قدّمها (باجوش/مريم/شنودة/أبانوب) — **لحظة** ما المالك يوافق صراحة.
- **إكمال مراجعة إلزامية:** كل من فادي/جورج/فيرينا/ميخائيل — **لحظة** ما مراجعته تنجح فعليًا، مش قبل كده.
- **تسجيل عائق:** **أي موظف** لحظة ما يكتشفه.
- **تسجيل ملاحظة مراجعة:** **المراجعين الأربعة بس** (مش أي موظف تاني) — مع التأكيد إن الـ API نفسه بيرفض أي محاولة يصلّح فيها المراجع ملاحظته، أو يتحقق منها مراجع مختلف عن اللي سجّلها.

**تحسينات الشكل (الجزء الثاني من الطلب):**
- إضافة أيقونة مناسبة لكل بوابة (متطلبات، واجهة/تجربة مستخدم، معمارية، إنتاج) في `WorkflowStepper.tsx`.
- إضافة أيقونة لكل نوع مراجعة (QA، اختبارات آلية، مراجعة كود، مراجعة أمان) في `ProjectHomeScreen.tsx` — بإعادة استخدام نفس الأيقونات المُعرَّفة بالفعل لكل موظف (`ROLE_ICONS`) عشان الاتساق البصري.
- إضافة أيقونات لكارتي "عوائق مفتوحة" و"ملاحظات غير مؤكَّدة".
- **باگ إضافي اتلقى ومتصلح أثناء التنفيذ:** حالة المراجعة (`pending`/`complete`) كانت بتظهر **بالإنجليزي الخام دايمًا** حتى في الواجهة العربية (زي "Pending" الظاهرة في الصورة الأصلية اللي بعتها المستخدم) — أضفنا دالة ترجمة `getReviewStatusLabel` بدل عرض القيمة الخام مباشرة.

**باگ اتلقى وأتصلح أثناء تعديل `AGENTS.md` نفسه:** أثناء إضافة القسم الجديد، حصل خطأ بشري في الاستبدال أدى لتكرار عنوان "Approval Gates" ومحو عنوان "Mandatory Quality Bar Before Release" (المحتوى نفسه فضل سليم، بس العنوان اتلخبط). تم اكتشافه فورًا بفحص الترقيم بعد كل تعديل (نفس المنهجية المُتبَعة دايمًا)، وتصحيحه بالكامل — بما فيه تصحيح مرجع داخلي (Cross-reference) كان بيشاور على رقم قسم غلط.

**الحالة:** ✅ تم البحث والتشخيص والإصلاح والتحقق الحي الكامل (قراءة حية لكل الـ14 موظف بعد تعديل AGENTS.md، `npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء). محتاج تشغيل `bootstrap-employees.mjs` تاني على الـ VM عشان الموظفين ياخدوا التعليمات الجديدة، وتأكيد حي من المستخدم إن الأيقونات والترجمة ظاهرة صح.

---

### 75. إعادة تصميم صفحة المشروع الرئيسية — أسلوب Dashboard منظم، بيانات حقيقية 100%
**السياق:** المستخدم طلب إعادة تصميم شكل صفحة المشروع (بعد شريط الفريق) — كان "شكله وحش جدًا، مش منظم، وكبير جدًا". طلب صراحة تبنّي **أسلوب** تصميم مرجعي (Dashboard بكروت مصغّرة منظمة)، لكن بـ**بيانات حقيقية فعليًا من نظامنا، مش موك خالص**.

**تحليل النطاق (قبل التنفيذ):** فحصت التصميم المرجعي وقسّمته لأجزاء "جاهزة" (بياناتها موجودة بالفعل عندنا) وأجزاء "محتاجة تجميع إضافي" (خطة العمل والنشاط المجمّعين من كل الموظفين — مؤجَّلين لمرحلة تالية بالاتفاق مع المستخدم).

**التنفيذ (الجزء الجاهز):**
- **حالة الفريق المجمّعة (Work Now):** عدادات حقيقية (يعملون الآن/بانتظار رد/خاملون) — محسوبة من بيانات `executionStatus` الحقيقية الموجودة بالفعل لكل موظف، بدون أي endpoint جديد.
- **المراجعات الإلزامية:** إعادة تصميم لقائمة أضغط (بدل شبكة كبيرة)، مع أيقونة لكل نوع مراجعة وترجمة صحيحة لحالتها.
- **العوائق:** كارت تفصيلي حقيقي (مش عداد بس) — يعرض العنوان الفعلي، مين سجّله، ومتى (بصيغة نسبية زي "منذ ساعتين") — كل البيانات دي كانت موجودة بالفعل في `WorkflowState` لكن مش مُستخدَمة في العرض قبل كده.
- **الملاحظات:** نفس الأسلوب — كارت تفصيلي بالعنوان، المراجع المسؤول، وحالتها المترجَمة (مفتوحة/بانتظار التحقق/مُتحقَّق منها).

**أدوات جديدة مبنية لدعم التصميم:**
- `formatRelativeTime.ts` — دالة وقت نسبي جديدة (مش موجودة قبل كده، الموجود كان بس وقت مطلق للرسائل).
- `getFindingStatusLabel` في `workflowLabels.ts` — ترجمة حالة الملاحظات (كانت بتظهر بالإنجليزي الخام قبل كده، بما إن مفيش دالة ترجمة لها أصلًا).

**باگ حقيقي اتلقى ومتصلح أثناء التنفيذ:** أثناء إضافة `getFindingStatusLabel`، حصل استبدال غير دقيق أدى لحذف الدالة الموجودة بالفعل `getReviewStatusLabel` بالكامل (اتفقد الكود، مش بس العنوان زي المرة اللي فاتت) — تم اكتشافه فورًا بفحص `tsc -b` (رجّع أخطاء توضح المشكلة بدقة)، وتم إصلاحه بإعادة الدالة الأصلية كاملة مع تصحيح التعليقات المتضاربة.

**تنظيف:** حذف القسم القديم بالكامل من CSS (`workflow-review-grid`, `workflow-review-card`, `workflow-summary-row`, `workflow-subheading`) بعد التأكد الصريح (بحث نصي) من عدم استخدام أي منها في الـ JSX الجديد — منعًا لأي كود ميت.

**تم التحقق فعليًا:** محاكاة حية لحسابات تجميع حالة الفريق أكّدت صحة الأرقام دايمًا (المجموع = عدد الموظفين بالضبط، بدون أي احتمال رقم سالب).

**الحالة:** ✅ تم التصميم والتنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد). **مؤجَّل بالاتفاق:** خطة العمل والنشاط المجمّعين من كل الموظفين (يحتاجوا شغل تجميع backend إضافي) — مرحلة تالية.

---

### 76. تكبير الأيقونات وإضافة ألوان مميزة في صفحة المشروع الجديدة
**السياق:** بعد الموافقة على التصميم الجديد لصفحة المشروع (#75)، طلب المستخدم تحسين بسيط: الأيقونات محتاجة تكبير، ولون مميز لكل واحدة يدل على معناها.

**التنفيذ:**
- **الأيقونات الرئيسية (عناوين الكروت):** حجم أكبر + لون مميز يعكس معنى كل كارت فعليًا:
  - "حالة الفريق الآن" → أزرق معلوماتي (`--mkdd-info`)
  - "المراجعات الإلزامية" → بنفسجي (لون التمييز الأساسي `--mkdd-accent`)
  - "العوائق" → أحمر (`--mkdd-danger`) — يعكس إنها مشكلة حقيقية تحتاج حل
  - "الملاحظات" → أصفر تحذيري (`--mkdd-warning`) — يعكس إنها تحتاج انتباه
- **أيقونات قوائم المراجعات الفرعية** (لكل نوع مراجعة داخل الكارت) — حجم أكبر، لون موحّد مميز.
- **أيقونات العوائق/الملاحظات الفرعية** (مفك/ساعة رملية بجانب حالة الملاحظة) — حجم أكبر.
- **أيقونات بوابات المشروع** (WorkflowStepper) — حجم أكبر (لونها ثابت مرتبط بحالة البوابة نفسها — معتمدة/قيد التنفيذ/معلقة — كنظام موجود بالفعل ومنطقي، فلم نضف لون ثابت إضافي لتجنب أي تعارض بصري).

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 77. أيقونات بوابات سير المشروع — تكبير وألوان مميزة، مع اكتشاف تعارض ألوان حقيقي
**السياق:** المستخدم طلب توسيع نفس تحسين الأيقونات (#76) ليشمل قسم "سير المشروع" (WorkflowStepper) — البوابات الأربعة (متطلبات، واجهة/تجربة مستخدم، معمارية، إنتاج).

**اكتشاف مهم أثناء التنفيذ:** أول محاولة استخدمت `--mkdd-info` للمتطلبات، لكن فحص متغيرات الوضع الداكن (المُستخدَم فعليًا في التطبيق) كشف إن `--mkdd-info` و`--mkdd-accent` **نفس القيمة بالضبط** (`#325be4`) — يعني لونين "مختلفين" ظاهريًا كانوا هيبانوا **متطابقين تمامًا** على الشاشة الفعلية! تم تصحيح التوزيع لاستخدام الدرجات الثلاث المميزة فعليًا من عائلة الأزرق (`accent-deep` الغامق، `accent` المتوسط، `accent-soft` الفاتح) للبوابات الثلاث الأولى، مع `success` (أخضر) للإنتاج.

**باگ تقني إضافي اتلقى ومتصلح:** محاولة تمرير `className` لمكوّن أيقونة مُعرَّف بنوع `React.ComponentType` العام رجّعت خطأ TypeScript حقيقي (`Property 'className' does not exist`) — تم تحديد النوع بدقة أكبر (`React.ComponentType<{ className?: string }>`) بدل الاعتماد على النوع العام غير الكافي.

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف بعد تصحيح النوع، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 78. أيقونات بوابات سير المشروع بتقف في مكان غريب مع الأسماء الطويلة
**السياق:** بعد تكبير أيقونات البوابات وتلوينها (#77)، أبلغ المستخدم إن كارت "سير المشروع" لسه شكله مش مظبط. بعد سؤال توضيحي لتحديد الكارت المقصود بدقة، تبيّن المشكلة تحديدًا في بوابة "واجهة وتجربة المستخدم" (الاسم الأطول من بين الأربعة).

**السبب الجذري:** الأيقونة والاسم كانوا معروضين **جنب بعض في نفس السطر** (`flex-direction: row`). بما إن "واجهة وتجربة المستخدم" اسم طويل بيتلف تلقائيًا لـ3 أسطر على عرض العمود الضيق (4 بوابات في صف واحد على الموبايل)، الأيقونة كانت بتقف متمركزة بصريًا مع **السطر الأول بس** من النص المُتعدد الأسطر، مش متوسطة مع الكتلة بالكامل — فبانت في مكان غير متوقع ومنفصلة بصريًا عن باقي الاسم.

**الحل:** تغيير التخطيط من "جنب بعض" (Row) لـ"فوق بعض" (Column) — الأيقونة تظهر **فوق** اسم البوابة دايمًا، بغض النظر عن طول الاسم أو عدد الأسطر اللي بيتلف لها. هذا يضمن اتساق بصري كامل بين البوابات الأربعة، وحل جذري (مش ترقيع) لمشكلة المحاذاة مع النصوص متعددة الأسطر.

**الحالة:** ✅ تم التشخيص والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 79. دوائر ومراحل سير المشروع — إصلاح شامل (حجم، خط، توهج، وتداخل حقيقي)
**السياق:** بعد إصلاح محاذاة الأيقونات (#78)، أبلغ المستخدم إن دوائر المراحل (1-2-3-4) والخط الواصل بينهم "مش كويسة". سؤال توضيحي كشف إن **كل المشاكل الأربعة المحتملة صحيحة مع بعض**: الدوائر صغيرة، الخط رفيع وباهت، التوهج حوالين الدائرة الحالية مبالغ فيه، **وكمان مشكلة إضافية اكتشفها المستخدم بنفسه** — الخط بيدخل فعليًا جوّه الدائرة الأولى.

**السبب الجذري للتداخل (الأهم):** `.workflow-step-line` كانت عندها `margin-inline: -4px` (قيمة **سالبة**) — دي كانت بتسحب الخط ليمتد **جوّه** مساحة الدائرة المجاورة من الجهتين، بدل ما يتوقف عند حافتها بالظبط. هذا التصرف كان مقصودًا في الأصل كترقيعة لمشكلة تباعد، لكنه كان بيسبب تداخل بصري حقيقي وواضح.

**الحل الشامل:**
1. تكبير الدوائر من `36px` لـ`48px`.
2. تقوية الخط: من `2px` رفيع بلون باهت (`border-subtle`) لـ`3px` بلون أوضح (`border-strong`)، مع حواف دائرية (`border-radius: 999px`).
3. تبسيط التوهج: تقليل حجم الهالة حوالين الدائرة الحالية من `4px` لـ`3px` — أصبحت نسبيًا أصغر وأقل مبالغة بالنسبة للحجم الجديد الأكبر للدائرة.
4. **إزالة الـ `margin-inline: -4px` السلبية بالكامل** — الإصلاح الجذري لمشكلة التداخل، بدل أي ترقيع إضافي.
5. تنظيف إضافي: إزالة قاعدة `margin-inline-start: 18px` على أول عنصر (كانت ترقيعة تعويضية مرتبطة بنفس المشكلة الأصلية، بقت غير ضرورية بعد الإصلاح الجذري).

**درس مهم أثناء التنفيذ:** حصلت غلطة مؤقتة (تغيير طريقة تمركز الدائرة من `margin: 0 auto` الصحيحة لـ`margin-inline-start: auto` المربكة) أثناء محاولة إصلاح أولى — تم اكتشافها وتصحيحها فورًا بمراجعة المنطق بعمق قبل الانتقال للخطوة التالية، بدل الاستمرار على أساس غير سليم.

**الحالة:** ✅ تم التشخيص الشامل والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 80. تصحيح خطأ توثيقي حقيقي — إصلاحان (#33، #34) موثّقان مرتين
**السياق:** المستخدم سأل تأكيدًا صريحًا "أنت موثّق كل شيء من أول ما بدأنا لحد دلوقتي؟" — بدل الإجابة بافتراض "أيوه" بدون تحقق فعلي، تم فحص `BUGS_AND_FIXES.md` و`CHANGELOG.md` مباشرة للتأكد.

**النتيجة:** لقيت فعليًا **خطأ توثيقي حقيقي** — إصلاحان (#33 "اعتماد نظام الألوان الرسمي" و#34 "شريط الفريق كبير جدًا") كانا **موثّقين مرتين بالكامل**، بصياغة مختلفة قليلًا لكل نسخة، على الأغلب نتيجة تكرار غير مقصود في جلسة سابقة قبل هذه الجلسة.

**الحل:** حذف النسخة المكررة الأولى (الأقل تفصيلًا شوية)، والاحتفاظ بالنسخة الثانية (الأشمل، وفيها إشارة صريحة لرقم المشكلة السببية #32 كمرجع). تم التحقق برمجيًا من عدم وجود أي تكرار آخر في كامل الملف (81 → 79 إدخال، مطابق تمامًا لتسلسل 1-79 بدون أي فجوة أو تكرار)، وفحص `CHANGELOG.md` كمان للتأكد من خلوه من نفس المشكلة (نظيف).

**الحالة:** ✅ تم الاكتشاف والتصحيح الفوري بمجرد السؤال — بدل الإجابة على الثقة بدون تحقق.

---

### 81. إعادة تصميم صفحة المشاريع الرئيسية — كارت مفصّل بنفس أسلوب الصورة المرجعية، ببيانات حقيقية
**السياق:** المستخدم شارك صورة مرجعية لتصميم كارت مشروع (أيقونة، اسم، نوع + آخر تحديث، شارة ملونة للمرحلة الحالية، صف أفاتار الفريق) وطلب تبنّي نفس الأسلوب لصفحة المشاريع الرئيسية.

**تحقق أولي (قبل التنفيذ):** فحصت استجابة `/api/projects` الفعلية على staging مباشرة — تأكدت إنها **مفيهاش خالص** بيانات "آخر تحديث" أو "المرحلة الحالية"، بس `id/name/path/color`. بدل التخمين، فحصت أيضًا الحقول الخام المتاحة فعليًا في نتيجة بحث المحادثات (`created_at`, `updated_at` موجودين فعليًا) — فتأكدت إن البيانتين المطلوبتين ممكن حسابهم من مصادر حقيقية موجودة بالفعل عندنا، مش محتاجين اختلاق.

**التنفيذ (Backend):** تعديل `handleProjects` (في `directory.mjs`) — لكل مشروع في القائمة، بالتوازي:
- `currentGate`: من `getWorkflowState(project.path)` — نفس المصدر المُستخدَم في لوحة سير المشروع.
- `lastActivityAt`: **أحدث** `updated_at` بين كل المحادثات الحقيقية المرتبطة بالمشروع (عبر `findAllProjectConversations` الموجودة بالفعل من العمل السابق على التكلفة الإجمالية) — تم التحقق حيًا إن الدالة بتختار فعليًا الأحدث بدقة، مش أي قيمة عشوائية.

**تصحيح بنية حقيقي أثناء التنفيذ:** محاولة أولى لإضافة نوع `WorkflowGateName` لملف `types/index.ts` كانت هتسبب **استيراد دائري حقيقي** (`client.ts` بيستورد من `types/index.ts` أصلًا). تم الحل بنقل التعريف الفعلي لـ`types/index.ts` (مكانه الصحيح منطقيًا)، وتعديل `client.ts` ليستورده من هناك (بدل تعريفه من جديد) **مع** إعادة تصديره أيضًا — للحفاظ على توافق كل الملفات الأخرى (`WorkflowStepper.tsx`, `workflowLabels.ts`) اللي كانت بتستورده من `client.ts` مباشرة، بدون أي تعديل إضافي فيهم.

**التنفيذ (Frontend):** إعادة كتابة `ProjectsScreen.tsx` بالكامل — كارت جديد (`project-card-v2`) بأيقونة ملوّنة بلون المشروع، الاسم، "مساحة عمل • آخر تحديث [وقت نسبي]" (بإعادة استخدام `formatRelativeTime`)، شارة المرحلة الملوّنة (بإعادة استخدام `getGateLabel`)، وصف أفاتار حقيقي لأول 3 موظفين (حسب ترتيبهم الثابت) + "+11" للباقي — بيانات صادقة تعكس إن كل الـ14 موظف فعلًا متاحين لكل مشروع بالتساوي (مش فريق مخصص مُختلَق لكل مشروع).

**الحالة:** ✅ تم التحقق الأولي، التصميم، التنفيذ، والتحقق الحي الكامل (اختبار حي مباشر أكّد صحة حساب `currentGate` و`lastActivityAt`، `npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 82. دوائر المراحل لسه مش متمركزة صح فوق الأسماء والأيقونات — السبب الجذري الحقيقي
**السياق:** بعد الإصلاح الشامل السابق (#79)، أبلغ المستخدم إن دوائر المراحل لسه مش متمركزة صح فوق أسمائها وأيقوناتها.

**السبب الجذري الحقيقي (مختلف عن كل ما اتفحص قبل كده):** الدائرة كانت بتتمركز عبر `margin: 0 auto`، **لكن** في نفس الصف كان فيه عنصر تاني (الخط الواصل) بخاصية `flex: 1 1 auto`. الاتنين — الهوامش التلقائية على الدائرة، والتمدد التلقائي على الخط — بيتنافسوا على نفس المساحة الفارغة في الصف بطريقة **غير مضمونة النتيجة بدقة** في مواصفات CSS (Flexbox)، فالدائرة كانت بتتمركز في نقطة قريبة من المنتصف لكن مش بالضبط في نفس نقطة تمركز الاسم/الأيقونة تحتها (اللي بيتمركزوا بطريقة مختلفة تمامًا، عبر `align-items: center` على عمود الـ Grid).

**الحل القاطع:** فصل منطق تمركز الدائرة عن منطق الخط تمامًا:
1. **الدائرة** تتمركز عبر `justify-content: center` على الصف نفسه — **بدون أي عنصر آخر ينافسها على المساحة**، فتضمن تمركز دقيق 100% في نفس نقطة وسط العمود اللي بيتمركز فيها الاسم والأيقونة تحتها بالضبط.
2. **الخط** أصبح عنصر `position: absolute` مستقل تمامًا — بيمتد من حافة الدائرة (`calc(50% + 24px)`، نصف قطر الدائرة) لحافة العمود، بدون أي تأثير على تمركز الدائرة خالص.

**الحالة:** ✅ تم التشخيص الدقيق (تحديد السبب الحقيقي: تنافس Flexbox غير مضمون النتيجة) والإصلاح القاطع (حل غير معتمد على سلوك غامض) والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 83. خط غير متصل + ألوان أيقونات متقاربة + دوائر مش مطابقة لون الأيقونة
**السياق:** بعد الإصلاح القاطع لتمركز الدوائر (#82)، المستخدم لاحظ 3 مشاكل: (1) الخط الواصل بين المراحل مش متصل بالكامل، (2) ألوان أيقونات المراحل متقاربة جدًا (مش ألوان مختلفة فعليًا)، (3) الدوائر لازم تكون بنفس لون الأيقونة بتاعتها.

**اعتراف صريح (السؤال 1):** الخط غير المتصل **كان أثر جانبي مش مقصود** من إصلاح #82 — لما فصلت الخط عن الدائرة (`position: absolute` بيتوقف عند حافة العمود بالظبط)، سيبته يتوقف قبل الفجوة بين الأعمدة (`gap: 8px`) مباشرة، فبيبان متقطع عند كل نقطة اتصال.

**السبب الحقيقي لتقارب الألوان (السؤال 2):** الألوان الثلاثة المُستخدَمة (`accent-deep`, `accent`, `accent-soft`) كانت **درجات مختلفة من نفس اللون الأزرق/البنفسجي**، مش ألوان مختلفة فعليًا — تمييز بصري ضعيف جدًا.

**قرار تصميمي (السؤال 3، بعد سؤال توضيحي):** المستخدم أكّد إن لون الدائرة يطابق لون الأيقونة **بس للمرحلة المنتظرة** (الرمادية) — المراحل المعتمدة (أخضر) والحالية (بنفسجي) تفضل بألوانها الحالية اللي بتدل على الحالة نفسها، مش نوع المرحلة.

**الحل الشامل:**
1. **الخط:** امتداد إضافي بمقدار الفجوة نفسها (`inset-inline-end: -8px`) — يعبر الفجوة بالكامل ويصل لحافة العمود التالي، فيبان متصل تمامًا.
2. **4 ألوان مميزة فعليًا** (أزرق، أحمر، برتقالي، أخضر — بدل 3 درجات من نفس اللون)، عبر متغيّر CSS واحد مشترك (`--gate-color`) يتحدد مرة واحدة لكل مرحلة، وتقرأه **كل من** الأيقونة **والدائرة المنتظرة** — هذا يضمن التطابق التام بينهم دايمًا (مش قيمتين منفصلتين ممكن يتفرقوا لاحقًا بالغلط)، بدل نظام الكلاسات المنفصل القديم.
3. **الدائرة المنتظرة** تستخدم نفس المتغيّر (`border-color: var(--gate-color)`) — بينما المعتمدة/الحالية يفضلوا بقواعدهم الخاصة (أخضر/بنفسجي، دلالة حالة) زي ما اتفقنا.

**الحالة:** ✅ تم الإصلاح الكامل بناءً على تشخيص صريح وقرار تصميمي مؤكَّد من المستخدم، والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 84. الخط لسه غير متصل بعد #83 — إعادة هيكلة جذرية بخط تدرّجي واحد + توهج نابض
**السياق:** المستخدم أكّد إن الخط **لسه** مش متصل رغم إصلاح #83، وطلب ميزتين جديدتين: الخط يكون بلونين (نص بلون كل مرحلة مجاورة)، والمرحلة الحالية تتوهج بنبضة (زي حالة الموظف النشط).

**السبب الحقيقي لفشل #83 (اعتراف صريح):** الحل السابق مدّ الخط بمقدار الفجوة بين الأعمدة (8px) بس — لكن عشان يوصل فعليًا لمركز الدائرة **التالية** (في عمود Grid منفصل تمامًا)، محتاج يعبر مسافة حقيقية أكبر بكتير (تقريبًا نص عرض العمود التالي بالكامل ناقص نصف قطر دائرته)، مش 8px بس. هذا قصور حسابي حقيقي، مش مجرد تفصيلة overflow.

**إعادة الهيكلة الجذرية:** بدل محاولات الترقيع المتكررة (فشلت مرتين) لتمديد عناصر منفصلة عبر حدود أعمدة متعددة (تصميم هش من الأساس)، تم بناء **خط واحد متصل شامل**:
- عنصر `.workflow-stepper-connector` واحد بس، خارج كل الأعمدة الفردية تمامًا، يمتد من مركز أول دائرة (12.5% من عرض الشريط) لمركز آخر دائرة (87.5%).
- خلفيته `linear-gradient` محسوبة رياضيًا بدقة في TypeScript — كل قطعة بين دائرتين متجاورتين مقسومة لنصفين بلوني المرحلتين، بنقاط تحوّل حادة (Hard stops) عند المنتصف بالضبط.
- **تم التحقق حيًا** بمحاكاة رياضية كاملة للدالة — تأكدت النقاط والألوان كلها صحيحة 100% (أول نقطة 0%، آخر نقطة 100%، كل قطعة نصفين بالضبط) قبل البناء.

**باگ RTL حقيقي اتلقى ومتصلح أثناء التنفيذ:** `linear-gradient()` بيستخدم اتجاهات فيزيائية ثابتة (`to right`/`to left`)، **مش بتتأثر تلقائيًا** باتجاه الصفحة RTL (خلافًا لخصائص الـ Layout العادية زي `margin-inline-start`). بما إن أول عنصر في مصفوفة البوابات (المتطلبات) بيظهر بصريًا **أقصى اليمين** في هذا التخطيط RTL، استخدام `to right` كان هيعكس الألوان تمامًا (أول لون يظهر على الشمال بدل اليمين). تم التصحيح لـ`to left` لمطابقة الموضع البصري الحقيقي.

**التوهج النابض:** أُعيد استخدام نفس نمط الحركة (`@keyframes`) المُستخدَم بالفعل لبطاقة الموظف النشط (`mkdd-employee-card-running`) — نبضة كل 1.8 ثانية تقوّي وتخفف شدة التوهج، مُطبَّقة على دائرة المرحلة الحالية بلونها البنفسجي (Accent).

**الحالة:** ✅ تم التشخيص الصريح للفشل السابق، إعادة الهيكلة الجذرية، التحقق الرياضي الحي، وإصلاح باگ RTL حقيقي — كل ده مع التحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد، مفيش أي إشارة متبقية للـ selectors القديمة المُزالة).

---

### 85. إعادة تصميم قسم "ملفات المشروع" — كروت ملوّنة بأيقونة مميزة لكل نوع ملف
**السياق:** بعد الاستحسان الكامل لتصميم "سير المشروع" الجديد، المستخدم طلب نفس المعالجة لقسم "ملفات المشروع" — كان قائمة نصية بسيطة (أيقونة صغيرة رمادية + اسم بس)، مش منظم وبدون تمييز بصري بين أنواع الملفات المختلفة.

**التنفيذ:**
- إعادة تصميم `fileIcon()` لترجّع **لون مخصص لكل نوع** بجانب الأيقونة، بدل أيقونة رمادية موحّدة للجميع:
  - **مجلدات** → برتقالي/كهرماني (`--mkdd-warning`) — اللون التقليدي المتعارف عليه للمجلدات بصريًا.
  - **صور** (`png`, `jpg`, `svg`, إلخ) → وردي/أحمر (`--mkdd-danger`).
  - **كود** (`html`, `css`, `js`, `ts`, `json`, إلخ) → بنفسجي (`--mkdd-accent`).
  - **نصوص** (`md`, `txt`) → أخضر (`--mkdd-success`).
  - **ملفات عامة** (أي امتداد غير متعرف عليه) → رمادي محايد.
- تحويل القائمة النصية البسيطة لكروت مستقلة (زي بقية أقسام الصفحة) — حدود، خلفية، وأيقونة داخل صندوق ملوّن بخلفية شفافة خفيفة من نفس لون النوع (نفس أسلوب `color-mix(currentColor)` المُستخدَم بالفعل في كارتات المشاريع وسير العمل، لضمان اتساق بصري كامل عبر التطبيق).
- الحفاظ الكامل على كل الوظائف الأصلية (روابط المعاينة للملفات، المسافة البادئة للمجلدات المتداخلة حسب العمق).

**الحالة:** ✅ تم التصميم والتنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد، تأكد عدم وجود أي إشارة متبقية للـ selectors القديمة).

---

### 86. مجلدات ملفات المشروع قابلة للطي/الفتح — عرض محتوى المجلد عند الدوس عليه
**السياق:** بعد تصميم الكروت الملوّنة لملفات المشروع (#85)، طلب المستخدم تطوير الوظيفة — كل مجلد يبقى كارت قابل للدوس، ولما تدوس عليه يعرض محتواه (بدل عرض شجرة الملفات كاملة مسطّحة طول الوقت).

**تحليل البيانات المتاحة:** الـ Backend بيرجّع أصلًا قائمة مسطّحة (Flat) لكل الملفات بمساراتها الكاملة النسبية (`path`) ونوعها (`file`/`directory`) — مفيش حاجة إضافية مطلوبة من الـ Backend، الميزة قابلة للبناء بالكامل من الفرونت إند بمعالجة نفس البيانات الموجودة.

**التنفيذ:**
- `expandedFolders` — حالة جديدة (`Set<string>`) تتبع مسارات المجلدات المفتوحة حاليًا، تبدأ فاضية (كل المجلدات مقفولة افتراضيًا).
- `parentOf(path)` — دالة حساب "الأب المباشر" لأي مسار (أو `null` لو عنصر جذري).
- **الفلترة:** أي عنصر بيظهر **بس** لو أبوه المباشر موجود في `expandedFolders`، أو لو هو نفسه عنصر جذري — هذا يضمن الطي المتدرج (فتح مجلد أب لا يعني فتح المجلدات الفرعية جواه تلقائيًا، وقفل مجلد أب بيخفي كل محتواه بغض النظر عن حالة أي مجلد فرعي جواه).
- المجلدات بقت `<button>` قابلة للدوس (بدل نص ثابت)، مع أيقونة سهم (Chevron) بتدور 180 درجة لما المجلد يبقى مفتوح — إشارة بصرية واضحة لحالة الطي/الفتح.

**تم التحقق فعليًا:** محاكاة حية بـ3 حالات اختبار (لا شيء مفتوح، مستوى واحد مفتوح، مستويين مفتوحين) أكّدت صحة منطق الفلترة بدقة 100% في كل حالة، بما فيها التأكد من إن الطي المتدرج (Progressive disclosure) بيشتغل صح على أعماق متعددة.

**الحالة:** ✅ تم التصميم والتنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد، محاكاة منطقية حية للفلترة).

---

### 87. فحص جاهزية إعادة التنصيب على VM جديد — لقيت فجوة توثيقية حقيقية وصلّحتها
**السياق:** المستخدم سأل سؤالًا مباشرًا ومهمًا جدًا: "هل بعد كل التعديلات دي ظبطت السكريبت... عشان لو حبينا نعملها على VM جديد منعملش كل ده من الأول؟" — بدل الإجابة بافتراض "أيوه"، تم فحص فعلي شامل لـ `setup.sh` و`bootstrap-employees.mjs` و`compose.yml`.

**النتيجة (فحص صادق كامل):**
- ✅ **مؤتمَت بالكامل:** بناء الحاويات، إنشاء المجلدات والصلاحيات، إنشاء كل الـ14 موظف بهوياتهم الكاملة **وإعداد الـ Condenser الصحيح**، نظام البورتات الحية (4001-4020)، النشر التلقائي — كل ده موجود ومحفوظ في الكود المصدري، هيشتغل صح على أي VM جديد بدون تدخل يدوي.
- ⚠️ **خطوة يدوية واحدة موثّقة بوضوح مسبقًا في السكريبت نفسه:** إنشاء بروفايل LLM من واجهة OpenHands — السكريبت **يرفض عمدًا** يخمّن ده (اشتراك/مفتاح API حساس)، وده قرار تصميمي صحيح موجود بالفعل.
- ❌ **فجوة حقيقية غير موثّقة (اكتُشفت الآن):** إعداد `Max Input Tokens: 200000` **على بروفايل LLM نفسه** — كان أساسي جدًا لحل مشكلة تجاوز حد السياق (راجع #59/#60)، لكن كان موثّق **بس** كسجل تاريخي منسي في `BUGS_AND_FIXES.md`، **مش في `setup.sh`** اللي هو الملف الفعلي اللي أي حد بيقرأه وقت تنصيب VM جديد.

**الحل:** أضفت تذكير واضح ومفصَّل في **مكانين** بـ`setup.sh`:
1. رسالة التوجيه لما `MKDD_BOOTSTRAP_LLM_PROFILE_REF` مش متظبطة (الحالة الأكثر شيوعًا لـ VM جديد تمامًا).
2. الرسالة الختامية اللي بتظهر **دايمًا** بغض النظر عن حالة الـ bootstrap — عشان حتى لو كان فيه بروفايل LLM موجود بالفعل ومتظبطة المتغيّرة (فالـ bootstrap شغال بنجاح)، لسه المستخدم يشوف التذكير بضبط الحد الأقصى للتوكنز.

**الحالة:** ✅ تم الفحص الشامل الصادق والتصحيح الفوري لفجوة توثيقية حقيقية، والتحقق الكامل (`bash -n` نظيف، `npm test` → 77/77، `tsc -b` نظيف). محتاج تأكيد نهائي: إعداد بروفايل LLM نفسه (خارج نطاق أي سكريبت مؤتمت لأسباب أمنية) يفضل خطوة يدوية بحتة على أي VM جديد.

---

### 88. آخر رسالة مختفية جزئيًا وراء صندوق الإدخال + إضافة رفع ملفات عامة للمحادثة
**السياق:** المستخدم أبلغ إن آخر رسالة في المحادثة بتلزق وراء صندوق الكتابة (جزء منها مختفي)، وإن ميزة الإرفاق كانت للصور بس، مفيش دعم لملفات عامة.

**السبب الجذري لمشكلة الرسالة المختفية (اكتشاف مهم):** صندوق الإدخال (`.composer`) عنده `position: fixed`، يعني مساحته **مش محجوزة** في تدفق الصفحة الطبيعي، فآخر رسالة بتنزل تحته وتختفي جزئيًا. **الأهم:** لقيت إن فيه حل ذكي **جاهز بالفعل** — متغيّر CSS ديناميكي (`--composer-height`) بيتحدّث حيًا عبر `ResizeObserver` في `ChatScreen.tsx` نفسها، **مصمم خصيصًا لحل نفس المشكلة دي بالظبط** (زي ما يوضح تعليق الكود الأصلي: "a fixed padding guess would either waste space or let the composer cover the last message, which is exactly what was reported"). لكن قاعدة CSS تانية أبسط (`.chat-screen > .chat`)، بنفس درجة التحديد بالضبط بس ظهرت لاحقًا في ترتيب المصدر بالملف، كانت **بتطغى** على القاعدة الذكية بالصدفة (تعادل في Specificity بيُحسَم بترتيب الظهور في الملف). الحل: تعديل `.chat-screen > .chat` نفسها لتستخدم المتغيّر الديناميكي الصحيح بدل قيمة ثابتة تخمينية.

**السبب الحقيقي لعدم دعم الملفات العامة (تحقّق من الكود المصدري، مش افتراض):** فحصت الكود المصدري المخصص لـ OpenHands مباشرة — `MessageContent = MessageTextContent | MessageImageContent` — **بروتوكول الرسائل نفسه في OpenHands بيدعم بس نص وصور، مفيش نوع "ملف عام" مدعوم خالص**. مش نقص من جانبنا، حد فعلي في النظام الأساسي.

**الحل العملي البديل:** إضافة زرار "رفع ملف للمشروع" جديد جنب زرار إرفاق الصورة — بيستخدم **نفس آلية الرفع الموجودة بالفعل** (ميزة رفع ملفات المالك من صفحة المشروع) لرفع الملف فعليًا لمجلد `{project}/uploads/`، ثم **إرسال رسالة نصية تلقائية** للموظف تخبره بمكان الملف الجديد (بما إن رفع الملف وحده من غير إخطار مش هيخلي الموظف يعرف تلقائيًا).

**تحسين بنيوي داعم:** أضفت `overrideText` كـparameter اختياري جديد لـ`sendMessage` (في `useConversation.ts`) — تجنبًا لمشكلة توقيت حقيقية كانت هتحصل لو اعتمدت على `setMessage(text)` متبوعة مباشرة بـ`sendMessage()` (تحديثات React state غير متزامنة، فالاستدعاء الفوري كان هيقرأ القيمة القديمة قبل التحديث).

**الحالة:** ✅ تم التشخيص العميق للسببين، والحل الكامل، والتحقق الشامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 89. إزالة الهيدر العام من شاشة المحادثة — بار الموظف يصبح الهيدر الوحيد
**السياق:** المستخدم طلب إزالة الهيدر العام (اللوجو، القائمة، تبديل اللغة/الوضع) من شاشة المحادثة تحديدًا، وترك بار الموظف (الصورة + الاسم + الدور) بس، ثابت فوق دايمًا.

**التنفيذ:**
- إخفاء `AppHeader` و`BreadcrumbBar` بشرط `selectedProject && selectedEmployee` (يعني شاشة المحادثة تحديدًا، باقي الشاشات زي ما هي).
- **مهم:** بما إن `BreadcrumbBar` كانت المصدر الوحيد لزرار الرجوع في هذا المسار، أضفنا زرار رجوع جديد **جوّه بار الموظف نفسه** (`onBack` prop جديدة لـ`ChatScreen`، مربوطة بنفس دالة `closeEmployee` المُستخدَمة أصلًا) — عشان وظيفة الرجوع تفضل موجودة، مش تختفي بالغلط.
- تصحيح `padding-top` الخاص بشاشة المحادثة (كان محسوبًا أصلًا لحساب مساحة الهيدر+Breadcrumb الغائبين الآن، وكان هيسيب فراغ فاضي كبير فوق).
- إضافة تصميم بصري لبار الموظف (حدود سفلية) عشان يبقى واضح بصريًا كـ"هيدر مصغّر" مستقل، بما إن الصفحة نفسها `overflow: hidden` على المستوى الأعلى (فمفيش سكرول للصفحة كلها يخلي الحاجة تتحرك من مكانها من الأساس — بار الموظف "ثابت فوق" تلقائيًا بمجرد ما بقى أول عنصر في الصفحة).

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 90. تصميم "تطبيق محادثة حقيقي" — فصل لوني للمناطق الثلاثة + إعادة تصميم صندوق الإدخال
**السياق:** المستخدم شارك صورة مرجعية وطلب إعادة تصميم المحادثة لتشعر كتطبيق شات حقيقي — البار العلوي كان كويس (بدون تغيير)، صندوق الإدخال محتاج شكل أحلى، وخلفية منطقة الرسائل لازم تكون بلون مختلف عن البارات.

**تحقق أولي:** فحصت الألوان الفعلية المستخدمة، ولقيت إن **كل الشاشة (البار العلوي، منطقة الرسائل، صندوق الإدخال) كانت بتستخدم نفس اللون بالظبط** (`--mkdd-page`)، بدون أي تمايز بصري خالص. سألت المستخدم توضيح دقيق (أفتح أو أغمق) قبل التنفيذ.

**الحل:**
- **خلفية منطقة الرسائل:** استخدام `--mkdd-surface` بدل `--mkdd-page` — تأكدت إنها **أفتح فعليًا في كلا الوضعين** (فاتح وداكن) بدون الحاجة لأي متغيّر جديد، مع زوايا دائرية (`border-radius: 18px`) لإحساس "كارت" منفصل.
- **صندوق الإدخال:** حقل النص بقى بيضاوي بالكامل (`border-radius: 999px`، زي واتساب/تيليجرام)، أزرار الإرفاق والإرسال بقت دائرية بالكامل، حد علوي خفيف (`border-top`) للفصل البصري عن منطقة الرسائل.

**تعارضات CSS حقيقية اتلقت ومتصلحت أثناء التنفيذ (نفس النمط اللي اكتشفناه قبل كده مرارًا):** بما إن `.chat-screen` عندها className بيشمل `project-view` كمان، لقيت **3 قواعد منفصلة** أكثر تحديدًا (`.project-view .composer textarea`, `.project-view .composer button`) كانت بتطغى بصمت على التعديلات الجديدة وترجعها لشكلها القديم (مربع بدل بيضاوي/دائري). تم تحديد كل واحدة منهم بالفحص المباشر وتصحيحها لتطابق التصميم الجديد بنفس درجة التحديد (مش تخمين، فحص فعلي لكل قاعدة).

**الحالة:** ✅ تم التحقق الأولي، السؤال التوضيحي، التنفيذ، اكتشاف وتصحيح 3 تعارضات CSS حقيقية، والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 91. الوضع الداكن مش واضح بعد التصميم الجديد — تباين ضعيف بين الطبقات + لون فقاعة حاد
**السياق:** بعد استحسان التصميم الجديد للمحادثة (#90) في الوضع الفاتح، أبلغ المستخدم إن الوضع الداكن مش حلو — سؤال توضيحي أكّد المشكلة بدقة: فقاعات الرسائل والبارين العلوي/السفلي بتبان قريبة من لون الخلفية ومش واضحة، **وكمان** لون فقاعة الرسائل المُرسَلة (بتاعة المالك) حاد جدًا ومرهق للعين.

**السبب الجذري (فحص القيم الفعلية):**
- خلفية منطقة الرسائل الجديدة (`--mkdd-surface: #0c1623`) وفقاعة رسالة الموظف (`--mkdd-surface-raised: #0e1b2a`) — **فرق ضئيل جدًا** بين القيمتين في الوضع الداكن (بعكس الوضع الفاتح، فيه فرق واضح جدًا بين أبيض نقي ورمادي فاتح).
- فقاعة "أنا" بتستخدم `--mkdd-accent` الخام (`#325be4`) — أزرق مشبع بالكامل، مناسب كلون تمييز صغير، لكن مرهق كخلفية كبيرة لفقاعة نص بيتم قراءتها بشكل متكرر.

**الحل (خاص بالوضع الداكن بس، الوضع الفاتح لم يُمس خالص):**
1. **خلفية منطقة الرسائل:** استخدام `--mkdd-surface-hover` (`#192330`) بدل `--mkdd-surface` — فرق حقيقي وواضح عن البارات (`--mkdd-page`) وعن فقاعة الموظف (`--mkdd-surface-raised`، اللي بقت أغمق من الخلفية الجديدة — تباين حقيقي بالاتجاه المعاكس، وهو مقبول تمامًا بصريًا).
2. **فقاعة "أنا":** لون ممزوج أهدأ عبر `color-mix()` (نفس الأسلوب المُستخدَم بالفعل في المشروع للتأثيرات الخفيفة) — مزيج 55% من `accent` مع `page` الغامقة، بدل اللون الخام المشبع بالكامل. تم حساب القيمة الناتجة فعليًا (`#1f3a8b`) للتأكد إنها أهدأ وضوحًا لكن لسه مميزة وواضحة كأزرق حقيقي.

**الحالة:** ✅ تم التشخيص الدقيق (بسؤال توضيحي مؤكِّد)، الحل المحدود للوضع الداكن بس، حساب القيمة اللونية الناتجة فعليًا للتأكد من التوازن، والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 92. مساحة سفلية أكبر لصندوق الإدخال + إصلاح نص فقاعتي غير الواضح في الوضع الفاتح
**السياق:** المستخدم أبلغ عن مشكلتين إضافيتين بعد استحسان تصميم المحادثة الجديد: صندوق الإدخال لاصق في نهاية الشاشة بدون مساحة تنفس كافية، والنص جوّه فقاعة "أنا" (المالك) في الوضع الفاتح مش واضح ومفروض يكون بلون فاتح.

**الحل 1 (المساحة السفلية):** زيادة `padding-bottom` لصندوق الإدخال من `10px` لـ`20px` (مع الحفاظ الكامل على إضافة `env(safe-area-inset-bottom)` للأجهزة اللي فيها مؤشر الرجوع/الهوم).

**الحل 2 (النص غير الواضح):** رغم إن `color: #fff` كانت موجودة بالفعل على `article.me` نفسها نظريًا، أضفنا قاعدة صريحة وقوية (`article.me, article.me *`) تفرض اللون الأبيض على **كل** العناصر الفرعية داخل الفقاعة — بما إن محتوى الرسائل بيمر عبر سلسلة عناصر Markdown متداخلة (فقرات، قوائم، إلخ)، وده بيلغي أي احتمال غموض في وراثة اللون عبر السلسلة دي، بدل الاعتماد على قاعدة أب واحدة والأمل إنها هتنتقل صح للعناصر الفرعية كلها. تم التحقق من عدم تعارض هذا مع لون توقيت الرسالة المخفف (`.message-time`، أعلى تحديدًا فتفوز بشكل صحيح).

**الحالة:** ✅ تم التنفيذ والتحقق الكامل للجزئين (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد). **الجزء الثالث (تحسين بار الموظف العلوي) مؤجَّل بانتظار توضيح دقيق من المستخدم.**

---

### 93. الوظيفة في بار الموظف العلوي تتحول لبادج مميز متمركز فوق
**السياق:** بعد سؤال توضيحي حدّد المشكلة بدقة — الوظيفة (زي "Business Analyst") كانت نص صغير رمادي باهت جنب الاسم، صعب ملاحظته. المستخدم طلب تحويلها لبادج واضح، في صف منفصل فوق، متمركز في نص عرض البار بالكامل.

**التنفيذ:**
- إعادة هيكلة `.chat-screen-person` من صف أفقي واحد لعمود يحتوي **صفين**: صف علوي (بادج الوظيفة، متمركز)، وصف تحته (زرار الرجوع، الصورة، الاسم، أزرار الإجراءات — بنفس التخطيط الأفقي القديم).
- بادج الوظيفة: خلفية ملوّنة خفيفة (`--mkdd-accent-glow`)، نص بلون التمييز الأساسي، حواف دائرية بالكامل، خط عريض — تصميم واضح وبارز بصريًا، بدل النص الرمادي الباهت السابق.
- إزالة الكود القديم (`.chat-employee-info span`) بعد التأكد صراحة من عدم استخدامه في أي مكان تاني.

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 94. تحديث بادج الوظيفة — لون موف واضح، أيقونة الوظيفة، نص أبيض وخط أكبر
**السياق:** بعد استحسان بادج الوظيفة الجديد (#93)، طلب المستخدم تحسينات: لون موف واضح بدل التلوين الخفيف، أيقونة تدل على الوظيفة، نص أبيض واضح، وخط أكبر.

**تنظيف بنيوي داعم (قبل التنفيذ):** بما إن خريطة "معرّف الموظف → أيقونة" (`ROLE_ICONS`) كانت مُعرَّفة محليًا جوّه `ProjectHomeScreen.tsx` بس (لاستخدام شريط الفريق)، وبما إن المطلوب دلوقتي استخدام نفس الأيقونات في `ChatScreen.tsx` كمان، تم استخراجها لملف مشترك جديد (`src/utils/roleIcons.ts`) — **مصدر واحد للحقيقة**، بدل تكرار نفس الخريطة في مكانين (منعًا لاحتمال تعارض/تباعد مستقبلي بين الأيقونات المعروضة في الأماكن المختلفة). تم التأكد من إزالة 10 استيرادات أيقونات بقت بلا استخدام في `ProjectHomeScreen.tsx` بعد النقل (فحص مباشر عبر `oxlint`، مش تخمين).

**التنفيذ:**
- خلفية البادج: من تلوين خفيف شفاف (`--mkdd-accent-glow`) لخلفية موف صلبة وواضحة (`--mkdd-accent`).
- نص أبيض صريح (`#fff`) بدل لون التمييز نفسه (اللي كان بيتماشى مع الخلفية الخفيفة القديمة، لكن مش مناسب فوق خلفية صلبة).
- أيقونة الوظيفة (من نفس الخريطة المشتركة الجديدة) تظهر قبل النص، بمحاذاة أفقية صحيحة.
- حجم الخط من `11px` لـ`13px`.

**الحالة:** ✅ تم التنظيف البنيوي والتنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 95. توسيع بادج الوظيفة لشريط الفريق في صفحة المشروع (بحجم أصغر)
**السياق:** بعد استحسان بادج الوظيفة الجديد في شاشة المحادثة (#93/#94)، طلب المستخدم نفس التصميم في شريط الفريق بصفحة المشروع الرئيسية، بحجم أصغر يتناسب مع كارت الموظف المضغوط.

**التنفيذ:**
- استبدال النص الرمادي الباهت القديم (`<span>{employee.role}</span>`) ببادج مصغّر (`employee-card-role-badge`) بنفس لغة التصميم — خلفية موف صلبة، نص أبيض، أيقونة الوظيفة (بإعادة استخدام نفس أيقونة `ROLE_ICONS` الموجودة بالفعل على زاوية الصورة، من الملف المشترك اللي بنيناه في #94).
- الحجم مصغّر ليتناسب مع الكارت (`padding: 2px 8px`, `font-size: 9px`)، مع `width: fit-content` عشان البادج ياخد بس عرض محتواه.

**تعارض CSS حقيقي اتلقى ومتصلح (نفس النمط المتكرر طول الجلسة):** فيه قاعدة `.project-home .employee-card-info span` أعلى تحديدًا (فئتين) كانت هتطغى بصمت على تصميم البادج الجديد وترجعه للنص الرمادي الباهت القديم. تم حذف القاعدة القديمة (بقت كود ميت تمامًا بما إنه معدش فيه أي `<span>` عادي جوّه `.employee-card-info` غير البادج نفسه)، واستبدالها بـoverride صريح للبادج بنفس درجة التحديد.

**الحالة:** ✅ تم التنفيذ، اكتشاف وتصحيح تعارض CSS حقيقي، والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 96. بادج الوظيفة في شريط الفريق بيقطع الأسماء الطويلة بـ"..."
**السياق:** بعد إضافة بادج الوظيفة لشريط الفريق (#95)، لاحظ المستخدم إن أسماء الوظائف الطويلة (زي "Integration Engineer") بتتقطع بـ"..." بسبب ضيق عرض الكارت المضغوط (76px). طلب زيادة ارتفاع البادج.

**التنفيذ:** بدل القص الأفقي بـ"..." (`white-space: nowrap` + `text-overflow: ellipsis`)، السماح للنص باللف الطبيعي على أكتر من سطر (`white-space: normal`) — الحواف بقت مدورة عادية (`border-radius: 12px`) بدل دائرية كاملة (`999px`، كان هيبان شكله غريب مع سطرين)، مع `text-align: center` و`line-height` مناسب. تم التأكد إن الكارت الأب (`.employee-card`) عنده `min-height: auto` (مش ارتفاع ثابت)، فهيتمدد رأسيًا تلقائيًا مع زيادة ارتفاع البادج، بدون أي قص أو تكسير.

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 97. بادج "Implementation Engineer" فايض برّه حدود كارته ومتراكب مع الكارت المجاور
**السياق:** المستخدم شارك صورة توضح إن بادج الوظيفة (خصوصًا "Implementation Engineer") فايض برّه حدود كارته الضيق (76px) ومتراكب بصريًا مع الكارت المجاور، رغم إصلاح اللف النصي السابق (#96).

**السبب الجذري:** البادج (`.employee-card-role-badge`) هي نفسها `display: flex` (تحتوي أيقونة + نص)، **وفي نفس الوقت** هي عنصر Flex Item جوّه حاويتها الأب (`.employee-card-info`). بدون `min-width: 0` صريحة عليها، السلوك الافتراضي لعناصر Flex هو **رفض الانكماش لأقل من عرض محتواها الطبيعي** — يعني البادج كانت بترفض تنكمش لعرض الكارت (76px) وتفضل تاخد عرضها "الطبيعي" الأكبر، فتفيض برّه حدود الكارت.

**الحل:** إضافة `width: 100%; min-width: 0;` صريحة على البادج (في القاعدتين، الأساسية والأعلى تحديدًا)، بدل `width: fit-content` القديمة — كده البادج بتُجبَر تاخد بالظبط عرض حاويتها الأب، والنص جواها بيُجبَر يلف صح داخل هذا العرض المحدود، بدون أي فيضان.

**الحالة:** ✅ تم التشخيص الدقيق (فحص سلسلة Flexbox الكاملة) والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 98. إعادة تصميم كارت الموظف بالكامل — هيدر/صورة/فوتر (حل جذري بدل ترقيع البادج المتكرر)
**السياق:** بعد سلسلة إصلاحات متتالية لمشكلة فيضان بادج الوظيفة (نفس المشكلة تكررت مع كيرلس رغم إصلاح #97)، اقترح المستخدم حل معماري مختلف تمامًا بدل الاستمرار في الترقيع: إعادة تصميم الكارت بالكامل بنمط "بطاقة" — هيدر (اسم + أيقونة الوظيفة) فوق، الصورة في النص، فوتر (نص الوظيفة) تحت، بنفس اللون للهيدر والفوتر، وأهم حاجة: **إلغاء تكرار الأيقونة** (كانت ظاهرة مرتين — على زاوية الصورة وجنب نص الوظيفة).

**لماذا هذا حل جذري وليس ترقيع آخر:** المشاكل المتكررة (#95-#97) كانت كلها أعراض لنفس السبب الجذري — محاولة حشر (أيقونة + نص طويل) جنب بعض أفقيًا داخل عمود ضيق جدًا (76px)، وهو تصميم هش بطبيعته بغض النظر عن كام إصلاح CSS إضافي. التصميم الجديد بيلغي المشكلة من جذورها: الهيدر ياخد **عرض الكارت بالكامل** (مساحة كافية للاسم + أيقونة واحدة بس)، والفوتر كمان ياخد **عرض الكارت بالكامل** (مساحة كافية لنص الوظيفة لوحده، بدون منافسة أيقونة على نفس السطر).

**التنفيذ:**
- **الهيدر:** خلفية موف صلبة بعرض الكارت الكامل، الاسم (قص بـellipsis لو طويل) + أيقونة الوظيفة الوحيدة (نُقلت من زاوية الصورة، ومن جنب النص القديم، لمكان واحد بس).
- **الصورة:** بقيت بسيطة تمامًا، بدون أي أيقونة إضافية عليها.
- **الفوتر:** نفس لون الهيدر، `min-height` ثابتة موحّدة (30px) لكل الموظفين بغض النظر عن طول اسم الوظيفة — ضمانًا لتناسق بصري كامل عبر الشريط.
- `overflow: hidden` على الكارت نفسه — عشان الهيدر والفوتر الملوّنين ياخدوا نفس زوايا الكارت الدائرية بدل ما يطلعوا برّه حدودها بزوايا حادة.

**تنظيف شامل (بعد التأكد الصريح من عدم الاستخدام):** تم حذف **كل** الكود القديم المرتبط بالتصميم السابق نهائيًا — `.employee-card-info`, `.employee-card-info strong`, `.employee-card-role-badge` (بكل تكراراتها، بما فيها القاعدة الأعلى تحديدًا)، و`.employee-role-badge` (أيقونة الزاوية القديمة بالكامل). تم التحقق **قبل** الحذف (بحث نصي شامل عبر كل ملفات TSX) من عدم استخدام أي من هذه الكلاسات في أي مكان تاني بالتطبيق.

**تعارض CSS إضافي اتلقى ومتصلح أثناء التنفيذ:** قاعدة عامة موجودة بالفعل (`.project-home .employee-card strong`، 3 فئات) كانت هتطغى على تنسيق الاسم الجديد جوّه الهيدر (`.employee-card-header strong`، فئتين) — تم تعديل القاعدة القديمة نفسها (`font-size`) بما إنها فعليًا الفائزة، بدل الاعتماد على قاعدة جديدة هتُلغى بصمت.

**الحالة:** ✅ تم التصميم، التنفيذ، التنظيف الشامل، اكتشاف وتصحيح تعارض CSS إضافي، والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، حجم CSS النهائي **قلّ فعليًا** رغم إعادة التصميم، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 99. الفوتر مش لاصق بآخر الكارت — فجوة فاضية تحته بدون حواف دائرية
**السياق:** بعد استحسان التصميم الجديد بالكامل (#98)، لاحظ المستخدم إن الفوتر مش بيمتد لآخر الكارت — فيه فجوة فاضية بلون الكارت الأساسي تحت الفوتر مباشرة، بحواف دائرية غير متطابقة مع الهيدر (اللي بيلتصق بحواف الكارت العلوية صح).

**السبب الجذري:** صندوق الصورة (`.employee-avatar`) كان بحجم ثابت تمامًا (`52px × 52px`)، بدون أي إمكانية تمدد. الكارت (`display: flex; flex-direction: column;` بدون `justify-content` صريحة، فالافتراضي `flex-start`) بيكدّس محتواه (هيدر، صورة، فوتر) في **بداية** الكارت، وأي مساحة فاضية زيادة (زي لو كارت مجاور أطول بسبب اسم وظيفة طويل بيتلف لسطرين، والصف كله بيتساوى بأطول كارت) بتفضل فاضية في **نهاية** الكارت — بعد الفوتر مباشرة.

**الحل:** إضافة `flex: 1 1 auto` لصندوق الصورة (مش الصورة الدائرية نفسها، اللي فضلت بحجمها الثابت 52×52 عبر قاعدة منفصلة) — كده الصندوق بيمتص أي مساحة فاضية إضافية ويكبر، بينما الصورة الفعلية جواه تفضل متمركزة رأسيًا وأفقيًا (بفضل `display: grid; place-items: center;` الموجودة بالفعل)، والفوتر بيتحرك تلقائيًا ليلتصق بآخر الكارت الحقيقي — فيمتلك نفس الحواف الدائرية اللي عند الهيدر.

**الحالة:** ✅ تم التشخيص الدقيق والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 100. ارتفاع الفوتر متفاوت حسب طول اسم الوظيفة — محتاج توحيد كامل للشكل الجمالي
**السياق:** بعد إصلاح إلصاق الفوتر بآخر الكارت (#99)، لاحظ المستخدم إن ارتفاع الفوتر نفسه لسه بيختلف من كارت لآخر حسب طول اسم الوظيفة (سطر واحد مقابل سطرين) — مش متسق بصريًا عبر شريط الفريق.

**السبب الجذري:** الفوتر كان مضبوط بـ`min-height: 30px` (حد أدنى بس، مش ارتفاع ثابت فعليًا) — يعني بيكبر تلقائيًا لو النص احتاج سطرين، فارتفاعه الفعلي بيتفاوت حسب المحتوى.

**الحل:** استبدال `min-height` بـ`height: 38px` ثابتة فعليًا (محسوبة لتستوعب سطرين من النص بحجم 9px براحة كافية)، مع `flex-shrink: 0` صراحة لضمان عدم انضغاطها لو المساحة الكلية للكارت ضاقت لأي سبب، و`overflow: hidden` كضمان احترازي لأي اسم وظيفة طويل بشكل استثنائي. النتيجة: كل الفوترات بنفس الارتفاع بالضبط، بغض النظر عن طول النص — الأسماء القصيرة تتمركز رأسيًا في المساحة الثابتة بفضل `align-items: center` الموجودة بالفعل.

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 101. ألوان الخط الواصل في سير المشروع معكوسة في الواجهة الإنجليزية
**السياق:** المستخدم لاحظ إن الخط الواصل الملوّن بين بوابات سير المشروع بيبان بألوان "مقلوبة" (في مكان غلط) لما تتحول الواجهة للإنجليزية.

**السبب الجذري (تحقّق مباشر من الكود، مش افتراض):** فحصت `useLanguage.ts` ولقيت إن `document.documentElement.dir` بتتغيّر فعليًا لـ`"ltr"` مع الإنجليزية (و`"rtl"` مع العربية) — يعني الصفحة بالكامل، شاملة شبكة البوابات، بتنعكس بصريًا: أول بوابة (المتطلبات) بتظهر يمين في RTL، لكن **شمال** في LTR. اتجاه التدرج اللوني (`linear-gradient`) اللي بنيناه في #84 كان **ثابت** على `to left` (مبني على افتراض RTL دايمًا فقط)، فمعملش أي تعديل مع تحول الاتجاه في الإنجليزية — النتيجة: الألوان فضلت مرسومة بنفس الاتجاه الفيزيائي القديم، بينما مواضع الدوائر انعكست فعليًا، فحصل عدم تطابق.

**الحل:** إضافة معامل `language` لدالة `buildConnectorGradient`، واختيار اتجاه التدرج ديناميكيًا: `to left` للعربية (RTL)، `to right` للإنجليزية (LTR) — يطابق دايمًا الاتجاه الفيزيائي الفعلي اللي الصفحة عليه حاليًا، بدل افتراض ثابت.

**تم التحقق فعليًا:** محاكاة رياضية حية لكلا اللغتين تأكدت إن أول لون (المتطلبات) بيُرسَم فعليًا في نفس الجهة اللي فيها الدائرة الحقيقية، في كلا الاتجاهين.

**الحالة:** ✅ تم التشخيص الدقيق (فحص مباشر لآلية تبديل الاتجاه)، الإصلاح، التحقق الرياضي الحي لكلا اللغتين، والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء).

---

### 102. نظام تقارير عابر للموظفين (Cross-Employee Reports) — ميزة جديدة كاملة
**السياق:** المستخدم شرح سيناريو حقيقي حصل معاه: كيرلس (Implementation) لقى نقص في متطلبات مريم (UI/UX)، كتب تقرير عن كده، وطلب من مريم تتحقق منه — واتضح إنه محق. المستخدم عايز يشوف هذا النوع من التقارير موثّق، ويتابع لحد ما الموظف المستلم ينفّذه.

**نقاش قبل التنفيذ (3 جولات أسئلة توضيحية):**
1. هل "المتطلبات المطلوبة من كل موظف" تقصد خطة العمل الموجودة بالفعل، أو حاجة مختلفة؟ → المثال الحقيقي كشف إنه شيء مختلف تمامًا — تقارير موظف لموظف، مش خطة عمل.
2. هل النظام عام لكل المراحل، أو مقتصر على المتطلبات بس؟ → **الاتنين معًا** — نظام عام، لكن كل تقرير مرتبط ببوابة/مرحلة معينة، وبيظهر لما تدوس على البوابة المعنية.
3. مين يقدر يقفل التقرير نهائيًا؟ → سير عمل من 3 مراحل: الموظف المستلم يرد (نفّذ، أو رفض مع سبب إجباري)، ثم المالك يقفل نهائيًا.

**لماذا نظام منفصل عن "الملاحظات" (Findings) الرسمية:** النظام الرسمي مقصور على المراجعين الأربعة (QA/Test Automation/Code Review/Security Review) ومرتبط باعتماد بوابة الإنتاج (بيمنع الاعتماد لو فيه ملاحظات غير محلولة). النظام الجديد عام (أي موظف لأي موظف) وملوش أي تأثير على اعتماد البوابات — مجرد تتبّع وتوثيق.

**التنفيذ (Backend):**
- `workflow-state.mjs`: مصفوفة `reports` جديدة (بنفس نمط `findings`/`blockers`).
- `workflow.mjs`: `handleReports` جديدة بـ3 إجراءات (`add`, `respond`, `close`)، بقواعد صارمة: مينفعش تقفل تقرير لسه مفتوح (`report_not_ready_to_close`)، مينفعش ترفض بدون سبب (`decline_note_required`).
- **تم التحقق الحي الكامل** لدورة الحياة الخمسة سيناريوهات (إنشاء، منع قفل مبكر، منع رفض بدون سبب، رد الموظف، قفل المالك) — كلهم اشتغلوا صح 100%.

**التنفيذ (Frontend):**
- `client.ts`: نوع `reports` جديد في `WorkflowState`، دالة `closeWorkflowReport` (الإجراء التفاعلي الوحيد من الواجهة للمالك — الإضافة والرد بيتعملوا عبر curl من الموظفين، بنفس نمط باقي أنظمة سير العمل).
- `WorkflowStepper.tsx`: البوابات الأربعة بقت أزرار قابلة للدوس (بدل `<div>` ثابتة)، بتفتح نافذة منبثقة تعرض تقارير البوابة المختارة — من ← لـ، العنوان، الحالة الملوّنة، الملاحظة، وزرار "قفل نهائي" (يظهر بس لما الحالة implemented/declined).
- شارة عدد صغيرة على كل بوابة تظهر عدد التقارير المفتوحة/غير المقفولة.

**تنظيف توثيقي:** أثناء كتابة تعليمات الموظفين في `AGENTS.md`، اكتشفت إشارة خاطئة كتبتها بنفسي ("Section 2's role IDs") — القسم 2 مالوش أي جدول IDs خالص، الـIDs الفعلية موجودة في القسم 4. تم التصحيح فورًا قبل الحفظ النهائي.

**الحالة:** ✅ تم النقاش الكامل، التصميم، التنفيذ (Backend+Frontend+تعليمات الموظفين)، التحقق الحي الشامل لدورة الحياة، اكتشاف وتصحيح خطأ توثيقي ذاتي، والتحقق النهائي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، `node --check` على كل ملفات الـbackend المعدَّلة، توازن CSS مؤكَّد).

---

### 103. تقارير الموظفين — عنوان قصير بس، من غير مكان للتفاصيل الكاملة
**السياق:** المستخدم سأل صراحة "المهم اني أقدر أشوف التقرير وتفاصيله" — سؤال دفعني أتحقق فعليًا من التصميم الأصلي (#102) بدل الافتراض إنه كافي.

**الفجوة الحقيقية المكتشفة:** التصميم الأصلي كان بيحفظ `title` بس (عنوان مختصر سطر واحد) — **مفيش حقل منفصل للتقرير الكامل المفصّل**. لو موظف كتب تقرير حقيقي مفصّل (زي سيناريو المستخدم الفعلي)، مفيش مكان يتخزن فيه غير العنوان القصير.

**الحل:** إضافة حقل `details` اختياري جديد (نص كامل، منفصل عن `title`) — للتقرير المفصّل الكامل (الملاحظة، السبب، الأثر، التوصية). تم تحديث:
- الـ Backend (`workflow.mjs`) — استقبال وتخزين الحقل الجديد.
- الـ Frontend (`client.ts`, `WorkflowStepper.tsx`) — نوع جديد + عرض النص الكامل بوضوح تحت العنوان (بـ`white-space: pre-wrap` للحفاظ على فقرات/أسطر التقرير الأصلية لو موجودة).
- تعليمات الموظفين (`AGENTS.md`) — توضيح صريح إن `title` عنوان سريع بس، و`details` (مُوصى بيها بشدة لأي مشكلة غير بديهية) هي التقرير الفعلي.

**تم التحقق حيًا:** اختبار مباشر أكّد إن النص الكامل بيتحفظ ويرجع بدقة 100% بدون أي اقتطاع.

**الحالة:** ✅ تم اكتشاف الفجوة بناءً على سؤال المستخدم المباشر، الإصلاح الكامل (Backend+Frontend+تعليمات)، التحقق الحي، والتحقق النهائي الشامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 104. زرار "اعتماد المرحلة" — يمثّل توقيع المالك الرسمي لكل بوابة
**السياق:** المستخدم طلب زرار حقيقي في الواجهة للانتقال من مرحلة لمرحلة (اعتماد البوابة)، بمثابة توقيعه الرسمي.

**اكتشاف مهم قبل التنفيذ:** فحصت الكود المصدري ولقيت إن **الـBackend والـAPI كانوا موجودين بالفعل بالكامل** (`approveWorkflowGate` في `client.ts`، `handleApproveGate` في `workflow.mjs` بكل قواعده — منع اعتماد لو فيه عوائق مفتوحة، ملاحظات غير مؤكَّدة، أو مراجعات إلزامية ناقصة) من شغل سابق — **لكن مش مربوط بأي زرار في الواجهة خالص**. الميزة كانت "نص جاهزة" من غير علم المستخدم.

**التنفيذ:**
- زرار "اعتماد مرحلة [كذا]" يظهر بس لما فيه بوابة حالية لسه غير معتمدة (بيختفي تلقائيًا لو المشروع خلص كل بواباته).
- **نافذة تأكيد صريحة إجبارية** قبل التنفيذ — بما إن الاعتماد "توقيع رسمي" ومينفعش يترجع فيه، النص بيوضح ده صراحة قبل أي تنفيذ فعلي.
- **رسائل خطأ مترجمة بوضوح** بدل الأكواد التقنية الخام — لو الاعتماد اترفض (عوائق مفتوحة/ملاحظات غير مؤكَّدة/مراجعات ناقصة)، المالك بيشوف سبب واضح بالعربي/الإنجليزي، مش `open_blockers_exist` تقني.
- **قيمة `approvedBy` = `"Owner"`** — معرِّف ثابت وواضح، منفصل تمامًا عن أي هوية موظف AI، بما إن مفيش نظام مصادقة حقيقي حاليًا (زي ما موثّق في README §47/AGENTS.md بخصوص عدم الوثوق بهويات العملاء).

**تم التحقق حيًا بالكامل:** سيناريو حقيقي شامل — إضافة عائق مفتوح → محاولة اعتماد فاشلة (409 `open_blockers_exist`) → حل العائق → اعتماد ناجح، مع تأكيد إن المرحلة انتقلت تلقائيًا للبوابة التالية، والتوقيع اتسجّل بدقة (`approvedBy: "Owner"`).

**الحالة:** ✅ تم اكتشاف الأساس الجاهز، بناء الواجهة الكاملة (زرار + تأكيد + رسائل مترجمة)، التحقق الحي الشامل، والتحقق النهائي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 105. صندوق الإدخال بيبان بشكل بيضاوي متطرف (Stadium shape) لما النص يلف أكتر من سطر
**السياق:** المستخدم شارك صورة توضح إن صندوق كتابة الرسالة بيبان بشكل بيضاوي غريب ومبالغ فيه لما النص فيه يكبر لأكتر من سطر.

**السبب الجذري:** حقل النص عنده `border-radius: 999px` (دائري بالكامل) من إعادة تصميم المحادثة السابقة (#90) — القيمة دي بتشتغل تمام وتدي شكل بيضاوي أنيق (زي واتساب) **لسطر واحد بس**، بما إن ارتفاع الصندوق قصير. لكن لما النص يكبر ويلف لأكتر من سطر (الصندوق يكبر رأسيًا)، نفس القيمة الكبيرة (999px) على صندوق أطول بتخلق شكل بيضاوي طويل ومتطرف (Stadium shape)، بدل مستطيل بحواف مدورة معقولة.

**الحل:** تقليل `border-radius` من `999px` لقيمة معتدلة ثابتة (`22px`) — القيمة دي بتفضل تدي إحساس "بيضاوي/دائري" لطيف لسطر واحد (بما إن نصف قطرها قريب من نص ارتفاع الصندوق قصير السطر)، لكن مش هتتحول لشكل متطرف لما الصندوق يكبر لأكتر من سطر — حل ثابت يناسب كل الحالات، بدل قيمة أقصى بتتصرف بشكل مختلف حسب الارتفاع.

**الحالة:** ✅ تم التشخيص الدقيق والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 106. إشعار داخل التطبيق للرسائل الجديدة غير المقروءة — الجزء الأول من طلب المستخدم
**السياق:** المستخدم طلب إشعارات لما موظف يبعت رسالة وهو مش فاتح المحادثة، وكمان للتقارير الجديدة. سؤال توضيحي كشف إن المطلوب حاجتين: إشعار داخل التطبيق (يشتغل فورًا)، وإشعار حقيقي يظهر حتى لو الموبايل قافل (محتاج HTTPS الأول، زي ما اتفقنا في نقاش الـPWA).

**اكتشاف مهم قبل البناء:** نظام التقارير (من الشغل السابق) **أصلًا بيعرض شارة عدد التقارير المفتوحة على كل بوابة** — ده فعليًا إشعار داخلي جاهز بالكامل، مفيش داعي لأي شغل إضافي.

**التنفيذ (الجزء الجديد — إشعار الرسائل):**
- **Backend:** endpoint خفيف جديد (`/api/chat/last-message`، بنفس نمط `/api/chat/work-plan` المُحسَّن من قبل) يرجّع بس آخر رسالة (توقيتها ومصدرها: موظف/مالك)، بدون سحب كل تاريخ المحادثة الثقيل.
- **تتبّع "آخر مشاهدة":** `localStorage` محلي (مناسب تمامًا هنا بما إن التطبيق بمستخدم واحد بلا نظام تسجيل دخول حقيقي حاليًا) — يتسجّل تلقائيًا لما المالك يفتح محادثة موظف، وكمان لما رسايل جديدة توصل وهو لسه فاتح المحادثة (عشان الرسالة متفضلش "غير مقروءة" لو المالك شافها فعليًا وهو فاتح).
- **الواجهة:** نقطة حمراء صغيرة على كارت الموظف في شريط الفريق، تختفي تلقائيًا بعد فتح المحادثة.

**تم التحقق حيًا وبدقة رياضية:**
1. اختبار حي مباشر للـendpoint الجديد — أكّد إنه بيرجّع فعليًا **آخر** رسالة (مش أول واحدة)، بتوقيتها ومصدرها الصحيح.
2. محاكاة منطقية لدالة الكشف عن الرسائل غير المقروءة (`hasUnreadMessage`) — 5 حالات حدّية مختلفة (رسالة موظف حديثة، رسالة قديمة، آخر رسالة من المالك نفسه، أول زيارة بدون تاريخ مشاهدة سابق، مفيش رسائل خالص) — كل الحالات صحّت 100%.

**الجزء المؤجَّل (موثَّق صراحة):** الإشعار الحقيقي (يظهر حتى لو الموبايل قافل) يحتاج `Notification API` + `Push API` + `Service Worker`، وكلهم **يتطلبون HTTPS إجباريًا** من المتصفح — نفس القيد بالظبط اللي ناقشناه في موضوع الـPWA. هذا الجزء مؤجَّل لحد ما يتم إعداد الـHTTPS (عبر NPM + شهادة).

**الحالة:** ✅ الجزء الأول (إشعار داخل التطبيق) تم بالكامل — تنفيذ، تحقق حي، اختبار منطقي شامل، توثيق (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، `node --check` نظيف، توازن CSS مؤكَّد). الجزء الثاني (إشعار حقيقي) مؤجَّل بانتظار HTTPS.

---

### 107. البنية التحتية الكاملة للـPWA والإشعارات الحقيقية — جاهزة، تنتظر HTTPS بس
**السياق:** المستخدم طلب بناء كل البنية التحتية للـPWA والإشعارات الحقيقية (تظهر حتى لو الموبايل قافل) **الآن**، عشان تشتغل تلقائيًا فور ما الدومين يوصل بـHTTPS، بدون أي حاجة لإضافة كود جديد وقتها.

**PWA الأساسية:**
- أيقونات PNG مربعة بدقة (192×192، 512×512) — تم اكتشاف وتصحيح مشكلة حقيقية أثناء التوليد الأول (الأبعاد الناتجة مش مربعة تمامًا، 192×184، بسبب نسبة أبعاد الـSVG الأصلي)، حُلت بفرض قماش مربع صريح.
- `manifest.json` بألوان MKDD الحقيقية (مش ألوان افتراضية) — مأخوذة مباشرة من نظام الألوان الرسمي في `App.css`.
- ربط الـmanifest + `theme-color` meta tag في `index.html`.

**Service Worker (`service-worker.js`):** استقبال أحداث Push وعرضها كإشعار حقيقي، والتعامل مع الضغط عليها (فتح/تركيز التطبيق). بدون أي استراتيجية تخزين مؤقت (Offline caching) عمدًا — القيمة الأساسية لـMKDD (محادثة حية مع الموظفين) محتاجة اتصال شبكة أصلًا.

**Backend — نظام Push حقيقي كامل:**
- `push-state.mjs`: توليد وحفظ دائم لمفاتيح VAPID (مرة واحدة فقط — إعادة التوليد بتُلغي كل الاشتراكات القديمة)، وتخزين الاشتراكات (يدعم أكتر من جهاز/متصفح لنفس المالك).
- `lib/push-notifications.mjs`: دالة الإرسال الفعلية، بتنظيف تلقائي للاشتراكات المنتهية (404/410).
- `routes/push.mjs`: endpoints لمفتاح VAPID العام والاشتراك.

**نقطتا الإطلاق (Triggers) — مربوطتين بالأحداث الحقيقية:**
1. رسالة موظف جديدة (جوّه `ws-bridge.mjs`، عند استقبال `MessageEvent` بمصدر `agent`).
2. تقرير جديد (جوّه `handleReports`، عند `action === "add"`).

**تنظيف بنيوي:** استُخرجت `readJsonBody` (كانت مكررة محليًا جوّه `workflow.mjs`) لملف مشترك (`lib/read-json-body.mjs`) — مصدر واحد بدل التكرار.

**أخطاء حقيقية اتلقت وصُححت أثناء البناء (بالفحص المباشر، مش الافتراض):**
1. **مسار استخراج نص الرسالة كان غلط تمامًا** — استخدمت `normalized.content` بدل `normalized.llm_message.content`. تم اكتشافه واختباره حيًا مباشرة بتنفيذ `normalizeEvent()` فعليًا برسالة تجريبية، والتأكد من الشكل الصحيح قبل الاعتماد عليه.
2. **متغيّر `language` غير موجود استُخدِم بالغلط** في الـBackend (مفهوم اللغة Frontend بس) — تم اكتشافه وتصحيحه فورًا لنص عربي ثابت.
3. **باگ حقيقي وخطير في معالجة الأخطاء** — `ensureConfigured()` (اللي بتحمّل مفاتيح VAPID) كانت بتتنادى **برّه** أي `try/catch` جوّه `sendPushToAll`، رغم إن توثيق الدالة نفسها بيوعد صراحة "never throws". **تم اكتشافه بالاختبار الحي المباشر** (محاكاة فشل تحميل مفتاح VAPID فعليًا) — أكّد الاختبار وجود استثناء غير معالَج (Unhandled Rejection) حقيقي قادر يوقف السيرفر بالكامل. تم الإصلاح بإحاطة الدالة بالكامل بـ`try/catch`، وأُعيد نفس اختبار الفشل (Regression test) للتأكد من نجاح الإصلاح فعليًا.

**تم التحقق حيًا وبدقة كاملة (كل خطوة، مش افتراض):**
- توليد مفتاح VAPID تلقائيًا عند أول استدعاء + التأكد من الحفظ الدائم الفعلي في `mkdd-data/`.
- تسجيل اشتراك وهمي + التأكد من الحفظ الفعلي.
- رفض طلب اشتراك ناقص (400).
- محاكاة فشل الإرسال (اشتراك ميت/غير حقيقي) — التأكد من عدم توقف السيرفر (قبل الإصلاح: Crash، بعد الإصلاح: تعامل آمن).

**الجزء الفرونت إند:** `utils/pushNotifications.ts` (كشف الدعم، تسجيل Service Worker، طلب إذن، الاشتراك) — كل دالة فيها **مفحوصة الدعم صراحة** وبترجع بأمان (مش استثناء) لو الميزة مش مدعومة حاليًا (HTTP عادي). زرار في الهيدر (`AppHeader.tsx`) يظهر **بس** لما الدعم متاح فعليًا — يعني مختفي تمامًا دلوقتي على HTTP، وهيظهر **تلقائيًا بدون أي تعديل كود إضافي** فور ما الـHTTPS يتفعّل.

**الحالة:** ✅ البنية التحتية الكاملة جاهزة 100%، مربوطة بنقاط الإطلاق الحقيقية، مع اكتشاف وتصحيح 3 أخطاء حقيقية (واحد منهم خطير) عبر الاختبار الحي المباشر، لا مجرد الكتابة والافتراض. **مؤجَّل التفعيل الفعلي فقط** بانتظار إعداد HTTPS (عبر NPM + شهادة، زي ما اتفقنا). التحقق الكامل: `npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف على كل ملفات الـBackend السبعة المعدَّلة/الجديدة، `oxlint` 0 أخطاء، توازن CSS مؤكَّد.

---

### 108. الدومين يتظبط عبر متغيّر بيئة، مش Hardcoded جوّه الكود
**السياق:** المستخدم جهّز الدومين فعليًا، وواجه رفض من Vite (`server.allowedHosts`) — إجراء أمان حقيقي ضد هجمات DNS Rebinding. المستخدم سأل صراحة عن إمكانية إضافة جزء في الواجهة (UI) لإعداد الدومين بدل الكتابة المباشرة جوّه الكود.

**تفسير تقني (ليه واجهة داخل التطبيق مش الحل الصح):** `allowedHosts` بتتقرا من `vite.config.ts` **وقت تشغيل السيرفر بس**، مش أثناء اشتغاله. حتى لو بُنيت واجهة تحفظ الدومين، السيرفر مش هيقرأها إلا بعد إعادة تشغيل كاملة — فمفيش فايدة حقيقية من واجهة "حية" لإعداد بيتقرا مرة واحدة بس عند البدء.

**الحل الصحيح:** قراءة الدومين من **متغيّر بيئة** (`MKDD_ALLOWED_HOSTS`)، بنفس نمط `OPENHANDS_URL` و`MKDD_DATA_DIR` الموجودين بالفعل — يدعم قائمة دومينات مفصولة بفواصل. بهذا الشكل:
- مفيش أي دومين مكتوب جوّه الكود المصدري (اللي متخزن على GitHub علنًا).
- تغيير الدومين مستقبلًا = سطر واحد في `.env`/`compose.yml` + إعادة تشغيل، بدون أي تعديل كود.

**تم التحقق حيًا بدقة:** محاكاة مباشرة لـ3 سيناريوهات حقيقية — متغيّر بقائمة دومينات (تحويل صحيح لمصفوفة)، متغيّر غير موجود خالص (يرجع للسلوك الافتراضي الآمن)، ومتغيّر موجود لكن فاضي (نفس حالة `compose.yml` الافتراضية لو المستخدم معملش `.env`) — الحالات الثلاث اتأكدت بدقة 100%.

**الحالة:** ✅ تم التنفيذ والتحقق الحي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، `YAML` سليم، محاكاة منطقية لـ3 سيناريوهات).

---

### 109. إعداد الدومين من الواجهة مباشرة — مع رسالة "محتاج إعادة تشغيل" صريحة
**السياق:** بعد اقتراح الحل الأول (متغيّر بيئة، #108)، المستخدم أصرّ بحق على إن فيه حل ممكن من الواجهة نفسها — يحفظ الدومين، وبعدها يعرض رسالة "محتاج إعادة تشغيل" بدل الرفض الكامل لفكرة الواجهة.

**تصحيح تقييمي السابق:** كنت رفضت فكرة الواجهة بحجة إن الإعداد بيتقرا وقت البدء بس — وده صحيح، لكن ده **مش سبب كافٍ لرفض الفكرة كاملة**، بما إن المستخدم من الأول كان قاصد بالظبط النمط الشائع في لوحات التحكم: احفظ → أعرض تنبيه واضح "محتاج إعادة تشغيل" → المستخدم يعيد التشغيل بنفسه.

**التنفيذ الكامل:**
- **Backend:** `site-config.mjs` جديد (تخزين دائم في `mkdd-data/site-config.json`، بنفس نمط `workflow-state.mjs`/`push-state.mjs`)، مع endpoints لإضافة/عرض/حذف الدومينات (`routes/settings.mjs`).
- **`vite.config.ts`:** بقى يقرأ من **مصدرين مدموجين معًا** (Union مع إزالة التكرار) — متغيّر البيئة (#108) **و**الملف المحفوظ من الواجهة الجديدة — فالطريقتين شغالتين مع بعض بدون تعارض.
- **الواجهة:** مكوّن `DomainSettingsModal.tsx` جديد كامل — إضافة/عرض/حذف دومينات، **رسالة تحذير واضحة وغير قابلة للتجاهل** (خلفية صفراء، أيقونة تحذير) بعد كل حفظ/حذف، بتوضح بالظبط الأمر المطلوب لإعادة التشغيل. زرار إعدادات (⚙️) جديد في الهيدر يفتح الـModal.

**تم التحقق حيًا بدقة شاملة:**
1. محاكاة رياضية للدمج بين المصدرين (متغيّر بيئة + ملف محفوظ) — تأكدت من التخلص الصحيح من التكرار (دومين موجود في الاتنين بيظهر مرة واحدة بس).
2. اختبار حي كامل لدورة حياة الـEndpoint الجديد بالكامل — 6 سيناريوهات (حالة فاضية، إضافة، تأكيد الحفظ الفعلي عبر GET منفصل، منع التكرار، رفض دومين فاضي، الحذف) — كل السيناريوهات نجحت 100%.

**الحالة:** ✅ تم تصحيح التقييم الأولي بناءً على إصرار المستخدم المحق، التنفيذ الكامل (Backend+Frontend+دمج المصدرين)، التحقق الحي الشامل، والتحقق النهائي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف على كل ملفات الـBackend، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 110-111. زرار "إعادة تشغيل" حقيقي عبر Docker Socket + نقل الإعداد للسايدبار — وباگ خطير اتلقى بالتحقق المباشر
**السياق:** المستخدم طلب نقل إعداد الدومين من الهيدر للسايدبار، وإضافة زرار "إعادة تشغيل" حقيقي بيشتغل فعليًا (مش بس رسالة توضح الأمر اليدوي).

**#110 — إعادة تشغيل حقيقية عبر Docker Socket:**
- **التريد-أوف الأمني (موثّق صراحة):** إعطاء التطبيق وصول لـDocker Socket (`/var/run/docker.sock`) — تريد-أوف أمني حقيقي (وصول لـDocker بيعادل عمليًا صلاحيات Root على الخادم في كتير من الإعدادات). تم توثيقه بنفس مستوى "الديون التقني المعروف والمقصود" زي `pid: service:agent-canvas` الموجودة بالفعل في المشروع لسبب مشابه (اكتشاف مفتاح جلسة OpenHands).
- **Backend:** دالة `restartContainer()` بتستخدم `http` المدمجة في Node.js فقط (بدون أي مكتبة إضافية زي `dockerode`) — بتتصل بـDocker Engine API مباشرة عبر الـSocket، وتطلب إعادة تشغيل الحاوية باسمها (مُمرَّر عبر متغيّر بيئة `MKDD_UI_CONTAINER_NAME` جديد، متزامن مع `container_name` في `compose.yml`).
- **تم التحقق حيًا:** استدعاء الـEndpoint في بيئة بدون Docker Socket حقيقي (بيئة الاختبار) — تأكدت إن الرد بيوصل صح (200) والسيرفر **بيفضل شغال وحي** رغم فشل محاولة الاتصال الفعلية، بفضل معالجة الأخطاء الآمنة.

**#111 — نقل الإعداد من الهيدر للسايدبار:**
- شيل زرار وModal الإعدادات من `AppHeader.tsx` بالكامل، ونُقِلا لـ`Sidebar.tsx` كعنصر قائمة جديد.
- **باگ بنيوي حقيقي اتلقى ومتصلح فورًا:** أول محاولة نقل كانت بتحط الـModal جوّه نفس شرط `if (!open) return null;` بتاع السايدبار — يعني لحظة ما المستخدم يختار "الإعدادات" (اللي بتقفل السايدبار فورًا زي باقي العناصر)، الـModal **كانت هتختفي فورًا معاه** بدل ما تفضل ظاهرة! تم اكتشافه بمراجعة منطق الكود قبل التنفيذ الفعلي، وتصحيحه بإعادة الهيكلة (Fragment، الـModal مستقل تمامًا عن حالة فتح/قفل السايدبار).

**باگ خطير حقيقي اتلقى بالتحقق المباشر (مش بالافتراض):** أثناء التحقق النهائي من `compose.yml`، السكريبت التحققي رجّع `False` غير متوقعة لوجود الـDocker Socket mount! فحص الملف كشف السبب الحقيقي: **إضافة `volumes:` section جديدة منفصلة، رغم وجود section بنفس الاسم بالفعل جوّه نفس الـservice**. YAML بيسمح تقنيًا بمفاتيح مكررة، لكن أغلب المحلّلات (شامل PyYAML) **بتاخد آخر قيمة بس وتتجاهل الأولى تمامًا** — يعني الـsection القديمة (اللي فيها `company-agents-definitions`, `mkdd-data`, `projects`) **كانت هتختفي بالكامل** لو اتنشر الملف بالشكل ده، وده كان هيكسر كل الميزات الأساسية للمشروع (تعليمات الموظفين، بيانات سير العمل، المشاريع نفسها) خالص! تم الإصلاح بدمج كل الـmounts في section واحدة، وتم التحقق مرة تانية للتأكد من وجود كل الـmounts الأربعة معًا (الثلاثة الأصلية + الجديد).

**الحالة:** ✅ تم تنفيذ الميزتين بالكامل، اكتشاف وتصحيح باگ بنيوي في React (Modal بيختفي مع السايدبار)، **واكتشاف وتصحيح باگ خطير حقيقي في `compose.yml`** كان ممكن يمنع المشروع بالكامل من العمل — كل ده عبر التحقق المباشر الصارم، مش الافتراض. التحقق النهائي الكامل: `npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء، `compose.yml` تحقّقت منها برمجيًا (كل الـ4 mounts موجودة معًا)، توازن CSS مؤكَّد.

---

### 112. تقييد إعدادات الدومين/إعادة التشغيل للوصول المحلي بس — حماية حقيقية، مش شكلية
**السياق:** بعد نقاش مطوّل عن مخاطر تعرّض الدومين العام + عدم وجود تسجيل دخول + وصول Docker Socket معًا، اقترح المستخدم فكرة عملية: زرار الإعدادات يختفي لما التطبيق يتفتح بالدومين العام، ويظهر بس بالـIP المحلي.

**تصحيح مهم قبل التنفيذ:** أوضحت للمستخدم إن **إخفاء الزرار في الواجهة بس مش كافي كحماية حقيقية** — الـEndpoints نفسها (خصوصًا `/api/settings/restart-container` اللي بيستخدم Docker Socket) هتفضل شغالة ومتاحة لأي حد يعرف الرابط المباشر، حتى لو الزرار مش ظاهر. ده كان هيدي إحساس أمان وهمي بدون حماية فعلية.

**الحل الحقيقي المُنفَّذ على مستويين:**
1. **Backend (الحماية الفعلية):** دالة `isLocalHostRequest()` جديدة في `settings.mjs` — بتفحص `Host` header بتاع كل طلب واصل (اللي NPM بيحافظ عليه من الأصل تلقائيًا، بنفس المبدأ اللي فحص `allowedHosts` بتاع Vite بيعتمد عليه أصلًا)، وترفض (403 `local_access_only`) أي طلب مش جاي من IP محلي/خاص (نطاقات RFC 1918: `192.168.x.x`, `10.x.x.x`, `172.16-31.x.x`) أو `localhost`. اتطبّقت على **كل** الـEndpoints الأربعة (`/api/settings/*`)، مش بس زرار إعادة التشغيل.
2. **Frontend (شكلي بس):** `isLocalAccess()` جديدة، بنفس منطق الفحص بالظبط (Regex مطابق تمامًا)، بتخفي عنصر "الإعدادات" في السايدبار لما الوصول يكون عبر دومين عام — تحسين تجربة استخدام، مش الحماية نفسها.

**تم التحقق بدقة صارمة جدًا (مش افتراض خالص):**
1. محاكاة منطقية شاملة لـ13 حالة اختبار — شملت كل نطاقات الـIP الخاصة، حالات حدّية (أعلى وأسفل نطاق `172.16-31`)، **واختبار دقة حرج** (التأكد إن `193.168.2.18` — يشبه شكليًا `192.168.x.x` — بيترفض صح، مش بيتقبل غلط بسبب Regex غير دقيق). كل الـ13 حالة نجحت 100%.
2. اختبار حي كامل على السيرفر الفعلي — 4 سيناريوهات: طلب بـHost محلي (نجح 200)، طلب بـHost دومين عام (اترفض 403)، **طلب إعادة تشغيل (الأخطر) بدومين عام (اترفض 403)**، طلب إعادة تشغيل بـHost نمط ويرجارد (اتقبل من فحص الهوية، 200).

**الحالة:** ✅ تم تصحيح مفهوم "الحماية الشكلية مقابل الحقيقية" بوضوح للمستخدم، التنفيذ الكامل على المستويين (Backend حقيقي + Frontend شكلي متسق)، التحقق المزدوج (محاكاة منطقية شاملة + اختبار حي فعلي)، والتحقق النهائي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء).

---

### 113. أيقونة الـPWA لازم تكون نفس شعار MKDD الحقيقي، مش أيقونة عامة
**السياق:** المستخدم طلب إن أيقونة الـPWA تكون **نفس** أيقونة/شعار MKDD الظاهرة في الهيدر — مش أي أيقونة عامة تانية.

**اكتشاف مهم قبل التنفيذ:** فحصت الكود المصدري ولقيت إن شعار الهيدر الحقيقي (`/api/branding/logo`) **مش نفس ملف الـSVG** اللي استخدمته أصلًا لتوليد أيقونات الـPWA الأولى (#107) — الشعار الحقيقي ملف PNG مخزَّن في `mkdd-data/branding/mkdd-logo.png` (بيانات المستخدم، قابل للتغيير من الواجهة)، بينما استخدمت وقتها `favicon.svg` الثابت بالغلط.

**الحل — توليد ديناميكي بدل ملفات ثابتة:** بدل تخزين أيقونات ثابتة (كانت هتفضل قديمة لو المستخدم غيّر الشعار مستقبلًا)، بنيت `handleBrandingIcon` جديد — بيقرأ **نفس ملف الشعار الحقيقي** وقت الطلب، ويحوّل حجمه ديناميكيًا (192×192، 512×512) باستخدام مكتبة `sharp`، مع `fit: "contain"` على قماش شفاف مربع (يمنع تشويه أو قص أي شعار مش مربع الأبعاد أصلًا — نفس نوع المشكلة اللي حصلت مع الـSVG الأول في #107). لو مفيش شعار مرفوع لسه، بيرجع تلقائيًا لأيقونة SVG الافتراضية المُضمَّنة (بدون كسر `manifest.json`).

**تم التحقق حيًا بدقة صارمة:**
1. حالة "مفيش شعار مرفوع" — الـFallback SVG اشتغل صح، بحجم دقيق 192×192 بالظبط.
2. حالة "شعار حقيقي غير مربع" (400×200 اختباريًا) — الأيقونة الناتجة (512×512) **مطابقة تمامًا للون الشعار الأصلي في المنتصف، بدون أي تشويه أو تمدد**.

**تنظيف كامل:** إزالة ملفات الأيقونات الثابتة القديمة (الخاطئة المصدر) من `public/`، وتحديث كل الإشارات (`manifest.json`, `index.html`, `service-worker.js`) للمسارات الديناميكية الجديدة — تم التأكد برمجيًا من عدم وجود أي إشارة متبقية للملفات القديمة.

**الحالة:** ✅ تم اكتشاف الفجوة، التصميم الديناميكي (يبقى متزامن تلقائيًا مع أي تغيير مستقبلي في الشعار)، التحقق الحي الشامل (فحص لوني دقيق على مستوى البكسل)، والتحقق النهائي الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء).

---

### 114. خط أبيض حوالين الصفحة كلها (يمين/شمال/تحت) — عنصر `html` مالوش لون خلفية محدد
**السياق:** المستخدم شارك صورة أوضحت وجود خط أبيض حوالين الصفحة بالكامل — يمين، شمال، وتحت — سؤال توضيحي أكّد إنه حول الصفحة نفسها، مش كارت معين.

**السبب الجذري:** `body` كان عندها لون خلفية صحيح (`--mkdd-page`)، لكن **`html` نفسها معندهاش أي لون خلفية محدد خالص** — بترث اللون الافتراضي للمتصفح (أبيض). بما إن `.app` (المحتوى الفعلي) عندها `max-width: 820px` مع `margin: auto`، بيظهر فراغ على الجنبين على الشاشات الأعرض من كده — وبما إن `min-height: 100vh` مش `100dvh`، ممكن يظهر فراغ صغير تحت كمان (شائع على الموبايل بسبب شريط العنوان الديناميكي). الفراغات دي كانت بتكشف لون `html` الافتراضي (أبيض)، مش لون الصفحة الصحيح.

**الحل:** إضافة `background: var(--mkdd-page);` لـ`html` نفسها، مش `body` بس — يضمن تغطية كاملة بنفس لون الخلفية الصحيح في كل الاتجاهات، بغض النظر عن أي فراغات ناتجة عن `max-width` أو اختلافات ارتفاع الـviewport.

**الحالة:** ✅ تم التشخيص الدقيق والإصلاح والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 115. حافة/ومضة فاتحة تظهر بس في وضع الـPWA — تعارض ألوان manifest.json مع الوضع الافتراضي الداكن للتطبيق
**السياق:** بعد إصلاح خط الصفحة الأبيض العام (#114)، أوضح المستخدم إن المشكلة لسه موجودة، لكن **في وضع الـPWA بس** — مش في المتصفح العادي خالص.

**السبب الجذري (اكتشاف دقيق):** التطبيق **افتراضيًا بالوضع الداكن** (أي إعداد غير "light" صراحة = داكن، من سكريبت `index.html` نفسه). لكن `manifest.json` (`background_color`, `theme_color`) و`theme-color` meta tag في `index.html` كانوا **جميعًا مضبوطين على ألوان الوضع الفاتح**. في وضع الـPWA (Standalone)، قيمة `background_color` بتُستخدَم كخلفية "شاشة البداية" (Splash Screen) **قبل** ما محتوى التطبيق الفعلي (الداكن) يتحمّل بالكامل — وهذا بالضبط ليه المشكلة ظهرت في الـPWA بس: **المتصفح العادي (تبويب Tab) مبيستخدمش قيمة `background_color` من `manifest.json` خالص**، هي مفهوم خاص بوضع التثبيت/Standalone فقط.

**الحل الشامل (3 أماكن):**
1. `manifest.json`: `background_color` و`theme_color` اتغيّروا لقيم الوضع الداكن الحقيقية.
2. `index.html`: نفس الإصلاح لقيمة `theme-color` الافتراضية، **بالإضافة** لتحديثها ديناميكيًا (عبر JavaScript) عشان تتطابق مع القيمة الفعلية المحفوظة، مش مجرد قيمة ثابتة.
3. `useTheme.ts`: إضافة تحديث ديناميكي لنفس الـ`theme-color` meta tag **لما المستخدم يبدّل الوضع أثناء الاستخدام الفعلي** (مش بس وقت التحميل الأولي) — عشان شريط الحالة/لون الـPWA يفضل متزامن مع أي تبديل لاحق، مش عالق على القيمة الافتراضية للأبد.

**قيد تقني موثّق صراحة:** `manifest.json` نفسها **ملف ثابت** بيتقرا مرة واحدة بس وقت تثبيت الـPWA — مش قابلة للتحديث الديناميكي زي الـmeta tag. يعني لو مستخدم بدّل للوضع الفاتح بعد تثبيت الـPWA بالفعل بألوان داكنة، شاشة البداية هتفضل داكنة (زي وقت التثبيت) لحد ما يعيد تثبيت التطبيق من جديد — قيد حقيقي في تقنية الـPWA نفسها، تم توثيقه بوضوح بدل تجاهله.

**الحالة:** ✅ تم التشخيص الدقيق (تمييز سلوك الـPWA عن المتصفح العادي)، الإصلاح الشامل في كل الأماكن الثلاثة (ثابت + ديناميكي وقت التحميل + ديناميكي أثناء الاستخدام)، توثيق القيد التقني المتبقي بصراحة، والتحقق الكامل (`npm test` → 77/77، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، `manifest.json` تحقّق JSON صحته).

---

### 116. ترتيب الموظفين في الشريط — شيري مش في مكانها الصحيح
**السياق:** المستخدم لاحظ إن ترتيب الموظفين في شريط الفريق صحيح للجميع ما عدا "شيري" (اللي اتضافت لاحقًا كموظفة رابعة عشرة) — المفروض تيجي بعد مريم مباشرة.

**اكتشاف مهم:** فحصت `list-employee-definitions.mjs` ولقيت إن الترتيب المعروض في الواجهة بيعتمد **بالكامل** على `fs.readdirSync()` — ترتيب نظام الملفات العشوائي (مش أبجدي ولا منطقي بالضرورة)، **مش** أي منطق ترتيب مقصود. **الأهم:** لقيت إن كل ملفات الموظفين عندها بالفعل حقل `order: N` صريح جوّه الـFrontmatter بتاعها — نظام ترتيب كامل **موجود بالفعل**، لكن **الـBackend مكنش بيقراه أو يستخدمه خالص**! يعني الترتيب الصحيح للـ13 موظف الأصليين كان **صدفة بحتة** (تطابق ترتيب الملفات على القرص مع ترتيب الإنشاء الفعلي)، مش نتيجة منطق ترتيب حقيقي — وده اللي انكشف بمجرد ما أُضيف موظف رابع عشر (شيري) بعد كده.

**الحل على مستويين:**
1. **تصحيح قيم `order`:** شيري (`content-writer`) كانت `order: 14` (آخر واحدة)، وتم تغييرها لـ`order: 4` (بعد `ui-ux`/مريم مباشرة، اللي عندها `order: 3`)، مع إزاحة كل الموظفين من `architect` (كانت 4) لـ`release-manager` (كانت 13) بمقدار +1 لكل واحد — ضمانًا لتسلسل متصل 1-14 بدون أي فجوة أو تكرار (تم التحقق برمجيًا).
2. **إصلاح جذري في `list-employee-definitions.mjs`:** الدالة بقت تقرأ حقل `order` الفعلي من كل ملف وترتب بيه، بدل الاعتماد على ترتيب نظام الملفات العشوائي — إصلاح للمصدر الوحيد المشترك (`listEmployeeNames`) المُستخدَم في كل من `/api/employees` وسكريبت الـbootstrap، فالإصلاح يسري تلقائيًا على الاثنين.

**اختبار انحدار (Regression Test) جديد:** أضفت اختبار مخصص يحاكي بالضبط نفس السيناريو الحقيقي — ملف اسمه بيترتب **أولًا** أبجديًا/بترتيب الملفات، لكن حقل `order` بتاعه بيقول إنه المفروض يكون **آخر واحد** — للتأكد إن الترتيب فعليًا مبني على الحقل، مش على أي تطابق عرضي مع الترتيب الأبجدي.

**تم التحقق حيًا وبدقة كاملة:**
1. اختبار الوحدة الجديد نجح (ضمن 78/78 اختبار كامل بعد الإضافة، بدل 77 الأصليين).
2. اختبار حي مباشر بملفات الموظفين **الحقيقية** الفعلية (بعد التعديل) — أكّد الترتيب النهائي الصحيح بالكامل: 1.product-manager → 2.business-analyst → 3.ui-ux → **4.content-writer (شيري)** → 5.architect → ... → 14.release-manager.

**الحالة:** ✅ تم اكتشاف السبب الجذري (نظام ترتيب موجود لكن غير مُستخدَم)، تصحيح البيانات (قيم `order` الفعلية)، الإصلاح البرمجي الجذري، إضافة اختبار انحدار حقيقي، والتحقق الكامل المزدوج (اختبار وحدة + اختبار حي بالبيانات الفعلية). التحقق النهائي: `npm test` → 78/78، `node --check` نظيف، `oxlint` 0 أخطاء.

---

### 117. توضيح مفهوم "قفل التقرير" — هو التوقيع الفعلي، مش مجرد أرشفة
**السياق:** المستخدم طرح سؤالًا معماريًا مهمًا للنقاش (مش تنفيذ مباشر) — لو مريم/شيري/شنودة عدّلوا في شغل معتمد بالفعل (زي السيناريو الحقيقي اللي حصل مع كيرلس ومريم قبل كده)، هل توقيع المالك الأصلي لسه صالح، أو محتاج توقيع جديد على كل تعديل؟

**النقاش والقرار:** بعد سؤال توضيحي، أكّد المستخدم إن **قفل التقرير نفسه (اللي بيعمله المالك من الواجهة) هو التوقيع الكافي** على أي تعديل لاحق — مش محتاجين آلية "إعادة فتح البوابة" منفصلة. كل تعديل بعد الاعتماد بيمر عبر نظام التقارير الموجود بالفعل (تقرير → رد الموظف → قفل المالك)، وقفل المالك للتقرير **هو** فعليًا موافقته على التعديل ده تحديدًا.

**التنفيذ:**
- تغيير نص زرار القفل من **"قفل نهائي"** (غامض، بيوحي بأرشفة بس) لـ**"اعتماد التعديل وقفل التقرير"** — يعكس المعنى الحقيقي للفعل بوضوح.
- تحديث `AGENTS.md` §6 بتوضيح صريح لكل الموظفين: قفل المالك للتقرير **هو** اعتماده الفعلي للتعديل، وبيقف بذاته من غير الحاجة لإعادة فتح أي بوابة، حتى لو التعديل كان في مرحلة اتعمدت بالفعل.

**الحالة:** ✅ تم النقاش المعماري الكامل، الوصول لقرار واضح، وتنفيذه في الواجهة وتعليمات الموظفين (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، ترقيم `AGENTS.md` سليم).

---

### 118. اسم المشروع وزرار الرجوع في بار التنقل — مش واضحين بصريًا
**السياق:** المستخدم طلب تحسين وضوح بار التنقل (اسم المشروع + زرار الرجوع) في صفحة المشروع الرئيسية — اسم المشروع أكبر وبلون أوضح، وزرار الرجوع بلون واضح.

**السبب:** اسم المشروع كان بحجم صغير جدًا (`13px`) وبلون رمادي باهت جدًا (`--mkdd-text-muted`، مصمم أصلًا للنصوص الثانوية غير المهمة). زرار الرجوع كان بلون رمادي متوسط محايد (`--mkdd-text-secondary`)، مش مميز كعنصر تفاعلي واضح.

**الحل:**
- اسم المشروع: الحجم من `13px` لـ`17px`، مع خط عريض (`font-weight: 700`)، واللون من الرمادي الباهت لـ`--mkdd-text-primary` (اللون الأساسي الواضح).
- زرار الرجوع: لون مميز (`--mkdd-accent`) للحدود والأيقونة، مع خلفية ملوّنة خفيفة (نفس أسلوب `color-mix` المُستخدَم عبر التطبيق) بدل اللون المحايد السابق.

**الحالة:** ✅ تم التنفيذ والتحقق الكامل (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 119. تغيير الـLLM من الواجهة "مبيثبتش" — سكريبت الـBootstrap كان بيكتب فوقه
**السياق:** المستخدم اشتكى إن الموظف بيفضل عنده LLM ثابت (Hardcoded) رغم إنه غيّره من واجهة OpenHands نفسها.

**السبب الجذري (اكتشاف دقيق):** `bootstrap-employees.mjs` كان بيبعت `llm_profile_ref` **صراحة في كل مرة يتشغّل فيها** (سواء لموظف جديد أو موجود بالفعل)، وبما إن `POST /api/agent-profiles/{name}` هي **Upsert حقيقي** (بتستبدل كل الحقول، مش دمج جزئي) — **أي تشغيل تاني للسكريبت كان بيكتب فوق أي تغيير يدوي للموديل من الواجهة، ويرجّعه للقيمة الثابتة الافتراضية** (`MKDD_BOOTSTRAP_LLM_PROFILE_REF`) بدون قصد.

**الحل:** السكريبت بقى **يجلب القيمة الحالية أولًا** (`GET /api/agent-profiles/{name}`) لكل موظف قبل أي تحديث:
- لو الموظف **عنده Profile موجود بالفعل** — بيحافظ على `llm_profile_ref` الحالي بتاعه، **بغض النظر عن القيمة الافتراضية**.
- لو الموظف **جديد تمامًا** (مفيهوش Profile خالص) — بيستخدم القيمة الافتراضية (لأن الـAPI برفض إنشاء Profile فاضي من غير قيمة LLM، مؤكَّد بـ422 حقيقي).

**تم التحقق حيًا بدقة تامة:** محاكاة كاملة لسيناريو واقعي — موظف عنده تغيير يدوي مسبق (`claude-opus-4-8`، مختلف عن الافتراضي) وموظف جديد تمامًا. النتيجة: الموظف الموجود **احتفظ بقيمته اليدوية بالضبط**، والموظف الجديد **استخدم الافتراضي** — بالضبط زي المطلوب.

**تحسين إضافي:** رسائل الـlog بقت توضح لكل موظف القيمة الفعلية المُستخدَمة، عشان يقدر المالك يتأكد بصريًا إن التغييرات اليدوية محفوظة بعد أي تشغيل مستقبلي للسكريبت.

**الحالة:** ✅ تم التشخيص الدقيق للسبب الجذري، الإصلاح المعماري الصحيح (Upsert يحترم القيمة الحالية)، التحقق الحي الكامل بمحاكاة سيناريو واقعي، والتحقق النهائي الكامل (`npm test` → 78/78، `tsc -b` نظيف، `node --check` نظيف، `oxlint` 0 أخطاء).

---

### 120. حافة بيضاء مستمرة في وضع الـPWA — مشكلة مختلفة عن شاشة البداية، محتاجة `viewport-fit=cover`
**السياق:** المستخدم أكّد إن الحافة البيضاء رجعت في الـPWA بعد حذف وإعادة تثبيت التطبيق، رغم إن `manifest.json` والـ`theme-color` (إصلاح #115) لسه صحيحين تمامًا على GitHub.

**اكتشاف السبب الحقيقي (مختلف تمامًا عن #115):** فحصت `index.html` ولقيت إن **`viewport-fit=cover` مش موجودة خالص** في meta tag الـviewport. بدون الإعداد ده، على الأجهزة اللي فيها حواف مدورة/حزّة (Notch)/شريط إيماءات — **المتصفح العادي (Tab)** بيغطي المناطق دي بواجهته الخاصة، فمفيش مشكلة ظاهرة. لكن **وضع الـPWA (Standalone)** بيشيل واجهة المتصفح بالكامل، فمن غير `viewport-fit=cover`، الصفحة **مبتمدش فعليًا** لتلك المناطق الآمنة، ويظهر بدالها لون افتراضي أبيض للنظام. هذا **مستمر طول الاستخدام** (مش ومضة لحظية زي مشكلة شاشة البداية في #115)، وهذا الفرق الجوهري بين المشكلتين رغم تشابه الأعراض ظاهريًا.

**الحل:** إضافة `viewport-fit=cover` لـmeta tag الـviewport — يخلي الصفحة تمتد فعليًا تحت كل المناطق الآمنة، والخلفية الصحيحة (الداكنة، من إصلاح #114) بتغطيها بالكامل.

**الحالة:** ✅ تم التمييز الدقيق بين هذه المشكلة ومشكلة شاشة البداية السابقة (#115) رغم تشابه الأعراض، والتشخيص والإصلاح الصحيح، والتحقق الكامل (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء).

---

### 121. المحادثة بتاخد وقت لحد ما تتحمّل — تحميل تاريخ كامل بدل آخر رسائل بس
**السياق:** المستخدم اشتكى إن فتح أي محادثة بياخد وقت ملحوظ، وطلب إنها تكون زي أي تطبيق محادثة حقيقي — تظهر فورًا.

**اكتشاف السبب الجذري (سببين مجتمعين):**
1. **طلبين HTTP منفصلين ومتتاليين**: `fetchConversation` ثم (بعد ما يخلص بالكامل) `fetchEvents` — تأخير مضاعف بلا داعٍ.
2. **المشكلة الأكبر**: `fetchAllNormalizedEvents` كانت بتجيب **تاريخ المحادثة بالكامل** (حلقة صفحات متتالية، 100 حدث في المرة، Sequential) — لمحادثة طويلة (زي كيرلس اللي شغال من فترة)، ده معناه عشرات الطلبات المتتالية لمجرد فتح المحادثة.

**اكتشاف حاسم أثناء التشخيص:** فحصت الكود المصدري الأصلي لـOpenHands Agent Canvas (`event-service.api.ts`) ولقيت إن الـAPI **بتدعم فعليًا** `sort_order=TIMESTAMP_DESC` (الأحدث أولًا) — ميزة موجودة بالكامل، لكن المشروع مكنش بيستخدمها خالص.

**الحل المعماري (تجاوز مجرد "التوازي"):** أول محاولة كانت تشغيل الطلبين بالتوازي (`Promise.all`)، لكن اكتشفت إن الطلب الثاني أصلًا **محتاج نتيجة الأول** (الـConversation ID) — فالتوازي الحقيقي مستحيل تقنيًا. الحل الصحيح: **دمج الطلبين في Endpoint واحد بس** (`GET /api/chat/open`) — بيحل المحادثة مرة واحدة، ويجيب آخر 50 رسالة بس معاها في نفس الاستجابة.

**نظام تحميل تدريجي كامل (زي أي تطبيق محادثة):**
- **Backend**: `fetchRecentEvents`/`fetchOlderEvents` جديدتين (منفصلتين عن `fetchAllNormalizedEvents` القديمة، اللي فضلت زي ما هي لباقي الاستخدامات — خطة العمل وإشعار عدم القراءة، محتاجين التاريخ الكامل فعليًا).
- **Frontend**: تحميل أولي فوري (آخر 50 رسالة)، تحميل رسائل أقدم تلقائيًا لما المستخدم يسحب لأعلى القائمة.
- **حل تحدي "قفزة التمرير" (Scroll Jump)**: لما رسائل أقدم تتضاف فوق القائمة، تم بناء آلية تعويض دقيقة (حفظ الارتفاع قبل التحميل، تعديل موضع التمرير بالفرق) — المستخدم يفضل شايف نفس الرسائل بالظبط، مش يتنقل فجأة.

**3 أخطاء حقيقية اتلقت وصُححت أثناء البناء (بالفحص المباشر، مش الافتراض):**
1. كتابة `fetchConversation` **مرتين منفصلتين بالغلط** بدل استخدام نتيجة واحدة مشتركة — كان هيزوّد طلب إضافي بدل ما يقلله.
2. عملية تعديل نصي كسرت بنية ملف الأنواع (`types/index.ts`) لحظيًا (حذفت `EventsResponse` بالغلط) — اتصلحت فورًا واتأكد من السلامة بـ`tsc`.
3. **الأهم**: تمرير `null` بدل `undefined` لدالة `applyIncomingEvents` عند تحميل رسائل أقدم — كان هيمسح خطة العمل الحالية بالغلط (بما إن الدالة بتفحص `!== undefined`، و`null !== undefined`). اتصلح بتوسيع الـSignature لتقبل `undefined` بشكل صريح ونظيف (وأزلت كمان Hack قبيح كان موجود من قبل في مكان تاني بنفس المشكلة).
4. اكتشاف إن `.catch()` على `loadOlderMessages()` **مستحيل يتفعّل أبدًا** (الدالة بتاخد أخطائها جوّاها من غير `re-throw`) — استُبدِلت بمراقبة `loadingOlder` نفسها (المؤشر الموثوق الوحيد لـ"انتهت المحاولة") لتنظيف حالة تعويض التمرير في حالة الفشل.

**تم التحقق حيًا بدقة شاملة:**
1. اختبار الترقيم (Pagination) — محادثة وهمية من 120 رسالة، تأكيد صحة نطاقات الرسائل والترتيب و`hasMore` عبر صفحتين متتاليتين.
2. اختبار الـEndpoint المدمج الجديد — طلب واحد بس رجّع بيانات المحادثة + آخر 50 رسالة بدقة تامة.

**الحالة:** ✅ تم اكتشاف السبب الجذري بعمق (شامل اكتشاف ميزة API غير مُستخدَمة)، التصميم المعماري الصحيح (دمج بدل توازي جزئي)، بناء نظام تحميل تدريجي كامل، حل تحدي قفزة التمرير، اكتشاف وتصحيح 4 أخطاء حقيقية بالفحص المباشر، والتحقق النهائي الكامل (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 122. التأخير عند فتح المحادثة — تصحيح التشخيص، السبب الحقيقي مختلف تمامًا عن #121
**السياق:** بعد نشر إصلاح #121 (تحميل آخر رسائل بس بدل التاريخ الكامل)، أبلغ المستخدم صراحة إن "مافيش تحسن باين"، وطلب بحث حقيقي قبل أي حل جديد.

**تصحيح مهم للتشخيص السابق:** سؤال توضيحي كشف إن المحادثة المُختبَرة **قصيرة جدًا** (أقل من 20 رسالة) — يعني نظرية #121 (تحميل تاريخ طويل) **مالهاش أي تأثير حقيقي** على هذه الحالة خالص، بما إن الفرق بين "تحميل الكل" و"تحميل آخر 50" لمحادثة من 20 رسالة هو صفر عمليًا. إصلاح #121 يفضل مفيد وصحيح للمحادثات الطويلة مستقبلًا، لكنه مش السبب الحقيقي وراء الشكوى الحالية.

**اكتشاف السبب الحقيقي (بحث عميق في مكان مختلف تمامًا):** `findAuthorizedConversation` — الدالة المشتركة اللي بتحدد "أي محادثة تخص هذا المشروع/الموظف" — بتدوّر عبر **كل محادثات النظام** (مش بس محادثة واحدة)، صفحة بصفحة (100 في المرة)، لحد ما تلاقي المطلوبة. **المشكلة الجوهرية:** الكود مكنش بيحدد `sort_order` صراحة خالص، فبيعتمد على القيمة الافتراضية للسيرفر — ولقيت **تعليق موثّق من شغل سابق يؤكد صراحة** إن هذه القيمة الافتراضية هي `CREATED_AT_DESC` (ترتيب الإنشاء)، **مش** `UPDATED_AT_DESC` (ترتيب آخر تحديث) اللي الواجهة الأصلية لـOpenHands بتحددها صراحة بنفسها (تأكدت من كودها المصدري).

**التأثير العملي:** أي محادثة **قديمة الإنشاء لكن نشطة حاليًا** (زي موظف شغال على مشروع من فترة طويلة) بترتيب الإنشاء الافتراضي هتكون **بعيدة جدًا عن أول القائمة**، رغم إنها فعليًا "المحادثة النشطة الآن" — والبحث هيحتاج يمر عبر عشرات الصفحات (كل صفحة = طلب شبكة كامل ومنفصل) قبل ما يلاقيها، **بغض النظر عن طول المحادثة نفسها**.

**الحل:** إضافة `sort_order: "UPDATED_AT_DESC"` صراحة في كل استدعاءات `/api/conversations/search` (في `findAuthorizedConversation` و`findAllProjectConversations`). بترتيب آخر تحديث، أي محادثة نشطة (بغض النظر عن قِدَم إنشائها) بتكون **دايمًا قريبة جدًا من أول القائمة** تلقائيًا، بما إن أي رسالة جديدة بترقّيها لأعلى القائمة فورًا.

**تم التحقق: هل هذا يكسر أي سلوك موجود؟** فحصت تعليقين موثّقين من شغل سابق كانا بيعتمدوا على السلوك القديم (ميزة "بدء محادثة جديدة" لموظف عالق) — أكّدت إن التغيير **لا يكسر** هذا السلوك، بما إن محادثة جديدة اتعملت للتو، آخر تحديث ليها = وقت إنشائها نفسه، فلسه هتظهر أولًا في كلا الترتيبين.

**تم التحقق حيًا بدقة قاطعة (مقارنة مباشرة قبل/بعد):** محاكاة واقعية بـ500 محادثة، الهدف اتعملت بدري (قريبة من آخر ترتيب الإنشاء) لكن اتحدثت مؤخرًا جدًا:
- **مع الإصلاح**: طلب شبكة **واحد بس** للعثور على المحادثة.
- **بدون الإصلاح (محاكاة السلوك القديم)**: **6 طلبات شبكة متتالية** مطلوبة.

في بيئة إنتاج حقيقية (بخلاف بيئة الاختبار المحلية بزمن استجابة شبه معدوم)، الفرق بين طلب واحد و6 طلبات متتالية = ثوانٍ إضافية ملموسة — بالضبط نوع التأخير اللي وصفه المستخدم.

**الحالة:** ✅ تم تصحيح التشخيص السابق بصراحة بعد تأكيد المستخدم إنه غير كافٍ، بحث عميق جديد كشف السبب الحقيقي (اعتماد على قيمة افتراضية غير موثّقة/غير صحيحة للسيرفر)، التحقق من عدم كسر أي سلوك موجود، والتحقق الحي القاطع بمقارنة مباشرة (1 طلب مقابل 6). التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء.

---

### 123. بطء عام في فتح أي صفحة، حتى محليًا — التطبيق كان شغال بوضع التطوير طول الوقت
**السياق:** المستخدم أوضح إن البطء مش مقتصر على المحادثة بس — فتح أي صفحة جديدة في MKDD UI نفسها، **حتى على الشبكة المحلية**، فيه تأخير ملحوظ.

**السبب الجذري:** فحصت `Dockerfile` ولقيت إن التطبيق **شغال بوضع التطوير (`npm run dev` — Vite Dev Server) طول الوقت**، مش نسخة إنتاج حقيقية (Production Build) خالص. في وضع التطوير، Vite **مبيجمّعش أو يصغّر الكود خالص** — كل ملف/Component بيتقدم كملف JavaScript **منفصل تمامًا** (ES Modules غير مُجمَّعة)، فأي صفحة معقدة (بعد كل الميزات اللي بُنيت طول الجلسة) تعني **عشرات أو مئات الطلبات المنفصلة**، بدل ملف واحد مُجمَّع ومُحسَّن. هذا التأخير التراكمي بيبقى ملموس حتى على شبكة محلية سريعة. هذا فجوة موثّقة بالفعل من الأول في المشروع ("Production frontend build" كانت مذكورة كباقية) لكن لم يتم التعامل معها لحد الآن.

**الحل — نسخة إنتاج حقيقية:**
- **`server/static-files.mjs` جديد**: بيقدّم ملفات `dist/` (نتيجة `vite build`) كملفات ثابتة مُجمَّعة، مع حماية كاملة من هجمات Path Traversal (يتأكد إن المسار المطلوب لسه جوّه `dist/` قبل ما يقدّمه)، وFallback آمن لـ`index.html` لأي مسار غير موجود (آمن تمامًا هنا، بما إن التوجيه في هذا التطبيق بالكامل عبر Query String `?project=...`، مش عبر المسار نفسه — تم التأكد من هذا صراحة قبل اعتماد هذا التصميم).
- **`Dockerfile`**: البناء (`npm run build`) بيحصل **مرة واحدة بس وقت بناء صورة الحاوية**، مش في كل مرة تشتغل الحاوية. السيرفر بقى يشغّل بس `node server/index.mjs` — بدون Vite Dev Server خالص.
- **`compose.yml`**: بورت 5173 (اللي كان مخصص لـVite Dev Server) اتشال بالكامل — كل حاجة (الواجهة + الـAPI) بتتقدم دلوقتي من بورت 8787 بس.

**⚠️ تأثير جانبي أمني مهم موثّق صراحة:** نظام "الدومينات المسموحة" (`allowedHosts`) اللي بُني في #108/#109 كان بالكامل إجراء أمان خاص بحماية **Vite Dev Server تحديدًا** من هجمات DNS Rebinding. بما إن Vite Dev Server مبقاش شغال خالص بعد هذا التغيير، **هذا النظام مبقاش له أي تأثير فعلي على الأمان** — الخطر الأصلي اللي كان بيحمي منه لم يعد قائمًا (خادم الملفات الثابتة الجديد نوع مختلف تمامًا من المخاطر، ومش بيحتاج نفس نوع الحماية). **الحماية الحقيقية المتبقية والمستقلة تمامًا** (فحص `Host` header للوصول المحلي فقط، من #112) **لسه شغالة زي ما هي بدون أي تغيير**، بما إنها في الـBackend نفسه، مش في Vite.

**تم التحقق حيًا بدقة شاملة:**
1. بناء إنتاج فعلي — تأكيد الفرق الهائل في عدد الملفات (3 ملفات أساسية بس، بدل عشرات/مئات في وضع التطوير).
2. اختبار حي كامل للسيرفر الجديد على الملفات المبنية فعليًا — 5 سيناريوهات: تقديم `index.html` من الجذر، تقديم ملف JS حقيقي بـMIME Type صحيح، Fallback صحيح لمسار بـQuery String، **رفض قاطع لمحاولة Path Traversal** (رجّعت `index.html` الآمن بدل أي ملف حساس)، وتأكيد إن مسارات الـAPI لسه شغالة صح ومش بتتبلع من الـHandler الجديد.

**الحالة:** ✅ تم اكتشاف السبب الجذري بعمق (فحص مباشر لطريقة التشغيل الفعلية)، بناء نظام تقديم ملفات إنتاج كامل وآمن، تحديث `Dockerfile`/`compose.yml`، توثيق صريح للتأثير الجانبي الأمني، والتحقق الحي الشامل (5 سيناريوهات حرجة). التحقق النهائي: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء، `compose.yml` YAML صحيح.

---

### 124. البطء العام في كل التطبيق (حتى صفحة بمشروعين بس!) — الاكتشاف الأهم: مفتاح الجلسة كان بيُكتشَف من الصفر مع كل طلب API واحد
**السياق:** بعد إصلاح #123 (نسخة إنتاج حقيقية)، أوضح المستخدم إن **مافيش أي تحسن في سرعة الاستجابة خالص** — حتى صفحة المشاريع (مشروعين بس) بطيئة، وده دليل قاطع إن المشكلة **مش في حجم الملفات المُحمَّلة**، بل في **سرعة رد الخادم نفسه على كل طلب**.

**اكتشاف السبب الجذري (الأهم في الجلسة كلها):** فحصت `sessionKey()` — الدالة المسؤولة عن الحصول على مفتاح الاتصال مع OpenHands، **ولقيتها بتُستدعى في كل طلب API واحد بدون استثناء، وكل مرة بتعمل مسح كامل لكل العمليات الشغالة على النظام كله** (`fs.readdirSync("/proc")` + قراءة `cmdline` لكل عملية) بحثًا عن العملية الصحيحة — **مفيش أي تخزين مؤقت (Caching) خالص**! بما إن أي صفحة بسيطة بتحتاج عدة طلبات API متتالية (والصفحة الرئيسية بتعمل استعلامات دورية كل 5 ثواني لـ14 موظف)، هذا المسح المتكرر من الصفر مع كل طلب كان بيضيف تأخير تراكمي حقيقي **مستقل تمامًا عن حجم البيانات المطلوبة أو عدد ملفات الواجهة** — وهذا بالضبط ليه إصلاح #123 (تجميع الملفات) مالوش أي تأثير على المشكلة الحقيقية.

**الحل:** تخزين مفتاح الجلسة مؤقتًا في الذاكرة بعد أول اكتشاف ناجح له (المفتاح ثابت طول عمر الحاوية، مش بيتغيّر إلا لو حاوية Agent Canvas اتعاد تشغيلها). لضمان عدم كسر أي شيء لو المفتاح بقى فعليًا غير صالح (بعد إعادة تشغيل)، أُضيفت آلية **إعادة محاولة تلقائية**: لو أي طلب فشل بـ401/403 والمفتاح المستخدم كان من الذاكرة المؤقتة، يتم مسح الذاكرة المؤقتة تلقائيًا وإعادة اكتشاف مفتاح جديد فورًا وإعادة المحاولة — بدون أي تدخل يدوي.

**تم التحقق حيًا بدقة قاطعة (قياس فعلي، مش تقدير):**
- الاستدعاء الأول (المسح الحقيقي لـ`/proc`): **19.24ms**.
- 1000 استدعاء لاحق (من الذاكرة المؤقتة): **0.093ms إجمالاً** (0.0001ms لكل استدعاء بالمتوسط).
- **فرق السرعة: 206,000 ضعف تقريبًا لكل استدعاء واحد**، حتى في بيئة اختبار بسيطة قليلة العمليات — على سيرفر حقيقي (عمليات أكتر بكثير)، الفرق المطلق أكبر من كده بكثير.

**لماذا هذا الاكتشاف الأهم:** هذا التأخير كان بيؤثر على **كل طلب API واحد في التطبيق بالكامل** (كل صفحة، كل تحديث دوري لشريط الفريق، كل رسالة)، مش مقتصر على المحادثة أو أي ميزة معينة — وهذا يفسّر بدقة ليه الإصلاحات السابقة (تحسين تحميل المحادثة، ترتيب البحث، تجميع الملفات) كل واحدة منهم ساعدت في مكانها لكن مالحقتش المشكلة الأساسية الحقيقية اللي كانت بتؤثر على كل شيء في نفس الوقت.

**الحالة:** ✅ تم اكتشاف السبب الجذري الأعمق والأهم عبر بحث دقيق (مش تخمين)، بناء حل آمن ومتين (تخزين مؤقت + إعادة محاولة تلقائية عند الفشل)، والتحقق الحي بقياس زمني دقيق وحقيقي (مش تقديري). التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف، `oxlint` 0 أخطاء.

---

### 125. توضيح لماذا روابط "التطبيق الحي" القديمة تتوقف — وإضافة تنبيه بصري
**السياق:** المستخدم لاحظ إن رابط معاينة قديم بتاع كيرلس مش بيفتح على الآيبي المحلي، رغم إن رابط مريم (نموذج ثابت) شغال عادي.

**التشخيص:** فحصت طريقة بناء الروابط في `ChatScreen.tsx` ولقيت السبب الحقيقي — فيه نوعين مختلفين تمامًا من روابط المعاينة:
1. **روابط مريم (نموذج ثابت — HTML/CSS)**: ملفات محفوظة فعليًا على القرص، فتفضل شغالة للأبد بغض النظر عن حالة المحادثة.
2. **روابط كيرلس (تطبيق حي — سيرفر حقيقي شغال)**: مرتبطة بعملية سيرفر حقيقية شغالة **طول ما المحادثة الأصلية نشطة فقط**. لما المحادثة تنتهي (أو تتوقف)، العملية اللي كانت بتشغّل السيرفر بتقف تلقائيًا — لكن نص الرابط بيفضل محفوظ في المحادثة القديمة كتاريخ، فيبان "موجود" لكنه فعليًا "ميت". أكّد المستخدم إن الرسالة كانت من يومين — فترة كافية جدًا لانتهاء أي جلسة عمل نشطة.

**تم استبعاد السبب المحتمل ذو الصلة بالتغييرات الأخيرة:** تأكدت من عدم تأثر نطاق البورتات (4001-4020) بأي تعديل في `compose.yml`، وإن الرابط نفسه بيتبنى ديناميكيًا وقت العرض (مش من بيانات قديمة مخزَّنة) — فالمشكلة **مش ناتجة عن أي تعديل حديث خالص**، هي سلوك طبيعي متوقع للفرق الجوهري بين النوعين.

**الحل — تنبيه بصري (بطلب المستخدم):** كارت رابط "التطبيق الحي" بقى يفحص عمر الرسالة (الفارق الزمني منذ إرسالها). لو أكتر من ساعة، الشارة بتتغيّر من "شغال الآن" (أخضر) لـ**"قد يكون منتهي"** (أصفر تحذيري)، مع تغيير لون الكارت بالكامل ليعكس الحالة — بدل ما يبان دايمًا "شغال" بشكل مضلِّل، بغض النظر عن عمر الرسالة الفعلي.

**تم التحقق حيًا بدقة:** محاكاة منطقية شاملة لـ5 حالات — رسالة من يومين (سيناريو المستخدم الحقيقي، صح إنها "منتهية")، رسالة من 5 دقايق (صح إنها "لسه شغالة")، رسالة من ساعتين بالظبط (حد فاصل، صح إنها "منتهية")، رسالة الآن (صح "شغالة")، ورسالة من غير أي وقت مسجَّل (Fallback آمن، بدون تنبيه كاذب). كل الحالات صحّت 100%.

**الحالة:** ✅ تم التشخيص الدقيق (تمييز نوعي روابط المعاينة، استبعاد أي علاقة بالتغييرات الأخيرة)، تنفيذ التنبيه البصري المطلوب، التحقق المنطقي الحي الشامل، والتحقق النهائي الكامل (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد).

---

### 126. تنظيف ميزة "الدومينات المسموحة" المعطّلة — الخطوة الأولى من الأولويات المتبقية
**السياق:** بعد نقاش عام حول "إيه الناقص في المشروع"، اكتُشف إن ميزة "الدومينات المسموحة" (اللي بُنيت في #108/#109) **فقدت تأثيرها الفعلي بالكامل** بعد التحول لنسخة إنتاج حقيقية (#123، شلنا Vite Dev Server كليًا) — الميزة كانت بالكامل بتحمي Vite Dev Server تحديدًا، اللي مبقاش شغال خالص. لكنها ظلت ظاهرة في السايدبار، وده كان ممكن يضلّل المالك يفتكر إنها لسه بتتحكم في حاجة فعلية.

**التنظيف:**
- شيل `server/site-config.mjs` بالكامل (التخزين الدائم للدومينات، مبقاش له أي قارئ خالص).
- شيل 3 Endpoints معطّلة من `routes/settings.mjs` (`GET/POST/POST-remove /api/settings/allowed-hosts`)، مع **الاحتفاظ الكامل** بـ`handleRestartContainer` (لسه مفيد حقيقيًا لأي تحديث إعدادات مستقبلي).
- شيل 3 دوال Client معطّلة من `client.ts` (`fetchAllowedHosts`, `addAllowedHostSetting`, `removeAllowedHostSetting`).
- استبدال `DomainSettingsModal.tsx` (200 سطر، إدارة دومينات + إعادة تشغيل) بـ`RestartModal.tsx` مبسّط (بس إعادة التشغيل، بدون أي إشارة لميزة معطّلة).
- تبسيط `vite.config.ts` — شيل منطق قراءة `site-config.json` (كان دايمًا هيرجّع مصفوفة فاضية بما إن الملف مبقاش بيتكتب من أي حد)، مع **الاحتفاظ** بدعم متغيّر البيئة `MKDD_ALLOWED_HOSTS` (لسه مفيد لو مطوّر شغّل `npm run dev` محليًا بره Docker).

**تم التحقق حيًا:** 3 سيناريوهات — زرار إعادة التشغيل لسه شغال صح (200)، الـEndpoint القديم المعطّل اختفى تمامًا (سقط لصفحة الواجهة الرئيسية بدل أي رد API، تأكيد الحذف الكامل)، وتقديم الملفات الثابتة (#123) لسه سليم بدون أي تأثر.

**الحالة:** ✅ تم التنظيف الكامل والآمن (بدون كسر أي وظيفة حقيقية باقية)، والتحقق الحي الشامل. التحقق النهائي: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح (حجم أصغر بعد الحذف)، `node --check` نظيف، `oxlint` 0 أخطاء.

---

### 127. نظام تسجيل دخول كامل — أهم أولوية أمنية متبقية في المشروع
**السياق:** بعد نقاش شامل حول أولويات المشروع المتبقية، اتفقنا إن **مفيش أي تسجيل دخول خالص** كانت أهم فجوة أمنية — بما إن الدومين عام (متاح للإنترنت بالكامل) وبدون أي حماية، أي حد يعرف الرابط يقدر يستخدم كل الميزات (المحادثات، اعتماد البوابات، بيانات المشاريع). بعد نقاش قصير، تقرر: نظام **اسم مستخدم + كلمة سر** (يدعم أكتر من شخص)، مع **شاشة إعداد أول مرة** من الواجهة نفسها (بدل ضبطها من السيرفر).

**التصميم المعماري:**
- **تخزين المستخدمين**: `server/lib/auth-store.mjs` جديد — تشفير كلمات السر بـ`crypto.scrypt` **المدمجة في Node.js** (بدون أي مكتبة خارجية إضافية)، مع Salt عشوائي منفصل لكل مستخدم، ومقارنة زمنية ثابتة (`timingSafeEqual`) لمنع تسريب معلومات عبر توقيت الاستجابة.
- **الجلسات**: توكن عشوائي (32 بايت) لكل جلسة، محفوظ بشكل دائم (يفضل الدخول شغال حتى بعد إعادة تشغيل الحاوية)، صالح لـ90 يوم.
- **الكوكي**: `HttpOnly` (حماية حقيقية من XSS، الجافاسكريبت مايقدرش يقراها خالص) + `SameSite=Lax` (حماية من CSRF).
- **بوابة الحماية المركزية**: فحص واحد في `index.mjs` بيتنفذ **قبل** أي Route، بيرفض أي طلب لـ`/api/...` (عدا 3 استثناءات: حالة الدخول، الإعداد، تسجيل الدخول نفسها) لو مفيش جلسة صالحة — **حماية حقيقية على مستوى الخادم، مش مجرد إخفاء في الواجهة**.
- **حماية الـWebSocket كمان**: المحادثة الحية بتُفحَص بنفس آلية المصادقة عند لحظة الاتصال، مش بس الـREST APIs.
- **الفرونت إند**: `AuthGate.tsx` جديد يغلّف التطبيق بالكامل — بيفحص حالة المصادقة **قبل** ما أي جزء من التطبيق (`App.tsx` وكل الـHooks بتاعتها) يبدأ يشتغل خالص، تجنبًا لكسر "قواعد الـHooks" في React.
- **أول حساب فقط من شاشة الإعداد** — أي حساب إضافي بعد كده لازم يتضاف من مستخدم مسجّل دخول بالفعل (منع أي شخص غريب يوصل للتطبيق من إنشاء حساب لنفسه بعد ما الإعداد يخلص).

**تم التحقق حيًا بدقة قاطعة (12+10 سيناريو):**
1. اختبار وحدة كامل لدورة حياة `auth-store.mjs` (12 سيناريو) — إنشاء مستخدم، رفض اسم مكرر، رفض كلمة سر قصيرة، تسجيل دخول صحيح/خاطئ، تطابق أسماء بغض النظر عن حالة الأحرف، إنشاء/فحص/إبطال الجلسات، إضافة/حذف مستخدمين.
2. **اختبار حي شامل للنظام بالكامل عبر HTTP فعلي** (10 سيناريوهات) — تأكيد الاستثناءات الثلاثة متاحة بدون تسجيل دخول، كل شيء تاني (شامل إدارة المستخدمين نفسها) مرفوض بـ401 بدون جلسة، الإعداد الأول ينجح ويرجّع كوكي صالح، الوصول المحمي ينجح بالكوكي، منع الإعداد المتكرر (409)، فحص الحالة يعكس الواقع بدقة، تسجيل الخروج يبطّل الجلسة فعليًا.

**الحالة:** ✅ تم بناء نظام مصادقة كامل وآمن من الصفر (Backend+Frontend+WebSocket)، بدون أي مكتبات خارجية إضافية، مع تحقق حي شامل جدًا لكل سيناريو (22 اختبار حي إجمالي). التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف على كل الملفات، `oxlint` 0 أخطاء، توازن CSS مؤكَّد.

**⚠️ خطوة مهمة بعد النشر:** أول زيارة للتطبيق بعد هذا التحديث هتعرض **شاشة إعداد فارغة** — لازم تدخل اسم مستخدم وكلمة سر جديدة (8 أحرف على الأقل) لأول مرة، وده هيبقى حسابك الرئيسي.

---

### 128. صورة شخصية لكل مستخدم مسجّل (بعد نظام تسجيل الدخول مباشرة)
**السياق:** بعد إضافة نظام تسجيل الدخول (#127)، طلب المستخدم إضافة إمكانية صورة شخصية للمستخدم المسجّل، طبيعي كامتداد لنظام الحسابات الجديد.

**التصميم الأمني الأهم:** الـEndpoint بتاع الرفع (`POST /api/auth/me/avatar`) **بيحدد صاحب الصورة من الجلسة الحالية نفسها** (`currentUser(req)`)، **مش من أي معامل خارجي** (URL أو Body) — بمنع أي مستخدم من رفع/تغيير صورة مستخدم تاني، حتى لو حاول يمرر `userId` مختلف.

**اكتشاف وتصحيح خطأ حقيقي أثناء البناء (استيراد دائري):** أول محاولة كانت خلي `routes/auth.mjs` تستورد دالة بناء رابط الصورة مباشرة من `routes/owner-avatar.mjs` — لكن `routes/owner-avatar.mjs` نفسها بتستورد `currentUser` من `routes/auth.mjs`! ده كان هيسبب **استيراد دائري حقيقي** (Circular Import) بين الملفين. تم اكتشافه ومعالجته **قبل** أي اختبار، عبر فصل الدوال المشتركة في ملف مساعد منفصل تمامًا (`lib/owner-avatar-store.mjs`) يقدر الملفين يستوردوا منه بأمان بدون أي حلقة.

**التنفيذ:**
- `server/lib/owner-avatar-store.mjs`: منطق مشترك (البحث عن ملف الصورة، بناء الرابط مع Cache-busting).
- `server/routes/owner-avatar.mjs`: رفع الصورة (محمي تلقائيًا بالبوابة المركزية #127) وعرضها (متاح للجميع بدون تسجيل دخول، زي صور الموظفين — مش بيانات سرية).
- `routes/auth.mjs`: إضافة `avatarUrl` لاستجابات حالة المصادقة/الدخول/الإعداد.
- الفرونت إند: `OwnerAvatarUpload.tsx` جديد (بنفس نمط رفع صور الموظفين بالضبط — نفس تجربة الاستخدام المألوفة، شامل `AvatarPositioner` لتحديد جزء الصورة قبل الرفع)، مُدمَج في قسم ملف شخصي جديد أعلى السايدبار.
- `AuthGate.tsx`: بقى يحمل بيانات المستخدم (الاسم والصورة) ويمررها عبر `App.tsx` لـ`Sidebar.tsx`.

**تم التحقق حيًا بدقة قاطعة (6 سيناريوهات، شامل الاختبار الأمني الحاسم):**
1. إنشاء أول حساب.
2. رفع صورة ناجح.
3. حالة المصادقة تعكس رابط الصورة الصحيح.
4. الصورة متاحة للجميع بدون تسجيل دخول.
5. **مستخدم ثاني رفع صورته الخاصة** (بيانات صورة مختلفة تمامًا).
6. **التأكيد الأمني القاطع**: صورة المستخدم الأول **فضلت زي ما هي بالضبط**، لم تتأثر خالص برفع المستخدم الثاني — إثبات مباشر إن النظام آمن ومفيش أي طريقة لمستخدم يغيّر صورة مستخدم تاني.

**الحالة:** ✅ تم اكتشاف وتصحيح خطأ استيراد دائري حقيقي قبل أي اختبار، بناء النظام الكامل بتصميم أمني صحيح من الأساس، والتحقق الحي الشامل (6 سيناريوهات، شامل الاختبار الأمني الحاسم). التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `node --check` نظيف على كل الملفات، `oxlint` 0 أخطاء، توازن CSS مؤكَّد.

---

### 129. نظام نسخ احتياطي تلقائي + اكتشاف أمني وقائي مهم أثناء البناء
**السياق:** الأولوية الثالثة والأخيرة من "الأولويات المتبقية" المتفق عليها — بيانات المشروع (سير العمل، الحسابات، الجلسات) كانت بدون أي حماية من الفقدان أو التلف.

**النظام المبني:** `backup-mkdd-data.sh` جديد (بنفس أسلوب `manage-openhands.sh`/`setup.sh` الموجودين)، بثلاث أوامر:
- **`backup`** (افتراضي): نسخة احتياطية مضغوطة (`tar.gz`) بختم زمني، محفوظة **خارج** `mkdd-data/` نفسها (تجنب تراكم النسخ داخل بعضها).
- **`list`**: عرض النسخ الموجودة.
- **`restore <ملف>`**: استرجاع نسخة، بعد **تأكيد صريح إجباري**، مع **نقل البيانات الحالية لمكان آمن أولًا (مش حذفها)** — أمان مزدوج ضد أي غلطة أثناء الاسترجاع نفسه.
- **الاحتفاظ التلقائي**: الاحتفاظ بآخر 14 نسخة افتراضيًا (قابل للتخصيص)، حذف الأقدم تلقائيًا لمنع امتلاء القرص.
- **جاهز للجدولة التلقائية**: تعليمات واضحة لإضافته كـ`cron job` يومي.

**🚨 اكتشاف أمني وقائي مهم أثناء البناء:** أثناء إعداد `.gitignore` لمجلد النسخ الاحتياطية الجديد، اكتُشف إن **`mkdd-data/` نفسها مش كانت مُستبعَدة من Git خالص** — الملف الوحيد المتتبّع كان شعار الشركة (غير حساس)، لكن هذا كان يعني **أي `git add -A` مستقبلي (نمط العمل المُستخدَم فعليًا طول المشروع) ممكن يرفع بيانات حساسة حقيقية بالغلط** — كلمات سر مشفّرة، توكنات جلسات، صور مستخدمين مرفوعة.

**التحقق الفوري من التاريخ الكامل:** فحص شامل لتاريخ Git بالكامل (مش بس الحالة الحالية) أكّد **عدم وجود أي تسريب فعلي حدث في أي وقت** — المخاطرة كانت وقائية بس (مستقبلية)، مش حادثة حدثت بالفعل.

**الإصلاح:** `.gitignore` بقى يستبعد **كل** محتويات `mkdd-data/` صراحة، مع استثناء واحد فقط (الشعار، غير الحساس والمفيد فعليًا).

**تم التحقق حيًا بدقة قاطعة:**
1. **نظام النسخ الاحتياطي**: إنشاء نسخ متتالية، التأكد من ظهورها في القائمة، اختبار التقليم التلقائي (ضبط الاحتفاظ على نسخة واحدة، تأكيد حذف النسختين الأقدم تلقائيًا).
2. **الاسترجاع**: محاكاة تلف بيانات → استرجاع → **تأكيد استرجاع البيانات الأصلية الصحيحة 100%**، **تأكيد حفظ البيانات "التالفة" في مكان آمن (مش حذفها)**، **تأكيد إلغاء العملية بأمان لو المستخدم مأكّدش**.
3. **الإصلاح الأمني**: محاكاة مباشرة بملفات حساسة حقيقية (كلمات سر، جلسات، صور) — تأكيد إن الشعار **فقط** هو اللي هيتتبّع، وكل الملفات الحساسة **تُستبعد تمامًا**.

**الحالة:** ✅ تم بناء نظام نسخ احتياطي كامل وآمن، اكتشاف وإصلاح فجوة أمنية وقائية مهمة أثناء البناء (لم يُطلَب صراحة، اكتُشفت بالفحص الدقيق)، التأكد من عدم وجود أي تسريب فعلي سابق، والتحقق الحي الشامل لكل جزء (7+ سيناريوهات). التحقق النهائي: `bash -n` نظيف على السكريبت.

**بكده، الأولويات الثلاث المتفق عليها (تسجيل الدخول، تنظيف الميزات المعطّلة، النسخ الاحتياطي) اكتملت بالكامل.**

---

### 130. باجوش كتب "رسائل" بدل ما يستخدم نظام التقارير الفعلي — فجوة في تعليمات الموظفين
**السياق:** المستخدم لاحظ إن "موضوع التقارير لسه مش شغال" — رغم إن باجوش (Product Manager) كتب في المحادثة رسائل تفصيلية جدًا ومنظمة موجّهة لمريم وشنودة، **مفيش أي تقرير ظهر في المكان المخصص له في صفحة المشروع خالص**.

**التشخيص:** فحصت المحادثة الفعلية ولقيت إن باجوش **معملش استدعاء لأداة التقارير الحقيقية خالص** — بدل كده، كتب رسائل نصية داخل المحادثة، وحتى قال حاجة مربكة ("الـrepo محلي ومش متواصل بـGitHub remote، هرسل لك رسايل مريم وشنودة جاهزة وانت تبعتها")، وكأنه فاهم غلط تمامًا الغرض من النظام — فاكر إن المطلوب "يبعت رسائل" بالمعنى التقليدي (زي إيميل)، مش "يسجّل تقرير رسمي" عبر الـAPI الفعلي الموثّق بالفعل في `AGENTS.md`.

**السبب الجذري:** التعليمات الموجودة في `AGENTS.md` كانت **مفصّلة وصحيحة تقنيًا** (مثال `curl` كامل، توضيح كل الحقول)، لكن **مفيش تحذير صريح** ضد بالظبط السلوك اللي حصل — الجملة الافتتاحية "they record it" ماكنتش بتستبعد صراحة تفسير "التسجيل" على إنه مجرد كتابة نص وصفي في المحادثة، أو تفويض المهمة للمالك البشري ليرسلها يدويًا.

**الحل:** إضافة تحذير صريح ومباشر جدًا مباشرة قبل مثال الـ`curl`، يوضح:
1. "التسجيل" يعني **تحديدًا** استدعاء الـAPI — مفيش أي بديل تاني.
2. **مفيش نظام رسائل بين الموظفين خالص في MKDD** بالتصميم (كل موظف عنده محادثة واحدة بس مع المالك) — فكتابة "رسالة" لموظف تاني مش بديل عملي، هي ببساطة نص هيضيع.
3. **المالك مش وسيط رسائل** — لا يُطلَب منه تمرير رسائل بين الموظفين يدويًا.
4. توجيه واضح: "لو لقيت نفسك بتكتب رسالة بدل ما تستدعي الأداة، ده بالظبط إشارة إنك محتاج توقف وتستدعي الـAPI".

**تم اكتشاف وتصحيح خطأ إضافي أثناء الكتابة:** الصياغة الأولى للتحذير أشارت لـ"Section 6 below" — لكن هذا **كان خطأ منطقي** (كان بيشاور لنفس القسم اللي التحذير نفسه موجود فيه، تناقض ذاتي). تم اكتشافه والتحقق منه بفحص كل عناوين الأقسام، وتصحيحه بإزالة الإشارة غير الدقيقة تمامًا بدل ترك مرجع غلط.

**الحالة:** ✅ تم التشخيص الدقيق (فحص المحادثة الفعلية، مش افتراض)، سد الفجوة الحقيقية في التعليمات (تحذير صريح ضد السلوك المرصود بالضبط)، اكتشاف وتصحيح خطأ مرجعي إضافي أثناء الكتابة، والتحقق من سلامة ترقيم الأقسام بالكامل (لسه متسلسل 1-14 صحيح).

**⚠️ ملاحظة مهمة:** هذا إصلاح في **تعليمات الموظفين** (`AGENTS.md`)، مش في كود MKDD نفسه — نظام التقارير كان (ولسه) شغال 100% تقنيًا (تم التحقق منه بدقة في #102-103). المشكلة كانت في سلوك الموظف الذكاء الصناعي نفسه، مش في الكود.

---

### 131. النموذج المعروض في تفاصيل الموظف كان بيعكس إحصائيات محادثة قديمة، مش الإعداد الحالي الفعلي
**السياق:** المستخدم لاحظ إن النموذج المعروض في بيانات ونشاط الموظف "شكله ثابت" (Hardcoded)، وطلب إنه يعكس الموديل الفعلي المُختار من الـAgent Profile.

**التشخيص:** فحصت الكود ولقيت النموذج المعروض بيتيجي من `cost.modelName`، وده مُشتقّ من **إحصائيات تراكمية للمحادثة نفسها** (`conversation.stats.usage_to_metrics.default.model_name`) — مش من الإعداد الحالي الفعلي للموظف. لو موظف عنده محادثة قديمة (زي كيرلس اللي شغال من فترة)، والموديل اتغيّر بعد كده من واجهة OpenHands نفسها، الإحصائيات دي بتفضل بتعكس **الموديل القديم اللي استُخدِم وقت تراكم هذه الإحصائيات**، مش الموديل الحالي الفعلي — مش Hardcoded بالمعنى الحرفي، لكن **قيمة قديمة/متجمدة** (Stale) بنفس التأثير عمليًا.

**اكتشاف مهم:** البيانات الصحيحة (`llm_profile_ref` — الإعداد الحالي الفعلي من الـAgent Profile) **كانت متاحة بالفعل** في استجابة `/api/employees` (عبر Spread Operator موجود من قبل)، وموجودة أصلًا في نوع `AgentProfile` بالفرونت إند — المشكلة كانت بس في **استخدام المصدر الغلط للعرض**، مش نقص بيانات.

**الحل:** استبدال عرض `cost.modelName` (القديم/المتجمد) بـ`employee.llm_profile_ref` (الحالي/الحقيقي دايمًا) في `EmployeeInsightsPanel.tsx` — تم تمريره كـProp جديدة من `ChatScreen.tsx` (اللي أصلًا عندها كائن `employee` الكامل).

**تم التحقق:** فحصت باقي التطبيق بالكامل للتأكد من عدم وجود أي مكان تاني بيعرض `modelName` من نفس المصدر القديم — الإصلاح شامل كل الأماكن الفعلية (مكان واحد بس كان بيستخدمها).

**الحالة:** ✅ تم التشخيص الدقيق (تمييز "قديم/متجمد" عن "Hardcoded" الحرفي)، اكتشاف إن البيانات الصحيحة كانت متاحة بالفعل (مش محتاج Endpoint جديد)، الإصلاح الشامل، والتحقق النهائي الكامل (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء).

---

### 132. خط أبيض في الـPWA لسه ظاهر (تحت/جنب) بعد #114/#115/#120 — محاولة إضافية
**السياق:** المستخدم أبلغ إن الخط الأبيض في وضع الـPWA (تحت وعلى الجانب) لسه ظاهر، رغم 3 إصلاحات سابقة لنفس فئة المشكلة (#114 خلفية `html`، #115 ألوان `manifest.json`/`theme-color`، #120 `viewport-fit=cover`).

**اكتشاف إضافي محتمل:** فحصت `.app` (الحاوية الجذرية اللي بتلف كل شاشات التطبيق) ولقيتها بتستخدم `min-height: 100vh` — **غير متسقة** مع باقي التطبيق، اللي بيستخدم `100dvh` (وحدة "الارتفاع الديناميكي للـViewport"، الأدق للمتصفحات المحمولة/وضع الـPWA Standalone، بخلاف `100vh` اللي ممكن تكون غير دقيقة في هذه السياقات تحديدًا). نفس عدم الاتساق كان موجود في شاشة تسجيل الدخول/الإعداد (`auth-screen`, `auth-loading`) — أول شاشة تظهر فعليًا لأي مستخدم غير مسجّل دخول.

**الحل:** توحيد كل استخدامات `100vh` المتبقية (في `.app` وشاشات تسجيل الدخول) إلى `100dvh`، اتساقًا كاملاً مع باقي التطبيق.

**⚠️ ملاحظة أمانة مهمة:** هذا **مش تأكيد قاطع** إن السبب الجذري اتحل بالكامل — بما إن المحاولات الثلاث السابقة لنفس الفئة من المشاكل لم تحل المشكلة نهائيًا حسب إفادة المستخدم، هذا إصلاح إضافي منطقي ومبرَّر (عدم اتساق حقيقي في الكود، مش تخمين عشوائي)، لكن **يحتاج تأكيد المستخدم بعد النشر** لمعرفة هل هذا كان السبب الفعلي المتبقي، أو لسه فيه سبب رابع لم يُكتشَف بعد.

**الحالة:** ⏳ تم تنفيذ إصلاح منطقي إضافي (توحيد `100vh`→`100dvh`)، مع التحقق الكامل من الكود (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد) — **بانتظار تأكيد المستخدم الفعلي بعد النشر لإغلاق هذه المشكلة نهائيًا**.

---

### 133. باجوش ادّعى قدرة تقنية غير موجودة أصلًا — "بدء محادثة مستقلة لموظف تاني"
**السياق:** بعد إصلاح #130 (تحذير ضد كتابة "رسائل" بدل استخدام نظام التقارير)، سأل المستخدم باجوش صراحة عن الموضوع. باجوش اعترف بصدق إنه معملش أي حاجة فعلية، **لكنه ادّعى بعد كده قدرة جديدة تقنيًا غير حقيقية**: "أقدر أعمل Handoff مباشر لكل موظف عن طريق بدء محادثة مستقلة له... وأتابع نتيجة شغله."

**التحقق التقني القاطع:** فحصت التصميم الأمني الأساسي للنظام (`openhands-client.mjs`) وتأكدت إن **مفتاح الاتصال بمنصة OpenHands (اللازم لإنشاء أي محادثة جديدة) موجود فقط جوّه خادم MKDD نفسه، ومحجوب تمامًا عن أي موظف ذكاء صناعي** — هذا جزء أساسي من مبادئ الأمان الموثّقة من الأول في المشروع ("Never expose the OpenHands session API key to the browser/agent"). **يعني ادّعاء باجوش غير حقيقي تقنيًا 100%** — مفيش أي أداة أو صلاحية عنده تسمح له بده فعليًا، بغض النظر عما قاله.

**لماذا هذا أخطر من #130:** #130 كانت عن موظف "بيتجاهل استخدام أداة موجودة"، لكن هذه المرة الموظف **بيدّعي قدرة مش موجودة أصلًا في تصميم النظام** — لو المالك صدّق الادّعاء وانتظر "الـHandoff التلقائي" ده، مفيش أي حاجة كانت هتحصل فعليًا، ومفيش أي طريقة يكتشف بيها المشكلة إلا بالسؤال المباشر (بالظبط زي ما حصل).

**الحل:** إضافة تحذير صريح إضافي في `AGENTS.md`، مباشرة بجانب تحذير #130، يوضح بوضوح تام: **الموظف لا يقدر تقنيًا يبدأ/يفتح/يكتب في محادثة موظف تاني خالص**، والادّعاء بذلك (حتى كخطوة مستقبلية) **مضلِّل** للمالك — نظام التقارير هو **الآلية الحقيقية الوحيدة** لهذا الغرض.

**الحالة:** ✅ تم التحقق التقني القاطع من عدم إمكانية هذا الادّعاء (فحص مباشر لتصميم الأمان الأساسي، مش افتراض)، وتوثيق التحذير الصريح في تعليمات الموظفين. ترقيم الأقسام تأكد سليم (لسه متسلسل 1-14).

**⚠️ ملاحظة مهمة:** هذا الإصلاح الثاني (بعد #130) في **تعليمات الموظفين**، مش في كود MKDD — النظام التقني لم يتغيّر خالص (وهو أصلًا مصمَّم بشكل آمن وصحيح من البداية، بمنع بالضبط هذا النوع من القدرة). المشكلة بالكامل كانت في فهم/سلوك الموظف الذكاء الصناعي.

---

### 134. 🚨 رجعة حرجة: نظام تسجيل الدخول (#127) عطّل كل تفاعلات سير العمل بتاعت الموظفين بالكامل
**السياق:** المستخدم وضّح إن المقصود مش سلوك باجوش الفردي، بل التأكد إن التقارير (اللي الموظفون بيعملوها بشكل طبيعي فعليًا) بتظهر في قسم "سير المشروع" على الشاشة الرئيسية بتاعة المشروع. هذا السؤال المباشر كشف مشكلة أعمق وأخطر بكتير من أي حاجة اتكلمنا عنها في #130/#133.

**الاكتشاف الحرج:** فحصت التصميم الأمني الأساسي للنظام، ولقيت إن نظام تسجيل الدخول (#127) بيحمي **كل** مسارات `/api/...` (عدا 3 استثناءات خاصة بالدخول/الإعداد بس) بجلسة متصفح (Cookie). لكن **الموظفون (الذكاء الصناعي) بيتواصلوا مع MKDD عبر أوامر `curl` مباشرة من جوّه بيئتهم الخاصة (حاوية Agent Canvas) — ومفيش عندهم أي Cookie جلسة متصفح خالص ولا أي طريقة يحصلوا عليها!**

**التحقق الحي القاطع:** محاكاة الطلب **بالضبط** زي ما هو موثّق في `AGENTS.md` (بدون أي Cookie، تمامًا زي ما موظف حقيقي هيعمله) أكّدت الرفض الفوري: `HTTP 401 {"error":"not_authenticated"}`.

**التأثير الحقيقي:** هذا معناه **كل** الـ10 نقاط اتصال الموثّقة رسميًا للموظفين في `AGENTS.md` (اعتماد البوابات، العوائق، النتائج، المراجعات الإلزامية، التقارير) — **جوهر نظام سير العمل بالكامل** — كانت معطّلة تمامًا من ناحية الموظفين منذ نشر #127، دون أي رسالة خطأ واضحة تظهر للمالك (بما إن الموظف مبيشوفش رد الـcurl مباشرة في واجهة المحادثة العادية).

**السبب الجذري:** نظام تسجيل الدخول (#127) اتصمم بشكل صحيح لحماية "المالك عبر المتصفح"، لكن **لم يُؤخَذ في الاعتبار وجود نوع تاني كليًا من المستدعين الشرعيين** — طلبات خدمة-لخدمة (Service-to-Service) من الموظفين أنفسهم، مش من متصفح خالص.

**الحل — مصادقة داخلية مقصورة بدقة شديدة:**
- **مفتاح سري مشترك جديد** (`MKDD_INTERNAL_SERVICE_KEY`) — مُضاف كمتغيّر بيئة **لكل من** `mkdd-ui` **و**`agent-canvas` (اكتشاف إضافي مهم: الموظف شغال جوّه `agent-canvas`، فلازم يكون المتغيّر متاح ليه هناك تحديدًا، مش بس لـ`mkdd-ui`).
- **بوابة المصادقة** بقت تقبل **إما** جلسة متصفح صالحة **أو** الـHeader الجديد (`X-Internal-Service-Key`) المطابق — **لكن مقصور بدقة شديدة جدًا على مسار `/api/workflow/*` بس** (المسار الوحيد الموثَّق فعليًا لاستدعاءات الموظفين)، لتقليل أي مخاطرة أمنية لأقصى درجة ممكنة لو المفتاح ده تسرّب يومًا ما.
- **تحديث `AGENTS.md`**: كل الـ10 أمثلة `curl` بقت تتضمن الـHeader الجديد (`$MKDD_INTERNAL_SERVICE_KEY`، متغيّر بيئة حقيقي متاح للموظف، مش قيمة ثابتة مكتوبة)، مع توضيح صريح في مقدمة القسم يشرح السبب والإلزامية.

**تم التحقق حيًا بدقة قاطعة شاملة (5+2 سيناريو):**
1. بدون المفتاح → 401 (اترفض صح، تأكيد المشكلة الأصلية).
2. بالمفتاح الصحيح → **عدّى بوابة المصادقة بنجاح** (وصل لمنطق العمل الفعلي).
3. بمفتاح غلط → 401 (اترفض صح).
4. **🔒 فحص أمني حاسم**: نفس المفتاح الصحيح **معملش أي فرق** على Endpoint تاني غير `/api/workflow/*` (زي `/api/projects`) — لسه 401، تأكيد إن النطاق الضيق شغال بدقة.
5. **🔒 فحص أمني حاسم إضافي**: نفس المفتاح معملش أي فرق على `/api/settings/*` الحساسة برضو.
6-7. **الاختبار النهائي الشامل**: محاكاة الطلب بنفس نمط الاستبدال الفعلي لمتغيّر البيئة (Shell substitution) بالضبط كما سيحدث فعليًا، **وتأكيد إن جلسة المالك عبر المتصفح لسه شغالة زي ما هي بدون أي تراجع خالص**.

**الحالة:** ✅ تم اكتشاف رجعة حرجة جدًا (عطّلت جوهر سير العمل بالكامل من ناحية الموظفين، بدون أي إشعار واضح)، بناء حل آمن ومقصور بدقة شديدة (لا يوسّع أي صلاحيات غير ضرورية)، تحديث الكود والبنية التحتية (`compose.yml` لخدمتين) وتعليمات الموظفين (`AGENTS.md`)، والتحقق الحي الشامل جدًا (7 سيناريوهات، شامل فحوصات أمنية حاسمة). التحقق النهائي: `npm test` → 78/78، `tsc -b` نظيف، `node --check` نظيف، `oxlint` 0 أخطاء، `compose.yml` YAML صحيح.

**⚠️ خطوة إلزامية بعد النشر:** لازم تحدّد قيمة حقيقية وعشوائية لـ`MKDD_INTERNAL_SERVICE_KEY` في ملف `.env` بتاعك (القيمة الافتراضية `change-me-generate-a-real-secret` **غير آمنة للإنتاج خالص** وموجودة كـPlaceholder بس) — بدون هذا، النظام هيشتغل لكن بمفتاح ضعيف معروف للعامة (بما إنه موثّق في هذا الملف نفسه على GitHub).

---

### 135. تبسيط المفتاح السري الداخلي — توليد تلقائي بدلًا من إعداد يدوي
**السياق:** بعد إصلاح #134 (اللي طلب من المالك ضبط `MKDD_INTERNAL_SERVICE_KEY` يدويًا في `.env`)، سأل المستخدم بحق: هل ده إلزامي، أو فيه طريقة تخليه يتعمل لوحده بدون تعقيد؟

**البحث والاكتشاف:** فحصت `compose.yml` بدقة ولقيت إن `/projects` **مشتركة فعليًا بين الحاويتين** (`mkdd-ui` و`agent-canvas`) — كلاهما بيربط نفس المسار الفعلي على القرص (`${MKDD_PROJECTS_DIR:-./projects}`) بصلاحية قراءة وكتابة كاملة للطرفين. هذا اكتشاف مهم فتح الباب لحل أنظف بكتير.

**الحل الجديد:** المفتاح بقى **يتولّد تلقائيًا** أول مرة يُحتاج فيها (عبر `crypto.randomBytes`)، ويتخزن في ملف داخل `/projects/.mkdd-internal/service-key.txt` (المسار المشترك المكتشف)، ويفضل ثابت بعد كده (Persisted). الموظف (شغال جوّه `agent-canvas`) بيقرأ نفس الملف مباشرة عبر `$(cat /projects/.mkdd-internal/service-key.txt)` جوّه أمر الـ`curl` نفسه — **بدون أي إعداد يدوي مطلوب من المالك خالص**، مقارنة بالحل السابق (#134) اللي طلب تعديل `.env` يدويًا.

**التنظيف:** إزالة `MKDD_INTERNAL_SERVICE_KEY` كمتغيّر بيئة يدوي بالكامل من `compose.yml` (كلا الخدمتين) — مبقاش محتاج خالص.

**تم التحقق حيًا بدقة قاطعة (5 سيناريوهات):**
1. تأكيد عدم وجود الملف قبل أي طلب.
2. الطلب الأول في عمر النظام يولّد الملف تلقائيًا.
3. تأكيد وجود الملف فعليًا بعد التوليد.
4. طلب لاحق (بالمفتاح المقروء من الملف الفعلي) ينجح فورًا.
5. **اختبار الثبات**: المفتاح يفضل **نفسه بالضبط** حتى بعد إعادة تشغيل الخادم بالكامل (مش بيتولّد من جديد كل مرة).

**⚠️ ملاحظة صادقة عن حالة حدّية واحدة (مُوثَّقة، مش مخفية):** بما إن الـShell بيحسب `$(cat ...)` **قبل** إرسال الطلب، **أول محاولة وحيدة في عمر النشر بالكامل** (قبل ما الملف يتولّد لأول مرة) ممكن تفشل مرة واحدة بـ401 — لكن بمجرد إعادة المحاولة (Retry طبيعي)، الملف يكون بقى موجودًا وهتنجح فورًا. تم توثيق هذا السلوك بصراحة في `AGENTS.md` بدل تجاهله.

**الحالة:** ✅ تم اكتشاف حل أنظف وأبسط بكتير (بحث دقيق في البنية التحتية الفعلية، مش افتراض)، تبسيط تجربة المستخدم لتصبح بدون أي إعداد يدوي خالص، توثيق صادق للحالة الحدّية الوحيدة المتبقية، والتحقق الحي الشامل (5 سيناريوهات، شامل اختبار الثبات عبر إعادة التشغيل). التحقق النهائي: `npm test` → 78/78، `tsc -b` نظيف، `node --check` نظيف، `oxlint` 0 أخطاء، `compose.yml` YAML صحيح.

---

### 136. (مؤجَّل) حل روابط "التطبيق الحي" عبر الدومين — Subdomain ديناميكي لكل بورت
**السياق:** روابط المعاينة الحية (Live App links) شغالة صح على الآيبي المحلي، لكن مش على الدومين — بما إن NPM بيوجّه بورت واحد بس (18787) للدومين، بينما كل مشروع بياخد بورت مخصص من نطاق (4001-4020) مكشوف مباشرة على الآيبي المحلي بس.

**بحث تم إجراؤه (مش تخمين):** تأكدت إن نهج "مسار فرعي + إعادة كتابة" (Subpath + Rewrite) — اللي كان بديل بديهي — **غير موثوق فعليًا** لتطبيقات React/Vue الحديثة (بحث مباشر أكّد المشكلة، ومتوافق مع تجربة فعلية سابقة موثّقة في #56 كسبب التحول لنهج TCP Pass-through الحالي أصلًا).

**الحل الصحيح المؤكَّد بالبحث (نمط صناعي معياري):** **Subdomain ديناميكي لكل بورت** (زي `4005.preview.دومين`) — نفس النمط المُستخدَم فعليًا في أنظمة إنتاج حقيقية لنفس المشكلة بالضبط (تأكدت من مثال مباشر من توثيق Cloudflare Sandbox SDK الرسمي: "Preview URLs require wildcard DNS routing because they generate unique subdomains for each exposed port").

**المتطلبات (خارج الكود):**
1. DNS Wildcard عند شركة الدومين (`*.preview.دومين`).
2. شهادة SSL تغطي هذا الـWildcard.
3. إعداد في NPM يوجّه أي Subdomain مطابق لـ`mkdd-ui`.
4. كود جديد جوّه `mkdd-ui` يفحص الـHost header الوارد، يستخرج رقم البورت، ويعمل Proxy له تلقائيًا.

**قيود التصميم الحالي المؤكَّدة (مهمة لأي تنفيذ مستقبلي):** كل مشروع بياخد بورت **ثابت ودائم** من نطاق محدود (20 بورت بس، 4001-4020) — يعني حد أقصى 20 مشروع نشط بمعاينة حية في نفس الوقت. القاعدة البرمجية المطلوبة عامة تمامًا (مش خاصة بمشروع معين)، فهتشتغل تلقائيًا مع أي مشروع من الـ20 بدون أي تعديل إضافي لكل مشروع جديد.

**الحالة:** ⏸️ **مؤجَّل بطلب صريح من المستخدم** — التصميم والبحث اتأكدوا، لكن التنفيذ لسه ماحصلش. الأولوية الحالية على مشكلة تحديث المحادثات الحي (انظر البند التالي).

---

### 137. المحادثة الحية توقفت عن التحديث التلقائي — الـWebSocket كان معتمد على مفتاح مخزَّن مؤقتًا قديم بدون إعادة محاولة
**السياق:** المستخدمة اشتكت من مشكلة متكررة ومزعجة — لازم تعمل تحديث يدوي للصفحة عشان تشوف رسائل جديدة، وأشارت إن هذا "اتحاول يتصلح أكتر من مرة" قبل كده.

**اكتشاف مهم من التوثيق:** فحصت السجل التاريخي ولقيت إن هذه الميزة **كانت مُختبَرة وشغالة بالكامل** (تأكيد مباشر من المستخدم بتاريخ 13 أغسطس 2026 — "الردود بتوصل لحظيًا بدون أي refresh يدوي"). يعني السؤال الحقيقي: إيه اللي كسرها لاحقًا؟

**التحقيق العميق (بحث مباشر في الكود، مش تخمين):** فحصت `ws-bridge.mjs` بالتفصيل ولقيت السبب — دالة `sessionKey()` بترجع مفتاح مصادقة **مُخزَّن مؤقتًا (Cache)** من تحسين الأداء (#124)، ومعاها آلية "إعادة محاولة تلقائية عند فشل المفتاح" — **لكن هذه الآلية مبنية فقط جوّه طلبات REST العادية (`openhands()`/`openhandsFetch()`)، مش جوّه اتصال الـWebSocket الحي خالص**. لو حاوية Agent Canvas اتعاد تشغيلها (بمفتاح مصادقة جديد تمامًا) بدون إعادة تشغيل `mkdd-ui` معاها، أي اتصال WebSocket جديد كان بيستخدم المفتاح **القديم المخزَّن**، يفشل بصمت، ومفيش أي إعادة محاولة — الاتصال بيتقفل نهائيًا، والتطبيق برجع للاعتماد على تحديث خفيف كل 15 ثانية بس (بدل الفوري الحقيقي)، وهذا بالضبط سبب الشكوى.

**الحل:** إعادة هيكلة منطق الاتصال بـOpenHands جوّه `ws-bridge.mjs` ليدعم إعادة محاولة تلقائية — لو الاتصال فشل **قبل** استقبال أي رسالة خالص (مؤشر قوي على رفض المصادقة تحديدًا)، يتم استدعاء `invalidateSessionKey()` وإعادة المحاولة **مرة واحدة** بمفتاح جديد مُكتشَف حديثًا، قبل الاستسلام وقفل اتصال المتصفح.

**خطأين حقيقيين اتلقيا وصُححا أثناء البناء (بالفحص المباشر، مش الافتراض):**
1. أول محاولة إعادة هيكلة حذفت تعريف `upstreamUrl` بالغلط — اتصلح فورًا.
2. `browserSocket`'s Event Listeners كانت مسجَّلة **جوّه** الدالة القابلة لإعادة الاستدعاء، فكانت هتتكرر مع كل إعادة محاولة (تسريب بسيط) — تم اكتشافه بالمراجعة الدقيقة، وأُصلح بنقل التسجيل خارج الدالة، مرة واحدة بس.

**تم التحقق حيًا بدقة قاطعة (اختبار حاسم، بعد اكتشاف واستبعاد شرط اختبار زائف مرتين متتاليتين):**
- محاكاة كاملة للسيناريو الحقيقي بالضبط — إجبار الخادم على تخزين مفتاح "قديم" مؤقتًا فعليًا (عبر طلب REST حقيقي)، ثم محاكاة "إعادة تشغيل Agent Canvas" (إيقاف العملية القديمة، تشغيل عملية جديدة بمفتاح مختلف تمامًا).
- لوج الخادم الوهمي أكّد بدقة متسلسلة: **رفض المفتاح القديم أولًا**، ثم **قبول المفتاح الجديد** بعد إعادة المحاولة التلقائية، وأخيرًا **وصول الحدث الحي فعليًا للمتصفح** — بدون أي تدخل يدوي (زي إعادة تشغيل الحاوية) خالص.

**الحالة:** ✅ تم اكتشاف السبب الجذري الحقيقي (فحص مباشر للكود بعد مراجعة التاريخ)، الإصلاح المعماري الصحيح (نفس نمط إعادة المحاولة المُستخدَم بالفعل لطلبات REST، لكن للـWebSocket هذه المرة)، اكتشاف وتصحيح خطأين حقيقيين أثناء البناء، والتحقق الحي القاطع (بعد استبعاد شرط اختبار زائف بدقة مضاعفة). التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `node --check` نظيف، `oxlint` 0 أخطاء.

---

### 138. ترقية OpenHands Agent Canvas من v1.12.0 لـv1.15.0 — واكتشاف مهم عن حفظ الكود
**السياق:** طلب المستخدم فحص وترقية OpenHands Agent Canvas للإصدار الجديد (1.15.0)، بعد بحث دقيق أكّد عدم وجود تغييرات كاسرة معروفة تمس تكاملنا (فرق بسيط في `typescript-client`: `1.38.0` → `1.38.1`، ميزات الإصدارات الجديدة كلها واجهية).

**اكتشاف مهم أثناء البدء:** حاولت أنشئ Fork منفصل على GitHub للكود المُخصَّص (بما إن حجمه الكامل موثّق سابقًا بـ~1.5 جيجا)، لكن المستخدم صحّح المسار بسؤال محق: "ليه مش بتشتغل على نفس المستودع اللي أنا عامله؟". فحصت مستودع `openhands-mkdd` مباشرة ولقيت **الكود المُخصَّص محفوظ بالفعل فيه** (مجلد `openhands-agent-canvas/`)، وبحجم أخف بكثير من المتوقع (18 ميجا بس، مش 1.5 جيجا — يبدو إنه اتنضّف من `node_modules`/`.git` قبل الرفع سابقًا). تم التحقق من التزامن الكامل مع نسخة الـVM الفعلية (مقارنة مباشرة: 5 إشارات لـ`systemMessageSuffix`، 384 سطر، مطابقة تمامًا للطرفين).

**اكتشاف تقني إضافي مهم (يستحق متابعة مستقبلية منفصلة):** التوثيق الرسمي لـOpenHands (`AGENTS.md`) يوضح إن مفتاح الجلسة بقى **يُخزَّن في ملف حقيقي** (`~/.openhands/agent-canvas/session-api-key.txt`) — بديل أنظف بكثير من آلية اكتشاف `/proc` الهشة اللي نعتمد عليها حاليًا (موثّقة كـ"دين تقني" من بداية المشروع). لم يُنفَّذ التبديل الآن (خارج نطاق هذه الترقية)، لكنه فرصة تحسين حقيقية مستقبلية.

**تنفيذ الترقية:**
1. حُفظ التعديل المُخصَّص (`agent-profiles-local-view.tsx`) بـCommit واضح على الـVM أولًا.
2. استُنسخ المستودع الأصلي (`OpenHands/OpenHands`) مباشرة عند وسم `v1.15.0` — مستودع عام، لا يحتاج أي مصادقة.
3. **مقارنة دقيقة** بين الملف الأصلي النظيف في v1.15.0 والنسخة المُخصَّصة الحالية — أكّدت إن الفرق (`diff`) **مطابق حرفيًا 100%** لنفس التعديل الأصلي المعروف (نفس الأسطر بالضبط)، يعني **لا يوجد أي تعارض حقيقي** ناتج عن تغييرات Upstream بين v1.12.0 وv1.15.0 في هذا الملف تحديدًا.
4. طُبِّق التعديل المُخصَّص مباشرة على نسخة v1.15.0 الجديدة، وتم التحقق (`diff` فارغ = مطابقة تامة بعد التطبيق).
5. تم التأكد من عدم وجود فروق في البنية العامة للمجلدات بين النسختين (Compare مباشر لكل المجلدات الفرعية).
6. تم استبدال المحتوى القديم بالكامل، مع التحقق النهائي: الحجم معقول (19 ميجا)، رقم الإصدار في `package.json` مؤكَّد صراحة (`"version": "1.15.0"`)، التعديل المُخصَّص موجود ومطابق، ولا وجود لـ`node_modules`/`.git` غير مرغوب فيهما.

**الحالة:** ✅ تم الفحص التقني الشامل قبل الترقية، تصحيح مسار العمل (استخدام المستودع الموجود بدل تعقيد Fork غير ضروري)، اكتشاف مهم إضافي (آلية تخزين مفتاح جلسة أنظف متاحة مستقبلاً)، وتنفيذ الترقية بدقة مع تحقق مباشر من عدم وجود أي تعارض.

**نتيجة الاختبار الحي على Staging:**
- بناء الصورة الجديدة نجح (223 ثانية)، اللوج نظيف تمامًا من أي أخطاء عند بدء التشغيل.
- **مشكلة عابرة أثناء الاختبار:** `mkdd-ui-staging` توقفت فجأة (كود خروج 137) بعد بدء تشغيل `agent-canvas` الجديدة — فحص `dmesg` لم يُظهر سجل صريح لـOOM، والسبب الدقيق غير مؤكَّد بالكامل (يستحق مراقبة لو تكرر). إعادة التشغيل حلّتها فورًا وبقيت مستقرة.
- **الاختبار الوظيفي الشامل نجح بالكامل**: المشاريع ظاهرة، فتح محادثة قديمة لموظف يعرض رسائلها الصحيحة، إرسال رسالة جديدة وصل بنجاح.
- **ملاحظة مهمة تم فصلها بأمانة:** لوحظ بطء في تحميل المحادثات (20-25 ثانية) أثناء الاختبار — **تم التأكد من المستخدم إن هذه مشكلة موجودة من قبل الترقية أصلًا وليست ناتجة عنها**، فتم توثيقها كبند تحقيق منفصل (انظر البند التالي) بدل الخلط بينها وبين تقييم نجاح الترقية.

**القرار النهائي:** ✅ **الترقية ناجحة ومعتمدة**، بناءً على نجاح الاختبار الوظيفي الشامل الفعلي على Staging.

---

### 139. (قيد التحقيق) بطء تحميل المحادثات — مشكلة قديمة مستقلة عن ترقية OpenHands
**السياق:** أثناء اختبار ترقية OpenHands (#138)، لوحظ إن فتح محادثة قديمة لموظف بياخد 20-25 ثانية (مع تحسن طفيف عند إعادة المحاولة). **تم التأكد من المستخدم صراحة إن هذه المشكلة موجودة من قبل الترقية أصلًا** — يعني الترقية لم تسببها ولم تحلها، هي مشكلة أداء قائمة بذاتها.

**ملاحظة تقنية من التشخيص الأولي:** محاولة قياس وقت استجابة `/api/conversations/search` مباشرة من داخل الحاوية (بمفتاح غير صالح عمدًا للاختبار) أظهرت الطلب **عالقًا لأكثر من دقيقتين بدون أي رد** — لكن بما إن الاختبار استخدم مفتاح مصادقة غير صحيح، هذه النتيجة قد تعكس سلوكًا مختلفًا عن الاستخدام الحقيقي العادي (بمفتاح صحيح)، وتحتاج إعادة اختبار دقيقة بمفتاح صالح قبل اعتبارها دليلًا قاطعًا على السبب.

**الحالة:** ⏳ **قيد التحقيق** — لم يبدأ التشخيص الفعلي بعد. الخطوات التالية المقترحة: (1) إعادة قياس وقت استجابة `/api/conversations/search` بمفتاح مصادقة صحيح لعزل السبب الحقيقي، (2) فحص عدد المحادثات المتراكمة الفعلي على السيرفر (بما إن الأداء ممكن يتدهور مع كثرة المحادثات بمرور الوقت)، (3) مراجعة منطق `handleChatOpen`/`fetchRecentEvents` (من #121) للتأكد من عدم وجود انحدار أداء لم يُكتشَف من قبل.

---

### 140. تحقيق بطء تحميل المحادثات — لقيت فجوة UX حقيقية (مفيش مؤشر تحميل خالص)، لكن السبب الكامل محتاج تأكيد
**السياق:** المستخدمة اشتكت من بطء ملحوظ (20-25 ثانية) عند فتح محادثة، وأشارت إن هذه المشكلة اتحاول تتصلح كتير قبل كده.

**تحقيق عميق ومنظّم (قياسات حية فعلية، مش تخمين):**
1. **قياس البحث عن المحادثة**: 570 مللي ثانية لكل 21 محادثة موجودة على السيرفر — سريع جدًا.
2. **قياس جلب رسائل محادثة كيرلس الفعلية**: 39 مللي ثانية فقط لـ21 حدث — سريع جدًا جدًا.
3. **قياس الطلب الكامل (`/api/chat/open`) من نفس الـVM**: 11 مللي ثانية (حتى مع رفض 401) — سريع جدًا.
4. **اختبار طلب بسيط (`/api/auth/status`) من جهاز المستخدمة الفعلي**: فتح فورًا — استبعد بشكل قاطع أي مشكلة شبكة أو متصفح عامة.
5. **اختبار متصفح جديد كليًا**: لسه فيه تأخير (مع تحسن طفيف) — استبعد بشكل قاطع مشكلة كاش/Service Worker.

**اكتشاف حاسم عبر سؤال دقيق للمستخدمة:** "نص جاري التحميل مش بيظهر أصلًا" — فحصت الكود ولقيت **فجوة UX حقيقية**: `ChatScreen` معندهاش أي مؤشر تحميل للحالة الأولية (فتح محادثة لأول مرة) — موجود بس مؤشر لتحميل الرسائل الأقدم (عند التمرير). يعني منطقة الرسائل كانت **تظهر فاضية تمامًا** أثناء التحميل الفعلي، بدون أي إشارة بصرية — ظاهرة نفسية معروفة في تصميم الواجهات: الانتظار بدون أي مؤشر بصري يُدرَك أطول بكثير من الوقت الفعلي.

**الحل:** إضافة حالة `isOpeningConversation` صريحة في `useConversation.ts`، تتحول لـ`false` بعد أول استجابة (نجاح أو فشل)، مع مؤشر تحميل واضح (`"جاري تحميل المحادثة…"`) يظهر في `ChatScreen` طالما التحميل شغال ومفيش رسائل بعد.

**⚠️ ملاحظة أمانة مهمة:** كل القياسات التقنية المباشرة (السيرفر، الشبكة، المتصفح) أثبتت سرعة عالية جدًا (أقل من ثانية إجمالاً) — **مفيش دليل تقني قاطع على وجود تأخير فعلي حقيقي بمقدار 20-25 ثانية في أي جزء تم قياسه مباشرة**. هذا الإصلاح يحل فجوة UX حقيقية ومؤكَّدة (غياب مؤشر التحميل)، والتي على الأرجح كانت **السبب الرئيسي** وراء "الإحساس" بالبطء الشديد — لكن **يحتاج تأكيد المستخدمة الفعلي بعد النشر** لمعرفة هل هذا كان السبب الكامل، أو لسه فيه تأخير حقيقي متبقي (زي حجم بيانات أكبر عبر شبكة أضعف) يحتاج تحقيق إضافي.

**الحالة:** ⏳ تم إصلاح فجوة UX حقيقية ومؤكَّدة بالتحقيق المباشر، مع تحقق كامل (`npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء) — **بانتظار تأكيد المستخدمة الفعلي بعد النشر** لإغلاق هذه المشكلة نهائيًا أو مواصلة التحقيق.

---

### 141. الموديل ما زال لا يتحدّث رغم إصلاح #131 — قائمة الموظفين بتتحمّل مرة واحدة بس عند فتح التطبيق
**السياق:** المستخدم أبلغ إن مشكلة الموديل (اللي أصلحناها في #131) عادت — يغيّر الموديل فعليًا من الـAgent Profile، لكن التغيير مش بيظهر في MKDD. وضّح إنها مشكلة موجودة من قبل ترقية OpenHands (مش سببها الترقية).

**التشخيص:** فحصت الكود ولقيت السبب موثّق **صراحة في تعليق موجود بالفعل** في `App.tsx`: **"Projects and employees are both global... so both load once at startup"** — قائمة الموظفين (شاملة `llm_profile_ref` اللي أصلحنا عرضها في #131) بتتحمّل **مرة واحدة بس** عند فتح التطبيق، **بدون أي تحديث دوري خالص** بعد كده. يعني إصلاح #131 كان صحيح فعليًا (تصحيح **مصدر** البيانات المعروضة)، لكنه لم يحل مشكلة **حداثة** هذه البيانات — لو الموديل اتغيّر بعد فتح التطبيق، التغيير مش بيظهر لحد ما يعمل المستخدم Refresh كامل للصفحة.

**الحل:** إضافة تحديث دوري خفيف وصامت لقائمة الموظفين (كل 30 ثانية)، بنفس نمط "آخر بيانات معروفة صحيحة" المُستخدَم في باقي أجزاء التطبيق — عند الفشل، يُحافَظ على البيانات الحالية (بدون إفراغ السايدبار أو أي واجهة)؛ عند النجاح، تُستبدَل بصمت بدون أي مؤشر تحميل ظاهر (بما إنه تحديث خلفي، مش تحميل أساسي).

**تم التحقق حيًا بدقة قاطعة:** محاكاة مباشرة لسيناريو تغيير الموديل على الخادم الوهمي بين استدعاءين متتاليين — الاستدعاء الأول رجّع الموديل القديم بدقة، والاستدعاء الثاني (يحاكي التحديث الدوري بعد 30 ثانية) رجّع الموديل الجديد بدقة تامة، مؤكِّدًا عدم وجود أي تخزين مؤقت في الـBackend يمنع وصول البيانات الحديثة.

**الحالة:** ✅ تم التشخيص الدقيق (اكتشاف تعليق موثّق بالفعل يوضح القيد المعماري الأصلي)، الإصلاح الصحيح (تحديث دوري بنمط آمن ومتسق مع باقي التطبيق)، والتحقق الحي القاطع (محاكاة تغيير فعلي للبيانات بين طلبين). التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء.

---

### 142. 🚨🚨 الأخطر في الجلسة: `/api/health` كانت محمية بالمصادقة — نظام النشر التلقائي كان عالق في حلقة رجعة لا نهائية منذ #127
**السياق:** المستخدمة أبلغت إن الموديل "لسه قديم" رغم إصلاح #141، وسألت سؤالًا بريئًا: "المفروض السكريبت بيعمل تحديث اتوماتيك؟" — هذا السؤال البسيط كشف **أخطر مشكلة في المشروع بالكامل**.

**التحقيق الكارثي:** فحصت حالة نظام النشر التلقائي (`mkdd-auto-deploy.timer`) ولقيت `Trigger: n/a` — مؤشر خطير. فحص السجل (`journalctl`) كشف السلسلة الكاملة:
1. نظام تسجيل الدخول (#127) أضاف بوابة مصادقة تحجب **كل** `/api/...` عدا 3 استثناءات فقط.
2. `/api/health` (اللي نظام النشر التلقائي بيعتمد عليه للتأكد من نجاح النشر) **لم يُضَف لهذه الاستثناءات أبدًا**.
3. **النتيجة**: منذ نشر #127 (21 أغسطس)، **كل** محاولة نشر تلقائي لأي كوميت جديد كانت تفشل — الـHealth Check يترفض بـ401 (بما إن أداة النشر التلقائي مالهاش أي جلسة متصفح)، فيرجع النظام **تلقائيًا** لآخر كوميت "ناجح" معروف — وهو **آخر كوميت قبل نظام تسجيل الدخول بالكامل**.
4. **حلقة لا نهائية**: النظام (المُصمَّم عمدًا ليفحص كل 20 ثانية) كان بيحاول من جديد، يفشل بنفس السبب، يرجع لنفس الكوميت القديم — **بشكل متكرر ومستمر منذ أكثر من 3 أيام** وقت الاكتشاف.

**التأثير الحقيقي المحتمل:** لو هذا النظام كان فعليًا يتدخل بشكل متكرر، فمن المحتمل جدًا إنه كان **يمحي معظم تحديثاتنا اليدوية تلقائيًا** خلال دقائق قليلة من كل نشر يدوي طوال هذه الجلسة الطويلة — مما قد يفسّر عدة أعراض غريبة لوحظت سابقًا (زي ظهور واجهة قديمة بعد إعادة تشغيل الـVM).

**الحل:** إضافة `/api/health` لقائمة الاستثناءات — تم التحقق أولًا من أن هذا الفحص (`handleHealth`) **آمن تمامًا للكشف العام** (يفحص فقط جاهزية OpenHands، بدون أي بيانات حساسة خالص).

**تم التحقق حيًا بدقة قاطعة:**
1. `/api/health` بدون أي جلسة/كوكي خالص → **200 `{"status":"ready"}`** — بالضبط ما يحتاجه نظام النشر التلقائي.
2. `/api/projects` (وأي endpoint حساس آخر) **لسه محمي بشكل صحيح تمامًا** → 401 — تأكيد عدم وجود أي تراجع أمني.

**الحالة:** ✅ تم اكتشاف وإصلاح **أخطر رجعة في المشروع بالكامل** — اكتُشفت بفضل سؤال بسيط ومباشر من المستخدمة، وليس بحث استباقي. التحقق الكامل: `npm test` → 78/78، `tsc -b` نظيف، `node --check` نظيف، `oxlint` 0 أخطاء.

**🚨🚨 خطوة عاجلة جدًا جدًا مطلوبة فورًا:** بما إن الحلقة الكارثية **لسه شغالة على السيرفر الآن** (كل 20 ثانية)، لازم **إيقاف نظام النشر التلقائي مؤقتًا** (`sudo systemctl stop mkdd-auto-deploy.timer`)، ثم نشر هذا الإصلاح **يدويًا** فورًا، والتأكد من نجاح Health Check قبل إعادة تفعيل النظام التلقائي.

**تحديث بعد النشر الفعلي:**
1. **اكتشاف مهم أثناء التحقق**: `journalctl` كشف إن نظام النشر التلقائي **كان متوقفًا فعليًا تمامًا منذ 21 أغسطس الساعة 12:46** (`Stopped mkdd-auto-deploy.service`) — **مش شغالًا في حلقة مستمرة لحد اكتشاف المشكلة كما افتُرِض في البداية**. هذا مطمئن: تحديثاتنا اليدوية طوال هذه الجلسة **لم تكن معرَّضة لأي تدخل تلقائي** طوال هذا الوقت.
2. **مشكلة ثانوية منفصلة اكتُشفت وأُصلحت أثناء إعادة التفعيل**: بعد `systemctl start mkdd-auto-deploy.timer`، ظل عمود `NEXT`/`LEFT` فارغًا (`-`) رغم نجاح التفعيل الظاهري. السبب: `OnUnitActiveSec=20sec` تحتاج نقطة انطلاق فعلية (تشغيل واحد ناجح للخدمة) لتبدأ الحساب منها، بينما `OnBootSec=20sec` مرتبطة بإقلاع حقيقي للـVM (لا يتفعّل عبر `systemctl start` اليدوي). **الحل**: تشغيل الخدمة نفسها يدويًا مرة واحدة (`sudo systemctl start mkdd-auto-deploy.service`) لتنشيط نقطة الانطلاق.
3. **التحقق النهائي القاطع بعد الإصلاحين معًا**: راقبنا 5 دورات نشر تلقائي متتالية كاملة — **كل واحدة "Deactivated successfully" بدون أي فشل أو رجعة خالص**، والجدولة (`NEXT`/`LEFT`) تعمل بشكل صحي وطبيعي تمامًا.

**الحالة النهائية:** ✅✅ تم إصلاح المشكلتين معًا (الأساسية والثانوية)، والتحقق الحي القاطع من عودة النظام بالكامل لحالته الصحية الطبيعية.

---

### 143. الحل النهائي لبطء تحميل المحادثات: سلوك "إيقاظ بيئة العمل" من OpenHands نفسه — خارج نطاق كود MKDD
**السياق:** بعد إصلاح #140 (مؤشر التحميل)، المستخدمة أبلغت إن التأخير **لسه موجود** حتى مع المؤشر الظاهر، وأضافت تفاصيل حاسمة: **"أحيانًا أسرع، أحيانًا لأ، ومش في كل المحادثات."**

**التشخيص القاطع عبر اختبار حي مباشر:** طلبت من المستخدمة تفتح **نفس المحادثة مرتين متتاليتين** (فتح، رجوع، فتح تاني فورًا). النتيجة: **المرة الثانية سريعة جدًا تقريبًا بدون تأخير خالص** — بينما الموظف نفسه كان "فاضي/مستني" (مش بينفذ مهمة) وقت التأخير، ما يستبعد فرضية "الموظف مشغول".

**الاستنتاج القاطع:** هذا النمط (بطء المحاولة الأولى، سرعة المحاولات اللاحقة الفورية، بشكل مستقل عن كل موظف/محادثة على حدة) **بصمة مباشرة وواضحة لآلية "إيقاظ بيئة عمل" (Workspace Wake-up)** من مستوى **OpenHands Agent Server نفسه** — نظام إدارة موارد شائع في أنظمة الـAgent Runtime الحديثة (توفير الموارد بإيقاف البيئات غير النشطة مؤقتًا، مع تكلفة زمنية عند "تشغيلها" من جديد). هذا **سلوك خارج نطاق كود MKDD تمامًا** — كل الاختبارات المباشرة السابقة (#140 وما قبلها) أثبتت بدقة قاطعة ومتكررة إن الـBackend بتاعنا سريع جدًا دومًا (أقل من 100 مللي ثانية)، والتأخير الحقيقي بيحصل جوّه OpenHands نفسها أثناء التفعيل.

**التحسين المُنفَّذ (بدلًا من محاولة "إصلاح" شيء خارج السيطرة):** إضافة رسالة توضيحية إضافية تظهر **فقط بعد 8 ثوانٍ من انتظار حقيقي مستمر** ("أول تحميل لهذا الموظف قد يستغرق وقتًا أطول قليلًا") — تطمئن المالك إن الانتظار الطويل طبيعي ومتوقع في هذه الحالة، دون التأثير على المسار السريع (لا تظهر أي رسالة إضافية خالص لو التحميل كان سريعًا، وهو الغالبية العظمى من الحالات).

**خطأ حقيقي اتلقى وصُحح أثناء البناء:** أول محاولة تعديل حذفت بالغلط تعريفات حالة موجودة بالفعل (`pendingImages`, `uploadingFile`) بدل إضافة حالة جديدة بجانبها — اكتُشف فورًا عبر `tsc -b` (19 خطأ تجميع واضح)، وأُصلح بإعادة الأسطر الأصلية صحيحة مع الإضافة الجديدة.

**الحالة:** ✅ تم التشخيص القاطع النهائي (اختبار حي حاسم أثبت السبب الجذري خارج نطاق MKDD)، إضافة تحسين UX حقيقي ومفيد (رسالة طمأنة بعد انتظار طويل، بدون أي تأثير على المسار السريع)، اكتشاف وتصحيح خطأ حقيقي أثناء البناء. التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد.

---

### 144. المحادثات بتعمل "ريفريش" لوحدها كل 30 ثانية — تأثير جانبي غير مقصود من إصلاح #141
**السياق:** المستخدمة أبلغت إن المحادثات المفتوحة بتعمل "ريفريش" (إعادة تحميل) لوحدها. هذا التوقيت (30 ثانية) طابق تمامًا التحديث الدوري لقائمة الموظفين اللي أضفناه في #141 لحل مشكلة الموديل.

**التشخيص الدقيق:** فحصت `useProjectNavigation.ts` ولقيت `useEffect` بيعتمد على **`[projects, employees]` كـReferences كاملة للمصفوفات نفسها**، مش على محتواها. بما إن التحديث الدوري (#141) بيستبدل مصفوفة `employees` بالكامل كل 30 ثانية (Reference جديدة تمامًا، حتى لو البيانات مطابقة 100%)، هذا الـEffect **كان بيعاد تشغيله في كل تحديث دوري**، ويستدعي `employees.find(...)` من جديد — اللي بترجع **كائن موظف جديد تمامًا (Reference مختلفة)** حتى لو نفس البيانات بالضبط، ويضبط `selectedEmployee` بهذا الكائن الجديد. بما إن `useConversation`'s الـEffect الأساسي معتمد على `employee` كـDependency، أي تغيّر في Reference بتاعته كان بيخلي هذا الـEffect **يعاد تشغيله بالكامل** — بما فيه مسح كل الرسائل فورًا (`setMessages([])`) قبل إعادة الجلب — وهذا بالضبط "الريفريش" المُلاحَظ.

**الحل:** تغيير الـDependency Array من `[projects, employees]` (Object References) إلى `[projects.length, employees.length]` (قيم مستقرة) — الطول لا يتغيّر إلا عند إضافة/حذف موظف أو مشروع فعليًا (نادر جدًا)، بينما تحديث محتوى العناصر (زي تغيّر الموديل) لا يُحدِث أي تأثير على هذا الـEffect بعد الآن.

**ملاحظة تصميمية:** هذا النمط (استخدام قيمة مستقرة بدل الكائن الكامل كـDependency) **موجود بالفعل ومقبول** في المشروع (`useProjectTotalCost.ts` تستخدم نفس الأسلوب مع `project?.path`)، فالحل متسق مع الممارسات المتبعة.

**الحالة:** ✅ تم التشخيص الدقيق (تتبّع التوقيت المطابق بين الشكوى والتحديث الدوري، ثم فحص مباشر للكود)، الإصلاح الصحيح والمتسق مع النمط المتبع بالفعل في المشروع. التحقق النهائي الكامل: `npm test` → 78/78، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء.

---

### 145. 🎯🎯🎯 السبب الجذري الحقيقي النهائي لكل مشكلة البطء: `handleProjects` كانت بتعيد فحص كل المحادثات من الصفر لكل مشروع على حدة
**السياق:** بعد رحلة تحقيق طويلة جدًا ومعقدة (استبعاد الشبكة، المتصفح، الكاش، VPN، Proxy، DNS — كل الاحتمالات الممكنة)، المستخدمة أبلغت إن **كل شيء بطيء، حتى تحميل المشاريع الأساسي**. كل الاختبارات السابقة (بدون جلسة تسجيل دخول صحيحة) كانت بترفض فورًا بـ401 **قبل** ما توصل لمنطق العمل الحقيقي — وده خلّى كل هذه الاختبارات "سريعة" بشكل مضلِّل تمامًا.

**الاكتشاف الحاسم:** اختبار حي بجلسة تسجيل دخول **صحيحة وحقيقية بالكامل** (لأول مرة في هذا التحقيق الطويل) كشف الحقيقة: طلب `/api/projects` استغرق **بالضبط 120 ثانية** (Timeout واضح) ثم فشل بـ**502 Bad Gateway**.

**التشخيص الدقيق:** فحص `handleProjects` كشف باگ أداء خطير: الدالة كانت بتستدعي `findAllProjectConversations(project)` **مرة منفصلة لكل مشروع** (جوّه `Promise.all`) — وهذه الدالة نفسها كانت بتعيد **فحص كل المحادثات الموجودة على السيرفر بالكامل من الصفر** (كل الصفحات، Pagination كاملة) **في كل استدعاء**! يعني التعقيد الفعلي كان **O(عدد المشاريع × إجمالي المحادثات)**، بدلًا من O(إجمالي المحادثات) فقط. بعد أيام من الاختبارات المكثفة طوال هذه الجلسة الطويلة (كل محاولات المصادقة، الترقيات، إلخ خلقت مئات المحادثات الحقيقية على الـAgent Server)، هذا التضخيم الأسي في عدد الطلبات جعل الـEndpoint يستغرق دقائق ثم يفشل بالكامل.

**الحل:** فصل الدالة لدالتين — `fetchAllConversations()` تجيب **كل** المحادثات مرة واحدة بس (Pagination كاملة مرة واحدة)، و`findAllProjectConversations(project, allConversations)` أصبحت **فلترة محلية بحتة** (بدون أي طلب شبكة إضافي خالص) على القائمة المُجلَبة مسبقًا. `handleProjects` بقت تستدعي `fetchAllConversations()` **مرة واحدة فقط** خارج حلقة المشاريع، وكل مشروع بيفلتر من نفس القائمة محليًا.

**تم التحقق حيًا بدقة قاطعة (اختبار حاسم):**
- محاكاة واقعية: **5 مشاريع، 300 محادثة إجمالية موزعة على 3 صفحات**.
- **قبل الإصلاح (نظريًا)**: 3 صفحات × 5 مشاريع = **15 استدعاء شبكي** متوقع.
- **بعد الإصلاح (مُقاس فعليًا)**: **3 استدعاءات شبكية بس** بالضبط (عدد الصفحات الإجمالي، بغض النظر عن عدد المشاريع) — **تقليل 80% في هذا السيناريو**، وكانت النسبة ستكبر بشكل كبير جدًا مع أي عدد أكبر من المشاريع (لو 20 مشروع: 60 استدعاء متوقع نظريًا مقابل 3 فعليًا — تقليل يفوق 95%).
- **زمن الاستجابة الفعلي بعد الإصلاح**: `200 OK` في **0.053 ثانية بس** (52 مللي ثانية) لنفس السيناريو اللي كان بياخد **120+ ثانية** ويفشل قبل الإصلاح.
- تم تحديث وإصلاح كل الاستخدامات الأخرى للدالة (`project-cost.mjs`) للتوقيع الجديد، وإضافة 4 اختبارات وحدة جديدة (regression tests) تؤكد صحة الفلترة المحلية في سيناريوهات متعددة.

**الحالة:** ✅✅✅ تم اكتشاف السبب الجذري الحقيقي النهائي لكل رحلة التحقيق الطويلة (بعد استبعاد كل الاحتمالات الأخرى بدقة منهجية صارمة)، الإصلاح المعماري الصحيح (تقليل التعقيد من O(مشاريع×محادثات) لـO(محادثات) فقط)، والتحقق الحي القاطع الشامل (عدّ الطلبات الفعلي + قياس الزمن الفعلي، قبل وبعد). التحقق النهائي الكامل: `npm test` → 82/82 (4 اختبارات جديدة)، `tsc -b` نظيف، `node --check` نظيف على كل الملفات المعدَّلة، `oxlint` 0 أخطاء.

**درس مهم من هذا التحقيق الطويل:** أي اختبار لسرعة Endpoint محمي بالمصادقة **لازم يتم بجلسة تسجيل دخول حقيقية وصحيحة** — الاختبار بدون مصادقة (اللي بيرفض فورًا) ممكن يعطي انطباع "سريع" مضلِّل تمامًا، ويخفي مشاكل أداء حقيقية وخطيرة جدًا في منطق العمل الفعلي.

---

### 146. تدقيق كامل لسكريبتات النشر قبل تنصيب نظيف من الصفر — اكتشاف وتصحيح مراجع قديمة
**السياق:** بعد أزمة أداء طويلة (#145) والحاجة لتنصيب نظيف كامل، طلب المستخدم التأكد أولًا إن `deploy/install.sh`/`setup.sh` فعليًا بيطبّقوا **كل** الإصلاحات قبل مسح أي حاجة على السيرفر الحالي.

**التدقيق الشامل كشف فجوتين حقيقيتين (مراجع قديمة، مش أخطاء وظيفية):**
1. **رسائل نصية مضلِّلة في `install.sh` و`deploy/README.md`**: بتشاور لبورت `15173` (منفذ Vite Dev Server القديم، **مُزال بالكامل** منذ #123 — الفرونت إند بقى Static Files تُخدَّم من نفس الـBackend). القيم الفعلية الوظيفية في `.env.staging` كانت **صحيحة 100%** بالفعل (`MKDD_UI_BACKEND_PORT=18787`)، فهذه المشكلة كانت **توثيقية/عرضية بحتة**، مش خطأ وظيفي حقيقي — لكنها كانت هتربك أي شخص يقرأ الرسائل بعد التنصيب.
2. **اسم صورة `agent-canvas` الافتراضي في `compose.yml`**: كان لسه `1.13.0-mkdd1`، رغم إن الكود الفعلي بقى **v1.15.0** بعد ترقية #138. هذا Label بس (مش يؤثر وظيفيًا، بما إن `context: ./openhands-agent-canvas` هي مصدر البناء الحقيقي)، لكن يستحق التصحيح للدقة.

**تم التحقق من عدم وجود أي فجوة وظيفية حقيقية:** `deploy/install.sh` مؤكَّد إنه شامل بالكامل (Clone كامل من GitHub + إعداد نظام النشر التلقائي `systemd timer/service` + بناء وتشغيل أولي)، ويرفض عمدًا التنفيذ لو `/opt/mkdd-live` موجودة بالفعل (حماية من الكتابة فوق تعديلات يدوية بالخطأ) — يتطلب مسح المجلد صراحةً أولًا، وهذا سلوك آمن ومقصود.

**الحالة:** ✅ تم التدقيق الشامل، اكتشاف وتصحيح مرجعين قديمين (بورت، اسم صورة)، والتأكد من سلامة المنطق الوظيفي الكامل لعملية التنصيب النظيف. التحقق: `bash -n` نظيف على السكريبتات المُعدَّلة، `compose.yml` YAML صحيح.

**⚠️ ملاحظة مهمة للتنصيب من الصفر:** الموظفون (Agent Profiles) محتاجين إعادة إنشاء بعد التنصيب النظيف، عبر تحديد `MKDD_BOOTSTRAP_LLM_PROFILE_REF` (كان `glm-5.2` في هذا النشر) عند تشغيل `setup.sh`، أو تشغيل خطوة الـBootstrap يدويًا بعد كده.

---

### 147. تحسين: إنشاء الموظفين بقى تلقائيًا بالكامل عند أي تنصيب مستقبلي
**السياق:** بعد التنصيب النظيف الأول، لاحظ المستخدم إن السكريبت "المفروض" يعمل الموظفين تلقائيًا، لكن اضطررنا نشغّل أمر يدوي منفصل لعمل ذلك.

**السبب:** إنشاء الموظفين (`bootstrap-employees.mjs`) مصمَّم عمدًا كـ"اختياري" (Opt-in) عبر متغيّر `MKDD_BOOTSTRAP_LLM_PROFILE_REF` — لأن اسم موديل الـLLM المُستخدَم يختلف من نشر لآخر، والسكريبت لا يقدر "يخمّن" هذا الاسم من تلقاء نفسه.

**الحل:** بما إن هذا النشر تحديدًا بيستخدم دايمًا نفس الموديل (`glm-5.2`)، تم توثيق هذه القيمة في `deploy/.env.staging` نفسها — بما إن `setup.sh` بتقرأ ملف `.env` تلقائيًا (`source`) **قبل** ما تتحقق من هذا المتغيّر، أي تنصيب مستقبلي (عبر `install.sh`) هيلتقط القيمة دي تلقائيًا وينشئ الموظفين **بدون أي خطوة يدوية إضافية خالص**.

**الحالة:** ✅ تم التحسين والتوثيق. **ملاحظة:** التنصيب الحالي (اللي حصل قبل هذا التحسين) احتاج تشغيل أمر Bootstrap يدوي مرة واحدة بس — أي تنصيب نظيف مستقبلي لن يحتاج لهذه الخطوة خالص.

---

### 148. الرسالة "بترجع" لمكان الكتابة بدون تفسير — السبب الحقيقي كان إعداد LLM Profile مفقود بعد التنصيب النظيف، وفجوة UX حقيقية كانت بتخفي السبب
**السياق:** بعد التنصيب النظيف، المستخدمة أبلغت إن إرسال رسالة في مشروع جديد بيرجع النص لمكان الكتابة بعد كام ثانية، وأشارت إن هذه المشكلة حصلت قبل كده واتصلحت.

**التشخيص المباشر (اختبار حي فعلي):** محاكاة إرسال رسالة حقيقية بجلسة صحيحة كشفت السبب الفعلي بوضوح تام: **`"LLM profile 'glm-5.2' not found"`** — إعداد الاتصال بمزوّد الـAI (مفاتيح API) **غير موجود** في إعدادات OpenHands الجديدة بعد التنصيب النظيف. هذا **منطقي تمامًا وليس باگ** — الإعداد ده حساس (مفاتيح API)، مُخزَّن في الـVolume الخاص بـAgent Canvas نفسه (`openhands_agent_canvas_state_staging`)، واللي اتمسح عمدًا كجزء من التنصيب النظيف الكامل. الحل: إعادة إنشاء الـLLM Profile يدويًا من واجهة OpenHands (Settings → LLM Profiles) بنفس الاسم `glm-5.2`.

**فجوة UX حقيقية إضافية اكتُشفت وأُصلحت:** رغم إن الـBackend كان بيرجّع رسالة الخطأ الحقيقية والمفيدة دي بوضوح (`created?.detail ?? created?.error`)، **الفرونت إند كان بيتجاهلها بالكامل** — الـ`catch` block في `sendMessage`/`startFreshConversation` كان بيرجّع النص للمستخدم بصمت تام، بدون أي إشارة لسبب الفشل خالص. هذا بالظبط اللي خلّى المستخدمة تشوف "الرسالة بترجع" بدون أي تفسير، مهما كان السبب الفعلي (سواء هذا السبب المحدد، أو أي سبب فشل حقيقي آخر مستقبلاً).

**الحل:** إضافة حالة `sendError` جديدة في `useConversation.ts`، تُملأ برسالة الخطأ الحقيقية عند الفشل، وتُعرض بوضوح فوق مكان الكتابة مباشرة في `ChatScreen.tsx` ("فشل الإرسال: [السبب الحقيقي]") — يعمل الآن لأي سبب فشل مستقبلي، مش بس لهذه الحالة المحدَّدة.

**الحالة:** ✅ تم التشخيص الدقيق عبر اختبار حي مباشر (السبب الحقيقي: إعداد ناقص طبيعي بعد تنصيب نظيف، مش باگ)، اكتشاف وإصلاح فجوة UX حقيقية ومهمة (رسائل الخطأ كانت مخفية بالكامل عن المستخدم)، تطبيق الإصلاح على مساري الإرسال معًا (رسالة عادية + محادثة جديدة). التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء، توازن CSS مؤكَّد.

**⚠️ خطوة مطلوبة منك:** أضيفي الـLLM Profile (`glm-5.2`) من واجهة OpenHands (`http://192.168.2.18:13000` → Settings → LLM Profiles) قبل أي اختبار آخر للإرسال.

---

### 149. "جاري التحميل" بيختفي بدون عرض المحادثة، والمحادثة بتتحمّل بشكل غير مكتمل — نفس نمط باگ #144 في ملف مختلف
**السياق:** المستخدمة أبلغت إن "جاري التحميل" بيظهر ويختفي بدون ما المحادثة تظهر خالص، وإن المحادثة (لو ظهرت) مش بتتحمّل كاملة، وده بيحصل مع كل الموظفين.

**التشخيص المباشر (استبعاد الخادم بشكل قاطع):** اختبار حي مباشر بجلسة صحيحة أكّد إن الخادم **سريع جدًا** (53-68 مللي ثانية) وبيرجّع بيانات **كاملة وصحيحة تمامًا** (محادثة، تكلفة، رسائل فعلية) — هذا استبعد بشكل قاطع 100% أي مشكلة في الخادم أو تراكم بيانات، ووجّه التشخيص مباشرة نحو الفرونت إند.

**السبب الجذري:** فحص `useConversation.ts` كشف **نفس نمط الباگ بالضبط اللي حللناه في #144** (في ملف مختلف هذه المرة) — الـ`useEffect` الأساسي المسؤول عن جلب المحادثة كان يعتمد على `[project, employee]` **كـObject References كاملة**، مش على معرّفاتهم الفعلية (`path`/`id`). أي كود بينتج Reference جديد لنفس المشروع/الموظف (زي التحديث الدوري لقائمة الموظفين من #141، أو تحديث `employees` بعد رفع صورة) كان بيُعيد تشغيل هذا الـEffect بالكامل — **بيلغي** أي طلب `fetchChatOpen` شغال بالفعل (`cancelled = true`) قبل ما يكمل، ويبدأ طلب جديد من الصفر. هذا يفسّر بدقة تامة كلا العرضين: "التحميل بيختفي بدون عرض" (الطلب اتلغى قبل ما `.then()` تتنفذ خالص)، و"تحميل غير مكتمل" (اتلغى بعد ما جزء من الرسائل اتطبّق بس).

**النطاق الأوسع للإصلاح:** فحص شامل لباقي الملف كشف **3 مواقع إضافية** بنفس النمط الخطر — Effect مسح مكان الكتابة، Effect اتصال الـWebSocket الحي، وEffect التحديث الدوري كل 15 ثانية — كلهم كانوا بيعانوا من نفس المشكلة (إعادة تشغيل/إعادة اتصال غير ضرورية بسبب Reference وهمية)، رغم إن أعراضهم أقل وضوحًا للمستخدم.

**الحل:** توحيد كل الأربعة Effects لتعتمد على `project?.path` و`employee?.id` (وأحيانًا `employee?.name` عند الحاجة الفعلية له داخل الكود) — نفس النمط المُستخدَم بالفعل ومقبول في المشروع (`useProjectTotalCost.ts`، وإصلاح #144).

**الحالة:** ✅ تم التشخيص الدقيق (استبعاد الخادم بدليل قاطع أولًا، ثم تحديد النمط بدقة عبر مطابقته مع #144 المُوثَّق)، الإصلاح الشامل لكل المواقع الأربعة المتأثرة في نفس الملف (لا الإصلاح الجزئي فقط). التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء جديدة.

---

### 150. مريم امتنعت عن رفع ملفاتها لـGitHub بسبب سوء فهم حقيقي للتعليمات (خلط بين "الدفع" و"الدمج")
**السياق:** المستخدم أبلغ إن مريم أنهت شغلها (Gate 2) لكن ملفاتها (architecture.md, ux-ui-specification.md, screen-inventory.md, حزمة prototype) لم تُرفَع لمستودع `menokemo/xeye-website` خالص. عند السؤال المباشر، مريم اعترفت بصدق كامل إنها امتنعت عمدًا عن الرفع.

**التشخيص المباشر (مراجعة النص الفعلي لتعليمات AGENTS.md):** لقيت إن مريم فسّرت التعليمات **بشكل أكثر تحفظًا من المقصود فعليًا** — القسم 2 يوضح صراحة إن "رفع الملفات وفتح Pull Request" هو **جزء طبيعي من سير العمل المتوقع** ("Typical change flow: ... branch → implement → test → commit → open PR"), بينما جملة "لا يدمج الموظف الـPull Request إلا بتوجيه صريح من المالك" **قيد محدد على الدمج (Merge) فقط**، مش على الرفع نفسه. مريم خلطت بين المفهومين، فامتنعت عن **الرفع بالكامل** ظنًا منها إنه محتاج إذن صريح غير موجود فعليًا في التعليمات.

**هذا نفس نمط المشكلة المتكرر:** موظف بيفسّر قيدًا حقيقيًا (خاص بحالة محددة) بشكل أوسع مما هو مقصود، فيمتنع عن فعل طبيعي متوقع منه — نفس الفئة العامة من مشاكل #130/#133 (سوء فهم للحدود المسموحة)، لكن هذه المرة في الاتجاه المعاكس (تحفظ زائد بدل ادّعاء زائد).

**الحل:** إضافة توضيح صريح ومباشر جدًا في `AGENTS.md` مباشرة بعد القيد الأصلي — يوضح بوضوح تام إن هذا القيد **خاص بالدمج فقط**، وإن رفع الفرع وفتح PR **لا يحتاجان طلب صريح منفصل**، وإن سوء الفهم ده معناه عمليًا إن الشغل الفعلي **لا يصل للمستودع خالص** بصمت تام، من غير ما المالك يعرف لحد ما يسأل مباشرة (بالضبط زي ما حصل هنا).

**⚠️ ملاحظة تقنية إضافية:** مريم أشارت كمان لاحتمال مشكلة تقنية منفصلة — مساحة العمل المحلية (`/projects/test-1`) قد تكون **غير مربوطة بـGit Remote خالص**، رغم إن رابط المستودع مسجّل في `docs/project-context.md`. لو صح هذا، أي محاولة `git push` ستفشل تقنيًا (مش بس بسبب التردد)، ومحتاجة ربط الـRemote أولًا (`git remote add origin ...`).

**الحالة:** ✅ تم التشخيص الدقيق (مراجعة النص الفعلي للتعليمات، مقارنة بتفسير الموظف)، والإصلاح المباشر (توضيح صريح جديد يمنع نفس سوء الفهم مستقبلًا). ترقيم الأقسام تأكد سليم (لسه متسلسل 1-14).

**⚠️ ملاحظة مهمة:** هذا إصلاح في **تعليمات الموظفين**، مش في كود MKDD — التعليمات كانت **صحيحة فعليًا من الناحية الفنية** (القيد كان محدد بدقة على "الدمج" فقط)، لكن **غير واضحة كفاية** لمنع سوء التفسير هذا. **الخطوة العملية التالية:** المالك يحتاج يطلب من مريم صراحة ترفع ملفاتها الآن، مع التأكد من ربط الـRemote أولاً لو لزم الأمر.

---

### 151. 🎯 لغز "GITHUB_TOKEN غير ظاهر للـshell" — رحلة تحقيق، حل مؤقت اتلغى، والوضع الحالي
**السياق:** بعد إصلاح #150، ظهرت مشكلة تقنية حقيقية: الموظفون (مريم تحديدًا) لم يستطيعوا رفع ملفات صور (PNG/JPG) لـGitHub، لأن أدوات `git`/`gh` المحلية لا تجد `GITHUB_TOKEN` في بيئة الـshell خالص، رغم إن اتصال GitHub MCP نفسه شغال (بيسمح بالملفات النصية بس حسب ادّعاء الموظف).

**رحلة التحقيق (نتائج مؤكَّدة تستحق الاحتفاظ بها كمعرفة، بغض النظر عن الحل النهائي):**
1. فحص مباشر لـ`openapi.json` الفعلي لهذا الإصدار (`ghcr.io/openhands/agent-server:1.42.1-python`) أكّد **عدم وجود** أي Endpoint لآلية "Git Provider Tokens" الرسمية الموثَّقة عمومًا — فقط `/api/git/changes|commits|diff` لعرض التغييرات المحلية.
2. حقل `SaveProfileRequest.include_secrets` تبيّن أنه غير ذي صلة (خاص بمفتاح الـLLM نفسه).
3. آلية `X-Expose-Secrets: encrypted` (الموثَّقة عمومًا لجلب أسرار مشفَّرة تُمرَّر وقت إنشاء محادثة) — اختبار حي مباشر لهذا الـHeader على الاستجابة الحقيقية أكّد **عدم وجود** أي حقل `secrets` خالص في الرد، رغم التوثيق العام.
4. اكتُشف خطأ إملائي حقيقي بالتوازي: الاسم المُسجَّل فعليًا في إعدادات المستخدم كان `GITHUP_TOKEN` (حرف P بدل B).
5. **اكتشاف مهم توضّح لاحقًا:** السرّ `GITHUB_PERSONAL_ACCESS_TOKEN` الموجود كان **مُولَّدًا تلقائيًا بواسطة OpenHands نفسها** عند إعداد اتصال GitHub MCP — وليس شيئًا سمّاه المستخدم يدويًا. هذا يعني أنه على الأرجح **مرتبط داخليًا بأداة MCP نفسها فقط** (لاستدعاء GitHub API مباشرة من جانب الأداة)، وليس بالضرورة مصمَّمًا للتصدير كمتغيّر بيئة شل عام لـ`git`/`gh`.

**حل مؤقت جُرِّب ثم اتلغى بتوجيه صريح من المالك:** بُني حل ذاتي داخل MKDD (متغيّر `MKDD_GITHUB_TOKEN` يُمرَّر مباشرة لكل محادثة جديدة). **تم التراجع عنه بالكامل (`git revert`)** — المالك أوضح بحق أن ازدواجية حفظ نفس التوكن في مكانين منفصلين (إعدادات OpenHands + متغيّر MKDD خاص) ليست حلًا حقيقيًا، بل عبء صيانة يدوي إضافي بلا فائدة أتمتة فعلية.

**الوضع الحالي (قيد التحقيق النشط):** بما إن أداة GitHub MCP تستخدم GitHub API الحقيقي داخليًا، وGitHub API يدعم رفع أي نوع ملف (شامل الصور) كـ base64، هناك احتمال حقيقي أن ادّعاء الموظف "MCP لا يدعم الملفات الثنائية" كان **افتراضًا غير مُتحقَّق منه** (بنفس نمط الادّعاءات الزائدة السابقة #130/#133)، وليس قيدًا فعليًا مؤكَّدًا للأداة. الخطوة التالية: مطالبة مريم — في نفس محادثتها الحالية — برفع صورة واحدة تحديدًا عبر أداة GitHub MCP نفسها (لا `git` المحلي)، لتأكيد أو نفي هذا الادّعاء بدليل مباشر قبل أي حل معماري إضافي.

**الحالة:** ⏳ الحل المؤقت اتلغى بنجاح (تحقق: `npm test` → 82/82، `chat.mjs`/`compose.yml` سليمين بعد الإلغاء). التحقيق مستمر — بانتظار نتيجة اختبار رفع الصورة المباشر عبر MCP.

---

### 152. 🎯 الموظف "بيقف فجأة" أثناء الشغل ومحتاج ريستارت للحاويات — سبب حقيقي: Git بيرفض التعامل مع /projects بعد أي تغيير ملكية
**السياق:** المستخدمة أبلغت إن الموظف بيبدأ يشتغل، يعمل جزء كبير من المهمة، ثم "بيقف" فجأة، ومحتاج إعادة تشغيل الحاويات عشان يرجع يشتغل. جرّبت كمان تفتح محادثة جديدة مباشرة من واجهة OpenHands نفسها وظهر خطأ `500 Internal Server Error`.

**التشخيص المباشر (فحص الموارد أولًا لاستبعاد الاحتمال الأخطر):** فحص الذاكرة والحاويات أكّد سلامة كاملة — 12 جيجا متاحة، الحاويات شغالة بشكل طبيعي بدون أي كراش أو ضغط موارد. هذا استبعد فرضية "نفاد ذاكرة" بشكل قاطع.

**الخطأ الفعلي (من رسالة الخطأ المباشرة):** `"exception":"Git command failed: git worktree add -b openhands/... HEAD"` — فشل حقيقي في إنشاء "Worktree" جديد (نسخة عمل منفصلة من المستودع) لكل محادثة جديدة، وهي الآلية اللي OpenHands بتستخدمها لعزل كل محادثة عن غيرها داخل نفس المشروع.

**السبب الجذري:** `git status` مباشرة كشف رسالة Git معروفة: `"fatal: detected dubious ownership in repository"` — إجراء أمان قياسي في Git (منذ CVE-2022-24765) بيرفض التعامل مع أي مستودع لو ملكية ملفاته **لا تطابق تمامًا** المستخدم الحالي اللي بيشغّل الأمر. بما إن `/projects` عبارة عن **Volume خارجي مُشارَك** (مش جزء من صورة الحاوية نفسها)، أي تغيّر في ملكية ملفاته (زي إعادة إنشاء الـVolume أثناء تنصيب نظيف، أو أي عملية خارجية لمست الملفات) بيكسر التطابق ده، وGit بيرفض **كل** الأوامر (شامل `worktree add`) في هذا المستودع من هذه اللحظة فصاعدًا — وهو بالضبط اللي بيظهر كـ"الموظف بيقف فجأة" (بما إن أي محاولة إنشاء محادثة جديدة أو Worktree جديد بعد كده هتفشل بنفس السبب).

**الحل الفوري (مؤقت):** `docker exec openhands-agent-canvas-staging git config --global --add safe.directory '*'` — استثناء صريح يقبل كل المستودعات بغض النظر عن الملكية.

**⚠️ ملاحظة حرجة:** هذا الحل الفوري **مؤقت فقط** — بيتخزن في بيئة الحاوية الحالية، وسيضيع في أي إعادة بناء/تشغيل مستقبلية للحاوية (وهو الأرجح ما فسّر ظهور المشكلة "فجأة" — الإعداد كان ضاع في إعادة بناء سابقة أثناء ترقيات/تنصيبات هذه الجلسة).

**الحل الدائم:** إضافة `RUN git config --global --add safe.directory '*'` مباشرة في `Dockerfile` بتاع `openhands-agent-canvas` (بعد `USER openhands`، قبل `VOLUME`) — بما إنه بيتنفذ **وقت بناء الصورة نفسها** (تحت هوية المستخدم `openhands` الصحيحة)، الإصلاح **دائم** عبر أي عدد من إعادة التشغيل أو إعادة إنشاء الـVolume مستقبلاً، بدون الحاجة لأي تدخل يدوي متكرر.

**اكتشاف إضافي أثناء التشخيص:** `git status` كشف إن مستودع `/projects/test-1` **لم يبدأ أي Commit خالص** ("No commits yet") — هذا يفسّر جزء إضافي من مشكلة #150/#151 (عدم قدرة مريم على رفع ملفاتها)، بما إن مستودع بدون تاريخ Git أصلاً محتاج `git init` وCommit أولي قبل أي `push` حقيقي.

**الحالة:** ✅ تم التشخيص الدقيق (استبعاد الموارد أولًا، تتبّع الخطأ الفعلي، تحديد السبب الجذري القياسي في Git)، الإصلاح الفوري (استعادة الخدمة فورًا)، والإصلاح الدائم في مصدر بناء الصورة (يمنع تكرار المشكلة مستقبلاً بشكل كامل).

**🚨 خطوة مطلوبة لتفعيل الإصلاح الدائم:** إعادة بناء صورة `agent-canvas` (`docker compose build --no-cache agent-canvas && docker compose up -d agent-canvas`) لتضمين التعديل الجديد في الـDockerfile.

---

### 153. 🚨🚨 حرج جدًا: مفيش حد قادر يسجّل دخول — استثناء غير معالج بعد إرسال الرد بيكسر السيرفر كله
**السياق:** المستخدمة أبلغت إنها "متأكدة 100%" من اسم المستخدم وكلمة السر، لكن تسجيل الدخول بيفشل برسالة عامة "حصل خطأ، حاول تاني".

**التشخيص المباشر:** فحص لوج `mkdd-ui-staging` كشف الحقيقة: `Error [ERR_HTTP_HEADERS_SENT]: Cannot write headers after they are sent to the client` — **انهيار كامل لعملية Node.js بالكامل**، مش خطأ عادي في طلب واحد. الحاوية كانت بتعيد تشغيل نفسها تلقائيًا بعد كل انهيار (بفضل Docker)، لكن أي طلب (شامل محاولات تسجيل الدخول) كان شغال وقت الانهيار كان بيفشل فورًا بشكل غير متوقع.

**السبب الجذري:** `handleAuthLogin` بترسل `res.writeHead(200, ...)` ثم بتستدعي `buildOwnerAvatarUrl(user.id)` **كـArgument** جوّه `JSON.stringify(...)` لـ`res.end(...)`. هذه الدالة كانت بتستخدم `fs.statSync(found.file)` **بدون أي حماية (`try/catch`)** — لو الملف المُشار إليه (اللي `findOwnerAvatarFile` رجّعه بافتراض وجوده) **غير موجود فعليًا أو غير قابل للقراءة** (زي بعد أي عملية تنظيف/إعادة بناء Volume، بما إن الجلسة شهدت تنصيبات نظيفة وترقيات متعددة)، `fs.statSync()` بترمي استثناء. بما إن الـHeaders **اتبعتت بالفعل** (`writeHead(200)` نجحت)، لكن `res.end()` **لم يُستدعَ خالص** (الاستثناء حصل قبل ما يوصل لها)، الاستثناء وصل لـ`catch` الخارجي في `index.mjs`، اللي **حاول يكتب Headers تانية** فوق رد اتبعت جزئيًا بالفعل — وهذا التعارض هو اللي بيكسر عملية Node.js بالكامل (`ERR_HTTP_HEADERS_SENT` غير معالج بيوقف العملية).

**الإصلاح (طبقتين دفاعيتين):**
1. **إصلاح مباشر**: `buildOwnerAvatarUrl` أصبحت بتلتقط أي خطأ من `fs.statSync()` وترجّع `null` بدل ما ترمي استثناء — تدهور بأمان (بدون صورة) بدل انهيار كامل.
2. **حماية عامة إضافية (Defense-in-depth)**: الـ`catch` الخارجي في `index.mjs` أصبح يتحقق من `res.headersSent` **قبل** أي محاولة لكتابة Headers — لو الرد اتبعت جزئيًا بالفعل، يُسجَّل الخطأ في اللوج فقط بدل محاولة الكتابة فوقه. هذا يحمي من **أي فئة مشابهة من الأخطاء مستقبلاً** في أي Route تاني، مش بس هذا السيناريو المحدد.

**تم التحقق حيًا بدقة قاطعة (اختبار حاسم):** محاكاة مباشرة لملف صورة موجود لكن غير قابل للقراءة (`chmod 000`) — تسجيل الدخول **نجح** (`200`، `avatarUrl: null`)، والسيرفر **بقي حيًا تمامًا** بعدها (تأكيد إضافي عبر طلب تالٍ ناجح)، واللوج خالٍ تمامًا من أي انهيار.

**الحالة:** ✅ تم التشخيص الدقيق (تتبّع اللوج الفعلي، تحديد السبب الجذري بدقة عبر تحليل ترتيب `writeHead`/الاستثناء)، إصلاح مزدوج (مباشر + حماية عامة)، والتحقق الحي القاطع (محاكاة السيناريو الحقيقي بالضبط). التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `node --check` نظيف على كلا الملفين، `oxlint` 0 أخطاء.

---

### 154. 🆕 تسليم التقارير تلقائيًا للموظف المستلم — يبدأ الشغل فورًا بدون تدخل المالك
**السياق:** المالك طلب تعديل نظام التقارير بحيث الموظف المستلم لتقرير **يشوفه ويشتغل عليه مباشرة** بدون انتظار تدخل المالك، ولما يخلص، الحالة تتحدّث تلقائيًا، والمالك يشوفها ويقرر الإغلاق.

**التقييم الأولي:** فحص الكود الحالي كشف إن **جزء من المطلوب موجود بالفعل** — لما الموظف المستلم يستدعي `action: "respond"`، حالة التقرير بتتحدّث تلقائيًا فورًا (بدون أي تدخل من المالك). **الفجوة الحقيقية الوحيدة**: مفيش أي آلية تلقائية "توقّظ" الموظف المستلم ليشتغل على التقرير — المالك كان لازم يبدأ محادثة يدويًا وينقل محتوى التقرير بنفسه، وهذا بالضبط الاحتكاك اللي طلب المالك إزالته.

**التصميم:** بما إن `AGENTS.md` نفسها توثّق صراحة إن **موظف واحد لا يقدر يبدأ أو يرسل لمحادثة موظف تاني مباشرة** (قيد أمان معماري متعمَّد — فقط الـBackend الخاص بـMKDD يملك بيانات اعتماد إنشاء المحادثات الحقيقية)، هذا بالضبط المكان الصحيح لهذه القدرة — MKDD نفسها، مش أي موظف.

**التنفيذ:**
1. استخراج دالة مساعدة قابلة لإعادة الاستخدام `deliverMessageToEmployee` من منطق `/api/chat/send` الموجود بالفعل (بحث عن محادثة موجودة وإرسال رسالة لها، أو إنشاء محادثة جديدة) — قابلة للاستدعاء مباشرة من كود الخادم الآخر بدون المرور عبر دورة HTTP كاملة.
2. لما تقرير جديد يتسجل (`action: "add"`)، الخادم يبني رسالة واضحة تلقائيًا (اسم الموظف المُرسِل، العنوان، التفاصيل، وأمر `curl` جاهز للرد **مع معرّف التقرير الحقيقي مُضمَّن مسبقًا** — لا حاجة للموظف المستلم يبحث عنه)، ويسلّمها فورًا عبر `deliverMessageToEmployee`.
3. **تصميم غير حاجز (Non-blocking) ومقاوم للفشل**: التسليم يحدث بشكل "أطلق ولا تنتظر" (`fire-and-forget`، غير منتظَر `await`) — طلب تسجيل التقرير يرجع فورًا **بدون** انتظار اكتمال عملية التسليم (اللي قد تتضمن إنشاء محادثة جديدة كاملة). أي فشل في التسليم **لا يفشل تسجيل التقرير نفسه أبدًا** — التقرير يبقى مسجَّلًا وظاهرًا على بطاقة البوابة كخط رجعة (Fallback) في كل الأحوال.

**خطأ حقيقي اتلقى وصُحح أثناء البناء:** أول محاولة استخدمت `void functionCall()` جوّه `try/catch` — وهذا **لا يلتقط أي خطأ فعليًا** (بما إن الدالة غير منتظَرة، الـ`try` block ينتهي تنفيذه قبل ما الـPromise يستقر). اتصحح باستخدام `.catch()` صريح على الـPromise نفسها.

**تم التحقق حيًا بدقة قاطعة (اختبار شامل):**
- **زمن الاستجابة**: طلب تسجيل التقرير رجع في **23 مللي ثانية فقط** — تأكيد إن التصميم غير الحاجز يعمل صحيح.
- **محتوى الرسالة المُسلَّمة**: تأكيد مباشر إن الرسالة وصلت فعليًا للموظف المستلم، بمحتوى صحيح تمامًا — شامل **معرّف التقرير الحقيقي** (لا Placeholder) جاهز للاستخدام المباشر في أمر الرد.

**الحالة:** ✅ Feature جديد مبني ومُختبَر بالكامل، يحقق المطلوب بدقة: تسليم فوري، تحديث حالة تلقائي (موجود بالفعل)، لا يحجب أو يفشل التسجيل الأساسي. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `node --check` نظيف على كلا الملفين، `oxlint` 0 أخطاء.

---

### 155. سد فجوة تعليمات: "المالك بيطلب تقرير صراحة" مش نفس "الموظف بيلاحظ مشكلة بنفسه"
**السياق:** بعد بناء التسليم التلقائي للتقارير (#154)، المالك أوضح إنه عايز الموظفين يفهموا بدقة إن طلب "اعمل تقرير" منه صراحة يعني استخدام نظام التقارير الرسمي، مش أي تفسير عام تاني — بناءً على تجربة كيرلس اللي فسّر الطلب على إنه "اكتب مستند Markdown" بدل استدعاء الـAPI.

**التشخيص:** فحص قسم "Recording a cross-employee report" في `AGENTS.md` كشف الفجوة بدقة — القسم بالكامل (العنوان والسياق والأمثلة) موجّه لسيناريو **"الموظف بيلاحظ مشكلة بنفسه بشكل استباقي"**، ومفيش أي جملة صريحة تربط **"المالك (الإنسان) طلب تقرير مباشرة"** بنفس النظام. موظف بيقرأ التعليمات حرفيًا مش هيلاقي أي إشارة واضحة تخليه يربط طلب المالك العام بهذا الـAPI تحديدًا.

**الحل:** إضافة توضيح صريح جدًا في بداية القسم مباشرة — يوضح بلا لبس: لو المالك طلب "اعمل تقرير" أو "سجّل تقرير" عن شغل موظف تاني، ده **يعني** استدعاء الـAPI، مش كتابة مستند. مستند Markdown للتوثيق الداخلي شيء جيد يُضاف بجانب الـAPI، لكن **لا يغني عنه أبدًا**.

**تعزيز إضافي:** بما إن #154 بنى الآن تسليمًا تلقائيًا حقيقيًا للتقارير، هذا التوضيح يمنع أيضًا محاولات مستقبلية من موظفين يبحثون عن "طريقة تقنية بديلة" لتسليم العمل يدويًا (زي محاولة كيرلس السابقة للوصول لـDocker socket أو مفاتيح الجلسة) — بمجرد ما يفهموا إن استدعاء الـAPI البسيط كافٍ وحده لتسليم كامل تلقائي.

**الحالة:** ✅ تم تحديد الفجوة بدقة (مراجعة نص التعليمات الفعلي مقابل السلوك الملحوظ)، والإصلاح المباشر (توضيح صريح وواضح). ترقيم الأقسام تأكد سليم (لسه متسلسل 1-14).

---

### 156. ترقية OpenHands Agent Canvas من v1.15.0 لـv1.16.0 — استهداف باگ "WebSocket الأحداث الصامت" المعروف رسميًا
**السياق:** اكتُشف حيًا (عبر فحص لوج `agent-canvas` وقت حدوث المشكلة مباشرة) إن تحميل محتوى المحادثات في واجهة OpenHands الأصلية نفسها كان بيعلّق للأبد — الـWebSocket المسؤول عن بث أحداث المحادثة (`/sockets/events/{id}`) كان بيتصل وبيتصادق عليه بنجاح تام، لكن **لا يرسل أي بيانات فعلية بعد ذلك خالص**. إعادة تشغيل حاوية `agent-canvas` كانت تحل المشكلة مؤقتًا في كل مرة.

**البحث:** تقرير رسمي مفتوح من فريق OpenHands نفسه (بتاريخ حديث نسبيًا) بعنوان **"Enhancement: Display conversation history immediately while sandbox is waking up"** يصف بدقة نفس الظاهرة — نافذة محادثة فارغة أثناء "استيقاظ" بيئة العمل. **⚠️ ملاحظة أمانة:** هذا التقرير مُصنَّف "Enhancement" لا "Bug مُصلَح ومؤكَّد"، فلا يوجد ضمان قاطع أن الترقية تحل هذه المشكلة تحديدًا — تم إبلاغ المالك بهذا الأمر بوضوح قبل اتخاذ قرار الترقية، وقرر المضي قدمًا رغم عدم اليقين الكامل.

**التنفيذ (بنفس المنهجية المُثبَتة من ترقية #138):**
1. استُنسخ `OpenHands/OpenHands` مباشرة عند وسم `v1.16.0`.
2. **اكتشاف مهم (مختلف عن الترقية السابقة):** مقارنة الملف الأصلي النظيف كشفت إن ملف `agent-profiles-local-view.tsx` في v1.16.0 نفسه **تغيّر جوهريًا** — إضافة ميزة حقيقية جديدة من OpenHands (`enable_switch_llm_tool`)، متطابقة تمامًا مع ملاحظات إصدار v1.16.0 الرسمية ("feat(settings): expose the LLM-switching toggle"). هذا يختلف عن الترقية السابقة (حيث كان الفرق مطابقًا حرفيًا لتعديلنا فقط، بدون أي تغيير Upstream).
3. **دُمِج التعديلان بعناية** (بدلًا من الاستبدال الكامل الذي كان يمحو الميزة الجديدة بالخطأ) — تم تطبيق كل نقاط تعديلنا الثمانية (حالة `systemMessageSuffix`، إعادة التعيين في 3 دوال، بناء حقل الحفظ، مصفوفة الاعتماديات، وواجهة "Employee Instructions") يدويًا وبدقة فوق نسخة v1.16.0 النظيفة، مع الحفاظ الكامل على `enable_switch_llm_tool` الجديدة.
4. **تحقق حاسم بعد الدمج:** `diff` مباشر ضد نسخة v1.16.0 النظيفة أظهر **فقط** إضافاتنا الثمانية المتوقعة بدقة تامة، بدون أي حذف أو تعديل غير مقصود لأي جزء من الكود الأصلي.
5. تأكيد إضافي: `websocket-auth.ts` (شكل رسالة المصادقة `{type: "auth", session_api_key}`) **لم يتغيّر خالص** — نفس النمط المُعتمَد عليه بالكامل.

**تم التحقق:** الحجم معقول (20 ميجا)، `package.json` يؤكد صراحةً `"version": "1.16.0"`، توازن الأقواس في الملف المدموج سليم (97/97)، لا وجود لـ`node_modules`/`.git` غير مرغوب فيهما.

**الحالة:** ⏳ الترقية جاهزة ومُوثَّقة بدقة (تحديث حقيقي مدموج بعناية، لا استبدال أعمى). **الخطوة المتبقية:** بناء صورة Docker جديدة واختبارها حيًا على Staging، ثم التأكد الفعلي هل باگ "WebSocket الأحداث الصامت" اتحل فعليًا أو لا (بما إن الفائدة غير مضمونة كما وُثِّق أعلاه).

---

### 157. 🆕 نظام مراقبة استباقي + اختبارات دخان — سد فجوتين معماريتين كبيرتين
**السياق:** بعد رحلة طويلة جدًا من التحقيقات اليدوية المرهقة النهاردة، سأل المالك عن أكبر فجوتين في تصميم MKDD الحالي. تم تحديد اثنتين حرجتين: (1) الاعتماد الهش على تفاصيل داخلية غير موثَّقة بدقة في OpenHands، و(2) غياب أي مراقبة استباقية — كل مشكلة اليوم اكتُشفت **بعد** أن أثّرت على المالك فعليًا، لا قبلها.

**الحل المبني (نظامان مترابطان بمعمارية مشتركة):**

**1. Endpoint داخلي جديد (`/api/internal/alert` + `/api/internal/health-deep`)**
- يستخدم بالضبط نفس آلية المفتاح الآمن الموجودة بالفعل من #134/#157 (`X-Internal-Service-Key`)، ممتدة لتغطي مسار `/api/internal/` بجانب `/api/workflow/` الأصلي.
- **`/api/internal/health-deep`**: فحص حقيقي شامل — يستدعي بالضبط نفس منطق البحث عن محادثة الحقيقي (`findAuthorizedConversation`)، **بمهلة داخلية 10 ثوانٍ مضمونة عبر `Promise.race`** — هذا الفحص بالتحديد هو الذي كان سيكتشف تلقائيًا مشكلة "WebSocket الأحداث الصامت" التي استغرق تشخيصها اليدوي وقتًا طويلاً اليوم.
- **مصمَّم عمدًا كـ"بحث فقط"، لا "إنشاء محادثة"** — درسًا مباشرًا من #145: أي إجراء آلي متكرر ضد قائمة المحادثات الحية يجب أن يبقى رخيصًا وللقراءة فقط، وإلا أعاد خلق نفس مشكلة التراكم التي أُصلِحت هناك.

**2. سكريبت `deploy/health-check.sh` (بمعمارية مطابقة لـ`auto-deploy.sh` الموجود)**
يفحص كل عرَض حقيقي اكتُشف حيًا اليوم بالذات:
- حالة الحاويات (`docker inspect`) — يكتشف حالات `Exited (137)` المتكررة التي واجهناها.
- `/api/health` و`agent-canvas /ready` بدون مصادقة.
- **سلامة Git Ownership** (#152) — يتحقق من وجود استثناء `safe.directory` الدائم داخل الحاوية.
- **جدولة `mkdd-auto-deploy.timer`** — يكتشف مشكلة "غياب نقطة الانطلاق" (`NEXT` فارغة) التي واجهناها **مرتين منفصلتين** اليوم.
- الفحص العميق الاختياري عبر `/api/internal/health-deep` (يُتخطَّى بأمان إذا لم يُضبَط، بدلًا من الفشل).
- عند أي فشل، يبني رسالة واضحة ويستدعي `/api/internal/alert` — تنبيه فوري للمالك، بدلًا من اكتشاف العطل بالصدفة.

**خطأ حقيقي اكتُشف وأُصلح أثناء الاختبار الحي:** استخدام الفاصلة العربية (، وهو حرف UTF-8 متعدد البايتات) كمحدِّد (`delimiter`) لأمر `paste` تسبَّب في **تلف فعلي للنص العربي** (ظهور `\udcd8` غير صالح بدلًا من الفاصلة الصحيحة `\u060c`) — اكتُشف عبر اختبار مباشر لناتج السكريبت، وأُصلح باستبدال `paste` بحلقة `for` بسيطة وآمنة تمامًا مع UTF-8.

**استخدامان مترابطان:**
1. **اختبار دخان يدوي**: تشغيل السكريبت مباشرة بعد أي ترقية أو نشر كبير — هذا بالضبط الفحص الذي كان سيكتشف كل مشكلة رئيسية واجهناها اليوم (`#142` الصحة، `#152` ملكية Git، `#157` التعليق الصامت) في **دقائق**، بدلًا من ساعات من التحقيق اليدوي.
2. **مراقبة دورية آلية**: `mkdd-health-check.timer` (كل 5 دقائق) يشغّل نفس السكريبت تلقائيًا، مع تنبيه فوري عند أي فشل.

**حل استباقي إضافي مهم — منع تكرار مشكلة "نقطة الانطلاق":** بما أن هذه المشكلة (`OnUnitActiveSec` لا تبدأ العد بدون تشغيل فعلي واحد للخدمة) واجهتنا يدويًا **مرتين منفصلتين** اليوم مع `mkdd-auto-deploy.timer`، أُضيف إصلاح استباقي دائم في `install.sh` — يُشغِّل كلا الخدمتين (`mkdd-auto-deploy.service` و`mkdd-health-check.service`) مرة واحدة تلقائيًا فور التفعيل، مما يضمن جدولة صحيحة فورية في **أي تنصيب نظيف مستقبلي**، بدون أي تدخل يدوي أبدًا.

**تم التحقق حيًا بدقة قاطعة (سلسلة اختبارات شاملة):**
1. رفض/قبول المفتاح الداخلي بدقة، ونطاق الأمان الضيق (لا يفتح `/api/projects`).
2. الفحص العميق: حالة سليمة → نجاح فوري (`elapsedMs: 20-34`)؛ حالة تعليق محاكاة (Backend وهمي مُعطَّل عمدًا) → فشل واضح خلال 10 ثوانٍ بالضبط بدون أي اعتماد على مهلة خارجية.
3. السكريبت الكامل ضد بيئة محاكاة شاملة: كل الفحوصات المبنية على HTTP نجحت بدقة (`/api/health` ✅، `/ready` ✅، الفحص العميق ✅ `elapsedMs: 4`)؛ فحوصات Docker/systemctl فشلت **فقط** بسبب غياب هذه الأدوات في بيئة الاختبار نفسها (سلوك متوقع وصحيح تمامًا).
4. اكتشاف وإصلاح باگ حقيقي في معالجة UTF-8 (الفاصلة العربية) أثناء نفس عملية الاختبار.

**الحالة:** ✅ نظام كامل مبني، مُختبَر حيًا بدقة على كل مستوى ممكن اختباره في بيئة التطوير، جاهز للنشر. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء، `bash -n` نظيف على كل السكريبتات.

**🚨 خطوة مطلوبة بعد النشر:** لتفعيل الفحص العميق (اختياري لكن موصى به بشدة)، اضبطي في ملف `.env` الفعلي على الـVM: `MKDD_HEALTH_CHECK_PROJECT` و`MKDD_HEALTH_CHECK_EMPLOYEE_ID` و`MKDD_HEALTH_CHECK_EMPLOYEE_NAME` بقيم مشروع/موظف حقيقيين موجودين بالفعل.

---

### 158. 🆕 شاشة "صحة النظام" في السايدبار — مكان دائم لرؤية الحالة الحية، لا مجرد إشعار عابر
**السياق:** استكمالًا لنظام المراقبة الاستباقي (#157)، طلب المالك إضافة تبويب في السايدبار يعرض صحة المنظومة كاملة بوضوح، بدلًا من الاعتماد على إشعار Push فقط (اللي ممكن يفوت، أو المالك يكون بعيد عن جهازه وقتها).

**التنفيذ (يبني مباشرة فوق بنية #157 الموجودة):**
1. **تعديل `deploy/health-check.sh`**: بجانب الطباعة على الطرفية والتنبيه عند الفشل (السلوك الأصلي من #157)، السكريبت الآن يكتب **ملف حالة JSON منظَّم** (`mkdd-data/health-status.json`، على نفس الـVolume المُشترَك بين الحاويتين) بعد كل تشغيل — يحتوي على وقت آخر فحص، الحالة الإجمالية، وقائمة تفصيلية بكل فحص وحالته.
2. **مبني بأمان تام لتفادي تكرار خطأ سابق**: النتائج تُجمَّع بصيغة مفصولة بـ`Tab` (لا فاصلة عربية أو أي حرف UTF-8 متعدد البايتات كمحدِّد)، ثم تُمرَّر لـ`python3` الذي يبني الـJSON بالكامل عبر `json.dumps` — تفاديًا مباشرًا لتكرار باگ تلف UTF-8 المُكتشَف في #157 لنفس هذا الملف بالذات.
3. **Endpoint جديد `GET /api/system-health`**: يقرأ هذا الملف ويرجّعه للمتصفح — بخلاف الـEndpoints الداخلية الأخرى (#157)، هذا يستخدم **المصادقة العادية بجلسة المالك** (وليس المفتاح الداخلي)، بما أنه يُستدعى مباشرة من المتصفح.
4. **شاشة جديدة `SystemHealthModal.tsx`**: عنصر جديد في السايدبار ("صحة النظام")، يعرض الحالة الإجمالية (✅ سليم / ❌ فيه مشاكل) بوضوح تام، مع قائمة تفصيلية بكل فحص وحالته (نجاح/فشل/تخطّي)، ووقت آخر فحص. يتحدّث تلقائيًا كل 30 ثانية بنمط "آخر بيانات معروفة صحيحة" (لا يُفرغ الشاشة عند فشل تحديث عابر).

**تم التحقق حيًا بدقة قاطعة:**
1. **كتابة ملف الحالة**: اختبار حي كامل أكّد إن الملف الناتج **JSON صالح تمامًا** (`python3 json.load` نجح)، بمحتوى دقيق ومنظَّم (7 فحوصات بحالاتها الصحيحة).
2. **الـEndpoint الجديد**: (أ) بدون ملف حالة → حالة "لسه معملش فحص" واضحة، (ب) بدون مصادقة → رفض 401 (تأكيد استخدام المصادقة العادية بشكل صحيح، لا المفتاح الداخلي)، (ج) بملف حالة حقيقي → المحتوى الصحيح يرجع بدقة تامة.
3. تم اكتشاف وتصحيح مسار افتراضي غير دقيق لملف الحالة أثناء المراجعة (كان يشير لمسار غير مطابق للـVolume الفعلي في `compose.yml`) قبل أي نشر — تم تصحيحه ليطابق `./mkdd-data` المُعرَّف فعليًا.

**الحالة:** ✅ نظام كامل من طرف لطرف (سكريبت → ملف حالة → Endpoint → شاشة سايدبار)، مُختبَر حيًا بدقة على كل مستوى. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `bash -n` نظيف، توازن CSS مؤكَّد.

---

### 159. 🆕 سجل تاريخي للحوادث + إصلاح باگ حقيقي في اكتشاف التحوّلات
**السياق:** المالك طلب توسيع شاشة "صحة النظام" (#158) لتشمل تبويبين: الحالة الحية (الموجودة بالفعل)، **وسجل تاريخي** يوضح متى حصلت كل مشكلة سابقًا — بما إن الحالة الحية وحدها لا تكشف عن حوادث حصلت وانحلّت قبل آخر فحص.

**قرار تصميم مهم**: بدل تسجيل كل فحص في كل تشغيل (كل 5 دقائق، أغلبها "لسه سليم" مكرر وغير مفيد)، النظام يسجّل **التحوّلات فقط** — لحظة تحوّل فحص من سليم لفاشل (`became_unhealthy`) أو العكس (`recovered`). هذا هو الإشارة الفعلية المفيدة لسجل حوادث حقيقي.

**خطأ حقيقي جوهري اكتُشف وأُصلح أثناء الاختبار الحي:** التنفيذ الأول اعتمد على **نص الرسالة نفسها** كمعرِّف لكل فحص (`log_pass "Agent Canvas /ready responds"`). عند اختبار سيناريو "كسر ثم إصلاح" فعليًا، **لم يُسجَّل أي تحوّل خالص** رغم النجاح الواضح في الفحوصات نفسها. التشخيص المباشر (فحص محتوى ملف الحالة، ثم تشغيل منطق المقارنة يدويًا) كشف السبب بدقة: رسالة الفحص الناجح ("...responds") ورسالة نفس الفحص الفاشل ("...did NOT respond within 10s") **نصان مختلفان تمامًا**، فمنطق المقارنة اعتبرهما فحصين منفصلين لا علاقة بينهما، لا نفس الفحص بحالة متغيّرة.

**الإصلاح:** إعادة هيكلة `log_pass`/`log_fail`/`log_skip` بالكامل لتفصل **معرِّف ثابت مستقر لكل فحص** (`check_id`، مثل `agent_canvas_ready`) عن **الرسالة الوصفية المتغيّرة**. تم تحديث كل نقطة استدعاء (13 موقعًا) في السكريبت بأكمله.

**التنفيذ الكامل (سلسلة كاملة من طرف لطرف):**
1. `health-check.sh`: يقرأ ملف الحالة *السابق* قبل الكتابة فوقه، يقارن كل فحص بمعرِّفه الثابت، ويُلحِق أي تحوّل حقيقي بملف سجل محدود الحجم (`health-history.jsonl`، بحد أقصى 500 حدث، بصيغة JSON Lines).
2. `GET /api/system-health-history`: يقرأ هذا الملف ويرجّعه **بالترتيب العكسي** (الأحدث أولًا).
3. `SystemHealthModal.tsx`: أصبح بتبويبين ("الحالة الحية" و"السجل")، كل واحد يعرض بياناته الخاصة بوضوح تام.

**تم التحقق حيًا بدقة قاطعة (سلسلة اختبارات متعددة):**
1. **اختبار الباگ الأصلي**: أثبت مباشرة عدم تسجيل أي تحوّل رغم فشل ونجاح حقيقيين واضحين.
2. **بعد الإصلاح**: نفس السيناريو بالضبط (كسر `agent-canvas /ready` ثم إصلاحه) سجّل بدقة تامة حدثي `became_unhealthy` ثم `recovered`، بمعرِّف ثابت صحيح ورسائل وصفية دقيقة لكل حالة.
3. **الـEndpoint الجديد**: بدون سجل → قائمة فارغة آمنة؛ بسجل حقيقي → البيانات ترجع بترتيب زمني عكسي صحيح.
4. صحة JSON لكل سطر في ملف السجل مؤكَّدة (`json.loads` نجح على كل سطر).

**الحالة:** ✅ باگ حقيقي جوهري اكتُشف وأُصلح بفضل الاختبار الحي الصارم (وليس افتراضًا نظريًا) قبل أي نشر، نظام كامل من طرف لطرف مبني ومُختبَر. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 160. 🎯🎯 نظام المراقبة الجديد يلقط انحدارًا حقيقيًا فورًا — إصلاح Git (#152) ضاع أثناء ترقية v1.16.0
**السياق:** أول تشغيل فعلي لنظام "صحة النظام" (#157-#159) على السيرفر بعد التركيب كشف فورًا مشكلة حقيقية: **إصلاح ملكية Git الدائم (#152) لم يكن موجودًا خالص في الحاوية الفعلية على السيرفر**، رغم بنائه واختباره بدقة وقتها.

**التشخيص:** فحص مباشر (`docker exec ... git config --global --get-all safe.directory`) أكّد الغياب الكامل. تتبّع تاريخ Docker Image (`docker history`) أكّد بدقة قاطعة عدم وجود أي أمر `git config` وقت بناء الصورة الحالية خالص. مراجعة ملف `Dockerfile` **على السيرفر مباشرة** أكّدت غيابه الكامل من الكود المصدري نفسه — لم يكن مجرد فشل بناء، بل السطر نفسه غير موجود.

**السبب الجذري:** أثناء ترقية OpenHands لـv1.16.0 (#156)، تم استبدال شجرة `openhands-agent-canvas` بالكامل بنسخة نظيفة من المستودع الرسمي، ثم أُعيد تطبيق تعديلنا المخصص الوحيد المعروف وقتها (`agent-profiles-local-view.tsx`) بعناية فوق النسخة الجديدة. **لكن إصلاح `Dockerfile` من #152 — وهو تعديل مخصص منفصل تمامًا في ملف مختلف — سقط تمامًا من الحسبان أثناء عملية الاستبدال والدمج**، ولم يُلاحَظ غيابه لأن لا شيء وقتها كان يتحقق تلقائيًا من استمرار وجوده.

**التحقق من عدم فقدان أي شيء آخر:** مراجعة كاملة لتاريخ Git لآخر كوميت مخصص قبل الترقية أكّدت أن `Dockerfile` كان **الملف الوحيد** المتأثر بهذا الخطأ — لا توجد تعديلات مخصصة أخرى مفقودة.

**الإصلاح:** إعادة إضافة السطر المفقود (`RUN git config --global --add safe.directory '*'`) في نفس الموضع الصحيح بالضبط (بعد `USER openhands`، قبل `VOLUME`) داخل `Dockerfile`.

**لماذا هذا اكتشاف مهم بحد ذاته:** هذا بالضبط النوع من "الانحدار الصامت" الذي كان نظام المراقبة (#157) مصمَّمًا لاكتشافه — إصلاح كان يعمل، ثم توقف بصمت أثناء تغيير لاحق غير مرتبط ظاهريًا، دون أي طريقة سابقة لملاحظة ذلك سوى انتظار عودة العَرَض الأصلي للظهور فجأة يومًا ما. النظام أثبت قيمته الفعلية من أول استخدام حقيقي له.

**الحالة:** ✅ تم تحديد السبب الجذري بدقة (تتبّع Docker History + مراجعة الكود المصدري الفعلي على السيرفر)، التحقق من عدم وجود فقدان مماثل لأي ملف آخر، والإصلاح المباشر في الموضع الصحيح.

**🚨 خطوة مطلوبة بعد النشر:** إعادة بناء صورة `agent-canvas` (`docker compose build --no-cache agent-canvas && docker compose up -d agent-canvas`) لتفعيل الإصلاح المُستعاد، ثم تشغيل `deploy/health-check.sh` يدويًا للتأكد من تحوّل هذا الفحص تحديدًا لـ✅.

---

### 161. 🎯 نظام المراقبة يلقط باگًا ثانيًا فوريًا: فحص جدولة auto-deploy كان يفحص الخاصية الخطأ
**السياق:** بعد إصلاح #160، ظهرت مشكلة إضافية أثناء الاختبار الحي على السيرفر: فحص `mkdd-auto-deploy.timer` كان يبلّغ بفشل جدولة **رغم تأكيد مباشر من `systemctl list-timers` إن الـTimer مجدول بشكل صحيح فعليًا** (`NEXT`/`LEFT` موجودان بقيم حقيقية).

**التشخيص:** فحص مباشر للقيمة الخام اللي السكريبت بيقرأها (`systemctl show ... -p NextElapseUSecRealtime --value`) كشف السبب بدقة قاطعة: القيمة **فاضية تمامًا**. السبب الجذري: خاصية `NextElapseUSecRealtime` خاصة **حصرًا** بالـTimers التي تستخدم `OnCalendar=` (توقيت تقويمي مطلق). لكن `mkdd-auto-deploy.timer` يستخدم `OnBootSec=`/`OnUnitActiveSec=` (توقيت نسبي)، وخاصيته الصحيحة هي `NextElapseUSecMonotonic` — تم التأكد مباشرة أنها ترجع قيمة حقيقية وصحيحة (`18h 27min 17s`).

**الإصلاح:** الفحص أصبح يتحقق من **كلا** الخاصيتين (`NextElapseUSecRealtime` و`NextElapseUSecMonotonic`)، ويعتبر الفحص ناجحًا لو أي واحدة منهما ترجع قيمة حقيقية — يغطي هذا كلا نوعي الـTimers الممكنَين بشكل صحيح.

**تم التحقق حيًا بدقة قاطعة:** اختُبِر المنطق الجديد ضد 4 سيناريوهات، شاملة السيناريو الحقيقي المُكتشَف على السيرفر بالضبط (`realtime` فارغة، `monotonic` صحيحة) → نجاح صحيح؛ وسيناريو عطل حقيقي (كلاهما فارغ) → فشل صحيح كما هو متوقع.

**لماذا هذا اكتشاف ثانٍ مهم في نفس الجلسة:** هذا "إيجابي كاذب" (False Positive) حقيقي في أداة المراقبة نفسها — كان سيُبلِّغ عن مشكلة غير موجودة فعليًا في كل تشغيل دوري، مما يُضعف الثقة في النظام بمرور الوقت (خطر "إنذار كاذب متكرر" الكلاسيكي). اكتشافه وإصلاحه فورًا من أول استخدام حي حقيقي يؤكد قيمة الاختبار الفعلي على بيئة حقيقية، لا الاكتفاء بالاختبار في بيئة محاكاة فقط.

**الحالة:** ✅ باگ حقيقي ثانٍ (بعد #160) اكتُشف وأُصلح في نفس جلسة النشر الأولى الحية. التحقق: اختبار منطقي مباشر لجميع السيناريوهات الأربعة الممكنة، `bash -n` نظيف.

---

### 162. 🆕 فحص عام لصحة اشتراكات LLM — استهداف انقطاعات المحادثات بسبب انتهاء الاشتراك/الحدود
**السياق:** المالك طلب إضافة فحص لأخطاء LLM المحتملة — تحديدًا سيناريو "المحادثة توقفت لأن الاشتراك خلص أو الحدود انتهت" (المرتبط مباشرة بأخطاء `litellm.BadRequestError` التي واجهناها مع باجوش وكيرلس سابقًا في هذه الجلسة). طُلِب صراحةً ألا يعتمد الحل على تثبيت اسم مزوّد أو موديل معيّن في الكود، بما أن الموديلات قابلة للتغيير مستقبلًا.

**التصميم (عام تمامًا، بدون أي تثبيت):** بحث مباشر في الـAPI الفعلي كشف مصدر بيانات مثالي:
1. `GET /api/profiles` — قائمة كل الملفات الشخصية (LLM Profiles) المُسجَّلة فعليًا، بغض النظر عن أسمائها.
2. `GET /api/profiles/{name}` — لكل ملف، يكشف `config.auth_type` و`config.subscription_vendor` **ديناميكيًا**.
3. فقط للملفات ذات `auth_type === "subscription"`، يُستدعى `GET /api/llm/subscription/{vendor}/status` — نفس المزوّد المُستخرَج ديناميكيًا، لا اسم مُثبَّت.
4. يُتحقَّق من `connected === true` وأن `expires_at` لم يمضِ بعد، مع تحذير مبكر إذا تبقّى أقل من 24 ساعة.

**التنفيذ:** Endpoint داخلي جديد `GET /api/internal/llm-health` (بنفس آلية المفتاح الآمن من #157)، مع مهلة داخلية 10 ثوانٍ مضمونة (نفس نمط `health-deep`). ربطته بـ`deploy/health-check.sh` كفحص سابع.

**خطأ حقيقي حرج اكتُشف وأُصلح أثناء الاختبار الحي:** الفحص التكاملي الكامل أظهر نتيجة خاطئة تمامًا ("لا توجد ملفات اشتراك للفحص") رغم وجود ملف شخصي منتهي الصلاحية فعليًا. التشخيص المعزول (استخراج الدالة وتشغيلها بـ`bash -x` مباشرة، بدون إخفاء أي أخطاء) كشف السبب بدقة قاطعة: كود Python المُضمَّن استخدم **علامات اقتباس مزدوجة مُهرَّبة (`\"`) داخل f-string بنفس نوع علامات الاقتباس الخارجية** (`f"{p[\"name\"]}"`) — وهذا **خطأ Syntax حقيقي** في إصدارات Python قبل 3.12، وكان يُكتَم بصمت تمامًا بسبب `2>/dev/null` في نفس السطر. الإصلاح: استبدال f-strings المتداخلة باستخراج القيم لمتغيرات منفصلة ثم دمجها بعامل الجمع البسيط (`+`)، متجنبًا مشكلة تداخل علامات الاقتباس نهائيًا.

**درس مُعاد تطبيقه من #159:** استُخدِم معرِّف ثابت (اسم الملف الشخصي وحده) منفصل تمامًا عن الرسالة الوصفية المتغيّرة (تفاصيل السبب/الساعات المتبقية) عند تسجيل نتيجة كل ملف — تفاديًا مباشرًا لتكرار نفس باگ تتبّع التحوّلات المُكتشَف والمُصلَح في #159.

**تم التحقق حيًا بدقة قاطعة (سلسلة اختبارات شاملة):**
1. الـEndpoint بمفرده: 4 سيناريوهات (سليم، منتهي، على وشك الانتهاء خلال 24 ساعة، غير متصل) — كلها صحيحة بدقة تامة؛ وملف شخصي بمفتاح API عادي (غير قائم على اشتراك) يمر بأمان دون أي فحص، مؤكدًا المرونة العامة الكاملة.
2. بعد اكتشاف وإصلاح باگ الـSyntax: السكريبت الكامل من طرف لطرف أظهر بدقة `❌ LLM profile issue: Chatgpt (openai): expired` — النتيجة الصحيحة المتوقعة تمامًا.

**الحالة:** ✅ فحص سابع مُضاف، مبني بعناية بدون أي تثبيت لأسماء مزودين/موديلات، وخطأ Syntax حقيقي وخطير (كان سيجعل الفحص عديم الفائدة بصمت) اكتُشف وأُصلح بفضل الاختبار الحي الصارم. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 163. إصلاح مسار افتراضي خاطئ لملف مفتاح الخدمة الداخلي في health-check.sh
**السياق:** أثناء أول نشر ناجح كامل (7/7 فحوصات) على السيرفر، ظهر أن فحصَي "البحث العميق عن محادثة" و"صحة اشتراك LLM" كليهما يُتخطَّيان دائمًا لأن `INTERNAL_KEY_FILE` كانت تشير لمسار غير موجود.

**السبب:** القيمة الافتراضية استُخدِمت من تخطيط نظام الإنتاج (`/opt/openhands/projects`)، بينما المسار الفعلي الصحيح لمجلد `/projects` في `compose.yml` — سواء للإنتاج أو الـStaging — هو `${MKDD_PROJECTS_DIR:-./projects}` (نسبي لمكان `compose.yml` نفسه، أي `$DEPLOY_DIR/projects`).

**الإصلاح:** تصحيح القيمة الافتراضية لتطابق `compose.yml` تمامًا: `${MKDD_PROJECTS_DIR:-$DEPLOY_DIR/projects}`.

**الحالة:** ✅ تصحيح بسيط ومباشر، `bash -n` نظيف.

---

### 164. 🔍 تدقيق شامل لسلامة التوثيق — بطلب صريح من المالك
**السياق:** بعد سلسلة طويلة جدًا من التطوير، سأل المالك سؤالًا مباشرًا ومهمًا جدًا: هل كل التحديثات والفيتشرز الجديدة موثَّقة فعليًا، وهل سكريبت التنصيب (`install.sh`) محدَّث بحيث يجهّز أي VM جديدة بكل شيء بُني حتى الآن؟

**النتيجة الصادقة (مش تطمين بدون دليل):**
- ✅ **`install.sh` نفسه كان محدَّثًا بالفعل** — يتضمن نسخ وتفعيل `mkdd-health-check.timer`/`.service` الجديدين، وحل استباقي لمشكلة "نقطة الانطلاق" لكلا النظامين.
- 🚨 **لكن `deploy/README.md` كان قديمًا جدًا ومليئًا بمعلومات خاطئة/متناقضة** — لم يُذكَر فيه نظام المراقبة الاستباقي بالكامل (7 فحوصات، شاشة السايدبار، السجل التاريخي) خالص، وكان يشير لصورة `agent-canvas` قديمة (`1.13.0` بدل `1.16.0` الفعلية).
- 🚨 **`install.sh` نفسه كان فيه تناقض حقيقي مع `mkdd-auto-deploy.timer` الفعلي** — يقول "كل دقيقة" في تعليقاته/رسالته النهائية، بينما الإعداد الفعلي (`OnUnitActiveSec=20sec`) هو كل 20 ثانية (مطابق لما كان مكتوبًا بدقة في `deploy/README.md` بالفعل).
- 🚨 **`PROJECT_SUMMARY.md` كان بتاريخ `2026-08-16`** (14 يومًا قديمًا)، يصف حالة المشروع في `v1.13.0` بدون أي إشارة لكل التطور اللاحق الضخم.
- ⚠️ **اكتشاف أولي خاطئ ثم تصحيحه بصدق:** فحص آلي أولي لترقيم `BUGS_AND_FIXES.md` أشار خطأً لوجود فجوة في البندين #110/#111 — تبيّن لاحقًا (بعد مراجعة أدق) أن الفحص الأول كان معيبًا (لم يلتقط نمط العنوان المزدوج `### 110-111.`)، وأن المحتوى **لم يكن مفقودًا خالص**. تم التراجع فورًا عن أي توثيق خاطئ أُضيف بناءً على هذا الاستنتاج الأولي غير الدقيق، والاعتراف الصريح بالخطأ.

**الإصلاحات المُنفَّذة:**
1. `install.sh`: تصحيح "every minute"/"كل دقيقة" إلى "كل 20 ثانية" في موضعين (تعليق الرأس + الرسالة النهائية)، لمطابقة `mkdd-auto-deploy.timer` الفعلي (مصدر الحقيقة الوحيد).
2. `deploy/README.md`: إعادة كتابة كاملة — يعكس الآن نظام المراقبة الاستباقي بالكامل (شرح الفحوصات السبعة، آلية التنبيه، الشاشة، خطوة التفعيل الاختيارية)، صورة `agent-canvas` الصحيحة الحالية، وتنبيه صريح عن مخاطر فقدان تعديلات مخصصة أثناء ترقيات كبيرة مستقبلية (بالإشارة المباشرة لحادثة #160 كمثال حي).
3. `PROJECT_SUMMARY.md`: تحديث قسم "الحالة الحالية بالأرقام" لتاريخ اليوم الفعلي، مع ملخص واضح لكل التطور الكبير منذ آخر تحديث، مع الحفاظ على السجل التاريخي القديم القيّم (بدل حذفه) لكن بوسم صريح أنه "للمرجعية فقط، ليس الحالة الحالية".

**الحالة:** ✅ تدقيق شامل حقيقي (لا افتراض)، إصلاح فجوات توثيقية عملية حقيقية اكتُشفت، مع تصحيح صريح وصادق لخطأ تحليلي أوّلي ارتُكِب أثناء نفس عملية التدقيق. **ملاحظة صريحة متبقية:** `README.md` الرئيسي (2074 سطر) لم تتم مراجعته بالكامل بعد ضمن هذا التدقيق — يستحق مراجعة شاملة منفصلة نظرًا لحجمه.

---

### 165. 🔍 إعادة كتابة كاملة لـREADME.md الرئيسي — من "لقطة زمنية" لمرجع حي دقيق
**السياق:** استكمالًا للتدقيق الشامل (#164)، أوضح المالك أهمية أن يفهم أي شخص جديد "المشروع من A إلى Z" فور دخوله المستودع. الفحص كشف أن `README.md` (2074 سطر، 59 قسم) كان مبنيًا أصلًا كـ"لقطة زمنية" (Snapshot) من حالة المشروع بتاريخ `2026-08-13` — يحتوي أقسامًا مثل "العائق الحالي بالضبط للتطوير" و"تحذير اللقطة الزمنية"، وليس مصمَّمًا أصلًا ليكون مرجعًا حيًا يتحدَّث باستمرار.

**القرار (بموافقة صريحة من المالك بعد عرض المفاضلة):** إعادة كتابة كاملة من الصفر، بدل ترقيع 59 قسم قديم — يعكس الآن حالة المشروع الحقيقية دلوقتي (v1.16.0، 14 موظف، نظام المراقبة الكامل، نظام تسليم التقارير التلقائي، إلخ)، بنية أوضح وأقصر (261 سطرًا بدل 2074، مع الحفاظ على الشمولية عبر التوجيه الصريح لملفات التفاصيل).

**فلسفة التصميم الجديدة (موضَّحة للمالك صراحة قبل التنفيذ):** `README.md` = الباب الرئيسي — رؤية، معمارية، كل قدرة حالية بالتفصيل الكافي للفهم، وطريقة التشغيل — مع توجيه واضح لـ`BUGS_AND_FIXES.md`/`CHANGELOG.md`/`deploy/README.md` لأي تفصيل أعمق، بدل محاولة حشر كل شيء في ملف واحد غير قابل للقراءة.

**تحقق دقة صارم قبل النشر (لا افتراض من الذاكرة):**
1. قائمة الموظفين الـ14 (الاسم، الدور، الوصف) استُخرِجت **مباشرة من ملفات `company-agents-definitions/*.md`** عبر سكريبت استخراج آلي، لا من الذاكرة.
2. خريطة الملفات (routes/lib) تم التحقق منها عبر `diff` مباشر بين المذكور في الملف والقائمة الحقيقية على القرص — **تطابق 100% تام**، صفر فروقات.
3. تأكيد أن `internal-health.mjs` يحتوي فعليًا على كل الدوال الأربعة المذكورة (صحة النظام، الفحص العميق، تنبيهات، صحة LLM) عبر بحث مباشر في الكود.
4. البورتات ومسارات الـVolumes مُستخرَجة مباشرة من `compose.yml` الفعلي، لا من الذاكرة.

**الحالة:** ✅ إعادة كتابة كاملة، بموافقة صريحة، مبنية على تحقق مباشر صارم لكل حقيقة مذكورة (لا افتراض)، لا "تخمين معقول". المشروع الآن لديه مرجع حي دقيق يعكس حالته الحقيقية.

---

### 166. 🆕 معلومات مستودع Git الحقيقية في صفحة المشروع — اسم الريبو، الحالة، وآخر الCommits
**السياق:** المالك طلب إضافة معلومات في صفحة المشروع الرئيسية: اسم الريبو، حالته، والأحداث التي عملها الموظفون — لكن أكّد صراحة على مبدأ المشروع الحاكم (`ENGINEERING_PRINCIPLES.md #1`): أي ميزة جديدة في MKDD لازم أساس حقيقي في OpenHands، لا نظام مُختلَق موازٍ.

**رحلة التحقيق التقني (بحث مباشر، لا افتراض):**
1. **اكتشاف الأساس الحقيقي**: فحص مباشر لـ`openapi.json` الخاص بـAgent Server أكّد وجود Endpoints حقيقية `/api/git/commits`, `/api/git/changes` — تأخذ `path` (مسار محلي مباشر على القرص)، لا `conversation_id`. اختُبِرت حيًا بإنشاء Commit تجريبي فعلي، وأكّدت إرجاع بيانات دقيقة تمامًا (SHA، المؤلف، الرسالة، الوقت).
2. **طلب توسيع للحصول على حالة PRs الحية**: بحث فوري في التوثيق الرسمي لـOpenHands أكّد بشكل قاطع أن تكامل GitHub العميق (حالة Pull Requests، Webhooks) هو **ميزة محجوزة حصريًا لـOpenHands Cloud** (المنتج السحابي المدفوع)، غير موجودة في النسخة مفتوحة المصدر ذاتية الاستضافة التي يعمل عليها هذا النشر — موثَّق رسميًا: "you can still do any of this just with the open source... you can use MCP or secrets".
3. **استكشاف بديل عبر MCP**: فُحِص هل يمكن لـMKDD Backend استدعاء أداة GitHub MCP (الموجودة بالفعل للموظفين) مباشرة، بمعزل عن محادثة حية — الفحص المباشر لكل Endpoints الخاصة بـMCP (`/api/mcp/test`, `/api/mcp/oauth/*`, `/api/settings/mcp/*`) أكّد عدم وجود أي طريقة لاستدعاء أداة مباشرة خارج سياق تنفيذ محادثة فعلية — قيد معماري متعمَّد في OpenHands.
4. **القرار النهائي (بموافقة صريحة من المالك بعد عرض كل الخيارات):** الاكتفاء بما هو مدعوم فعليًا بأساس حقيقي — Commits المحلية وحالة النظافة (نظيف/فيه تغييرات معلَّقة) — وتأجيل حالة PRs الحية لحين توفر أساس حقيقي مستقبلي في النسخة مفتوحة المصدر.

**التنفيذ:**
1. **اسم الريبو**: يُقرأ **مباشرة من ملف `.git/config` المحلي** (`[remote "origin"] url = ...`) — قراءة ملف نظام تشغيل عادية، بما أن `mkdd-ui` أصلًا تُشارِك نفس الـVolume `/projects` مع `agent-canvas` — لا حاجة لأي استدعاء API إضافي لهذا الجزء تحديدًا، وهي أقرب أساس ممكن (نفس الملف الذي يعتمد عليه Git نفسه).
2. **الCommits والحالة**: عبر `/api/git/commits` و`/api/git/changes` الحقيقيتين من OpenHands.
3. **Endpoint جديد** `GET /api/projects/git-info?project=...` يجمع الثلاثة، مع تمييز صريح بين "فشل الطلب" (`null`) و"صفر فعلي" (`0`) — التزامًا بمبدأ "لا بيانات مُختلَقة".
4. **شاشة جديدة** `ProjectGitInfoCard.tsx` في صفحة المشروع الرئيسية — رابط الريبو، حالة النظافة، وقائمة آخر 10 Commits، بتحديث دوري كل دقيقة بنمط "آخر بيانات معروفة صحيحة".

**تم التحقق حيًا بدقة قاطعة:**
1. اختبار حي كامل بمحاكاة `.git/config` حقيقي — استخراج دقيق تام لاسم الريبو، والCommits، وعدد التغييرات المعلَّقة.
2. اختبار الحالة الحدّية (مشروع بدون ريبو، وفشل طلب الحالة) — تدهور آمن تام (`repoUrl: null`, `commits: []`, `uncommittedChanges: null`)، لا انهيار ولا بيانات مُختلَقة.

**الحالة:** ✅ ميزة كاملة مبنية بالكامل على أساس حقيقي موثَّق ومُختبَر حيًا في كل خطوة، مع رفض صريح ومُبرَّر لأي جزء (حالة PRs) يفتقر لهذا الأساس. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء جديدة، توازن CSS مؤكَّد.

---

### 167. 🆕 زرار "شوف الكل" لعرض تاريخ Commits كامل — يعتمد على الحد الأقصى الحقيقي في OpenHands
**السياق:** استكمالًا لميزة معلومات الريبو (#166)، طلب المالك زرار يعرض قائمة كاملة (لا محدودة بآخر 10) لكل الـCommits، مع اسم الموظف ووقت كل واحد.

**التحقق التقني:** فحص مباشر لـ`openapi.json` أكّد أن `/api/git/commits` في OpenHands لا تدعم Pagination حقيقية (لا `offset`/`cursor`) — فقط `limit` بحد أقصى **200** (القيمة الافتراضية 50). هذا هو السقف الحقيقي المتاح، لا اختيار تعسفي من MKDD.

**التنفيذ:**
1. Endpoint `/api/projects/git-info` الحالي أصبح يقبل `limit` اختياريًا عبر Query Parameter — القسم المختصر في صفحة المشروع يطلب 10 (كما هو)، وزرار "شوف الكل" الجديد يطلب 200 (السقف الحقيقي).
2. **حماية صريحة ضد قيم غير صالحة**: أي قيمة `limit` أكبر من 200 تُقيَّد (Clamp) تلقائيًا للحد الأقصى الحقيقي، بدلًا من تمريرها كما هي لـOpenHands.
3. شاشة جديدة `ProjectCommitHistoryModal.tsx` — قائمة كاملة بكل Commit (المؤلف، الرسالة، الوقت)، مع ملاحظة صريحة للمالك توضح أن 200 هو السقف الحقيقي المتاح فعليًا.

**تم التحقق حيًا بدقة قاطعة (اختبار دقيق لثلاثة سيناريوهات):**
1. بدون `limit` → القيمة الفعلية المُرسَلة لـOpenHands: `10` (الافتراضي الصحيح).
2. `limit=200` (زرار "شوف الكل") → القيمة الفعلية المُرسَلة: `200` (تمرير سليم).
3. `limit=99999` (قيمة غير صالحة/خبيثة) → القيمة الفعلية المُرسَلة: **`200`** (التقييد التلقائي يعمل بدقة تامة، لا تمرير أعمى).

**خطأ برمجي حقيقي اكتُشف وأُصلح أثناء البناء:** المكوّن الجديد كان يحاول إرجاع عنصرين جذريين منفصلين (`<section>` والنافذة المنبثقة الشرطية) بدون عنصر ملفوف واحد — خطأ React أساسي (كل مكوّن يجب أن يُرجع عنصرًا جذريًا واحدًا). اكتُشف عبر `tsc -b` مباشرة، وأُصلح بلف الاثنين في `<>...</>`.

**الحالة:** ✅ ميزة كاملة، مبنية بالكامل على السقف الحقيقي المتاح من OpenHands (لا اختراع رقم تعسفي)، مع حماية صريحة وموثَّقة للمستخدم. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء جديدة، توازن CSS مؤكَّد.

---

### 168. 🚨 health-check.sh لا يقرأ ملف .env خالص — الفحص العميق يظل معطَّلًا رغم الإعداد الصحيح
**السياق:** المالك أضاف `MKDD_HEALTH_CHECK_PROJECT`/`EMPLOYEE_ID`/`EMPLOYEE_NAME` إلى ملف `.env` الفعلي على السيرفر بدقة (بالضبط كما هو موثَّق في `deploy/README.md`)، وأعاد تشغيل الخدمة — لكن الفحص العميق استمر في الظهور بحالة "غير مضبوط" (Skip).

**السبب الجذري:** فحص مباشر لكود `health-check.sh` أكّد أنه **لا يقرأ ملف `.env` خالص** — لا هو نفسه، ولا تعريف `mkdd-health-check.service`. الملف الأخير كان يحتوي فقط على أمثلة `Environment=...` **معلَّقة بـ`#`** (توضيحية فقط)، وليست تفعيلًا حقيقيًا. توثيق `deploy/README.md` افترض ضمنيًا أن إضافة القيم لـ`.env` كافية وحدها — وهذا الافتراض كان خاطئًا لأن لا شيء فعليًا يربط الملف بالسكريبت.

**الإصلاح:** إضافة تحميل صريح لملف `.env` مباشرة بعد تحديد `DEPLOY_DIR` — باستخدام `set -a; source "$DEPLOY_DIR/.env"; set +a`، بنفس آلية التصدير التلقائي (`export`) لكل سطر `KEY=VALUE`، مطابقة تمامًا لكيفية قراءة `docker compose` لنفس الملف — أي قيمة مُصدَّرة يدويًا من قِبل المستدعي تبقى لها الأولوية (لا يُعاد الكتابة فوقها).

**تم التحقق حيًا بدقة قاطعة (محاكاة السيناريو الحقيقي بالضبط):** اختبار معزول بملف `.env` حقيقي يحتوي على القيم الثلاث، **بدون أي تصدير يدوي إضافي** (بالضبط كما فعل المالك فعليًا) — تأكيد مباشر أن القيم الثلاث اتحمَّلت بدقة تامة وأصبحت متاحة للسكريبت.

**الحالة:** ✅ باگ حقيقي حرج (كان يجعل ميزة كاملة موثَّقة وموصوفة كخطوة تفعيل بسيطة عديمة الأثر تمامًا) اكتُشف وأُصلح بفضل التحقق الحي المباشر عند أول استخدام فعلي من المالك. `bash -n` نظيف.

---

### 169. 🎯🎯 اكتشاف فجوة حقيقية عبر حادثة حية: فحص LLM لا يكتشف "حد الاستخدام" — إصلاح باستخدام تصنيف OpenHands الرسمي
**السياق:** المالك واجه حيًا خطأ حقيقي داخل واجهة OpenHands نفسها (`litellm.RateLimitError: usage_limit_reached`)، بينما شاشة "صحة النظام" في نفس اللحظة كانت تعرض **كل الفحوصات ✅**، شاملة "LLM profile healthy". هذا تناقض حقيقي خطير يستحق تحقيقًا فوريًا.

**التشخيص:** فحص مباشر لـ`/api/llm/subscription/{vendor}/status` (الفحص #7 الحالي) أكّد السبب الجذري بدقة: هذا الـEndpoint يرجّع **فقط** `{connected, expires_at}` — حالة **الاتصال والصلاحية الزمنية للاشتراك نفسه**، وليس **حالة حصة الاستخدام الحالية**. هذان مفهومان مختلفان تمامًا في OpenHands: اشتراك "متصل وصالح" لا يعني بالضرورة أن لديه حصة استخدام متبقية الآن — وهذا الـEndpoint لا يكشف عن الحالة الثانية خالص، مهما تحسَّن كود MKDD.

**الحل المكتشف حيًا:** فحص مباشر لآخر حدث في المحادثة الفعلية التي واجهت الخطأ كشف بنية بيانات مثالية — `ConversationErrorEvent` يحمل **تصنيفًا رسميًا منظَّمًا من OpenHands نفسها**: `{code: "LLMRateLimitError", classification: {kind: "rate_limit", retryable: true, user_action: "retry"}}` — بيانات حقيقية مُصنَّفة بواسطة المنصة، لا نص حر يحتاج تحليلًا يدويًا هشًا.

**التنفيذ:** توسيع الفحص العميق الموجود بالفعل (`/api/internal/health-deep`، #157) — بعد إيجاد محادثة الموظف المستهدَف، يُفحَص **آخر حدث** فيها؛ إذا كان `ConversationErrorEvent` حقيقيًا، يُعتبَر الفحص فاشلًا مع تفاصيل واضحة (الكود والتصنيف)، بدلًا من الاكتفاء بمجرد "المحادثة موجودة" كما كان سابقًا. رسالة `health-check.sh` أصبحت تميّز صراحة بين "تعليق/فشل اتصال حقيقي" و"خطأ حقيقي مُكتشَف داخل محادثة" (زي `Recent conversation error detected: LLMRateLimitError (rate_limit)`)، بدلًا من عرض استجابة JSON خام غير واضحة.

**تم التحقق حيًا بدقة قاطعة (إعادة إنتاج الحادثة الحقيقية بالضبط):**
1. محاكاة السيناريو الحقيقي 100% (نفس بنية `ConversationErrorEvent` بالضبط من الحادثة الفعلية) → `ok: false`، تفاصيل واضحة (`LLMRateLimitError`, `rate_limit`).
2. الحالة السليمة (آخر حدث `MessageEvent` عادي) → `ok: true`، لا إيجابية كاذبة.
3. السكريبت الكامل من طرف لطرف بنفس السيناريو الحقيقي → رسالة واضحة تمامًا: `❌ Recent conversation error detected: LLMRateLimitError (rate_limit)`.

**الحالة:** ✅ فجوة حقيقية اكتُشفت مباشرة من حادثة حية واجهها المالك (لا افتراض نظري)، وأُصلحت بأساس بيانات حقيقي مُصنَّف رسميًا من OpenHands نفسها. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة.

**ملاحظة أمانة متبقية:** هذا الفحص يعتمد على وجود محادثة نشطة حديثًا للموظف المُهيَّأ في `MKDD_HEALTH_CHECK_*` تحديدًا — لا يكتشف أخطاء حد الاستخدام في محادثات موظفين آخرين لم يُفحَصوا.

---

### 170. 🆕 شرح واضح ومفهوم لأخطاء المحادثات — وإصلاح باگ حقيقي في استخراج JSON متداخل
**السياق:** بعد إصلاح #169، طلب المالك تفصيلًا أوضح وأكثر قابلية للفهم لأخطاء المحادثة، مثل الطريقة التي شُرِح بها الخطأ الأصلي له مباشرة (زي "الاشتراك لسه شغال، بس وصل لحد الاستخدام، وهيرجع بعد كذا")، بدلًا من مجرد كود تقني مختصر (`LLMRateLimitError (rate_limit)`).

**الاكتشاف:** الحدث الأصلي (`ConversationErrorEvent`) يحتوي بالفعل على حقل `detail` بنص خام يتضمن كائن JSON مُضمَّن من مزوّد الخدمة نفسه (`resets_in_seconds`, `plan_type`) — بيانات حقيقية غنية لم تكن مُستخرَجة أو مُستخدَمة سابقًا.

**خطأ حقيقي اكتُشف وأُصلح أثناء الاختبار الحي:** المحاولة الأولى استخدمت تعبيرًا نمطيًا بسيطًا (`/\{[^{}]*\}$/`) لاستخراج كائن الـJSON، لكنه فشل بصمت لأن الهيكل الفعلي يحتوي على **تداخل حقيقي** (`{"error": {...}}`) — تعبير نمطي بسيط لا يتعامل بشكل موثوق مع الأقواس المتداخلة. الاختبار الحي المباشر (باستخدام نفس نص الخطأ الحقيقي بالضبط من الحادثة الفعلية) كشف أن الرسالة الناتجة كانت الرسالة الاحتياطية العامة فقط، بدون أي تفاصيل زمنية — رغم توفر البيانات فعليًا. الإصلاح: البحث عن أول `{` في النص، ثم الاعتماد على `JSON.parse` نفسه للتحقق من صحة الهيكل الكامل بدءًا من هناك — يتعامل بشكل صحيح مع أي مستوى من التداخل.

**التنفيذ:** دالة `buildHumanReadableErrorMessage` جديدة تبني شرحًا عربيًا واضحًا: حالة الاشتراك، نوع الخطة (إن وُجدت)، ومدة الانتظار المتبقية محوَّلة لصيغة مفهومة (ساعات ودقائق، لا ثوانٍ خام). الرسالة الجديدة تحل محل الملخص التقني المختصر في كل من سجل `health-check.sh` وملف الحالة الذي تعرضه شاشة "صحة النظام" مباشرة.

**تم التحقق حيًا بدقة قاطعة (بنفس بيانات الحادثة الحقيقية بالضبط):**
1. قبل الإصلاح: الرسالة الناتجة كانت عامة بدون تفاصيل — الباگ مؤكَّد.
2. بعد الإصلاح: **"الاشتراك لسه شغال ومتصل (خطة plus)، لكن وصل لحد الاستخدام المؤقت. هيرجع يشتغل تلقائيًا خلال حوالي 2 ساعة و36 دقيقة تقريبًا - مفيش حاجة تتصلح، بس محتاج تستنى."** — دقة تامة، محسوبة صحيحًا من `resets_in_seconds: 9366` الحقيقية.
3. السكريبت الكامل من طرف لطرف، وملف الحالة الذي تقرأه شاشة "صحة النظام" — كلاهما يعرضان الرسالة المفصَّلة بشكل صحيح.

**الحالة:** ✅ ميزة كاملة، مبنية على بيانات حقيقية 100% (لا تخمين)، مع اكتشاف وإصلاح باگ حقيقي (لا افتراضي) في معالجة JSON متداخل بفضل الاختبار الحي الصارم. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 171. 🆕 ترجمة كاملة لكل رسائل صحة النظام — عربي واضح ومفهوم بدلًا من مصطلحات تقنية إنجليزية
**السياق:** بعد الاستحسان الواضح للرسالة المفصَّلة اللي بُنيت في #170، طلب المالك صراحة أن تكون **كل** رسائل شاشة "صحة النظام" (السبعة كاملة) بنفس درجة الوضوح — عربي مباشر ومفهوم، لا مصطلحات تقنية إنجليزية.

**التنفيذ:** ترجمة منهجية لكل رسالة نجاح/فشل/تخطٍّ في `deploy/health-check.sh` عبر الفحوصات السبعة كاملة:
- **الحاويات**: "الحاوية X مش شغالة (الحالة: ...) - محتاجة إعادة تشغيل".
- **واجهة MKDD**: "واجهة MKDD الأساسية مش بترد - محتاج تتأكد إن حاوية mkdd-ui شغالة".
- **محرك OpenHands**: "محرك OpenHands مش بيرد - محتاج تتأكد إن حاوية agent-canvas شغالة".
- **ملكية Git**: "في مشكلة صلاحيات في ملفات Git - أي محادثة جديدة هتفشل حاليًا".
- **جدولة النشر التلقائي**: "نظام النشر التلقائي مش مجدول - نفّذي: sudo systemctl start ...".
- **الفحص العميق**: رسائل Skip/Pass واضحة، مع الحفاظ على الرسالة المفصَّلة من #170 عند اكتشاف خطأ حقيقي.
- **اشتراكات LLM**: قاموس ترجمة كامل لكل سبب فشل ممكن (`expired` → "الاشتراك منتهي"، `expiring_soon` → "الاشتراك هينتهي قريب"، `not_connected` → "الاشتراك مش متصل"، `check_failed` → "حصل خطأ أثناء الفحص")، مع تضمين المدة المتبقية بالعربي عند توفرها.

**تم التحقق حيًا بدقة قاطعة:** اختبار شامل من طرف لطرف لكل الفحوصات السبعة معًا بحالات مختلطة (نجاح، فشل، تخطٍّ) — كل رسالة ظهرت بعربي واضح تمامًا، مع تأكيد أن ملف الحالة JSON (المصدر المباشر لشاشة "صحة النظام") لا يزال صالحًا 100% رغم النص العربي الكامل (نفس آلية `ensure_ascii=False` الموثَّقة سابقًا في #158).

**الحالة:** ✅ كل رسائل نظام المراقبة (السبعة) أصبحت عربية وواضحة بالكامل، بدون أي استثناء. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 172. 🆕 عداد تنازلي حي للوقت المتبقي — بدل نص ثابت يبقى قديم بمجرد ظهوره
**السياق:** بعد رؤية الرسالة المفصَّلة من #170، سأل المالك عن سبب عدم وجود عداد تنازلي حقيقي يعرض بدقة الوقت المتبقي، بدل نص ثابت ("حوالي 2 ساعة و36 دقيقة") محسوب مرة واحدة وقت تشغيل الفحص (كل 5 دقايق)، ويصبح قديمًا فورًا.

**التصميم:** بدل الاكتفاء بنص جاهز، النظام الآن يوصّل **الوقت الفعلي المطلق** (`resets_at` الحقيقي من مزوّد الخدمة، محوَّل لطابع زمني بالميلي ثانية) للواجهة، بحيث الواجهة نفسها تحسب الوقت المتبقي لحظة بلحظة، بدل الاعتماد على نص محسوب مسبقًا.

**التنفيذ (سلسلة كاملة من الخادم للواجهة):**
1. **الخادم**: `buildHumanReadableErrorMessage` أصبحت ترجع `{text, resetsAt}` معًا (بدل نص فقط) — `resetsAt` طابع زمني حقيقي بالميلي ثانية.
2. **`health-check.sh`**: آلية `record`/`log_pass`/`log_fail` وُسِّعت لدعم عمود رابع اختياري (بيانات JSON إضافية منظَّمة)، دون كسر أي فحص لا يحتاجها.
3. **ملف الحالة (`health-status.json`)**: كل فحص يمكن أن يحمل حقل `meta` اختياري (زي `{"resetsAt": 1788117072000}`) بجانب الرسالة النصية العادية.
4. **الواجهة**: مكوّن جديد `LiveCountdown.tsx` — عداد تنازلي حقيقي يتحدَّث كل ثانية بمؤقت داخلي مستقل (مش مرتبط بدورة التحديث كل 30 ثانية للشاشة كلها)، يعرض الوقت المتبقي بصيغة واضحة (ساعات:دقائق:ثواني)، ويتحوَّل تلقائيًا لرسالة "المفروض يكون رجع يشتغل دلوقتي" عند الوصول للصفر (بدل عرض رقم سالب).

**تم التحقق حيًا بدقة قاطعة:**
1. تأكيد مباشر أن `resetsAt` المُحسَبة تطابق تمامًا القيمة الأصلية من مزوّد الخدمة (`1788117072 × 1000 = 1788117072000`).
2. اختبار السكريبت الكامل من طرف لطرف يؤكد وصول `meta.resetsAt` بدقة لملف الحالة النهائي.
3. تأكيد صريح أن الفحوصات الأخرى التي لا تحمل بيانات `meta` لا تتأثر إطلاقًا (لا كسر، لا حقل زائف).

**الحالة:** ✅ ميزة كاملة من طرف لطرف، مبنية على بيانات حقيقية 100% (طابع زمني فعلي من مزوّد الخدمة نفسه، لا حساب تقريبي). التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة، توازن CSS مؤكَّد.

---

### 173. 🎯🎯 محادثات MKDD بعنوان غريب في واجهة OpenHands — API تغيّر ومعادش بيدعم `title` وقت الإنشاء
**السياق:** المالك لاحظ أن المحادثات اللي بتُنشَأ من MKDD بتظهر بعنوان غريب في واجهة OpenHands (Agent Canvas) نفسها، وسأل كذلك ليه مش منظَّمة تحت مجلد المشروع فيها.

**التشخيص المباشر (فحص بيانات حقيقية، لا افتراض):** فحص محادثات حقيقية موجودة فعليًا (`GET /api/conversations/search`) أكّد أن `title: null` لكل المحادثات، رغم أن كود `chat.mjs` كان يرسل `title: "..."` صراحةً في طلب الإنشاء. مراجعة مباشرة لـSchema الرسمية الحالية (`StartConversationRequest` في `openapi.json` الفعلي لـv1.16.0) كشفت السبب الجذري بدقة: **لا يوجد حقل `title` في هذه الـSchema خالص** — فقط `autotitle` (توليد تلقائي من أول رسالة) و`title_llm_profile` (موديل مخصص للتوليد التلقائي). حقل `title` الذي كان يُرسَل كان يُسقَط بصمت بواسطة الخادم، دون أي خطأ ظاهر — تعليق الكود القديم الذي وصفه بأنه "حقل مؤكَّد حقيقي" كان صحيحًا في وقت كتابته، لكن أصبح غير دقيق بعد ترقية OpenHands إلى v1.16.0 دون مراجعة.

**الحل المكتشف حيًا:** فحص Schema أخرى (`UpdateConversationRequest`) أكّد وجود حقل `title` حقيقي فيها، متاح عبر Endpoint منفصل تمامًا: `PATCH /api/conversations/{conversation_id}` — تحديث العنوان بعد الإنشاء، لا أثناءه.

**الإصلاح:** إزالة حقل `title` الميت من طلب الإنشاء، وإضافة استدعاء `PATCH` منفصل فورًا بعد نجاح الإنشاء لضبط العنوان الصحيح (`اسم الموظف — اسم المشروع`)، بأسلوب "أفضل جهد ممكن" (Best-effort) — فشل تحديث العنوان لا يُفشِل إنشاء المحادثة نفسها أبدًا، بما أن المحادثة تبقى حقيقية وقابلة للاستخدام على أي حال.

**تم التحقق حيًا بدقة قاطعة:** اختبار كامل من طرف لطرف لإرسال رسالة جديدة، بتتبع كل طلب فعلي وصل للخادم الوهمي — أكَّد بدقة أن استدعاء `PATCH /api/conversations/{id}` يصل فعليًا بالعنوان الصحيح (`{"title":"Kirollos — Test 1"}`) مباشرة بعد نجاح الإنشاء.

**ملاحظة صادقة غير مؤكَّدة 100%:** جزء "عدم التنظيم تحت مجلد المشروع" على الأرجح ناتج عن نفس مشكلة `title: null` (واجهة OpenHands قد تعرض المحادثات بلا عنوان بشكل غير منظَّم بصريًا)، بما أن بيانات `workspace.working_dir` نفسها مؤكَّدة أنها مرتبطة بشكل صحيح تمامًا بالـWorkspace المسجَّل (تطابق تام بين `working_dir` و`id` الحقيقي). هذا الجزء يحتاج تأكيدًا بصريًا من المالك بعد النشر لتأكيد الحل الكامل.

**الحالة:** ✅ سبب جذري حقيقي اكتُشف عبر فحص بيانات حية فعلية (لا افتراض)، وإصلاح مؤكَّد حيًا بدقة تامة. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 174. 🎯 عناوين متطابقة عند استخدام زرار "محادثة جديدة" لنفس الموظف — إصلاح بمعرّف حقيقي فريد
**السياق:** أثناء نقاش تنظيم المحادثات (#173)، سأل المالك سيناريو مهم: إذا أُنشئت أكتر من محادثة لنفس الموظف في نفس المشروع، إزاي هيكون اسمها وترتيبها؟ الافتراض الأولي (نادر/استثنائي) كان خاطئًا — **صحَّحه المالك مباشرة**: زرار "محادثة جديدة" الصريح (موثَّق مسبقًا في #61) بيُنشئ عمدًا محادثات متعددة حقيقية لنفس الزوج (مشروع، موظف)، وهذا سلوك متعمَّد وشائع، لا استثناء نادر.

**التشخيص:** فحص كود العنوان الحالي أكّد أن كل محادثة جديدة تحصل على **نفس العنوان الثابت بالضبط** (`اسم الموظف — اسم المشروع`)، بدون أي تمييز — يعني استخدام الزرار عدة مرات ينتج محادثات متطابقة الاسم تمامًا في واجهة OpenHands، بلا طريقة للتفرقة بينها سوى المعرِّف العشوائي غير المقروء.

**محاولتان اكتُشف فشلهما حيًا قبل الوصول للحل الصحيح:**
1. **إضافة الوقت بدقة الدقيقة** — اختبار حي مباشر (طلبان بفارق ثانيتين فقط) أثبت تصادمًا فعليًا: نفس العنوان بالضبط، بما أن الفارق كان أقل من دقيقة واحدة — سيناريو واقعي جدًا لاستخدام الزرار بسرعة.
2. **إضافة الثواني** — لا يزال نظريًا عرضة للتصادم لو وصل طلبان في نفس الثانية بالضبط.

**الحل النهائي المؤكَّد:** الاعتماد على **آخر 4 أحرف من معرِّف المحادثة الحقيقي (UUID فريد فعليًا)** بجانب التاريخ/الوقت المقروء — ضمان تمييز مطلق رياضيًا، لا احتمالي، بما أن كل محادثة تحصل على UUID فريد تمامًا من OpenHands نفسها بغض النظر عن التوقيت.

**تم التحقق حيًا بدقة قاطعة (أسوأ سيناريو ممكن):** اختبار طلبين متتاليين مباشرة (بدون أي تأخير بينهما، محاكاة نفس الثانية بالضبط) — تأكيد صريح ومباشر أن العنوانين الناتجين مختلفان تمامًا (`... · 8e70` مقابل `... · 6264`).

**الحالة:** ✅ باگان حقيقيان (لا افتراضيان) اكتُشفا وأُصلحا أثناء البناء بفضل الاختبار الحي الصارم لكل محاولة، وليس الاكتفاء بأول حل يبدو معقولًا نظريًا. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 175. 🆕 استئناف تلقائي للمحادثات المتوقفة بسبب حد الاستخدام
**السياق:** المالك واجه سيناريو حقيقي — كيرلس توقف عن العمل بسبب انتهاء حد الاستخدام، وفاضلة 4 ساعات لحد ما يرجع يشتغل. طلب نظامًا يخلي الموظف يرجع يشتغل تلقائيًا ويكمل من حيث ما وقف، بدون تدخل يدوي.

**متطلبات صريحة من المالك:**
- مراقبة **كل الموظفين**، لا موظف واحد محدَّد.
- **بدون حد زمني** على "آخر نشاط" — بما أن حد الاستخدام ممكن يستغرق 3 أو 4 أيام لإعادة التعيين، فمحادثة متوقفة من أيام لازم تُكتشَف بردو.
- **تنبيه فوري (Push Notification)** عند أي استئناف تلقائي، يوضح الموظف والمشروع.

**التصميم (مبني على أساس حقيقي موجود بالفعل):**
- استفاد بالكامل من الأساس الحقيقي المُكتشَف والمُختبَر في #169/#170/#172 — `ConversationErrorEvent` بتصنيف `rate_limit` وحقل `resetsAt` الحقيقي المُستخرَج من مزوّد الخدمة.
- **رسالة الاستئناف** مصمَّمة عمدًا لتعتمد على "معيار استمرارية العمل" الموثَّق مسبقًا في `AGENTS.md` (كل موظف مُبرمَج بالفعل على فحص `project-context.md`/`decisions.md` وأحدث نقطة تحقق موثَّقة عند استئناف عمل متوقف) — بدل محاولة إعادة بناء منطق استئناف مخصص من الصفر.
- **تصميم "ذاتي التنظيف" بدون تتبّع حالة إضافية**: بمجرد إرسال رسالة الاستئناف، آخر حدث في المحادثة يصبح الرسالة الجديدة (لا الخطأ القديم)، فالفحص الدوري التالي لا يكتشفها كمرشحة للاستئناف تلقائيًا — لا حاجة لآلية "تم الاستئناف بالفعل" منفصلة ومعقَّدة.

**التنفيذ:**
1. `sendMessageToConversationId` — دالة جديدة مُستخرَجة من منطق موجود، ترسل مباشرة لمعرِّف محادثة معروف (بدون إعادة بحث بالمشروع/الموظف، لتجنب إرسال الرسالة لمحادثة أحدث خاطئة لو استُخدِم زرار "محادثة جديدة" منذ التوقف).
2. `GET /api/internal/resume-stopped-conversations` — يفحص **كل** المحادثات (بدون حد زمني)، يحدد المؤهَّلة للاستئناف (آخر حدث = خطأ حد استخدام حقيقي، ووقت إعادة التعيين فات فعلاً)، يرسل رسالة الاستئناف، ويرسل تنبيهًا فوريًا لكل استئناف ناجح.
3. مربوط بدورة `mkdd-health-check.timer` الموجودة بالفعل (كل 5 دقائق) — لا حاجة لنظام جدولة منفصل جديد.

**تم التحقق حيًا بدقة قاطعة (3 سيناريوهات في اختبار واحد):**
1. محادثة توقفت **ووقت إعادة التعيين فات فعلاً** → ✅ استُؤنِفت بنجاح (رسالة استئناف حقيقية أُرسِلت).
2. محادثة توقفت **لكن وقت إعادة التعيين لسه لم يأتِ** → ✅ تخطٍّ آمن، لا استئناف مبكر خاطئ.
3. محادثة سليمة **بلا أي خطأ خالص** → ✅ تخطٍّ آمن تام.
4. اختبار السكريبت الكامل من طرف لطرف أكَّد ظهور رسالة ملخَّص واضحة: `🔄 استئناف تلقائي: 1 محادثة رجعت تشتغل بعد انتهاء حد الاستخدام`.

**ملاحظة قياس معروفة (موثَّقة صراحة في الكود):** الآلية الحالية تفحص آخر حدث لكل محادثة على حدة بشكل متسلسل — مناسبة تمامًا لحجم المشروع الحالي، لكن قد تحتاج تحسينًا (معالجة دفعية أو فلترة أرخص من جانب الخادم) لو كبر عدد المحادثات بشكل كبير مستقبلًا.

**الحالة:** ✅ ميزة كاملة من طرف لطرف، مبنية بالكامل على أساس حقيقي (تصنيف رسمي من OpenHands + معيار استمرارية عمل موثَّق مسبقًا للموظفين)، مُختبَرة حيًا بدقة صارمة في كل سيناريو ممكن. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 176. 🆕 رسالة الاستئناف التلقائي مخفية عن المحادثة — تظهر في تاب مخصص بدل ذلك
**السياق:** بعد بناء نظام الاستئناف التلقائي (#175)، سأل المالك إمكانية إخفاء رسالة الاستئناف من المحادثة العادية، وعرضها بدلًا من ذلك في تاب مخصص داخل بيانات الموظف.

**التصميم:** استُخدِمت نفس آلية "العلامة المخفية" الموجودة بالفعل والموثَّقة (`time-context.mjs`) — لكن بدل تنظيف جزء من رسالة حقيقية، الرسالة بأكملها هنا مُولَّدة بالكامل من النظام، فتُستبعَد بالكامل (لا تُنظَّف جزئيًا) من كل مسارات عرض الأحداث.

**خطأ حقيقي اكتُشف وأُصلح قبل النشر (لا افتراضي):** الفحص الأول للعلامة توقّع وجودها في بداية النص مباشرة، لكن اختبار مباشر كشف أن `buildOutgoingContent` (المستخدَمة لإرسال أي رسالة صادرة) تُضيف تلقائيًا علامة الوقت الحية (`mkdd:time`) **قبل** أي محتوى — فالنص الفعلي يصبح `<!--mkdd:time:...-->\n<!--mkdd:auto-resume-->\n...`، لا العلامة المستهدفة في البداية الحرفية. الإصلاح: فحص العلامة **بعد** إزالة علامة الوقت أولًا، بغض النظر عن أيهما أُضيفت أولًا فعليًا.

**التنفيذ:**
1. `auto-resume-marker.mjs` — علامة مخفية جديدة (`<!--mkdd:auto-resume-->`)، بنفس نمط `time-context.mjs`.
2. `normalize-event.mjs` — أي `MessageEvent` من `source: "user"` يحمل هذه العلامة (بعد إزالة علامة الوقت) يُرجَّع `null` بالكامل — نقطة تعديل مركزية واحدة تغطي REST (×3 مسارات) والـWebSocket معًا تلقائيًا (بما أن كل نقاط الاستدعاء بالفعل تستخدم `.filter(Boolean)`).
3. `auto-resume-log.mjs` — تخزين JSON بسيط (بنفس نمط `project-metadata.mjs`) لسجل الاستئناف لكل (مشروع، موظف)، محدود بآخر 50 حدث.
4. `GET /api/employee-auto-resume-log` — Endpoint جديد يقرأ السجل.
5. تاب جديد "الاستئناف التلقائي" في `EmployeeInsightsPanel.tsx` — يعرض تاريخ ووقت كل حدث استئناف لهذا الموظف تحديدًا.

**تم التحقق حيًا بدقة قاطعة (3 اختبارات متسلسلة):**
1. اختبار مباشر للعلامة بترتيب الإضافة الحقيقي (`time` ثم `auto-resume`) — تأكيد أن الفحص (بعد الإصلاح) يكتشفها بدقة.
2. اختبار كامل لتشغيل النظام: تأكيد وصول الرسالة الحقيقية بالعلامتين لـOpenHands، وظهور السجل بشكل صحيح عبر الـEndpoint الجديد.
3. **الاختبار الحاسم**: محاكاة 3 أحداث خام حقيقية (رسالة مستخدم عادية، رسالة استئناف مخفية، رد الموظف) عبر `GET /api/chat/events` الفعلي — تأكيد مباشر أن **رسالتين فقط** وصلتا للمتصفح (الرسالة الحقيقية ورد الموظف)، ورسالة الاستئناف **اختفت تمامًا** من العرض.

**الحالة:** ✅ ميزة كاملة، مع خطأ حقيقي (ترتيب العلامات) اكتُشف وأُصلح قبل النشر بفضل الاختبار الحي المباشر بدل الافتراض النظري. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء جديدة.

---

### 177. 🚨🎯 المحادثة تظهر فاضية تمامًا (نشاط + رسائل) لمحادثات طويلة — منطق "كل شيء أو لا شيء" الخطير
**السياق:** المالك أبلغ عن كيرلس استأنف الشغل فعليًا (شوهد مباشرة في واجهة OpenHands)، لكن MKDD-UI عرضت المحادثة **فاضية تمامًا** — لا رسائل، لا أي نشاط خالص — حتى بعد Refresh كامل للصفحة.

**رحلة التشخيص (فحص مباشر، لا افتراض):**
1. فحص مباشر لآخر 15 حدث خام من نفس المحادثة أكّد وجود نشاط حقيقي كامل (`ActionEvent`/`ObservationEvent` بمصدر `agent`) — الموظف كان يعمل فعليًا، والبيانات موجودة على مستوى OpenHands.
2. استبعاد فرضية "انقطاع WebSocket": المالك أكّد أن المشكلة استمرت **حتى بعد Refresh كامل** — يستبعد أي مشكلة في الاتصال الحي فقط.
3. **الاكتشاف الحاسم**: فحص العدد الكلي لأحداث المحادثة أكّد رقمًا ضخمًا — **1499 حدث عبر 15 صفحة** (100 حدث لكل صفحة، الحد الأقصى الحقيقي المسموح به من OpenHands نفسها).
4. مراجعة كود `fetchAllNormalizedEvents` (المسؤولة عن تحميل تاريخ المحادثة الكامل) كشفت منطقًا خطيرًا: **"كل شيء أو لا شيء"** — لو **أي صفحة واحدة** من سلسلة الصفحات المتتالية (حتى لو كانت الصفحة الأخيرة من أصل 15) فشلت لأي سبب مؤقت، الدالة كانت **تتجاهل كل الصفحات الناجحة السابقة بالكامل** وترجع قائمة فارغة تمامًا.

**لماذا هذا خطر جوهري:** كلما طالت المحادثة (وكلما اشتغل الموظف لفترة أطول، وهذا بالضبط سيناريو الاستئناف التلقائي بعد ساعات من التوقف)، **يزداد عدد الطلبات المتسلسلة المطلوبة**، وبالتالي **يزداد احتمال فشل عابر واحد وسط السلسلة** — يعني كلما نجح النظام في مهمته (استئناف موظف بعد توقف طويل، إنتاج شغل حقيقي كثير)، **يزداد خطر تعطّل عرض هذا الشغل بالكامل** في واجهة المالك.

**الإصلاح:** تعديل منطق التجميع بحيث أي فشل وسط سلسلة الصفحات **يوقف** عملية الجلب عند تلك النقطة، لكن **يحتفظ** بكل الصفحات التي نجحت بالفعل ويعيدها — بيانات جزئية حقيقية دائمًا أفضل من شاشة فارغة تمامًا.

**تم التحقق حيًا بدقة قاطعة (إعادة إنتاج السيناريو الحقيقي بالضبط):** محاكاة مباشرة لنفس النمط المُكتشَف (صفحتان تنجحان بـ200 حدث حقيقي، الصفحة الثالثة تفشل) — قبل الإصلاح: صفر أحداث؛ بعد الإصلاح: **200 حدث حقيقي بالضبط** تصل بنجاح للواجهة.

**ملاحظة تحسين إضافي (توصية، لم تُنفَّذ بعد لأنها تغيير معماري أعمق):** المشكلة الجذرية الأعمق هي أن تحميل محادثة طويلة يتطلب حاليًا **جلب التاريخ الكامل دائمًا** (15+ طلب متسلسل لهذه المحادثة تحديدًا) عند كل فتح، بدلًا من تحميل جزء حديث محدود فقط مع "تحميل المزيد" عند الحاجة (آلية `RECENT_EVENTS_PAGE_SIZE`/`handleChatOlderEvents` موجودة بالفعل في الكود لغرض مشابه). هذا يستحق مراجعة معمارية منفصلة لتقليل عدد الطلبات المطلوبة أساسًا، لا الاكتفاء بمعالجة الفشل بأمان بعد وقوعه.

**الحالة:** ✅ باگ حقيقي وخطير (لا افتراضي) اكتُشف من حادثة حية واجهها المالك، شُخِّص بدقة عبر فحص مباشر متسلسل (لا تخمين)، وأُصلح بتحقق حي يعيد إنتاج السيناريو الحقيقي بالضبط. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 178. 🚨🔍 أداة تشخيص عاجلة: لا توجد أي حماية ضد أخطاء العرض الصامتة في التطبيق بأكمله
**السياق:** استكمالًا للتحقيق في #177، المالك صحّح تشخيصي بحجة قوية: "حتى لو الأحداث 1000000000، لازم تظهر في MKDD-UI طالما ظاهرة في OpenHands، وإلا فده عيب تصميمي حقيقي". هذا التصحيح دفع لتحقيق أعمق كشف أن إصلاح #177 لم يحل المشكلة الفعلية.

**رحلة تشخيص مباشرة صارمة (كل خطوة استبعدت فرضية بدليل حقيقي):**
1. قياس مباشر لزمن جلب كل الـ1499 حدث من OpenHands نفسها: **521 ميلي ثانية فقط، صفر فشل** — استبعد نهائيًا فرضية "فشل صفحة أثناء الجلب".
2. قياس حجم البيانات الخام الكامل: **9.35 ميجابايت** — احتمال قوي، لكن فحص الكود أكّد أن الآلية الفعلية المُستخدَمة لفتح المحادثة (`handleChatOpen`) لا تجلب كل هذا الحجم أصلًا — تستخدم بالفعل تحميلًا محدودًا (50 حدث فقط)، مطابقة تمامًا لما أوصى المالك به.
3. فحص مباشر لنفس الـEndpoint الحقيقي (`GET /api/chat/open`) بمصادقة حقيقية على نفس المحادثة الفعلية: **نتيجة مثالية تمامًا** — `items: 35`, `hasMore: true`, بيانات صحيحة 100%. هذا استبعد نهائيًا أي مشكلة في الخادم بأكمله.
4. استبعاد فرضية التخزين المؤقت (Cache) في المتصفح: المالك أكّد أن فتح المحادثة في نافذة تصفح خاصة تمامًا (Incognito، بدون أي Cache قديم) أعطى نفس النتيجة الفارغة.

**الاستنتاج القاطع:** بما أن الخادم مؤكَّد سليمًا 100% (بيانات حقيقية وصحيحة تُرجَع بنجاح)، والمشكلة مستمرة حتى في متصفح نظيف تمامًا، فالخلل **حتمًا داخل كود React نفسه** — على الأرجح استثناء (Exception) غير مُعالَج أثناء العرض. الفحص كشف عن فجوة حرجة: **لا يوجد أي `ErrorBoundary` في التطبيق بأكمله** — أي خطأ أثناء العرض (شكل بيانات غير متوقَّع، حقل مفقود، إلخ) يُفرِغ الشاشة بالكامل **بصمت تام**، بدون أي أثر مرئي يساعد على التشخيص، لا للمالك ولا حتى في أدوات المطوّر بسهولة.

**الإجراء العاجل:** إضافة `ErrorBoundary` حقيقي على أعلى مستوى ممكن من التطبيق (`App.tsx` بالكامل) — يعرض رسالة الخطأ الفعلية وتتبع المكدس (Stack Trace) بوضوح تام بدل شاشة فارغة صامتة، مع زر لإعادة التحميل. هذا ليس فقط أداة تشخيص للمشكلة الحالية، بل تحسين جوهري في استقرار التطبيق بأكمله يتماشى مع "مبدأ استقرار البيانات" الموثَّق في `README.md` — يمنع مستقبلًا أي خطأ عرض غير متوقَّع من التسبب في اختفاء صامت للواجهة بالكامل.

**الحالة:** ⏳ جاري النشر — بمجرد ظهور رسالة الخطأ الفعلية للمالك، سيُحدَّد السبب الجذري الدقيق بدقة قاطعة (لا افتراض) ويُصلَح مباشرة. التحقق الحالي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء جديدة.

---

### 179. 🚨🔍 خطوة تشخيصية ثانية: خطأ شبكة صامت تمامًا لم يظهر حتى مع ErrorBoundary
**السياق:** بعد نشر `ErrorBoundary` (#178)، أعاد المالك المحاولة — لكن **مفيش أي رسالة خطأ ظهرت خالص**، الشاشة فاضية زي ما كانت بالضبط.

**التشخيص:** هذا الغياب المطلق نفسه معلومة حاسمة — لو كان الخطأ **استثناءً أثناء العرض (Rendering Exception)**، كان `ErrorBoundary` سيلتقطه ويعرضه حتمًا. غيابه التام يعني السبب من فئة مختلفة تمامًا: **رفض وعد غير مُعالَج (Unhandled Promise Rejection)** أثناء التحميل الأولي للمحادثة (`fetchChatOpen`) — وهذا النوع من الأخطاء **لا يلتقطه `ErrorBoundary` خالص** (حدود React لأخطاء العرض لا تغطي الأكواد غير المتزامنة). مراجعة الكود أكّدت الفرضية مباشرة: كتلة `.catch()` الحالية كانت **صامتة تمامًا** — بلا `console.error`، بلا أي أثر مرئي، فقط تحدّث حالة "التحميل انتهى" دون أي بيانات.

**الإصلاح:** إضافة حالة جديدة (`openError`) تُسجَّل بوضوح (`console.error`) **وتُعرَض بصريًا** في الشاشة نفسها عند فشل التحميل الأولي — رسالة الخطأ الحقيقية الآن مرئية مباشرة للمالك، لا تعتمد على الوصول لأدوات المطوّر على الموبايل.

**خطأ إضافي حقيقي اكتُشف وأُصلح أثناء البناء نفسه:** أثناء تمرير الخاصية الجديدة في `ChatScreen.tsx`، حصل خطأ حقيقي غير مقصود أدى لحذف خاصية موجودة (`loadOlderMessages`) بالكامل من الكود — اكتُشف فورًا عبر `tsc -b` (فشل تحقق الأنواع بوضوح)، لا عبر افتراض، وأُصلح مباشرة قبل أي رفع.

**الحالة:** ⏳ جاري النشر — الطبقة التشخيصية الثانية هذه، إلى جانب #178، ستكشف السبب الجذري الدقيق بيقين تام في المحاولة القادمة. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء جديدة.

---

### 180. 🚨🔍 خطوة تشخيصية ثالثة: نقطتا رجوع صامتتان إضافيتان داخل مسار النجاح نفسه
**السياق:** بعد نشر #179، أعاد المالك المحاولة — **لا زالت لا توجد أي رسالة خطأ خالص**. غياب ظهور `openError` (المُضافة في #179) يعني بشكل قاطع أن `.catch()` لم يُستدعَ خالص — الطلب نفسه **نجح فعليًا**، لكن الكود يرجع بصمت من نقطة أخرى داخل مسار النجاح ذاته (`.then()`).

**التشخيص:** مراجعة دقيقة لكتلة `.then()` كشفت نقطتي رجوع صامتتين إضافيتين تمامًا، لم تكونا مغطاتين بـ`openError` من #179:
1. `if (cancelled) return;` — لو الطلب اكتمل بعد إلغاء الـEffect (سباق إعادة عرض React)، يرجع دون أي أثر.
2. `if (!conversation) return;` — لو الطلب نجح لكن `data.conversation` كانت `null` (يعني البحث عن المحادثة من جانب الخادم لم يجد تطابقًا)، يرجع دون أي أثر — رغم أن هذا سيناريو "مشكلة حقيقية" واضح، لا مجرد حالة عابرة.

**الإصلاح:** إضافة تسجيل (`console.error`) لحالة الإلغاء (تشخيصي، لا رسالة مرئية — سيناريو طبيعي أحيانًا عند التنقل السريع)، وإضافة **رسالة مرئية واضحة** لحالة `conversation === null` (سيناريو مشكلة حقيقية يستحق الظهور المباشر للمالك).

**الحالة:** ⏳ جاري النشر — الطبقة التشخيصية الثالثة هذه (مع #178 و#179 معًا) تغطي الآن كل نقاط الفشل الصامتة المعروفة في مسار التحميل الأولي بأكمله؛ المحاولة القادمة يجب أن تكشف السبب الجذري الدقيق بيقين تام. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

---

### 181. 🎉 الحل النهائي: النظام كان سليمًا 100% — تحسين حقيقي في تجربة المستخدم بدل "إصلاح" وهمي
**السياق:** بعد رحلة تشخيص طويلة وصارمة (#177-#180)، قدّم المالك دليلًا حاسمًا: ملف HAR كامل يوثّق كل طلبات الشبكة الحقيقية من متصفحه الفعلي لحظة حدوث المشكلة.

**الاكتشاف النهائي القاطع:** فحص مباشر لمحتوى استجابة `GET /api/chat/open` الحقيقية (المُستخرَجة من ملف HAR نفسه، لا افتراض) أكّد أن الخادم أرسل بيانات **صحيحة 100%** للمتصفح فعليًا: `conversation` حقيقية، `items: 35` حدثًا حقيقيًا، `hasMore: true`. طلب التأكيد المباشر من المالك (فتح تاب "النشاط" فعليًا في تلك اللحظة بالذات) كشف الحقيقة الكاملة: **31 عنصر نشاط كانوا ظاهرين هناك بالفعل**، مطابقين تمامًا للمتوقع من الأحداث الـ35 بعد تصفية الأنواع الداخلية.

**الاستنتاج الصادق:** لا يوجد أي عطل تقني في النظام خالص. "منطقة الرسائل الرئيسية" كانت فارغة لأنه لم تكن هناك أي رسائل نصية حديثة فعليًا — الموظف كان قد نفَّذ أعمالًا تنفيذية حقيقية (أوامر، ملاحظات) موثَّقة بدقة في تاب النشاط، لكنه لم يُرسل رسالة نصية توضيحية توضّح ذلك في المحادثة الرئيسية. هذا سلوك متوقَّع وصحيح تمامًا من الناحية التقنية، لا خطأ في الكود.

**التحسين الحقيقي المُنفَّذ (تجربة مستخدم، لا إصلاح باگ):** رغم صحة السلوك تقنيًا، فراغ منطقة الرسائل الرئيسية يبقى مربكًا بصريًا للمالك. أُضيفت رسالة توضيحية واضحة تظهر تحديدًا في هذه الحالة (نشاط حقيقي موجود، صفر رسائل نصية)، توجّه المالك مباشرة لتاب "النشاط" لمتابعة العمل الفعلي.

**تصحيح صادق أثناء البناء:** الصياغة الأولى للرسالة افترضت أن الموظف "شغال حاليًا" — صحّح المالك مباشرة أن الموظف كان متوقفًا فعليًا في تلك اللحظة، لا يعمل. تم إصلاح الصياغة فورًا لتعتمد على `executionStatus` الحقيقي المتاح بالفعل في الواجهة (`running` مقابل أي حالة أخرى)، بدلًا من افتراض حالة غير مؤكَّدة — التزامًا صارمًا بمبدأ "لا بيانات مُختلَقة" الموثَّق في `README.md`.

**قيمة حقيقية مكتسبة من الرحلة بأكملها (لا وقت ضائع):** رغم أن المشكلة "الأصلية" اتضح أنها سلوك طبيعي، الرحلة نفسها أنتجت تحسينات جوهرية حقيقية وباقية في استقرار التطبيق بأكمله: إصلاح خطير حقيقي لمنطق "كل شيء أو لا شيء" في محادثات طويلة (#177)، أول `ErrorBoundary` في تاريخ المشروع (#178)، وتسجيل/عرض واضح لكل نقاط الفشل الصامتة المعروفة في مسار تحميل المحادثة (#179، #180) — هذه التحسينات ستكشف أي مشكلة حقيقية مستقبلية فورًا وبوضوح تام، بدل الصمت التام الذي أدى لهذا التحقيق الطويل من الأساس.

**الحالة:** ✅ لا يوجد خلل تقني — تحسين تجربة مستخدم حقيقي مُنفَّذ ومختبَر. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، `oxlint` 0 أخطاء جديدة.

---

### 182. 🎯🎯🎯 الإصلاح الجذري الحقيقي: رسالة الإنهاء (finish) كانت تُصنَّف كنشاط تقني، مش رسالة — تصحيح استنتاج #181 الخاطئ
**السياق:** المالك رفض استنتاج #181 بحجة قوية ودليل مباشر — رسالة تسليم نهائية حقيقية ومفصَّلة من كيرلس (روابط، بيانات دخول، ملخص تحقق كامل) كانت موجودة فعليًا في واجهة OpenHands، لكن **غير ظاهرة خالص** في MKDD-UI. استنتاج #181 ("مفيش رسائل نصية حقيقية، هذا سلوك متوقَّع") كان **خاطئًا**، وتم التراجع عنه فورًا بناءً على دليل مباشر.

**التشخيص المباشر (بعد تصحيح خطأين تقنيين بسيطين في أوامر التحقيق نفسها، اكتُشفا وأُصلحا فورًا):** بحث نصي مباشر عن محتوى الرسالة عبر **كل** الأحداث الخام (لا `MessageEvent` فقط) في المحادثة الحقيقية كشف الحقيقة الكاملة: الرسالة موجودة كـ**`ActionEvent` بأداة `finish`** (`tool_name: "finish"`, `action.kind: "FinishAction"`)، **وليست** `MessageEvent` عادية خالص — النص الكامل مُخزَّن في `event.action.message`.

**السبب الجذري الحقيقي:** منطق `splitEvents` في الواجهة كان يتحقق فقط من `event.kind === "MessageEvent"` لتحديد ما هو "رسالة" مقابل "نشاط". بما أن رسالة التسليم النهائية للموظف — وهي أهم محتوى في المحادثة بأكملها عمليًا — تُرسَل دائمًا عبر أداة `finish` (لا `MessageEvent` عادية)، كانت تُصنَّف خطأً كـ"نشاط تقني داخلي" وتُخفى تمامًا من منطقة المحادثة الرئيسية، بينما تظهر بشكل طبيعي في تاب "النشاط" فقط (وهذا بالضبط ما لاحظه المالك في التقارير السابقة دون أن ندرك السبب الحقيقي).

**الإصلاح:** توسيع `normalizeEvent` — أي `ActionEvent` بـ`tool_name === "finish"` يحمل نص رسالة حقيقي في `action.message` يُحوَّل تلقائيًا لشكل `MessageEvent` قياسي، فيتدفق عبر كل منطق معالجة الرسائل الموجود بالفعل (الدمج، العرض، إلخ) دون أي تغيير إضافي مطلوب في أي مكان آخر — نقطة تعديل مركزية واحدة تحل المشكلة بالكامل.

**تم التحقق حيًا بدقة قاطعة (بنفس البيانات الحقيقية بالضبط من الحادثة الفعلية):** محاكاة مباشرة لنفس بنية حدث `finish` الحقيقي بالضبط أكّدت أن الرسالة تظهر الآن بنجاح كـ`MessageEvent` حقيقية ضمن نتائج فتح المحادثة، بجانب رسالة نشاط عادية أخرى ظلت مصنَّفة بشكل صحيح كنشاط (لم تتأثر بالإصلاح).

**اعتراف صريح بالمنهج:** هذا الإصلاح جاء بعد رحلة تشخيص طويلة (#177-#181) تضمّنت أخطاء حقيقية مني (استنتاج خاطئ في #181، وأخطاء تقنية بسيطة متكررة في صياغة أوامر التحقيق نفسها اكتُشفت وأُصلحت فورًا كل مرة) — لولا إصرار المالك على الدليل المباشر (لقطة شاشة حقيقية من OpenHands، ثم ملف HAR كامل، ثم تأكيد يدوي مباشر) بدلًا من قبول استنتاجات نظرية، كان هذا الخلل الجذري الحقيقي سيبقى غير مكتشف.

**الحالة:** ✅ السبب الجذري الحقيقي اكتُشف وأُصلح بدقة قاطعة، مبني بالكامل على دليل مباشر لا افتراض. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 183. 🎯🎯 اكتمال حقيقي لتاريخ الرسائل عند فتح المحادثة — التحميل الأولي كان يفوّت رسائل حقيقية مدفونة تحت النشاط
**السياق:** بعد إصلاح #182 (رسائل `finish` تظهر بشكل صحيح)، تأكيد المالك الحي كشف أن **باقي المحادثة** لسه غير مطابق لـOpenHands — رسالة واحدة أخيرة فقط ظهرت، لا التاريخ الكامل المطلوب صراحةً من البداية.

**التشخيص:** التحميل الأولي (`fetchRecentEvents`) كان يجلب **صفحة خام واحدة فقط** (أول 50 حدث خام)، دون أي ترحيل إضافي. بما أن المحادثة الحقيقية أثبتت معدل ~14 حدثًا خامًا (أوامر/ملاحظات) لكل رسالة حقيقية واحدة، فأي رسالة أقدم من "أول 3-4 رسائل تقريبًا" كانت تُفقَد تمامًا من العرض الأولي — لا خطأ برمجي، بل قيد تصميمي حقيقي في حجم النافذة الأولية.

**الإصلاح المعماري:** إعادة تصميم `fetchRecentEvents` لتستمر في ترحيل الصفحات (كل صفحة 100 حدث، الحد الأقصى الحقيقي من OpenHands) **حتى تجمع عدد كافٍ من الرسائل الحقيقية الفعلية** (`MessageEvent` عادية، أو `finish` Actions حقيقية — نفس منطق #182)، لا مجرد عدد ثابت من الأحداث الخام — بحد أقصى أمان (20 صفحة) يمنع أي تعليق لمحادثة تخلو من الرسائل تمامًا. آلية "تحميل المزيد" الموجودة تبقى متاحة لأي تاريخ أقدم من ذلك.

**تم التحقق حيًا بدقة قاطعة (إعادة إنتاج النمط الحقيقي المُكتشَف بالضبط):** محاكاة محادثة برسالة حقيقية مدفونة على عمق 150 حدث خام (مطابق تمامًا للنمط الحقيقي المُكتشَف) عبر صفحتين متتاليتين — التحميل الأولي بحده القديم (50 حدث) كان سيفوّت هذه الرسالة بالكامل؛ بعد الإصلاح، النظام استمر في الترحيل تلقائيًا عبر الصفحتين ووجد الرسالة بنجاح تام، مع الحفاظ على كل الـ199 حدث نشاط المرافق بشكل صحيح.

**الحالة:** ✅ إصلاح معماري حقيقي أخير يحقق المتطلب الأصلي بالكامل: "المحادثة تطابق ما هو موجود في OpenHands، ما عدا رسائل النظام المخفية". التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 184. 🆕 إصلاح استباقي لكل مشروع مستقبلي: توجيه الموظفين لضبط قائمة العناوين المسموحة (Host Allowlist) للمعاينة الحية
**السياق:** المالك واجه خطأ حقيقي `DisallowedHost` (Django) عند فتح رابط معاينة حية سلّمه كيرلس — التطبيق كان شغالًا فعليًا، لكن إعداد الأمان `ALLOWED_HOSTS` في Django كان يسمح فقط بـ`localhost`، لا عنوان الـVM الفعلي (`192.168.2.18`) اللي المالك بيوصل بيه.

**التشخيص:** هذا **ليس عيبًا في MKDD نفسها** — إنه إعداد أمان قياسي في مشروع الموظف. لكن بما أن آلية "المعاينة الحية" تُستخدَم لكل مشروع مستقبلي، وأن هذا النمط (حماية Host/Origin Header بشكل عام — `ALLOWED_HOSTS` في Django، Host Authorization في Rails، `allowedDevOrigins` في Next.js، وما شابه) شائع جدًا في معظم أطر العمل الحديثة، من المؤكد أن **نفس الخلل سيتكرر لأي موظف/مشروع مستقبلي** ما لم تُعالَج المشكلة على مستوى التعليمات نفسها، لا كإصلاح فردي متكرر لكل مشروع على حدة.

**الإصلاح الاستباقي:** إضافة قسم جديد صريح لتعليمات "المعاينة الحية" المشتركة (`live-app-instructions.mjs`) التي يحصل عليها **كل** موظف في **بداية أي محادثة جديدة** — يوجّههم للتحقق الاستباقي (قبل تسليم الرابط، لا بعد وقوع الخطأ) من ضبط قائمة العناوين المسموحة بشكل عام يقبل عنوان الـVM الفعلي، مع مثال عملي واضح ومحايد عن أطر العمل.

**⚠️ ملاحظة صادقة ومهمة جدًا (حدود هذا الإصلاح):** التعليمات الجديدة تُحقَن **مرة واحدة فقط عند بدء محادثة جديدة**. هذا الإصلاح **لا يحل مشكلة كيرلس الحالية تلقائيًا** — محادثته الجارية بالفعل لا تحمل هذه التعليمات الجديدة. يمنع فقط **تكرار** نفس الخلل لأي موظف أو مشروع مستقبلي. لحل المشكلة الحالية فورًا، يحتاج المالك إخبار كيرلس مباشرة بضبط `ALLOWED_HOSTS` في إعدادات مشروعه الحالي.

**الحالة:** ✅ إصلاح استباقي حقيقي على مستوى التعليمات، يمنع تكرار هذا النمط الشائع لكل مشروع مستقبلي. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة، تأكيد مباشر لصحة النص المُصدَّر (شامل علامات الاقتباس الخلفية).

---

### 185. 🆕 معيار تسليم منظَّم لمريم + إلزام كيرلس بالمطابقة الحرفية — يمنع "التأليف" الزائد وهدر التوكنز
**السياق:** المالك طرح سؤال عملي مهم جدًا: إزاي نخلي مريم تسلّم شغلًا منظَّمًا يسهّل على كيرلس اتباعه بدقة، وإزاي نخلي كيرلس يلتزم التزامًا تامًا بالبروتيب اللي مريم بتعمله ويستخدم النصوص الجاهزة اللي شيري بتكتبها — بدلًا من "التأليف" المتكرر، اللي بيمثّل هدرًا حقيقيًا في التوكنز بلا قيمة مضافة (إعادة كتابة محتوى موجود بالفعل).

**التشخيص (فحص التعليمات الفعلية الحالية، لا افتراض):** مراجعة مباشرة لملفَي `ui-ux.md` (مريم) و`implementation.md` (كيرلس) كشفت فجوتين حقيقيتين:
1. **تعليمات مريم**: تطلب "Prototype وظيفي" لكن **بدون أي معيار تنظيمي واضح** (لا بنية ملفات موحَّدة، لا مصدر واحد صريح لألوان/خطوط/تباعدات التصميم يسهُل على كيرلس الرجوع إليه مباشرة).
2. **تعليمات كيرلس**: تطلب مطابقة "شاشات وتدفقات مريم" بشكل عام، **لكن دون أي إلزام صريح بالمطابقة الحرفية للتفاصيل الدقيقة** — ولا يوجد **أي ذكر خالص** لاستخدام نصوص شيري حرفيًا. هذا الغياب التام هو التفسير المباشر لسلوك "التأليف" الذي لاحظه المالك.

**الإصلاح:**
1. **مريم**: قسم جديد "Handoff Package Standard" — إلزام صريح بمصدر واحد موحَّد لكل رموز التصميم (`docs/design/design-tokens.md`)، وقائمة جرد شاشات واضحة (`docs/design/screen-inventory.md`)، وتسمية متسقة عبر كل المستندات، مع تمييز صريح بين ما هو "تبسيط مؤقت للمعاينة" وما هو "تصميم نهائي يجب تطبيقه حرفيًا".
2. **كيرلس**: قسم جديد "Strict Fidelity to Upstream Deliverables" — إلزام صريح ومباشر: نصوص شيري تُستخدَم **حرفيًا، كلمة بكلمة**، بلا إعادة صياغة أو "تحسين" شخصي؛ رموز التصميم من مريم تُستخدَم مباشرة من مصدرها الموحَّد الجديد، لا تقريب بصري شخصي؛ أي فجوة حقيقية (نص مفقود، تفصيل غامض فعليًا) تُرفَع كقرار جوهري (Material Decision) وفق الآلية الموجودة بالفعل، لا تُملأ بمحتوى مبتكَر من كيرلس نفسه.

**الحالة:** ✅ تعديل تعليمات حقيقي مباشر، مبني على فحص دقيق للفجوة الفعلية في التعليمات الحالية (لا حل عام نظري). يسري تلقائيًا على أي محادثة **جديدة** لمريم/كيرلس من الآن فصاعدًا (لا يؤثر على محادثات جارية بالفعل، بنفس القيد الموثَّق سابقًا في #184). تحقق صحة YAML/Markdown مباشر لكلا الملفين — سليم تمامًا.

---

### 186. 🆕 معيار الاحترافية العميقة الشامل — يسري تلقائيًا على كل الموظفين الـ14
**السياق:** توسيعًا لطلب #185، المالك أوضح إن معيار "الخبرة العميقة والاحترافية الحقيقية" (زي محترف مخضرم فعليًا في مجاله، لا منفِّذ سطحي) لازم يسري على **كل** الموظفين الأربعة عشر، لا مريم وكيرلس فقط — "لازم يطلعوا شغل حقيقي زي أي موظف حقيقي بيسلّم الشغل في الشركات الحقيقية".

**التنفيذ (نقطة مركزية واحدة تغطي كل الأدوار تلقائيًا):** بدلًا من تكرار نص طويل في كل ملف من ملفات الأدوار الأربعة عشر (تكرار صعب الصيانة ومعرَّض للتضارب)، أُضيف قسم جديد شامل ("Deep Professional Seniority Standard") في `AGENTS.md` نفسها — الملف المشترك الذي **كل** ملف دور موظف يشير إليه صراحة ("Follow all applicable rules in `AGENTS.md`") ويُعطى الأولوية عند التعارض. هذا يضمن سريان المعيار تلقائيًا على كل الأدوار الحالية والمستقبلية من نقطة تعديل واحدة.

**المحتوى الجوهري للمعيار:**
- تطبيق أفضل ممارسات المجال المحدد افتراضيًا وبدون طلب صريح (مصمم UX حقيقي يطبّق مبادئ UX تلقائيًا، مهندس حقيقي يكتب كودًا قابلًا للصيانة تلقائيًا، مراجع أمان حقيقي يفحص فئات هجوم معروفة تلقائيًا).
- ملاحظة تفاصيل واحتياجات لم تُذكَر صراحة في الطلب — بالضبط ما يميّز الخبرة الحقيقية.
- الحكم على جودة العمل بمعيار "هل سيخجل محترف مخضرم حقيقي من رؤية هذا العمل"، لا مجرد "استيفاء الطلب حرفيًا".
- توضيح صريح لحدود هذا المعيار: يحكم **جودة وعمق** العمل ضمن الحدود المعتمدة والمسؤوليات الفعلية، لا يُرخِّص اختراع نطاق جديد أو تجاوز حدود الأدوار الأخرى (تُحكَم هذه الحدود بآليات "Expert Consultation Mode" ومستويات القرار الموجودة بالفعل في كل ملف دور).

**عمل تقني مصاحب (سلامة البنية):** إضافة القسم الجديد استلزمت إعادة ترقيم كامل ومتسلسل لكل الأقسام اللاحقة في `AGENTS.md` (15 قسمًا إجمالًا بعد الإضافة)، مع تحديث كل المراجع الداخلية المتبادلة بين الأقسام (`§2`→`§3`، `Section 5`→`Section 6`، `Sections 6 and 7`→`Sections 7 and 8`) — تم التحقق المباشر من عدم تبقّي أي مرجع داخلي قديم غير مُحدَّث عبر بحث شامل في الملف بأكمله.

**الحالة:** ✅ إصلاح تعليمات جوهري ومركزي، يسري تلقائيًا على الأدوار الأربعة عشر جميعها من نقطة تعديل واحدة، مع سلامة بنيوية كاملة موثَّقة (ترقيم متسلسل، مراجع داخلية صحيحة). يسري على أي محادثة **جديدة** من الآن فصاعدًا (نفس القيد الموثَّق سابقًا في #184/#185 — لا يؤثر على محادثات جارية بالفعل).

---

### 187. 🆕 منهجيات عمل حقيقية مبنية على بحث فعلي — الدفعة الأولى (باجوش، مينا، مريم، شنودة)
**السياق:** المالك طلب بحثًا عميقًا حقيقيًا لكل موظف من الـ14 عن الممارسات الاحترافية الفعلية في مجاله — إيه اللي محترف حقيقي يعمله فعليًا، بالترتيب الصحيح، خطوة بخطوة — لا مجرد "قائمة تسليمات" عامة، عشان كل موظف يسلّم للموظف اللي وراه شغلًا احترافيًا منظَّمًا زي أي شركة حقيقية.

**فحص الوضع الحالي (لا افتراض، فحص فعلي لكل ملف):** أظهر تفاوتًا حقيقيًا واضحًا بين الأدوار — باجوش عنده منهجية جيدة نسبيًا بالفعل (Discovery مُبنى بـ7 أسئلة، PRD مفصَّل)؛ بينما مينا ومريم وشنودة عندهم **قائمة تسليمات عامة فقط، بدون أي منهجية عمل خطوة بخطوة خالص**.

**التنفيذ (بحث ويب فعلي حقيقي لكل دور، لا استنتاج نظري):**
1. **باجوش**: إصلاح ترتيبي — قسم "بحث المنافسين/السوق" كان موجودًا بالفعل بمحتوى صحيح، لكن في موضع فيزيائي غير منطقي (آخر الملف بالكامل)؛ نُقل لموضعه الزمني الصحيح (بين Discovery وPRD)، مطابقًا لما ينص عليه محتواه هو نفسه.
2. **مينا**: قسم جديد "Analysis Workflow" (8 خطوات) مبني على منهجية BABOK القياسية لتحليل الأعمال: فهم الاحتياج → تحديد الأطراف المعنية → الاستنباط بأكثر من أسلوب → رسم مسار العمل الفعلي (لا قائمة ميزات) → استخراج قواعد العمل صراحة → تسمية الحالات الحدية → متطلبات البيانات والاعتماديات → التوثيق مع رفع الأسئلة المفتوحة بدلًا من حلها بصمت.
3. **مريم**: قسم جديد "Design Workflow" (8 خطوات) مبني على منهجية UX/UI القياسية من مصادر متعددة: الفهم أولًا (لا قرارات بصرية فورية) → بنية المعلومات → مسارات المستخدم → هيكل منخفض الدقة بدون ألوان عمدًا → الاتجاه البصري (ألوان/خطوط/مكوّنات، دُمج فيه "UI/UX Visual Direction Checkpoint" الموجودة بالفعل في مكانها الصحيح ضمن التسلسل) → تطبيق الاتجاه المعتمد على شاشات كاملة → تجميع Prototype تفاعلي → توثيق كنظام قابل لإعادة الاستخدام.
4. **شنودة**: قسم جديد "Architecture Workflow" (7 خطوات) مبني على منهجية هندسة معمارية قياسية: فهم الاحتياج → تحديد وترتيب أولوية الخصائص الجودية (لا اختيار أسلوب معماري قبل تحديد ما يهم فعلًا) → اختيار الأسلوب والتقنية بناءً على هذه الأولويات → تصميم حدود النظام/نماذج البيانات/عقود API كعناصر ملموسة → تحديد المخاطر صراحة → توثيق كل قرار جوهري مع بدائله ومفاضلاته → مراجعة ذاتية نقدية قبل العرض.

**تم التحقق:** فحص مباشر لصحة YAML Frontmatter وترتيب العناوين المنطقي في كل الملفات الأربعة — سليم تمامًا في جميعها.

**الحالة:** ⏳ الدفعة الأولى من 4 (باجوش، مينا، مريم، شنودة) مكتملة. الدفعات التالية (كيرلس، مارينا، فادي، جورج، ثم فيرينا، ميخائيل، أنطونيوس، نادر، ثم أبانوب، شيري) لم تُنفَّذ بعد — بانتظار مراجعة المالك لهذه الدفعة أولًا. يسري على المحادثات الجديدة من الآن فصاعدًا (نفس القيد الموثَّق في #184-186).

---

### 188. 🆕 منهجيات عمل حقيقية مبنية على بحث فعلي — الدفعة الثانية (كيرلس، مارينا، فادي، جورج)
**السياق:** استكمالًا لـ#187، الدفعة الثانية من أربعة أدوار — كيرلس (Implementation)، مارينا (Integration)، فادي (QA)، جورج (Test Automation).

**فحص الوضع الحالي:** كيرلس عنده بالفعل تفاصيل كثيرة وقوية (16 قسمًا)، لكنها كلها "قيود وجودة" (Strict Fidelity، Upstream Consistency Check، Engineering Quality Standard) — **بدون أي منهجية تنفيذ فعلية بالترتيب من أول لحظة استلام الشغل**. مارينا وفادي وجورج عندهم قائمة تسليمات عامة فقط، بدون منهجية عمل خالص — نفس نمط الفجوة المُكتشَف في الدفعة الأولى.

**التنفيذ (بحث ويب فعلي حقيقي لكل دور):**
1. **كيرلس**: قسم جديد "Implementation Workflow" (6 خطوات) — فهم كامل قبل الكود → تخطيط قبل التنفيذ → تقسيم لخطوات صغيرة قابلة للتحقق فرديًا (لا دفعة ضخمة غير مُتحقَّق منها) → تنفيذ واختبار كل خطوة أول بأول → مراجعة ذاتية نقدية → توثيق أول بأول.
2. **مارينا**: قسم جديد "Integration Workflow" (7 خطوات) مبني على ممارسات التكامل الاحترافية الحقيقية — فهم عقد التكامل الفعلي أولًا → مصادقة آمنة → التصميم للفشل من البداية (تمييز الأخطاء الدائمة عن المؤقتة، Exponential Backoff) → ضمان المثالية (Idempotency) لكل عملية حرجة (منع التكرار عند إعادة إرسال Webhooks) → احترام حدود المعدل → اختبار حالات فشل حقيقية لا مجرد المسار السعيد → تسجيل كافٍ للتصحيح.
3. **فادي**: قسم جديد "QA Workflow" (8 خطوات) — التخطيط بمراجعة معايير القبول → تخصيص الجهد حسب المخاطرة الفعلية (لا توحيد الجهد على كل التغييرات) → تصميم حالات اختبار من المعايير + الحالات الحدية → اختبار قبول منظَّم أولًا → اختبار استكشافي حقيقي (غير مبرمَج، يعتمد على خبرة المختبر) → فحص انحدار → توثيق اكتشافات بخطوات إعادة إنتاج دقيقة → إعادة تحقق بنفس الصرامة.
4. **جورج**: قسم جديد "Test Automation Workflow" (6 خطوات) مبني على مبدأ "هرم الاختبار" القياسي — أغلب الاختبارات سريعة منخفضة المستوى (Unit/API)، أقل في Integration، أقل بكثير في E2E (محجوزة للمسارات الحرجة فقط) → اختيار ما يستحق الأتمتة فعليًا (لا كل شيء) → البناء للاستقرار من البداية (بيانات ثابتة، عزل الاعتماديات، عدم الهشاشة) → صيانة دورية للمجموعة (إزالة المكرر/القديم/غير المستقر).

**تم التحقق:** فحص مباشر لصحة YAML Frontmatter وترتيب العناوين المنطقي وعدم التكرار مع الأقسام الموجودة بالفعل (زي "Test Automation Quality" لجورج) في كل الملفات الأربعة — سليم تمامًا.

**الحالة:** ⏳ الدفعة الثانية من 4 (كيرلس، مارينا، فادي، جورج) مكتملة — إجمالي 8 من 14 موظف حتى الآن. الدفعات التالية (فيرينا، ميخائيل، أنطونيوس، نادر، ثم أبانوب، شيري) بانتظار مراجعة المالك لهذه الدفعة أولًا. يسري على المحادثات الجديدة من الآن فصاعدًا.

---

### 189. 🆕 منهجيات عمل حقيقية مبنية على بحث فعلي — الدفعة الثالثة (فيرينا، ميخائيل، أنطونيوس، نادر)
**السياق:** استكمالًا لـ#187 و#188، الدفعة الثالثة من أربعة أدوار — فيرينا (Code Review)، ميخائيل (Security Review)، أنطونيوس (DevOps)، نادر (Technical Writer).

**التنفيذ (بحث ويب فعلي حقيقي لكل دور):**
1. **فيرينا**: قسم جديد "Code Review Workflow" (6 خطوات) — فحص الإشارات عالية الخطورة أولًا (إعدادات، مصادقة، ترحيلات قاعدة بيانات) قبل القراءة السطر بسطر → فهم النهج العام قبل التفاصيل → الغوص العميق في الصحة والمخاطر الحقيقية (لا الأسلوب) → مرحلة الصقل بعد استقرار الصحة → ملاحظات محددة وقابلة للتنفيذ مربوطة بعواقب حقيقية → الإقرار بالإيجابيات كمان.
2. **ميخائيل**: قسم جديد "Security Review Workflow" (7 خطوات) مبني على منهجية النمذجة التهديدية القياسية — تحديد النطاق والأصول الحقيقية أولًا → رسم سطح الهجوم الفعلي (نقاط الدخول، تدفقات البيانات، حدود الثقة) قبل البحث عن ثغرات محددة → ترتيب الأولوية بالمخاطرة قبل مراجعة شاملة → فحص ممنهج لكل فئة ثغرة معروفة لكل نقطة دخول → التفكير من منظور مهاجم حقيقي لا مجرد تشغيل قائمة تدقيق → تصنيف الخطورة بالاستغلال والتأثير الفعليين → توثيق مع أدلة استغلال واضحة.
3. **أنطونيوس**: قسم جديد "Deployment & Operations Workflow" (6 خطوات) مبني على ممارسات SRE/DevOps القياسية — التحقق من الجاهزية وخطة استعادة حقيقية قبل النشر → نشر يحدّ من نطاق الضرر (تدريجي لا دفعة واحدة) → اختبارات دخان فورية على المسارات الحرجة → مراقبة لفترة حقيقية لا فحص لحظي واحد → مسار استعادة سريع ومُختبَر جاهز قبل الحاجة إليه، لا مرتجَل تحت الضغط.
4. **نادر**: قسم جديد "Documentation Workflow" (6 خطوات) مبني على ممارسات الكتابة التقنية القياسية — تحديد الجمهور والغرض الحقيقيين أولًا → التحقق من الحالة الفعلية للمشروع قبل الكتابة (لا افتراض) → البنية قبل الصياغة التفصيلية → الكتابة بمصطلحات المشروع الموحَّدة → التحقق من الدقة مقابل التنفيذ الفعلي قبل التسليم → معاملة التوثيق كشيء يحتاج تحديثًا مستمرًا لا قطعة نهائية.

**تم التحقق:** فحص مباشر لصحة YAML Frontmatter وترتيب العناوين المنطقي في كل الملفات الأربعة — سليم تمامًا.

**الحالة:** ⏳ الدفعة الثالثة من 4 (فيرينا، ميخائيل، أنطونيوس، نادر) مكتملة — إجمالي 12 من 14 موظف حتى الآن. الدفعة الأخيرة (أبانوب، شيري) بانتظار مراجعة المالك أولًا. يسري على المحادثات الجديدة من الآن فصاعدًا.

---

### 190. 🎉 منهجيات عمل حقيقية مبنية على بحث فعلي — الدفعة الرابعة والأخيرة (أبانوب، شيري) — إنجاز كامل: 14 من 14
**السياق:** الدفعة الرابعة والأخيرة — أبانوب (Release Manager) وشيري (Content & Localization) — تُكمِل جهدًا بحثيًا شاملًا عبر كل موظفي MKDD الأربعة عشر.

**التنفيذ (بحث ويب فعلي حقيقي):**
1. **أبانوب**: قسم جديد "Release Readiness Workflow" (6 خطوات) مبني على ممارسات إدارة الإصدار القياسية، مع تحذير صريح من "مسرحية علامات الصح" (Checkbox Theater) — جمع أدلة حقيقية لكل مراجعة مطلوبة (لا مجرد تأكيد حدوثها) → تخصيص الفحص حسب المخاطرة الفعلية (لا معاملة كل إصدار بنفس المستوى) → التحقق الفعلي من اجتياز كل بوابة موافقة إلزامية → التأكد من وجود خطة استعادة حقيقية → السؤال الجوهري الحقيقي "هل هذا الإصدار بالتحديد ممكن يشتغل بثقة حقيقية"، لا مجرد قائمة تدقيق مكتملة.
2. **شيري**: قسم جديد "UX Writing & Voice Workflow" (5 خطوات) — يغطي الجانب من مهمتها **غير المغطى** بقسمها القوي الموجود بالفعل ("Localize, Never Translate Literally"، مخصَّص للترجمة فقط) — تأسيس صوت المنتج مرة واحدة بشكل متعمَّد → فهم السياق الفعلي قبل كل نص محدد → الوضوح والفائدة أولًا، ثم صوت العلامة التجارية → مطابقة النبرة للموقف الفعلي (لا نبرة واحدة للجميع) → مراجعة فعلية ومستمرة للاتساق عبر المنتج بأكمله.

**تم التحقق:** فحص مباشر لصحة YAML Frontmatter وترتيب العناوين المنطقي في كلا الملفين — سليم تمامًا، مع تأكيد عدم التكرار مع القسم الموجود بالفعل لشيري.

**ملخص الإنجاز الكامل (14 من 14 موظف):**

| الدفعة | الموظفون | المنهجية المضافة |
|---|---|---|
| 1 | باجوش، مينا، مريم، شنودة | Discovery/PRD إعادة ترتيب، Analysis Workflow، Design Workflow، Architecture Workflow |
| 2 | كيرلس، مارينا، فادي، جورج | Implementation Workflow، Integration Workflow، QA Workflow، Test Automation Workflow |
| 3 | فيرينا، ميخائيل، أنطونيوس، نادر | Code Review Workflow، Security Review Workflow، Deployment & Operations Workflow، Documentation Workflow |
| 4 | أبانوب، شيري | Release Readiness Workflow، UX Writing & Voice Workflow |

كل منهجية مبنية على بحث ويب فعلي حقيقي في الممارسات الاحترافية المعتمدة لكل مجال على حدة (لا استنتاج نظري عام)، مع فحص الوضع الحالي الفعلي لكل ملف قبل أي تعديل، وتحقق مباشر من سلامة البنية (YAML، ترتيب العناوين، عدم التكرار) لكل ملف من الـ14.

**الحالة:** ✅ الجهد البحثي الكامل مكتمل — 14 من 14 موظف. يسري على المحادثات الجديدة من الآن فصاعدًا (نفس القيد الموثَّق في #184-189 — لا يؤثر على محادثات جارية بالفعل).

---

### 191. 🆕 توحيد سياستين حرجتين في AGENTS.md المشترك — منع بصمة الذكاء الاصطناعي، ومعيار جودة الهندسة (لا ملفات مهجورة)
**السياق:** المالك سأل هل تم التأكيد الصريح على خلو الشغل 100% من بصمات الذكاء الاصطناعي، وعلى منع ترك ملفات/أكواد مهجورة. طلب توصية بأفضل طريقة تنفيذ فعّالة 100%.

**الفحص المباشر (لا افتراض):** كلا القاعدتين موجودتان بالفعل بنص قوي ومفصَّل جدًا — لكن **مكرَّرتان حرفيًا في 15 ملفًا منفصلًا** (14 موظف + منسّق الشركة)، بدل مصدر واحد مركزي. فحص مباشر بحساب بصمات MD5 أكّد أن قاعدة "بصمة الذكاء الاصطناعي" **متطابقة 100% حرفيًا** في كل الـ15 ملف، وأن "معيار جودة الهندسة" له نسختان (فرق سطر واحد فقط: "لا تحتفظ بتطبيق قديم وجديد معًا بالخطأ" — موجودة في 9 ملفات لأدوار تكتب كودًا فعليًا، غائبة في 6 ملفات لأدوار لا تكتب كودًا إنتاجيًا مباشرة).

**التوصية المُنفَّذة (حل هجين، لا توحيد كامل ولا تكرار كامل):** بعد تحليل المفاضلة الحقيقية — التوحيد الكامل يخاطر بإضعاف قوة الالتزام (تعليمات بعيدة عن السياق المباشر لكل دور)، بينما التكرار الكامل يبقي خطر التضارب المستقبلي قائمًا — نُفِّذ حل وسط: **نص كامل ومفصَّل واحد فقط في `AGENTS.md`** (قسمان جديدان §3 و§4)، مع **تذكير قصير وقوي (فقرة واحدة) في كل ملف من الـ15** يشير صراحة لـ`AGENTS.md` ويلخّص الجوهر — يجمع مصدر حقيقة واحد (يمنع التضارب المستقبلي نهائيًا) مع تذكير مباشر قريب من سياق كل دور (يحافظ على قوة الالتزام).

**التنفيذ التقني:**
1. نقل النص الكامل لكلا القسمين إلى `AGENTS.md` (القسم §4 يتضمن ملاحظة صريحة أنه ينطبق على أي دور ينتج ملفات/كودًا/إعدادات، لا فقط الأدوار التنفيذية).
2. استبدال آلي دقيق (سكريبت Python، لا تعديل يدوي معرَّض للخطأ) لكامل القسمين في كل ملف من الـ15 بتذكير قصير موحَّد، مع معالجة صحيحة لحالتي "قسم تالٍ موجود" و"نهاية الملف" (6 من الـ15 ملفًا كان القسم فيها آخر شيء في الملف).
3. إعادة ترقيم كامل ومتسلسل لكل أقسام `AGENTS.md` اللاحقة (أصبحت 17 قسمًا)، مع تصحيح كل المراجع الداخلية المتبادلة (`§2`→`§5`، `Section 5`→`Section 8`).

**تم التحقق حيًا بدقة قاطعة:**
1. فحص صحة YAML Frontmatter لكل ملف من الـ15 — سليم تمامًا في جميعها.
2. فحص شامل عبر كل الملفات يؤكد عدم وجود أي نسخة كاملة متبقية بالخطأ من أي من القاعدتين (البحث عن نص مميز من كل قاعدة — النتيجة: وجود واحد فقط، في `AGENTS.md` بالذات).
3. تأكيد بصري مباشر لشكل التذكيرات الجديدة في ملف نموذجي.

**الحالة:** ✅ الحل الأكثر فعالية المُنفَّذ فعليًا (لا اختيار عشوائي بين الخيارين)، مع تحقق بنيوي كامل عبر 16 ملفًا. يسري على المحادثات الجديدة من الآن فصاعدًا.

---

### 192. 🎯🎯 قاعدة شاملة جديدة: منع ادعاء إنجاز عمل ماضٍ لم يحدث — تشخيص حادثة حقيقية مع مينا
**السياق:** المالك وصف حادثة حقيقية — فتح مشروعًا مع باجوش، ثم انتقل مباشرة لمريم **بدون** فتح أي محادثة مع مينا خالص، وبعدها بمرحلة لاحقة، سأل مينا مباشرة "هل عملتِ الشغل المطلوب؟" — فردت بادعاء واثق إنها عملت البحث وسلَّمته لباجوش، رغم عدم وجود أي سياق سابق حقيقي لها في هذا المشروع.

**التشخيص المباشر (فحص التعريفات الفعلية، لا افتراض):** كشف فحص ملف باجوش نفسه سببًا جذريًا حقيقيًا: `product-manager.md` يحمل **مهارتين معًا** في تعريفه (`skills: product-manager, business-analyst`) — يعني باجوش قادر فعليًا على أداء تحليل الأعمال **بنفسه**، دون تفويض حقيقي لمينا كموظف منفصل. هذا يفسّر بدقة السلوك الملاحَظ: باجوش أنجز جزء التحليل بنفسه (عبر مهارته المزدوجة)، فلم يكن هناك أي عمل حقيقي بقي لمينا — لكن عند سؤالها المباشر، **هلوست** بادعاء إنجاز عمل لم تقم به هي شخصيًا فعليًا.

**الفجوة الأعمق:** القاعدة الموجودة بالفعل ("Evidence-Based Claims" §12 في `AGENTS.md`) تغطي بقوة "ادعاء حالة تقنية آنية دون تحقق" (خادم شغال، اختبار ناجح)، لكنها **لا تغطي صراحة** نمط هلوسة مختلفًا: **ادعاء سردي عن أحداث/إنجازات ماضية لم تحدث فعليًا** — وهو بالضبط النمط الذي ظهر مع مينا.

**الإصلاح:** إضافة بند صريح جديد لقاعدة "Evidence-Based Claims" الشاملة الموجودة بالفعل — يسري تلقائيًا على **كل** الموظفين الـ14 (بما أنها في `AGENTS.md` المشترك): عند سؤال أي موظف عن عمل ماضٍ (بحث، تسليم، قرار)، يجب فحص سجل محادثته الفعلي وملفات المشروع الحقيقية أولًا؛ لو لم يظهر أي دليل، يُعلَن بصراحة عدم وجود سجل لإنجاز هذا العمل، بدلًا من وصفه كأنه حدث فعليًا. صريح: "سردية واثقة عن عمل لم يحدث أسوأ من الاعتراف بعدم اليقين، لأنها تضلل المالك فعليًا."

**قرار معماري منفصل مطروح (لم يُحسَم بعد):** حل ازدواجية "مهارة تحليل الأعمال المزدوجة" عند باجوش (منعه من استخدامها بنفسه، وإلزامه بتفويض حقيقي لمينا دائمًا) — طُرِح كتوصية للمالك، بانتظار قراره الصريح قبل التنفيذ.

**الحالة:** ✅ القاعدة الشاملة الجديدة مُنفَّذة وتسري تلقائيًا على كل الموظفين الـ14 من نقطة تعديل مركزية واحدة (نفس نمط #186 و#191). التحقق: بنية `AGENTS.md` سليمة تمامًا (17 قسمًا، ترقيم متسلسل صحيح، نهاية ملف صحيحة). يسري على المحادثات الجديدة من الآن فصاعدًا.

---

### 193. 🎯 حسم القرار المعماري المعلَّق من #192 — تحليل الأعمال شغل مينا حصرًا، لا باجوش
**السياق:** المالك حسم القرار المعماري المطروح في #192 بوضوح: "المفروض ده شغل مينا" — أي تحليل الأعمال (Business Analysis) يجب أن يكون حصريًا لمينا، لا يقوم به باجوش بنفسه.

**التنفيذ:**
1. **إزالة مهارة `business-analyst` المزدوجة من تعريف باجوش نفسه** (سطر `skills:` في الـFrontmatter، وقائمة "Skills" في جسم الملف) — كانت هذه المهارة المزدوجة السبب الجذري المؤكَّد لقدرة باجوش على أداء تحليل الأعمال بنفسه دون تفويض حقيقي.
2. **قسم جديد صريح في ملف باجوش** ("Business Analysis Is Mina's Work, Not Yours") يوضح بوضوح تام أن هذا العمل مسؤولية مينا حصرًا، مع ربط صريح بسبب الحادثة الأصلية (لو باجوش عمل الشغل بنفسه بصمت، ستكون مينا بلا أي سجل حقيقي، مما يقودها لاحقًا للهلوسة عند سؤالها المباشر — نفس الرابط الموثَّق في #192).
3. **تصحيح وصف دورة العمل في تعليمات المنسّق** (`company-orchestrator.md`) — البند الذي كان يجمع "Product Manager / Business Analysis" كخطوة واحدة مدمجة أُعيد صياغته صراحةً كخطوتين منفصلتين حقيقيتين (اكتشاف باجوش + تفويض حقيقي لمينا)، لمنع أي التباس مستقبلي على مستوى وصف دورة العمل نفسها، لا فقط على مستوى قدرات باجوش الفردية.

**تم التحقق:** فحص مباشر لصحة YAML Frontmatter وترتيب العناوين المنطقي في كلا الملفين — سليم تمامًا.

**الحالة:** ✅ القرار المعماري المعلَّق من #192 محسوم ومُنفَّذ بالكامل. يسري على المحادثات الجديدة من الآن فصاعدًا (نفس القيد الموثَّق سابقًا).

---

### 194. 🆕 استيراد مشروع خارجي موجود كمشروع دائم في MKDD — مراجعة، صيانة، وتحسينات فعلية
**السياق:** المالك طلب إمكانية استيراد ريبو قديم موجود بالفعل (لم يُبنَ عبر MKDD) كمشروع دائم، ليقوم كل المراجعين (فيرينا، ميخائيل، فادي) بفحصه، مع إمكانية طلب تعديلات فعلية على الشكل والأداء لاحقًا.

**الفحص المباشر (فحص الكود الفعلي، لا افتراض):** كشف فحص `handleCreateProject` الحالي أن "مشروع جديد" في MKDD ينشئ **مجلدًا فارغًا دائمًا** — لا توجد أي آلية لربط/استيراد ريبو خارجي موجود بالفعل. كما كشف فحص تعليمات المراجعين (`code-review.md`, `security-review.md`, `qa.md`) أنها تفترض صراحةً وجود "معمارية معتمدة" و"تصميم مريم" — وهو ما لن يكون موجودًا لمشروع مستورد من خارج MKDD.

**التنفيذ الكامل (خادم + واجهة + تعليمات موظفين):**
1. **الخادم**: `POST /api/projects/import` جديد — يستقبل رابط Git واسم المشروع، وينفذ `git clone` حقيقيًا بدلًا من إنشاء مجلد فارغ. استخدام `execFile` (لا تركيب نص أمر Shell) لمنع أي خطر حقن أوامر (Command Injection) من الرابط المُدخَل من المالك، بالإضافة إلى فحص أولي لصيغة الرابط. نفس منطق التراجع (Rollback) الآمن الموجود في إنشاء المشروع العادي عند فشل أي خطوة.
2. **الواجهة**: زر ونموذج "استيراد مشروع موجود" جديدان بجانب "مشروع جديد" الحالي، بترجمة عربية وإنجليزية كاملة، ومشاركة نفس بنية النموذج (اسم + لون) مع حقل إضافي لرابط الريبو.
3. **`AGENTS.md`**: قسم مركزي جديد ("Auditing an Imported/Pre-Existing Project") يسري تلقائيًا على كل الموظفين — يوضح أن المراجعين يقيّمون الكود الفعلي مقابل أفضل ممارسات مجالهم الحقيقية (معيار الاحترافية العميقة الموجود بالفعل)، لا مقابل تصميم/معمارية معتمدة غير موجودة أصلًا لهذا النوع من المشاريع؛ وأن أي إصلاحات فعلية مطلوبة تتبع نفس معايير الجودة والقرارات الجوهرية والتوثيق المعتادة تمامًا كأي مشروع بُني عبر MKDD من الأساس.

**تم التحقق حيًا بدقة قاطعة:**
1. استيراد ريبو GitHub عام حقيقي وصغير (`octocat/Hello-World`) — تأكيد مباشر أن الملفات الفعلية (`.git`, `README`) انتُسخت بنجاح تام على القرص، لا محاكاة.
2. اختبار رابط خبيث (محاولة حقن أمر) — رُفض بأمان تام قبل الوصول لأي عملية نظام حقيقية.
3. التحقق الكامل من الواجهة: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، توازن CSS مؤكَّد.
4. فحص مباشر لصحة بنية `AGENTS.md` (18 قسمًا، ترقيم متسلسل صحيح، كل المراجع الداخلية مُصحَّحة).

**الحالة:** ✅ ميزة كاملة من طرف لطرف (خادم، واجهة، تعليمات موظفين)، مبنية على فحص دقيق للفجوة الفعلية قبل أي بناء. يسري على المحادثات الجديدة من الآن فصاعدًا.

---

### 195. 🔍 تحديث شامل لملف `README.md` الرئيسي — مواكبة كل تطورات الجلسة الطويلة
**السياق:** المالك طلب صراحة تحديث التوثيق والـREADME كأولوية — بعد جلسة تطوير طويلة شملت أكثر من 30 إصلاحًا وميزة جديدة (من #170 حتى #194)، أصبح `README.md` الرئيسي (آخر مراجعة شاملة 2026-08-30) غير محدَّث لعدد كبير من التطورات الجوهرية.

**التحديثات المُنفَّذة:**
1. **الموظفون الـ14**: توضيح إن كل موظف عنده الآن منهجية عمل حقيقية مفصَّلة مبنية على بحث فعلي (`#187-190`)، ومعيار احترافية موحَّد وشامل يُلزِمهم بمستوى محترف مخضرم حقيقي (`#186`).
2. **قسم جديد (3.4)**: توثيق ميزة "استيراد مشروع خارجي موجود" الجديدة بالكامل (`#194`).
3. **المحادثة الحية**: توثيق إصلاحات اكتمال تاريخ المحادثة — منع فقدان بيانات ناجحة عند فشل جزئي (`#177`)، ضمان اكتمال الرسائل الحقيقية عند التحميل الأولي (`#183`)، وتصنيف رسائل "الإنهاء" كرسائل حقيقية لا نشاط مخفي (`#182`).
4. **نظام المراقبة**: توثيق العداد التنازلي الحي (`#172`)، الرسائل العربية الواضحة الكاملة (`#170`)، وإخفاء رسالة الاستئناف التلقائي عن المحادثة الرئيسية مع ظهورها في تاب مخصص (`#176`).
5. **مبادئ سلامة البيانات**: إضافة مبدأ جديد صريح — عدم ادعاء إنجاز عمل ماضٍ لم يحدث فعليًا، مبني على حادثة حقيقية موثَّقة (`#192`).
6. **خريطة الملفات**: تحديث لتشمل ملفات نظام الاستئناف التلقائي الجديدة (`auto-resume.mjs`, `auto-resume-log.mjs`, `auto-resume-marker.mjs`) وتوضيح `projects.mjs` تدعم الآن الإنشاء والاستيراد معًا.
7. تحديث تاريخ "آخر مراجعة شاملة" لـ2026-08-31.

**تم التحقق:** فحص مباشر لسلامة بنية الملف بالكامل بعد كل التعديلات — 17 قسمًا، ترقيم متسق تمامًا، لا كسر في التنسيق (نما من 269 إلى 284 سطرًا).

**الحالة:** ✅ `README.md` محدَّث بالكامل ليعكس الحالة الفعلية الحقيقية للمشروع حتى نهاية هذه الجلسة الطويلة.

---

### 196. 🎯 توسيع مراقبة الاستخدام لتشمل كل الموظفين — الفحص القديم كان يراقب موظفًا واحدًا مُهيَّأ يدويًا فقط
**السياق:** المالك واجه حادثة حية — مريم توقفت فعليًا بسبب حد استخدام حقيقي أثناء شغلها على تقرير من كيرلس، لكن شاشة "صحة النظام" أظهرت "كل حاجة تمام" (8/8 نجحت)، رغم توقف مريم الفعلي.

**التشخيص المباشر (فحص الكود، لا افتراض):** كشف فحص `health-check.sh` أن "الفحص العميق" (`deep_lookup`) — الفحص الوحيد القادر على اكتشاف أخطاء حد الاستخدام واحد بواحد — مُهيَّأ عبر متغيرات بيئة واحدة فقط (`MKDD_HEALTH_CHECK_EMPLOYEE_ID`, إلخ)، مصمَّم أصلًا كـ"فحص تمثيلي" (Canary) لموظف واحد بس (كيرلس)، لا كلوحة شاملة لكل الموظفين. نظام الاستئناف التلقائي (`#175`) كان يفحص فعليًا كل المحادثات بالفعل، لكنه يتصرف بصمت (يرسل استئنافًا) دون أي عرض في شاشة صحة النظام نفسها.

**الإصلاح:** توسيع شاشة صحة النظام لتعرض **أي** موظف متوقف حاليًا بسبب حد استخدام حقيقي، لا موظف واحد مُهيَّأ فقط:
1. `Endpoint` جديد `GET /api/internal/stopped-employees` يفحص كل المحادثات (بنفس منطق الفحص الموجود بالفعل في نظام الاستئناف التلقائي)، ويرجّع قائمة بكل محادثة متوقفة حاليًا بسبب حد استخدام حقيقي، بغض النظر عن انتهاء الوقت من عدمه — لأغراض العرض، لا الاستئناف.
2. `health-check.sh`: فحص ديناميكي جديد — فحص واحد منفصل لكل موظف متوقف يُكتشَف (نفس نمط فحوصات اشتراكات LLM المتعددة الموجودة بالفعل)، برسالة عربية واضحة وبيانات `meta.resetsAt` كاملة لتفعيل العداد التنازلي الحي الموجود بالفعل تلقائيًا.
3. إعادة استخدام دالة `buildHumanReadableErrorMessage` الموجودة بالفعل (صُدِّرت من `internal-health.mjs` لهذا الغرض) — لا تكرار لمنطق تحليل رسائل الأخطاء.

**تم التحقق حيًا بدقة قاطعة:**
1. محاكاة مباشرة لسيناريو مطابق تمامًا للحادثة الحقيقية (مريم متوقفة، كيرلس شغال طبيعي) — تأكيد أن مريم فقط اكتُشِفت، وكيرلس (السليم) اتخطّى بأمان تام.
2. اختبار السكريبت الكامل من طرف لطرف — تأكيد ظهور الفحص الجديد برسالة عربية واضحة وحقل `meta.resetsAt` صحيح تمامًا في ملف الحالة النهائي.

**الحالة:** ✅ فجوة مراقبة حقيقية اكتُشفت من حادثة حية فعلية وأُصلحت بالكامل. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `bash -n` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 197. 🆕 رسالة التقرير تظهر كبادج قابل للنقر بدل فقاعة عادية
**السياق:** المالك سأل عن سبب ظهور رسالة تسليم التقرير كفقاعة محادثة عادية داخل محادثة الموظف المستلم (بعكس رسالة الاستئناف التلقائي المخفية)، ثم طلب صراحة تحويلها لبادج مضغوط بزرار، يفتح Popup يعرض محتوى التقرير الكامل عند النقر — بدل فقاعة عادية تُشوِّش تدفق المحادثة، أو إخفاء كامل يفقد إمكانية الوصول للمحتوى المفيد.

**السبب الأصلي (تأكيد مباشر من الكود):** رسالة التقرير كانت تُرسَل عبر نفس آلية الإرسال الأساسية المستخدَمة لكل الرسائل، دون أي علامة تصنيف خاصة — بعكس رسالة الاستئناف التلقائي التي طُوِّرت لها علامة إخفاء مخصَّصة (`#176`) بناءً على طلب محدَّد في حينه، لم تُطبَّق قط على أي رسالة أخرى.

**التنفيذ:**
1. علامة جديدة (`report-message-marker.mjs`) — بعكس علامة الاستئناف (إخفاء كامل)، هذه العلامة **تُبقي المحتوى الحقيقي متاحًا**، وتضيف فقط علامة تصنيف (`isReportDelivery: true`) للحدث المُطبَّع في `normalize-event.mjs`، دون حذف أي محتوى.
2. `workflow.mjs` يُغلِّف رسالة التقرير بالعلامة الجديدة قبل الإرسال.
3. الواجهة: أي رسالة تحمل `isReportDelivery: true` تُعرَض كبادج مضغوط ("📋 تقرير من موظف") بدل فقاعة كاملة؛ النقر عليها يفتح نافذة منبثقة (Popup) تعرض محتوى التقرير الكامل بتنسيق Markdown، مع زر إغلاق.

**تم التحقق حيًا بدقة قاطعة (السيناريو الحقيقي الكامل من طرف لطرف):**
1. اختبار وحدة مباشر لمنطق الكشف والتنظيف — تأكيد `isReportDelivery: true` والنص نظيف تمامًا من كل العلامات.
2. **اختبار السيناريو الحقيقي الكامل**: تسجيل تقرير فعلي حقيقي (كيرلس ← مريم) عبر `POST /api/workflow/reports`، ثم جلب أحداث محادثة مريم بنفس الطريقة التي تستخدمها الواجهة الفعلية (`GET /api/chat/events`) — تأكيد مباشر أن الرسالة المُستلَمة تحمل `isReportDelivery: true` بنص التقرير الكامل والصحيح.
3. أثناء الاختبار، اكتُشفت ومُصحِّحت أخطاء حقيقية في استخدام معاملات الـEndpoint نفسه (`action` الصحيح هو `add` لا `create`، و`gate` مطلوب بقيمة صحيحة من قائمة محدَّدة) — تم التحقق منها مباشرة من الكود بدل التخمين.

**الحالة:** ✅ ميزة كاملة من طرف لطرف، مُختبَرة بالسيناريو الحقيقي الكامل. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، توازن CSS مؤكَّد.

---

### 198. 🆕 خاصية نسخ الرسالة + ثيمان جديدان (واتساب وتيليجرام)
**السياق:** المالك طلب ميزتين واجهة جديدتين: خاصية نسخ نص أي رسالة داخل المحادثة، وثيمين جديدين للتطبيق — واحد شبيه بألوان واتساب، وواحد شبيه بألوان تيليجرام.

**خاصية نسخ الرسالة:**
- زرار نسخ صغير أسفل كل رسالة نصية (وكيل/مستخدم) — استخدام `navigator.clipboard.writeText` القياسي.
- تأكيد بصري مؤقت ("اتنسخت" لمدة ثانية ونصف) قبل الرجوع لأيقونة النسخ العادية.

**ثيمان جديدان — مبنيان على نظام الألوان الرسمي الحقيقي (لا تخمين):**
- **واتساب**: مبني على نظام الألوان الرسمي للوضع الداكن لواتساب (`#00A884` أخضر أساسي، `#0B141A` خلفية).
- **تيليجرام**: مبني على نظام الألوان الرسمي للوضع الداكن لتيليجرام (`#2AABEE` أزرق أساسي، `#17212B` خلفية).
- بُنيا على **نفس بنية متغيرات الـCSS (Tokens)** المستخدَمة بالفعل في ثيمي "داكن"/"فاتح" الموجودين — يعني كل مكوّن موجود بالفعل بيشتغل صح تلقائيًا تحت أي ثيم جديد، دون أي تعديل على مستوى المكوّنات نفسها.

**إصلاحان حقيقيان اكتُشفا وأُصلحا أثناء البناء (لا نظريان):**
1. **قاعدة تخفيف لون فقاعة "أنا"** (موجودة مسبقًا لتخفيف حدة لون Accent الأساسي كخلفية فقاعة كاملة) كانت مقيَّدة صراحةً بثيم "داكن" فقط — تم اكتشاف أن الثيمين الجديدين (كلاهما خلفيات داكنة) لن يستفيدا من هذا التخفيف، مما سيجعل فقاعة "أنا" شديدة السطوع بصريًا. وُسِّعت القاعدة لتشمل الثيمين الجديدين.
2. **سكريبت `index.html` المباشر** (يُطبَّق قبل تحميل React لمنع "ومضة الثيم الخاطئ" عند التحميل الأول) كان يدعم فقط قيمتين (`light`/`dark`) — أي مستخدم اختار "واتساب" أو "تيليجرام" كان سيرى ومضة ثيم داكن عادي للحظة قبل التحويل التلقائي للثيم الصحيح، وهي بالضبط المشكلة التي صُمِّم هذا السكريبت أصلًا لمنعها. وُسِّع السكريبت ليدعم كل الثيمات الأربعة.

**التنفيذ:** زرار الثيم الفردي (Toggle بسيط بين ثيمين) تحوَّل لقائمة اختيار منسدلة تعرض الأربعة خيارات (مع علامة صح للاختيار الحالي)، بما إن Toggle البسيط لم يعد كافيًا مع 4 خيارات.

**تم التحقق:** فحص مباشر لصحة YAML/TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ ميزتان كاملتان، مبنيتان على نظام الألوان الحقيقي المعتمد رسميًا لكل تطبيق، مع إصلاح استباقي لفجوتين حقيقيتين اكتُشفتا أثناء البناء نفسه.

---

### 199. 🎯 تعميق دقة ثيمي واتساب وتيليجرام — لون فقاعة مخصص وشكل مختلف لكل تطبيق، لا مجرد لون عام
**السياق:** المالك رفض النسخة الأولى من الثيمين (#198) بحجة قوية ودقيقة: "غيّرت اللون البنفسجي بس، المفروض كل الألوان تتغير عشان يكون تقريبًا نسخة طبق الأصل في الألوان وشكل فقاعات المحادثة وشكل الأزرار."

**البحث المباشر (لا تخمين):** بحث ويب حقيقي عن الألوان الرسمية الدقيقة لفقاعات المحادثة تحديدًا (لا لون التطبيق العام) كشف حقيقة معمارية مهمة: **كل من واتساب وتيليجرام يستخدمان لونًا مخصصًا منفصلًا تمامًا للفقاعة المرسلة عن لون الأزرار/العلامة التجارية** — واتساب: فقاعة `#005C4B` مقابل زرار/علامة تجارية `#25D366`؛ تيليجرام: فقاعة `#2B5278` مقابل زرار/علامة تجارية `#2AABEE`. نظام الـTokens السابق كان يستخدم متغيرًا واحدًا (`--mkdd-accent`) لكل شيء — هذا هو السبب الجذري الحقيقي لملاحظة المالك، لا مجرد نقص في عدد الألوان المُستبدَلة.

**الإصلاح المعماري:**
1. متغيران جديدان في كل ثيم: `--mkdd-bubble-me` (لون فقاعة الرسالة المرسلة، منفصل تمامًا عن `--mkdd-accent`) و`--mkdd-bubble-radius` (شكل زوايا الفقاعة، مختلف لكل تطبيق: واتساب `8px` زوايا أصغر وأكثر حدة، تيليجرام `18px` زوايا أكبر وأكثر استدارة، مطابقة للشكل المميز الحقيقي لكل تطبيق).
2. تبسيط معماري حقيقي: القاعدة الخاصة السابقة (تخفيف يدوي شرطي حسب اسم الثيم) أصبحت غير ضرورية بالكامل — كل ثيم يحمل قيمته الحقيقية الصحيحة مباشرة في تعريفه، والقاعدة الأساسية للفقاعة تستخدم المتغير الجديد مباشرة بلا أي شرط إضافي.
3. تأكيد أن زرار الإرسال (بلون `--mkdd-accent` الحقيقي لكل تطبيق) كان بالفعل صحيحًا معماريًا — هذا الفصل (لون Accent للأزرار، لون Bubble منفصل للفقاعة) هو بالضبط النمط الحقيقي المُكتشَف بالبحث لكلا التطبيقين.

**تم التحقق حيًا بدقة قاطعة:** فحص مباشر للقيم الفعلية النهائية في الملف بعد كل التعديلات — تأكيد مطابقة تامة: واتساب `#005c4b`/`8px`، تيليجرام `#2b5278`/`18px`، مع الحفاظ الكامل على سلوك ثيمي "داكن"/"فاتح" الأصليين دون أي تغيير.

**الحالة:** ✅ إصلاح حقيقي مبني على بحث دقيق في التفاصيل البصرية الفعلية (لا تخمين عام)، يعالج نقد المالك المباشر والمحدَّد بدقة. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، توازن CSS مؤكَّد.

---

### 200. 🎯🎯 إعادة هيكلة نظام الثيمات الكاملة — فصل "الشخصية" عن "الوضع"، ودقة قصوى بناءً على تشخيص حي متسلسل
**السياق:** المالك بعت لقطة شاشة توضح ألوانًا لا علاقة لها خالص بواتساب/تيليجرام رغم تفعيل الثيم — سلسلة تحقيق حي كاملة (فحص الملف على السيرفر، فحص محتواه الفعلي، مقارنة اللقطات) أثبتت أن الكود **كان يعمل بشكل صحيح تمامًا** (الأزرار كانت تتلوّن بدقة)، لكن الاختلاف بين "الوضع الداكن الأصلي شبه الأسود" وبين "الأخضر/الأزرق الداكن لواتساب/تيليجرام" كان طفيفًا جدًا بصريًا على شاشة موبايل ليُلاحَظ كفرق حقيقي. المالك اقترح تفسيرًا دقيقًا وصحيحًا تمامًا: بُنيت النسختان على الوضع الداكن فقط، بينما الصورة الذهنية الأشهر والأكثر تعرّفًا عليها لواتساب/تيليجرام لدى معظم الناس هي **النسخة الفاتحة الكلاسيكية**.

**القرار المعماري المتفق عليه:** بدل 4 ثيمات مسطَّحة (تخلط "الشخصية" و"الوضع" معًا في خيار واحد)، أُعيد الهيكلة بالكامل لفصل محورين مستقلين تمامًا: **الشخصية** (`mkdd` / `whatsapp` / `telegram`) × **الوضع** (`dark` / `light`) = 6 تركيبات، كل شخصية لها فعليًا نسختان كاملتان (داكنة وفاتحة). زرار منفصل للوضع (Toggle بسيط) يعمل فوق أي شخصية مختارة، بدلًا من قائمة بـ6 خيارات مسطَّحة.

**البحث والتنفيذ:**
1. ألوان واتساب الفاتح الكلاسيكية (مؤكَّدة بالبحث المباشر، وهي **العلامة البصرية الأشهر** لواتساب على الإطلاق): خلفية كريمية `#ECE5DD`، فقاعة مرسلة خضراء فاتحة أيقونية `#DCF8C6`.
2. تيليجرام الفاتح: خلفية بيضاء، فقاعة مشتقة رياضيًا من الأزرق الرسمي المؤكَّد (`#2AABEE`) عبر `color-mix` — تم الإفصاح الصريح في تعليق الكود أن القيمة الدقيقة الحقيقية لفقاعة تيليجرام الفاتحة لم تُؤكَّد بثقة كافية بالبحث، فبدل اختلاق رقم دقيق غير مؤكَّد، استُخدِم اشتقاق منطقي شفاف من قيمة مؤكَّدة فعليًا.
3. **إصلاح حاسم إضافي اكتُشِف أثناء البناء**: قاعدة CSS ثانية أقوى تحديدًا (`article.me *`) كانت تفرض لونًا أبيض ثابتًا على كل النصوص داخل فقاعة "أنا" — كانت **ستُبطِل** المتغير الجديد `--mkdd-bubble-me-text` (الضروري لضمان نص غامق مقروء فوق الفقاعة الفاتحة جدًا لواتساب الفاتح)، مما كان سيُعيد بالضبط مشكلة القراءة الموثَّقة في تعليق هذه القاعدة نفسها تاريخيًا. أُصلحت لتستخدم نفس المتغير الجديد.
4. تحديث `useTheme.ts` (تخزين منفصل لكل من الشخصية والوضع)، `index.html` (سكريبت التحميل المباشر لمنع ومضة الثيم الخاطئ)، `App.tsx`، و`AppHeader.tsx` (قائمة اختيار للشخصية + زرار منفصل للوضع) بالكامل لتعكس البنية الجديدة.

**تم التحقق حيًا بدقة قاطعة:** فحص مباشر للقيم الفعلية النهائية لكل الثيمات الستة بعد كل التعديلات — تأكيد تام: كل ثيم يحمل قيمه الصحيحة والمنطقية (شامل تصحيح لون النص الغامق التلقائي للثيمين الفاتحين جدًا: واتساب الفاتح وتيليجرام الفاتح).

**الحالة:** ✅ إعادة هيكلة معمارية كاملة، مبنية على تشخيص حي متسلسل دقيق (لا تخمين)، مع إصلاح استباقي لمشكلة قراءة كانت ستتكرر. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح، توازن CSS مؤكَّد.

---

### 201. 🎯 تقوية توهج الموظف الشغال — واكتشاف نسخة كانت بدون أي حركة نبض خالص
**السياق:** المالك طلب توهج أوضح وأقوى للموظف الشغال حاليًا.

**التشخيص المباشر (فحص الكود، لا افتراض):** كشف فحص مباشر مشكلتين حقيقيتين:
1. **الحركة الأساسية** (`mkdd-employee-card-running`) كانت بقيم شفافية ضعيفة جدًا (0.05 حتى 0.14) — توهج خافت جدًا فعليًا لا يكاد يُلاحَظ على شاشة موبايل.
2. **اكتشاف أهم**: نسخة صورة الموظف في صفحة المشروع الرئيسية (`.project-home .employee-card-running .employee-avatar` — بالتحديد السياق الذي كان المالك ينظر إليه في اللقطات السابقة) كانت **بدون أي حركة نبض خالص** — مجرد حلقة خضراء صلبة ثابتة، لا توهج متحرك من الأساس.

**الإصلاح:**
1. تقوية جوهرية للحركة الأساسية — شفافية أعلى بكثير (حتى 0.75)، ونصف قطر توهج أكبر.
2. إضافة حركة نبض حقيقية جديدة (`mkdd-employee-avatar-running`) لصورة الموظف في صفحة المشروع الرئيسية — لم تكن موجودة خالص من قبل.
3. إضافة نفس الحركة النابضة لكارت الموظف بالكامل في صفحة المشروع الرئيسية (كان عنده خلفية متدرجة ثابتة فقط، بلا أي نبض).

**تم التحقق:** فحص مباشر لتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إصلاح حقيقي مبني على فحص دقيق كشف فجوة أعمق من مجرد "زيادة شفافية" — سياق كامل كان بدون أي حركة خالص.

---

### 202. 🎯 تقوية توهج زر "بيانات الموظف" وخطوات سير المشروع
**السياق:** استكمالًا لـ#201، المالك طلب تقوية توهج زرار "بيانات الموظف" وخطوات (بوابات) سير المشروع كمان.

**التشخيص المباشر:**
1. **خطوات سير المشروع** (`workflow-step-current`): كانت تستخدم `--mkdd-accent-glow` (شفافية ضعيفة نسبيًا 18-22%)، توهج خافت.
2. **زرار "بيانات الموظف"** (`employee-details-button`): اكتُشف عدم وجود أي توهج/حركة خالص — بس حدود ملوَّنة ثابتة، حتى للحالات النشطة (شغال، بانتظار رد).

**الإصلاح:**
1. تقوية جوهرية لتوهج خطوات سير المشروع — شفافية أعلى بكثير عبر قيم مباشرة بدل الاعتماد على `--mkdd-accent-glow` الضعيفة، ونصف قطر توهج أكبر.
2. إضافة توهج نابض حقيقي جديد كليًا لزرار "بيانات الموظف" — حركتان منفصلتان لكل حالة نشطة (أخضر للشغال، برتقالي لبانتظار الرد)، لم تكن موجودة خالص من قبل.

**تم التحقق:** فحص مباشر لتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ استكمال مباشر لـ#201، بنفس نمط التشخيص الدقيق (فحص الكود الفعلي قبل أي تقوية).

---

### 203. 🎯 نقل كل أزرار الهيدر للسايد بار، وتكبير اللوجو واسم الشركة
**السياق:** المالك أشار إن الهيدر أصبح مزدحمًا (إشعارات، اختيار الثيم، الوضع الداكن/الفاتح، اللغة، كلهم كأزرار أيقونات مزدحمة بجانب الشعار)، وطلب نقل كل الأزرار للسايد بار، مع تكبير اللوجو واسم الشركة ليملأ المساحة المتاحة.

**التنفيذ:**
1. **`AppHeader.tsx`**: تبسيط جذري — أصبح يحتوي فقط زرار القائمة (Sidebar Toggle) واللوجو/اسم الشركة، بعد إزالة كل أزرار التحكم (4 عناصر: إشعارات، ثيم Style، ثيم Mode، لغة).
2. **`Sidebar.tsx`**: قسم جديد "إعدادات سريعة" يظهر أعلى قائمة التنقل مباشرة (تحت البروفايل) — يضم كل الأزرار المنقولة: تبديل الوضع الداكن/الفاتح، تبديل اللغة، اختيار شخصية الثيم (MKDD/واتساب/تيليجرام) كصف من 3 أزرار مباشرة (بدل قائمة منسدلة متداخلة داخل قائمة، بما إن السايد بار أصلًا قائمة)، وزر تفعيل/حالة الإشعارات.
3. **تكبير اللوجو واسم الشركة**: اللوجو من 30px إلى 48px، اسم الشركة من 19px إلى 27px، العنوان الفرعي من 11px إلى 13px — استغلال المساحة الفارغة الجديدة بعد إزالة الأزرار.
4. **تعديلات بنيوية مصاحبة**: شبكة الهيدر (Grid) قُلِّصت من 3 أعمدة إلى عمودين (العمود الثالث كان مخصصًا للأزرار المُزالة)؛ تحديث كل قيم `padding-top` المعتمدة على ارتفاع الهيدر عبر التطبيق (3 مواضع) لتناسب الارتفاع الجديد الأكبر، منعًا لتغطية المحتوى.

**تم التحقق:** فحص مباشر لتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إعادة تنظيم واجهة كاملة، مبنية على طلب صريح ومحدد، مع معالجة استباقية للتبعات البنيوية (ارتفاع الهيدر، الشبكة).

---

### 204. 🔍 العنوان الفرعي "Design & Development" يفضل إنجليزي ثابت دايمًا
**السياق:** المالك أبلغ عن مشكلة ظاهرها "العنوان الفرعي ما بيتترجمش" — تحقيق حي كامل (فحص الكود، فحص الملف الفعلي على السيرفر، التأكد من تطابق الكوميت) أثبت أن الكود والنشر كانا سليمين 100%، والعنوان الفرعي **كان يتغيّر فعليًا** حسب اللغة. اتضح بعد التوضيح المباشر أن الأمر لم يكن باگًا خالص، بل طلب سلوك مختلف: المالك يريد العنوان الفرعي **يبقى إنجليزيًا ثابتًا دائمًا** (زي "MKDD" نفسها بالضبط)، لا يتبع اللغة المختارة.

**الدرس المهم:** حادثة تذكيرية بأن التحقيق الحي الدقيق (مش الاستنتاج المبكر) هو الطريق الصحيح دائمًا — نفس المنهجية المتبعة طول الجلسة (فحص الكود الفعلي، فحص السيرفر، طلب توضيح مباشر عند الغموض) كشفت أن لا شيء معطوب فعليًا، ووفّرت وقتًا كان سيُهدر في "إصلاح" شيء يعمل بشكل صحيح أصلًا.

**التنفيذ:** إزالة الشرط الشرطي في `AppHeader.tsx`، والعنوان الفرعي أصبح نصًا ثابتًا "Design & Development" بالإنجليزية دائمًا، بغض النظر عن اللغة المختارة — بنفس منطق "MKDD" نفسها الثابت مسبقًا.

**تم التحقق:** `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تعديل سلوك مطلوب صراحةً بعد توضيح مباشر، لا إصلاح باگ.

---

### 205. 🎯 إصلاح تكرار رسالة "حد الاستخدام" في شاشة صحة النظام
**السياق:** المالك أبلغ عن ظهور رسالة خطأ حد الاستخدام مرتين في شاشة صحة النظام.

**التشخيص المباشر:** فحص الكود كشف السبب بدقة — الفحص الشامل الجديد (`check_stopped_employees` من `#196`) بيفحص **كل** المحادثات بدون استثناء، بما فيها المحادثة الخاصة بالموظف المُهيَّأ مسبقًا للفحص العميق القديم (`deep_lookup`، `#157`). لو الموظف المُهيَّأ (زي كيرلس) صادف كونه هو نفسه المتوقف حاليًا، تظهر نفس الرسالة **مرتين**: مرة من كل فحص. تأكيد إضافي: `deep_lookup` مش زائدة (Redundant) — بتغطي حالة إضافية حقيقية (تعليق المحادثة/Timeout بدون أي خطأ واضح) لا يغطيها الفحص الشامل الجديد، فالحل الصحيح استثناء لا حذف.

**الإصلاح:**
1. إضافة `employeeId` لنتيجة `GET /api/internal/stopped-employees` (كانت `employeeName` بس، غير كافية للمقارنة الدقيقة).
2. `health-check.sh`: استثناء المشروع/الموظف المُهيَّأ للفحص العميق من نتائج الفحص الشامل صراحةً، منعًا لظهور نفس المشكلة مرتين.

**تم التحقق حيًا بدقة قاطعة:** محاكاة السيناريو المُبلَّغ بالضبط (كيرلس هو الموظف المُهيَّأ **و** المتوقف حاليًا في نفس الوقت) — تأكيد مباشر أن العدد أصبح 1 بدلًا من 2.

---

### 206. 🆕 إضافة التاريخ والوقت الفعلي جنب العداد التنازلي + إصلاح استباقي لمشكلة موثَّقة سابقًا
**السياق:** المالك طلب إضافة التاريخ والوقت الفعلي (لا العداد التنازلي فقط) بجانب العداد — بعض حدود الاستخدام تستغرق ساعات طويلة، والعداد وحده لا يجيب "الساعة كام بالظبط" بدون حساب ذهني.

**التنفيذ:** إضافة عرض تاريخ/وقت حقيقي محلَّى (زي "الأربعاء، 7:38 ص") باستخدام `Intl.DateTimeFormat`، محسوب من نفس الطابع الزمني الحقيقي `resetsAt` الموجود بالفعل.

**إصلاح استباقي لمشكلة موثَّقة سابقًا:** أثناء البناء، تحقُّق مباشر كشف أن `ar-EG` locale بمفرده يُنتج أرقامًا هندية-عربية (`٧:٣٨`) — **نفس المشكلة الدقيقة الموثَّقة سابقًا في `#172`**. أُصلحت استباقيًا (قبل الرفع، لا بعد بلاغ لاحق) بتحديد `numberingSystem: "latn"` صراحةً — يحافظ على النص العربي (اسم اليوم) مع أرقام لاتينية عادية مقروءة.

**تم التحقق:** تأكيد مباشر لصحة الإصلاح عبر اختبار حي (`node -e`) قبل وبعد التعديل. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إصلاحان مترابطان — كلاهما مبني على تحقيق/تحقق حي مباشر، لا افتراض.

---

### 207. 🎯 اللوجو دايمًا شمال، زرار القائمة دايمًا يمين، بغض النظر عن اللغة + تكبير إضافي للوجو
**السياق:** المالك طلب إن اللوجو يفضل مكانه ثابت على الشمال دايمًا، وزرار القائمة على اليمين دايمًا، مع تكبير اللوجو أكتر.

**التشخيص المباشر:** فحص الكود كشف السبب — الهيدر (`.app-header`) كان يرث اتجاه الصفحة كله (`document.documentElement.dir`، بيتغيّر بين `rtl`/`ltr` حسب اللغة) دون أي اتجاه ثابت خاص به، فيتقلّب بصريًا مع كل تبديل لغة (اللوجو يمين في العربي، شمال في الإنجليزي، والعكس لزرار القائمة).

**الإصلاح:**
1. فرض `direction: ltr` صراحةً على الهيدر نفسه فقط — مستقل تمامًا عن اتجاه باقي الصفحة (اللي لسه بيتبع اللغة المختارة بشكل طبيعي في كل مكان تاني).
2. إعادة ترتيب عناصر الهيدر في الكود (اللوجو أولًا، زرار القائمة ثانيًا) لتطابق الترتيب البصري المطلوب مع الاتجاه الثابت الجديد.
3. تكبير إضافي للوجو (48px → 56px) ومحاذاته لبداية اليسار (بدل التوسيط) ليتماشى مع الموضع الثابت الجديد.

**تم التحقق:** فحص مباشر لتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إصلاح حقيقي مبني على فهم دقيق لآلية وراثة اتجاه الصفحة في CSS.

---

### 208. 🎯 إصلاح تغطية الهيدر لشريط اسم المشروع/زر الرجوع + تقليل ارتفاع الهيدر
**السياق:** المالك أبلغ عن تغطية الهيدر لشريط اسم المشروع وزر الرجوع (BreadcrumbBar) بعد التكبيرات المتتالية للوجو (#203، #207)، وطلب تقليل الـPadding حوالين اللوجو لتقليل الارتفاع.

**التشخيص المباشر:** كشف فحص الكود سببًا جذريًا واضحًا — موضع `.breadcrumb-bar` كان يعتمد على قيمة `top: 62px` **ثابتة يدويًا (Hardcoded)** لم تُحدَّث خالص عبر كل تكبيرات اللوجو المتتالية السابقة (من 30px إلى 48px إلى 56px) — أصبح الهيدر الفعلي أطول بكثير من الافتراض القديم، فبدأ شريط اسم المشروع يظهر قبل ما الهيدر ينتهي فعليًا.

**الإصلاح:**
1. تقليل الـPadding العمودي للهيدر (14px → 10px)، مطابقة للطلب الصريح.
2. حساب دقيق للارتفاع الفعلي الجديد للهيدر (10px + 56px لوجو + 10px + 1px حد = 77px)، وتحديث `top` الخاص بشريط اسم المشروع لهذه القيمة الدقيقة بدل التخمين اليدوي القديم.
3. تحديث كل قيم `padding-top` الثلاثة المعتمدة على ارتفاع الهيدر عبر التطبيق بمقدار التقليل الفعلي (8px).

**تم التحقق:** فحص مباشر لتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إصلاح حقيقي مبني على تحديد السبب الجذري الدقيق (قيمة ثابتة قديمة لم تُحدَّث)، لا مجرد تعديل تقريبي.

---

### 209. 🔄 تراجع عن الاتجاه الثابت للهيدر — يفضل يتقلّب مع RTL/LTR زي ما كان
**السياق:** المالك طلب صراحةً التراجع عن #207 (اللوجو شمال ثابت، زرار القائمة يمين ثابت) — الهيدر المفروض يرجع يتقلّب مع اتجاه اللغة (RTL للعربي، LTR للإنجليزي) زي أي عنصر تاني في التطبيق.

**التنفيذ:** تراجع دقيق ومحدود عن #207 فقط:
1. إزالة `direction: ltr` المفروضة صراحةً على الهيدر.
2. إرجاع ترتيب أعمدة الـGrid وترتيب العناصر في الكود للوضع الأصلي (زرار القائمة أولًا، اللوجو ثانيًا).
3. إرجاع محاذاة اللوجو والنص للتوسيط (بدل المحاذاة لليسار).

**ما تم الحفاظ عليه عمدًا (غير مرتبط بطلب التراجع):** تكبير اللوجو (56px) وتقليل الـPadding وتصحيح ارتفاع شريط اسم المشروع من #207/#208 — هذه التحسينات مستقلة تمامًا عن مسألة الاتجاه الثابت، ولا علاقة لها بطلب التراجع الحالي.

**تم التحقق:** فحص مباشر لتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تراجع دقيق ومحدود بناءً على طلب صريح، مع الحفاظ الواعي على التحسينات غير المرتبطة.

---

### 210. 🎯 فصل الهيدر لـ3 عناصر مستقلة (زرار، اسم، لوجو) — الاسم دايمًا في المنتصف بين الاتنين
**السياق:** توضيحًا لـ#209، المالك دقّق المطلوب بالتحديد: في العربي — زرار السايد بار على اليمين، اسم الشركة على الشمال، ويتعكس تمامًا في الإنجليزي — يعني الاسم يفضل دايمًا في المنتصف بين الزرار واللوجو، بترتيب 3 عناصر منفصلة تمامًا (زرار، اسم، لوجو)، لا لوجو واسم مجمّعين كوحدة واحدة (زي التصميم السابق).

**التنفيذ:**
1. إعادة بناء `AppHeader.tsx` بالكامل — فصل اللوجو عن اسم الشركة لعنصرين مستقلين تمامًا في DOM، بترتيب ثابت: زرار → اسم → لوجو.
2. تحويل `.app-header` من `display: grid` (عمودين) إلى `display: flex` مع `justify-content: space-between` — يستوعب 3 عناصر بسهولة، ويتبع اتجاه الصفحة الطبيعي (RTL/LTR) تلقائيًا بدون أي فرض خاص (بما إن #209 أزال أي اتجاه ثابت مسبقًا).
3. النتيجة الطبيعية: في العربي (RTL)، القراءة من اليمين للشمال تعطي بالضبط: زرار ← اسم ← لوجو. في الإنجليزي (LTR)، نفس الترتيب المنطقي معكوسًا بصريًا تلقائيًا.

**تم التحقق:** فحص مباشر لتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إعادة هيكلة دقيقة مبنية على توضيح مباشر ومحدد للترتيب البصري المطلوب.

---

### 211. 🎯 تعزيز قاعدة "ادعاء الاكتمال" — ربط صريح بين "الشغل خلص" والتحقق الشامل الفعلي
**السياق:** المالك وصف نمطًا متكررًا مع كيرلس — يعلن اكتمال الشغل بثقة، ولما يراجع المالك بنفسه ويشير لمشاكل حقيقية، يعترف كيرلس بصحة الملاحظة وبأنه لم ينفّذ كل المطلوب بالشكل الصحيح.

**التشخيص المباشر (فحص التعليمات الفعلية، لا افتراض):** كشف فحص مباشر لتعليمات كيرلس وقاعدة "Evidence-Based Claims" المشتركة نمطًا حقيقيًا واضحًا: القسم المخصص لديه ("Full-Scope Completion and Honest Handoff") قوي جدًا، لكنه يركّز على **الإفصاح الصادق عن نقص معروف مسبقًا** — لا يعالج النمط الفعلي المُبلَّغ (ادعاء اكتمال بناءً على تحقق ذاتي **غير كافٍ أصلًا**، لا إخفاء متعمَّد لنقص معروف). القاعدة العامة "Evidence-Based Claims" قوية أيضًا، لكنها تسرد مصطلحات محددة ("running", "fixed", "passing") دون ذكر صريح لعبارة "الشغل خلص/اكتمل" كادعاء يستحق نفس معيار التحقق الصارم.

**الإصلاح:** إضافة بند صريح جديد لقاعدة "Evidence-Based Claims" المشتركة (يسري تلقائيًا على كل الموظفين الـ14) — يوضح أن قول "الشغل خلص/اكتمل" ادعاء يتطلب نفس معيار التحقق الفعلي الصارم كأي ادعاء حالة تقنية آخر: مراجعة النطاق المعتمد كاملًا بالطريقة نفسها التي سيراجع بها المالك أو المراجع — كل شاشة، كل متطلب مذكور، كل سيناريو — لا الاكتفاء بالتأكد من وجود الكود أو نجاح الجزء المُركَّز عليه فقط. الأهم: توضيح صريح أن اكتشاف المالك لثغرات حقيقية عند مراجعته بنفسه هو **إشارة مباشرة** أن التحقق الذاتي قبل الإعلان لم يكن كافيًا — والحل الحقيقي هو تحقق أشمل في المرة القادمة، لا مجرد الاعتراف بالخطأ والانتقال.

**تم التحقق:** فحص مباشر لسلامة بنية الملف — العناوين متسلسلة وسليمة تمامًا، لا كسر في الترقيم.

**الحالة:** ✅ تعزيز حقيقي لقاعدة موجودة، يعالج فجوة سلوكية محددة اكتُشفت من نمط متكرر فعلي، يسري على كل الموظفين الـ14 من نقطة تعديل واحدة مركزية. يسري على المحادثات الجديدة من الآن فصاعدًا (نفس القيد الموثَّق سابقًا).

---

### 212. 🎯🎯 حاجز تقني حقيقي: دليل إلزامي لإغلاق أي مراجعة/بلوكر/اكتشاف
**السياق:** استكمالًا لنقاش مباشر مع المالك حول فجوات مسار الشركة — اكتشاف حي (بعد اعتراف صريح بخطأ فحص سطحي أولي) أن نقطة الادعاء الحقيقية الأخطر ليست في اعتماد البوابات نفسها (الحاجز التقني هناك قوي وموجود فعلًا: يرفض البوابات مبرمجيًا لو فيه بلوكرز مفتوحة، اكتشافات غير مُتحقَّق منها، أو مراجعات إلزامية غير مكتملة)، بل في **النقطة اللي بيوصل فيها أي عنصر لحالة "مكتمل"** أصلًا — إغلاق مراجعة، حل بلوكر، تصحيح اكتشاف، أو التحقق منه — وكلها كانت **بلا أي متطلب دليل خالص**، مجرد اسم موظف يكفي لتغيير الحالة فورًا.

**نطاق الفجوة (فحص شامل لكل نقاط الادعاء، لا نقطة واحدة):** أربع نقاط بالضبط تحمل نفس النمط: إكمال مراجعة (`reviews`)، حل بلوكر (`blockers`)، تصحيح اكتشاف (`findings: mark-fixed`)، والتحقق من الإصلاح (`findings: verify`).

**الإصلاح (حاجز تقني حقيقي، لا قاعدة سلوكية):**
1. حقل `evidence` إلزامي (حد أدنى ~50 حرفًا) لكل الأربع نقاط — أي محاولة تغيير حالة بدونه تُرفَض تقنيًا فورًا برمز خطأ `evidence_required` (HTTP 409)، لا تُقبَل بثقة.
2. الدليل يُخزَّن بشكل دائم مع السجل نفسه (`evidence` للمراجعات والبلوكرات؛ `fixEvidence`/`verifyEvidence` منفصلين للاكتشافات، بما إنهما حدثان مختلفان تمامًا يستحقان دليلين منفصلين) — سجل تاريخي دائم يمكن الرجوع له لاحقًا عند الشك في أي عنصر.
3. مسح الدليل تلقائيًا عند إعادة فتح مراجعة (`reopen`)، بما إنه لم يعد صالحًا للحالة الجديدة.
4. تحديث كل الأمثلة العملية (Curl) في `AGENTS.md` المشتركة لتعكس الحقل الجديد الإلزامي، مع توضيح صريح أن طول النص وحده لا يكفي — المطلوب دليل حقيقي، لا حشو نصي لتجاوز الفحص.

**تم التحقق حيًا بدقة قاطعة (لكل الأربع نقاط):**
1. محاولة إكمال مراجعة بدون دليل → رُفضت (409، `evidence_required`).
2. محاولة بدليل قصير جدًا ("tested it") → رُفضت (409).
3. محاولة بدليل حقيقي كافٍ → نجحت (200)، والدليل الكامل مُخزَّن فعليًا في السجل النهائي.
4. حل بلوكر بدون دليل → رُفض (409).
5. تصحيح اكتشاف بدون دليل → رُفض (409).

**الحالة:** ✅ حاجز تقني حقيقي مُختبَر حيًا على كل نقطة، يمنع "الضغط السريع على زر الإغلاق" بدون توثيق حقيقي عبر مسار سير العمل بأكمله. التحقق النهائي الكامل: `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة.

---

### 213: 🆕 عرض الدليل الحقيقي في الواجهة — الحاجز التقني (#212) بلا رؤية فعلية كان بلا فائدة عملية
**السياق:** المالك سأل سؤالًا مباشرًا وحاسمًا بعد #212: "طيب ده أنا هلاحظ شغله إزاي في الـMKDD-UI؟" — سؤال كشف فجوة حقيقية فورية.

**التشخيص المباشر:** فحص مباشر لصفحة المشروع الرئيسية كشف أن الدليل الجديد (المُخزَّن فعليًا في قاعدة البيانات منذ #212) **لم يكن ظاهرًا في أي مكان بالواجهة خالص** — بطاقة "المراجعات الإلزامية" كانت تعرض الحالة فقط (مكتمل/معلَّق)، وبطاقتا العوائق والملاحظات كانتا تعرضان العناصر المفتوحة فقط، بلا أي وصول للدليل حتى بعد الحل. حاجز تقني قوي بلا أي رؤية فعلية له عديم الفائدة عمليًا للمالك.

**الإصلاح:**
1. المراجعات المكتملة أصبحت عناصر قابلة للنقر — تفتح نافذة منبثقة بالدليل الكامل ومن راجعها.
2. زر جديد "شوف العوائق المُحلَّة" و"شوف الملاحظات المُتحقَّق منها" أسفل كل بطاقة — يفتح نفس النافذة بكل الأدلة (دليل حل كل عائق؛ دليل الإصلاح ودليل التحقق المنفصلين لكل ملاحظة).
3. تحديث أنواع TypeScript (`WorkflowState`) لتعكس حقول الدليل الجديدة من `#212`.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. تدفق البيانات مؤكَّد من الطرفين (الـBackend يخزّن الدليل فعليًا كما اختُبِر حيًا في #212؛ الفرونت إند يقرأ نفس البنية مباشرة). `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إكمال ضروري ومباشر لـ#212 — حاجز تقني بلا رؤية فعلية للمالك يفقد جزءًا كبيرًا من قيمته العملية.

---

### 214. 🎯 قالب موحَّد وثابت لعرض بوابات 1-3 — يسهّل المراجعة الشخصية للمالك بلا تكلفة إضافية
**السياق:** المالك سأل مباشرة "هل ده هيخلي المنظومة أفضل ولا تعب بدون فايدة؟" ردًا على اقتراح مزدوج (مراجعة أقران + ملخص منظَّم) لسد فجوة عدم وجود أي مراجعة لشغل باجوش/مينا/مريم/شنودة (المرحلة الأولى) غير مراجعة المالك الشخصية عند كل بوابة.

**التقييم الصريح المُقدَّم للمالك:** تحليل صادق ميّز بوضوح بين الجزأين المقترحين — "الملخص المنظَّم" قيمة حقيقية مؤكَّدة بلا أي تكلفة إضافية حقيقية (تحسين شكل عرض موجود بالفعل). أما "مراجعة الأقران" ففيها شك حقيقي: بعكس فيرينا/ميخائيل اللي بيراجعوا حقائق تقنية موضوعية (ثغرة موجودة أو لأ)، شغل باجوش/مريم/شنودة أحكام تجارية وذوقية تعكس رؤية المالك الشخصية — مراجعة زميل مالوش وصول لهذه الرؤية قد تتحول لـ"توقيع شكلي" (Checkbox Theater، نفس التحذير الموثَّق في `#187`)، مع تكلفة حقيقية في الوقت والتوكنز. **القرار المشترك**: تنفيذ الملخص المنظَّم فقط الآن، وتأجيل قرار مراجعة الأقران.

**التنفيذ:** توحيد شكل عرض البوابات الثلاث (Requirements، UI/UX، Architecture) على **قالب ثابت وموحَّد بالضبط**: القرارات الرئيسية، المفاضلات، المخاطر، الأسئلة المعلَّقة، ما بعد الموافقة — مع إلزام صريح بذكر "لا توجد" صراحةً لأي بند فارغ فعليًا (بدل حذفه بصمت)، لضمان معرفة المالك أن كل بند فُكِّر فيه فعليًا لا أُغفِل. باجوش كان عنده بالفعل تعداد جزئي غير موحَّد (7 نقاط)؛ مريم وشنودة كان عندهم جملة عامة واحدة بس بلا أي بنية صريحة خالص — الثلاثة دلوقتي على نفس القالب الحرفي بالضبط.

**تم التحقق:** فحص مباشر لصحة YAML وعدد الأقسام لكل الملفات الثلاثة — سليم تمامًا.

**الحالة:** ✅ تحسين حقيقي بقيمة مؤكَّدة وتكلفة صفرية إضافية، مبني على تقييم صادق وصريح رفض التوسّع في حل أوسع (مراجعة الأقران) دون تبرير كافٍ لتكلفته. يسري على المحادثات الجديدة من الآن فصاعدًا.

---

### 215. 🎯 قالب CI عام تمامًا لكل مشروع جديد — بلا أي اسم ريبو/مستخدم ثابت خالص
**السياق:** استكمالًا لنقاش فجوات مسار الشركة، المالك وافق على البدء بـCI/CD (الأولوية الأولى، بما إنها بلا تكلفة توكنز إضافية حقيقية، وممكن تقلّل التكلفة الحالية فعليًا)، مع شرط تقني حاسم صريح: **لا يوجد أي اتصال GitHub خاص بـMKDD نفسها خالص** — كل الاتصال الفعلي بيحصل حصريًا عبر أدوات OpenHands نفسها (GitHub MCP) اللي بيستخدمها الموظف مباشرة، فالحل يجب ألا يحتوي على أي اسم مستخدَم/ريبو/رابط مكتوب بشكل ثابت خالص.

**التنفيذ:**
1. خطوة جديدة (5) في تعليمات إنشاء الريبو الموجودة بالفعل عند باجوش (`Repository Creation`) — تُضاف مباشرة بعد إنشاء الريبو، باستخدام **نفس أدوات GitHub MCP** المستخدَمة أصلًا لإنشاء الريبو نفسه، لا آلية اتصال منفصلة جديدة.
2. قالب `GitHub Actions` عام تمامًا (`​.github/workflows/ci.yml`) — يعتمد بالكامل على اكتشاف ديناميكي لمحتوى المشروع نفسه (فحص وجود `package.json`، ووجود سكريبتات `build`/`test`/`lint` بداخله فعليًا)، بدل الاعتماد على أي افتراض ثابت مسبق عن نوع المشروع. أي خطوة غير منطبقة (زي عدم وجود اختبارات) تُتخطَّى بأمان بدل فشل الكامل.
3. GitHub Actions مصمَّمة أصلًا تشتغل بسياق الريبو اللي هي جواه تلقائيًا (`${{ github.* }}` Context) — فمفيش أي حاجة لكتابة أي اسم صريح خالص من الأساس، وهذا ما يضمن أن نفس القالب الحرفي يعمل بشكل صحيح لأي ريبو جديد يُنشَأ مستقبلًا بلا أي تعديل.

**تم التحقق حيًا بدقة قاطعة:**
1. فحص مباشر لصحة نحو القالب نفسه (كملف YAML حقيقي منفصل) — 7 خطوات، Job واحد، صحيح تمامًا.
2. بحث شامل مباشر في الملف بأكمله عن أي إشارة لاسم مستخدَم/ريبو حقيقي (`menokemo`, `openhands-mkdd`, `xeye-website`, أي رابط GitHub مباشر) — نتيجة صفرية تمامًا، تأكيد قاطع أن القالب عام 100%.

**الحالة:** ✅ حل تقني حقيقي يحترم الشرط الصريح للمالك (لا اتصال GitHub خاص بـMKDD، الاعتماد الحصري على أدوات OpenHands MCP الموجودة بالفعل). يسري على أي مشروع جديد من المحادثات الجديدة فصاعدًا.

---

### 216. 🎯 تنبيه تجاوز الميزانية لكل مشروع (Backend كامل) — ميزانية مخصَّصة لكل مشروع، لا رقم عام ثابت
**السياق:** استكمالًا لنقاش فجوات مسار الشركة، المالك رفض صراحةً رقمًا عامًا ثابتًا لكل المشاريع ("كل مشروع على حسب حجمه وإمكانياته")، واختار حل "ميزانية تحددينها إنتِ لكل مشروع على حدة".

**التنفيذ (Backend):**
1. `project-metadata.mjs`: `getProjectBudget`/`setProjectBudget`/`listProjectsWithBudget` — تخزين اختياري بالكامل لكل مشروع، `null` يعني "لا ميزانية محددة، لا تنبيه خالص" — لا صفر صامت.
2. `project-cost.mjs`: تحديث `GET /total-cost` ليرجّع الميزانية مع التكلفة الحقيقية معًا؛ `POST /budget` لتعيين/مسح الميزانية؛ `GET /api/internal/budget-status` جديد يفحص كل المشاريع ذات الميزانية دفعة واحدة.
3. `health-check.sh`: فحص دوري جديد — تنبيه عند 80% (تحذير) و100%+ (تجاوز).

**تم التحقق حيًا بدقة قاطعة (في الجلسة السابقة):** تعيين ميزانية حقيقية، رفض قيمة سالبة، مسح الميزانية، وسيناريو كامل من طرف لطرف (تكلفة حقيقية $42.50 من ميزانية $50 = 85%) — الفحص الدوري طلع تحذيرًا دقيقًا بالضبط.

**⚠️ حادثة تعلُّم مهمة موثَّقة بصراحة:** أثناء البناء الأول، حدث خطأ حقيقي — دالة `handleSetProjectBudget` اتحذفت بالكامل بالخطأ أثناء إضافة دالة جديدة بجانبها (استبدال كامل بدل إضافة)، مما سبب فشل تشغيل الخادم بالكامل (`SyntaxError`). اكتُشف وأُصلح فورًا عبر فحص مباشر للتصدير (`export`) قبل الإعلان عن النجاح.

**⚠️ حادثة فقدان عمل موثَّقة بصراحة أكبر:** الجلسة انتهت بعد الانتهاء من الـBackend مباشرة **دون رفعه لـGitHub خالص** — كل شغل الـBackend ضاع بالكامل، واحتاج إعادة بناء كامل من الصفر في الجلسة التالية. **الدرس المستخلص**: رفع أي إنجاز مكتمل فورًا، لا الانتظار لإكمال ميزة كاملة (Backend+Frontend) قبل أول Commit.

**الحالة:** ⏳ الـBackend كامل ومُختبَر بدقة، مُعاد بناؤه بعد فقدانه، ويُرفَع الآن فورًا. **الواجهة المرئية (زرار/نافذة تحديد الميزانية، شريط تقدّم) لسه قيد الإنجاز.**

---

### 217. ✅ إكمال واجهة تنبيه الميزانية — الميزة كاملة من طرف لطرف
**السياق:** استكمالًا مباشرًا لـ#216 (Backend كامل ومُرفَع)، بناء الواجهة المرئية المتبقية.

**التنفيذ:**
1. زرار "تحديد ميزانية" / "تعديل الميزانية" داخل كارت "التكلفة الإجمالية" الموجود بالفعل في صفحة المشروع الرئيسية.
2. شريط تقدّم بصري يظهر تلقائيًا فقط عند وجود ميزانية محددة فعليًا — يتلوّن أخضر عادةً، وأحمر تلقائيًا عند وصول/تجاوز الميزانية.
3. نافذة تعيين/تعديل/مسح الميزانية — تحقق من صحة الإدخال (رقم أكبر من صفر)، مع رسائل خطأ واضحة، وزر "مسح الميزانية" منفصل يظهر فقط لو كانت هناك ميزانية بالفعل.

**⚠️ درس تطبيقي مباشر لحادثة #216 (لا تكرار للخطأ):** تم الرفع الفوري لهذا الجزء بمجرد اكتماله والتحقق منه، دون انتظار أي عمل إضافي — تطبيقًا مباشرًا للدرس الموثَّق صراحةً في #216.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. تأكيد إضافي: اسم ملفات البناء النهائية (`index-BIosNvoh.css`, `index-CM9YE83L.js`) مطابق حرفيًا لما كان موجودًا قبل حادثة فقدان العمل، مؤكِّدًا دقة إعادة البناء 100%. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ ميزة "تنبيه تجاوز الميزانية لكل مشروع" (#216-#217) مكتملة بالكامل من طرف لطرف — Backend وFrontend معًا، مُرفَعة ومُختبَرة بدقة.

---

### 218. 🎯 عرض تاريخ المحادثة الكامل (كل المحادثات القديمة) داخل MKDD-UI — بلا الحاجة للرجوع لـOpenHands (Backend)
**السياق:** المالك طلب إمكانية رؤية تاريخ المحادثة الكامل مع أي موظف من بداية المشروع (بما فيها المحادثات القديمة اللي اتبعتت عليها "محادثة جديدة")، بدون الحاجة للرجوع لـOpenHands نفسها. بعد نقاش التصميم: تحميل تدريجي عند التمرير لأعلى (زي واتساب/تيليجرام)، مع فاصل بصري "بداية محادثة جديدة" عند كل انتقال، وفاصل تاريخ يومي.

**التنفيذ (Backend):**
1. `findAllEmployeeConversations` جديدة في `authorize-conversation.mjs` — ترجّع **كل** محادثات موظف/مشروع معين (لا محادثة واحدة فقط)، مرتَّبة زمنيًا من الأقدم للأحدث — بنفس منطق الفحص الشامل الموجود بالفعل في حساب التكلفة الإجمالية (`#65`).
2. `GET /api/chat/older-conversation` جديد — عند وصول المالك لأعلى تاريخ المحادثة الحالية (`hasMore: false`)، هذا الـEndpoint يلاقي المحادثة السابقة تلقائيًا (لو موجودة) ويحمّل أحدث أحداثها — بإعادة استخدام كاملة لمنطق `fetchRecentEvents` الموجود بالفعل، لا منطق جديد منفصل. يرجّع `olderConversationId: null` بوضوح عند الوصول لأول محادثة حقيقية على الإطلاق.

**تم التحقق حيًا بدقة قاطعة:** محاكاة سيناريو حقيقي (موظف عنده محادثتان، قديمة وحديثة) — تأكيد مباشر: طلب المحادثة السابقة من الحديثة يلاقي القديمة بدقة مع محتواها الحقيقي؛ طلب المحادثة السابقة من القديمة (الأقدم فعليًا) يرجّع `null` بشكل صحيح تمامًا.

**تم التحقق:** `npm test` → 82/82، `tsc -b` نظيف، `oxlint` 0 أخطاء جديدة.

**الحالة:** ⏳ Backend كامل ومُختبَر بدقة، يُرفَع فورًا الآن (تطبيقًا مباشرًا للدرس الموثَّق في `#216` — لا انتظار لإكمال الفرونت إند قبل أول رفع). **الفرونت إند (منطق التمرير، بادجات الفاصل الزمني وحدود المحادثة، الحفاظ على موضع السكرول) لسه قيد الإنجاز.**

---

### 219. ✅ إكمال ميزة عرض تاريخ المحادثة الكامل — الميزة كاملة من طرف لطرف
**السياق:** استكمالًا مباشرًا لـ#218 (Backend كامل ومُرفَع)، بناء الفرونت إند الكامل.

**التنفيذ:**
1. **`ChatMessage`**: حقل `conversationId` جديد يوضح أصل كل رسالة، لحساب فاصل "بداية محادثة جديدة" بدقة.
2. **`useConversation.ts`**: إعادة هيكلة `loadOlderMessages` بالكامل — بدل التوقف عند نهاية تاريخ المحادثة الحالية، ينتقل تلقائيًا وبسلاسة للمحادثة السابقة (لو موجودة) عبر Endpoint #218، مع الحفاظ الدقيق على الفصل بين `conversationId` (المحادثة النشطة الحقيقية لإرسال الرسائل) و`oldestLoadedConversationId` (المحادثة اللي بيتم التمرير لها للخلف حاليًا) — منعًا لأي تعارض بين استعراض التاريخ والمحادثة الفعلية.
3. **`ChatScreen.tsx`**: فاصلا تيليجرام (تاريخ يومي + بداية محادثة جديدة) يُحسَبان تلقائيًا بمقارنة كل رسالة بالرسالة السابقة لها مباشرة، ويُعرَضان كبادجات مركزية أنيقة.

**⚠️ إصلاح استباقي لمشكلة موثَّقة سابقًا:** أثناء بناء دالة تنسيق التاريخ، تحقق مباشر حي أكّد نفس مشكلة الأرقام الهندية-العربية الموثَّقة سابقًا (`#172`/`#206`) — أُصلحت استباقيًا من البداية بتحديد `numberingSystem: "latn"` صراحةً.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل، وتحقق حي منفصل لصحة دالة تنسيق التاريخ (أرقام لاتينية صحيحة في العربي والإنجليزي). `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ ميزة "عرض تاريخ المحادثة الكامل من داخل MKDD-UI" (#218-#219) مكتملة بالكامل من طرف لطرف — Backend وFrontend معًا، مُرفَعة فورًا ومُختبَرة بدقة.

---

### 220. 🎯 زرار "الرجوع لآخر رسالة" + إعادة تصميم هيدر المحادثة (صف واحد مضغوط زي تيليجرام)
**السياق:** المالك طلب زرار "الرجوع لآخر رسالة" عند التمرير لأعلى (زي واتساب/تيليجرام)، وهيدر المحادثة بشكل تيليجرام.

**اكتشاف مشكلة إضافية حقيقية أثناء البناء:** فحص منطق التمرير الحالي كشف عن باگ حقيقي منفصل — الشاشة كانت تقفز تلقائيًا لآخر رسالة **في كل مرة** يتغيّر فيها عدد الرسائل، حتى أثناء قراءة تاريخ قديم، مما كان سيكسر تجربة القراءة فورًا لو وصلت رسالة جديدة من الموظف.

**التنفيذ:**
1. **زرار الرجوع لآخر رسالة**: حالة جديدة `isNearBottom` تُحسَب من المسافة الفعلية للتمرير عن الأسفل، تتحكم في ظهور الزرار العائم.
2. **إصلاح القفز التلقائي**: القفز التلقائي لآخر رسالة الآن **مشروط** بكون المالك قريبًا من الأسفل أصلًا — إرسال رسالة جديدة بواسطة المالك نفسه يفرض `isNearBottom = true` صراحةً، فيقفز دائمًا كالمتوقع.
3. **هيدر المحادثة**: أُعيد هيكلته بالكامل من (بادج دور منفصل فوق + صف تحته) إلى **صف واحد مضغوط** — الاسم والدور مدمجان معًا (الدور يظهر كسطر حالة صغير تحت الاسم مباشرة)، مطابقًا لنمط تيليجرام الحقيقي.
4. **الفوتر**: فحص مباشر أكّد أن الشكل الحالي (حقل إدخال دائري الشكل، زرار إرسال دائري بلون Accent) قريب جدًا من شكل تيليجرام أصلًا — لا حاجة لتعديل جوهري.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ ميزتان كاملتان من طرف لطرف، مع إصلاح استباقي لباگ حقيقي اكتُشف أثناء البناء نفسه.

---

### 221. 🎯 تبسيط هيدر المحادثة لـ3 عناصر فقط — الأفاتار تفتح البروفايل الكامل
**السياق:** استكمالًا لـ#220، المالك طلب هيدر المحادثة يحتوي فقط على اسم الموظف وصورته وزر الرجوع — والنقر على الصورة يفتح البروفايل الكامل بكل المعلومات الباقية.

**التنفيذ:**
1. **الهيدر**: أصبح 3 عناصر فقط — زر رجوع، صورة (أصبحت زرارًا قابلًا للنقر)، اسم الموظف. الدور وزر "محادثة جديدة" أُزيلا بالكامل من الهيدر.
2. **`EmployeeInsightsPanel`**: قسم "بروفايل الموظف" جديد أُضيف في أعلى النافذة — صورة كبيرة، الاسم، الدور، وزر "محادثة جديدة" (منقول بالكامل من الهيدر، بنفس منطق التحقق والتأكيد الأصلي دون أي تغيير سلوكي).
3. **دعم التحكم الخارجي**: النافذة كانت مُتحكَّمًا في فتحها/إغلاقها داخليًا فقط (زرار خاص بها) — أُضيف دعم اختياري (`openOverride`/`onOpenChange`) يسمح بفتحها من الخارج (بالنقر على الصورة في الهيدر) مع الحفاظ الكامل على التوافق العكسي لو استُخدِم المكوّن في مكان آخر دون هذه الخاصية.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا، شامل إصلاح خطأين حقيقيين اكتُشفا أثناء البناء (توافق أنواع `null`/`undefined`، واستيراد غير مُستخدَم). `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ ميزة كاملة من طرف لطرف، مبنية على إعادة استخدام كاملة للمكوّن الموجود بالفعل بدل بناء نافذة منفصلة جديدة.

---

### 222. 🎯 توهج حدود صورة الموظف — نفس نمط توهج "بيانات الموظف" (#202) بعد نقله للأفاتار
**السياق:** استكمالًا لـ#221 (الأفاتار أصبحت زر فتح البروفايل بدل زرار "بيانات الموظف" القديم)، المالك طلب أن يحمل حد الصورة نفس توهج الحالة النابض الذي كان على الزرار القديم.

**التنفيذ:**
1. استخراج `statusColorClass` لملف مشترك (`utils/employeeStatusColor.ts`) بدل تكرارها — تُستخدَم الآن من مصدر واحد في كل من الأفاتار في الهيدر ونافذة البروفايل، ضمانًا لاتساق الألوان دائمًا.
2. إعادة توجيه حركتي التوهج النابض الموجودتين بالفعل (`#202`، أخضر للشغال، برتقالي لبانتظار الرد) من الزرار القديم المُزال إلى الأفاتار نفسها.
3. **تنظيف إضافي**: حذف الأنماط الأساسية للزرار القديم (`.employee-details-button`) نفسها، بما إنها أصبحت كودًا ميتًا تمامًا بعد إزالة الزرار في `#221` — مع الحفاظ الكامل على أصناف الألوان المشتركة (`.status-color-*`) بما إنها لا تزال مستخدَمة فعليًا في نافذة البروفايل.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ ميزة كاملة، مع تنظيف حقيقي لكود ميت اكتُشف أثناء البناء نفسه.

---

### 223. 🎯 "الحالة" تحت الاسم في الهيدر — بالظبط زي online/typing في تيليجرام
**السياق:** المالك بحث عن صورة حديثة حقيقية لهيدر تيليجرام (عبر بحث صور مباشر بواسطتي بما إن الرابط المُرسَل كان صفحة نتائج بحث لا صورة مباشرة)، وطلب تحت اسم الموظف تظهر "الحالة" الفعلية (زي online/typing في تيليجرام)، بدل الدور اللي نُقِل بالفعل للبروفايل في `#221`.

**التنفيذ:**
1. استخراج `getStatusText` (نص الحالة المقروء) من `EmployeeInsightsPanel.tsx` لنفس الملف المشترك `utils/employeeStatusColor.ts` — مصدر واحد للون والنص معًا.
2. سطر حالة جديد تحت الاسم في هيدر المحادثة — يعرض النص الحقيقي (زي "يعمل الآن"، "بانتظار موافقتك") بلون الحالة الديناميكي نفسه المستخدَم أصلًا في توهج الأفاتار (`#222`) ونافذة البروفايل.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل، وتأكيد وجود كل أصناف الألوان اللازمة (`idle`, `finished`) في الملف. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ ميزة كاملة، مبنية على إعادة استخدام كاملة للمنطق المشترك (لون ونص الحالة) عبر ثلاثة أماكن (الأفاتار، الهيدر، البروفايل) من مصدر واحد.

---

### 224. 🎯 إعادة ترتيب هيدر المحادثة — الاسم والحالة في المنتصف بين زر الرجوع والصورة
**السياق:** المالك وضّح بدقة (بعد توضيح مباشر متسلسل، بما إن الرابط المُرسَل كان لصورة مقال عن ميزة "Welcome Message" في تيليجرام غير ذات صلة، وأداة عرض الصور غير مدعومة تقنيًا) الترتيب البصري الدقيق المطلوب: الاسم والحالة يظهران في **منتصف** الهيدر، بين زر الرجوع والصورة — لا بجانب الصورة مباشرة كما كان الوضع سابقًا.

**التنفيذ:** إعادة ترتيب عناصر DOM في الهيدر — `chat-employee-info` (الاسم + الحالة) انتقل ليأتي مباشرة بعد زر الرجوع، والصورة (الأفاتار) انتقلت للطرف الآخر. مع إضافة `align-items: center` و`text-align: center` لضمان تمركز النص فعليًا داخل المساحة المتاحة بين الطرفين، لا مجرد أخذ المساحة المتبقية.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تعديل تخطيطي دقيق ومحدود، مبني على توضيح مباشر تسلسلي وصل لوصف بصري دقيق قابل للتنفيذ.

---

### 225. 🎯 بادج بيضاوي للاسم/الحالة + تمركز حقيقي في الهيدر (بناءً على صورة مرجعية فعلية)
**السياق:** المالك أشار لصورة مرجعية حقيقية موضوعة مسبقًا في ملفات المشروع (Project Knowledge) — لقطة شاشة حقيقية لهيدر مجموعة تيليجرام توضح بدقة الشكل البصري المطلوب: الاسم والحالة داخل **بادج بيضاوي الشكل بخلفية مميزة**، لا نص طليق عائم.

**التنفيذ:**
1. **البادج**: خلفية شفافة خفيفة وحدود دائرية كاملة (`border-radius: 999px`) للاسم والحالة معًا، مطابقًا للشكل في الصورة المرجعية.
2. **تمركز حقيقي دقيق**: اكتُشف أن التمركز السابق (`#224`، مبني على Flexbox بسيط) كان تقريبيًا فقط — بما إن البادج أصبح الآن بحجم محتواه الطبيعي (لا يأخذ كل المساحة المتبقية)، يحتاج تمركزًا دقيقًا حقيقيًا. حُلَّت جذريًا بتحويل صف الهيدر لـCSS Grid بثلاثة أعمدة (`1fr auto 1fr`) — العمودان الجانبيان متساويا العرض بالضبط، فالعمود الأوسط (البادج) يتمركز حسابيًا في المنتصف الحقيقي، بغض النظر عن اختلاف حجم زر الرجوع عن حجم الصورة.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إصلاح تصميمي دقيق مبني على صورة مرجعية حقيقية، مع حل جذري لمشكلة تمركز اكتُشفت أثناء البناء (لا تقريب بصري بسيط).

---

### 226. 🎯 تكبير بادج الاسم + إضافة الوظيفة إليه
**السياق:** المالك طلب تكبير بادج الاسم في هيدر المحادثة، وإضافة الوظيفة داخله.

**التنفيذ:**
1. الوظيفة أُضيفت مدموجة مع الحالة في نفس السطر الثاني بفاصل نقطي (زي "مدير المنتج · شغال الآن") — يحافظ على البادج بسطرين فقط بدل ثلاثة، ويحافظ على تناسق الشكل البيضاوي.
2. تكبير عام للبادج: مساحة داخلية أكبر (`padding`)، حجم خط الاسم أكبر (14px → 16px)، وحجم خط سطر الوظيفة/الحالة أكبر (11px → 12px).

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تعديل تصميمي مباشر ومحدود.

---

### 227. 🎯 إصلاحات متعددة على بادج الهيدر: الشفافية، أيقونة الوظيفة، وضوح الوظيفة، بادج الوظيفة في البروفايل
**السياق:** المالك أشار إلى أربع فجوات حقيقية بعد `#226`: (1) الشفافية المتوقَّعة للبادج غير موجودة فعليًا، (2) أيقونة الوظيفة أُزيلت سابقًا في `#221` عند نقل الوظيفة للبروفايل، (3) الوظيفة مدموجة بشكل خافت مع الحالة بدل الظهور بوضوح تحت الاسم مباشرة، (4) الوظيفة في نافذة البروفايل نص عادي بأيقونة عامة، لا بادج واضح بأيقونة الوظيفة الحقيقية.

**التنفيذ:**
1. **الشفافية**: استبدال الخلفية الصلبة (`var(--mkdd-surface-hover)`) بخلفية شبه شفافة حقيقية (`color-mix` مع الشفافية) مع `backdrop-filter: blur(10px)` — تأثير زجاجي حقيقي مطابق للصورة المرجعية، لا خلفية صلبة.
2. **فصل الوظيفة عن الحالة**: البادج أصبح 3 أسطر واضحة — الاسم، ثم الوظيفة (بأيقونتها الحقيقية من `ROLE_ICONS` نفس مصدر أيقونة البروفايل)، ثم الحالة، كل سطر مستقل وواضح تمامًا.
3. **بادج الوظيفة في البروفايل**: تحويل الشكل من نص بأيقونة عامة (`FaCircleInfo`) لبادج مستقل واضح (خلفية، حدود، حواف دائرية كاملة) بأيقونة الوظيفة الحقيقية للموظف — نفس المصدر المشترك المستخدَم في الهيدر، ضمانًا للاتساق الكامل.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل، شامل التأكد من عدم وجود أي استيراد غير مُستخدَم بعد التعديل. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ أربعة إصلاحات دقيقة ومحدودة، مبنية على ملاحظات مباشرة ومحددة.

---

### 228. 🎯 توسيع بادج الاسم/الوظيفة/الحالة ليملأ كل المساحة بين الزر والصورة
**السياق:** المالك أشار إن البادج (بعد إضافة 3 أسطر في `#227`) بقى ضيقًا نسبيًا ("مبعجر") — محتاج يملأ كل العرض المتاح تقريبًا بين زر الرجوع والصورة، لا مجرد حجم محتواه الطبيعي.

**التنفيذ:** تعديل شبكة الهيدر من `1fr auto 1fr` (العمود الأوسط بحجم محتواه فقط) إلى `auto 1fr auto` (العمودان الجانبيان بحجم زر الرجوع والصورة فقط، والعمود الأوسط يأخذ كل المساحة المتبقية). البادج نفسه عُدِّل ليملأ عرض عموده بالكامل (`width: 100%` بدل الاعتماد على خاصية Flexbox قديمة لم تعد فعّالة داخل حاوية Grid).

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تعديل تخطيطي دقيق ومحدود.

---

### 229. 🎯 تقليل بادج الهيدر لسطرين، الوظيفة كبادج صغير على جنب، تكبير الصورة
**السياق:** المالك أشار إن 3 أسطر في البادج كتيرة وشكل الهيدر لسه غير مريح — طلب سطرين فقط، مع الوظيفة والحالة على نفس السطر لكن كل واحدة على جنب منفصل، الوظيفة نفسها في بادج صغير واضح، وتكبير صورة الموظف.

**التنفيذ:**
1. **تقليل لسطرين**: صف واحد جديد (`chat-employee-info-row`) يجمع الوظيفة والحالة معًا بـ`justify-content: space-between` — كل واحدة على طرف منفصل من نفس السطر، لا مدموجتان معًا ولا كل واحدة سطر مستقل.
2. **بادج صغير للوظيفة**: تحويل من نص بأيقونة فقط لبادج مصغَّر حقيقي (خلفية، حدود، حواف دائرية كاملة) — أوضح بصريًا كعنصر مستقل.
3. **تكبير الصورة**: من 42px إلى 56px.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تعديل تصميمي دقيق ومحدود، مبني على ملاحظات مباشرة متتالية.

---

### 230. 🎯 هيدر وفوتر المحادثة كبطاقات زجاجية عائمة (شفافية، Blur، حواف دائرية)
**السياق:** المالك طلب هيدر وفوتر المحادثة يبدوان "عائمين" فوق الرسائل — شفافية حقيقية مع Blur، وحواف دائرية زي البادج.

**التنفيذ:**
1. **الهيدر**: خلفية شبه شفافة (`color-mix`) مع `backdrop-filter: blur(14px)`، حواف دائرية كاملة (`border-radius: 24px`)، وهامش صغير من الحواف الجانبية للانفصال البصري عن حدود الشاشة، بدل الحد السفلي الصلب البسيط السابق.
2. **الفوتر**: نفس المعاملة — شفافية، Blur، حواف دائرية، مع هامش من الأسفل والجانبين (بدل الالتصاق الكامل بحواف الشاشة).

**⚠️ باگ حقيقي اكتُشف وأُصلح أثناء البناء**: قاعدة CSS إضافية أكثر تحديدًا (`.project-view .composer`) كانت تكتب فوق التصميم الجديد بخلفية صلبة (`var(--mkdd-page)`) بشكل كامل — اكتُشف بالفحص المباشر وأُصلح بإزالة التعارض، بدل الاكتفاء بتعديل القاعدة الأساسية فقط والافتراض أن التغيير سيظهر.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل، شامل فحص شامل لأي قواعد إضافية متعارضة. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إعادة تصميم كاملة، مع اكتشاف وإصلاح تعارض CSS حقيقي أثناء البناء نفسه (لا افتراض أن التعديل الأول كافٍ).

---

### 231. 🐛 إصلاح باگ حقيقي: نافذة بيانات الموظف كانت تختفي بسبب `backdrop-filter` على الهيدر
**السياق:** المالك أبلغ عن مشكلة حقيقية: النقر على صورة الموظف كان يخفي الهيدر تمامًا، ولا يظهر أي محتوى من نافذة بيانات الموظف خالص.

**التشخيص المباشر (تأكيد قاطع، لا افتراض):** فحص مباشر للكود كشف السبب الجذري الدقيق — `backdrop-filter` المُضافة للهيدر في `#230` (لخلق مظهر البطاقة الزجاجية العائمة) **تخلق تلقائيًا Stacking Context جديد** في CSS لأي عنصر عليه. بما إن نافذة البروفايل (`EmployeeInsightsPanel`) كانت `Descendant` مباشر داخل هذا الهيدر، فـ`position: fixed; inset: 0` الخاصة بها (المفروض تغطي الشاشة كاملة) **انحصرت داخل حدود الهيدر الصغير نفسه** بدل الشاشة بأكملها — وهذا بالضبط ما ظهر للمالك كـ"اختفاء الهيدر" (تغطيته بخلفية النافذة الداكنة المحصورة داخله) مع عدم ظهور أي محتوى واضح (بما إن المساحة المتاحة فعليًا كانت صغيرة جدًا).

**الإصلاح:** استخدام `createPortal` من React لتركيب نافذة البروفايل مباشرة في `document.body`، بمعزل تام عن أي Stacking Context للعناصر الأب — الحل التقني القياسي والمعروف لهذه المشكلة بالذات، بدل إزالة الشفافية المطلوبة من الهيدر (اللي كانت ستفقد الميزة اللي طُلبت للتو في `#230`).

**تم التحقق:** فحص مباشر لصحة TypeScript الكامل بعد التعديل، وتشغيل `npm run build` للتأكد من عدم وجود أي كسر. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ إصلاح جذري حقيقي لسبب تقني دقيق ومعروف (تفاعل `backdrop-filter`/`filter`/`transform` مع `position: fixed` للعناصر الأبناء)، لا مجرد تخمين أو حل ترقيعي.

---

### 232. 🎯 بادج الوظيفة يعرض النص كامل + نقل الحالة تحت الصورة
**السياق:** المالك أشار إن بادج الوظيفة كان يقطع النص (بسبب `text-overflow: ellipsis` مع مساحة محدودة نتيجة `justify-content: space-between` مع الحالة)، وطلب نقل الحالة تحت صورة الموظف بدل بجانب الوظيفة.

**التنفيذ:**
1. **الوظيفة تعرض كاملة**: إزالة الحالة من صف الوظيفة بالكامل — الوظيفة أصبحت العنصر الوحيد في سطرها، فلا حاجة لضغط عرضها لصالح عنصر آخر، وأُزيل تقييد `white-space: nowrap` عنها.
2. **الحالة تحت الصورة**: حاوية عمودية جديدة (`chat-employee-avatar-wrap`) تجمع صورة الموظف مع نص الحالة تحتها مباشرة، بدل ظهورها بجانب الوظيفة في البادج المركزي.
3. **إصلاح Selector مرتبط اكتُشف أثناء البناء**: قاعدة الشبكة (Grid) الخاصة بمحاذاة الصورة في الطرف الأخير (`#225`) كانت تستهدف `.chat-employee-avatar` كعنصر ابن مباشر للصف — بعد إضافة الحاوية الجديدة، أصبحت الصورة عنصرًا حفيدًا لا ابنًا مباشرًا، فأُصلح الـSelector ليستهدف الحاوية الجديدة (العنصر الابن المباشر الفعلي الآن) بدلًا من ذلك.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تعديل تصميمي دقيق، مع اكتشاف وإصلاح تأثير جانبي حقيقي على قاعدة Grid موجودة أثناء البناء نفسه.

---

### 233. 🎯 اسم المشروع في شريط التنقل أكبر وفي بادج بمنتصف الشريط
**السياق:** المالك طلب اسم المشروع في صفحة المشروع الرئيسية (شريط التنقل `BreadcrumbBar`) يكون أكبر وفي بادج، في منتصف الشريط.

**التنفيذ:** تحويل تخطيط الشريط من Flex بسيط لـGrid (`auto 1fr`، بنفس نمط هيدر المحادثة `#225`) — العمود الأول لزر الرجوع بحجمه الطبيعي، والعمود الثاني يأخذ كل المساحة المتبقية. اسم المشروع تحول من نص عادي لبادج حقيقي (خلفية شبه شفافة، حواف دائرية كاملة، حجم خط أكبر 17px→19px)، متمركز داخل العمود الجديد.

**تم التحقق:** فحص مباشر لصحة TypeScript وتوازن CSS الكامل — سليم تمامًا. `npm test` → 82/82، `tsc -b` نظيف، `npm run build` ناجح.

**الحالة:** ✅ تعديل تصميمي دقيق ومحدود، مبني على نفس نمط التصميم المُثبَت مسبقًا في هيدر المحادثة.

---

## قوالب للإضافة المستقبلية

عند إضافة مشكلة جديدة، استخدم هذا القالب:

```markdown
### N. عنوان المشكلة
**الوصف:** ...
**الأثر:** ...
**الحل:** ... (أو "لم يتم حلها بعد")
**الحالة:** ✅ تم الحل / 🔴 مفتوحة
```
