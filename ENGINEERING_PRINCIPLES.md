# ENGINEERING_PRINCIPLES.md

قواعد هندسية ثابتة للمشروع، إضافة إلى (مش بديل عن) قواعد التوثيق في `BUGS_AND_FIXES.md`/`CHANGELOG.md`/`PROJECT_SUMMARY.md`.

---

## 1. كل ميزة أو زر في `mkdd-ui` لازم يكون له أساس حقيقي في `openhands-agent-canvas`

**القاعدة:**

> **OpenHands Agent Canvas هو "العقل" الحقيقي للنظام.** `mkdd-ui` طبقة واجهة/حوكمة فوقه فقط. أي زر أو ميزة أو إجراء يظهر في `mkdd-ui` **يجب** أن يكون مبنيًا على آلية حقيقية موجودة فعليًا وموثّقة/مؤكَّدة في كود أو API الخاص بـ `openhands-agent-canvas` (أو الـ Agent Server وراءه) — **وليس** واجهة مُتخيَّلة أو مُختلَقة تظن إننا "هنعملها بعدين".

**السبب:**

هذا امتداد مباشر لمبدأ `README.md` قسم 47 ("Data Integrity Principles" — الواجهة يجب ألا تختلق بيانات لتبدو مكتملة). نفس المبدأ ينطبق على **الإجراءات (actions)**، مش بس البيانات المعروضة: زر "إنشاء مشروع جديد" لازم يستدعي API حقيقي بيعمل حاجة حقيقية في OpenHands (زي إنشاء workspace فعلي)، مش يظهر Modal جميل ومفيش وراه أي تنفيذ حقيقي.

**كيفية التطبيق العملي (خطوات إلزامية قبل أي تنفيذ لميزة جديدة في mkdd-ui):**

1. **ابحث الأول** في كود `openhands-agent-canvas` (الـ frontend) عن الآلية المكافئة — هل الميزة دي موجودة بالفعل في واجهة Agent Canvas نفسها؟ لو آه، هي الأساس الموثوق.
2. **تأكد من الـ API الفعلي عبر `openapi.json` الحي دائمًا، حتى لو كود الـ frontend واضح.** الكود بيستخدم SDK خارجي (`@openhands/typescript-client`) بيغلّف شكل الـ HTTP body الحقيقي — قراءة استدعاء الدالة في الكود (مثل `addWorkspaces(items)`) لا تكفي لمعرفة شكل الـ JSON الفعلي المُرسَل، لأن الـ SDK ممكن يضيف غلاف (envelope) غير ظاهر في الكود. **مثال حقيقي (مشكلة #23):** الكود أوحى بإرسال array مباشر، لكن الـ API الفعلي احتاج `{"workspaces": [...]}`. لا تخمّن — دائمًا افحص الـ schema الفعلي.
3. **اختبر على staging أولاً** (بيئة معزولة وآمنة) قبل أي تعميم على الإنتاج.
4. **وثّق الاكتشاف** في `BUGS_AND_FIXES.md` بنفس أسلوب توثيق مشاكل #16-#23 (الوصف، الدليل الحقيقي، الحل، الحالة).

**مثال ملموس (رحلة الموظفين، 2026-08-13):** قبل ما نبني `bootstrap-employees.mjs`، فحصنا فعليًا:
- الـ `openapi.json` الحي على الـ VM لمعرفة الـ endpoint الصحيح (`POST /api/agent-profiles/{name}`).
- استجابة حقيقية من الإنتاج (`GET /api/employees`) لمعرفة شكل البيانات.
- رسائل خطأ حقيقية (`422: llm_profile_ref field required`) لتصحيح الافتراضات الخاطئة.

لم نخمّن أي حقل من الصفر — كل قرار بُني على دليل حقيقي من الكود أو الـ API الحي.

---

## قوالب للإضافة المستقبلية

عند إضافة مبدأ هندسي جديد، استخدم هذا القالب:

```markdown
## N. عنوان المبدأ

**القاعدة:** ...
**السبب:** ...
**كيفية التطبيق العملي:** ...
```
