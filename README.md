# MKDD — نظام تشغيل شركة سوفتوير بالذكاء الاصطناعي

> **آخر مراجعة شاملة:** 2026-08-31. هذا الملف مرجع حي — يُحدَّث كل ما يتغير شيء جوهري في المشروع، لا لقطة زمنية ثابتة. للسجل الزمني الكامل لكل تغيير، راجع `CHANGELOG.md`. لأي مشكلة تُكتشف وحلها بالتفصيل، راجع `BUGS_AND_FIXES.md`.

---

## 1. إيه هو MKDD؟

**MKDD** نظام تشغيل لشركة سوفتوير مبنية بالكامل على وكلاء ذكاء اصطناعي، مبني فوق **OpenHands Agent Canvas** (محرك تنفيذ الوكلاء نفسه — بيوفّر المحادثات، تشغيل الأوامر، تعديل الملفات، إلخ).

**الفكرة الأساسية:** بدل محادثة عامة واحدة تسأل فيها الذكاء الاصطناعي "اعمل لي كذا"، MKDD بيقسّم الشغل على **14 موظف افتراضي** بأدوار محددة (Product Manager, Architect, Implementation Engineer, QA...)، كل واحد بهوية ثابتة وتعليمات واضحة، ضمن **سير عمل إجباري بـ4 بوابات** (Requirements → UI/UX → Architecture → Production)، مع قواعد حوكمة صارمة (مراجعات إجبارية، لا نشر بدون موافقة صريحة من المالك).

**المبدأ المعماري الحاكم:**
```
GitHub  = مصدر الحقيقة للكود
OpenHands = محرك التنفيذ (المحادثات، الأدوات، بيئة العمل)
MKDD    = طبقة الهوية، سير العمل، والحوكمة
```

---

## 2. ليه المشروع ده موجود

OpenHands لوحده أداة قوية جدًا، لكنها عامة — "ابدأ محادثة واسأل عن أي حاجة". MKDD بيضيف طبقة أعلى بتحوّلها لتجربة "شركة سوفتوير حقيقية":

- موظفين بهوية ثابتة (مش جلسات مجهولة).
- سير عمل واضح بمراحل وموافقات.
- بوابات إجبارية (مفيش برمجة قبل اعتماد المعمارية، مفيش نشر بدون موافقة الإنتاج).
- مراجعات إجبارية (QA، اختبارات آلية، مراجعة كود، مراجعة أمان) قبل أي اعتماد نهائي.
- محادثة منفصلة لكل موظف في كل مشروع — عزل كامل، مفيش "جروب شات" جماعي.
- لوحة تحكم واحدة (Project Home) تعرض حالة كل شيء — مين شغال، على إيه، إيه اللي خلص، وإيه اللي عالق.

---

## 3. النموذج الأساسي للمنتج

### 3.1 المشروع
```
1 مشروع MKDD  =  1 Workspace في OpenHands  =  1 مستودع Git
```

### 3.2 الموظف
هوية ثابتة (اسم، دور، تعليمات) مربوطة بـ Agent Profile حقيقي في OpenHands. لكل موظف محادثة منفصلة تمامًا لكل مشروع.

### 3.3 التنقل
```
المشاريع → صفحة المشروع الرئيسية → محادثة موظف معين
```

### 3.4 استيراد مشروع خارجي موجود
بجانب "مشروع جديد" (مجلد فارغ)، فيه زرار "استيراد مشروع موجود" — بيستقبل رابط Git حقيقي، وينسخ الريبو بالكامل كمشروع دائم في MKDD (لا فحص لمرة واحدة). مناسب لمشاريع قديمة لم تُبنَ عبر MKDD، تحتاج مراجعة (فيرينا، ميخائيل، فادي) أو تحسينات فعلية لاحقًا (كيرلس، مريم). المراجعون يقيّمون هذه المشاريع مقابل أفضل ممارسات مجالهم الحقيقية، لا مقابل تصميم/معمارية MKDD معتمدة (غير موجودة أصلًا لهذا النوع من المشاريع) — راجع `BUGS_AND_FIXES.md #194`.

---

## 4. الموظفون الـ14

| # | الاسم | الدور | المسؤولية الأساسية |
|---|---|---|---|
| 1 | باجوش (Bagosh) | Product Manager | اكتشاف المنتج، توضيح المتطلبات، PRD، تحديد النطاق، موافقة المالك قبل أي تطوير |
| 2 | مينا (Mina) | Business Analyst | دعم اكتشاف المنتج، توضيح المتطلبات، تحليل سير العمل، محاذاة أصحاب المصلحة |
| 3 | مريم (Mariam) | UI/UX Designer | تصميم تجربة المستخدم، تدفقات التفاعل، تعريف الشاشات، نماذج أولية فعلية |
| 4 | شنودة (Shenouda) | Architect | حدود النظام، استراتيجية نموذج البيانات، الـ APIs/العقود، القرارات التقنية، المخاطر، الأمان |
| 5 | كيرلس (Kirollos) | Implementation Engineer | بناء التطبيق الفعلي الكامل حسب التصميم المعتمد والمعمارية المعتمدة |
| 6 | مارينا (Marina) | Integration Engineer | تنفيذ والتحقق من الـ APIs، الـ webhooks، تكاملات الطرف الثالث، مصادقة التكامل |
| 7 | فادي (Fady) | QA Engineer | قبول مستقل، استكشافي، انحداري، وبصري — التحقق من السلوك مقابل المتطلبات |
| 8 | جورج (George) | Test Automation Engineer | بناء وتشغيل اختبارات آلية (API، تكامل، متصفح/E2E، انحدار) |
| 9 | فيرينا (Verena) | Code Reviewer | مراجعة كود مستقلة — الصحة، القابلية للصيانة، ملاءمة المعمارية |
| 10 | ميخائيل (Mikhail) | Security Reviewer | مراجعة أمان مستقلة — مصادقة، تفويض، تحقق من المدخلات، حقن |
| 11 | أنطونيوس (Antonious) | DevOps / SRE | النشر، البنية التحتية، المراقبة، إعدادات وقت التشغيل، التراجع |
| 12 | نادر (Nader) | Technical Writer | صيانة README، توثيق الإعداد/التطوير، توثيق الـ API، توثيق المعمارية |
| 13 | أبانوب (Abanoub) | Release Manager | التحقق من جاهزية الإصدار، يتطلب نجاح QA/الاختبارات الآلية/مراجعة الكود/الأمان قبل النشر |
| 14 | شيري (Sherry) | Content & Localization Writer | كل نص يشوفه المستخدم النهائي، بأي لغة، توطين حقيقي لا ترجمة حرفية |

كل موظف معرَّف في ملف منفصل تحت `company-agents-definitions/` (هوية، تعليمات، مهارات مرتبطة)، بالإضافة لملف `AGENTS.md` المشترك (قواعد الشركة العامة اللي تنطبق على الجميع).

**منهجية عمل حقيقية لكل دور:** كل موظف من الـ14 عنده الآن قسم "Workflow" مفصَّل خاص بمجاله — خطوات فعلية بالترتيب الصحيح (زي رحلة تصميم UX/UI الحقيقية لمريم من البحث للاتجاه البصري للـPrototype، أو منهجية مراجعة أمان مبنية على النمذجة التهديدية لميخائيل)، مبنية على بحث فعلي في ممارسات الصناعة المعتمدة لكل مجال، لا مجرد "قائمة تسليمات" عامة — راجع `BUGS_AND_FIXES.md #187` حتى `#190`.

**معيار احترافية موحَّد وشامل:** قسم "Deep Professional Seniority Standard" في `AGENTS.md` يُلزِم كل موظف بالعمل بمستوى محترف مخضرم حقيقي في مجاله — يطبّق أفضل الممارسات تلقائيًا دون أن يُطلَب منه، يلاحظ تفاصيل لم تُذكَر صراحةً، ويحكم على جودة شغله بمعيار "هل سيخجل محترف حقيقي من رؤية هذا العمل" (`BUGS_AND_FIXES.md #186`).

---

## 5. سير عمل الشركة (4 بوابات إجبارية)

1. **Requirements** — لا برمجة قبل اعتماد هذه البوابة.
2. **UI/UX**
3. **Architecture** — لا برمجة قبل اعتماد هذه البوابة تحديدًا.
4. **Production** — لا نشر إنتاجي بدون موافقة صريحة من المالك.

### قواعد إجبارية (مش اقتراحات)
- مراجعة QA، مراجعة اختبارات آلية، مراجعة كود، مراجعة أمان — كلها إجبارية قبل اعتماد بوابة Production.
- المراجع لا يصلح ملاحظته بنفسه — نفس المراجع يتحقق من الإصلاح لاحقًا.
- لا دمج (merge) بدون موافقة صريحة من المالك.
- عوائق مفتوحة (blockers) تمنع تقدم البوابة.
- حالة سير العمل محفوظة (persisted) بشكل منفصل تمامًا عن تاريخ محادثات OpenHands — راجع `server/workflow-state.mjs`.
- **حاجز تقني حقيقي، لا سلوكي فقط**: اعتماد أي بوابة يُرفَض برمجيًا لو فيه عوائق مفتوحة أو اكتشافات غير مُتحقَّق منها، ولبوابة الإنتاج تحديدًا لو أي مراجعة إلزامية غير مكتملة. كمان، إغلاق أي مراجعة/عائق/اكتشاف يتطلب حقل دليل حقيقي (لا مجرد اسم موظف) — يُرفَض تقنيًا بدونه (`BUGS_AND_FIXES.md #212`).

---

## 6. نظام التقارير بين الموظفين

أي موظف يقدر يسجّل ملاحظة حقيقية عن شغل موظف آخر (مش بس مراجعو الجودة الأربعة) — عبر استدعاء API حقيقي `/api/workflow/reports`، مربوطة ببوابة سير العمل ذات الصلة.

**تسليم تلقائي (بدون تدخل المالك):** لحظة تسجيل التقرير، الخادم يبني رسالة واضحة تلقائيًا (اسم المُرسِل، العنوان، التفاصيل، أمر رد جاهز بمعرّف التقرير الحقيقي) ويسلّمها فورًا لمحادثة الموظف المستلم — يبدأ الشغل عليها مباشرة، بدون انتظار المالك يوصّلها يدويًا. لما الموظف المستلم يرد، حالة التقرير تتحدّث تلقائيًا. تفاصيل التصميم الكاملة في `BUGS_AND_FIXES.md #154`.

**عرض كبادج قابل للنقر:** رسالة التقرير لا تظهر كفقاعة محادثة عادية داخل المحادثة — تُعرَض كبادج مضغوط ("📋 تقرير من موظف")، والنقر عليه يفتح نافذة منبثقة بمحتوى التقرير الكامل، دون التشويش على تدفق المحادثة الطبيعي (`BUGS_AND_FIXES.md #197`).

---

## 7. المحادثة الحية والوقت الحقيقي

الدردشة تعتمد على مزيج REST + WebSocket:
- تحميل أولي عبر REST.
- أحداث حية عبر WebSocket (`server/lib/ws-bridge.mjs`) مع إعادة اتصال تلقائية عند الانقطاع.
- رسالة تفاؤلية فورية عند الإرسال (تظهر قبل تأكيد الخادم).
- دمج الأحداث بمعرّف فريد (لا استبدال كامل للتاريخ عند كل تحديث).

**مبدأ الاستقرار الأساسي:** بيانات مؤكَّدة لا تختفي أبدًا بسبب فشل مؤقت في طلب تحديث — الواجهة تحتفظ بآخر بيانات صحيحة معروفة وتحدّث في الخلفية، ولا تعرض حالة "تحميل" كاملة إلا لو معندهاش أي بيانات مؤكَّدة من الأساس.

**اكتمال حقيقي لتاريخ المحادثة:** فشل صفحة واحدة أثناء تحميل تاريخ طويل لا يُفرِغ كل البيانات الناجحة السابقة (`BUGS_AND_FIXES.md #177`). التحميل الأولي يستمر في الترحيل حتى يضمن عددًا كافيًا من الرسائل الحقيقية، لا مجرد أول صفحة خام (`BUGS_AND_FIXES.md #183`). رسالة التسليم النهائي للموظف (عبر أداة `finish`) تُصنَّف وتُعرَض كرسالة حقيقية في المحادثة، لا نشاط تقني مخفي (`BUGS_AND_FIXES.md #182`).

---

## 8. المعاينة الحية للتطبيقات الحقيقية

أي تطبيق حقيقي يبنيه موظف (زي كيرلس) بيحصل على **بورت مخصص دائم** من نطاق `4001-4020` (راجع `server/lib/live-port-registry.mjs`)، بتمرير خام 100% بدون أي تعديل محتوى أو إعادة كتابة مسارات — فئة كاملة من مشاكل "المسار المطلق اللي بيفلت من إعادة الكتابة" أصبحت مستحيلة بنيويًا بهذا التصميم.

---

## 8.5. معلومات مستودع Git في صفحة المشروع

صفحة المشروع الرئيسية تعرض اسم مستودع Git المرتبط، حالته (نظيف/فيه تعديلات معلَّقة)، وآخر 10 Commits — كل ذلك مبني على أساس حقيقي 100% من OpenHands ومن الملفات المحلية مباشرة، **بدون** أي استدعاء لـGitHub API خارجي (حالة Pull Requests الحية ميزة محجوزة لـOpenHands Cloud المدفوع، غير متاحة في النسخة مفتوحة المصدر التي يعمل عليها هذا النشر — تفاصيل الاستنتاج الكاملة في `BUGS_AND_FIXES.md #166`).

---

## 9. نظام المراقبة الاستباقي وصحة النظام

بعد جلسة تطوير طويلة اكتُشفت فيها مشاكل استقرار حقيقية (انهيار السيرفر بالكامل، مشكلة ملكية Git تكسر كل محادثة جديدة، انقطاعات اشتراك LLM) كان لازم اكتشافها يدويًا كل مرة، بُني نظام مراقبة كامل يكتشف هذه الفئة من المشاكل **تلقائيًا وقبل** ما تؤثر على المالك.

### الفحوصات السبعة (`deploy/health-check.sh`)
1. حالة الحاويات (`mkdd-ui` و`agent-canvas` فعليًا شغالين).
2. استجابة `/api/health` بدون مصادقة.
3. استجابة `/ready` بتاعة OpenHands نفسها.
4. سلامة ملكية Git داخل `agent-canvas` (استثناء `safe.directory`).
5. جدولة نظام النشر التلقائي نفسه (`mkdd-auto-deploy.timer` عنده موعد تشغيل حقيقي قادم).
6. فحص عميق اختياري: فتح محادثة حقيقية لموظف حقيقي (يكتشف تعليق الخادم/WebSocket).
7. صحة اشتراكات LLM — **عام تمامًا، بدون تثبيت أي اسم مزوّد أو موديل في الكود** — يكتشف الملفات الشخصية المُسجَّلة فعليًا وقت التشغيل، ويفحص فقط ما هو قائم على اشتراك.

### الآلية
```
كل 5 دقائق (mkdd-health-check.timer)
    ↓
health-check.sh يشغّل الفحوصات السبعة
    ↓
يكتب النتيجة لملف حالة مشترك (mkdd-data/health-status.json)
يسجّل أي "تحوّل" حالة (سليم↔فاشل) في سجل تاريخي محدود الحجم (health-history.jsonl)
    ↓
عند أي فشل → تنبيه فوري (Push Notification) للمالك عبر /api/internal/alert
```

**الشاشة:** من سايدبار MKDD → "صحة النظام" — تبويب "الحالة الحية" (نتيجة كل فحص الآن) وتبويب "السجل" (تاريخ كل مرة اتغيّرت فيها حالة أي فحص).

**تفعيل الفحص العميق (اختياري، موصى به):** إضافة `MKDD_HEALTH_CHECK_PROJECT`, `MKDD_HEALTH_CHECK_EMPLOYEE_ID`, `MKDD_HEALTH_CHECK_EMPLOYEE_NAME` في ملف `.env` الفعلي على الخادم (بمشروع/موظف حقيقيين موجودين فعلاً).

تفاصيل التصميم والاختبار الكاملة في `BUGS_AND_FIXES.md #157` حتى `#163`.

**الاستئناف التلقائي:** بالإضافة للمراقبة، النظام يتخذ إجراءً فعليًا عند اكتشاف محادثة توقفت بسبب حد استخدام حقيقي انتهى وقته — يرسل رسالة استئناف تلقائيًا (تعتمد على معيار استمرارية العمل الموثَّق مسبقًا لدى كل موظف)، مع تنبيه فوري للمالك. يغطي كل الموظفين، بدون حد زمني على "آخر نشاط" (بما أن حد الاستخدام قد يستغرق أيامًا لإعادة التعيين). رسالة الاستئناف نفسها مخفية عن المحادثة الرئيسية (لا تظهر كرسالة عادية)، وتظهر بدلًا من ذلك في تاب "الاستئناف التلقائي" داخل نافذة بيانات الموظف. تفاصيل التصميم الكاملة في `BUGS_AND_FIXES.md #175` و`#176`.

**كل رسائل الفحوصات بعربي واضح ومفهوم تمامًا** — لا مصطلحات تقنية إنجليزية، شامل عداد تنازلي حي (لا نص ثابت يبقى قديمًا فورًا) يعرض بدقة الوقت المتبقي حتى إعادة تعيين حد الاستخدام، مبني على الطابع الزمني الحقيقي من مزوّد الخدمة نفسه (`BUGS_AND_FIXES.md #170` حتى `#172`).

**مراقبة شاملة لكل الموظفين، لا موظف واحد مُهيَّأ فقط:** بالإضافة للفحص العميق التمثيلي، الشاشة تعرض تلقائيًا **أي** موظف متوقف حاليًا بسبب حد استخدام حقيقي — فحص ديناميكي منفصل لكل موظف يُكتشَف، مع رسالة واضحة وعداد تنازلي حي لكل واحد (`BUGS_AND_FIXES.md #196`).

---

## 10. النشر التلقائي (Auto-Deploy)

أي commit يوصل لفرع `main` على GitHub، بيتنزل ويتبني ويتشغّل تلقائيًا خلال ثوانٍ، مع rollback تلقائي عند الفشل — راجع `deploy/README.md` للتفاصيل الكاملة (المعمارية، التركيب، المراقبة).

**عزل كامل عن الإنتاج:** بيئة الـstaging (اللي عليها النشر التلقائي شغال) معزولة تمامًا عن أي حاوية إنتاجية شغالة — بورتات، أسماء حاويات، أسماء صور، وVolumes منفصلة تمامًا. لا تعارض ممكن يحصل حتى لو الاثنين شغالين في نفس اللحظة على نفس الخادم.

---

## 11. الأمان — المبادئ الحاكمة

- **مفتاح جلسة OpenHands لا يصل للمتصفح أبدًا** — يبقى Server-side فقط.
- **مفتاح خدمة داخلي مشترك** (`X-Internal-Service-Key`) لتفويض استدعاءات موثوقة بدون جلسة متصفح (موظف بيسجّل تقرير عبر `curl`، أو سكريبت المراقبة الآلي) — نطاق ضيّق ومحدود عمدًا لمسارات `/api/workflow/*` و`/api/internal/*` فقط، لا يمنح وصولًا لأي endpoint آخر مهما كان.
- **إعدادات حساسة (إعادة تشغيل الحاوية، إلخ) مقيَّدة على الوصول المحلي فقط** — فحص حقيقي على مستوى الخادم (`Host` header)، لا مجرد إخفاء زر في الواجهة.
- **لا رسائل مباشرة بين الموظفين** — موظف لا يقدر يبدأ أو يكتب في محادثة موظف آخر مباشرة؛ فقط MKDD نفسها (عبر نظام التقارير) تملك هذه القدرة.
- **بيانات هوية العميل (`employeeId`/`employeeName`) غير موثَّقة بمصادقة كاملة بعد** — دَين تقني معروف، محتاج طبقة هوية مستخدم حقيقية لاحقًا (راجع القسم 13).
- **تطبيع آمن للأحداث** — الواجهة تعرض نشاطًا تشغيليًا آمنًا (اسم الأداة، ملخص الإجراء، حالة الخطأ) ولا تُسرِّب سلسلة التفكير الداخلية للنموذج أبدًا.

---

## 12. مبادئ سلامة البيانات

- **لا بيانات مُختلَقة أبدًا** — خطة العمل (Work Plan) مُستخرَجة من أحداث `task_tracker` الحقيقية، والتكلفة من إحصائيات المحادثة الفعلية. لو المقياس غير متاح، الواجهة تعرض "غير متاح"، لا رقمًا مُختلَقًا.
- **لا ادعاء إنجاز عمل ماضٍ لم يحدث** — أي موظف يُسأل عن عمل سابق (بحث، تسليم، قرار) يجب أن يتحقق من سجله الفعلي وملفات المشروع أولًا؛ لو لم يظهر أي دليل، يُعلَن بصراحة عدم وجود سجل، لا وصفه كأنه حدث فعليًا (`AGENTS.md` §13، مبني على حادثة حقيقية موثَّقة في `BUGS_AND_FIXES.md #192`).
- **GitHub مصدر الحقيقة للكود** دائمًا.
- **حالة سير العمل تحت سيطرة MKDD الصريحة**، منفصلة عن تاريخ محادثات OpenHands.

---

## 13. الحالة الحالية — إيه اللي لسه ناقص

- **مصادقة مستخدم حقيقية متعددة المستخدمين** — لسه غير موجودة؛ حساب مالك واحد فقط حاليًا.
- **آلية اكتشاف مفتاح الجلسة عبر `/proc`** — دَين تقني معروف، يحتاج آلية أسرار مدعومة رسميًا بدلًا منه.
- **نسخ احتياطي شامل** — `backup-mkdd-data.sh` تحفظ بيانات MKDD نفسها، لكن تاريخ محادثات الموظفين (على Volume خاص بـOpenHands) ليس جزءًا من النسخ الاحتياطي حاليًا.
- **PWA، تغطية اختبارات أشمل** — مؤجلة لحد ما الأساسيات تستقر بالكامل.
- **CI**: مُنفَّذ لكل مشروع جديد — قالب `GitHub Actions` عام تمامًا (بلا أي اسم ريبو/مستخدم ثابت) يُضاف تلقائيًا عند إنشاء الريبو، يكتشف ديناميكيًا سكريبتات build/test/lint الموجودة فعليًا في `package.json`. **CD حقيقي (نشر تلقائي بعد بوابة الإنتاج) لسه غير موجود.**

---

## 14. أول خطوة لأي حد جديد

```bash
git clone https://github.com/menokemo/openhands-mkdd.git
cd openhands-mkdd
./setup.sh
```

هذا يبني ويشغّل كل شيء (Agent Canvas + MKDD UI) بالإعدادات الافتراضية:
- MKDD UI: `http://localhost:8787`
- Agent Canvas: `http://localhost:8000` / `http://localhost:3000`

للتطوير على الـ backend فقط:
```bash
cd mkdd-ui
npm ci
npm test       # الاختبارات الآلية
npm run build  # التحقق من صحة TypeScript والبناء
```

**لتركيب نظام النشر التلقائي + المراقبة على خادم جديد (خطوة واحدة، مرة واحدة):** راجع `deploy/README.md`.

---

## 15. خريطة الملفات

```
openhands-mkdd/
├── setup.sh                        ← يشغّل كل حاجة من الصفر (docker compose build + up)
├── compose.yml                     ← تعريف الخدمات (agent-canvas + mkdd-ui)، قابل للتهيئة بالكامل
├── deploy/                         ← نظام النشر التلقائي + المراقبة الاستباقية
│   ├── auto-deploy.sh                 نشر تلقائي من GitHub كل 20 ثانية
│   ├── health-check.sh                7 فحوصات صحة، يدوي أو دوري (راجع القسم 9)
│   ├── install.sh                     تركيب لمرة واحدة على خادم جديد
│   ├── mkdd-auto-deploy.timer/.service
│   ├── mkdd-health-check.timer/.service
│   └── .env.staging                   بورتات/أسماء معزولة تمامًا عن الإنتاج
│
├── mkdd-ui/                         ← منتج MKDD نفسه (الواجهة + الـ backend)
│   ├── src/                            React frontend
│   └── server/
│       ├── index.mjs                   نقطة الدخول (رفيعة، تربط الـ routes فقط)
│       ├── lib/                        منطق مشترك:
│       │   auth-store, authorize-conversation, auto-resume-log,
│       │   auto-resume-marker, employee-display-info,
│       │   internal-service-key, live-port-registry, normalize-conversation,
│       │   normalize-event, openhands-client, owner-avatar-store,
│       │   project-metadata, push-notifications, time-context, work-plan,
│       │   ws-bridge
│       └── routes/                     كل route في ملفه الخاص:
│           auth, auto-resume (استئناف تلقائي للمحادثات المتوقفة), avatars,
│           branding, chat, conversation, directory,
│           internal-health (صحة النظام + LLM + تنبيهات), owner-avatar,
│           preview, project-cost, project-files, project-live-port,
│           project-upload, projects (إنشاء + استيراد مشروع خارجي), push,
│           settings, workflow
│
├── openhands-agent-canvas/         ← نسخة معدَّلة من OpenHands (fork، v1.16.0)
├── company-agents-definitions/     ← تعريف كل موظف + AGENTS.md (قواعد الشركة)
├── company-skills/                 ← مهارات كل دور (SKILL.md لكل موظف)
├── mkdd-data/branding/              ← أصول العلامة التجارية
│
├── README.md                        ← هذا الملف — المرجع الشامل الحي
├── PROJECT_SUMMARY.md               ← مرجع سريع + سجل تاريخي للتطور
├── BUGS_AND_FIXES.md                ← كل مشكلة اكتُشفت وحلها بالتفصيل الكامل
├── CHANGELOG.md                     ← سجل زمني مختصر لكل تغيير
└── ENGINEERING_PRINCIPLES.md        ← مبادئ هندسية ثابتة (مثال: أساس حقيقي إلزامي لكل ميزة، لا افتراض)
```

---

## 16. أين أدوّر على تفاصيل أعمق؟

- **"ليه اتصلحت مشكلة معينة، وإزاي؟"** → `BUGS_AND_FIXES.md` (كل بند مرقَّم، مُفصَّل، مع طريقة التحقق الفعلية).
- **"إيه اللي اتغيّر يوم كذا؟"** → `CHANGELOG.md`.
- **"إزاي أنصّب النظام على خادم جديد بالكامل؟"** → `deploy/README.md`.
- **"إيه المبادئ الهندسية الثابتة اللي كل ميزة جديدة لازم تلتزم بيها؟"** → `ENGINEERING_PRINCIPLES.md`.
- **"مرجع سريع لأي حد جديد + سجل تاريخي للتطور؟"** → `PROJECT_SUMMARY.md`.
