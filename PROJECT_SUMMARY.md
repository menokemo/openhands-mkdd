# PROJECT_SUMMARY.md

مرجع سريع لأي حد جديد ينضم للمشروع — إيه اللي إحنا عملناه، إيه اللي شغال، وإيه اللي لسه مطلوب.

> هذا الملف يُحدَّث كل ما يتغير شيء جوهري في المشروع. للتفاصيل الكاملة، راجع `README.md`. لسجل التغييرات الزمني، راجع `CHANGELOG.md`. للمشاكل المعروفة، راجع `BUGS_AND_FIXES.md`. للمبادئ الهندسية الثابتة، راجع `ENGINEERING_PRINCIPLES.md`.

---

## إيه هو المشروع؟

**MKDD** = نظام تشغيل لشركة سوفتوير AI، مبني فوق **OpenHands Agent Canvas** (محرك تنفيذ الوكلاء). بدل محادثة واحدة عامة، MKDD بيقسم الشغل على **14 موظف افتراضي** بأدوار محددة، ضمن **4 بوابات عمل إجبارية** (Requirements → UI/UX → Architecture → Production)، مع قواعد حوكمة صارمة (مراجعات إجبارية، لا merge بدون موافقة، إلخ).

**المبدأ المعماري:** GitHub = مصدر الحقيقة للكود. OpenHands = محرك التنفيذ. MKDD = طبقة الشغل والحوكمة.

---

## الحالة الحالية بالأرقام (2026-08-30)

| العنصر | القيمة | الحالة |
|---|---|---|
| **عدد الموظفين** | **14** (شيري، Content & Localization Writer، آخر إضافة) | ✅ مؤكَّد حيًا |
| **إصدار OpenHands Agent Canvas** | **1.16.0** (مصدر) + agent-server **1.42.1** + automation **1.9.0** | ✅ مؤكَّد حيًا على **staging**، مبني بتعديل مخصص مدموج بعناية فوق النسخة الرسمية |
| **نظام المراقبة الاستباقي** | 7 فحوصات آلية (حاويات، صحة الـbackend، Git، جدولة النشر التلقائي، فحص محادثة عميق، صحة اشتراكات LLM)، شاشة سايدبار "صحة النظام" (حالة حية + سجل تاريخي)، تنبيه فوري عند أي فشل | ✅ مبني ومُختبَر حيًا بالكامل، مفعَّل عبر `mkdd-health-check.timer` (كل 5 دقايق) — `BUGS_AND_FIXES.md #157-163` |
| **نظام تسليم التقارير التلقائي** | تقرير يُسجَّل بين موظفين → يوصل تلقائيًا لمحادثة الموظف المستلم فورًا، بدون تدخل يدوي من المالك | ✅ مبني ومُختبَر حيًا — `BUGS_AND_FIXES.md #154` |
| **الإنتاج الحقيقي على الـ VM** | ما زال بمعزل تام عن كل تطوير الـstaging (بورتات/أسماء/صور منفصلة تمامًا) | ⚠️ محتاج ترقية منفصلة لاحقًا لو تقرر |

**⚠️ ملاحظة مهمة لأي حد جديد:** الجزء اللي تحت ده (بتاريخ 2026-08-16) سجل تاريخي قيّم لأول مرحلة من المشروع، لكنه **مش الحالة الحالية**. بين 2026-08-16 و2026-08-30 حصل تطور كبير جدًا — أهمه: ترقية OpenHands لـv1.15.0 ثم v1.16.0، إصلاحات استقرار حرجة متعددة (انهيار كامل للسيرفر بسبب استثناء غير معالَج، مشكلة ملكية Git تكسر كل محادثة جديدة)، بناء نظام مراقبة استباقي كامل من الصفر، تسليم تلقائي للتقارير. **للتفاصيل الكاملة والدقيقة لأي تغيير، `BUGS_AND_FIXES.md` و`CHANGELOG.md` هما المرجع الموثوق دائمًا — لا هذا الملف ولا `README.md`.**

---

## الحالة بالأرقام (سجل تاريخي — 2026-08-16، محفوظ للمرجعية فقط)

| العنصر | القيمة | الحالة |
|---|---|---|
| **عدد الموظفين** | **14** (آخر إضافة: شيري، Content & Localization Writer) | ✅ مؤكَّد حيًا (`listEmployeeNames()` رجّعت 14 بالظبط) |
| **إصدار OpenHands Agent Canvas** | **1.13.0** (المصدر) + agent-server **1.42.1** + automation **1.7.1** | ✅ مؤكَّد حيًا على **staging** (بناء وتشغيل ناجح، محادثات حقيقية شغالة) |
| **المعاينة الحية لتطبيقات حقيقية** | بورت مخصص دائم لكل مشروع (نطاق 4001-4020)، تمرير خام 100% | ✅ مؤكَّد حيًا (اختبار عزل شبكي كامل)، محتاج تأكيد نهائي مع كيرلس |
| **الإنتاج الحقيقي على الـ VM** | **لسه معملوش تحديث خالص** — فضل بمعزل تام طول عملية الترقية والتطوير | ⚠️ لسه على النسخة القديمة عمدًا، محتاج ترقية منفصلة لاحقًا لو تقرر |

**تفصيلة مهمة لأي حد جديد:** كل شغل الترقية والتطوير الأخير (v1.13.0، الموظفة الجديدة، كل الإصلاحات) اتعمل واتأكد منه على بيئة **staging** (`mkdd-live`، بورتات 15173/18787/18000/13000) — **مش الإنتاج الحقيقي**. الإنتاج (`/opt/openhands/`، بورتات 5173/8787/8000/3000) فضل بمعزل تام عمدًا طول الوقت ده، زي ما اتفقنا من البداية.

---

## بنية الريبو

```
openhands-mkdd/
├── setup.sh                     ← يشغّل كل حاجة من الصفر (docker compose build + up)
├── compose.yml                  ← تعريف الخدمات (agent-canvas + mkdd-ui)، قابل للتهيئة بالكامل
├── deploy/                      ← نظام النشر التلقائي (auto-deploy من GitHub كل 20 ثانية)
│   ├── auto-deploy.sh
│   ├── install.sh
│   └── .env.staging             ← بورتات/أسماء معزولة تمامًا عن الإنتاج
├── mkdd-ui/                     ← منتج MKDD نفسه (الواجهة + الـ backend)
│   ├── src/                     ← React frontend
│   └── server/                  ← Node.js backend
│       ├── index.mjs            ← نقطة الدخول (رفيعة، تربط الـ routes)
│       ├── lib/                 ← منطق مشترك (auth, normalize, work-plan, ws-bridge, time-context)
│       ├── routes/              ← كل route في ملفه (branding, workflow, chat...)
│       └── scripts/             ← bootstrap-employees.mjs
├── openhands-agent-canvas/      ← نسخة معدّلة من OpenHands (fork)
├── company-agents-definitions/  ← تعريف كل موظف (اسم، دور، تعليمات) + AGENTS.md (قواعد الشركة)
├── company-skills/              ← مهارات كل دور (SKILL.md لكل موظف)
├── mkdd-data/branding/          ← أصول العلامة التجارية
├── README.md                    ← التوثيق الكامل والمرجعي للمشروع
├── ENGINEERING_PRINCIPLES.md    ← مبادئ هندسية ثابتة (زي: أساس حقيقي إلزامي لكل ميزة)
├── BUGS_AND_FIXES.md            ← كل مشكلة عُرفت وحلها (أو حالتها)
├── CHANGELOG.md                 ← سجل زمني لكل تغيير
├── PROJECT_AUDIT_REPORT.md      ← تقرير فحص شامل بأرقام فعلية
└── REALTIME_CHAT_RESEARCH.md    ← بحث فني لحل الـ realtime chat (منفَّذ بالكامل)
```

---

## إيه اللي شغال دلوقتي (Working)

- الواجهة (Projects → Project Home → Employee Chat) شغالة.
- الـ 14 موظف + الـ conversation isolation (project + employee) شغالين.
- بوابات الـ workflow + الموافقات + الـ blockers/findings شغالة ومحفوظة (persisted).
- استخراج Work Plan الحقيقي من `task_tracker` events (مفيش بيانات مُختلَقة).
- استخراج التكلفة الحقيقية من إحصائيات المحادثة.
- تطبيع آمن للأحداث (Activity) — بدون تسريب chain-of-thought.
- **جديد (2026-08-13):** المشروع أصبح قابلاً للبناء والتشغيل من الصفر على أي جهاز عبر `./setup.sh` — تم التحقق فعليًا ببناء ناجح.
- **جديد (2026-08-13):** `server/` منظّم في `lib/` + `routes/` بدل ملف واحد ضخم، مع 37 اختبار آلي.
- **جديد (2026-08-13):** نظام نشر تلقائي (`deploy/`) — أي commit يوصل لـ `main` على GitHub يتنزل ويتبني ويتشغّل تلقائيًا على الـ VM خلال 20 ثانية، مع rollback تلقائي عند الفشل. **معزول بالكامل عن الإنتاج** (بورتات/أسماء/صورة/volume منفصلة تمامًا). التركيب لمرة واحدة عبر `deploy/install.sh`.
- **جديد (2026-08-13):** نظام تفعيل الموظفين (`bootstrap-employees.mjs`) — تم اكتشاف إن الموظفين (Agent Profiles) بيانات runtime مش مولّدة من الكود، وتم بناء سكريبت يُنشئهم من ملفات `company-agents-definitions/` مع تعليماتهم الكاملة، مربوطين بـ LLM profile يحدده المستخدم. **تم التأكيد الفعلي: 13/13 موظف اتعملوا بنجاح على الـ staging.**
- **جديد (2026-08-13):** الـ realtime chat بقى شغال عبر WebSocket حقيقي (بدل الاعتماد الكلي على polling)، مع reconnect تلقائي ورسالة تفاؤلية فورية عند الإرسال. **تم التأكيد الفعلي: محادثة حية على staging، الردود بتوصل لحظيًا بدون refresh.** التفاصيل في `REALTIME_CHAT_RESEARCH.md`.
- **جديد (2026-08-13):** زر "+ مشروع جديد" — ينشئ مجلد مشروع فعلي ويسجّله كـ Workspace حقيقي عند OpenHands، مبني على الآلية الحقيقية المؤكَّدة من كود Agent Canvas نفسه (طبقًا لـ `ENGINEERING_PRINCIPLES.md` #1).
- **جديد (2026-08-13):** عنوان تلقائي لكل محادثة جديدة (اسم الموظف + اسم المشروع)، ظاهر في واجهة Agent Canvas نفسها كمان.
- **جديد (2026-08-13):** الموظفين بقى عندهم وعي بالوقت الحقيقي (مع تحويل صحيح للمنطقة الزمنية المحلية)، عبر حقن وقت مخفي في كل رسالة — تفاصيل كاملة في `BUGS_AND_FIXES.md` #25.
- **جديد (2026-08-13):** إعادة تصميم بصري لشاشات المشاريع/المشروع/المحادثة بناءً على مرجع تصميم من المستخدم — هيدر علامة تجارية موحّد، شريط أفاتار الموظفين، تحسينات للمحادثة.
- **جديد (2026-08-13):** رفع صورة حقيقية لأي موظف (كان مسار وهمي من قبل — تم اكتشاف وإصلاح الباگ)، ولون غلاف اختياري لكل مشروع (تخزين مخصص لـ MKDD بما إن OpenHands مفهوش هذا المفهوم).
- **جديد (2026-08-13):** إصلاح هيدر صفحة المشروع والـ composer — كانا مكتوبين `sticky` لكن قواعد CSS متضاربة كانت بتلغيهم فعليًا؛ تم التحويل لـ `fixed` مع تعويض المساحة صح.
- **جديد (2026-08-13):** كروت الموظفين بقت صف أفقي قابل للتمرير (بدل عمودي)، وصورة الموظف بقى ليها بصمة إصدار تمنع مشاكل كاش المتصفح، ورفع الصورة اتبنى في مكوّن منفصل بمؤشرات تحميل/خطأ واضحة على الشاشة.
- **جديد (2026-08-13):** هيدر موحّد (`AppHeader.tsx`) واحد بس لكل التطبيق (خط Bodoni Moda)، Sidebar بتصنيف المشاريع (نشطة/قربت تخلص/خلصت — بيانات حقيقية من الـ workflow) وقائمة الموظفين، بروفايل موظف مع تغيير صورة، خط Cormorant Garamond لباقي التطبيق. خريطة كاملة للملفات موثقة في `README.md` قسم 59.
- **جديد (2026-08-13):** نظام الألوان الرسمي (`design/MKDD_Color_System_Light_Dark_AR_RTL.docx`) اتبنى بالكامل — 20 توكن دقيق بقيم HEX رسمية لكل وضع، بعد ما اكتشفنا الوضع الفاتح كان متكسر فعليًا (4 قيم لون ثابتة نسيناها جوه gradients). شريط الفريق اتصغّر بشكل كبير برضو حسب طلب صريح.
- **جديد (2026-08-15):** موظفة 14 — شيري (Content & Localization Writer)، مسؤولة عن كل نص يشوفه المستخدم النهائي، بأي لغة، مع التوطين الحقيقي مش الترجمة الحرفية. بتشتغل بالتوازي مع مريم من بداية مرحلة UI/UX عشان البروتايبات تطلع بنصوص حقيقية من الأول. اتضافت لجدول توجيه الأوركستريتور وقائمة الهوية المشتركة في كل الملفات الـ14.
- **جديد (2026-08-14):** ترقية OpenHands Agent Canvas لإصدار 1.13.0 (agent-server 1.42.1، automation 1.7.1). تحقّق كامل قبل التنفيذ (استنساخ النسخة الحقيقية، تطبيق تعديلنا المُخصَّص عليها بنجاح 100%، تأكيد التوافق)، ثم تنصيب حي كامل ناجح على staging. تفاصيل كاملة في `BUGS_AND_FIXES.md` #37.
- **حرج، تم إصلاحه (2026-08-15):** بعد الترقية، اكتُشف إن أي محادثة **جديدة** لأي موظف (مش القديمة الموجودة بالفعل) كانت بتفشل بصمت تمامًا — الـ agent-server الجديد بقى يرفض اسم الموظف النصي كـ `agent_profile_id` ويطلب الـ UUID الحقيقي بتاعه. تم التشخيص بالكامل (تنبيهات تشخيصية مؤقتة، اختبار `curl` مباشر) والإصلاح (`resolveAgentProfileUuid()` في `chat.mjs`) والتحقق الحي (سيرفر OpenHands وهمي بمحاكاة دقيقة). تفاصيل كاملة في `BUGS_AND_FIXES.md` #48.
- **إعادة تصميم كاملة (2026-08-16):** المعاينة الحية لتطبيقات حقيقية (زي شغل كيرلس) — بعد سلسلة إصلاحات متتالية كانت بتكتشف مسار جديد كل مرة، تم التخلي الكامل عن نموذج "بورت مشترك + مسار فرعي + إعادة كتابة" واستبداله بـ **بورت مخصص دائم لكل مشروع (نطاق 4001-4020) + تمرير خام 100% بدون أي تعديل محتوى خالص** — الفئة الكاملة من مشاكل "المسار المطلق اللي بيفلت" بقت مستحيلة بنيويًا. تفاصيل كاملة (شاملة باگين حقيقيين اتلقوا وأتصلحوا أثناء التنفيذ، منهم فجوة في `npm test` نفسه) في `BUGS_AND_FIXES.md` #56.
- **مراجعة شاملة (2026-08-16):** كل ملفات الموظفين الـ14 — لقينا إن قاعدة "تحقق قبل الادعاء" في `AGENTS.md` كانت محصورة في التوقيعات الرسمية والفحص البصري بس، مش أي ادعاء عادي أثناء الشغل اليومي (زي "السيرفر شغال" — بالظبط اللي حصل مع كيرلس). أضفنا قاعدة عامة شاملة (§8) بتنطبق على الجميع تلقائيًا، وقسم "Deliverables" واضح كان ناقص في 10 من 14 موظف. تفاصيل كاملة في `BUGS_AND_FIXES.md` #57.

## إيه اللي لسه ناقص (Not Complete)

- **اختفاء بيانات الموظفين أحيانًا:** مشكلة معروفة، تفاصيل التحقيق في `BUGS_AND_FIXES.md` #4.
- **آلية اكتشاف مفتاح الجلسة عبر `/proc`:** دين تقني (technical debt)، يحتاج آلية secret مدعومة رسميًا.
- **Authentication حقيقي متعدد المستخدمين:** لسه غير موجود؛ `employeeId`/`employeeName` قادمين من العميل (client) بدون تحقق هوية كامل.
- **PWA، CI/CD، تغطية اختبارات أشمل:** لسه مؤجلة لحد ما الأساسيات تستقر بالكامل.

---

## أول خطوة لأي حد جديد

```bash
git clone https://github.com/menokemo/openhands-mkdd.git
cd openhands-mkdd
./setup.sh
```

هذا يبني ويشغّل كل شيء (Agent Canvas + MKDD UI) بالإعدادات الافتراضية المؤكدة عمليًا. بعدها:
- MKDD UI: `http://localhost:5173`
- Agent Canvas: `http://localhost:3000`

للتطوير على الـ backend فقط:
```bash
cd mkdd-ui
npm ci
npm test      # يشغّل الاختبارات الآلية
npm run build # يتأكد من صحة الـ TypeScript والبناء
```

---

## أولويات التطوير القادمة (بالترتيب)

مأخوذة من `README.md` (قسم 52) و`PROJECT_AUDIT_REPORT.md`:

1. **P0:** مراقبة استقرار اختفاء بيانات الموظفين بعد التحول للـ WebSocket (تحسّن ملحوظ لكن غير مؤكَّد رسميًا كسبب جذري — `BUGS_AND_FIXES.md` #4)، حل تحديث Work Plan اللحظي (لسه بيعتمد على resync كل 15 ثانية).
2. **P1:** إضافة هوية مستخدم حقيقية (authentication)، فرض الموافقات من طرف الـ backend حصريًا.
3. **P2:** آلية أسرار مستقرة (بدل `/proc`)، CI/CD، اختبارات أشمل.
4. **P3:** تحسينات UX نهائية.
5. **P4:** PWA (بعد استقرار كل ما سبق).
